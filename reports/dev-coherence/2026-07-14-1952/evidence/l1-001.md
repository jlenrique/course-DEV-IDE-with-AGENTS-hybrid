# L1-001 — inherited motion sequence-document parity mismatch

- Check: `workflow-stage-lockstep`
- Classification: alteration / high
- Structural-walk result: `motion = NEEDS REMEDIATION`
- Current-session causality: inherited; the workbook Epics 36–40 session did not edit the motion prompt pack or motion structural-walk manifest.

The motion dry run reports that the creative-directive resolution markers in
`docs/workflow/production-prompt-pack-v4.2-narrated-lesson-with-video-or-animation.md`
are out of the order required by `state/config/structural-walk/motion.yaml`. The
same dry run also reports a sequence-document parity block for the `motion-gate`
stage marker (`## 7F) Motion Gate`).

This finding is carried into the session handoff. It does not alter the immediate
workbook live-run configuration because motion remains OFF, but L1 is not clean
and the agentic L2 sweep therefore did not run.

The generated structural-walk result was summarized in
`../harmonization-summary.md`; the machine-readable finding is retained in
`../trace-report.yaml`.
