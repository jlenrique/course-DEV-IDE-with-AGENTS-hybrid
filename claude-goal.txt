GOAL (v7, 2026-06-22) — Build the P2-4c reading-path tuple classifier as far forward as reliable, via the NEW CYCLE Codex dev agent. BMAD-governed; party-mode gates.

DEV MODEL (CHANGED — Codex IS used this session): NEW CYCLE per CLAUDE.md + memory feedback_new_cycle_codex_dev_handoff. Per story: Claude pre-authors/refreshes the spec + a codex-dev-prompt-{key}.md → STOP at the Codex-ingestion boundary → operator dispatches Codex (T1–T10 + self-review) → Codex returns a handoff → Claude T11: reproduce the full battery + 3-layer bmad-code-review + fully-spawned party-mode CLOSE + commit + flip done + push. NOT Claude-direct dev edits.

CONTEXT: Branch fidelity-perception-arc-2026-06-19, HEAD e488322. Catalog is v1.1 (party+operator-ratified D1–D3); held-out round PASSED (A6 primary-key top-1 13/14=0.93). Spec spec-p2-4c-reading-path-tuple-refactor.md ready-for-dev (§0 v1.1 amendments, §4 S1 ACs, §5 S2/S3, §6 gaps G1–G3, §7 conformance, §9 build contract). P2-4a (dp-v1.5) is GREEN and stays green (additive). Open as Class S — run the Step-1a Cora gate at open.

ARC (stop wherever reliability runs out; partial with a clean handoff beats a rushed gate):
1. S1 — deterministic geometry + ADDITIVE tuple schema. D1+D3 as ONE derivation re-baseline: enum stays primary-name + optional tuple sibling fields (incl. nullable callout_intent stub); dp-v1.5→v1.6 minor; pinned derivation + shape-pin test; tightened _looks_z; default-degradation counter; multi_column un-quarantined (N≥2, now N≥4); 6 RED-first fixtures. Author Codex prompt → Codex T1–T10 → Claude T11 → done. Full lockstep regen IN-CYCLE (4-file reading-path set + manifest dp-v1.6 + -gen witness + Check-9 SHA).
2. IF reliable: party gate to resolve G2 (image-role tier rubric) + G3 (escalation predicate) → S2 (per-element image-role tier, incl. tier-2.5 evidentiary provisional) → Codex → T11 → done.
3. IF reliable: S3 (live gpt-5.5 escalation, ≥gpt-5.5 floor, parse-seam: oppositional refinement [G1 cue] + callout_intent label + process-confirm) → Codex → T11 → done.
4. IF reliable: finalize P2-4b — score the BUILT classifier's tuples vs the operator-confirmed held-out labels (primary-key top-1 ≥0.85 + per-axis vector; callout_intent EXCLUDED pending its harvest).

HARD FENCES:
- Codex does dev (T1–T10); Claude does T11; never skip bmad-code-review before flipping done.
- No mocks — real live gpt-5.5+ on S3; determinism via recorded-real parse-seam only, never a production-path fixture.
- Additive non-regression: P2-4a stays GREEN; no enum value removed; JSON-schema enum widened not narrowed.
- Never leave the lockstep/manifest half-modified (block-mode-dirty): if a regen can't complete reliably, STOP at the pre-regen boundary with a clean handoff.
- callout_intent stays OUT of the primary-key + full-tuple top-1 (probationary per-axis vector); enum provisional (3 seeded values) pending harvest.
- Tooling free (.venv, ruff, pytest, lint-imports, git). Push at safety checkpoints + completion. No force-push, no --no-verify. Quinn→John on impasse before escalating.

WRAP-UP (mandatory at stop): update SESSION-HANDOFF + next-session-start-here + deferred-inventory (+ project-context if rules/substrate change), resume banner, commit + push.

FURTHEST RELIABLE POINT = last BMAD gate cleanly passed (green battery + bmad-code-review + party CLOSE + committed + pushed + lockstep-coherent). Terminal states best→floor: (a) S1+S2+S3 done + P2-4b finalized; (b) S1+S2 done; (c) S1 done; (d) S1 Codex prompt at the ingestion boundary (clean pre-dispatch handoff).

STOP & ASK ONLY ON: true impasse after Quinn→John; a governance change beyond dp-v1.6 / D1–D3; or needing the callout_intent harvest labels.
