GOAL (enhanced-vo, 2026-06-29) - Enhanced VO arc: make the directed voice REAL (v3 text-driven). Branch dev/p5-downstream-consumption-2026-06-26. Class S. Epic key: enhanced-vo. Party CONSENSUS 2026-06-29 (Dr. Quinn).

WHY: directed-voice fires inert or inaudible on real decks; 27 eleven_v3 clips proved expressive control is TEXT-driven (tags + phrasing), not v2 numerics.

STORY A - enhanced-vo-1-role-slide-linkage (ZERO live spend; GATES B). Emit a stable slide_key as DATA on the manifest at Irene's clustering output. Consumers READ it from the shared contract and never import clustering code (M3 fence). Build a deterministic source-to-final slide-map fixture on a REAL clustered Gary deck. Identity join via slide_key: RED-first, build-breaking, no fuzzy-ordinal fallback. Stability: same source slide -> same key across two clustering runs.

STORY B - enhanced-vo-2-v3-provider-text-compiler (after A green). Reuse scripts/api_clients/elevenlabs_client.py::text_to_speech_with_request_id for the ElevenLabs call (v3 model_id, tags-via-text, seed, request IDs, normalization). Do not reinvent the provider call. Four sha256 text channels: canonical, provider, display, captions. Add a TAG-ONLY pure compiler in app/specialists/_shared/: strip_tags(provider) == canonical byte-exact, asserted, and keyed to a closed enumerated eleven_v3 tag allowlist. If the allowlist is not enumerable, hand back. Add render_strategy:v3 plus rhetorical_role additively; no version bump; v2 mapper stays frozen and byte-identical off. Captions must have zero tag leak, with a hard assert and cross-channel tag-bleed mutation test. Re-key skip-if-exists on provider sha. Keep the fidelity firewall: Irene-authored wording must remain source-contained vs original narration, with zero tolerance for changed numerals, clinical terms, negations, or comparators. Storyboard B must show literal provider bytes and captions before any live spend. Initial roles: warm_callback (structural; A/B rhetoric-vs-tag) and contrast_emphasis (tonal; tag-channel probe). Voice: Sarah. Seed is test hygiene.

SUCCESS BAR (pre-registered, both outcomes valued): blind A/B on the Descript final video, tonal-role slide, identical words in both arms (A=no tag, B=[tag]); operator judge; first-run-stands; second-deck cross-confirm. "Indistinguishable" means tag is plumbing-only, which is a fundable finding that redirects to rhetoric+voice. It is not a failure. No numeric expressiveness metric. Only offline gates and live no-leak proxy can fail; the A/B is a finding, not a gate.

HOW - BMAD spine per story: party GREEN-LIGHT -> bmad-create-story (+validate) -> optional testarch-atdd -> bmad-dev-story (RED-first) -> bmad-code-review -> party CLOSE. Retrospective at arc close. Author Story B only after Story A is green. Update sprint-status first, next-session-start-here second. Push at least every 2 hours.

PARTY MODE (binding): core = canonical agents for the workflow: John/PM, Winston/architect, Amelia/dev, Murat/test. Then add no more than two specialty agents, such as bmad-agent-marcus planning persona (not the langGraph Marcus-SPOC), Vera, or CD-Dan. Dr. Quinn synthesizes. John/PM makes final calls only at impasse (Quinn -> John -> human).

LIVE-TEST-AS-YOU-GO: live-test each component as it lands. The Descript E2E is confirmation, not first contact. No mocks for live claims: use real eleven_v3 and real Descript where the story calls for live proof. Run live foreground with hard timeout and flushed logging. Reuse captured audition assets where possible; do not re-spend unnecessarily. Use PYTHONIOENCODING=utf-8 and .venv/Scripts/python.exe.

GOTCHAS: Enrique reads Irene deltas from cache_prefix, not contributions[]; name the analogous slide_key read site. eleven_v3 has an observed around-5000-character practical max. Voice availability is workspace/API-key scoped. For block_mode_trigger_paths, read the regime doc at T1 if pipeline-manifest.yaml or node/edge definitions are touched. Commit the working-tree four-voice slate (Sarah default) with this arc.

SSOT: _bmad-output/implementation-artifacts/claude-code-brief-enhanced-vo-v3-generation-2026-06-29.md; _bmad-output/planning-artifacts/enhanced-vo-party-consensus-2026-06-29.md; _bmad-output/implementation-artifacts/enhanced-vo-1-role-slide-linkage.md; _bmad-output/planning-artifacts/p5-directed-voice-arc-strawman-2026-06-27.md section S. Branch consolidation is owed before the next arc.

DONE = both stories party-CLOSED, live-proven incrementally, isolated-tag A/B finding recorded either way, sprint-status and handoff updated, four-voice slate committed, pushed.
