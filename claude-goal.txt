GOAL (v4, 2026-06-22) — Reading-path catalog v1 → ratified classifier build (autonomous, BMAD-governed, ≤2h, Claude-direct dev; NO Codex)

CONTEXT: Branch fidelity-perception-arc-2026-06-19 (open as Class P, upgrade to S on first substrate touch). Operator-led slide-perception training ROUND 1 is CLOSED at 26/54 slides — sufficient to tune. Single source of truth: _bmad-output/implementation-artifacts/reading-path-operator-review-round1-notes.md (read the 🔑 tuple-synthesis, ⚙️ operationalization, 🔁 protocol blocks). v0-draft: reading-path-patterns-catalog.md. The flat 7-pattern enum was INVALIDATED. Held-out 14 slides were NEVER shown — keep it that way.

OBJECTIVE: From the round-1 evidence, advance as far down the dev plan as is RELIABLE in ONE ≤2h session under BMAD governance. Partial progress with a clean handoff beats a rushed gate.

ARC (stop wherever 2h or reliability runs out):
1. SYNTHESIZE → author reading-path-patterns-catalog.md v1 in COMPOSITIONAL-TUPLE form {macro_layout × image_role(1–4) × text_substructure × narration_cadence}. Bake in the operator-validated deltas: ADMIT two_up_comparison (4 fits) + multi_column (3 fits); REASSIGN f_pattern (2/3 flagged → message-led/decorative; keep 0 exemplars); GATE diagram_driven on foreground+opaque+load-bearing (kind:diagram ≠ instructional); encode the 4-TIER image-role axis {decorative·illustrative·instructional·pointer} + the 6 universal VO principles + per-pattern narration-cadence deltas. Default scan = top_down position-order (NOT Z).
2. PARTY-MODE GREEN-LIGHT the v1 catalog: fully-spawned bmad-party-mode (Winston/John/Murat/Mary/Amelia; add Caravaggio for layout). Record consensus verbatim. On impasse, run the ratified Quinn→John chain BEFORE escalating to me.
3. IF green-lit and time remains: author a ready-for-dev SPEC to refactor the P2-4a reading-path classifier (today: 7-pattern closed enum, dp-v1.5) toward the tuple repertoire. Schema-shape discipline; pipeline-lockstep regime on any manifest/pack touch.
4. IF time remains: BUILD via bmad-quick-dev (or bmad-dev-story) with a fully-spawned party + a Claude DEV AGENT (Amelia, RED-first) — NOT Claude-direct edits. Then bmad-code-review (3-layer) BEFORE flipping any story done.

HARD FENCES:
- NO Codex. Dev runs Claude-direct through the dev agent under party governance.
- P2-4b CALIBRATION is OPERATOR-GATED: it needs operator-labeled scan orders on the held-out 14. Do NOT self-label, do NOT touch the held-out set, do NOT run calibration. That leg is the natural STOP boundary.
- No mocks — real, live gpt-5.5+ on any classifier escalation leg; test determinism via recorded-real responses behind a parse seam ONLY, never a fixture on the production path.
- Run allowed tooling freely (.venv python, ruff, pytest, lint-imports, git) without per-command prompts. No force-push, no --no-verify.

WRAP-UP (mandatory at 2h regardless of where the arc lands): update SESSION-HANDOFF.md + next-session-start-here.md + deferred-inventory.md, write the resume banner, COMMIT + PUSH to origin per push-cadence policy.

FURTHEST RELIABLE POINT = last BMAD gate cleanly passed (green validation + committed + pushed). Acceptable terminal states, best→floor: (a) classifier refactored + code-reviewed + story done; (b) spec ready-for-dev + party-ratified catalog v1; (c) party-ratified catalog v1; (d) catalog v1 drafted + party round in progress.

STOP & ASK ME ONLY ON: true impasse after the Quinn→John chain; a governance change (gate-mode/K-policy/pack-version Tier-2+); or needing the operator-gated held-out labels.
