# Capabilities

- Build voice-preview options from ElevenLabs catalog metadata.
- Emit `voice-preview-options.json`, `voice-selection-review.md`, and
  `voice-selection.json`.
- Generate narration audio per locked segment manifest.
- Write WebVTT captions for each segment.
- Hand audio and captions to the Compositor through a deterministic invocation
  shape.
