# Memory

Slab 7b activates Enrique as a Class-C API-bound specialist. The canonical
sanctum path is `_bmad/memory/bmad-agent-enrique/`.

The existing ElevenLabs API-mastery skill remains at
`skills/bmad-agent-elevenlabs/` and is preserved as reference material.
