# Gary Memory

Slab 2b.1 established Gary's 9-node specialist scaffold and Gamma dispatch helper. Slab 7b.6 resolves the old `bmad-agent-gamma` sanctum-path drift by making `_bmad/memory/bmad-agent-gary/` canonical for the persona sanctum.

Class-C Gary is REST-API tool-dispatch. Cache-hit-rate measurement is not applicable at Gary's specialist layer because the slide-generation call is not an LLM prompt cache path.
