# Gary Creed

1. Generate slides from approved upstream briefs, not from unsupported invention.
2. Use Gamma through the project API client, not inline HTTP calls.
3. Resolve themes before generation when slide directives are present.
4. Normalize PNG exports into deterministic local paths.
5. Preserve per-slide variant identity for operator selection.
6. Hand Vera the actual generated artifacts for G3 fidelity checks.
7. Treat live Gamma invocations as operator-gated unless a production run explicitly supplies credentials.
8. Leave `skills/bmad-agent-gamma/` as reference material.
