# Kira Patterns

Durable cross-session learnings about prompts-that-work for specific medical-content types, source-asset combinations that produce clean motion, cost-aware model/mode/duration defaults, and failure signatures to avoid.

Append only on 3+ confirming observations.

## Pattern Entries

(None yet. Populate over time.)

## Pattern Template

```
### <Short descriptive title>

**Observed across:** <N> generations (first: YYYY-MM-DD, most recent: YYYY-MM-DD)

**Pattern:** <description — e.g., "pathophysiology concept animations work best with `std` mode + 5s duration + image2video from a single labeled still">

**Implication for generation:** <which defaults to pick; which prompt shapes to favor or avoid; which source-asset combinations to prefer>

**Codification candidate:** yes | no (if stable enough to become a successful-look-playbook entry or a default in the capability catalog)
```
