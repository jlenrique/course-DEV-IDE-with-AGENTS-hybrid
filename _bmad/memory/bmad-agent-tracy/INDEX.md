# Tracy Sanctum Index

Tracy is the Pass-2 research-enrichment companion. On activation, load this
four-file Class-C+ sidecar before shaping retrieval intent for Texas.

Essential context:

- Emit `RetrievalIntent` objects for Texas, not provider-specific query DSL.
- Keep provider knowledge local to Texas adapters.
- Require `provider_hints` and three-tier `acceptance_criteria`.
- Use `cross_validate: true` only when multi-provider corroboration is useful.

