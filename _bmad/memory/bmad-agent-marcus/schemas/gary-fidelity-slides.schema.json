{
  "$defs": {
    "VeraFidelityCriterion": {
      "additionalProperties": false,
      "description": "Vera-authored criterion carried into Gary's slide package.",
      "properties": {
        "criterion_id": {
          "description": "Stable Vera fidelity criterion identifier.",
          "minLength": 1,
          "title": "Criterion Id",
          "type": "string"
        },
        "description": {
          "description": "Operator-readable fidelity criterion text.",
          "minLength": 1,
          "title": "Description",
          "type": "string"
        },
        "severity": {
          "description": "Closed Vera severity: MUST, SHOULD, or MAY.",
          "enum": [
            "MUST",
            "SHOULD",
            "MAY"
          ],
          "title": "Severity",
          "type": "string"
        },
        "vera_source_ref": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional provenance pointer to the Vera source contract.",
          "title": "Vera Source Ref"
        }
      },
      "required": [
        "criterion_id",
        "severity",
        "description"
      ],
      "title": "VeraFidelityCriterion",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Per-plan-unit fidelity criteria package for Gary dispatch.",
  "properties": {
    "plan_unit_id": {
      "description": "Plan-unit identifier for this fidelity payload.",
      "minLength": 1,
      "title": "Plan Unit Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Schema version for FR-7c-51 bump-on-change discipline.",
      "title": "Schema Version",
      "type": "integer"
    },
    "target_section": {
      "description": "Target course section for the fidelity payload.",
      "minLength": 1,
      "title": "Target Section",
      "type": "string"
    },
    "vera_criteria": {
      "description": "Vera criteria prepopulated for Gary slide generation.",
      "items": {
        "$ref": "#/$defs/VeraFidelityCriterion"
      },
      "minItems": 1,
      "title": "Vera Criteria",
      "type": "array"
    }
  },
  "required": [
    "plan_unit_id",
    "target_section",
    "vera_criteria"
  ],
  "title": "GaryFidelitySlides",
  "type": "object"
}