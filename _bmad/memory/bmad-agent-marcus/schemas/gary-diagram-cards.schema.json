{
  "$defs": {
    "DiagramCard": {
      "additionalProperties": false,
      "description": "Per-slide diagram requirement for Gary.",
      "properties": {
        "caption": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional rendering caption for the diagram card.",
          "title": "Caption"
        },
        "slide_index": {
          "description": "One-based slide index.",
          "minimum": 1,
          "title": "Slide Index",
          "type": "integer"
        },
        "specification": {
          "description": "Operator-authored per-slide visual specification.",
          "minLength": 1,
          "title": "Specification",
          "type": "string"
        },
        "visual_kind": {
          "description": "Closed diagram visual kind for Gary rendering.",
          "enum": [
            "flowchart",
            "sequence",
            "comparison",
            "literal-visual"
          ],
          "title": "Visual Kind",
          "type": "string"
        }
      },
      "required": [
        "slide_index",
        "visual_kind",
        "specification"
      ],
      "title": "DiagramCard",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Per-plan-unit diagram card package for Gary dispatch.",
  "properties": {
    "cards": {
      "description": "Diagram cards for literal visual requirements.",
      "items": {
        "$ref": "#/$defs/DiagramCard"
      },
      "minItems": 1,
      "title": "Cards",
      "type": "array"
    },
    "plan_unit_id": {
      "description": "Plan-unit identifier for this diagram-card payload.",
      "minLength": 1,
      "title": "Plan Unit Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Schema version for FR-7c-51 bump-on-change discipline.",
      "title": "Schema Version",
      "type": "integer"
    },
    "target_section": {
      "description": "Target course section for the diagram-card payload.",
      "minLength": 1,
      "title": "Target Section",
      "type": "string"
    }
  },
  "required": [
    "plan_unit_id",
    "target_section",
    "cards"
  ],
  "title": "GaryDiagramCards",
  "type": "object"
}