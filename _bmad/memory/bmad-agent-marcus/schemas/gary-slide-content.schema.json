{
  "$defs": {
    "SlideContentEntry": {
      "additionalProperties": false,
      "description": "Per-slide text payload prepared for Gary.",
      "properties": {
        "body": {
          "description": "Slide body text.",
          "minLength": 1,
          "title": "Body",
          "type": "string"
        },
        "content_kind": {
          "description": "Closed slide content kind for Gary payload shaping.",
          "enum": [
            "narrative",
            "exposition",
            "summary"
          ],
          "title": "Content Kind",
          "type": "string"
        },
        "slide_index": {
          "description": "One-based slide index.",
          "minimum": 1,
          "title": "Slide Index",
          "type": "integer"
        },
        "title": {
          "description": "Slide title text.",
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "slide_index",
        "title",
        "body",
        "content_kind"
      ],
      "title": "SlideContentEntry",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Per-plan-unit slide content package for Gary dispatch.",
  "properties": {
    "plan_unit_id": {
      "description": "Plan-unit identifier for this Gary pre-dispatch payload.",
      "minLength": 1,
      "title": "Plan Unit Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Schema version for FR-7c-51 bump-on-change discipline.",
      "title": "Schema Version",
      "type": "integer"
    },
    "slides": {
      "description": "Slide content entries for the plan unit.",
      "items": {
        "$ref": "#/$defs/SlideContentEntry"
      },
      "minItems": 1,
      "title": "Slides",
      "type": "array"
    },
    "target_section": {
      "description": "Target course section for the slide content payload.",
      "minLength": 1,
      "title": "Target Section",
      "type": "string"
    }
  },
  "required": [
    "plan_unit_id",
    "target_section",
    "slides"
  ],
  "title": "GarySlideContent",
  "type": "object"
}