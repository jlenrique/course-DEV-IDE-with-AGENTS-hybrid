{
  "$defs": {
    "OutboundEnvelopeEntry": {
      "additionalProperties": false,
      "description": "One emitted writer payload referenced by the Gary outbound envelope.",
      "properties": {
        "dispatched_at": {
          "description": "Timezone-aware dispatch timestamp for this payload.",
          "format": "date-time",
          "title": "Dispatched At",
          "type": "string"
        },
        "operator_id": {
          "description": "Operator identifier for this payload dispatch.",
          "minLength": 1,
          "title": "Operator Id",
          "type": "string"
        },
        "payload_ref": {
          "description": "Relative path or URI to the emitted writer payload.",
          "minLength": 1,
          "title": "Payload Ref",
          "type": "string"
        },
        "target_section": {
          "description": "Target course section for this writer output.",
          "minLength": 1,
          "title": "Target Section",
          "type": "string"
        },
        "writer_id": {
          "description": "Closed canonical Marcus-bound writer identifier.",
          "enum": [
            "gary-slide-content",
            "gary-fidelity-slides",
            "gary-diagram-cards",
            "gary-theme-resolution",
            "gary-outbound-envelope"
          ],
          "title": "Writer Id",
          "type": "string"
        }
      },
      "required": [
        "writer_id",
        "target_section",
        "payload_ref",
        "dispatched_at",
        "operator_id"
      ],
      "title": "OutboundEnvelopeEntry",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Gary pre-dispatch envelope aggregating emitted writer payloads.",
  "properties": {
    "dispatched_at": {
      "description": "Timezone-aware envelope dispatch timestamp.",
      "format": "date-time",
      "title": "Dispatched At",
      "type": "string"
    },
    "entries": {
      "description": "Writer payload entries included in this envelope.",
      "items": {
        "$ref": "#/$defs/OutboundEnvelopeEntry"
      },
      "minItems": 1,
      "title": "Entries",
      "type": "array"
    },
    "operator_id": {
      "description": "Operator identifier for this envelope dispatch.",
      "minLength": 1,
      "title": "Operator Id",
      "type": "string"
    },
    "plan_unit_id": {
      "description": "Plan-unit identifier for this outbound envelope.",
      "minLength": 1,
      "title": "Plan Unit Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Schema version for FR-7c-51 bump-on-change discipline.",
      "title": "Schema Version",
      "type": "integer"
    },
    "target_section": {
      "description": "Target course section for this outbound envelope.",
      "minLength": 1,
      "title": "Target Section",
      "type": "string"
    }
  },
  "required": [
    "plan_unit_id",
    "target_section",
    "entries",
    "operator_id"
  ],
  "title": "GaryOutboundEnvelope",
  "type": "object"
}