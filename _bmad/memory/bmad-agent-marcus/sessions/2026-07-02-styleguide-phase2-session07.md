# Session log — 2026-07-02 (session 07): Gamma Styleguide Phase-2 with Juanl

> NOTE: MEMORY.md/BOND.md carried pre-existing uncommitted modifications from
> another context this session — per the dirty-worktree fence I did NOT edit or
> commit them. Durable items below are QUEUED for curation into MEMORY.md at the
> next clean opportunity.

## What happened (operator-present, Class S)

Phase-2 style building. Party green-light 5/5 (A1–A14). Landed: lifecycle
schema (candidate/permanent/deprecated at the validator write-gate; picker
badges); frozen `style-test-strip-v1` 4-slide probe corpus; videographic
register proven live (5 renders, flights 2/2b/2c/2d/2e); candidate
`videographic-glance-track` v2; first-bind witness discharged
(gpt-image-2-mini); 5 ledger gleanings; model hunt v1/v2 (verdict pending).
SSOT: `gamma-styleguide-phase2-session07-greenlight-party-record-2026-07-02.md`.

## QUEUED FOR MEMORY.md curation (durable lessons)

- **Operator preference (BINDING, 19:39):** style-development renders go through
  Gary's AUTHENTIC dispatch seam with high-fidelity simulated-Irene briefs — no
  hand-rolled payloads. Receipts label briefs SIMULATED (never count toward
  permanence, Winston rider).
- **Operator working style observed:** wants one step at a time, judges live
  renders by eye at the gammaUrl, challenges test design hard (his
  keyword-homogenizer catch was correct and produced the session's best
  finding). Present per-voice party digests, not walls; full record in the
  party file.
- **Gotcha — Gamma UI vs API:** the doc's "prompt design" panel shows editor
  defaults (Auto), NOT the generation request's imageOptions. Wire-payload
  capture (RecordingClient subclass pattern) is the proof mechanism.
- **Gotcha — model behavior:** `gpt-image-2-mini` renders anime-register
  imagery + Japanese in-image text under a clinical brief (training-origin
  default). Avoid for TJU clinical register; ideogram family for text-in-image.
- **Craft law (operator-named): the uncanny valley of forced continuity** —
  prose-requested literal cross-card visual continuity produces inconsistent
  callbacks that read as ERRORS; symbolic systems are better but insufficient.
  Per-card independence, or construction-route disaggregation. Nothing between.
- **The house look is instruction-dominant:** styleguide keywords→prose channel
  homogenizes across image models (hunt v1 vs v2 controlled pair). Model pin =
  weaker control than keyword channel; design review queued with Dan.
- **Cluster-head grammar (operator-directed):** Irene identifies cluster-HEAD
  slides carrying the high-value image; children self-prompted, images only
  when warranted. Today every child brief gets an image — this is a NEW Irene
  contract for the at-scale story.
- **Videographic = future MODE/overlay** orthogonal to style identity (like
  Classic/Studio); a modality OWNS its implied setting deltas; Gary composes at
  resolve time. Interstitial style keeps the B-companion earmark (operator
  override of Dan's relocation ruling).

## Open at close

Model-hunt verdict pending (operator eye); then double-pull + 3-card
confirmation → Dan pins the pinned-A variant → interstitial rename → the
match-this-look + blow-your-mind flights → dual-gate close (Murat + Dan) with
the modality-schema + keyword-homogenizer reviews.
