{
  "additionalProperties": false,
  "description": "STUB resolution entry; 1.3 replaces with the full cascade-resolution shape.",
  "properties": {
    "level": {
      "description": "Resolution level (e.g. 'per-call', 'per-specialist', 'registry-default'); 1.3 tightens this to a Literal.",
      "minLength": 1,
      "title": "Level",
      "type": "string"
    },
    "resolved": {
      "description": "The model identifier the cascade resolved to at this level.",
      "minLength": 1,
      "title": "Resolved",
      "type": "string"
    },
    "timestamp": {
      "description": "Timezone-aware resolution timestamp.",
      "format": "date-time",
      "title": "Timestamp",
      "type": "string"
    }
  },
  "required": [
    "level",
    "resolved",
    "timestamp"
  ],
  "title": "ModelResolutionEntry",
  "type": "object"
}
