# {{name}} expertise

Specialist `{{name}}` reads domain references from this directory during the
`plan` stage.

- `mcp_tool`: `{{mcp_tool}}`
- `expertise_tier`: `{{expertise_tier}}`

Populate this directory with domain references and keep this README updated.
