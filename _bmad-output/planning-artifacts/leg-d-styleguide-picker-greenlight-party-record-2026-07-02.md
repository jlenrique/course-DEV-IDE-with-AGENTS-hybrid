# Leg-D Green-Light — Party Record (2026-07-02, session 03)

**Arc:** Gamma Styleguide Library · **Branch:** `dev/gamma-styleguide-leg-c-live-2026-07-01` · **Feature:** operator feature #2 — HTML style-picker at CD-entry.
**Round:** fully-spawned party, 6 seats: Winston / John / Murat / Amelia + specialty Dan (CD owner) / Sally (UX). Scout report (read-only, file:line-cited) preceded the round.
**Outcome: GREEN-LIGHT 6/6 — FORK A (pre-run picker), amendments binding.**

## The fork, settled unanimously: **A — PRE-RUN local picker** (B: in-run gate node REJECTED)
- Winston: the directive's `gamma_settings[]` IS the existing operator binding surface — A puts a face on it; B invents a SECOND binding mechanism needing precedence rules (correctness hazard). SPOC framing: the runtime executes a chosen production; style selection is a planning-time decision. Provenance is MORE auditable under A — baked into the run's own input artifact.
- John: the real operator picks styles when COMPOSING a run, pre-spend; B is "scope laundering a UX story into pipeline architecture" (Tier-2 governance inside a ~1.5-2h runway). J-4: the chooser/gh-pages pattern is referenced for round-trip SHAPE only — guards Tier-2 creep.
- Murat: B = a brand-new unproven dispatch seam — exactly the D1/F1 shape; A writes into a seam gary + the Leg-C floor ALREADY read in production. Zero new runtime seams.
- Amelia (verified in-repo): `production_runner.py:1541-1574` reads the directive yaml directly with zero gary import = the exact precedent; shippable today.
- Dan: pre-run unambiguously — an in-run gate smuggles operator choice into runtime and blurs style-selection ownership vs run mechanics.

## RATIFIED DESIGN
- **Module:** `app/marcus/orchestrator/styleguide_picker.py` (peer of `html_review_pack.py`). Reads the SSOT `state/config/gamma-style-guides.yaml` DIRECTLY (yaml.safe_load; no `gary.styleguide_library` import — display+selection only, gary keeps resolution semantics). Stdlib-only.
- **Pick capture (Amelia):** local ephemeral-port `http.server` loop; the rendered HTML form POSTs the pick; server exits on receipt. CLI-numbered input = DEGRADED FALLBACK only (browser-won't-open path). Trivially testable via urllib POST — no browser automation needed in unit tests.
- **Output:** writes/patches the run directive's `gamma_settings[]` (`styleguide` + `variant_id`, existing A/B semantics VERBATIM — J-3) + a `styleguide_picker_provenance` block (picked_at, picker_version, SSOT sha256, guide names, variant mapping, written_by) INTO the directive (Winston: the run's own input artifact carries the audit trail).
- **last_used (Dan D-1, Winston, Amelia — BINDING):** append-only sidecar JSONL `state/config/gamma-styleguide-picks.jsonl` (mirrors `gamma-learned-observations.jsonl`); the picker RENDERS the sidecar-derived value joined by guide id at render time; `presentation.last_used` in the SSOT stays null FOREVER (no non-CD hand touches the write-gated file — the Leg-B2 two-store contract).
- **thumbnail_ref (Dan D-2 — distinct discipline):** curated provenance, not telemetry. The harvester COPIES real evidence PNGs into the stable CD-owned home `state/config/gamma-styleguide-thumbnails/`; `presentation.thumbnail_ref` is filled as a DELIBERATE curation write under the validator write-gate (this story performs the initial curation for the 3 seed guides under this ruling), never a runtime write-back.
- **Probe visibility (Dan D-3):** the two Leg-C floor probes gain `status: probe`… CORRECTION: `status` is an existing enum field — implement as an additive `presentation.visibility: probe` marker (validator additive-safe) if `status: probe` trips the enum; default picker view lists PRODUCTION-intent guides only; `--include-probes` opt-in. Scaffolding must not masquerade as an intentional style.
- **J-1:** the last_used writer is a standalone utility, never imported into the CD authoring path. **J-2:** thumbnails = real evidence-dir PNGs only, zero placeholders (no-mocks applies to UI assets). **A-1:** fail-loud tests on directive-patch + sidecar (malformed styleguide name → error, never silent skip).

## MURAT'S AC REGIME (binding; D1/F1 antidotes)
AC-1 render from the REAL SSOT loader (no fixture dict). AC-2 cite-the-consumer: a picker-written directive resolved by the REAL `_gamma_settings_from_directive` (gary's actual import path). AC-3 same against `_min_cluster_floor_from_directive` (shape parity with the floor consumer). AC-4 last_used idempotency + the hermetic write-gate validator PASSES after EVERY write (M-2 rider: on each write-back test). AC-5 non-CD-writer boundary: CD-owned fields byte-identical pre/post. AC-6 every rendered thumbnail_ref resolves to a real on-disk PNG (fail on dangling). AC-7 provenance block survives to the downstream directive reader. **AC-8 LIVE leg:** a REAL pick event driven through the actual rendered HTML + localhost POST (headless harness, not hand-authored JSON) → directive → the REAL `production_runner` payload-builder boundary (unpaid; pin-test discipline); **arbiter = the pinned dispatch-payload snapshot diffed against the picker's independently-emitted pick-event log** (M-3: two artifacts, two code paths, independently generated). M-1: consumer symbols imported by real production module path — no shadow copies.
- Live bar (John): real SSOT, real rendered+clicked HTML, real pick event, real directive on disk, the real payload builder binding the picked guide, real sidecar timestamp. Amelia's cut-if-squeezed (defer the live leg) is OVERRIDDEN by the arc's live-testing doctrine — the live leg stays; the paid Gamma walk is what's deferred (Murat: overkill for this surface).

## SCOPE FENCE (one story, today)
SHIPS: picker render (SSOT + thumbnails + sidecar-derived last_used) · single-select fast path + A/B two-select · localhost pick capture + CLI fallback · directive write + provenance · sidecar append · initial thumbnail curation (3 seeds) · probe-hiding · tests per the AC regime · AC-8 live leg.
DEFERS: gh-pages publishing · multi-select beyond A/B · Marcus-SPOC conversational integration (Phase-2 calls this surface) · any manifest/gate change · CD-side batch fold of sidecar → presentation.
**Honesty banner (Dan):** Leg-D is the FIRST READER of Leg-A's write-only presentation block — a substrate-completion moment; the story claims an operator pick surface + provenance round-trip, NOT any in-run gating.

## SALLY (UX) — folded on arrival
(see addendum below)
## SALLY (UX) ADDENDUM — GREEN-LIGHT (S-1/S-2/S-3 folded as amendments)
Card GRID (thumbnail PRIMARY, large top; display_name + distinguishing bold always-visible; narrative block behind an expand toggle; last_used as a muted top-right chip). **A/B = two persistent SLOTS pinned above the grid** ("Style A / Style B"); card click fills the next empty slot (single-select fast path = one click); slots carry swap/clear icons — the slot bar IS the state (kills which-card-is-which ambiguity). **Confirm** = one button, disabled until ≥1 slot filled; on click writes directive + provenance, then the page replaces the button with a green RECEIPT PANEL (file path, picked names, timestamp) and the launching terminal echoes the same receipt. S-1: probe/draft entries render a warning chip, excluded from default slot-fill, require explicit second click. S-2: slots never silently reorder after swap (highlight on change). S-3: missing/stale thumbnail renders an explicit "no live render" placeholder — never a confidently wrong image. Non-negotiable: `distinguishing` legible at default zoom, no hover-only information. MVP today: static single page, cards from the SSOT at load, slot bar, confirm→write, receipt. Defers: search/filter, feels_like tags, pagination, in-page thumbnail regen (Phase-2 at ~8+ styles).

**FINAL: GREEN-LIGHT 6/6 (FORK A) — Winston GL(A) · John GL(A)+J-1..J-4 · Murat GWA(A)+M-1..M-3+AC-regime · Amelia GL(A)+A-1 · Dan GWA+D-1..D-3 · Sally GL+S-1..S-3. Dev opens.**
