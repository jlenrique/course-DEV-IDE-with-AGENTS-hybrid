---
scope: Operator HUD flight deck v1 — runtime-owned projection contract, HUD data/render, transport, notifier service
purpose: build-substrate
altitude: feature
updated: 2026-07-11T14:41
---

- (constraint) [ADOPTED] Party plan §5.3(a): runtime-owned versioned operator-surface projection contract — ONE status document written by the production runner, in BOTH node walks (standing two-walk gotcha), shared Pydantic model, dual JSON-Schema shape-pins so producer changes fail producer tests.
- (constraint) [ADOPTED] Party plan §5.3(b) + brief open-question pin: transport = poll-first (static file + mtime/etag-gated 2-5s poll as v1 default); SSE only as Tier-1 follow-on IF the gate-verdict flow independently justifies a served operator API. Runtime↔HUD server lifecycle is THIS phase's decision. Operator pinned 'up to you' at brief review.
- (constraint) [ADOPTED] Party plan §5.3(c): retire-path required for legacy bundle-gate/coordination.db readers, including killing run_hud.py _query_active_run_id silent wrong-run fallback (UX spine already mandates refuse-to-render on identity uncertainty).
- (constraint) [ADOPTED] Party plan §5.3(d) perf/traps budget: 525KB run.json must be mtime-gated (parse only on change); LangSmith live call OUT of the data-collection path; stale watch-mode docstring fixed at dev.
- (constraint) [ADOPTED] Brief principle 1: runtime start path executes pre-flight and gates SPOC spawn; HUD only visualizes. Pre-flight authoritative whether or not HUD is open. Read-only stays pure — no verdict transport through the HUD.
- (constraint) [ADOPTED] Brief addendum §F: notifier = small shared service the HUD consumes, reusable later by a dev-side producer (dev-run stall notifications) without touching the HUD. Channels: on-HUD visual + optional sound + phone push. v1 event classes: batch_pause_resumed, paused_at_gate, paused_at_error, health_threshold, run_stalled.
- (constraint) [ADOPTED] Governance: run_hud.py, progress_map.py + their tests are pipeline-lockstep block_mode_trigger_paths; all dev manifest-regime governed; envelope status-model files to be ADDED to block_mode_trigger_paths per Murat L1 reverse tripwire (party plan evidence strategy) — manifest edits are Tier-2 party-gated.
- (constraint) [ADOPTED] Murat evidence strategy (story ACs): L1 enum set-equality contract test against envelope status vocabulary; L2 golden run-dir fixtures per pause class + one legacy-shaped dir rendering unrecognized; L3 first-run-stands live witness per pause class promoted into L2 golden set. DoD per story names its witness set.
- (constraint) [ADOPTED] Reuse tiering (party plan §4 + brief constraints): retarget the data layer, keep the render shell — April HUD layers cleanly separable, ~98 passing pins preserved where compatible; data layer becomes a thin consumer of the runtime-owned projection.
- (decision) Workspace bound at architecture-operator-hud-2026-07-11 (product-named folder, consistent with ux-operator-hud convention). Fast path chosen by operator; deliverables = spine + interactive HTML walkthrough deck. Altitude: feature (keeps epics).
- (decision) Phone-push mechanism (web-verified 2026-07-11): PRIMARY = Pushover (one-time USD4.99/platform; native APNs/FCM so reliable on locked/dozing phones; priority -2..2 maps to event classes; 10k msgs/month account pool per May-2026 change; single authenticated HTTPS POST, no SDK). FALLBACK = ntfy hosted (zero-cost, topic-per-event-class, but iOS app documented buggy by its own dev + hosted topics are password-by-obscurity + 1req/10s rate limit). [ASSUMPTION] Wire both behind Apprise v1.9.8 (active, released 2026-07-04) so failover is a config-string swap — one small dep buys mechanism independence for the shared notifier service. Twilio eliminated (A2P 10DLC registration burden absurd for solo self-notification); Telegram eliminated (no priority granularity, alerts drown in social app).
- (version) Verified current 2026-07-11: Apprise v1.9.8 (2026-07-04); Pushover API stable, free-tier pooling change effective 2026-05-01; ntfy actively maintained, hosted rate limit ~60-burst then 1req/10s.
- (event) Brownfield sweep (Explore subagent): _persist_envelope (production_runner.py:221) is the ONLY run.json writer, invoked at every state transition in BOTH walks (register 2652, in-flight 2683, pauses 2174/2412/2519, terminals 3128/4042, reject 3223, batch-clear 3385) — the projection emission choke-point. ONE bypass exists: recover reenter path writes run.json directly at 3501-3504; must be routed through _persist_envelope at dev. trial-start.json is CLI-written (trial.py:630), NOT runner-written — projection cannot depend on it. resume-command.json runner-written at 3193-3199.
- (event) Sweep: coordination.db production_runs is written ONLY by the skills layer (manage_run.py, deployment_coordinator.py); the migrated runtime NEVER writes it; run_hud._query_active_run_id queries statuses planning/active that the runtime never sets -> frequently None -> _resolve_active_bundle silently falls back to newest-mtime bundle (the wrong-run bug, run_hud.py:105-184). Retire this whole reader chain from the HUD path.
- (event) Sweep: serving substrate exists — FastAPI 0.136 + uvicorn 0.45 already deps; app/runtime/server.py (runtime /invoke), app/http/gate_endpoint.py (DecisionCard http transport POST /gate/verdict). No http.server usage; April HUD is a static file with location.reload(). HUD server must avoid route/port collision with runtime server + gate endpoint. Notification substrate: NONE exists (no push/webhook/sound code). watchdog lib present (sanctum watcher). import-linter FORBIDS app.gates.** from importing threading/apscheduler/schedule — stall-timer must live outside app/gates. No heartbeat field exists; progress inference today = artifact mtimes + envelope timestamps; projection should carry explicit seq + transition timestamps for the watchdog.
- (event) Sweep: shape-pin patterns — (a) byte-identical emitted-schema pin (test_decision_card_shape_pin.py: decision_card_schema_text() == committed app/models/schemas/decision_cards.schema.json) and (b) dual producer+consumer parity (tests/contracts/test_lesson_plan_json_schema_parity.py: field-presence, enum parity, round-trip, bidirectional required/optional). Use BOTH for the projection per mandate. run_hud data/render seam already clean: collect_hud_data(...)->dict / render_html(data). hud_data_sources.read_active_trial does the full 525KB run.json parse discarding 99%; read_adhoc_summary is the only live network call (LangSmith list_runs) — cut both from the data path. Governance: adding new files to block_mode_trigger_paths = Tier-2 manifest edit, party-gated; L1 = check_pipeline_manifest_lockstep.py (8 checks); Cora preclosure hook classifies diffs.
- (version) Stack verified from pyproject: Python >=3.11; pydantic >=2.7,<3; fastapi >=0.136,<1; uvicorn >=0.45,<1; httpx >=0.28,<1; requests >=2.31,<3; watchdog >=4,<6; jsonschema >=4; pytest xdist default excludes live/serial markers (HUD network/db tests must mark serial or live).
- (decision) Spine drafted, 14 ADs. New calls beyond adopted mandates: AD-1 projection file = state/config/runs/<trial_id>/operator-surface.json, model OperatorSurfaceProjection v1 at app/models/runtime/operator_surface.py; AD-4 additive-only within v1, version bump on breaking change, unknown schema_version -> UNRECOGNIZED; AD-6 GET-only FastAPI app (routes /, /projection, /healthz), ETag=mtime+size, [ASSUMPTION] default port 8791; AD-7 HUD server = child of runtime session, /healthz 200 is a pre-flight item, survives run completion until session exit, [ASSUMPTION] standalone 'trial hud' CLI for post-hoc viewing; AD-9 notifier at app/notify/ (outside app/gates import-linter fence), Apprise URL strings, Pushover primary/ntfy fallback, creds env-only, fire-and-forget; AD-10 stall watchdog = projection seq + last_transition_at monotonicity, [ASSUMPTION] 600s budget; AD-12 Dev-Cycle + M5 panels retired with their pins; render seam stays pure dict->HTML.
- (assumption) [ASSUMPTION] roster: hud.port default 8791; stall budget 600s; Apprise as new dep (>=1.9.8,<2); 'trial hud' standalone CLI; projection soft size target (perf budget) — all correctable at operator review or story level.
- (question) Open: does the runtime start path today have a natural place to launch a child process (HUD server) + run phase-02 heartbeats before SPOC spawn? Sweep confirmed pre-flight substrate = ready_for_trial scripts (operator-run) — the in-runtime pre-flight execution point is a dev-phase discovery; AD-11 binds the contract, not the call site.
- (version) CORRECTION (web-verify reviewer): Apprise 1.9.8 released 2026-03-08, NOT 2026-07-04; current release is 1.12.0 (2026-07-04). Pin >=1.9.8,<2 still resolves; pover:// and ntfy:// URL formats re-verified current.
- (decision) Web-verify fixes folded into spine: AD-2 gains bounded-retry rule (os.replace raises PermissionError on Windows while a reader holds the file; ~5x backoff; write failure logs, never crashes runner; consumers open-read-close); AD-6 ETag recipe pinned to '{seq}-{mtime_ns}' with route-implemented If-None-Match->304 (Starlette FileResponse does not do 304); Stack table: jsonschema is dev extra <5; requests annotation de-conflicted with Apprise-only push.
- (event) Reviewer gate complete. Rubric: CONDITIONAL PASS (1 critical C1 write-site/seq ownership; H1 notifier lifecycle; H2 unenforced arrows; H3 unbounded growth). Adversary: NOT READY verdict with 3 critical (F1 _persist_envelope cannot see non-envelope sections; F2 emission cadence vs staleness budgets; F3 wrong-server-on-port healthz), 6 high (F4 two-truths crash window; F5 seq double-duty; F6 dual event derivation; F7 watchdog fate-sharing; F8 strict-vs-lenient parse; F9 os.replace vs open readers), 7 medium, 2 low. Web-verify: 3 high (os.replace PermissionError; FileResponse no 304; Apprise version record wrong). Reconciler: 13 partial/dropped items. ALL resolved as spine amendments: AD-2 rewritten as emission-cadence AD (assembler + 3 legal triggers + tripwire); AD-15 rewritten as OperatorSurfaceAssembler owning non-envelope sections + lifecycle-stage presence + manifest identity + walk_generation; NEW AD-17 (run.json is truth, envelope_digest, reconcile-on-entry); NEW AD-18 (contract-owned derive_event_transitions); NEW AD-19 (HudConfig single owner); AD-4 dual parse surfaces (strict producer / lenient consumer, import-linter forbids consumer strict parse); AD-6 ETag schema_version:seq + route-implemented 304; AD-7 pinned start sequence + identity-checked healthz (trial_id+nonce) + ephemeral-port standalone + hud-server.log; AD-9 notifier own process, not fate-shared, producer-dead detection, restart semantics with own state dir; AD-10 seq/progress_seq split; AD-12 run_hud.py deprecation-stub disposition + no-hud_data_sources import; AD-14 trigger-path pre-registration clause; AD-16 256KB tripwire; AD-3 CLI-co-located command builder + parser round-trip; paradigm arrows enforced via import-linter; Deferred gains SSE party rider + UX Flow-1 between-runs divergence (one-line UX update owed at story time).
- (decision) AD-9 [ASSUMPTION] 'watcher thread inside HUD server process' SUPERSEDED same-session by adversary F7: notifier is its own process, launched by start path, not fate-shared with the runtime session (the watchdog must survive the wedge classes that kill the session).
- (event) spine finalized
