# Leg-C GREEN-LIGHT Party Record — `min_cluster_floor` scripted class

**Arc:** Gamma Styleguide Library (Phase-1 machinery). **Branch:** `dev/gamma-styleguide-library-2026-07-01`. **Date:** 2026-07-01. **Legs A + B:** CLOSED.

**Consensus verdict: 6/6 RATIFY-WITH-AMENDMENTS.** Two live tensions resolved by orchestrator synthesis (no impasse; Quinn not required). Amelia's conditional-BLOCK on binding-time was **discharged** by a forensic investigation (EARLY-BOUND, HIGH confidence) run BEFORE this record was committed.

**Gate mode:** DUAL + the 07G VO↔on-screen perception gate NON-WAIVABLE. **Depends:** Leg-A only (done) — NOT Leg-E.

---

## Team (canonical core + ≤2 specialists)

| Voice | Role | Verdict |
|---|---|---|
| 🏗️ Winston | Architect | RATIFY-WITH-AMENDMENTS |
| 📋 John | PM | RATIFY-WITH-AMENDMENTS |
| 🧪 Murat | Test Architect | RATIFY-WITH-AMENDMENTS |
| 💻 Amelia | Developer | RATIFY-WITH-AMENDMENTS (cond. BLOCK on binding-time → discharged) |
| 🎙️ Irene | Clustering/narration + 07G owner | RATIFY-WITH-AMENDMENTS |
| 🎨 Dan | Creative Director | RATIFY-WITH-AMENDMENTS |

Dr. Quinn synthesizes on impasse; John PM tiebreaks. Codex shadow-monitor consulted (standing guidance; consult again at the dev gate).

---

## Per-voice positions (faithful condensation)

**🏗️ Winston** — Irene reads the floor ONLY from the resolved-styleguide carrier in the run bundle via a **scripted-only typed accessor with no path to `gamma_settings[]`** (symmetric to Gary's metadata-quarantine). Floor consumed **strictly at Pass-1 clustering time, upstream of per-sub-slide narration** — an *input to* the same clustering model 07G already validates, never a post-hoc reshaper. `scripted` ships as a **sealed, validator-enforced closed-vocab namespace with exactly one hardwired consumer and NO generic engine** ("a switch with one case, not a rule interpreter") + a **fail-loud dead-config detector** so the namespace can't rot into a junk drawer.

**📋 John** — `min_cluster_floor` is trivially declarative; **BLOCK any open-ended engine** ("v1 is a field, not a framework"). DONE-bar = differential floor-off-vs-floor-on live proof on **seed styles only**, 07G passes on floor-on (protected invariant outranks the feature). Cut: no engine, no 2nd class, no picker (Leg-D), no audit (Leg-E), no roster growth. Dead-config detector → follow-on (surface-doubling). Gate = dual + 07G confirmed (touches the clustering path upstream of the protected invariant).

**🧪 Murat** — differential-effect-FIRED pair: same corpus/seed, floor-off (control) vs floor-on (treatment); arbiter = cluster count from the **on-disk Pass-1 artifact**, never a driver's inline claim. **Anti-vacuous (non-negotiable): control MUST cluster BELOW the floor** — `count_K < N ≤ count_T`; if no real corpus clusters below N → unfalsifiable → BLOCK signal. **Determinism pinned** (`count_K == count_K'`) before attributing the delta; optional monotonic dose. **07G with teeth**, not silence — real read-path-match verdict artifact on the floored deck + a **teeth-witness** (desynced floored deck → 07G FLAGS it). **Dead-config detector needs its own discriminating pair** (fired-and-consumed → quiet; present-but-never-read → MUST raise) + loud unknown-`scripted`-key rejection. **Learned-store-stays-empty pin** (Leg-C adds zero learned entries).

**💻 Amelia** — the leg lives or dies on **binding-time**: is the styleguide selection early-bound (in state before Irene Pass-1) or Gary-stage late-bound? Refused to sign on inference → demanded a grep-cited T1 proof. Hard amendment: **enforce the floor as a post-clustering, split-only, monotone constraint — never a re-parameterization of the clustering objective** (she would BLOCK the "let the algorithm re-partition freely" path as a 07G risk). RED-first: feed a known Pass-1 output, apply floor N, assert (a) ≥N clusters AND (b) **flattened text byte-identical to pre-floor** (that byte-identity IS the 07G guard in unit form). **Dead-config detector = ratification condition** (set-but-never-read → fail loud, else silent no-op = invisible dead config). #1 risk = binding-time; #2 = SPOC guardrail (justify the floor in follow-along terms, not run-cosmetics).

**🎙️ Irene** — the floor is directional: HELPS on genuinely multi-beat content (the chunking-for-follow-along she already wants); HURTS on incompressible content by orphaning figure↔narration, breaking the establish→tension→develop→resolve arc, or over-fragmenting. Non-negotiable: **the floor is a SOFT target with source-content veto** — Pass-1 may return best-honest M < floor and emit a **styleguide-vs-content mismatch signal**; *fail loud on the mismatch, never by forcing a bad split.* **Figure↔narration atomicity inviolable** (explicit pre-check); split only along **legitimate cluster seams**; **re-run 07G per resulting sub-slide.** Live proof must include a supportive dense-split case, a **figure-bond-survives case**, and the **adversarial floor-too-high → honest-refusal, no over-fragment** case (won't waive).

**🎨 Dan** — `min_cluster_floor` is genuine CD intent ONLY as **style-identity** ("this style reads as a multi-beat narrative walk; below N beats it collapses to a stub deck"), NOT as a **content/pedagogy** claim ("this lesson is hard, force more clusters" — that's Irene's Pass-1, not a styleguide). Contract must make the first authorable and the second **unauthorable** via a mandatory **style-identity `rationale`** in provenance (review smell-test). `scripted` = **registry-bound closed enum** (v1 = exactly `min_cluster_floor`); adding a class = a party-mode governance act; **declarative payload (a value), never a mechanism**; one coded interpreter; envelope-authored, validator-gated, **no CD-direct-write**. Honesty banner: proves the *channel/pattern + one class under 07G supremacy*; defers general-intent-language, per-style floor-correctness, general reading-quality. **"The floor proposes; the non-waivable 07G gate disposes."**

---

## The two live tensions — orchestrator synthesis (no impasse)

**① Container shape — Winston/Dan (sealed `scripted` namespace) vs John (bare `min_cluster_floor: int` field).**
**RESOLVED → keep the `scripted` namespace.** John's substantive bar (no open engine / no framework / governance to add classes) is fully met without a bare field: (a) the `scripted` quarantine ALREADY exists as reserved metadata (`styleguide_library.py`), so reuse = less churn; (b) Gary's HARD metadata-quarantine already keeps `scripted.*` out of `gamma_settings[]` — a bare top-level field would need its own leak-prevention proof; (c) Dan's registry gives the auditable governance boundary. What John cuts — an open engine / map-of-rules / interpreter — nobody is building; a governed one-member enum with one coded consumer is "a switch with one case," not a framework.

**② Dead-config detector — Amelia/Winston/Murat (in-scope) vs John (follow-on).**
**RESOLVED → split it.** IN-SCOPE (narrow, essential): `min_cluster_floor` set-but-never-consulted → **fail loud** (Amelia's anti-silent-no-op guard) + unknown/unregistered `scripted` key → RED (each with its own discriminating pair, per Murat). DEFER (John's surface-doubling worry): the **general** dead/unreachable-config detector → named follow-on.

---

## Binding-time investigation — DISCHARGED (EARLY-BOUND, HIGH confidence)

Forensic trace (evidence-graded, file:line cited):
- **Node ordering:** `directive-composer` (pipeline start) → `04A/05/05B irene-pass1` (05B = "Cluster Plan G1.5" clustering node) → … → `07 gary` (`pipeline-manifest.yaml:1047-1052`). Irene Pass-1 **strictly precedes** Gary.
- **Directive availability:** run-level `directive.yaml` (carrying Leg-A's `styleguide` key) materialized before the node loop and threaded to every node (`production_runner.py:2049/2066/2124`) — present in state **before** Irene Pass-1.
- **Carrier seam exists:** `_runner_payload_for_specialist` (`production_runner.py:1378`) already threads directive-derived config to Gary (`gamma_settings`, `:1418-1425`); returns `None` for `irene-pass1` today. Payload flows runner → dispatch_adapter merge (collision-guarded, `dispatch_adapter.py:108-120`) → `cache_prefix` → Irene `_decode_envelope_payload` (`graph.py:1550-1558`) → `_act_pass_1` (`graph.py:1561-1587`).
- **⚠️ Material refinement:** Irene's existing `floor`/`cluster` grep hit is a **prompt-token floor** (unrelated). **Pass-1 clustering is LLM-emergent** (fixed system prompt; nothing enforces a minimum cluster count). So **honoring ≥N is net-new logic, not a plug-in — this is the leg's real work.** This VINDICATES Amelia's split-only-not-reparameterize ruling and Irene's soft-floor/legitimate-seams/figure-atomicity constraints (they already govern the net-new reality; no re-convene needed).
- **SSOT status:** `scripted` block is schema-declared but INERT; `min_cluster_floor` is NOT yet in `scripted_enum` (only `publication_target`/`descript_bound`; `gamma-style-guides.yaml:210-221`); the resolver deliberately DROPS `scripted` (`styleguide_library.py:249-263`) → a new scripted-only accessor is required.

---

## Ratified Leg-C shape (consolidated amendment set → story ACs)

**Contract (Dan/Winston):** sealed registry-bound `scripted` enum (v1 = `{min_cluster_floor}`; new class = party governance); declarative **value** (positive int), never a mechanism; one coded consumer; mandatory **style-identity `rationale`** in provenance (bars content/pedagogy smuggling); envelope-authored, validator-gated, **no CD-direct-write**; validator rejects unregistered keys; `scripted.*` reaches Irene only via a scripted-only typed accessor with **no path to `gamma_settings[]`**.

**Seam (Winston/Amelia):** read from the resolved-styleguide carrier at Pass-1, **before** per-sub-slide narration; orchestrator reads the SSOT yaml directly (mirroring `_gamma_settings_from_directive`) to respect the specialist/orchestrator import boundary; enforce as a **post-clustering, split-only, monotone** constraint (subdivide only; never merge/reorder/reassign canonical text order).

**Safety (Irene, central):** **SOFT floor with source-content veto** — emit a *styleguide-vs-content mismatch signal* when content can't honestly support N; fail loud on the mismatch, **never by forcing a bad split**; **figure↔narration atomicity inviolable**; split only along legitimate seams (existing internal source-structure boundaries; never mid-component); **re-run 07G per resulting sub-slide.**

**Live proof (Murat/Irene, no mocks):** differential pair, same corpus/seed, arbiter = cluster count from the **on-disk** Pass-1 artifact; **control must cluster below the floor** (`count_K < N ≤ count_T`; else unfalsifiable → BLOCK); determinism pinned before attribution; **07G with a real read-path-match artifact + teeth-witness** (desynced floored deck flags RED); **byte-identity unit guard** (floored flattened text == pre-floor); Irene's **floor-too-high → honest-refusal** adversarial case (non-waivable); a **figure-bond-survives** case; learned-store-stays-empty pin.

**Dead-config detector:** IN-SCOPE narrow (set-but-never-read → fail loud; unknown scripted key → RED, each with its own discriminating pair); GENERAL detector → follow-on.

**Gate:** DUAL + 07G non-waivable. **Honesty banner (Dan):** proves the *channel/pattern + one class conveyed faithfully under 07G supremacy*; defers general-intent-language, per-style floor-correctness, general reading-quality.

**Guardrails held:** SPOC-is-the-goal (justify the floor in follow-along/perception terms, not run-cosmetics); source-detail→Gamma conveyance protected; 07G VO↔on-screen protected at all costs; CD-never-mutates-config.

---

## Refined task breakdown (for the story spec)

- **Task 0 — SSOT prereqs:** add `min_cluster_floor` to `scripted_enum` + a scripted-only accessor (resolver currently drops `scripted`); validator enum + positive-int + unregistered-key RED; mandatory style-identity `rationale` field.
- **Task 1 — threading (feasible):** `irene-pass1` branch in `_runner_payload_for_specialist` reading `directive.styleguide` → SSOT `scripted.min_cluster_floor` → `{"min_cluster_floor": N}` (orchestrator reads SSOT yaml directly).
- **Task 2 — the real work:** deterministic post-clustering split-only floor-honoring in `_act_pass_1`, guarded by the byte-identity 07G unit test + soft-mismatch fallback + figure-atomicity pre-check.

---

## Follow-ons filed / deferred

- **General dead/unreachable-styleguide-config detector** → deferred-inventory (the broad detector beyond Leg-C's narrow set-but-never-read + unknown-key guards).
- `scripted` 2nd-class admission remains a future party-governance act (registry stays closed at one member).

## Next step

Record committed → `bmad-create-story` for Leg-C (Task 0/1/2 breakdown; amendment set as ACs; binding-time proof cited as discharged; live-proof/corpus criteria) → governance validators → full `/harmonize` before dev → Codex shadow-monitor at the dev gate.
