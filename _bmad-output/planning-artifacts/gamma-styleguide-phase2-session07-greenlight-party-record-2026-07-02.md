# Gamma Styleguide Phase-2 — Session-07 Green-Light Party Record (2026-07-02)

**Round 1 — session-plan green-light. Verdict: 5/5 RATIFY-WITH-AMENDMENTS.**
Seats (fully spawned): Winston (architect), Sally (UX), Caravaggio (presentation master), Dan/CD (registry owner), Gary (Gamma dispatch). Murat joins at dual-gate close. Creative trio (Carson + Caravaggio + Dr. Quinn) convenes at Flight 3.

## Operator-ratified session frame (binding)

- Phase-2 style BUILDING, operator present. Registry 3 → ~8 over the arc; today produces **CANDIDATES only**.
- **Promotion gate (operator, this session):** a candidate becomes a permanent registry style ONLY after a B-corpus stress test + re-run reliability proof in a later session. Today = A-corpus (test strip) evidence only.
- Three musts: (1) Match-this-look challenge from an operator target image (theme/template minting by operator is fair game); (2) Videographic register proof — one slide's notes deconstructed into 6–8 forced cards (at-scale version = filed follow-on story, NOT built today); (3) Blow-your-mind flight — creative-trio recipes vs randomized cat-walk fader roll.
- Leg-D picker = canonical evaluation display; gammaUrl direct links accompany every render; **no exports until keeper confirmation**.
- Frozen probe corpus `style-test-strip-v1` carved verbatim from Tejal C1-M1; every style renders the same strip.
- Learnings persist at 3 layers: observations ledger (candidate rows), styleguide record prose, Marcus sanctum + gamma-api-mastery references (under audit-pin discipline).

## Binding amendments (consolidated; owner in brackets)

| # | Amendment | Source |
|---|-----------|--------|
| A1 | **Lifecycle is schema, not convention (BLOCKING).** Every registry record carries an explicit lifecycle marking — `candidate | permanent (production) | deprecated` — enforced at the validator write-gate; safe default = candidate. Existing 3 guides retro-marked permanent. Candidates carry `promotion_criteria` (B-corpus + re-run) + `authored_session` provenance. Picker visibly badges candidates. Acceptable fallback if schema surgery overruns: structural separation via a staging file the picker reads distinctly. | Winston + Dan (independently converged) |
| A2 | Videographic guide declares its **input contract** explicitly (expected beat-count range, beat-length register ~15–30 words, `\n---\n` delimiter convention) in its scripted/record prose, so the at-scale follow-on inherits a stated contract. | Winston + Gary |
| A3 | **Corpus version + provenance stamped into every ledger observation row** (strip version field + pointer to Tejal source). | Winston |
| A4 | Test strip extended to **FOUR slides**: + title/section-break slide (typographic voice; sparse-slide collapse test). | Sally |
| A5 | Picker thumbnails sourced from the **data/figure slide** (harshest) for all candidates; Flight-3 A/B verdicts require **full-deck gammaUrl eyeing** — thumbnails alone are insufficient (saturation bias). | Sally |
| A6 | Videographic record encodes **glance-card rules as written, testable constraints**: ≤8 words primary text (~12 hard ceiling), one idea + one visual per card, fixed anchor grid across the run, on-screen text = keyword SUBSET of concurrent VO (VO-echo, not paraphrase — protects VO↔on-screen invariant). | Sally |
| A7 | **Cat-walk winners are auditions, not candidates.** Any randomized-roll output that wins must be reverse-engineered through the Flight-1 discipline (register named in one sentence, removal test, honest avoid_when) before candidacy. Equal render budget for trio vs cat (no cherry-picked masterpiece). | Caravaggio + Dan |
| A8 | **Creative-intent bar for every record:** narrative states what the style *believes* (creative language, not parameter captions); honest non-generic `avoid_when` (a refusal, not a disclaimer); coherence rationale distinguishing load-bearing elements from TJU-brand paint (the brand-remix seam). | Dan + Caravaggio (removal test) |
| A9 | **Flight-1 `target_provenance` block:** target image source/context/rights; operator's creative intent verbatim; the deliberate discard list (attributes NOT matched); iteration delta (attempt 1 → converged); provenance line for any operator-minted theme/template. | Dan |
| A10 | **Instruction-neutral test-strip corpus** — no embedded directives/keywords in the strip, or the source-wins seam partially overrides every candidate identically and we A/B the corpus instead of the styles. | Gary (hard) |
| A11 | **Delimiter sanitization + empty-segment stripping** before any `inputTextBreaks` dispatch (stray markdown `---` mints phantom/empty cards). | Gary |
| A12 | **Unverified-model live failure policy:** 4xx = availability-witness FINDING recorded in the receipt; ONE retry with nearest verified same-family sibling; record annotated "witness failed, fallback used"; NO silent substitution. | Gary |
| A13 | **Spend fence on the cat-walk roll:** fence to non-Ultra tiers or pre-approved burn budget; Marcus owns the running generation/credit tally across all flights. | Gary |
| A14 | Flight-1 payload discipline: `stylePreset` unset or `custom` on every match-this-look dispatch (named preset silently kills the `style` prompt). | Gary |

## Creative input banked for Flight 3 (Caravaggio recipe sketches)

- **"Chiaroscuro Clinical"** — near-black warm-charcoal ground, single high-key TJU-brand accent on the one thing the learner must see; photorealistic preset, dramatic directional light on human hands/instruments; low text amount, declarative tone. Fills the photo/human-presence + dark-palette gaps in one identity.
- **"Broadsheet Density"** — cream ground, hairline rules, one ink + one accent, lineArt-leaning custom illustration, high text amount, small-multiples energy; density permitted only in service of comparison. Fills the data-dense gap; honest avoid_when: never for emotional/motivational beats.

## Mid-session operator directive (2026-07-02, Flight 2 open — binding on the close gate)

**Videographic-as-MODE reframe (operator):** if the videographic register proves out, it likely becomes a **mode** — orthogonal to style identity, like Classic/Studio — applicable to ANY style: same look, chunked to glance-beats. Consequences: (a) today's `videographic-glance-track` candidate record is the A-corpus PROOF VEHICLE, not the end-state shape; (b) if promoted, the promotion act generalizes the glance rules (Sally A6) + input contract (Winston A2) into an any-style overlay (candidate schema homes: a cadence/mode axis alongside `production_mode`, or an extension of the `scripted` declarative channel) rather than keeping a one-off record; (c) the videographic-at-scale follow-on story inherits this as its target shape. Winston reviews the schema question at the dual-gate close.

## Flight-2 A-corpus verdict (operator, 2026-07-02 — live deck ermz3b1cp9cxc81)

**MILESTONE — register CONFIRMED:** each card lands as a strongly at-a-glance idea (headline + ~1/3-screen bold image). First-bind witness DISCHARGED live: `gpt-image-2-mini` VERIFIED-IN-PRODUCTION, first attempt, no fallback (receipt `evidence/videographic-flight2-20260702T222437Z/`).

**Honest gaps (operator-named):**
1. **Stills are TOO independent (operator correction 18:35 — this is the defect, not a neutral trait).** The core videographic grammar: within a narrative run, consecutive cards inhabit ONE VISUAL WORLD, progressively related — "first a city, then a city block, then a house on that block." Each image is a deeper/nested view of the previous image's world, never a new scene. Fully-independent stills are legitimate ONLY for interstitial / sub-heading / chapter-heading cards. Decompose-across-slides + rebuild-on-final-slide is the cluster-level expression of the same continuity requirement. **Generalization (operator 18:37):** the relation between establishing shot and downstream shots need NOT be whole-to-part — before/after, above/below, cause/effect, and many other logical relationships expressed visually all qualify. The modality contract is therefore a DECLARED per-run continuity relation (relation type = a variable Irene authors per cluster at scale); whole-to-part is merely Flight 2b's instance. Connects to the filed D2 visual-disaggregation thread + Leg-C cluster machinery.
2. **Sub-headline text sometimes too limited** — the glance cap needs per-card-role budgets, not one flat ceiling.
3. **Transition beats render perfectly; want INTER-CLUSTER bridge cards** — denser text, images "roped together" (connected-line-of-climbers metaphor, NOT literal imagery): a second card grammar within the same modality.

**Operator architecture question (binding on the close-gate schema review):** a modality (e.g. videographic with forced breaks) IMPLIES coupled settings (text amount, font scale, card grammar). If a style can't adapt on the fly (Irene/Gary would), is a modality flag alone insufficient — do we need separate style groupings per modality even at 90% shared identity? Marcus's staged recommendation: modality as a FIRST-CLASS OVERLAY that declaratively OWNS its implied setting deltas, composed by Gary at resolve time (base style → modality overlay → source-wins). Separate near-duplicate records per modality rejected as sibling-drift guaranteed. Schema home decided with Winston + Dan at close.

## Flight-2b verdict (operator, 18:44 — deck mcxi28c7yjxlyaf) — THE UNCANNY-VALLEY FINDING

Intention expressed very well; **excellent bookending** (establish/rebuild). BUT: prose-requested LITERAL continuity across independent probabilistic generations lands in an **uncanny valley** — the establish shot's elements are "called back" downstream inconsistently (some callbacks exact, others only tangential; "a road on the establishing slide is a completely different road on the downstream slide"). A partial literal match reads as an ERROR where a symbolic echo or a clean miss would not. **Avoid this effect.**

Routing (two escape routes, both banked):
1. **Abstraction route (prose-compatible, testable now):** stop asking for literal same-scene continuity; declare a consistent VISUAL SYSTEM instead — shared palette-coding, iconographic vocabulary, compositional anchor — with downstream cards echoing the establish card SYMBOLICALLY, never attempting the same physical scene. Probabilistic generation holds a *system* better than a *scene*.
2. **Construction route (the at-scale build):** literal continuity requires deriving downstream images from the SAME master visual by construction (master render → deterministic crops/zooms/annotations across cluster slides) — the visual-disaggregation story; zero uncanny valley by design. Flight 2b is its concrete evidence line.

**Flight 2c (operator-directed):** re-express an ESTABLISHED permanent style (`classic-freeform-x-cards`) as a videographic-moded version of itself — the live exhibit for modality-as-overlay — using the abstraction-route continuity contract. Modality deltas prototyped in the driver (the overlay schema home is still the close-gate decision): dimensions fluid→16x9, forced beat-split, glance grammar, pinned now-verified model. Same frameworks beats as 2b for a direct continuity comparison.

## Flight-2c verdict (operator, 18:54 — deck 9t55j0susbvjel5) + pivot

**Better** (symbolic system > literal scene), but 2 of the 4 downstream images remain distractingly off. **Operator decision: DROP forced relationship-building** (prose-declared continuity contracts retired after two strikes; literal AND symbolic both under-deliver at production bar). The construction route (visual disaggregation from one master visual) stands as the only credible path to true continuity — evidence now twofold (2b uncanny valley + 2c symbolic residue).

**Pivot to the production-shaped question (operator, 18:54):** a typical source slide arrives with illustration notes, sometimes detailed DESIGN notes, written to describe ONE illustration for ONE slide. What happens when such notes get deconstructed into a 5-6 card presentation of the same material? This is the source-detail→Gamma conveyance invariant meeting the videographic modality head-on: the design note travels the source-wins channel — does every card try to render the whole described illustration (six near-copies)? Does the generator fragment it sensibly? Chaos? **Flight 2d** = the naive-threading experiment: a real Tejal design brief (The Tightrope & The Net, psychological safety — 50/50 split-screen composition, palette, labels) passed whole, narration chunked to 6 beats, classic base + modality deltas, NO continuity contract. The render IS the answer; note-decomposition strategies (per-beat fragment routing, Irene-side) follow from what we observe.

## Flight-2d verdict (operator, 19:02 — deck ohcbrhj0screxab) + the cluster-head grammar

**Findings:** (1) whole-note threading → SIX different depictions of the SAME intention (tightrope+net) — the one-slide design note dominated every card; intention preserved, differentiation lost. (2) **Model-behavior catch: `gpt-image-2-mini` rendered in-image text in JAPANESE** (possibly model default) — availability witness stands, but in-image text is unreliable on this model; ledger candidate row at converge; ideogram family remains the documented text-in-image choice.

**Operator-directed grammar (the production shape, feeds the at-scale story):**
- Irene identifies **cluster-HEAD slides**; the head carries the high-value image (the design-note illustration).
- **Children generate their own images ONLY if Irene deems one warranted**, and a child's image is prompted by THAT child's narrative/topic — never by the head's design note.
- Mechanism note: per-card image intent rides inside each beat's inputText segment (a card's image derives from its own content); deck-wide additionalInstructions cannot scope per card. Per-card image suppression is not API-reachable either — "only if warranted" is Irene-side authorship at scale.

**Text-treatment ask:** deck-wide amount is too coarse; want "a little more narrative on a slide, but narrative in LARGE font." Flight-2e fader: amount brief→medium + large-display-type instruction + English-only/no-text-in-images mitigation for the Japanese catch.

## Flight-2e verdict + B-companion consult round (operator 19:10/19:14; 5-seat party consult)

**2e findings:** in-beat image intent STILL bled deck-wide (routing failed at the API layer — one generation = one shared visual brief, Gary confirmed as structural, not sanitizable); text presentation MUCH BETTER (amount=medium + large-display-type = the register's canonical text recipe). **Model finding (operator):** `gpt-image-2-mini` renders anime-register imagery + Japanese in-image text (training-origin default) — witness stands, aesthetic unsuitable for the clinical register; v2 record swaps to `recraft-v4` (catalog's proven editorial-medical-illustration family; swap not yet live-rendered — first B-corpus render witnesses it).

**B-companion consult (Winston/Irene/Sally/Gary/Dan, fully spawned; agent IDs on file):**
- **Winston:** per-source-unit anchoring dissolves the one-to-one arithmetic (unit → A-card(1) OR B-glance-run(N), joined via slide_key→plan-unit); but per-unit interleave = **Storyboard-gate-v2 semantics** (compose a deck FROM two decks per unit — a third kind of pick, party-governance territory; touches storyboard publisher, segment manifest, narration join, Gary resolve-time composition). Bank prose earmark only today.
- **Irene:** **NOT confused, provided** the interleave is declared in plan units before Pass-2: each unit tagged `A | B-run` with beat count N FROZEN at the Storyboard gate; one narration span per unit (a B-run = one span internally segmented into N beat-sentences of ~12-18 words ≈ 5-6s each); **card keywords derive FROM her narration** (VO-echo direction), never authored first. Refusals: beats after narration; register switches mid-unit; un-counted B-runs; new figures smuggled by glance compression.
- **Sally:** mixed cadence is GOOD attention design when motivated (glance-runs for arcs/transitions; exposition for reference content); rhythm rules: min run 4 cards, max ~10-12 then a "landing card", no A/B ping-pong (≥90s between switches), announce transitions. Picker needs: filmstrip preview of the run, duration estimates on both options, and a **lesson-level cadence bar** (the aggregate view where ping-pong failures actually live). New card role coined: **landing card** (run-closer, heavier budget).
- **Gary:** interleaving is downstream of dispatch (composition-manifest bookkeeping: deck+ordinal → timeline position); per-run dispatch is the honest isolation unit when runs diverge visually (rate/cost: generations not cards — batch same-register runs); registry earmark fields his seam would actually read: `companion_theme_ref` + a canned B-run instruction block — everything else is orchestration state. Source-wins precedence must survive the companion block.
- **Dan:** dictated the v2 record edit (applied below); B-companion earmark = **prose in use_when + one provenance line, NO new field** (schema home reserved for close gate); lifecycle stays candidate, promotion criteria unmoved.

## Banked this round (all validator-green; 99 styleguide/picker/ledger tests pass)

1. **`videographic-glance-track` v2** (Dan-dictated): text conviction rewritten (narrative-paragraph-in-LARGE-display-type; old ≤8-word cap DELETED); continuity PROHIBITION in avoid_when (uncanny-valley law; construction-route pointer); cluster-head input contract + 2e shared-brief caution in coherence; in-image-text directive with model-scoped provenance; model swap → `recraft-v4`; B-companion earmark prose in use_when; lifecycle candidate unchanged.
2. **Five ledger candidate observations** (corpus-stamped per A3, digest-idempotent): forced-split reliability; gpt-image-2-mini verified+Japanese/anime finding; prose-continuity uncanny valley; one-generation-one-visual-brief; the videographic text-fader recipe.
3. Follow-on queue for the close gate: videographic-at-scale story (now carrying Irene's gate contract + Sally's rhythm rules + Storyboard-gate-v2 semantics + per-run dispatch economics), modality-overlay schema home, B-companion formalization.

## Standing risk notes

- Winston: theme-swap survival test (style survives brand remix) = latent proof of the brand-carrier architecture — file as follow-on, not today.
- Gary: preserve-mode protects text only (layout/images still embellished); rate limits count generations not cards.
- Sally: videographic proof should ultimately be judged with VO played over it (production-time; today is style-only).

*Record author: Marcus (orchestrator). Full verbatim seat responses in session transcript; agent IDs on file.*
