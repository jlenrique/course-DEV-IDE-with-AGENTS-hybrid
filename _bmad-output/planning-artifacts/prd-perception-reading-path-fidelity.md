---
stepsCompleted: ['step-01-init', 'step-02-discovery', 'step-02b-vision', 'step-02c-executive-summary']
classification:
  projectType: 'multi-agent LLM content-production pipeline (backend/AI)'
  domain: 'EdTech / online course-content production'
  complexity: 'high'
  projectContext: 'brownfield'
inputDocuments:
  - '_bmad-output/planning-artifacts/beta-phase-1-closure-ratification-2026-06-19.md'
  - '_bmad-output/planning-artifacts/deferred-inventory.md'
  - 'docs/STATE-OF-THE-APP.md'
  - '_bmad-output/implementation-artifacts/upstream-severance-log.md'
  - 'skills/bmad-agent-content-creator/references/pass-2-grammar-riders-examples.md'
  - 'docs/project-context.md'
  - '_bmad-output/planning-artifacts/beta-spec-2026-06-19.md'
documentCounts:
  productBriefs: 0
  research: 0
  brainstorming: 0
  investigations: 0
  projectDocs: 5
workflowType: 'prd'
authority: '_bmad-output/planning-artifacts/beta-phase-1-closure-ratification-2026-06-19.md §7'
---

# Product Requirements Document - Perception + Reading-Path Narrative-Grounding Restoration & Enhancement (P2)

**Author:** Juanl
**Date:** 2026-06-19
**Authority:** `beta-phase-1-closure-ratification-2026-06-19.md` §7 · **Green-light:** party-mode §2, 2026-06-19 (Winston/John/Murat/Mary/Amelia — APPROVE-WITH-AMENDMENTS, applied below)

## Executive Summary

The course-content pipeline produces narrated slide decks: a corpus is ingested, a directive composed, storyboards drafted, slides rendered in Gamma, narration generated by ElevenLabs, QA'd at G5, and assembled for hand-off. As of 2026-06-19 the pipeline runs end-to-end and completes "**error-free (mechanics only — fidelity unverified)**" — the §6-ratified rebrand that holds until the P2-1 detector lands — but the narration is **grounded on the slide *brief* (what was requested), not the rendered slide (what the audience sees).** When Gamma restyles a briefed figure — which it routinely does — the narration confidently describes visuals that were never rendered.

**Two failure modes, distinct evidentiary strength (Mary):**
- **Perception loss — render-proven.** Direct, self-incriminating evidence: `_bmad-output/implementation-artifacts/evidence/narration-hallucinates-illustration-slide01-2026-06-19.png` (Storyboard-B slide-01). RENDERED = `$4.5T / 74% / 3x` stat callouts + a building photo. NARRATION = *"the upward line toward $5.2 trillion … the paired bars contrasting Independent Practice vs Hospital Employment"* — a dual-axis line+bar chart never rendered, wrong figure ($5.2T vs $4.5T). The narrated visual matches the ORIGINAL brief (`extracted.md` slide-1) **verbatim** → causal proof that Pass-2 grounds on the brief, not the render.
- **Reading-path loss — severance-log *inferred* (not render-proven).** The scan-order classification machinery (Z / F / center-out / triptych enum + `reading_path` field + emission lint + heuristic classifier) was a 5-part lockstep upstream; per `upstream-severance-log.md` (severance @ `3ed7c56`, 2026-04-24) only the worked-examples doc was absorbed. This leg's evidence is the severance record + the standing operator complaint ("narration doesn't follow the natural scan of the slide"), not a render-grounded smoking gun.

This PRD scopes the **repair and enhancement** of narrative grounding. It restores perception via a vision pass over the exported PNGs that yields a `PerceptionArtifact` of what is *actually* on each slide (elements, positions, numbers); rewires Pass-2 to narrate from perceived visuals; natively rewrites reading-path classification so narration follows the slide's scan order; and — co-equal in priority — ships a **fail-loud fidelity detector**.

**The detector is the load-bearing deliverable, and it is two-sided and sequence-isolated (Murat, binding):**
- **Two-sided:** it must both *catch* the slide-01 `$5.2T`-vs-`$4.5T` failure AND *pass* a known-good fidelity case. A detector that only ever fires is a stuck alarm, not a gate.
- **Sequence-isolated / RED-first:** it is its **own story (P2-1), NOT folded** into the repair, and lands **RED against the un-repaired trial commit** — proving it catches the failure *before* the perception/Pass-2/reading-path repair turns it green. This is the only defense against the "test tuned to the buggy output" / vacuous-pass trap.

**Target users:** the operator and the specialist agents (Irene, Gary, Vera, Quinn-R) on the production trial path. **Problem solved:** narration faithful to the rendered deck and *provably* so — eliminating authoritative-sounding hallucination and restoring trust in the "error-free" signal (shedding its "(mechanics only)" asterisk once P2-1 lands).

### What Makes This Special

Conventional slide-narration tooling grounds on the author's brief. This product grounds on the **rendered artifact** and narrates in **scan order**, then **proves fidelity with a gate that can see**. The load-bearing insight: *a quality gate blind to the slide is worse than no gate — it manufactures trust in wrong output.* The durable fix is therefore a detector that makes hallucination fail loudly and stands as a permanent regression guardrail.

**ONE perception authority — with an explicit precedence order (John/Winston):** the PNG-grounded `PerceptionArtifact` is the *sole authority for narration fidelity* — it is what Pass-2 grounds on and what the detector consumes. Vera's brief-derived perception is **demoted to a secondary *expectation* signal** (brief = intent; PNG = truth): it may be diffed against reality but **may not assert reality**. This is a single authority with a precedence rule, not two co-equal sources — the distinction that prevents the A23 two-source-of-truth fork (the exact failure class that bit the §02A→wrangler boundary). The detector reads the `PerceptionArtifact`; it does **not** run its own independent vision inference (else it becomes a back-door second perception source).

### Scope boundaries & regime exposure

**In scope:** perception over rendered PNGs (P2-2), Pass-2 consumption (P2-3), reading-path native rewrite (P2-4), the fidelity detector (P2-1), and modest pattern-repertoire enhancement (e.g., triptych / image-dominant-first) where Storyboard-B evidence shows mis-fit.

**Out of scope:** motion synthesis (carved, §4); the voice↔WPM fix (that's P1, in flight — no overlap); re-absorbing upstream reading-path code (native rewrite only, per §7 P2-4).

**Pipeline-lockstep regime exposure (Winston, binding — surfaced here, not inherited silently):** the vision pass is a **new pipeline step** (placed between §07 export and §08 Pass-2) — a textbook **Tier-2 (minor) pack/manifest event** under the versioning policy → **pipeline-lockstep regime + party-mode green-light BEFORE dev** for any manifest/pack touch (the vision step, `pass_2_template.py`, pattern-repertoire). The PRD body MUST additionally specify: (a) the **failure semantics of the new §07→vision→§08 seam** — what Pass-2 does when the vision pass fails or returns low-confidence perception (hard-fail vs degraded-but-flagged), so the one-authority decision holds under failure rather than silently degrading back to brief-derived; and (b) the named edit sites for P2-3 (`app/specialists/irene/graph.py` `_assemble_pass_2_prompt`/`_slide_roster` + `irene/authoring/pass_2_template.py`) and P2-2 (the `PerceptionArtifact` schema-shape story).

### Governance riders (binding)

- **§6 rebrand:** "error-free" reads **"error-free (mechanics only — fidelity unverified)"** everywhere until P2-1 lands.
- **§6 DoD gate (Mary):** every P2 story carries a gating checkbox to **strike/update its deferred-inventory entry + harvest cross-trial-learnings**. Specifically, this arc **strikes both source entries** (`beta-narration-hallucinates-nonexistent-illustrations` + `reading-path-enforcement-machinery-not-absorbed-at-severance`) and **creates one combined arc entry**, with bidirectional citation between the struck entries and the arc.

## Project Classification

- **Project Type:** Multi-agent, LLM-in-the-loop content-production pipeline (backend/AI system) — Irene→Gary→Vera→Quinn-R path plus a new vision-perception stage and a QA fidelity detector.
- **Domain:** EdTech / online course-content production.
- **Complexity:** High — LLM + vision model in the loop; Pydantic-v2 schema-shape discipline; pipeline-manifest lockstep risk; an anti-correlated QA metric requiring RED-first detector discipline.
- **Project Context:** Brownfield — regression repair (post-severance) + enhancement on a shipped pipeline.
