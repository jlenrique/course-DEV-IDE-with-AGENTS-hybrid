# gamma-image-model-enum-refresh — GREEN-LIGHT Party Record (2026-07-02)

**Session:** autonomous overnight (Task 2 of `goal-carried-findings-enum-refresh-2026-07-02.txt`). **Branch:** `dev/carried-findings-enum-refresh-2026-07-02`.
**Party:** fully spawned, 6 seats — Winston, John, Murat, Amelia (canonical core) + Gary (Gamma specialist) + Dan (CD). **Verdict: GREEN-LIGHT 6/6, RATIFY-WITH-AMENDMENTS.**
**Trigger:** MET at Leg-E close — learned-store rows `obs-gamma-enum-parity-image-model-2026-07-02-3b951096` (doc-drift) + `obs-gamma-enum-parity-image-model-coverage-gap-2026-07-02-1bc1d8d2` (coverage-gap), doc URL `https://developers.gamma.app/reference/image-model-accepted-values.md`, evidence `leg-e-gamma-docs-audit-20260702T043139Z`.
**Codex shadow-monitor:** standing session-04 ledger consulted; no live monitor lane this session (recorded honestly).

## Scope
Reconcile `app/specialists/gary/_act.py::IMAGE_MODEL_VALUES` with live docs: REMOVE `imagen-4-fast`, `qwen-image`, `qwen-image-fast`; ADD `flux-1-pro`, `flux-1-quick`, `flux-1-ultra`, `flux-kontext-max`, `gpt-image-1-mini-high`, `gpt-image-1-mini-low`, `gpt-image-1-mini-medium`, `gpt-image-2`, `gpt-image-2-hd`, `gpt-image-2-mini`, `imagen-3-pro`. Lockstep surfaces: `state/config/schemas/creative-directive.schema.yaml` + `.json` (same enum embedded), `skills/gamma-api-mastery/references/parameter-catalog.md`, `tests/test_audit_gamma_docs_driver.py` (assertions ride the real drift). Scouted: no shipped styleguide binds the stale trio; no pipeline block-mode trigger paths; `enum-parity-image-model` is the only exit-driving drift item (P6 exclusions).

## Ratified decisions

**D-1 STRICT DOC-PARITY (6/6).** Enum == documented accepted values. Additive-only makes the enum a third authority and permanent audit noise; deprecation-window has zero consumers. A doc-absent model that might still work live is an observed-tier promotion question for the DEFERRED promotion-path story. Removal called out legibly in change-set notes.

**D-2 LOCKSTEP + THE PARITY PIN IS THE ACTUAL DELIVERABLE (6/6).** All surfaces move in ONE change-set. MANDATORY new hermetic three-way parity pin: schema yaml ↔ schema json ↔ `IMAGE_MODEL_VALUES` set-equality (not subset). T1 discovery (Amelia, hard gate): json-from-yaml generation story (generated → pin json==generate(yaml); hand-edited or unknown → three-way pin). Murat's non-vacuity rider: the pin must be WITNESSED RED mid-sequence (refresh enum first, pin goes RED against stale schema, then refresh schema → GREEN). Single-SSOT (one constant, others derived) noted as legitimate FUTURE story (Gary/Winston) — out of scope; do not gold-plate. Catalog is prose: convention-synced in the same commit (optional non-gating mention-pin).

**D-3 PROVENANCE COMMENT (6/6).** Minimal, dated: doc URL + refresh procedure (`scripts/utilities/audit_gamma_docs.py`) + last-reconciled date, at the frozenset and in the schema YAML (not the json if generated). Pointer, not mirror — the ledger rows are the audit trail.

**D-4 SYNTHETIC-DRIFT FIXTURE (6/6); re-pin-to-confirmed REJECTED.** Re-pinning deletes the only RED-path witness of drift detection and re-couples tests to live-world state. Fabricated doc fixture, loudly named `synthetic_drift_*`, commented as fabricated (L-3), planted deltas in BOTH directions (value-in-enum-not-docs AND value-in-docs-not-enum), exact-diff assertions (exact-match only — `qwen-image` is a prefix of `qwen-image-fast`, Murat's substring hazard). Retire the :413/:416 stale-string assertions; a captured-real fixture test may remain for STRUCTURE only (sections parse), never specific model names.

**D-5 LIVE PROOF + LEDGER DISCIPLINE (6/6).** Live bar: re-run the audit driver against real developers.gamma.app (expected $0, no key; a 401 = investigate [known burst-throttle-masquerade gotcha], never loop-retry), FIRST-RUN-STANDS: `enum-parity-image-model` → confirmed, exit 0; any NEW unrelated live drift = reported finding, not a retry, not a close-blocker. LEDGER: the driver keeps its WRITE MONOPOLY — zero hand-authored ledger mutations (unanimous; hand-written rows would poison the store this arc just proved trustworthy). Dan's honesty rider: persisting `status: candidate` after acting is a lie in the other direction → per Dan option 2: (a) post-change driver re-run artifact in evidence, (b) story record cross-references the two candidate row ids → resolving commit SHA, (c) file **`gamma-ledger-resolution-row-kind`** follow-on into deferred-inventory, attached to the promotion-path story. If the re-run fails to flip the item, that is a driver gap FINDING, not a license to hand-craft provenance.

**GARY'S LIVE-AVAILABILITY PROBE — reconciled (orchestrator synthesis, Gary's own stated condition):** NO paid render rides in this story (John's fence #4; the enum is an availability gate, not a quality gate; nothing binds a new model until a styleguide does). CONDITIONS (binding): (1) every catalog/enum-adjacent entry for a never-rendered model carries a MACHINE-READABLE marking `provenance: documented-tier, unverified-in-production` (Dan: disclose-don't-forbid; must be displayable by the Phase-2 authoring surface, like the picker's approximation chip; clears ONLY on a real production render — another doc read does not clear it); (2) Phase-2's FIRST guide binding a newly-added model is DESIGNATED the live availability witness (recorded as a rider in the story + deferred-inventory). Gary's asymmetric-risk warning stands in the record: docs-ahead-of-API is the bite risk — a live rejection of a newly-added model at Phase-2 first-bind is a FINDING feeding the learned store, exactly the designed loop.

**DAN-1 CEREMONY DISCLAIMER (blocking):** story + party record carry verbatim-level: *"Manual reconciliation citing learned-store observations obs-gamma-enum-parity-image-model-2026-07-02-3b951096 + obs-gamma-enum-parity-image-model-coverage-gap-2026-07-02-1bc1d8d2 (doc URL + fetch 2026-07-02T04:31:39Z). The promotion-path ceremony (automation-proposes / CD-ratifies-via-envelope) remains UNBUILT; this change does not instantiate it and is not a template for skipping it."* Plus: the REMOVALS need demonstrated (not asserted) evidence — grep/validator run over `gamma-style-guides.yaml` + persisted directives showing zero stale bindings; any persisted hit flips removal into a migration (stop + rescope).

**JOHN'S SCOPE FENCE (binding):** must NOT ride in — promotion-path automation; catalog tier redesign (rows change, taxonomy doesn't); keywords→imageOptions.style; per-model quality vetting/test renders; styleguide edits; enum-refresh automation. Amelia's L-5: the "Unclassified — never rendered, tier TBD" catalog row is EXPLICIT in the AC text (no invented tiers; cite Gamma's own credit/pricing doc verbatim if it states tiers).

**GATE MODE (synthesized):** votes were 4 dual (Winston/John/Murat/Gary) vs 2 single (Amelia/Dan) — but Dan (the proposed second gate) declined co-gate in favor of a **blocking close-review checklist**, which satisfies the dual votes' intent (the CD voice gates the schema change with hand-back power). RATIFIED SHAPE: **Murat structural gate + Dan scoped-blocking close review**, Dan's four named hand-back conditions: (1) three surfaces byte-consistent + parity pin present; (2) machine-readable unverified-in-production marking present (not prose-only); (3) DAN-1 disclaimer in the story record; (4) driver re-run evidence present. Escalation trigger (Amelia): if T1 discovery finds a FOURTH embedded enum copy, escalate to full dual.

## Murat's close bars (structural gate)
(a) doc-fixture-vs-enum test RED at HEAD witnessed (exact 11-missing/3-phantom sets); (b) parity pin RED witnessed mid-sequence; (c) flipped-symbol sweep grep evidenced (all 14 value strings + `IMAGE_MODEL_VALUES` + `len(` count pins), exact-match only; (d) synthetic-drift detection test present post-refresh (both directions); (e) live driver exit 0 first-run-stands, driver-written rows only. Amelia's landmines L-1..L-7 in the dev brief (esp. L-4: grep `_bmad-output/`, `state/`, run dirs for runtime-reachable stale directives — persisted hits need explicit disposition at T1).

## Sizing
S; Amelia honest range 3.5–4.5h (K≈1.3). Dispatch: spawned dev agent RED-first per Murat's sequence → 3-lane `bmad-code-review` → live proof → Murat close + Dan checklist.
