# Carried-Findings Remediation — GREEN-LIGHT Party Record (2026-07-02)

**Session:** autonomous overnight (operator away until morning; autonomous consensus binding per standing governance).
**Branch:** `dev/carried-findings-enum-refresh-2026-07-02` (fresh off consolidated master `c0ce283b`).
**Goal:** `goal-carried-findings-enum-refresh-2026-07-02.txt` Task 1.
**Party:** fully spawned, 4 seats — Winston (architecture), John (PM), Murat (test architect, load-bearing + gate-closer), Amelia (dev). Each seat ran as an independent subagent with the full evidence brief (git archaeology + reproductions gathered this session).
**Verdict: RATIFY-WITH-AMENDMENTS, 4/4 on every decision. No impasse; no Quinn/John escalation.**
**Codex shadow-monitor:** standing ledger (session-04, `marcus-gamma-styleguide-shadow-monitor-2026-07-02-session-04.md`) reviewed at this gate; no live monitor lane is running this session (recorded honestly). Relevant standing guidance honored: vision-recording dirt treated as active context (finding (c) fixes its source); RED-first/live-proof discipline.

## Scope
The four Class-S START-gate findings carried from session 04 (SESSION-HANDOFF §session-04 Unresolved #1–2). Evidence pack (this session): (a) fixture at 125 since birth `fbd161cc`; assertion flipped 125→135 by `195be7c9` (mechanical sweep, no justification, message disclaims semantics changes); pr_rc does no arithmetic. (b) test oracle = bare `join_narration_segments` (dp-v1.2); publisher applies documented post-join export projection since 1.3-carry (+P5 voice_direction, +enhanced-vo.1 slide_key, conditional-on-presence). (c) `llm_live` auto-skips ONLY on missing/placeholder key (2026-04-24 semantics); real key in `.env` → real gpt-5.5 spend in DEFAULT pytest profile; 13 files carry `llm_live`; recapture writes `source_png_path` absolute (provider `str(path)` — correct at runtime, unportable in fixtures). (d) 20 pre-existing contracts-tree failures at HEAD, enumerated + reproduced (exit shows 20 failed / 293 passed / 1 skipped).

## Ratified decisions

**D-A (finding a): revert assertion to 125.0** — bad mechanical edit; fixture is the truth; do NOT edit the fixture.
Amendments (binding): (1) establish current color at HEAD first — if it somehow passes at 135, STOP (premise falsified; find the 135 source). (2) Mutation witness: scratch-run fixture→130 must go RED (proves the assertion reads the fixture-derived value). (3) Audit ALL test hunks of `195be7c9` for any change that is not a `marcus.X→app.marcus.X` string edit or documented contract-count bump; sweep 135/125 pins referencing this fixture across all batteries. Sibling findings → the (d) disposition ledger. (4) Check no sibling test consumes the same fixture value.

**D-B (finding b): option 1 — extract the post-join projection into a named pure function** in `storyboard_publisher` (e.g. `enrich_segments_for_export(segments, deltas, arc_map)`); byte-stability test asserts `yaml(project(join(...)))`. Options 2 (hand-replication fork) and 3 (subset weakening) REJECTED 4/4.
Amendments (binding): (1) characterization golden captured BEFORE the refactor; post-extraction byte-identity witnessed (mechanical lift only; mutate-in-place kept; Edge-#4 ambiguous-delta-id guard moves VERBATIM + gains an ambiguous-path fixture). (2) Anti-tautology architecture per Murat: byte-equality + mandatory literal anchors (≥1 per projected key family: cluster_id/cluster_role/cluster_position/narrative_arc/voice_direction/slide_key) + a projection unit test with HAND-WRITTEN expected literals covering delta-value-wins, both conditional branches, and an explicit pre-era byte-identity pin. (3) RED witness: comment out the cluster_role copy line → projection unit test AND ≥1 byte-test anchor go RED; witness archived in evidence. (4) Frozen `join_narration_segments` neck READ-ONLY throughout; publisher-side only. (5) `yaml.safe_dump` kwargs pinned identically in test and writer. (6) If the lift turns non-byte-neutral mid-flight: STOP, that item escalates (gate-mode carve-out).

**D-C1 (finding c, gating): GLOBAL flip** — `llm_live` ALSO requires `--run-live` (pass-1 deselection), pass-2 key-skip RETAINED (graceful skip when `--run-live` without key). Narrow re-mark REJECTED 4/4 ("fixing 1 of 13 files spending real money is a demonstration, not a remediation" — Murat).
Amendments: (1) gate-semantics contract test — RED at HEAD (llm_live runs with real key) → GREEN after flip; without `--run-live` all llm_live deselected EVEN with a real key; with `--run-live` all collected. (2) Rot guard: `--run-live --collect-only` collects the full llm_live set (pin count/module set). (3) Loud deselection (John): visible deselection reporting so live batteries can't die silently. (4) Marker-registration docstring updated at the definition site (Winston). (5) Verify `--run-live` + no-key → SKIP not ERROR; document that `--run-live` is the switch, not `-m llm_live`. (6) Triage confirms no standing battery depends on default-profile live execution.

**D-C2 (finding c, capture portability): normalize `source_png_path` to repo-relative posix at the CAPTURE layer** (test-side); runtime provider stays absolute (correct split).
Amendments: (1) out-of-repo-root at capture time → FAIL LOUD (Murat; orchestrator-reconciled over Amelia's warn-fallback — the runtime path, which legitimately sees absolute paths, is untouched); unit-test both branches; `resolve()` both sides before `relative_to` (Windows drive-letter case). (2) Fixture-hygiene guard (Winston): a test that fails loudly if any committed recording contains a drive-letter/backslash path. (3) Legacy-recording disposition (Murat's spec demand): committed recordings ALREADY hold portable form — they stay as-is; the D-C3 witness output is archived as evidence and reverted from the tree (recaptured LLM content is nondeterministic; committing it is fixture churn beyond this batch's scope).

**D-C3 (finding c, live proof): BOTH witnesses, sequenced** — (1) $0 default-profile deselection witness FIRST (collect-only + default run proving deselection with the real key present); (2) ONE real `--run-live` recapture, first-run-stands, written recordings inspected for repo-relative posix form. If perception anchors flake on variance, the portability judgment still stands on the written files; the flake is REPORTED, never retried-to-green (anchor variance itself is out of batch scope). Evidence archived under `_bmad-output/implementation-artifacts/evidence/`.

**D-D1 (finding d): one spawned triage pass, READ-ONLY FIRST** — complete 20-row disposition ledger BEFORE any test edit, at `_bmad-output/implementation-artifacts/contracts-triage-ledger-2026-07-02.md`.
Categories (Murat, binding): **L** legitimate-drift→repin (ONLY with named commit + governing story/party artifact; mechanical sweeps NEVER qualify; production output NEVER self-justifies), **R** regression→keep test, spawn separate fix item (production fixes EXIT this batch), **T** test-defect→rewrite with justification, **O** obsolete→delete citing superseding story, **E** escalate/defer (honest DEFERRED-UNADJUDICATED; never silent skip, never quiet repin).
Time-box (John, binding): **hard 90 minutes**; box expiry → remaining rows default to E/documented-baseline. Tier fence (Amelia): Tier-1 fit_report ×5 + modality_registry ×4 (2 family root-cause investigations covering 9 rows; family analysis allowed, family DISPOSITION forbidden) → Tier-2 adjudicate-only (marcus routing ×2, 33-2 disjoint-keys, transform-registry; fix only if <5 min) → Tier-3 may land E (roster-placeholder, quinn-r-gate, 33-1a, 30-1, blueprint-producer, specialist-error-taxonomy). RED-first per repin: RED at old pin / GREEN at new, provenance cited per row. Deferred-inventory gets ONE aggregate entry pointing at the ledger (no shadow register).
Heightened-suspicion rows (Winston): marcus routing pair + 33-2 disjoint-keys — invariant-class pins whose violation IS the regression; 33-2 reads like a real collision.

**GATE-MODE: single-gate (Murat structural) for the batch**, with: (1) L/O ledger rows countersigned by the Acceptance-Auditor lane of the 3-lane `bmad-code-review` (opens each cited artifact, confirms it authorizes the drift — Murat's self-review guard); (2) carve-outs — a non-byte-neutral (b) extraction, or any R-row production fix, exits the batch and takes its own gate mode.

**Sequencing (John, binding):** (a) → (c1+c2+c3) → (b) → (d) 90-min box → Task-2 enum-refresh with protected budget. If squeezed: (d) Tier-2/3 drop to ledger-only first; (b) and (c1) are non-negotiable.

**Cross-finding pattern (Winston, filed as observation):** (a) and (d) share a root cause — pin edits landing inside large mechanical sweeps without per-assertion rationale. Candidate future guardrail: pin-file changes in multi-file sweeps require stated per-assertion rationale in the commit body. Noted in the ledger; not this batch's scope.

## Dispatch
Spawned dev agent (findings a→c→b, RED-first per amendments) + spawned triage agent (finding d, read-only ledger first) — dispatched in parallel post-ratification. 3-lane `bmad-code-review` follows dev completion; Murat structural close after remediation + live witnesses.

## Task-3 sequencing round (2026-07-02 session 05, ~02:45; John/Murat/Winston, 3/3)

**Pick: (A) contracts-residuals — (i) row-20 error-taxonomy re-base MANDATORY; (ii) row-14 transform-registry application CONDITIONAL behind a spawned Texas-owner regex sanity check (detaches at any ambiguity — "opportunistic completion", never blocks (i)).** (B) keywords→imageOptions.style REJECTED for tonight on invariant grounds (protected source-detail→Gamma conveyance + paid A/B without the operator's eye); (C) promotion-path ceremony REJECTED on ownership grounds (CD ceremony shape is operator-voice territory). Both stay filed for operator-present sessions and go in the morning brief.

Ratified for (i): re-base `VoiceProviderTextError` (enhanced-VO v3) + `StyleguideError` (gamma Leg-A) onto `SpecialistDispatchError`, template = the 37f8323 taxonomy-rebase tranche. Murat's binding bars: (1) RED-first witness of PIN-AUD-3T captured pre-fix; (2) prove the ratchet is non-vacuous (behavioral/subclass assertion, not name-presence) — if vacuous and not cheaply tightenable, fall back to NONE + finding; (3) catch-site audit as an ENUMERATED TABLE (file:line, verdict unchanged/now-also-caught/narrowed) — MRO preservation shown per site, not argued; (4) no-regression sweep incl. the error-pause family tests 37f8323 protected; (5) pre-declared honest pause-point: any catch site whose semantics genuinely change on a judgment call = STOP with the table written. John's stop-triggers: audit fans out into redesign (2h budget check); Texas flags row-14 regexes → row-14 drops; any live-path behavior change beyond crash→error-pause touching protected invariants = pause, don't rationalize; hard floor T-minus-45-min → freeze at last green, document, WRAPUP. Gate: Murat single-gate structural (gate-owner's own call — acceptance artifact predates dev; Winston's dual preference noted; the 3-lane review retains the Auditor lane).
