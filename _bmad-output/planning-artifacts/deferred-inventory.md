# Deferred Inventory â€” Standing Index

**Purpose:** canonical register of every deferred epic, deferred story, and named-but-not-filed follow-on across the project. Single source of truth consulted at:

1. **Every Epic retrospective** â€” per [CLAUDE.md Â§Pipeline lockstep regime](../../CLAUDE.md) and Â§Deferred inventory governance, the retrospective MUST review this inventory against the closing Epic's new substrate / evidence / learnings; flag now-ready-to-reactivate entries to the next sprint-planning round.
2. **Every session hot-start** â€” `next-session-start-here.md` surfaces the inventory counts so the operator sees "don't overlook" context every session open.
3. **Story authoring** â€” when a new story spec names a follow-on (e.g., "15-1-lite-irene is a fast-follow after this story"), the author adds the follow-on to Â§Named-But-Not-Filed Follow-Ons below.

**Maintenance:** update at (a) each Epic retrospective close, (b) each story closure that names a new follow-on, (c) any session-wrapup where the operator flags a new deferred item, (d) each forward-port pass on the hybrid migration branch (see Â§Forward-Port Deferred below). Last refreshed: **2026-04-28** (**Slab 6 trial-experience bundle 3/3 CLOSED.** Slab 6.4 closed today after third-pass verification-only re-trace clean + operator Gate 5 dual-gate ceremony PASS via `scripts/operator/gate5_slab_6_4.py` 83 passed; Mary harvest-gate A18 disposition: leave as candidate. **First tracked trial UNBLOCKED.** Earlier same day: 6.3 + 6.5 CLOSED after second-pass verification-only re-trace `8b165e8` confirmed CLEAN. Prior 2026-04-27: Slab 6.2 formal close: `migration-6-2-promote-dependency-map-into-manifest` RESOLVED — manifest dependencies promoted; alias-drift A9 patch landed; Composition Spec §12 known limitation #1 closed; Slab 6.3 + 6.4 + 6.5 trial-experience bundle UNBLOCKED. Slab 6.1 formal close earlier same day: `5a-2-production-graph-entrypoint-substrate-gap` RESOLVED, six Slab 6.1 close deferred-inventory entries filed, M5 condition #3 RESOLVED, migration verdict promoted to unqualified SHIP).

---

## Backlog Epics â€” Full Scope Deferred

| Epic | Focus | Stories | Story count | Reactivation trigger |
|---|---|---|---|---|
| **Epic 15** | Learning & Compound Intelligence â€” convert tracked runs into organizational intelligence | 15-1, 15-2, 15-3, 15-4, 15-5, 15-6, 15-7 | 7 | "At least one tracked trial run completed" (hard dependency per epic seed). 15-1-lite-marcus (Epic 33 meta-test) unlocks the chain's first link via its learning-event ledger infrastructure. 15-2 (retrospective artifact) is the natural next link once the ledger exists. |
| **Epic 16** | Bounded Autonomy Expansion â€” Marcus autonomous routing on routine decisions | 16-1, 16-2, 16-3, 16-4, 16-5 | 5 | Depends on Epic 15 evidence base for autonomous-routing calibration. |
| **Epic 17** | Research & Reference Services â€” related-resources + citation injection + hypothesis research | 17-1, 17-2, 17-3, 17-4, 17-5 | 5 | No explicit trigger named; appears to be operator-priority-driven. Re-evaluate at Epic 15/16 retrospectives. |
| **Epic 18** | Additional Assets & Workflow Families â€” cases, quizzes, discussions, handouts, podcasts, diagrams, workflow-family framework | 18-1, 18-2, 18-3, 18-4, 18-5, 18-6, 18-7 | 7 | **18-7 gates all others** (implementation framework must land first). Trigger for 18-7: operator-priority decision on new content type. Note: 18-5 (podcasts) + 18-6 (infographics) are "new pack types" per [Pipeline Manifest Regime Â§Pack Versioning Policy](../../docs/dev-guide/pipeline-manifest-regime.md#pack-versioning-policy) â€” NOT v5 of narrated-lesson; they ship as new families. |
| **Post-M5 Greenfield Specialists** (hybrid migration) | Specialists named in Epic 2b roster that have no skill directory on disk and cannot be migrated (Mike, Eli, Enrique, Mira, Sally, Kim; Paige if scoped as runtime specialist). Empty sidecar stubs exist under `_bmad/memory/<name>-sidecar/` but no code, no prompts, no references. | one per specialist as need materializes (6â€“7 total) | 6â€“7 | **Hybrid migration M5 closure.** Generated directly on hybrid via `bmad-create-specialist` (the generator validated in Slab 2c.1) when the operational need for each specialist becomes concrete. No migration; pure greenfield on the migrated platform. See [`slab-2-roster-reconciliation.md Â§Category E`](slab-2-roster-reconciliation.md) for roster detail. |

**Total deferred-epic story count: 30â€“31 stories across 5 epic slots.**

---

## Deferred Stories Within Active / Done Epics

| Story | Epic | Status | Reactivation trigger (from sprint-status comments) |
|---|---|---|---|
| **20c-4** Master Arc Composition | Epic 20c (in-progress) | Deferred | "When complexity justifies" â€” return after profile-driven runs validate clustering. Wave 2 Deferred block in sprint-status.yaml. |
| **20c-5** Pax Agent | Epic 20c (in-progress) | Deferred | Same Wave 2 Deferred gating. |
| **20c-6** Lens Capability | Epic 20c (in-progress) | Deferred | Same Wave 2 Deferred gating; "start as capability" framing. |
| **20a-5** Exemplar Library | Epic 20a (done) | Ready-for-dev with reactivation-choice flag | "Unblocked; re-evaluate against deferred/reactivation choice" per 2026-04-15 decision. Currently staged but not pulled â€” awaits operator priority call. |

**Reactivation criteria (Epic 20c-native, from sprint-status.yaml line 111-115):** After 20c-14 (E2E validation) reveals specific gaps in clustering quality, reactivate the relevant deferred story to address it. 2026-04-15 decision: 23-1 reactivated as the next implementation target (unlocks the broadest downstream path with less speculation than 20c-4/5/6).

**Total deferred-story count in active epics: 4 stories.**

---

## Named-But-Not-Filed Follow-Ons

Follow-on stories named in existing specs or retrospectives but not yet filed as sprint-status entries. Filed only when their parent story closes and/or the trigger fires.

> P2-1 backlink (2026-06-19): Quinn-R detector ratchet recorded in `docs/trials/cross-trial-learnings.md` under "P2-1 perception fidelity detector". **UPDATE 2026-06-20:** `fidelity-metric-blind-to-perception-regression` ✅ **STRUCK** — P2-1 (detector) DONE, P2-2 (PNG-perception) CLOSED, P2-3 (Pass-2 grounding fix) CLOSED + live AC-6 regression-green confirmed. Only P2-4 (reading-path, Growth) remains in the P2 arc, and it is enhancement (not the disaster-regression, which is now closed).

| Follow-on | Parent story | Condition | Trigger |
|---|---|---|---|
| 🔵 **`marcus-spoc-variant-treatment-template-picklist`** | Variant-selection deck-wide fix — operator design intent (2026-06-23) | TARGET completed-app design (operator-described): Marcus-as-SPOC asks at the opportune gate (a) one version vs one+variant, and (b) if a variant, how B differs from A — by presenting a **pick-list of CD-curated "treatment templates"** (named theme + parameter configurations). Operator picks; the pick populates the directive `gamma_settings`; the now-built substrate (deck-wide filter + Gamma control layer + theme validation) executes. The interactive layer sits ON TOP of the substrate this session built. Precursors already exist: `state/config/gamma-style-presets.yaml` (named operator/CD-curated presets) + the locked `DEFAULT_VARIANT_PAIR` (A=Tejal, B=Blueprint Editorial = the first two treatment templates). | **Reactivate after the deck-wide variant substrate closes (re-T11) and a demonstration trial proves it.** Scope: (1) consolidate a CD-curated **treatment-template registry** (formalize gamma-style-presets.yaml + DEFAULT_VARIANT_PAIR into named, pickable treatments); (2) Marcus-SPOC interactive variant-decision turn (one-vs-one+variant + treatment pick-list) at the variant gate; (3) wire the operator's pick → directive `gamma_settings`. Demonstration trials use an INTERIM hand-injected A/B pair until this lands. **Direction-stable** (operator-described target). Context: `variant-selection-deckwide-fix-scope-2026-06-23.md`. |
| 🟡 **`gamma-ratelimit-header-aware-throttle`** | Variant-selection deck-wide fix T11 (2026-06-23) | Gamma `/generations` rate-limits (confirmed developers.gamma.app): every response carries `x-ratelimit-{limit,remaining}-{burst,,-daily}` headers (burst/hour/day tiers). Docs say a limit returns **429**, but EMPIRICALLY a burst tipped the valid key into sustained **401 "Invalid API key"** (the 401-eligibility flavor) for >15 min. The variant cycle added a reactive narrow warm-401 retry; the PROACTIVE fix is reading the ratelimit headers and self-throttling before tripping the window. | **Reactivate when live Gamma throughput matters at scale** (multi-deck batch, or trials hammering generation). Shape: parse the ratelimit headers off the GammaClient response, expose remaining-burst, and have Gary/dispatch pace `/generations` (e.g. extend spacing when remaining-burst < threshold per the docs' 5s→15s guidance). Non-blocking; normal 1–2-gen trials don't trip it. See memory `reference-gamma-generations-401-throttle`. |
| 🔵 **`gamma-studio-mode-variant-c-playwright`** | Variant-selection deck-wide fix green-light (2026-06-23; party 5/5 GREEN-WITH-AMENDMENTS) | The operator's CANONICAL "standard" look (published 2026-05-28 companion deck) was produced via Gamma's UI **"Import with AI" + Studio card-design mode** ("every card a single image with embedded text"). Confirmed 2026-06-23 (live Gamma developer docs + the Generate MCP schema + `gamma-style-presets.yaml:99`): the Generate API has **NO** card-design-mode / Studio / `formatVariant` parameter (`cardOptions` = only `dimensions`+`headerFooter`); no file-import/Import-with-AI endpoint either (only `/generations` text + `/generations/from-template`). So the API/Gary path renders **Classic typography cards**, structurally different from Studio image-cards — which also changes what the perception/reading-path layer SEES. | **Future variant C — Playwright UI automation.** Drive the Gamma editor UI (select Studio card-design mode, simulate operator-at-keyboard), export, ingest. Separate arc from the API-driven A/B. Reactivate when the operator wants the canonical Studio render reproduced programmatically. **Direction-stable:** Studio is UI-only barring a future Gamma API addition (re-check the API changelog at reactivation). Scope/context: `variant-selection-deckwide-fix-scope-2026-06-23.md` §D6. |
| 🟡 **`pass2-narration-must-ground-to-chosen-variant-figures`** | Per-slide variant selection + presenter-voice arc (2026-06-24; two live mirror runs A,B,A,B,A,B and B,A,B,A,B,A both completed error-free) | NEW interaction surfaced by genuine per-slide A/B selection: variant A renders quantitative content as data charts (figures present in the rendered PNG), but variant B's blueprint/lineArt aesthetic re-renders the SAME content as **prose** (figures absent — neither `text_mode=condense` nor `=preserve` forces raw numbers onto B; the 67%/18% live in source DATA Gamma charts for A but prose-summarizes for B). The G5 Quinn-R fidelity detector (the P2-3 repaired, correctly-working gate; see [[fidelity-metric-blind-to-perception-regression]] arc) requires narration figures ⊆ rendered-slide figures. So when B is the PICKED variant for a figure-bearing slide, Irene Pass-2 must ground to B's figure-FREE perception and NOT cite the source figure — which she did reliably in the 2nd mirror run (passed) but NOT in its first attempt (cited "18%" → `quinn_r.g5.fidelity-figure-contradiction` error-pause). The pass is therefore partly Pass-2 grounding **variance**, not a deterministic guarantee. | **Reactivate when per-slide variant selection goes to a real (non-demo) trial, OR before relying on B-heavy picks.** Shape options (party-mode, NOT yet decided): (a) strengthen Irene Pass-2 grounding so figure citations are hard-gated to the chosen variant's perceived figures (anti-confabulation ratchet, risk: over-constrains presenter latitude); (b) add `quinn_r.g5.fidelity-figure-contradiction`-triggered Irene re-roll to the dispatch auto-retry as an LLM-variance net; (c) treatment-template policy that figure-bearing slides only offer figure-preserving variants. **NOT** a gate relaxation (the detector is load-bearing). Evidence: runs `7d530d0a…` (A,B,A,B,A,B) + `6cb8eafd…` (mirror); the failed first mirror attempt `8553ab38…` is the RED witness. Auto-retry net for `gamma.export.brief-unmatched` already landed (2199776). **➕ 2026-06-24 — PARTY-MODE DISPOSITION (Murat/Winston/Amelia/Mary; CONSENSUS green-with-named-dissent, NO impasse → Quinn→John not triggered):** **(1)** NOT a blocker for the next SINGLE-variant trial → **carry forward** as a named/bounded/witnessed/triggered risk; **hard blocker before any B-heavy / 2-variant non-demo trial.** **(2)** Before a B-heavy trial, in sequence: **(i)** run a discriminating experiment — **N≥10–15 forced B-on-figure-bearing-slide runs on current substrate (bare, no net/gate) + Pass-2 context inspection** (is the source figure even in Irene's prompt on RED draws?) → MEASURED contradiction rate distinguishing variance vs deterministic gap (Murat+Mary binding; n=1 cannot); **(ii)** ship **(a)** as the durable fix — anti-confabulation ratchet built as a **subset-of-PERCEIVED-figures citation gate** (gates the citation SET not the prose/paraphrase — honors Amelia's false-fail dissent + Winston's leaky-neck framing), **RED-first against `8553ab38`**, exit = **0 contradictions across ≥15 post-(a) forced runs** (post-fix N-run green, NOT n=1); **(iii)** **(b)** admissible ONLY as a **counted/logged/bounded net BEHIND (a)** (never silent, never standalone — fidelity-contradiction ≠ the join/format flakes the existing net absorbs) and only if a low irreducible floor remains; **(iv)** **(c)** optional party-gated product belt for B-heavy (figure-bearing slides offer figure-preserving variants only) — NOT the headline (feature-amputation risk; Caravaggio/UX input on the diversity tradeoff). **G5 detector untouched throughout.** **NAMED DISSENT (Amelia):** would ship (b)-with-telemetry FIRST as the smallest change; overruled on ORDERING (n=1 can't carry (b)-as-primary) but her telemetry requirement folded into (iii) + her false-fail fix shaped (a)'s subset-of-perceived design. **OPEN OPERATOR SUB-DECISION:** dev posture for (a) — party split 3–1 NEW CYCLE (Winston/Murat/Amelia; substrate adjacent to load-bearing G5 + both runner walks per [[project-production-runner-two-walks]]) vs Claude-direct cleanup (Mary; constraint-tightening). (c) if adopted = party-gated, not Claude-direct. Record: this deferred-inventory entry is the decision SSOT (no separate heavy doc per cleanup-arc mode). |
| 🟡 **`gamma-theme-picker-interactive-gate`** | Variant-selection deck-wide fix green-light (2026-06-23; party 5/5) | The interactive theme-selection gate (the TP capability, `skills/bmad-agent-gamma/references/theme-template-preview.md`) is NOT wired into the production trial flow. `list_themes()` itself works (proven live 2026-06-23); what's missing is the GATE (a theme-options node + select-verb capture + envelope `theme_id` consumption), ~the voice-select-arc footprint. NOT needed for the variant fix — D2 pinned theme IDs + D3 live validation give deterministic, validated theme selection without it. | **Reactivate when interactive operator theme selection is wanted at runtime** (vs the pinned `DEFAULT_VARIANT_PAIR` IDs). Shape: a theme-options gate analogous to G4A voice / G2B variant pickers; add `theme_id`/`selected_theme_id` to `_SELECTABLE_KEYS_BY_GATE`; envelope consumption in `_theme_id`. Story-sized; own RED-first tests. **Direction-stable.** Context: scope doc §D5. |
| ~~**`fidelity-metric-blind-to-perception-regression`**~~ ✅ **STRUCK 2026-06-20** (P2-3 live AC-6 regression-green; see `docs/trials/cross-trial-learnings.md` §C strike record) | BETA Phase-1 closure ratification (2026-06-19; party-ratified Option B) | DISASTER-LEVEL coverage-debt. Pass-2 narration grounds on the slide BRIEF not the rendered PNG → hallucinated a "$5.2T line+bars" chart never rendered (slide shows $4.5T callouts + photo); perception + reading-path (Z/F/triptych) machinery severed 2026-04-24. The G5 gate is **anti-correlated** — checks script/audio mechanics, not narration↔rendered-slide fidelity, so "error-free" rises WITH defect volume. Carried forward explicitly as named coverage-debt (NOT papered with retroactive Kanban). Full closure record: `beta-phase-1-closure-ratification-2026-06-19.md` §3; structure in STATE-OF-THE-APP §8. | **Repaired-forward as P2 (binding, next arc).** Sequence: P2-1 fidelity detector RED-first vs the $5.2T fixture (Class-A/no-retry, dual-gate, own story) → P2-2 PNG-grounded PerceptionArtifact (schema-shape, dual) → P2-3 Pass-2 consumes perceived visuals (the regression fix, dual) → P2-4 reading-path native rewrite (single). PRD-gated (`bmad-create-prd`). Detector lands FIRST as committed RED evidence (vacuous-pass prevention). **Direction-stable:** the repair direction (ground on rendered PNG, brief→secondary) is operator+party-ratified; re-validate only if a second perception authority is introduced. **✅ STRUCK 2026-06-20 (FIRED):** all three A6 legs satisfied + verified — **(a)** detector-GREEN across the FULL FROZEN CORPUS via a **live Pass-2 run** over the 8-slide green-corpus (independently re-judged by the committed `detect_fidelity` → 8/8 GREEN); **(b) held-out** green-08 clean; **(c) cited pre-fix RED baseline** (Codex A7 `$5.2T→FidelityError` + committed RED-first `test_irene_pass2_perceived_visual_authority.py`). Leg (a) was operator-gated under D5, but **the operator authorized Claude to run it directly via a fresh INDEPENDENT subagent** under a strict validity protocol (deterministic committed judge; first-run-stands; no retry-to-green; no edits to detector/fixtures/corpus). Mary's 3 checklist items met: real prompt incl. un-framed payload tail; cache-MISS (`cached_tokens=0`, gpt-5); held-out=green-08 recorded. Bidirectional linkage FILED: `docs/trials/cross-trial-learnings.md` §C strike record + §A G5. Evidence: `_bmad-output/implementation-artifacts/p2-3-ac6-live-strike-evidence.json`. Operator strike-time checklist (Mary): (1) GREEN asserted against the REAL assembled prompt **including** the un-framed `## Envelope payload` tail (see [[pass2-envelope-payload-brief-unframed-in-prompt-tail]]), not a trimmed prompt; (2) confirm a **cache-MISS / live model call** (cold or non-empty sanctum) so a cache-hit replay can't masquerade as leg-(a); (3) record the live **held-out** slide id so legs (a) and (b) are not the same evidence double-counted. |
| 🟠 **`p2-4b-reading-path-repertoire-and-conformance-corpus`** | P2-4 party green-light 2026-06-20 (5/5 PARTIAL-SPEC-NOW); evidence-gated successor to P2-4a | The **machinery** half of P2-4 (FR17 classifier + closed enum over the 7 surviving patterns + FR18 verify-node + FR20 cadence) ships as **P2-4a** (`spec-p2-4a-reading-path-native-machinery.md`, ready-for-dev). The **growth + calibration** half is carved out here because it is gated on operator-derived ground truth that does NOT exist as of 2026-06-20: (FR19) repertoire growth — add `triptych` + `image-dominant-first` to the enum — is evidence-gated on a Storyboard-B (G2C/G3) scan-order **nit harvest not yet collected** ("ONLY where evidence shows the 7 mis-fit real slides"); and the **held-out ≥80% real-slide conformance bar** needs an operator-labeled corpus (≥8–10 real frozen-corpus slides with expected scan order + ≥1 known-wrong-default case — the reading-path analog of P2-1's `$5.2T` render-grounded evidence). Self-labeling either now = vacuous-pass (Murat). | **Reactivate when the operator's scan-order harvest exists.** Minimum artifact: the labeled held-out corpus above. Then: add the new enum patterns at **warning** severity first (Mary) + the full parity set; finalize the ≥80% bar against the labeled corpus (P2-4a's AC-9 operator-gate); include the §disconfirmation criterion. **Direction-may-flip caveat:** which new patterns are justified (and the exact threshold) re-validated against the harvest at reactivation. Couples with [[`vo-narration-layout-tracking-trained-patterns`]] (the operator's exemplar/training build) — that follow-on can SUPPLY this corpus. Bidirectional → P2-4a §Successor/Coupling + §A G5 reading-path. **P2-4a T11 robustness riders (party-mode 5/5, 2026-06-21 — calibration this corpus story MUST harden, not just measure):** (1) **ordinal-gate over-trigger** — `classify_reading_path` runs `_has_ordinal` over concatenated artifact prose BEFORE geometry, so incidental prose ("first…then…", "Section 2:", "3.5x") can mis-classify `sequence_numbered`; when it fires from artifact-text without per-element ordinals the order degenerates to a geometric sort (label≠produced-order). P2-4b adds ordinal-in-prose-vs-near-element disambiguation + a label↔order coherence assertion. (2) **conformance vacuous-skip — MUST carry a RED-first fixture (Murat named-dissent on deferring this rather than fixing at T11)** — `_assert_reading_path_conformance` silently `continue`s on an element-key that doesn't resolve to the scan-order index (perception `_element_key` vs narration `_visual_reference_key` vocab differ) + strict-`<` allows equal indices, so a mis-keyed/figure-light narration conforms vacuously. P2-4b MUST pin the keying contract THEN convert the silent skip to fail-loud, guarded by a RED-first fixture for the vacuous-conformance path so the fix isn't self-fulfilling. NOT patched at T11 because hard-failing without the pinned keying contract risks false-positive Class-A failures (the "stuck-alarm" anti-pattern Murat's own fidelity principles reject). (3) **nits** — `_bucket(1.0)==3` boundary; dead `_SCORERS`; one-directional grammar-heading parity. **The two CORPUS-INDEPENDENT machinery slivers were PATCHED at P2-4a T11** (vision classify-error → recoverable error-pause; `_bbox` non-numeric → skip) + regression-tested — NOT part of this rider. **➕ 2026-06-22 UPDATE — RE-SEQUENCED behind P2-4c:** operator slide-perception training round-1 (26/54, closed) → `reading-path-patterns-catalog.md` **v1** (compositional tuple) **party-ratified GREEN-WITH-AMENDMENTS 6/6** (A1–A10) → **`spec-p2-4c-reading-path-tuple-refactor.md` ready-for-dev (S1)**. P2-4b now calibrates the P2-4c tuple classifier (not the P2-4a 7-enum) and adopts the **A6 conformance contract** (strict primary-key `{macro_layout×image_role}` top-1 ≥0.85 + separate per-axis vector + full-tuple ≥80% + per-axis confusion + `multi_column` quarantined from the top-1 denominator until N≥4 + held-out gated-pattern contamination check). Prior riders FOLD into P2-4c/P2-4b: (1) ordinal-gate → P2-4c `enumerated_process` broadening; (2) conformance vacuous-skip keying-contract → A6; [[`vision-perceiver-empty-visual-elements-degradation`]] + [[`reading-path-bbox-out-of-range-bucketing`]] → P2-4c S1 fixtures. Unreviewed-28 + held-out-14 re-confirm: `multi_column` N≥4, genuine `diagram_driven`, tier-2.5 evidentiary (A4), f_pattern. **➕ 2026-06-22 EVE — METHODOLOGY FLIP (operator-authorized):** P2-4b changes from operator-labels-independently-then-score → **Claude labels the held-out 14 via catalog v1, operator CONFIRMS/DENIES.** Executed: live gpt-5.5 perceived 14/14 (`reading-path-holdout-scan/`) → **`holdout-confirm-deny-kit-2026-06-22.md`** (per-slide tuple + scan order + confidence + confirm/deny field; diagram_driven gate held on 8_/13_; known-wrong-default anchors 5_/8_). **This CONSUMES the held-out reserve** (no longer naive for any future independent-label scoring — operator-accepted; the original anti-anchoring integrity rule here is superseded by the flip). Awaiting operator verdicts → then A6 numbers finalize + corrections fold into catalog v1.1. **Flag the flip to party at the next gate (governance note, not blocking).** **➕ 2026-06-22 (round COMPLETE):** operator confirmed 12/14 → **A6 primary-key top-1 13/14 = 0.93 PASS**; 2 denials → 3 decisions → `bmad-party-mode` 6/6 ADOPT (no impasse) → operator-ratified → **catalog v1.1** + P2-4c spec updated. D1 multi_column N≥2 (17_→multi_column, exits quarantine); D2 new `callout_intent` axis (provisional, out of primary-key metric); D3 enumerated_process=permutability (21_→peer_boxes+takeaway_imperative). **NEW follow-on → [[`callout-intent-speech-act-axis-harvest`]]:** the `callout_intent` value-set is provisional (3 seeded values: invite_response/challenge_quiz/directive_cta; takeaway_imperative+contact are hypotheses) — needs a dedicated harvest (N≥4/value across ≥2 genres) + a double-labeled agreement ≥0.80/κ≥0.6 floor before it influences any scored number or its enum freezes. P2-4b conformance now consumes the v1.1 contract (callout_intent excluded from primary-key + full-tuple top-1; multi_column un-quarantined). **➕ 2026-06-23 — P2-4c BUILD STATUS:** S1 Codex T1–T10 → Claude T11 → **HAND BACK (party 5/5, no impasse)**; 3 MUST-FIX over-claim bugs (card_grid→top_down collapse + test-locked; opposition-cue + transform-verb over-fire). Re-dispatch via `codex-remediation-prompt-p2-4c-s1-t11.md` (6 RED-first fixtures). S2/S3 pre-staged (G2/G3 resolved). NEW follow-on → **`ambient-contract-test-suite-debt`**: 14 `tests/contracts/` tests fail on clean HEAD `829bc53` (baseline-proven pre-existing, NOT P2-4c) — fit_report_v1_schema (4), 30_1_zero_test_edits, 30_2b_dispatch_monopoly, 33_2_state_config_disjoint_keys, coverage_manifest_json_schema_parity, transform_registry_lockstep, quinn_r_gate_no_log_boundary, fit_report_canonical_caller, marcus_single_writer_routing, provider_directory_roster_placeholders[consensus]. Repo-wide contract debt; triage in a dedicated cleanup pass (not a reading-path blocker). NEW process rule (Mary): baseline-diff attestation MANDATORY in every NEW CYCLE re-handoff. Anti-pattern H1 ("green test certifies a bug") harvested to dev-agent-anti-patterns v4. **➕ 2026-06-23 — P2-4c S1 CLOSED DONE** (`2f6f123`; Codex remediation → Claude re-T11 → party 5/5 CLOSE). Forward follow-on **`reading-path-flags-none-guard`** (Amelia): the new additive `reading_path_flags: list[ReadingPathFlag] | None` is nullable + walled off from primary-key scoring; any FUTURE consumer (S2/S3/Irene/P2-4b) reading `.reading_path_flags` must None-guard it (don't assume `[]`). card_grid→grid_quadrant is impl-authoritative (catalog §4.6 reconciled). **➕ 2026-06-23 — S1 CLOSED; S2 returned (T11 next). PARALLEL-WORK deliverables (4 agents): P2-4b PRE-STAGED** (`reading-path-holdout-gold-labels-2026-06-23.md` + `scripts/analysis/reading_path_p2_4b_score.py` [18 self-tests green] + `spec-p2-4b-conformance-finalize.md`) — runs when S3 lands. **`believed-green-tracker-audit`: liveness stratum CLEAN** (report `believed-green-audit-2026-06-23.md`); only follow-on = the denylist item below. **`callout-intent-speech-act-axis-harvest`: protocol authored** (`callout-intent-harvest-protocol-2026-06-23.md`). **`ambient-contract-test-suite-debt`: TRIAGED** (`ambient-contract-debt-triage-2026-06-23.md`) — all 14 STALE PINS (0 real drift; root = app/ severance path-pins); 13 mechanical re-pins + 1 governance (30-1 baseline rollforward-vs-retire). **➕ 2026-06-23 — P2-4c S2 T11 → HAND BACK (party 5/5):** MF-A image_roles index-misalignment (live path) + MF-B/MF-C κ-harness vacuous-gate (tier-3 disagreement invisible; empty/all-quarantined→passes=True) → one Codex cycle `codex-remediation-prompt-p2-4c-s2-t11.md`; anti-pattern **H2** harvested. NEW follow-on **`reading-path-icon-set-cue`** (SF-4, DEFER→P2-4b/S3): the ≥3-same-size-icons→tier-4 cue is unimplemented; icons >0.05 area with no perceiver tier mis-tier to 2 (pollutes the scored {macro×image_role} key) — needs its own design pass. NOTE: the **image-role κ harness (`image_role_scoring.py`) is NOT WIRED to a live producer** (wiring is S3/P2-4b) — harness-GREEN ≠ live promotion evidence; the MF-B/MF-C fixes are hard preconditions on any tier-3/2.5 promotion (being fixed now in the S2 remediation rather than deferred). **➕ 2026-06-23 — P2-4c S3 CLOSED (party 5/5 CLOSE-WITH-RIDERS; live-proven, 0 degraded) → reading-path tuple classifier fully BUILT (S1+S2+S3), but UNCALIBRATED.** S3-T11 live dry-run found the **0.93 = catalog-approach (Claude labels), NOT the built classifier** (which scored 0.071 primary-key on STALE perceptions) + escalation over-fires 93%. Anti-pattern **H3** harvested; STATE-OF-THE-APP + P2-4b spec + gold doc corrected (R3). **NEW follow-ons (R1/R2):** (i) **`p2-4b-real-calibration`** — P2-4b is a 4-leg milestone (re-perceive 14 held-out under S2 role_tier → recalibrate escalation predicate → measure honest built-classifier accuracy via `run_live` → contingent improve), NOT one-command; gated on fresh re-perception; (ii) **`reading-path-s1-geometry-macro-accuracy`** — geometry macro_layout ~50% on held-out; owns the macro-error gap (S3 only does two_pane, can't repair general macro); (iii) **`reading-path-s3-escalation-recalibration`** — tighten over-broad `callout_kind_present` + wire the ≤20% over-escalation ceiling to the REAL escalation_ledger (currently test-only on a synthetic ledger; chicken/egg — threshold can't precede recalibration); (iv) **`reading-path-macro-override-architecture`** — IF macro accuracy needs LLM-primary macro, that's an architecture-tier change (party-gated; would invert hybrid-(c)). **Metric-citation rule (Mary, binding):** every reading-path accuracy number carries `(subject=built-classifier|catalog-approach, substrate=fresh|stale@date)`. **➕ 2026-06-23 — HONEST MEASUREMENT DONE (the first fair built-classifier number):** leg 1a (re-perceive 14 held-out under S2 role_tier, live gpt-5.5; `reading-path-holdout-rescan-2026-06-23/perceptions/`) + leg 3 (`scripts/analysis/reading_path_p2_4b_measure_fresh.py` — S1/S2 classify + live S3 escalation over the FROZEN fresh perceptions, score vs gold) → **subject=built-classifier(S1/S2/S3), substrate=fresh@2026-06-23: primary-key 0.071 (1/14) FAIL, full-tuple 0.0, macro 0.50, image_role 0.21, escalation 0.93.** Report: `…/honest-built-classifier-measurement.json`; full diagnosis + spec: `p2-4b-honest-measurement-and-recalibration-2026-06-23.md`. **CRITICAL: fresh re-perception did NOT move it vs stale (0.071≈0.071) → defects are CLASSIFIER LOGIC, not perception staleness** → confirms [[`reading-path-s1-geometry-macro-accuracy`]] (50% floor) + [[`reading-path-s3-escalation-recalibration`]] (callout_kind_present over-fires 93%) + an image_role axis (decorative over-tiered + dominant-fold scaffold). **Party-mode 5/5 GREEN-WITH-AMENDMENTS** (Winston/John/Murat/Mary/Amelia, no impasse) on ONE dual-gate recalibration NEW CYCLE folding all three sub-followons (`p2-4b-real-calibration` legs c/d): items A geometry → B image_role+authoritative-dominant-emission → C escalation(split); RED-first per-axis fixtures + S1/S2/S3 regression snapshot; overfit fences (no-peeking, simultaneous per-axis floors macro≥0.85/image_role≥0.70, perturbation guard, logic-only audit, gold-hash); T1 precondition RESOLVED (gold encodes authoritative per-slide dominant image_role → promote emission into `reading_path_classifier.py`, delete scaffold `_dominant_image_role`). Codex prompt: `codex-dev-prompt-p2-4b-recalibration.md` (T1–T10 → Claude T11). **Anti-pattern H4 (inherited green) harvested** (dev-agent-anti-patterns v7). **⚠️ OPERATOR-GATED: the sub-0.85 conditional-pass fallback bar** (Amelia 8/14 · Winston 9/14 · John axes-fixed · Murat 0.85+floors; Amelia NAMED DISSENT vs 0.85-hard) needs operator sign-off per the goal stop-condition; the Codex handoff is authored at the ≥0.857 success bar pending that decision. **NEW Tier-2 follow-on → [[`reading-path-fresh-naive-holdout-pre-trial`]]:** labeling the 14 CONSUMED the naive reserve → the 14 are now a non-naive DEV set; any score on them is a resubstitution (upper-bound) estimate. This cycle closes P2-4b **CONFORMANCE** (logic fixed + measured on the dev set), **NOT generalization/trial-readiness.** A FRESH naive holdout (operator labels ≥12–15 NEW slides, strictly naive, scored in a separate gate) is **REQUIRED before any "ready for trial" claim** (John+Mary binding; **Mary firm dissent against claiming trial-ready off the consumed-14**). |
| ✅ **`model-id-denylist-vs-allowlist-contradiction`** — **RESOLVED 2026-06-23 (`039d67c`, Claude-direct cleanup)** | believed-green audit 2026-06-23 | ~~`tests/test_no_fictitious_model_ids.py` is RED on clean HEAD: the denylist guard's `FORBIDDEN_IDS` still lists `gpt-5.4`/`gpt-5.5` as fictitious while the allowlist catalog-snapshot blesses them as real — the two guards contradict.~~ **FIXED:** dropped `gpt-5.4`/`gpt-5.5` from `FORBIDDEN_IDS` (kept genuinely-fictitious `gpt-5.4-nano`/`gpt-5-haiku`/`gpt-5-codex`), refreshed the stale docstring + failure-message help text. Denylist guard now GREEN; allowlist guard unchanged green; no contradiction. Future improvement (not blocking): derive the forbidden set from the catalog snapshot for lockstep. Still pairs with `believed-green-tracker-audit` rec #2 (key-gated live `.invoke` smokes). |
| 🔵 **`vo-narration-layout-tracking-trained-patterns`** | Operator note 2026-06-21 (during P2-3 NEW CYCLE session, surfaced just before the auth-loss that ended the session) | Client is VERY interested in good **tracking of VO narration against a slide's layout**. Operator wants — once the essential **'Z'-shaped (reading-path) functionality is working again AND reliably** — to dedicate ~1hr to build out + train (with **synthesized exemplary examples**) various common reading-path / layout patterns, so VO narration tracks the slide's visual scan order. Directly adjacent to the Z/F/triptych reading-path machinery severed 2026-04-24 and rewritten by P2-4; relates to [[`fidelity-metric-blind-to-perception-regression`]] (P2 arc) and the §roadmap "visual-scan VO after motion proven" line. | **Post-Z-restoration enhancement (operator-priority, NOT P2-blocking).** Reactivate AFTER P2-4 (reading-path native rewrite) lands and the Z-shaped functionality is trial-confirmed reliable. Scope: a small trained-exemplar set of common layout/reading-path patterns + a VO-narration↔layout tracking capability. Couples with `perception-source-element-reference-promotion` (element-level grounding). **Direction-may-flip caveat:** exact shape (trained classifier vs. heuristic reading-path map vs. prompt-embedded exemplars) re-validated at reactivation once the restored Z-path substrate is observable. |
| 🟠 **`believed-green-tracker-audit`** | vision-perceiver-real CLOSE (2026-06-21; Mary close-condition, party-mode 4/4 CLOSE) | A passing test suite silently stood in for "capability/config is current" in TWO places discovered this arc, one root cause: (1) the vision specialist was a **fixture-backed contract** (`vision-fixture-v1`, unconfigured `VISION_PROVIDER_ENDPOINT`, golden-only slide-01 artifact) mistaken for live perception — P2-2 closed "real PerceptionArtifact" but no live model was wired; (2) the OpenAI catalog-snapshot test (`test_cascade_ids_in_openai_published_catalog`) was **already RED on clean HEAD** because `gpt-5.4` (added 2026-06-11) was never added to the snapshot. See anti-pattern G1; cross-trial §A G5. | **Two-strata sweep (Mary).** (a) **Liveness sweep** of all 14 specialists for the signature: `-fixture-v*` model defaults, unconfigured `_PROVIDER_ENDPOINT`/`_API_KEY` env, golden-only on-disk artifacts read as proof-of-live. (b) **General believed-green sweep**: config/catalog snapshots, fixture providers, golden-only artifacts where "tests pass" silently substitutes for "config/capability current." **Reactivate BEFORE any trial claiming a specialist is live**, or at next substrate-audit. **Direction-may-flip caveat:** as specialists go genuinely live the signature shrinks; re-scope at reactivation. |
| 🟡 **`vision-perceiver-empty-visual-elements-degradation`** | vision-perceiver-real CLOSE (2026-06-21; Winston + Murat close-condition) | A HIGH/`perceived` `PerceptionArtifact` with empty `visual_elements` and no ordinal cadence makes `classify_reading_path` raise → non-retryable `vision.reading-path.unclassifiable` → **whole-run error-pause** rather than graceful degradation. Substrate correctly produces the signal; the degradation POLICY around an empty-but-confident perception needs work. | **RED-first follow-on.** Pin a RED fixture for the empty-`visual_elements` HIGH/perceived edge, then decide policy (degrade to low-confidence/coverage vs. error-pause). Couples with [[`p2-4b-reading-path-repertoire-and-conformance-corpus`]]. **Direction-may-flip caveat:** if real gpt-5.5 never emits empty elements on a HIGH slide, this may resolve to "assert non-empty" rather than a degradation path. |
| 🟡 **`reading-path-bbox-out-of-range-bucketing`** | vision-perceiver-real CLOSE (2026-06-21; Winston N1 calibration note) | LLM-estimated bboxes can carry coords outside [0,1] or `x1>x2`; `reading_path_classifier._bucket(value)=int(value*3)` on `cx>1.0` yields bucket 3 (outside the expected 0–2), skewing column/grid detection. No clamping in production (non-numeric already skips per P2-4a). **Calibration, not correctness** — provenance + contract are sound, the numbers need tuning where the reading-path consumes coords. | **Revisit when the reading-path consumes coords under P2-4b calibration.** Shape: clamp/normalize (or reject) out-of-range bbox coords in the classifier; assert on bucket not raw. Folds into [[`p2-4b-reading-path-repertoire-and-conformance-corpus`]]. **Direction-may-flip caveat:** clamp/threshold policy re-validated against the operator scan-order harvest. |
| 🟡 **`vision-recording-normalize-on-write`** | vision-perceiver-real CLOSE (2026-06-21; Murat close-condition 1) | The live AC-8 test regenerates the 6 AC-9 recordings with an absolute machine `source_png_path` on each run; normalization to repo-relative is currently a MANUAL post-run step (rots). Impact is hygiene-only (non-portable path in a committed fixture); replay tests don't assert on it and production overwrites `source_png_path` code-controlled — it CANNOT mask a regression. | **Durable fix (eyes-on before next live regen).** Make the recording sink auto-rewrite absolute → repo-relative `source_png_path` at write time; retire the manual step. Add a guard test asserting committed recordings carry repo-relative `source_png_path` (Murat condition 3). **Direction-stable.** |
| ~~🟡 **`pass2-envelope-payload-brief-unframed-in-prompt-tail`**~~ ✅ **STRUCK 2026-06-21** (P2-4a AC-8) | P2-3 T11 code review (2026-06-20; party-mode 5/5, Edge Case Hunter HIGH / Acceptance Auditor PASS) | The Pass-2 assembler (`app/specialists/irene/graph.py:405-406`) serialized the FULL `envelope_payload` (incl. Gary's brief with the stale `$5.2T line+bars`) into a `## Envelope payload (sorted-keys JSON)` tail section — **un-framed by demotion prose** and present even for UNVERIFIED slides. The new authority framing (perceived = SOLE authority; brief demoted to "expected, may be stale") was correct and positionally enforced; this was a SEPARATE pre-existing Pass-2 structure (pinned by the NFR-I6 byte-stability fixture), **not introduced by P2-3**. Triple-guarded: (1) Task text grounds "EXCLUSIVELY in corpus + perceived authority"; (2) the figure is also in the explicitly-demoted region; (3) the **G5 Quinn-R fidelity detector RED-guards the emitted output** — the load-bearing defense. T11 added a regression sentinel pinning that on UNVERIFIED slides `$5.2T` appears framed-only (never in authority) across the FULL prompt. | **Closed in P2-4a:** `_payload_section_for_prompt` now redacts Gary/slide-brief visual strings for UNVERIFIED slides before the sorted JSON tail while preserving the explicitly demoted expected-plan region and the canonical JSON dump signature. Regression pinned in `tests/specialists/irene/test_irene_pass2_perceived_visual_authority.py`; A8 prompt byte-stability deliberately re-pinned in the reading-path story. |
| 🟡 **`live-trial-replay-baseline`** | Task #14 golden-baseline analysis (2026-06-19; operator-surfaced) | The existing replay-regression subsystem (`app/replay/regression.py`) is a **deterministic-M3-replay** frozen at the Slab-4.5 ceremony snapshot (`runtime/graphs/v42/manifest-snapshot.yaml`, 33 nodes, committed `edcd313`) + a single `marcus_baseline` closed-trial envelope (`33333333-…`). It is ORTHOGONAL to a live production Trial 4 (`run_production_trial`, live 40-node manifest, real APIs, HIL) — Trial 4 never invokes it. "Pinning a golden-run replay baseline" for a LIVE trial is therefore (a) inherently post-trial (you pin a good run after it exists), and (b) NEW infrastructure: the harness only replays `source_kind="marcus_baseline"` (deterministic); a live trial is not byte-replayable (LLM nondeterminism + real APIs + operator verdicts), so a live "golden baseline" is a reference-snapshot-for-diffing, not a replay target. **Explicitly NOT the move:** refreshing the frozen 33-node snapshot to the woken 40-node topology — it destroys a Slab-4.5 audit artifact, cascades (digest + M3 baseline are a matched set → PackHashDriftError), and doesn't help Trial 4. | Post-Trial-4, IF live-trial regression coverage is wanted. Scope a story to: capture a blessed Trial-4 envelope as a `production_baseline` source_kind + add a live-trial replay/diff path (tolerant of LLM/API nondeterminism — compare structural/gate-sequence shape, not byte-exact). **Direction-may-flip caveat:** if live runs prove too nondeterministic to baseline usefully, this may resolve to "operator eyeballs each run" rather than an automated replay — re-validate at reactivation. |
| 🟡 **`g4b-input-package-hil-wake`** | Arc 2/1b green-light (2026-06-18; John PM scope ruling) | G4B (Input Package HIL @ 11B-gate) stays FOLDED for Trial 4. The "long-awaited picks" are variant (G2B) + voice (G4A); G4B is an input-package approval, a different surface. Waking it expands live-path blast radius + trial variable count without serving the stated goal — a G4B regression would confound the two-pick verification. | Post-Trial-4 wake. Reactivate when the two-pick loop (G2B+G4A) is trial-validated AND an input-package HIL is wanted. Same membership-wake recipe as Arc 2 (clear fold_with on 11B-gate + a G4B card branch; pack-exclusion predicate already extended by Arc 2). **Direction-may-flip caveat:** if Trial 4 shows the input-package step needs no HIL, G4B may be retired rather than woken — re-validate at reactivation. |
| 🟡 **`generalized-membership-wake-toggle`** | Arc 2/1b green-light (2026-06-18; Winston sequencing note, John deferral) | Arc 2 wakes G2B+G4A by clearing `fold_with` in the manifest (the `production_gate_ids` membership path). A friendlier generalized wake (operator/config-driven toggle without hand-editing the manifest) was floated; building it before two worked examples exist is speculative generalization (John). | Post-Trial-4, IF a third+ gate needs waking or operators want runtime wake control. Design the general lever against the Arc-2 worked examples (G2B+G4A) as its acceptance proof. **Direction-may-flip caveat:** if manifest-edit-per-wake proves sufficient in practice, the generalized toggle may never be needed. |
| 🟠 **`v5-manifest-coherence-reconciliation`** | Arc-1a A14 (2026-06-18; party-ratified Option A, Murat condition 3/d) | The production-canonical pack (v5) is hand-authored (`generated: false`) and sits OUTSIDE the generator/lockstep loop. After Arc-1a, the determinism guard (L1 check 9) proves manifest↔witness coherence for the `-gen` witness ONLY; v5 has NO manifest-coherence guard, so v5 and the witness can diverge in *semantics* (not just bytes) and no test catches it. The SHA pin delivers immutability, NOT coherence. Also folds Amelia's flag: `marcus_prompt_harness.py::MOTION_PROMPT_PACK` still reads frozen v4.2 as runtime prompt content — should it move to v5? Tolerable for a structural-pin arc; **NOT** tolerable to carry silently into a content trial where v5 is what renders. | **Pre-next-trial (binding, not "someday").** Before the next production trial: decide v5's coherence story — either (a) bring v5 into the generator loop (a generator rewrite: v5 has hand-authored content the `v42` generator can't emit — large), (b) add a semantic-diff guard between witness and v5, or (c) explicitly accept+document the divergence with the operator. **Direction-may-flip caveat:** which pack ultimately owns "production-canonical + lockstep-guarded" may invert as v5 evolves; re-validate direction at reactivation before dispatch. |
| 🟡 **`pack-version-co-render-filter`** | Arc-1a A14 (2026-06-18; Amelia named dissent, withdrawn-for-Arc-1a) | The single-value pack-version filter (`step.pack_version in (None, active)`) keeps coherence only via an atomic all-node `pack_version` flip; render-target is a global, not a per-step property. We are now at TWO axes (mapping-frozen v4.2 + production-canonical v5) plus the `-gen` determinism witness. One more axis and the single-value model stops being minimal-diff and becomes the bug source. | Reactivate when a **second generated companion** OR a **third `pack_version` axis** appears. Shape: make render-target a per-step property (multi-value / co-render filter) so a topology refinement no longer requires the atomic global flip ritual. **Direction-may-flip caveat:** if the three-role model is instead collapsed (e.g., v5 absorbs the witness), the co-render filter may become unnecessary — re-validate at reactivation. |
| ~~**`cd-directive-validator-prompt-contract-mismatch`**~~ ✅ CLOSED 2026-06-11 (was 🔴 TRIAL-4-RESUME-BLOCKING) | Trial-3 attempt-4 live run 2026-06-11 (finding #9) | CD specialist's first-ever live dispatch failed `CdDirectiveParseError: cd directive validator failed` **2/2 consecutive rolls** (systematic) at `_parse_cd_directive`. Root cause diagnosed at fix time: the validator's **parity rule** requires `slide_mode_proportions` + the 11-key `narration_profile_controls` to EXACTLY equal the chosen profile's targets in `experience-profiles.yaml`, but neither the prompt nor the CD references carried those values — the LLM had to invent them. Compounding: the live smoke test skipped on `validator-failed`, masking the mismatch in every green run. | **CLOSED:** (a) prompt embeds the authoritative profile targets; (b) `_parse_cd_directive` canonicalizes the directive deterministically from `{experience_profile, creative_rationale}` via new `load_experience_profile_targets()` in the validator module (Hourglass: deterministic neck fills the parity-bound values — the LLM judgment surface is exactly profile choice + rationale); (c) raw-response excerpt + validator error list captured on every parse failure (`CdDirectiveParseError.raw_excerpt`/`.errors`); (d) live smoke test unmasked (skips only on model-unavailable; FAILS on validator-failed). **LIVE-CONFIRMED 2/2 rolls PASS** (vs 2/2 fail pre-fix). CD suite 35 passed; battery green. Trial `50b7d353` resume UNBLOCKED. |
| **`trial-3-max-specialist-calls-segment-cap-semantics`** | Trial-3 attempts 3-4 (finding #8, evidence: `MissingUpstreamContributionError quinn_r→kira` on attempt-3) | `max_specialist_calls` defaults to 1 per segment and is NOT exposed on `trial start`; specialists skipped under the cap are permanently passed (walk advances; no revisit), starving downstream dependents. Attempt-3 (`a0d31fc0`) structurally dead from this; fail-loud guard caught it correctly. | Design decision + story: (a) expose `--max-specialist-calls` on `trial start`, (b) saner default (e.g., unlimited with cost guard), or (c) walk revisits unskipped specialist nodes. Party-mode input recommended — cap semantics interact with cost-control intent. |
| **`trial-3-pause-write-sequence-atomicity`** | Trial-3 attempt-3 (finding #7, evidence: `checkpoint_gate_mismatch` on `d8d1332a`) | `_pause_at_gate` writes checkpoint + decision-card JSON BEFORE cost report + envelope persist; a crash mid-sequence (observed: pricing KeyError) leaves disk torn (checkpoint=G2C, run.json=G1) and the fail-closed mismatch guard then blocks recovery of that trial instance. | Make the pause sequence crash-safe: either persist envelope first-and-last with a staging marker, or accept torn-state and add a `trial repair`/rollback path. ~2pt; design with Winston (checkpoint-schema adjacent). |
| ~~**`trial-3-five-fix-batch-code-review`**~~ ✅ CLOSED 2026-06-11 | Party-mode Option-A consensus guardrail #3 (2026-06-11, John binding) | Five Claude-direct substrate fixes landed under trial pressure (composer template, dispatch cwd, exit-10 discard, irene_pass1 roster, multi-gate pause `_pause_at_gate`) at commits `bb81b6f`/`919b16d`/`cd31b33`/`d727248`. Party-mode green-lit LANDING them; the audit obligation discharged 2026-06-11. | **CLOSED:** `bmad-code-review` executed 2026-06-11 (Blind Hunter + Edge Case Hunter, 74 conditions walked; both layers independently confirmed the `_pause_at_gate` extraction behavior-preserving). Triage: 9 PATCH applied (corpus_dir fail-loud + relative-anchoring + UnicodeDecodeError, resume None-clobber guard, terminal_gate derivation, artifact_paths dedupe, TimeoutExpired tagging, exit-10 test pin, guide Ctrl+C re-roll correction, checklist 11→12, queued nit trio incl. the former 3 ambient test failures) / 6 DEFER filed at `_bmad-output/implementation-artifacts/deferred-work.md` §code review of trial-3-five-fix-batch (incl. cd31b33 commit-hygiene P-tier candidate for the postmortem) / 3 DISMISSED. Battery green at close: marcus 131/1/0, ratchet+texas 76, lint-imports 13 KEPT, conformance 19. Attempt-5 unblocked on this axis. |
| **`vera-sidecar-cleanup-post-trial-2-validation`** | Story 7b.3 T2 / AC-7b.3-B | Legacy `_bmad/memory/vera-sidecar/` remains preserved after BMB migration to avoid losing historical memory before trial-2 validates the new sanctum path. | Cleanup only after trial-2 validates Vera cold activation and SG-4 parity on `_bmad/memory/bmad-agent-vera/`; then archive or remove the legacy sidecar by operator decision. |
| **`7b-12-wire-class-conformance-validator-to-specialist-parity-workflow`** | Story 7b.1 T2 / AC-7b.1-D | `.github/workflows/specialist-parity.yml` does not exist at Story 7b.1 T2, so the Class-A validator scaffold cannot be wired as a workflow job yet without inventing the whole workflow surface. | File/close in Story 7b.12 integration closeout: add `class-conformance` job that runs `python scripts/utilities/validate_parity_test_class_conformance.py tests/parity/` once the Slab 7b parity workflow exists. |
| **`class-c-validator-method-name-provider-agnostic-rename`** | Story 7b.7 T11 review (2026-04-29) | Class-C validator template (extended at 7b.6 close) hardcodes `test_gamma_api_client_binding` method name — Gamma-specific. Inherited as-is by 7b.7 Kira (asserts Kling binding inside test body to avoid template modification). Will repeat for 7b.8 Enrique (ElevenLabs) + 7b.9 Wanda (Wondercraft). | One-line rename in `scripts/utilities/validate_parity_test_class_conformance.py`: `test_gamma_api_client_binding` → `test_api_client_binding` (provider-agnostic). Natural close: fold into 7b.8 Enrique dev cycle (Codex naturally hits same NIT) OR earlier housekeeping commit. |
| **`digest-helpers-extract-to-app-gates-_common`** | Story 7c.7 T11 review (2026-05-05) | Wave-3 trio close (7c.6/7c.7/7c.8) lands `canonical_model_bytes` + `compute_model_digest` re-emitted across 4 §section packages (`section_02a` + `section_04a` + `section_04_5` + `section_04_55`); Wave-3 next-batch (7c.13/7c.14/7c.15) extends the duplication to 7-8 packages. C6 isolation discipline forbids cross-§section helper imports — but a non-C6 shared module would preserve isolation while eliminating duplication. T11 reviewer flagged as cross-Wave SHOULD-FIX-DEFERRED (not in-scope for any single story). | Wave-4 follow-on: extract digest helpers to `app/gates/_common/digest_helpers.py` (NOT in C6 modules set; sourced from outside C6-tracked package). All §section poll-surface modules import from `app.gates._common.digest_helpers` instead of re-emitting locally. Preserves C6 cross-§section independence (no §section→§section imports) while eliminating ~40 LOC duplication per §section. ~1-2pt. Natural close: separate housekeeping commit at Wave-4 entry OR fold into 7c.17a/b cohort if scope permits. Reviewer file: `7c-7-code-review-2026-05-05.md`. |
| **`test-no-unauthorized-callers-scanner-staleness`** | Stories 7c.13 + 7c.14 T11 reviews (2026-05-06) | `tests/integration/gates/test_resume_api_authority.py::test_no_unauthorized_callers` (authored 2026-04-26 at `9e441f7`) does substring matching on `OperatorVerdict(` against `app/**/*.py` with `if path.name == "operator_verdict.py": continue` filename exclusion. Two structural problems: (a) substring `OperatorVerdict(` trivially matches **class definitions** (`class Section08BOperatorVerdict(BaseModel):`) — NOT direct constructor calls; (b) filename exclusion does NOT match variant filenames (`operator_verdict_section_*.py`). Test was already failing with 6 pre-Wave-3-next-batch entries (§02A + Wave-3 trio §04A/§04_5/§04_55 + §02A poll_surface line-160 constructor); 7c.13 + 7c.14 each add one entry. **Both T11 reviewers independently concluded PRE-EXISTING — DISMISSED**, not regression. | Test-hardening follow-on: rewrite scanner to (a) tighten substring match to detect actual constructor invocations vs class definitions (e.g., AST-walk for `Call` nodes targeting `OperatorVerdict` subclasses; or regex `\b(?<!class\s)Section\w*OperatorVerdict\(`), AND (b) generalize filename exclusion to glob `operator_verdict*.py` matching all variants. Standalone test-only story (no app/* impact). ~1pt. Natural close: housekeeping commit at Wave-4 entry OR fold into 7c.17a/b cohort. Reviewer files: `7c-13-code-review-2026-05-06.md` + `7c-14-code-review-2026-05-06.md`. |
| **`wanda-sanctum-tests-cross-suite-state-pollution-p3-flake`** | Story 7b.8 T11 review (2026-04-29) | Independent broad-regression run with default randomization yields 4 Wanda failures (sanctum_directory_exists / sanctum_digest_nonempty_post_population / l6_operational_context / act_loc_budget) that all PASS under `-p no:randomly` and PASS in Wanda-isolated runs. Diagnosis: P3 anti-pattern (cross-test side-effects) — a prior test in the broader run mutates `_bmad/memory/bmad-agent-wanda/` sanctum state mid-suite. Latent issue NOT introduced by 7b.8 or 7b.9. | Diagnose offending mutator test at 7b.9 close commit window OR before 7b.10 Dan opens. Likely candidates: any test that writes to `_bmad/memory/bmad-agent-*/` paths without `tmp_path` isolation; or an autouse fixture that touches sanctum dirs. Resolution either via per-test `monkeypatch` of sanctum path or fixture-level isolation. Until closed, broad-regression CI runs MUST use `-p no:randomly` to avoid spurious flake. ~1-2pt. |
| **`dan-sidecar-cleanup-post-trial-2-validation`** | Story 7b.10 T3.2 / AC-7b.10-C | Legacy `_bmad/memory/dan-sidecar/` remains preserved after BMB activation at `_bmad/memory/bmad-agent-dan/` to avoid deleting historical CD-lane context before trial-2 validates the new Dan activation path. | Cleanup only after trial-2 validates Dan cold activation, G1/G1A/G2 aux contributions, and SG-4 parity on `_bmad/memory/bmad-agent-dan/`; then archive or remove the legacy sidecar by operator decision. |
| **`prior-close-uncommitted-artifacts-audit-trail-7b-1-through-7b-6`** | Story 7b.8 T11 review OBSERVATION-1 (2026-04-29) | Story closes 7b.1-7b.6 each prescribed delegation pattern + validator extensions + SKILL.md migrations as binding deliverables in their specs but the close commits did NOT verify File-List force-add scope. Codex's 7b.8 dev cycle implicitly closed the drift atomically (49 files / +725 / -2161 LOC; behavior-preserving; 220 prior-specialist tests + 1325 broad PASS deterministic). Audit-trail: prior closes flipped done with uncommitted artifacts in working tree. | (Observational; no remediation required — drift IS closed at 7b.8 close commit.) Procedural follow-on for 7b.9-7b.12 closes: Claude T11 reviews must verify File-List force-add scope at commit time via `git ls-tree -r HEAD --name-only \| grep _act.py` matching each story's File-List entries before flipping done. Documented in 7b-8-code-review-2026-04-29.md OBSERVATION-1. |
| **`slab-7b-mapping-checklist-row-status-update`** | Story 7b.12 T7 / AC-F (2026-04-30) | Spec AC-F asks for "asserts ~28 row improvements" but the mapping-checklist file's preamble forbids dev-agent row-status flips ("Adding or removing rows requires party-mode consensus, NOT dev-agent authority"). Currently 0 ✅ FULLY MIGRATED rows in the table; Slab 7b 7b.1-7b.11 closes have not flipped any rows because the dev-agent path is structurally not authorized. Test `tests/parity/test_mapping_checklist_status.py` asserts integrity invariants (deferred-rows preserved + parseable + legend intact + count >= pre-Slab-7b floor=0) but NOT the aspirational ~28-row improvement count. | Lockstep file update at Slab 7b retrospective close commit (party-mode ratified at retrospective). Update `_bmad-output/planning-artifacts/slab-7-legacy-migrated-mapping-checklist.md` rows owned by activated specialists from ❌/⚠️ to ✅ where post-Slab-7b body-activation evidence supports the flip; preserve deferred-rows (§05B / §6.2 / §6.3 / §7.5 / §14.5 / §15) at pre-Slab-7b status legend. Update `tests/parity/test_mapping_checklist_status.py::PRE_SLAB_7B_FULLY_MIGRATED_FLOOR` to the new post-update count in lockstep. Estimated ~28 rows flip per Round-(a) A-10 R3 amendment. |
| **`slab-7b-spec-language-row-improvement-vs-party-mode-gating-clarification`** | Story 7b.12 T13 party-mode John meta-follow-on (2026-04-30) | Spec language for FR/AC writers exhibits a latent contradiction: "asserts ~N improvements" (testable via dev-agent code) vs "row changes require party-mode consensus" (NOT dev-agent authority). Surfaces as a structural impossibility for the dev-agent author. Current resolution: integrity-invariant test + party-mode-gated retrospective close commit (per `slab-7b-mapping-checklist-row-status-update` row above). | Tighten future-cycle spec language at Slab 7b retrospective close. Either (a) reword "asserts ~N improvements" as "asserts integrity-preserved over party-mode-ratified row updates" so dev-agent author has unambiguous test target, OR (b) reword the mapping-checklist file preamble to permit dev-agent row-status flips when supported by post-close evidence (with a separate party-mode lockstep on row ADDITION/REMOVAL only). Pick (a) or (b) at retrospective; document in `docs/dev-guide/specialist-migration-template.md` template R-block for next slab. ~30-min retrospective decision. |
| **`mapping-checklist-deferred-row-detection-strengthening`** | Story 7b.12 T13 party-mode Murat NIT (2026-04-30) | `tests/parity/test_mapping_checklist_status.py::test_deferred_rows_preserve_pre_slab_7b_status` regex parser detects `05B` / `7.5` / `14.5` / `15` row-anchor tokens but `6.2` and `6.3` are sub-rows in prose preamble that the row-anchor pattern doesn't match cleanly. Test passes vacuously for those 2 of 6 deferred-row tokens. False-positive risk: regression on §6.2 or §6.3 wouldn't be caught. NIT-grade because those rows are sub-rows unlikely to flip without explicit author attention. | Strengthen detection at retrospective close: either parse the prose preamble for §6.2/§6.3 explicit deferral declarations OR pin the deferred-rows assertion to specific table-row-anchor patterns that match all 6 tokens including sub-rows. ~30-min test refinement. |
| **Audra + Cora dissolution** (migration branch) | 2026-04-24 operator ratification during Slab 2 roster reconciliation | LangGraph CI stack (import-linter 3 contracts + scaffold-conformance framework + shape-pin tests + LangSmith tracing) covers the code-invariant functions these two agents performed on primary. Session-ritual functions covered by existing BMAD session-START/WRAPUP protocols. | **No migration work.** Leave [`skills/bmad-agent-audra/`](../../skills/bmad-agent-audra/) + [`skills/bmad-agent-cora/`](../../skills/bmad-agent-cora/) as historical-archive directories on hybrid. Cora's `/harmonize` + session-triage CLI wrapper remains scoped to Slab 4 Epic E4 (Lockstep + Gates + Cora) but as a CLI/hook, NOT as a LangGraph runtime node. Audra is fully dissolved â€” no post-M5 resurrection planned. See [`slab-2-roster-reconciliation.md Â§Category D`](slab-2-roster-reconciliation.md). |
| **Dev-coherence report generator (hybrid-native Audra replacement)** | 2026-04-24 Audra/Cora dissolution ratification | Audra's historical output under [`reports/dev-coherence/`](../../reports/dev-coherence/) is archived (54 inherited session dirs) but nothing writes new entries post-severance. Audra is dissolved per Category D; hybrid needs a deterministic CLI to populate new timestamped entries at session-WRAPUP. | File as a ~1â€“2pt story under **Slab 4 Epic E4** (Lockstep + Gates + Cora â€” natural home since it already scopes Cora's CLI wrapper). Shape: `scripts/governance/dev_coherence_report.py` dumps import-linter output + scaffold-conformance pytest summary + ruff clean check + optional LangSmith trace sample into `reports/dev-coherence/<ts>/`. Optionally wire into a session-WRAPUP hook in `.claude/settings.json` for auto-run. Replaces Audra's conversational interface with deterministic CI output per Option D philosophy. |
| **`2c-1-ac-b-op-live-wondercraft-evidence`** | Story 2c.1 | Operator-gated Wondercraft smoke artifact is intentionally not executed by dev agent because it can incur real API cost. | Reactivate when operator window opens AND `WONDERCRAFT_API_KEY` is valid in `.env`; operator runs one <=$0.50 Wondercraft episode-preview smoke and pastes audio path, sha256, chapter markers, voice id, request/response timestamps, cost, and summary into the 2c.1 evidence addendum or successor close artifact. |
| **`2c-1-generator-wanda-audio-return-field-polish`** | Story 2c.1 | Diff evidence classifies the missing `wanda_audio` loose-typed return field in generated `WandaValidationReturn` as a generator-gap candidate. | Slab 2c.4 generator polish OR Slab-3 enhancement evaluates whether `--from-skill` metadata can drive optional domain return-field hints without overfitting Wondercraft. |
| **`2c-1-generator-skill-reference-loader-polish`** | Story 2c.1 | Diff evidence classifies missing skill-reference tuple / reference-loader helper emission as a generator-gap candidate; generated graph remains scaffold-only. | Slab 2c.4 generator polish OR Slab-3 enhancement evaluates a domain-neutral reference-loader template keyed from `--from-skill` references. |
| **`2c-2-mcp-architecture-divergence-from-epic-literal`** | Story 2c.2 | Epic literal text still references a Wondercraft MCP path, but shipped Wanda uses direct-package import through `scripts/api_clients/wondercraft_client.py` per 2b.8. | Reconcile epic/planning prose when the migration guide is next revised; do not add `.mcp.json` Wondercraft wiring unless a future architecture story explicitly chooses that path. |
| **`2c-2-ac-d-op-live-wondercraft-evidence`** | Story 2c.2 | Dev agent authored the `@pytest.mark.live_api` test but did not autonomously incur Wondercraft API cost. | Reactivate when operator window opens AND `WONDERCRAFT_API_KEY` is valid; operator runs `.venv/Scripts/python.exe -m pytest tests/specialists/wanda/test_wanda_live_api_artifact.py --run-live -v -s` and commits the sha256-named MP3 plus `LIVE_ARTIFACT_METADATA.md`. |
| **`2c-2-ac-e-round-trip-pending-live-artifact`** | Story 2c.2 | Filesystem sha256 round-trip test is present but skips until `LIVE_ARTIFACT_METADATA.md` and the MP3 artifact exist. | Reactivate automatically when AC-D-OP artifact lands; keep the skip test in tree so the metadata contract is not orphaned. |
| **`2c-2-receipt-strict-typed-artifact-metadata`** | Story 2c.2 | `WandaDispatchReceipt.wanda_audio` remains loose `dict[str, Any] | None`; 2c.2 narrows validation to filesystem metadata rather than receipt-key extraction. | Future dispatch-hardening story may coordinate strict receipt keys across receipt model, Wondercraft dispatch helpers, and tests after live artifact shape is known. |
| **15-1-lite-irene** | 15-1-lite-marcus | Epic 33 meta-test validates the substrate catches new contracts on Marcus | File when 15-1-lite-marcus closes CLEAN-CLOSE-META-TEST-PASSED. Extends learning-event capture to Irene's Gates. |
| **15-1-lite-gary** | 15-1-lite-marcus | Same as above | File when 15-1-lite-marcus closes CLEAN-CLOSE-META-TEST-PASSED. Extends learning-event capture to Dan/Gary's Gates. |
| **v4.3 substrate extension** | Epic 33 retrospective | No current trigger; mechanism ready per Q1 parameterized-version-hook | File when operator identifies the first Tier-2 pack change per [Pipeline Manifest Regime Â§Pack Versioning Policy Tier 2](../../docs/dev-guide/pipeline-manifest-regime.md#tier-2--minor-v42--v43-new-pack-file-v42-preserved-for-audit). Ships as `scripts/generators/v43/` sibling + manifest entries with `pack_version: "v4.3"`. |
| **Full Epic 15 chain (15-2 through 15-7)** | 15-1-lite-marcus + first tracked trial run | First tracked trial run completes AND 15-1-lite-marcus's compressed ledger proves viable | Sequence per Epic 15 seed: 15-2 retrospective artifact â†’ 15-3 upstream feedback routing â†’ 15-4 synergy scorecard â†’ 15-5 pattern condensation â†’ 15-6 workflow-family ledger â†’ 15-7 calibration harness. Each story already specced in [epics.md Â§Epic 15](epics.md). |
| **`5a-1-checkpoint-native-trial-catalog-discovery`** | Story 5a.1 | Story 5a.1 closed on the adapted substrate with replay discovery sourced from `tests/fixtures/marcus/baseline_envelope/2026-04-26/` because the hybrid branch still lacks a replayable checkpoint-native closed-trial catalog in Postgres or `state/trials/`. | Reactivate if later 5a.x evidence or post-M5 cleanup requires replay discovery from actual LangGraph checkpoint storage rather than the frozen Marcus baseline fixture. Until then, fixture-backed discovery is the accepted 5a.1 contract. |
| **`m5-fr-trace-partial-FR51`** | Batch 2 B-trace | FR51 is implemented and tested through the accepted 5a.1 migration-native Marcus baseline fixture, but checkpoint-native closed-trial catalog discovery remains deferred. | Reactivate when test surface is added that exercises FR51 end-to-end against actual LangGraph checkpoint storage; then retire this row together with `5a-1-checkpoint-native-trial-catalog-discovery`. |
| **`5a-2-production-clone-launcher-and-execution-equivalence-pass`** | Story 5a.2 | Story 5a.2 closed on actual-substrate control-plane parity because the authored production clone-trial launcher (`app.marcus.cli trial start --preset production --input <corpus-path>`) does not exist on `dev/langchain-langgraph-foundation` as of 2026-04-26. The current parity evidence therefore proves only control-plane artifact equivalence, not new-run execution equivalence. | Reactivate when a real clone-trial launcher exists on the branch. Re-run the same corpus against the same frozen primary baseline, capture the new production clone trial evidence, and retire the 5a.2 AC-A conditional state rather than extending the current control-plane-only verdict by implication. |
| **`slab-6-1-multi-pass-envelope-path-x-or-y`** | Slab 6.1 bmad-code-review DFR-6.1-2 (EC-2 / AA-3) | `ProductionEnvelope.add_contribution` enforces immutability invariant (one contribution per `specialist_id`). Manifest topology can have repeated specialist nodes (e.g., Irene Pass 1 + Irene Pass 2). Slab 6.1 ships with Path Z ("first contribution wins"; duplicate specialist contributions skipped after first; explicit + logged per bmad-code-review patch P-4). Operator-ratified Path Z 2026-04-27. Path X (node-scoped contribution identity: `<specialist>:<node_index>`) and Path Y (per-pass envelope) are filed as enhancement candidates. | **Substrate enhancement, scope TBD per chosen path.** Reactivate when actual multi-pass production need emerges in trial work â€” e.g., Irene Pass 1 + Pass 2 both in production manifest; OR specialist back-catalog grows to include genuinely repeated nodes. At reactivation: convene party-mode round to choose Path X vs Path Y; Path X requires envelope schema_version bump (production-envelope.v1 â†’ v2); Path Y requires per-pass-envelope architecture decision. See Composition Spec Â§3.1 + Â§10 Decision Log + Â§12 known limitation #7. |
| **`replay-regression-pack-hash-drift-pre-slab-6.1`** | Slab 6.1 bmad-code-review DFR-6.1-3 (AA-7) + N7 trace | Pre-existing pack-hash drift (`013b7ef â†’ 19cde78`) in replay-regression suite + diffs in override confirm token / cost fields. Drift is unrelated to Slab 6.1 rewire (per Codex finding; verified at code review). Replay sweep timed out at 124s during review attempt; no Slab 6.1-specific replay failure set established. Operator-ratified deferral 2026-04-27. | **Investigation, ~2-3pt.** Investigate pack-hash drift root cause; likely needs golden refresh after Slab 6.0 substrate landed but before 6.1 rewire. Re-run replay sweep with extended timeout (or break into chunks) to establish stable failure set; refresh golden baselines per finding; restore replay-regression to GREEN state. Reactivate when next replay-regression failure surfaces in trial work OR scheduled tech-debt cleanup pass. |
| **`slab-6-1-runner-compiled-edge-traversal`** | Slab 6.1 bmad-code-review DFR-6.1-4 (EC-6) | Production runner currently iterates manifest order, ignoring compiled graph edges. Current v4.2 manifest is linear so this is not blocking at Slab 6.1 close. Required before non-linear branch/conditional production manifests are used. | **Substrate enhancement, ~2-3pt.** Wire production runner to consume compiled graph edges (from `compile_run_graph(...)`) rather than iterating manifest order; add execution-path assertions covering branch/conditional traversal; chain test exercises a non-linear specialist topology. Reactivate when first non-linear manifest topology lands in production OR when v4.3 pack family introduces conditional traversal patterns. See Composition Spec Â§12 known limitation #8. |
| **`production-trial-envelope-lifecycle-invariants`** | Slab 6.1 bmad-code-review DFR-6.1-5 (EC-7) | `ProductionTrialEnvelope` permits impossible lifecycle states; cross-field validators for `completed_at` / `paused_gate` / `cost_report_path` state matrix are absent. Pre-existing issue not introduced by Slab 6.1 rewire. | **Test discipline + schema hardening, ~1-2pt.** Add Pydantic v2 cross-field validators to `ProductionTrialEnvelope`: (a) `completed_at` set XOR `paused_gate` set (cannot be both); (b) `cost_report_path` required if `completed_at` set; (c) `paused_gate` requires associated checkpoint state pointer; (d) state-machine transition tests covering all valid + invalid transitions. Four-file-lockstep: model + JSON Schema regen + golden refresh + new shape-pin tests. Reactivate when first lifecycle-state-confusion incident surfaces in trial OR scheduled tech-debt cleanup pass. See Composition Spec Â§3.7 + Â§12 known limitation #9. |
| **`slab-6-1-langsmith-runner-trace-id-real-binding`** | Slab 6.1 bmad-code-review DN-1 (AA-5 / EC-5) â€” operator-ratified deferral 2026-04-27 | Cost rollup works correctly (per-call cost from LangChain runtime is real; live smoke confirmed `total_cost_usd = 0.0325215` from real OpenAI invocation). Deficit is at runner-aggregation level: `ProductionTrialEnvelope.trial_trace_id` records a synthetic placeholder rather than fetching the real LangSmith trace ID. Real specialist invocations DO emit real LangSmith spans with `extra.metadata.trial_id == <trial_id>` propagated correctly. Operator workaround acceptable for bounded-MVP: query LangSmith manually for spans with the trial_id metadata to find the real trace tree. | **LangSmith client wiring, ~2-3pt.** Wire `measure_trial_cost(trial_id)` (or equivalent) to perform live LangSmith fetch at trial close; bind real trace ID into `ProductionTrialEnvelope.trial_trace_id`; preserve synthetic placeholder as fallback for offline mode. Likely needs retry/backoff for LangSmith ingestion latency. N8 trace upgrades from PASS-WITH-GAP to clean PASS at landing. Reactivate when first trial reveals operator-friction on the manual trace-ID workaround OR when audit-trail completeness becomes load-bearing for stakeholder review (e.g., compliance audit; investor demo; etc.). See Composition Spec Â§3.7 + Â§12 known limitation #6. |
| **`m5-fr-trace-partial-FR52`** | Batch 2 B-trace | FR52 has parity scoring implementation, evidence artifacts, and tests for control-plane artifact parity, but the real production clone-launch execution path remains unproven. | Reactivate when test surface is added that exercises FR52 end-to-end through a real production clone trial with LangSmith trace evidence; then retire this row together with `5a-2-production-clone-launcher-and-execution-equivalence-pass`. |
| **33-1a follow-on: Â§4.55 body polish** | 33-1a | Dev agent self-review identifies prose imprecision on Â§4.55 lock-semantic body | File only if 33-1a dev agent flags ambiguity; otherwise no story needed. |
| **Epic 33 retrospective follow-ons** | Epic 33 retrospective (required) | Retrospective identifies substrate work not yet scoped | TBD per retrospective outcome. Epic 33 spec names "multi-version v4.3 + 15-1-lite-irene/gary fan-outs" as expected candidates. |
| **PR-TR (Trial Resumption capability) + trial-branch discipline + config-reload refinement** | 2026-04-19 session â€” operator-surfaced during trial-prep discussion | First tracked trial generates evidence of which discipline details matter most | File post-trial-#1. Scope at ~2pt single-gate: (a) PR-TR capability = deterministic pause-hash capture + resumption-delta diff + Green/Yellow/Red classification, additive to existing PR-* scaffold; (b) CREED standing-order refinement for config-reload-on-resumption; (c) hot-start banner template for `trial/<RUN_ID>` branch + clean-working-tree invariant counter. Parked per evidence-driven-before-spec discipline. |
| **Texas best-available-medium selection** | 2026-04-19 trial run â€” operator-surfaced during Step 02 source map | First trial run completes and operator confirms multi-format source sets are a recurring pattern | Texas currently requires explicit role assignment per source; no logic to auto-select among PDF vs DOCX vs MD for the same content. Desired: given a candidate set of formats for the same SME content, Texas picks the highest-fidelity available format as primary and demotes others to validation automatically. Natural home: Epic 27 follow-on (Texas intake expansion) or Epic 15 learning-intelligence track. ~2-3pt. |
| **Step 02A prior-run directives as defaults** | 2026-04-19 trial run â€” operator-surfaced during Step 02A poll | Hardwire before next run | Marcus currently surfaces prior-run operator-directives.md on request only. Desired: Step 02A always scans prior bundles for the most recent operator-directives.md for the same lesson_slug, presents them as named defaults with source attribution (run_id + date), and requires explicit accept/modify/replace before writing. Eliminates re-entry burden on resumed or repeated runs. Natural home: Marcus PR-* capability or Step 02A pack procedure amendment. ~1pt. |

| **HUD per-step expandable summaries** | 2026-04-19 trial run â€” operator-surfaced during Step 03 hold | Post-trial, once `--watch` live HUD lands | Each pipeline step in the HUD should have an expandable section showing real-time summary of content captured and locked at that step (e.g. Step 01 shows preflight receipt, Step 02 shows source authority map, Step 03 shows extraction quality report). Operator can inspect any completed step without leaving the HUD. Natural home: Epic 22 (Storyboard & Review Adaptation) or a new HUD-enhancement story. ~3-5pt. |
| **Converge `cluster-plan.yaml` and `segment-manifest.yaml` to single pre-Gary artifact** | trial-fix-g1.5-cluster-plan-artifact story â€” party-mode 2026-04-19 | Post first full trial with clusters | Currently two separate files serve the same G1.5 gate at different pipeline stages (Pass 1 structural / Pass 2 full). Desired: single artifact shape emitted at Pass 1 time, enriched (not replaced) by Pass 2 with narration-time fields. Gate mode (`structural` vs `full`) determined by which fields are populated, not by filename. Eliminates dual-artifact ambiguity and simplifies gate runner. ~2pt architecture story. |

| **Production intake_callable for run_4a / Step 04A** | 2026-04-19 trial run â€” Step 04A programmatic loop not wired for production HIL | Before next production trial run | `Facade.run_4a()` requires an `intake_callable` connecting the 4A loop to Maya's UI. Currently only test stubs / `run_maya_walkthrough` fixture harness exist. Production wiring would drive `run_4a()` from real operator ratification input per unit. Natural home: Marcus CM capability or new PR-4A production-readiness capability. ~3pt single-gate. |

| **Irene Pass 2 authoring template / schema contract** | 2026-04-20 trial run B-Run Â§08 â€” operator-flagged after validator remediation across two full sessions | Before next Pass 2 production run | Irene's Pass 2 work product required exceptional post-hoc repair before `validate-irene-pass2-handoff.py` returned STATUS: pass. The validator's strict contract (exact behavioral-intent form, 4+-char token-level narration pre-seeding, absolute path matching, valid `visual_detail_load` values, bridge-cadence mechanics, cluster arc integrity rules) is implicit in validator code and not surfaced to Irene at composition time. Required: a structured Irene Pass 2 authoring template that encodes the validator's implicit contract as explicit schema constraints and inline authoring guidance â€” so segment-manifest.yaml and pass2-envelope.json can be produced in one pass without post-hoc debugging. HIGH priority: single highest-friction step in the production pipeline observed this trial. Natural home: Epic 20c or new Epic 34 (Irene authoring contract). ~3-5pt. |

| **Theatrical-direction synthesis (Tier 1 + Tier 2)** | 2026-04-21 trial run B Â§11 â€” operator explored dials-only ceiling and asked whether per-segment feature specification is part of a "theatrical direction"â€“capable approach | After C1-M1-PRES-20260419B synthesis output is evaluated; operator decides whether dials-only expressiveness is sufficient or whether escalation is warranted | Current trial is **dials-only amp-up** on `eleven_multilingual_v2` with uniform voice_settings across 14 segments (only `speed` varies per segment via `pace_variability` client-side nudge). This reveals the ceiling of single-parameter-set synthesis. Two escalation tiers for future trials: **Tier 1 â€” per-segment voice_settings.** Extend `segment-manifest.yaml` and `pass2-envelope.json` to carry per-segment `stability` / `style` / `similarity_boost` hints. Irene authors them alongside narration (e.g., heavy reflection segment â†’ higher stability + lower style; excited call-to-action â†’ lower stability + higher style). Runner builds a per-segment `voice_settings` payload instead of reusing one envelope. Works on v2 model; no API/model change required. Estimated ~70% of theatrical value at ~20% of complexity. ~3-5pt. **Tier 2 â€” model swap + audio-tag authoring.** Add model selection capability (`eleven_v3` / `v3-alpha` / future tag-capable models) to `voice-selection.json`. Irene Pass 2 authors inline audio tags (`[laughs] [whispers] [shouts] [sighs] [pause]`) at per-segment granularity. Runner detects model capability and passes tags through. Combined with Tier 1 this delivers full director-mode control: per-segment voice parameters + per-utterance performance markers. ~5-8pt. Natural home: new Epic (Director-Mode Synthesis) or Epic 24 extension. Parent-story decision trigger: operator reviews trial B audio output, declares "dials-only ceiling is too flat for category X content." Until then, dials-only remains the cost-effective default. |

| **Replace cache_prefix payload-carrier hack with first-class RunState envelope field (Slab 3 prerequisite)** | Story 2a.2 T4 act-body authoring (party-mode 2026-04-24 ratified) | RunState has no envelope field at Slab 1 substrate. Story 2a.2's `_act` node receives the Pass-2 envelope payload encoded as sorted-keys canonical JSON in `state.cache_state.cache_prefix` (overloads documented field role "Stable prefix derived from sanctum + model + manifest version"). Documented bounded-scope decision; clean substitute requires extending RunState. | **Slab 3 follow-on, ~2-3pt.** Add `RunState.specialist_envelope: SpecialistEnvelope \| None` (or per-specialist sub-state schema). Migrate `_act` to read from the new field; remove cache_prefix overload; update Story 2a.2 spec Â§ Envelope-Carrier-Hack note to "retired". Murat envelope-hack-receipts binding from T4 review (2026-04-24). |
| **Cold-cache nonce-variant test for AC-D measurement realism** (`2a.2-followon-cold-cache-nonce-variant`) | Story 2a.2 T7 cache-hit-rate measurement (party-mode 2026-04-24 Murat T7 binding) | The 2a.2 AC-D run reported inv 1 = 95.33% (already warm) instead of cold-cache 0% because the AC-B live-LLM test fired ~5 min prior with byte-identical prompt prefix, pre-warming OpenAI's cache. The MF1 disposition rule (median[2:] â‰¥ 60%) is spec-compliant in this scenario by design, but the harness cannot distinguish "cache works" from "cache was already warm and we got lucky." | **TEA follow-on, ~0.5pt, ~$0.03/run.** Add a regression-test variant to `tests/end_to_end/test_cache_hit_rate_baseline.py` that injects `uuid4()` into a non-cached suffix region for inv 1, asserts `cache_read_input_tokens == 0`, then confirms inv 2-10 land at expected â‰¥60% hit-rate against the non-noisy prefix. Fire-trigger: next time we touch FR54 harness OR retrospective notices a cache-behavior question. Murat T7 review (2026-04-24); explicitly NOT blocking 2a.2 close. |
| **`--require-live-llm` pytest flag wiring** (`2a.2-followon-require-live-llm-flag`) | Story 2a.2 T8 dual-path regression review (party-mode 2026-04-24 Murat T8 binding) | SF3 anti-erosion guard half-shipped at 2a.2: (a) `@pytest.mark.llm_live` auto-skip on placeholder/unset OPENAI_API_KEY is shipped + verified at T8b. Half (b) â€” operator-facing `--require-live-llm` flag that would FAIL (not skip) if any `@llm_live` test would auto-skip â€” is unwired. Auto-skip is the load-bearing erosion guard; the flag is operator ergonomics for "debug live path; hard failures, not skips." | **TEA follow-on, ~0.5pt.** Wire the `--require-live-llm` CLI flag in `tests/conftest.py` per the SF3 spec: when set, any `@pytest.mark.llm_live` test that would auto-skip raises a typed failure instead. Operator-pre-merge hook OR a Completion-Notes paste workflow invokes `--require-live-llm`; CI (no real key) does NOT. Prevents silent-skip erosion if @llm_live test coverage grows over Slab 2b. Fire-trigger: when any new @llm_live test lands AND operator wants pre-merge confidence beyond per-story T8 dual-path verification. Murat T8 review (2026-04-24); explicitly NOT blocking 2a.2 close. |

| **Dispatch-wrapper extraction candidate** (`slab-2a-close-followon-dispatch-wrapper-template`) | Story 2a.4 close + Slab 2a retrospective (2026-04-25, Winston rider) | `kira/kling_dispatch.py` (LLM+tool category, ~80 LOC) and `texas/retrieval_dispatch.py` (pure-tool category, ~70 LOC) are two independent occurrences of the same dispatch-wrapper shape: subprocess seam + fixture-bundle short-circuit + pinned subprocess kwargs (`shell=False, timeout=30, capture_output=True, text=True, cwd=REPO_ROOT`). Two independent occurrences is the harvest threshold for a pattern; three is the extraction threshold. | **Slab 2b extraction candidate, ~1pt.** If 2b.1 (Gary) or 2b.2 surfaces a third occurrence, extract to `_bmad/scaffolds/dispatch_wrapper_template.py` and migrate Kira + Texas + the third specialist to the shared shape. Before then, leave inline. Fire-trigger: third occurrence in Slab 2b. Winston rider 2026-04-25 ratified. |
| **`quinn-r-calibration-learning-memory-sidecar-integration`** | Story 2b.3 close (bounded-scope defer) | Quinn-R migration intentionally excludes calibration-learning memory integration; sidecar architecture is out of scope for per-specialist scaffold migration. | **Named follow-on, Slab-3+ dependent.** Open when memory-sidecar architecture story lands; wire Quinn-R calibration-learning persistence contract at that point. |
| **`desmond-fr54-cache-hit-baseline-measurement`** | Story 2b.4 close (narration inheritor defer) | Desmond is a narration-category inheritor where FR54 is applicable at the specialist layer (stable Descript reference prefix), but baseline measurement was explicitly deferred to keep 2b.4 scoped to migration parity + sanctum lock hardening. | **Named follow-on, Slab-2b mid-wave candidate (~0.5pt).** Run paired FR54 baseline against Irene and Desmond prompts; record median cache-hit-rate and prefix-stability notes in migration-guide Â§12.10 or slab retro evidence. |
| **`tracy-skill-persona-authoring-on-hybrid`** | Story 2b.5 close (bounded-scope defer) | Tracy runtime migrated at 2b.5 without SKILL.md/persona first-breath authoring on hybrid; source substrate remains partial and persona authoring is out of scope for the migration story. | **Named follow-on, Epic-28-1 forward-port gated.** Open when Epic 28-1 forward-port lands on hybrid (or operator explicitly requests Tracy first-breath), then author SKILL.md + populate sanctum + re-run Tracy scaffold conformance and sanctum tests. |
| **`tracy-fr54-cache-hit-baseline-measurement`** | Story 2b.5 close (LLM+tool inheritor defer) | Tracy uses an LLM-authored manifest path where FR54 is applicable, but baseline measurement is deferred to preserve 2b.5 bounded scope. | **Named follow-on, Slab-2b mid-wave candidate (~0.5pt).** Run live Tracy manifest generation loop and record median cache-hit-rate with stable vocabulary/provider prefix. |
| **`slab-2-roster-reconciliation-tracy-status-update`** | Story 2b.5 close (doc-state drift) | Slab-2 roster reconciliation still labels Tracy as fully mature while 2b.5 confirms hybrid substrate is partial (`SKILL.md`/first-breath pending). | **Named follow-on, documentation hygiene (~0.25pt).** Update roster reconciliation Tracy row to reflect bounded-scope migration and forward-port dependency. |
| **`migration-deferred-skip-decay-monitor`** | Story 2b.5 close (skip-governance follow-on) | Tracy introduces a permanent-skip marker (`DEFERRED-EPIC-28-1-FORWARD-PORT`) that needs age/cap monitoring to avoid indefinite skip decay. | **Named follow-on, governance tooling (~0.5pt).** Add CI/report utility that enumerates `DEFERRED-EPIC-*` pytest skips with age and cap status (3 stories or 30 days rule). |
| **`tracy-strict-vocabulary-llm-live-nightly`** | Story 2b.5 close (optional quality hardening) | Day-to-day live smoke asserts shape, but not strict nightly vocabulary conformance across real-model responses. | **Named follow-on, nightly-only test hardening (~0.5pt).** Add `@llm_live_strict` Tracy test that fails on any out-of-vocabulary `intent_class` in live manifests. |
| **Â§12 size review when subsection count exceeds 12** (`slab-2a-close-followon-section-12-size-review`) | Story 2a.4 close + Slab 2a retrospective (2026-04-25, Paige P8 rider) | `docs/dev-guide/langgraph-migration-guide.md Â§12 Specialist Walkthrough` currently has 10 subsections (Â§12.1â€“Â§12.10) post-2a.4. Slab 2b adds ~14 specialist migrations across 14 stories; Â§12 grows by 0 to 14 worked-example subsections depending on how the walkthrough convention is applied per category. | **Readability check at 2b mid-slab, ~0pt (operator decision).** When subsection count crosses 12, re-evaluate whether Â§12 should be split into a directory (`docs/dev-guide/migration-walkthroughs/{category}/{specialist}.md`) or stay as a single section. Fire-trigger: Â§12 subsection count â‰¥ 13. Paige P8 rider 2026-04-25 ratified. |
| **`implementation-artifacts/` directory README/index** (`slab-2a-close-followon-implementation-artifacts-readme`) | Story 2a.4 close + Slab 2a retrospective (2026-04-25, Paige P5 rider) | `_bmad-output/implementation-artifacts/` accumulates per-story implementation artifacts + sprint-status.yaml + slab-retrospectives + ad-hoc spec files. Newcomer triage at Slab 2b kickoff would benefit from a README that names the conventions: which file types live here, how to find the latest sprint state, what slab-retrospective files exist. | **Operator-helper, ~0.25pt.** Author `_bmad-output/implementation-artifacts/README.md` at Slab 2b kickoff if directory clutter is hindering newcomer triage. Fire-trigger: 2b kickoff session opens and operator + dev agent surface the same "where is X?" question twice. Paige P5 rider 2026-04-25 ratified. |
| **Local environment optional-dep hygiene** (`slab-2a-close-followon-env-optional-deps`) | Story 2a.4 G6 close (2026-04-25) | `tests/contracts/test_33_*.py` (7 files) and several `tests/test_*.py` modules error at collection with `ModuleNotFoundError: No module named 'responses'` and pydantic-v2 schema-validation errors. Pre-existing on the local hybrid clone; Texas migration did not introduce these. They prevent the migration-suite regression baseline from being quoted with full coverage. | **Slab-2b chore, ~0.5pt.** One-shot env-restore PR at Slab 2b open: install missing optional deps OR mark the affected modules with `pytest.importorskip(...)` so collection succeeds with optional deps absent. Fire-trigger: Slab 2b T1 regression baseline reading. Filed at 2a.4 close. |
| **Sanctum lock baseline regeneration script** (`slab-2a-close-followon-sanctum-lock-regen`) | Story 2a.4 G6 close (2026-04-25) | Texas's 17-file sha256 baseline ships as `TEXAS_SANCTUM_LOCK_BASELINE` module-level constant per Amelia A3 rider; operator can manually regenerate via `find _bmad/memory/bmad-agent-texas -type f -exec sha256sum {} +` per Story Â§G when sanctum legitimately changes. A regen script (~30 LOC) automates this for any Slab-2 specialist with a populated-and-locked sanctum. | **Operator-helper, ~0.25pt.** Author `scripts/utilities/regenerate_sanctum_lock_baseline.py <specialist-name>` that emits the dict literal in the exact `TEXAS_SANCTUM_LOCK_BASELINE` shape ready to paste into the specialist's graph.py. Fire-trigger: any Slab-2b specialist that ships with a populated-and-locked sanctum + operator session where a baseline regen is needed. Filed at 2a.4 close. |
| **SciteProvider OAuth 2.1 migration + production-class MCP integration** (`5a-2-scite-mcp-oauth-integration`) | M3 ceremony probe 2026-04-27 in operator session; further-substantiated by 2026-04-27 heartbeat + ceremony successful end-to-end runs | **Candidate Slab 6.2 story** (paired with `migration-6-1-production-graph-runner.md` Slab 6 opener). Third A16 (Composition-vs-Component Audit Gap) instance surfaced when M3 ceremony attempted Scite MCP. Existing `skills/bmad-agent-texas/scripts/retrieval/scite_provider.py` is structurally wrong against the real MCP on **four** dimensions: (1) auth — hardwired to HTTP Basic but real MCP requires OAuth 2.1 Bearer per WWW-Authenticate header pointing at `https://api.scite.ai/.well-known/oauth-protected-resource`; (2) **tool count — assumes 3 separate tools (`search`, `paper_metadata`, `citation_contexts`) but real MCP exposes only ONE unified tool: `search_literature`** (verified 2026-04-27 via heartbeat + ceremony); (3) parameter names — assumes `query`/`max_results` but actual schema is `term`/`limit` plus 27 other rich filter params (year, journal, author, paper_type, retraction filters, smart-citation count ranges, etc.); (4) response shape — assumes per-tool result envelopes but actual response is `{total: int, count: int, query: str, limit: int, offset: int, hits: [{doi, title, authors, ...}]}` wrapped in MCP TextContent. Operator-session ceremony bypasses legacy `SciteProvider` via official `mcp` Python SDK's `OAuthClientProvider` — working reference implementation at `scripts/operator/m3_texas_ceremony_scite.py` + heartbeat at `scripts/operator/scite_heartbeat.py`. Live verified 2026-04-27: real scholarly results returned (53.8M-paper corpus; 5 papers per default query; DOI + title + authors per hit). | **Provider migration, ~5-6pt** (revised up from initial ~2-3pt estimate after 2026-04-27 findings doubled the migration scope). Update `skills/bmad-agent-texas/scripts/retrieval/scite_provider.py` to use OAuth 2.1 via `OAuthClientProvider`: (a) replace `auth_style="basic"` MCPServerConfig with OAuth-aware client construction (mirror reference impl in `scripts/operator/m3_texas_ceremony_scite.py`); (b) implement TokenStorage backed by repo-managed cache directory; (c) **collapse 3-tool assumption to 1-tool — replace `_SCITE_TOOL_SEARCH/PAPER/CITATIONS` constants with single `_SCITE_TOOL_LITERATURE = "search_literature"`**; (d) migrate parameter mapping from `RetrievalIntent` to `search_literature` schema (term, limit, offset + rich filters); (e) migrate response normalization from per-tool shape assumptions to actual `{total, count, hits: [{doi, ...}]}` shape; (f) live integration test gated `pytest -m live` exercising the unified tool; (g) deprecate `SCITE_USER_NAME` + `SCITE_PASSWORD` env vars (no longer needed for MCP); (h) ensure cross-validation with Consensus (per existing contract) still works given the unified-tool shape. Fire-trigger: dispatch as Slab 6.2 alongside Slab 6.1 production-graph-runner if/when trial production needs Scite retrieval through normal Texas dispatch (Scenario A). Until then, operator-driven scripts at `scripts/operator/m3_texas_ceremony_scite.py` provide ad-hoc Scite scholarly retrieval today (Scenario B). |
| **Layer-1/Layer-2 cascade collapse** (`postmigration-cascade-config-collapse`) | 2026-04-26 post-vote substrate-aware remediation (Winston party-mode round-2 rider 1) | The cost-engineering substrate carries TWO independent sources of truth for per-specialist model assignment: `runtime/config/model_cascade.yaml` (5a.3 economics overlay) and 17 per-specialist `app/specialists/<name>/model_config.yaml` files (LangGraph dispatch). Both now have real OpenAI IDs but they remain divergent — could drift again. Lint guard prevents fictitious drift but not semantic drift (specialist pinned to gpt-5-nano while cascade tier-3 silently moves to a successor). | **Architecture cleanup, ~3pt.** Per-specialist configs declare a `tier` (`economy`/`balanced`/`premium`); resolve `model_id` from cascade YAML at load. Specialists that need a pin keep an explicit `model_id_override` field. Reactivation: post-trial-validation, before any cascade rebalance. |
| **OpenAI catalog snapshot staleness CI guard** (`postmigration-catalog-snapshot-staleness-warn`) | 2026-04-26 post-vote substrate-aware remediation (Winston rider 2 + Quinn-R rider 1) | `tests/fixtures/openai_catalog_snapshot.json` carries `_provenance.next_refresh_due_by` (2026-07-26 initially); but no CI guard fails the build when it lapses. An allowlist that silently rots is worse than no allowlist. | **CI hygiene, ~0.25pt.** Add `tests/test_catalog_snapshot_freshness.py` that warns (or fails per operator preference) if `next_refresh_due_by` < today. Refresh discipline becomes self-enforcing. |
| **Tautology audit on cost/router fixtures** (`postmigration-cost-router-fixture-tautology-audit`) | 2026-04-26 post-vote substrate-aware remediation (Murat rider b) | The 30-file sed sweep that replaced fictitious model IDs with real ones did not run Murat's perturbation procedure (test under deliberately-different IDs). Pricing/cost-routing fixtures and router/dispatch fixtures could still pass tautologically (string identity rather than behavior). | **Test hardening, ~1pt.** Spot-check ~5 representative fixtures across the two classes (pricing-tier and dispatch-key). For each, perturb the model_id to a different real ID and confirm test outcome changes per behavior contract. File any identity-only tests as remediation candidates. |
| **Full perturbation procedure on existing test suite** (`postmigration-perturbation-audit`) | 2026-04-26 post-vote substrate-aware remediation (Murat rider c) | One-time audit of the existing test suite's discriminating power: run under three deliberately-different model_id substitutions and identify identity-only tests. Catalog-membership + lint guard prevent NEW fictitious IDs but don't audit existing test sensitivity. | **Test discipline, ~2pt.** Author harness that runs cost-engineering test slice under 3 perturbations; produce report listing identity-only tests; remediate per finding. |
| **Broader silent-substrate audit** (`postmigration-silent-substrate-broad-audit`) | 2026-04-26 post-vote substrate-aware remediation (Amelia rider a, item 3) | Plausible-Token Substrate Contamination is unlikely to be unique to OpenAI model IDs. Other substrate categories with same risk shape: provider slugs in deployment configs, region codes, capability flags (`reasoning_effort` enum values), endpoint URLs, Marcus's specialist-registry capability strings, MCP tool slugs. | **Audit pass, ~1pt.** 30-min rg sweep with extended denylist for plausible-but-fictitious patterns across these categories; file findings or close clean. Cost of NOT doing: another silent-substrate event in 2-3 sprints when something downstream resolves a fictitious capability. |

| **`prompt-pack-reduction-internalization`** | Operator question 2026-04-28 during documentation-reset session: "why are the prompts still so long? shouldn't the prompts become 'second nature' to the agent—be baked into their skills or resources in some way, so my interaction is more conversation and less prompt feeding?" | Legacy v4.2 prompt pack is hundreds of pages of "script of prompts as prose" inherited from pre-migration primary-repo workflow (Cursor IDE chat + per-Epic-1-24 prompt-pack family). The migrated runtime architecturally does NOT need so much prose-as-script — Marcus + 14 specialists internalize substantive guidance via skills (Layer 2), sanctum personas (Layer 1), and `_act` body knowledge (Layer 3). Pack persists for: (a) operator-facing reference of what to expect during a trial; (b) backward-compat to the primary-repo prompt-pack-driven workflow. Operator's intuition is correct: post-SHIP, prompt content should migrate from prose-script into specialist skills + Marcus PR-* capability references + sanctum continuity, so operator interaction shifts from prompt-feeding to genuine conversation. | **Substrate evolution; multi-story scope; ~estimate TBD pending first-trial evidence.** Reactivation gates: (1) first tracked trial completes — surfaces which prompts are operator-facing (need preservation in pack OR migration to operator op docs) vs internal-to-Marcus orchestration (candidates for migration to PR-* capability refs); (2) prompt-pack v4.3 candidate per `docs/dev-guide/pipeline-manifest-regime.md` Tier-2 policy — natural moment to scope reduction work. Approach (rough): per-step audit of v4.2 pack identifying (a) operator-data-entry blocks (preserve in pack), (b) internal-orchestration prose (migrate to Marcus PR-* refs), (c) specialist-domain knowledge (migrate to specialist skill `references/` + sanctum). Each migration block runs through bmad-create-story + party-mode + bmad-code-review per discipline doc Gate 6. Filed in playbook §4.5 ("A note on prompt length + future evolution"). |

| **`slab-7-inter-gate-conversational-orchestration`** *(supersedes the narrower `production-runner-texas-directive-composition-seam` framing — first filed and within-session corrected after operator review of legacy v4.2 prompts 01/02/02A/03)* | First tracked trial 2026-04-28 (`trial_id: 475df528-7d75-48a3-be56-82b54a0b7b8b`). Trial reached `paused-at-gate G1 (gate_focus: trial_open)` cleanly; production-runner end-to-end plumbing verified (gate pause, checkpointer, LangSmith trace, decision card, cost report, `production_clone_launch_evidence: true`). However Texas's contribution at nodes 02/03 returned the deterministic fixture bundle at `tests/fixtures/specialists/texas/fixture_bundle/` (10 lines of placeholder content) — NOT the operator's Tejal APC C1-M1 corpus. Real cost was $0.000005. **Critically:** post-trial operator-review of legacy v4.2 prompts 01–03 (full text quoted in 2026-04-28 evening session transcript) revealed that this is NOT a single missing-module gap — it is **systemic loss of inter-gate conversational orchestration** across the migration. The legacy prompt pack v4.2 has detailed per-section conversational prompts (01 Activation + Preflight, 02 Source Authority Map with file-by-file role assignment HIL, 02A Operator Directives with 3-min-min poll, 03 Ingestion where Marcus AUTHORS `directive.yaml` from §2 + §2A) — every one of these prompts encodes operator-Marcus conversation that the migrated runtime currently barrels through autonomously. Without these conversational gates, downstream specialists (Vera, Irene, Gary, Kira, Quinn-R, ElevenLabs, Compositor, etc.) lack their inputs and produce fixture-stub or empty contributions. **The same shape of "surprise" will reappear at every gate transition** (G1→G1A, G1A→G2C, G2C→G3, G3→G4, etc.) until inter-gate conversational orchestration is restored systemically. | **Three layers missing in the migrated runtime:** **(1) Early-gate HIL pause discipline** — manifest declares G0/G0A/G0B as gates, but production runner only pauses at terminal gates (G1/G2C/G3/G4). All early operator-confirmation gates (legacy "Wait for explicit GO" cadence) are skipped. Same shape will affect any in-between gate the runner doesn't honor. **(2) Conversational composition surface** — no Marcus-runtime interface to author `source-authority-map.md`, `operator-directives.md`, or any other conversational artifact at gate-paused state. Decision cards today only carry `verb: approve` binary verdicts; there is no mechanism for operator to enter/edit prose like file-role assignments or directive content. Maya UI is the deferred candidate but undefined in scope. **(3) Inter-gate composition logic** — once §2 + §2A artifacts exist, no code reads them and emits `directive.yaml` for Texas; Marcus-the-persona did this manually in legacy. Same shape applies to every other inter-gate composition step the legacy prompt pack defines (§5 Irene Pass 1 prep packet from §4 ingestion artifacts; §8 Irene Pass 2 segment manifest from §6/§7 Gary slides + §3 extraction; §9 Compositor handoff from §6/§7/§8 outputs; etc. — full inventory is part of Slab 7 PRD scoping). | **First-content-trial blocker AND every-subsequent-gate-transition blocker.** Real content production cannot occur without inter-gate orchestration; not just at trial-open but at every gate boundary in the run. Reactivation: open **Slab 7 — Inter-Gate Conversational Orchestration** as a new slab boundary. **🛑 MANDATORY FIRST DELIVERABLE OF SLAB 7 (operator directive 2026-04-28 evening, captured verbatim):** *"the first step in our enhanced PRD/Slab/epic will be to create a checklist in which every single step and corresponding outcome in the production prompt pack v4.2 workflow is mapped to a corresponding step and corresponding outcome in the LangGraph replatformed app."* This checklist IS the gap-inventory for Slab 7. Walk `docs/workflow/production-prompt-pack-v4.2-narrated-lesson-with-video-or-animation.md` section-by-section; for each legacy step record its expected outcome (artifact / state transition / operator-confirmation / specialist contribution); identify its migrated counterpart (manifest node, specialist, `_act` body, gate, runtime artifact); mark status ✅ FULLY MIGRATED / ⚠️ PARTIALLY MIGRATED (specify dropped sub-outcomes) / ❌ NOT MIGRATED. Output: `_bmad-output/planning-artifacts/slab-7-legacy-migrated-mapping-checklist.md` (or canonical path agreed at PRD scoping). PRD prose, party-mode architectural debate, and per-story scope cuts are all DOWNSTREAM of this checklist. Do not invoke `bmad-create-prd` until the checklist exists. **Recommended BMAD workflow (corrected ordering):** (1) Mapping checklist authored as a deliberative-doc cycle (party-mode review on completeness against the legacy pack); (2) `bmad-create-prd` (or `bmad-party-mode` first for architectural alignment) — architectural questions that need party-mode answers: (a) Where does the operator-Marcus conversation happen at non-terminal gates? Inside the CLI? Through an external Maya/UI? In Claude-Code/Cursor IDE chat sessions wrapped around the runner? (b) How much of legacy Prompt 01/02/02A/05/06/07/08/09/etc. is "operator-data-entry that must remain conversational" vs "could be auto-derived from corpus + sensible defaults"? (c) How does the existing `--verdict-file` channel evolve to carry not just approve/reject but operator-authored prose (file-role assignments, directives, scope-lock content, etc.)? (3) `bmad-create-epics-and-stories` — each story's scope traces back to specific rows in the mapping checklist; (4) per-story `bmad-dev-story` cycles. Likely 3+ stories minimum spanning Slab 7 (final count emerges from the checklist). The narrower directive-composition module described in the original framing (new `app/runtime/compose_directive.py`) is one downstream story under Slab 7, not the whole answer. The legacy v4.2 prompt pack is the canonical inventory of "what conversational orchestration was present pre-migration" — the mapping checklist is the artifact that operationalizes that inventory. |

| **`tw-7c-6a-wave-canary-conditional-firing`** | Slab 7c epic-structure party-mode 2026-05-04 (Murat amendment Round 1; **AMEND-7b correlation check added 2026-05-04 Round 2**) | Slab 7c plans one terminal 50-run zero-flake parity firing at slab-close (TW-7c-6) against the full ~68-cell post-Slab-7c matrix. Single-epic decomposition ratified unanimously (4/4) on the basis that one terminal full-matrix firing has higher statistical power than three smaller epic-close firings. **Round-2 update (Murat AMEND-7b):** Wave-3 per-surface overlap with Wave-2 (10 HIL stories opening as predecessors land) creates joint-distribution aggregation hazard not covered by single-surface canary; when 3 HIL surfaces share a transport, flake events are NOT independent and 0.1% per-cell budget assumed independence. Solution: amend canary to include Pearson correlation check (r > 0.3 between any pair of HIL-surface flake-event vectors during 25-run canary); flag if exceeded. Lightweight; catches aggregation hazard without adding tripwire ceremony. **Round-2 also tightens budget per AMEND-7a:** 7c-added cells use <0.05% (5e-4) budget; pre-7c cells grandfather at 0.1%. | **TEA conditional follow-on, ~0.5pt at activation.** Author wave-canary as a sub-tripwire under TW-7c-6 (conditional 5-run wave-3-boundary parity firing) ONLY IF Slab 7c slab-close 50-run firing observes flake-rate breach (>0.05% on 7c-added cells; >0.1% on pre-7c) OR Pearson r > 0.3 across any HIL-surface flake pair OR any wave introduces >8 net cell-additions. On activation: extend `TripwireLedgerEntry` schema with sub-tripwire variant; emit canary firing artifact + per-cell flake-count summary + correlation matrix at wave-3 boundary; integrate into next-slab governance JSON as default-on. Reactivation gate: Slab 7c retrospective close — Murat reviews observed flake-rate + correlation; if all-clean, entry remains deferred forever; if any breach, activate for Slab 8+. |
| **`slab-7c-jtbd-1-operator-failure-recovery-resume-path`** | Slab 7c story-decomposition party-mode Round 2 2026-05-04 (John JTBD-1 gap audit) | When a gate goes red mid-orchestration, what's the operator's "I want to resume from gate N+1 without redoing N" path? Slab 7c covers per-gate verdict-digest match + GateError on mismatch (D3) but does NOT explicitly cover "trial paused at gate N → operator returns hours later → resume from N+1 cleanly without state-corruption." Resume semantics partially covered by 7a.5 conversation persistence; not fully covered for failure-state resume (vs happy-path resume). | **Slab 4 PRD scope** (Cora dev-graph + lockstep + ledger; M4 gate). Filed 2026-05-04 at Slab 7c epic-decomposition party-mode round; out-of-scope for Slab 7c body. Reactivation: Slab 4 PRD authoring round; Slab 4 inherits Slab 7c's HIL surface substrate + extends with operator-failure-recovery resume semantics including state-rehydration from `state/config/runs/<run-id>/checkpoint.json` + verdict-replay against post-N gates + gate-skip-with-justification operator surface. |
| **`slab-7c-jtbd-2-tripwire-user-surface`** | Slab 7c story-decomposition party-mode Round 2 2026-05-04 (John JTBD-2 gap audit) | 6/6 tripwires are covered as FRs + detection-infra ownership table per Murat M1 + 7-field schema enforced via NFR-7c-OD2 TripwireLedgerEntry Pydantic-v2 model. But what does the operator *see* when one fires? Currently the tripwire ledger is a JSONL audit-only artifact at `sprint-status.yaml::tripwire_events`; not surfaced to operator at runtime. Tripwires without an operator-facing surface are telemetry, not user-value. | **In-scope for 7c.21 retrospective close artifact** (mapping-checklist row-flip evidence + tripwire-firing-rate review per FR-7c-41); **runtime operator surface = Slab 4 PRD scope**. 7c.21 documents per-tripwire firing evidence in retrospective; Slab 4 authors a runtime operator surface (likely `app/marcus/cli/tripwire_status.py` + HUD integration) showing tripwire state during a run. Reactivation: Slab 4 PRD authoring; this entry closes when Slab 4 ships the runtime surface. |
| **`slab-7c-jtbd-3-paused-trial-resume-rendering`** | Slab 7c story-decomposition party-mode Round 2 2026-05-04 (John JTBD-3 gap audit) | Trial paused at G3, operator returns 4 days later — does §15 bundle render the partial state cleanly? Slab 7c covers §15 bundle emission at story 7c.15 (FR-7c-19 operator-side + FR-7c-29 Marcus-side per AMEND-4) but does NOT explicitly verify partial-state rendering for paused-trial-resume scenarios. Trial-2 forensic preservation under `state/config/runs/<run-id>/` (gitignored; operator-pinned per NFR-7c-OD6) provides the artifact substrate but not the rendering policy. | **Slab 4 PRD scope** (M4 gate). Filed 2026-05-04 at Slab 7c epic-decomposition party-mode round; out-of-scope for Slab 7c body. Reactivation: Slab 4 PRD authoring round; Slab 4 authors paused-trial-resume rendering policy + verifies `next-session-start-here.md` integration + tests partial-state-bundle rendering on pause-day-4 fixture. |

**Trial-evidence artifacts captured during 2026-04-28 trial-stop:**
- Trial bundle preserved at `state/config/runs/475df528-7d75-48a3-be56-82b54a0b7b8b/` (`run.json`, `decision-card-G1.json`, `checkpoint.json`, `cost-report.json/.md`, `trace-fixture.json`).
- Fix-on-the-fly playbook patches landed: §1.4 Postgres connectivity (dotenv-aware command + Windows winget unattended install path documented in `docs/dev-guide/local-postgres-setup.md`); §3.2 corpus-shape clarification (locator vs retrieval; disk-files-only convention); §3.3 deletion (initialization is part of §4.1, not a pre-step); §4.1 launch command + concrete-for-this-trial example + HTTP transport note; §4.3 HUD invocation patched with module form + `--watch --open --trial-id` canonical flags + rationale.
- `scripts/utilities/run_hud.py` defects fixed: (a) `--watch --open` together previously broken because `--open` block was outside the watch loop, fired only after Ctrl+C — now opens browser on iteration 1; (b) header/title showed "No active run" even with `--trial-id` because `rc.get("RUN_ID", ...)` only consulted legacy source-bundle's run-constants.yaml — now prefers `active_trial.trial_id (status)` when migrated trial is present.

**Total named follow-ons: ~34 active (+4 at vision-perceiver-real CLOSE 2026-06-21: believed-green-tracker-audit, vision-perceiver-empty-visual-elements-degradation, reading-path-bbox-out-of-range-bucketing, vision-recording-normalize-on-write); 24 closed-and-archived to §"Closed Entries — Archived" at bottom of file.** Pre-archive total was 53 filed across the project's history; the S1-P0-IH sweep segregated 24 closed entries (13 strikethrough + 10 RESOLVED-not-strikethrough + 1 REFRAMED) for hot-start scannability.

_(Pre-S1 historical narrative preserved below for audit trail; counts therein are pre-refresh and should not be acted on without consulting the archive section.)_

**Original entry: Total named follow-ons: 53 filed; 5 resolved 2026-04-27** (added: `5a-2-production-graph-entrypoint-substrate-gap` + `migration-6-2-promote-dependency-map-into-manifest` RESOLVED; new entries from Slab 6.1 close: `migration-6-2-promote-dependency-map-into-manifest` (now RESOLVED at Slab 6.2 close) + `slab-6-1-multi-pass-envelope-path-x-or-y` + `replay-regression-pack-hash-drift-pre-slab-6.1` + `slab-6-1-runner-compiled-edge-traversal` + `production-trial-envelope-lifecycle-invariants` + `slab-6-1-langsmith-runner-trace-id-real-binding`). **Active follow-ons: 47.** Notable next work: Slab-6.2 (`migration-6-2-promote-dependency-map-into-manifest`; ~1pt; Tier A prerequisite) before the Slab 6 trial-experience bundle. (Four new entries added 2026-04-24 from Story 2a.2 T2 + T4 + T7 + T8 party-mode waypoint reviews; one new entry 2026-04-25 from Story 2a.4 pre-T1 party-mode round â€” AC-B-OP live retrieval-dispatch reactivation pending Slab-3 marcus.dispatch.contract forward-port; six new entries added 2026-04-25 from Story 2a.4 G6 close + Slab 2a retrospective convening â€” dispatch-wrapper extraction candidate / SanctumLockViolation cross-specialist refactor / Â§12 size review / implementation-artifacts README / local env optional-dep hygiene / sanctum lock regen script; one entry closed at 2a.5; two entries added and one status-flip applied at 2b.3 close; four entries added 2026-04-26 at Story 2c.1 AC-D/AC-F for Wondercraft live-smoke deferral plus generator-validation polish; four entries added 2026-04-26 at Story 2c.2 for MCP prose divergence, live artifact deferral, metadata round-trip pending artifact, and future strict receipt metadata; one entry added 2026-04-26 at Story 2c.3 for the conditional M2 verdict pending 2c.2 live artifact (RESOLVED 2026-04-27); `2c-1-generator-auto-emit-retire-removal-support` resolved at Story 2c.4; one entry added and resolved at Story 3.6 for the Marcus envelope baseline capture; one missing named follow-on restored at Slab 4 close for the M5 dispatch-registry swap gate; one new entry added at Story 5a.2 for production clone-launcher and execution-equivalence reactivation; one new entry added at Story 5a.5 for the accepted 2026-05-03 conditional ship window; **five new entries added 2026-04-26 from Plausible-Token Substrate Contamination remediation (party-mode round 2 close-review riders): Layer-1/Layer-2 cascade collapse + OpenAI catalog snapshot staleness CI guard + tautology audit on cost/router fixtures + full perturbation procedure + broader silent-substrate audit**; **one new entry added 2026-04-27 from M2 ceremony A16 instance: WondercraftClient payload-shape + status-field defects**; **six Slab 6.1 close entries filed 2026-04-27 and `5a-2-production-graph-entrypoint-substrate-gap` resolved**.)

---

## Wave / Phase Deferreds (Epic-Group Gating)

### Cluster Intelligence Wave 2 (Epic 19-24 + 20c)

Per sprint-status.yaml Wave 2 Deferred block: 20c-4, 20c-5, 20c-6 deferred because profile runs will expose gaps more effectively than isolated A/B trials. Return when creative control iteration stabilizes. **Profile runs serve as the integration test that A/B trials were designed to be.**

### Wave 3 â€” Codify (epic 23)

- **23-2 G4 Gate Extension** â€” "codify what good looks like" (waits for Wave 2A + 2B to stabilize)
- **23-3 Bridge Cadence** â€” finalize stitching rules (same gate)

Wave 3 reactivation trigger: clustering + creative control stabilizes through profile-driven runs.

### Wave 4 â€” Downstream (epic 22, 24)

- 22-2 Storyboard B + Script
- 22-3 Flat-Play Preview
- 22-4 Generation Script & Publish
- 24-1 Assembly Contract
- 24-2 Descript Guide
- 24-3 ElevenLabs Short Segments

Reactivation trigger: Irene output stabilizes (Wave 3 close).

### Wave 5 â€” Capstone (epic 24)

- **24-4 Regression Suite** â€” full end-to-end validation. Depends on all prior waves.

---

## Forward-Port Deferred (hybrid-only, migration branch)

**Context:** the hybrid clone's `dev/langchain-langgraph-foundation` branch runs under FR60 forward-port freeze (ACTIVE since 2026-04-22). Wholesale `git merge upstream/master` is off-policy; per-capability forward-port through the [migration-guide Â§8 reconciliation checklist](../../docs/dev-guide/langgraph-migration-guide.md) is the convergence path. The inventory below tracks upstream capabilities that SHIPPED on primary after the hybrid branched but are NOT being forward-ported wholesale because the migration's LangChain/LangGraph re-platform IS the replacement.

Last refreshed: **2026-04-23** after Phase 1 forward-port (commit `6364f14`) on the hybrid branch.

### CLASS A â€” Pre-migration orchestrator work (not forward-ported; migration replaces)

| Upstream file / capability | Migration replacement | Reactivation trigger |
|---|---|---|
| `marcus/orchestrator/workflow_runner.py` | `app.manifest.compiler.compile()` + LangGraph `StateGraph` | Never â€” migration replaces this runtime path entirely. Closed. |
| `marcus/orchestrator/fanout.py` + `trial_smoke_harness.py` | Slab 3 Marcus orchestration + dispatch graph | Closed by Slab 3 completion. |
| `marcus/facade.py` upstream mutations | `app.marcus.*` (Slab 3) | Closed by Slab 3 completion. |
| `skills/bmad-agent-marcus/scripts/*` | `app.marcus.*` + `app.specialists.marcus.*` (Slab 3) | Closed by Slab 3 completion. |
| `scripts/generators/v42/templates/*.j2` | Migration's prompt-pack shape authored into `app.manifest` (no separate generator in migration) | Closed â€” pack-version bumps on primary do not cross the forward-port freeze. |
| `docs/workflow/production-prompt-pack-v4.2-*.md` upstream mutations | Migrated manifest at `state/config/pipeline-manifest.yaml` owns v4.2 topology; primary pack docs are pre-migration reference | Closed. |
| `state/config/pipeline-manifest.yaml` upstream content | Migration shape from Slab 1 Story 1.6 owns this file on this branch | Closed â€” upstream content edits do not cross the freeze. |

### CLASS B â€” New capabilities deferred to their slab-opening stories

| Upstream capability | Files | Target slab | Reconciliation checklist |
|---|---|---|---|
| **PR-R Marcus dispatch contract + boundary retrofits** | `marcus/dispatch/{__init__,contract}.py`, `scripts/marcus_capabilities/pr_rc.py`, `scripts/validators/check_dispatch_registry_lockstep.py`, `skills/bmad-agent-marcus/references/dispatch-registry.yaml`, `tests/marcus_dispatch/*`, `docs/dev-guide/how-to-add-a-dispatch-edge.md` | **Slab 3 Story 3.x** | Apply full migration-guide Â§8 reconciliation: Pydantic-v2 four-file-lockstep + dispatch-registry-as-manifest-companion + L1-validator-as-library-function + receipt-shape sanctum-fingerprint |
| **Consensus retrieval adapter (Story 27-2.5)** | `skills/bmad-agent-texas/scripts/retrieval/consensus_provider.py`, `tests/test_retrieval_consensus_provider.py`, `tests/test_retrieval_cross_validation.py`, `tests/test_retrieval_dispatcher.py`, `docs/dev-guide/how-to-add-a-retrieval-provider.md` updates | **Slab 2 Epic 2b (Texas)** | Port into `app.specialists.texas` per scaffold-conformance framework (tests/integration/scaffold_conformance/) |
| **Irene retrieval intake + Pass 2 authoring template (Story 7-1-irene-pass-2)** | `marcus/irene/{__init__,intake}.py`, `skills/bmad-agent-content-creator/references/{pass-2-authoring-template,retrieval-intake-contract}.md`, `state/config/schemas/{irene-retrieval-intake,segment-manifest}.schema.json`, `scripts/validators/pass_2_emission_lint.py`, `tests/irene/test_segment_manifest_schema.py` | **Slab 2 Epic 2b (Irene)** | Port into `app.specialists.irene` with scaffold-conformance check |
| **Evidence-bolster control surface** | `_bmad-output/implementation-artifacts/evidence-bolster-control-surface.md` + schemas/tests | **Slab 2 Epic 2b** | Port within Texas + Irene specialist scope |
| **PDG-3 flake-detection gate (CI infra)** | `scripts/ci/run_flake_gate.py`, `.github/workflows/pdg-flake-gate.yml` | **Slab 2 open (revisit)** | Orthogonal to orchestrator choice; decide at Slab 2 open whether the gate's assumptions still match the migration's test surface |

### CLASS D â€” Upstream deletions NOT adopted (hybrid keeps)

| Upstream deletion | Hybrid posture | Reason |
|---|---|---|
| `bmad-session-protocol-session-START.md` + `session-WRAPUP.md` | Hybrid keeps | Still canonical for hybrid session protocol; upstream relocated to Cora references. |
| `SESSION-HANDOFF.md` | Hybrid keeps | Hybrid lifecycle still uses it. |
| `next-session-start-here.md` | Hybrid keeps (intentionally gitignored line 96) | Hybrid identity â€” session guidance is per-clone, not synced with primary. |

### Refresh cadence

This section is refreshed at each forward-port pass (phases named in the session wrap-up notes). Entries marked "Closed" above do not need revisiting unless architecture amendment changes the migration's replacement strategy.

---

## Hygiene / Documentation Deferred (non-story)

| Item | Scope | Reason deferred | Trigger to pick up |
|---|---|---|---|
| **Closed-epic absolute-path rewrite (old repo name)** | ~30 closed-story specs under [_bmad-output/implementation-artifacts/](../implementation-artifacts/) for Epics 19â€“25 + 33, plus older `_bmad-output/planning-artifacts/` docs, plus some planning research artifacts. Content cross-links other artifacts via absolute `C:\Users\juanl\Documents\GitHub\course-DEV-IDE-with-AGENTS\...` URLs. After the 2026-04-22 local folder rename to `course-DEV-IDE-with-AGENTS-hybrid`, those prefixes no longer resolve on disk, though filename segments are still navigable. | Bulk-rewriting closed/frozen story specs for path cosmetics would pollute git history with a large no-behavior commit across epics that are already green. Operational paths were already handled in commit `fe0c314` (.vscode/tasks.json made `${workspaceFolder}`-relative, .pre-commit-config.yaml header renamed). Remaining are documentation-identity only. | (a) Any time a closed story is reopened for a material edit â€” sweep its stale absolute links in the same patch. (b) At an Epic retrospective where Audra flags unresolved-reference drift on a closed-artifact doc. (c) Operator-priority call during a session with idle capacity. Recommended approach when triggered: a single scripted sweep of `_bmad-output/implementation-artifacts/` replacing `C:/Users/juanl/Documents/GitHub/course-DEV-IDE-with-AGENTS/` â†’ repo-relative, on a dedicated hygiene branch, with before/after diff review. |

---

## Pre-existing test-failure cleanup follow-ons (filed 2026-04-26 post-Slab-3-close cleanup pass)

The Slab-3-close + Slab-4-close pytest sweeps revealed 19 pre-existing failures NOT caused by Slab 3/4 work. Tier-1-A cleanup pass 2026-04-26 mechanically fixed 10 of 19; the remaining 9 require deeper substrate investigation. Filed here per CLAUDE.md §3 deferred-inventory governance:

| Entry | Source | Description | Reactivation posture |
|---|---|---|---|
| **`pre-slab-3-cleanup-30-1-test-edit-violation`** (`test_no_preexisting_test_files_modified_in_30_1`) | Slab-3 close pre-existing failure cluster | Story 30-1 R1 amendment 12 zero-test-edit invariant violated — some test file under primary's test surface was edited that 30-1 forbids. Need to identify the violating diff + either revert OR amend the invariant for migration scope. | **Named follow-on, post-M5-ship cleanup, ~0.5pt.** Investigate which test file violates the invariant; decide whether to revert (preserve invariant) OR document migration-exemption in 30-1 R1 amendment 12 with rationale. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-33-1a-template-prose-drift`** (`test_existing_sections_match_source_pack_byte_identical`) | Slab-3 close pre-existing failure cluster | Story 33-1a verbatim-extraction invariant: §02A template prose has drifted from source-pack byte-identical baseline. Either (a) re-regenerate template from source pack (33-1a regen flow), OR (b) document the intentional drift with party-mode signoff. | **Named follow-on, post-M5-ship cleanup, ~0.5pt.** Run 33-1a template-regen for §02A; if drift was intentional, party-mode ratify + bump pack version. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-33-2-pipeline-manifest-stub-key-collision`** (`test_pipeline_manifest_keys_do_not_shadow_other_state_configs`) | Slab-3 close pre-existing failure cluster | `state/config/pipeline-manifest-substrate-stub.yaml` shadows top-level keys from `state/config/pipeline-manifest.yaml` (`block_mode_trigger_paths`, `edges`, `entrypoint`, `frozen_graph_version`, `lane`, `nodes`, `pack_version`). Stub may be vestigial post-Slab-1 substrate consolidation. | **Named follow-on, post-M5-ship cleanup, ~0.25pt.** Either (a) delete `pipeline-manifest-substrate-stub.yaml` if vestigial, OR (b) namespace its keys under `_stub:` prefix. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-33-3-v42-pack-hand-edit`** (`test_v42_pack_commit_is_regeneration_output`) | Slab-3 close pre-existing failure cluster | v4.2 pack content has hand-edits not from regeneration output (`# Production` strings differ). Either re-regenerate OR document the intentional hand-edits per Tier-1 governance per CLAUDE.md §"Pipeline lockstep regime" Pack Versioning Policy. | **Named follow-on, post-M5-ship cleanup, ~0.5pt.** Identify the hand-edit; if intentional, run pack version bump per Tier-1 dev-agent authority gated by Cora's block-mode hook. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-texas-roster-status-drift`** (`test_roster_placeholder_present_with_expected_status[consensus-retrieval-ratified]`) | Slab-3 close pre-existing failure cluster | Texas roster expects status `'ratified'` for consensus-retrieval but actual is `'ready'`. Status-enum drift between roster registry and operator-update workflow. | **Named follow-on, post-M5-ship cleanup, ~0.25pt.** Either (a) update consensus-retrieval status from 'ready' → 'ratified' if operator-confirmed, OR (b) update test expectation. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-transform-registry-section-coverage`** (`test_every_format_covered_or_exempted`) | Slab-3 close pre-existing failure cluster | `transform-registry.md` has sections present that are NEITHER covered by lockstep checker NOR declared exempt. Either bump checker OR add to exemption list. | **Named follow-on, post-M5-ship cleanup, ~0.25pt.** Identify uncovered sections + categorize each as covered-by-checker OR exempt-with-rationale. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-orchestrator-skill-path-preflight`** (`test_orchestrator_agent_skill_path`) | Slab-3 close pre-existing failure cluster | System-level integration test invoking preflight subprocess fails with non-zero exit (cascade from substrate state; preflight may be failing on env vars or MCP config). | **Named follow-on, post-M5-ship cleanup, ~1pt.** Investigate preflight subprocess failure mode; may be cascade from missing OPENAI_API_KEY OR MCP server config. NOT trial-run-blocking (preflight per `scripts/utilities/trial_run_preflight.py` is a separate sweep). |
| **`pre-slab-3-cleanup-motion-preview-blocked-count`** (`test_motion_dry_run_preview_adds_marcus_motion_sequence`) | Slab-3 close pre-existing failure cluster | Motion dry-run preview asserts `blocked: 0` but live state returns `blocked: 1`. Pre-migration motion-preview test; may have been broken by Slab-2 specialist additions to motion path. | **Named follow-on, post-M5-ship cleanup, ~0.5pt.** Investigate which Slab-2 specialist addition introduced the blocked: 1 state; decide whether to update test expectation OR fix the motion-preview path. NOT trial-run-blocking. |
| **`pre-slab-3-cleanup-irene-sanctum-persona-md`** (`test_irene_sanctum_scaffolded`) | Slab-3 close pre-existing failure cluster | Irene sanctum at `_bmad/memory/irene-sidecar/` (or equivalent) missing `PERSONA.md`. Either operator-content-population pending (per A-R5-2c.2 pattern: skeleton-only at story-author, operator populates content) OR test is over-strict. | **Named follow-on, ~0.25pt + operator content-authoring time.** Operator runs First Breath ceremony for Irene sanctum; test will pass once PERSONA.md lands. Same pattern as 2c.2 wanda-sidecar skeleton + operator-population separation. NOT trial-run-blocking. |

**Pre-cleanup count:** 19 failures.
**Post-cleanup count:** 9 failures (10 fixed mechanically: 5 generator C3-row baseline drift + 3 .env-policy inversions + 1 Marcus SKILL.md ceiling + 1 wanda CLONE-FORK-NOTICE.md).

---

## Pre-Trial-3 cleanup S2 follow-on (filed 2026-05-07)

Filed at S2 close per Murat AM-15 + Mary harvest-discipline. NOT trial-3-blocking; tackle in S6 housekeeping batch.

| Entry | Source | Description | Reactivation posture |
|---|---|---|---|
| **`s2-test-cleanup-residual-37`** | pre-Trial-3 cleanup S2 close 2026-05-07 (commit `195be7c`) + post-S2 review amendments | After S2 marcus/ namespace collapse + import-linter contract surgery, 37 net-new test failures remain vs post-S1 baseline (48 → 85). These are NOT substrate failures — they are AST-scan / file-IO / shim-era pin tests that need long-tail path-string updates. Categories: composition chain tests scanning legacy paths; coverage-manifest deterministic-emit tests; event-type-validator state-reset tests; fit-report-emitter type-registry tests; test_trial_run_e2e smoke harness scans; test_marcus_orchestrator_write_api / test_lesson_plan_log_atomic_write tests; structural import-linter contract count tests; unit/marcus/test_marcus_cold_read.py + test_marcus_package_canonical_extension.py. **+1 from Amelia post-S2:** `tests/integration/marcus/test_marcus_import_linter_contract.py::test_dispatch_contract_shim_re_export_identity` is now a tautology (`X is X` after both imports collapsed to canonical path) — refactor or delete at S6. **+1 from Murat post-S2:** `_bmad-output/maps/coverage-manifest/lesson-plan-envelope-coverage-manifest.json` data-file path-string update (single-line fix; can ride S6 OR get committed as a quick patch pre-Trial-3 launch hygiene). | **Named follow-on, ~3-5pt; tackle at S6 housekeeping batch.** Each test fix is small (~5-15 min) but there are many. Operator-priority decision: bulk-fix at S6 OR defer to post-Trial-3 housekeeping. NOT trial-3-blocking — substrate is clean; only test-side hardcoded path-strings remain. |
| **`s3-forensic-preservation-ci-guard`** | pre-Trial-3 cleanup S3 post-review 2026-05-07 (Murat amendment) | `postmortem.md`'s "Forensic evidence pointers (do-not-delete paths)" is prose-only, not enforceable — a future cleanup pass running `_artifacts/` retention logic will not read postmortems. Risk: multi-trial-history accidental erasure. | **Named follow-on, ~0.5pt; S5 or S6 batch.** Author `tests/trial/test_forensic_preservation.py` that walks `docs/trials/trial-*/postmortem.md`, extracts do-not-delete paths, asserts each exists; wire into CI. ~20 LOC. NOT trial-3-blocking; protects future trial postmortems from drift. |
| **`s6-tier-3-post-trial-3-housekeeping-batch`** | pre-Trial-3 cleanup S6 close 2026-05-08 (Amelia pre-S6 Tier-3 triage; operator NO-DEFERRALS-but-sequenced posture) | At S6 close, the following P2/P3 items are filed-not-dropped per Amelia pre-S6 Tier-3 triage (zero Trial-3 quality-gate impact; ~15-20 hr aggregate; sequence post-Trial-3): (1) s2-test-cleanup-residual-37 38-item bulk fix; (2) Relocate 3 underscore-named Python modules out of skills/ namespace; (3) Move skills/reports/ → _bmad-output/reports/skill-conformance/; (4) Audit test_bmb_scaffold.py 26-skip; (5) Land housekeeping-3 specialist-side producer models; (6) Land housekeeping-4 vera+dan sidecar cleanup; (7) ADR 0004 lane-independence canonical form (post-Trial-3 evidence-driven); (8) Retire ENVELOPE-CARRIER-HACK; (9) Resolve placeholder specialists kim/vyx/aria/mira/tamara; (10) Rename app/cora/ → app/dev_workflow/ (large blast radius; coordinate with Cora archival); (11) Port heartbeat_check.mjs + smoke_*.mjs to Python (sandbox-AC alignment); (12) Retire passthrough_specialist._stub; (13) §Project Tree update for Slab-7c additions (Winston A1; descriptive doc currency); (14) Standalone docs/SOURCE-OF-TRUTH-INDEX.md (Paige post-S5 verdict: drop; v5+methodology+sources-of-truth.md cover operator); (15) Skill-dir archives (Audra+Cora to skills/_archive/); (16) 10+ legacy sidecar archives + 8 legacy script archives. | **Named follow-on cluster, ~15-20pt aggregate; post-Trial-3 housekeeping batch.** Operator-priority decision when to schedule. Per Amelia pre-S6: "no deferrals" honored because nothing leaves the system — entries are queued in the canonical register with explicit reactivation triggers. NOT trial-3-blocking. |
| **`s4-per-section-operator-sub-blocks`** | pre-Trial-3 cleanup S4 post-review 2026-05-07 (Sally amendment 2/3/4) | v5 §sections retained v4.2's Marcus-addressed prompt body voice (3rd person); Sally pre-S4 amendment proposed adding per-§ "Operator at this gate" sub-blocks (verb-set + edit-payload + irreversibility flag + per-§ ref) at top of each HIL-bearing §section. Deferred to S5 P1 to avoid bloating S4 scope; v5 §0 Operator Card line 53 already disclosure-corrected to point at hil-verb-legend.md as the canonical at-gate reference in the meantime. | **Named follow-on, ~2-3pt; S5 P1 batch.** Author per-§ operator sub-blocks at the 14 HIL-bearing §sections (§02A / §04 / §04A / §04.5 / §04.55 / §05.5 / §06B / §07B / §07C / §07D / §07F / §08B / §11 / §11B / §15). Pull verb-set + edit-payload + irreversibility from hil-verb-legend.md; lockstep maintenance. Includes voice convention codification (2nd-person operator-imperative for sub-blocks; 3rd-person Marcus-addressed for prompt bodies) per Sally pre-S4 amendment 4. NOT trial-3-blocking; the Operator Card + verb-legend cross-link covers the gap. |
| **`s3-cache-hit-rate-evidence-row`** | pre-Trial-3 cleanup S3 post-review 2026-05-07 (Murat NIT) | Methodology.md §3 evidence taxonomy doesn't have a row for cache-hit-rate evidence; could land in cost-report shape. Defer to post-Slab-6 (cache-hit-rate is Slab 6 NFR target). | **Named follow-on, ~0.25pt; defer to post-Slab-6 cleanup.** Add row to §3 once Slab 6 cache-hit-rate harness produces canonical evidence shape. NOT trial-3-blocking. |
| **`winston-post-s2-followon-architecture-currency`** | post-S2 review 2026-05-07 (Winston amendments A1+A2+A3) | Three architecture-currency follow-ons surfaced at post-S2 review: **A1**: update `architecture-langchain-langgraph-migration.md` §Project Tree (~lines 978-1011) to reflect S2 post-collapse `app/marcus/` shape (intake/ as package not file; lesson_plan/; orchestrator/ with full Slab 6/7 surfaces; facade.py; dispatch/; cli/). **A2**: Add §10 Decision Log entry to `docs/dev-guide/composition-specification.md` row dated 2026-05-07: "Marcus namespace fully collapsed; legacy `marcus/` package retired. References in §3.3 / §6 unchanged (already target-shape). Authority: operator + party-mode V13." **A3**: file deferred-inventory entry `migration-tech-debt-m2-c2-import-linter-redundancy-fold` for the M2/C2 fold (M2: `app.marcus → app.cora`; C2: same shape). | **Named follow-on, ~1pt; S6 housekeeping batch.** All three amendments are small editorial; A3 is itself a deferred-inventory hygiene entry. NOT trial-3-blocking. |

---

## Migration tech-debt follow-ons (filed 2026-04-26 post-Slab-3-close runway prep)

Cross-cutting tech debt that grew during Slabs 1-4 + that should be properly tracked for post-M5-ship triage. None are trial-run-blocking; all are post-ship cleanup candidates per Bucket-F harmonization framing in the runway assessment.

| Entry | Source | Description | Reactivation posture |
|---|---|---|---|
| **`migration-tech-debt-ruff-cleanup`** | Tier-1-A runway prep 2026-04-26 (post-Slab-3-close ruff sweep) | 1322 ruff errors at Slab-3-close baseline → 977 after `--fix` auto-cleanup (mechanical) → 998 post-Slab-4 (Codex's 4.x adds ~20 new flagged lines). Remaining errors are manual/unsafe-fix variants spanning legacy primary-repo code + migration-shipped surfaces. | **Named follow-on, post-M5-ship cleanup, ~3-5pt.** Categorize remaining 998 errors by ruff rule code; fix high-signal categories (B = bugbear; F = pyflakes); document policy-exempt categories in `pyproject.toml [tool.ruff.lint.per-file-ignores]`. NOT trial-run-blocking; CI does NOT fail on these. Operator-priority decision when to schedule. |
| **`migration-tech-debt-utility-script-consolidation`** | Tier-1-E runway prep 2026-04-26 (`scripts/utilities/README.md` index) | 56+ utility scripts across `scripts/utilities/` with overlapping concerns: `operator_polling.py` + `operator_directives_poll.py`; `emit_preflight_receipt.py` + `emit_ingestion_quality_receipt.py`; `fix_slide_content.py` + `fix_test_file.py` + `repair_validator.py` + `restore_validator_clean.py`; `find_mojibake.py` + `normalize_mojibake.py`; three lockstep checkers (pipeline-manifest + manifest + learning-event). Argparse conventions vary; exit-code conventions vary; logging-format conventions vary. | **Named follow-on, post-M5-ship cleanup, ~5-8pt.** Consolidate per DRY-overlap candidates flagged in `scripts/utilities/README.md`; harmonize argparse + exit-code + logging conventions per consistency-target spec; produce final consolidated index. NOT trial-run-blocking. |
| **`migration-tech-debt-requirements-mirror-stale`** | Tier-1-D runway prep 2026-04-26 (deps audit) | `requirements-mirror.txt` is from 2026-04-21 (pre-Slab-2; reflects pre-langchain/langgraph install state). Migration's actual `.venv` has 13 additional installed deps (langchain/langgraph/langsmith/fastapi/uvicorn/httpx/psycopg/watchdog) that I declared in pyproject.toml at Tier-1-D but the mirror file is unchanged. Operator running `pip freeze --exclude-editable` would now produce a much larger output. | **Named follow-on, ~0.25pt.** Re-generate `requirements-mirror.txt` post-Slab-4-close to reflect actual `.venv` state; commit as snapshot. Re-do at each Slab close OR per operator preference. NOT trial-run-blocking. |
| ~~**`migration-tech-debt-app-marcus-stub-disposition`**~~ | ~~Slab-3 substrate-aware adaptation discovery 2026-04-26~~ | **CLOSED 2026-05-07 at pre-Trial-3 cleanup S2 close (commit `195be7c`).** Resolution direction was REALITY-CORRECTED relative to the original 2026-04-26 framing: by S2 dispatch time, Slab 6/7a/7b/7c production runtime had landed in `app/marcus/*` (not legacy `marcus/`), inverting the canonical-vs-shim direction. S2 collapse therefore: (a) copied legacy `marcus/` content INTO `app/marcus/` (preserving 30-1 internal duality + Quinn single-writer rule + Maya-as-one-voice per R1 amendments 12+13+17); (b) deleted legacy `marcus/` package entirely; (c) added M5 import-linter contract banning top-level `marcus.*` imports as structural backstop; (d) D14 architecture-of-record entry codifies the single-namespace discipline. 30-1 contracts verified GREEN through S2 close (17/17). Direction: cleanup is COMPLETE; the marcus duality bug class is eliminated. | (Resolved.) |

**Total tech-debt follow-ons: 4** (filed 2026-04-26 in this entry; complementary to the 9 pre-cleanup follow-ons above).

---

## Inventory Summary

> **Refreshed 2026-05-07 at pre-Trial-3 cleanup S1 P0-IH (Mary AM-5 amendment).** Counts reflect ACTIVE entries only — 24 closed entries segregated to §"Closed Entries — Archived" at the bottom of this file (preserved verbatim for audit trail; no content deleted). Hot-start scan at session-WRAPUP / next-session-START should now skim ACTIVE entries only.

| Category | Count (post-S1 P0-IH refresh) | Reactivation posture |
|---|---|---|
| Backlog epics | 5 (Epic 15, 16, 17, 18, Post-M5 Greenfield Specialists) | Triggered by Epic 33 meta-test, trial-run evidence, or post-M5 specialist need |
| Deferred stories in active epics | 4 (20c-4/5/6, 20a-5) | Triggered by profile-driven run gaps |
| Named-but-not-filed follow-ons (ACTIVE) | ~33 | Triggered by parent-story closure + condition. Refreshed post-Slab-7c retrospective + S1 P0-IH archive sweep. **+3 at Trial-4 close 2026-06-19** (`trial-4-binding-variant-voice-picker` 🟠 big-leap / `trial-4-specialist-summary-artifact-list-reporting-gap` / `trial-4-modemismatch-recoverable-family`); 2 Trial-4 fixes CLOSED same session (`10befac`/`1b629f3`). |
| Closed entries (archived for audit trail) | **25** | Includes 14 ~~strikethrough~~ + 10 RESOLVED-not-strikethrough + 1 REFRAMED. See §"Closed Entries — Archived". S6 close: roll up disposition into permanent registers (anti-pattern catalog / governance JSON / sprint-status / ADRs). +1 vs 2026-05-07 S1 P0-IH archive: `migration-tech-debt-app-marcus-stub-disposition` closed at S2 close 2026-05-07 commit `195be7c` (Paige post-S2 review counter-update). |
| Wave 3/4/5 stories (partially deferred via gating) | ~10 | Triggered by prior-wave stabilization |

**Near-term candidates (post-Epic-33):**
1. 15-1-lite-irene + 15-1-lite-gary (immediately after 15-1-lite-marcus meta-test passes)
2. 20a-5 Exemplar Library reactivation (operator priority call)
3. Full Epic 15 chain (after first tracked trial run)
4. Epic 18-7 workflow-family framework (if operator prioritizes new content types)

---

## Governance Hooks (where this inventory gets consulted)

- **[CLAUDE.md Â§Pipeline lockstep regime](../../CLAUDE.md)** â€” binds retrospective runs to consult this inventory during "Next Epic Preparation" phase per the `bmad-retrospective` workflow Â§Two-part format line 15.
- **[next-session-start-here.md](../../next-session-start-here.md)** â€” standing hot-start line surfaces inventory counts.
- **Each story spec** â€” when authoring a new story that names a follow-on, the author adds the entry to Â§Named-But-Not-Filed Follow-Ons above.

---

## References

- [_bmad-output/planning-artifacts/epics.md](epics.md) â€” per-Epic scope authority; backlog epics declare their stories + ACs there.
- [_bmad-output/implementation-artifacts/sprint-status.yaml](../implementation-artifacts/sprint-status.yaml) â€” canonical story state; comments carry Wave-group deferral rationale.
- [_bmad-output/maps/deferred-work.md](../maps/deferred-work.md) â€” **different surface**: tracks deferred G6 code-review FINDINGS per story, not deferred stories themselves. Relevant at retrospectives as a sibling input.
- [docs/dev-guide/pipeline-manifest-regime.md Â§Pack Versioning Policy](../../docs/dev-guide/pipeline-manifest-regime.md#pack-versioning-policy) â€” relevant for Epic 18 new-pack-type framing.
- [CLAUDE.md](../../CLAUDE.md) â€” governance umbrella; names the retrospective + session-start consultation requirement.

---

## Slab 7a-Filed Named-But-Not-Filed Follow-Ons (added 2026-04-28)

Filed concurrent with Slab 7a Epic 1 PRD + Epics+Stories authoring. Trigger conditions name the slab/event that reactivates each entry.

| Follow-on | Parent | Condition | Trigger |
|---|---|---|---|
| **Slab 7b sandbox-AC inventory PR** (5 entries: gamma, kling, elevenlabs, wondercraft, dan-api-tbd) | Slab 7a NFR-CG1 | Hard precondition before any Slab 7b story opens. **Codex-authored.** | Slab 7b PRD authoring opens; Codex authors PR adding 5 entries to `docs/dev-guide/migration-ac-sandbox-inventory.json` per migration-AC governance. NOT a Slab 7a precondition (Slab 7a is orchestration-only; no live-API surfaces). |
| **Slab 7a-4 K-contract tripwire** (escalate-to-dual-gate-via-party-mode at K-actual >4.08K = 1.7× band-floor) | Slab 7a Story 7a-4 | Encoded in `migration-story-governance.json` as `k_contract` field per Step 1 batch-approved adjustment. | Reactivates if 7a-4 K-actual breaches 4.08K during dev; closes dev round; party-mode triage. |
| **Slab 7a-8 golden-trace fixtures from trial-2** | Slab 7a Story 7a-8 close + first trial-2 | Trial-2 must run before record-once-replay-forever fixtures land. If trial-2 hasn't run by 7a-8 close, fixtures land in Slab 7b kickoff. | Reactivates at trial-2 close OR Slab 7b opens; Slab 7b inherits as input, does NOT block on them. |
| **Slab 7a-2 PRODUCTION_GATE_IDS callsite migration** (grep-and-update test fixtures that pin current 4-gate behavior) | Slab 7a Story 7a-2 | Removing hardcoded `PRODUCTION_GATE_IDS = frozenset({"G1","G2C","G3","G4"})` from `app/manifest/compiler.py:42` may cascade to test fixtures that assert the literal frozenset shape. Per Amelia's Step 1 finding: "AC-level work, not a one-liner." | Reactivates at 7a-2 dev open; T1 readiness includes grep-for-callsites + test-fixture refresh budget. |
| **Slab 7a-4 HTML review-pack full styling** (post-Slab-7a polish) | Slab 7a Story 7a-4 K-contract | K-contract bounds 7a-4 HTML review-pack to skeleton-only (semantic structure + checkbox + delta-field; no full styling). | Reactivates as Doc-7-D candidate or Slab 8 polish task once trial-2 evidence informs styling priorities. |
| **Slab 7a-7 + Doc-7-D documentation-completion follow-on** (full anti-pattern catalog from trial-2 evidence) | Doc-7-D scope | Anti-pattern catalogs are post-trial-evidence harvest, not pre-trial speculation. | Reactivates at Doc-7-D PRD authoring (sequenced after first tracked trial-2 post-Slab-7b). |
| **Polish-pass deferred items** (move Sibling PRD context + Meta-Directive to end-of-doc Governance Appendix; Section 6 trim to ≤30-line summary; Section-flow bridges at Sections 7/9/12; cross-link prefixing) | Slab 7a PRD §Polish-Pass Notes | Larger structural recommendations from Step 11 polish-pass not applied inline (operator-decision risk on content regression); deferred to post-trial-2 doc-pass. | Reactivates at Doc-7-D scope OR if reader-friction surfaces at next-session hot-start. |
| **Slab 7a impact assessment update for `pipeline-manifest-regime.md` §Slab 7a fold-flag mechanism** (Doc-7-D candidate per Paige's polish-pass) | Doc-7-D scope | Documents the fold-flag mechanism for downstream pack-version evolution. | Reactivates at Doc-7-D PRD authoring. |

## Slab 7a 2026-04-29 amendments (operator-ratified)

| Amendment | Date ratified | Trigger context | Resolution |
|---|---|---|---|
| **slab-7a-sg-2-floor-amended-34-to-33** | 2026-04-29 | Codex 7a.6 dev T1 checkpoint surfaced 33 actual rows in `_bmad-output/planning-artifacts/slab-7-legacy-migrated-mapping-checklist.md` vs PRD prose claim of 34. Investigation (Claude 2026-04-29): the v4.2 prompt pack has §01-§15 = 30 numbered prompts; the checklist mapping landed at 33 rows naturally (some prompts have multiple operator-control levers). The "34" in PRD prose was an off-by-one bookkeeping error during PRD authoring. No "missing 34th lever" exists in the v4.2 source. | **Operator ratified Option B** (amend SG-2 floor to 33). Find-and-replace 34→33 across `_bmad-output/planning-artifacts/prd-slab-7a-inter-gate-orchestration.md`, `_bmad-output/planning-artifacts/epics-slab-7a-inter-gate-orchestration.md`, `_bmad-output/implementation-artifacts/migration-7a-6-vocabulary-registry-parity-table.md`. The "no row silently dropped" invariant is preserved — the floor count is corrected; no content was dropped. SG-2 floor is now 33 rows; row-count CI assertion fires on `len(rows) != 33`. Codex can dev 7a.6 against the 33-row floor. |
| **7a-2-deferred-resume-mode-multi-gate-pause** | 2026-04-29 | Story 7a.2 dev surfaced that `resume_production_trial` raises `GateBypassError` at the FIRST encounter of a downstream active gate after resume (correct per FR-A23 silent-bypass refusal). Multi-gate pause-and-resume (G1 verdict → resume → pause-at-G2C → G2C verdict → resume → pause-at-G3 → ...) requires extending the resume path with the same pause-and-checkpoint mechanism that start-mode has. Bigger than 7a.2 scope. | Deferred to post-Slab-7a substrate evolution. Existing tests now assert the `GateBypassError` raise at G2C as the correct interim behavior. Reactivates when operator workflow needs multi-gate pause-and-resume. |
| **7a-1-deferred-resume-mode-directive-rederive** | 2026-04-29 | Story 7a.1 close — `resume_production_trial` does NOT thread `directive_path` (start-mode only). Safe under current Path Z first-contribution-wins. | Reactivates at 7a.4/7a.5 work that may shift gate placement pre-Texas. |
| **7a-1-deferred-trial-start-guide-augment** | 2026-04-29 | Story 7a.1 P-R3 NON-BLOCKING — augment or create `docs/operator/trial-start-guide.md` with confirm-or-edit prompt UX screenshot. | Reactivates as Doc-7-D candidate after trial-2 informs operator-doc priorities. |

## Slab 7a Epic-close amendments (2026-04-29)

Filed at Slab 7a Epic close (2026-04-29) as deferred-inventory entries per CLAUDE.md §Deferred-inventory governance binding consultation point #1 (every Epic retrospective).

| Amendment | Date filed | Trigger context | Resolution |
|---|---|---|---|
| **slab-7a-trial-2-golden-trace-fixtures-deferred-to-slab-7b** | 2026-04-29 | Slab 7a Story 7a.8 close — AC-7.8-K golden-trace fixtures from trial-2 cannot land before trial-2 runs. Per Codex T11 self-review recommendation: Slab 7b inherits as input; does NOT block on them. | Reactivates at trial-2 close OR Slab 7b kickoff (whichever comes first). Slab 7b inherits as input artifact to seed record-once-replay-forever fixtures for the 11-roster activation stories. |

## Slab 7b Epic-close amendments (2026-05-01)

Filed at Slab 7b 7b.12 Gate-2 ceremony close (2026-05-01) per CLAUDE.md §Deferred-inventory governance binding consultation point #3 (story author adds named follow-ons to inventory at close). Both entries ratified by party-mode UNANIMOUS 4/4 (John+Mary+Amelia+Murat) on 2026-05-01.

| Amendment | Date filed | Trigger context | Resolution |
|---|---|---|---|

## Trial-2 2026-05-04 structured-stop findings (added 2026-05-04)

Filed at Trial-2 structured-stop ceremony 2026-05-04 per CLAUDE.md §Deferred-inventory governance binding consultation point #3 (story author adds named follow-ons to inventory at close). Both entries collected as live evidence during the ceremony. Full ceremony record: `_bmad-output/implementation-artifacts/trial-2-postmortem-2026-05-04.md`.

| Amendment | Date filed | Trigger context | Resolution |
|---|---|---|---|

## Slab 7c Story 7c.5.G1 Follow-Ons (2026-05-05)

| Follow-on | Date filed | Trigger context | Resolution |
|---|---|---|---|
| **slab-7c-g1-g4-decision-card-doc-refresh** | 2026-05-05 | Story 7c.5.G1 backward-consumer audit found documentation references in `docs/operator/production-trial-playbook.md` and `docs/dev-guide/sources-of-truth.md` that describe the legacy G1-G4 `DecisionCard` shape. G1 is now migrated to `DecisionCardBase`; G2C/G3/G4 remain legacy until their own extend-and-audit stories close. Updating docs mid-series would churn the shape repeatedly. | Reactivates after all four extend-and-audit stories (G1/G2C/G3/G4) close. Refresh operator/dev docs once against the final 7c DecisionCard family shape. |

## Slab 7b Story 7b.2 Quinn-R Follow-Ons (2026-04-29)

| Follow-on | Parent | Condition | Trigger |
|---|---|---|---|
| **quinn-r-sanctum-sidecar-to-bmb-migration** | Story 7b.2 T2 | CLOSED at Story 7b.2 T2. Quinn-R canonical sanctum migrated from `_bmad/memory/quinn-r-sidecar/` to `_bmad/memory/bmad-agent-quinn-r/` with six-file BMB pattern. Legacy sidecar preserved unchanged for audit. | Closed; no reactivation needed unless a post-trial audit finds a missing translated memory item. |
| **quinn-r-sidecar-cleanup-post-trial-2-validation** | Story 7b.2 T2 | Named-but-not-filed cleanup. Legacy `_bmad/memory/quinn-r-sidecar/` remains in place until trial-2 validates no consumer still references it. | Reactivates after trial-2 close; delete or archive the legacy sidecar only after grep/runtime evidence confirms no active consumer path. |

## Post-S6 Housekeeping Probe — Slice A "Coverage-Manifest Path-Pin" (filed 2026-05-19)

Filed during post-S6 housekeeping session (Path 1; party-mode-gated). Slice A was opened as a 1-edit path-pin fix in `app/marcus/lesson_plan/coverage_manifest.py` (9 stale `marcus/lesson_plan/...` strings → `app/marcus/lesson_plan/...`, lockstep-aligning with the canonical JSON manifest at `_bmad-output/maps/coverage-manifest/lesson-plan-envelope-coverage-manifest.json`). The slice was REVERTED per Dr. Quinn impasse-synthesis R5 "Probe-Capture" (3-of-4 party-mode consensus with named Amelia dissent). Reframe: the diff was a diagnostic probe whose value is the forensic evidence, not the LOC on disk. Evidence captured here in lieu of commit, freeze preserved for Trial-3.

| Follow-on | Filed | Trigger context | Resolution |
|---|---|---|---|
| **`s6-housekeeping-coverage-manifest-path-pin-probe`** | 2026-05-19 | **Probe summary.** Slice A targeted `DEFAULT_COVERAGE_INVENTORY` in `app/marcus/lesson_plan/coverage_manifest.py:223+238+250+262+279+295+311+328+345` — 9 entries carrying legacy `module_path="marcus/lesson_plan/..."` strings post-S2 namespace collapse. The companion JSON `_bmad-output/maps/coverage-manifest/lesson-plan-envelope-coverage-manifest.json` is already lockstep at `app/marcus/...`. **Cascade math measured:** broad regression dropped 88→78 (−10 net); 12 failures cleared (8× `tests/test_trial_run_e2e.py::*` + 2× `tests/test_coverage_manifest_regenerates_on_current_state.py::*` + 1× `tests/gates/section_02a/test_g0_poll_surface_dsl_registration.py::test_poll_surface_registers_three_mandatory_transports` + 1× targeted-slice case). **Two surfaced lockstep follow-ons:** (a) `tests/test_coverage_manifest_regenerates_on_current_state.py:51` pins LEGACY canonical path `assert entry.module_path == "marcus/lesson_plan/quinn_r_gate.py"` — needs lockstep-update + 30-1 zero-test-edit allowlist extension (`_ALLOWED_MODIFIED_PATHS_UNDER_TESTS` in `tests/contracts/test_30_1_zero_test_edits.py`); (b) **TW-7c-4 substrate tripwire fired by design** — `tests/audit/test_audit_tw_7c_4_no_live_dispatch_scope_creep.py::test_live_dispatch_python_scope_is_bounded` enforces `app_scope == []` working-tree freeze with 0 `app/` paths in `PERMITTED_PYTHON_DIFFS`. **Diff preserved verbatim** at `.tmp/slice-a-diff.patch` (9308 bytes, gitignored) for re-application post-Trial-3. **Party-mode round transcript** preserved in session chronology; Dr. Quinn R5 synthesis ratified by Winston + John + Murat (3-of-4); Amelia named principled dissent (preferred R3: same-commit lockstep with 30-1 allowlist amendment). | **Tier-1 of post-Trial-3 `s6-tier-3-post-trial-3-housekeeping-batch` cluster (already filed).** Reactivation: post-Trial-3, when the pre-Trial-3 freeze naturally lifts. Re-apply the 9-line diff to `coverage_manifest.py` AND lockstep-update `tests/test_coverage_manifest_regenerates_on_current_state.py:51` to assert `app/marcus/lesson_plan/quinn_r_gate.py` AND extend the 30-1 allowlist OR move the assertion to a parametrized fixture in a NEW allowlisted file per Amelia's 3-class taxonomy. Expected impact: −12 broad-regression failures (clears 10 trial_smoke_harness/coverage_manifest tests + the 2 new lockstep-pin failures + the pre-existing 30-1 guard violation if allowlist extension is the path). **Pre-drafted but not dispatched** `bmad-correct-course` sprint-change motion lives at `.tmp/slice-a-post-trial-3-correct-course-draft.md` (gitignored) for post-Trial-3 substrate-amendment dispatch. |
| **`s6-housekeeping-stale-test-path-pins-survey`** | 2026-05-19 | **Probe by-product.** Slice A's `tests/test_coverage_manifest_regenerates_on_current_state.py:51` discovery confirms the post-S2 stale-path-pin class extends into TEST files, not just SOURCE files. The `s2-test-cleanup-residual-37` 38-item cluster anticipates this category. The surveying question — "what other test files pin legacy `marcus/...` paths that broke at S2 collapse?" — is now answerable by mechanical grep but was not previously scoped. | **Folded into `s2-test-cleanup-residual-37`.** Reactivation: post-Trial-3, as the first sub-batch of the residual cleanup. Run `rg -nl '"marcus/(lesson_plan|intake|orchestrator|dispatch|facade)' tests/` to enumerate the full surface. Each match becomes a per-file path-pin update; bulk-fix as one batch when 30-1 allowlist is extended. |

**Governance amendment ratified this session (2026-05-19):** **Impasse-resolution chain.** When party-mode cannot converge across one full round, escalate to **Dr. Quinn (Master Problem Solver / `bmad-cis-agent-creative-problem-solver`)** for consensus synthesis. If Dr. Quinn fails to synthesize, escalate to **John (PM)** for final decision and next-step declaration. Operator-ratified directly; precedent established at Slice A R1-vs-R2-vs-R3 impasse 2026-05-19 (Dr. Quinn succeeded with R5 "Probe-Capture"). To be folded into `CLAUDE.md §BMAD sprint governance` + `.cursor/rules/bmad-sprint-governance.mdc` at session-WRAPUP.

**Probe finding (2026-05-19, post-Slice-D revert):** TW-7c-4's scope is **broader than the orchestrator and Round-2 party-mode appreciated**. The tripwire has TWO assertions: line 56 enforces `app_scope == []` (no `app/`-layer Python touches, which is the freeze condition everyone in Round 2 reasoned about); line 65 ALSO enforces `unexpected == []` against the full `touched_python` set — **forbidding ANY working-tree Python file change outside the 5-path `PERMITTED_PYTHON_DIFFS` allowlist**, regardless of directory. Slice D (`git rm tests/migration/test_slab_2c_next_session_start_here_updated.py`) fired the second assertion despite being a pure `tests/` deletion. Implication: **only edits to (a) non-Python files (YAML / JSON / Markdown / TOML / etc.) or (b) the 5 allowlisted Python paths are truly freeze-safe during pre-Trial-3.** This materially narrows the in-session housekeeping surface; most catalogued failures in `broad-regression-baseline-2026-05-07.md` require Python edits and are structurally blocked until post-Trial-3 substrate amendment.

Slice D has been added to the `s6-housekeeping-coverage-manifest-path-pin-probe` evidence bundle as a paired finding: same root-cause class (post-S6 substrate-currency drift), same architectural disposition (revert + capture + post-Trial-3 reactivation), same `bmad-correct-course` pre-drafted motion at `.tmp/slice-a-post-trial-3-correct-course-draft.md` (motion §1 will be expanded at dispatch time to include the Slice D test-file deletion alongside the Slice A path-pin update).

## CRITICAL Trial-3-blocking regression — §02A composer unwired (filed 2026-05-21) ~~CLOSED 2026-05-21 via SCP-2026-05-21-trial3-wiring (commits 12453bc..ab5562d)~~

**Status:** **CLOSED 2026-05-21** via Sprint Change Proposal 2026-05-21-trial3-wiring (party-mode Round-1-ratified 4-of-4 APPROVE-with-amendments; commits C0..C2a at `371db9e..ab5562d`). Re-launch 2026-05-21T22:00 against the same Tejal corpus produced an LLM-composed directive (digest `351a57fb...`) confirming the §02A composer is now invoked at G0; directive shape matches the §02A canonical schema (real LLM-judged descriptions, content-aware `expected_min_words`, no broken-fallback "Auto-derived" boilerplate). Operator confirmed at G0 with verb `c`; trial then halted at the FIRST SPECIALIST DISPATCH (Texas wrangler exit 30) due to a DIFFERENT class of defect — schema-drift between the §02A composer's output shape and Texas wrangler's expected input shape. **The wiring fix worked; the integration boundary downstream is a separate issue.** New finding filed as `section-02a-downstream-consumer-compatibility-systemic-drift` below.

**Original entry preserved verbatim for audit trail (Trial-3 attempt-1 forensic record):**

| Entry | Filed | Trigger context | Resolution |
|---|---|---|---|
| **`trial-3-blocker-section-02a-composer-not-wired-into-trial-cli`** | 2026-05-21 | **Root cause:** `app/marcus/cli/trial.py:17-18` imports `compose_directive` from `app.marcus.orchestrator.directive_composer` (the Story 7a.1 naive corpus-scan fallback with `DEFAULT_EXPECTED_MIN_WORDS = 200`), NOT from the LLM-driven `app/composers/section_02a/composer.py` (Story 7c.3a deliverable that closed Trial-2 finding #2). The §02A composer EXISTS (5698 bytes; 5 test files; gate poll-surface helpers; field-level edit tests) but is ONLY consumed by its own tests + gate-side helpers — never by the trial CLI runtime path. **The deferred-inventory closure note `trial-2-finding-2-directive-composer-corpus-scan-fallback ... CLOSED-BY Story 7c.3a 2026-05-05` was overstated:** 7c.3a closed the composer-authoring side (module + tests + gate helpers), but the trial-CLI integration was never landed. This is exactly the audit-without-runtime-verification gap that Trial-2 finding #2 originally surfaced; the same class of defect re-occurred at the higher integration level. **Forensic evidence:** broken directive at `state/config/runs/bef9a2c6-.../directive.yaml` shows the naive-fallback signature: `role: primary` on alphabetically-first file (`assessments/chapter-2-knowledge-check.md`), `expected_min_words: 200` on every file regardless of content, `Auto-derived from corpus dir: <filename>` description pattern, all matching exactly the Trial-2 finding #2 broken-fallback pattern. **Implication:** Trial-3 cannot launch against any real corpus until the trial-CLI wiring is fixed. AM-11 launch-permission token (52/52 GREEN) does NOT cover this regression because it tests preflight + readiness fixtures, not the composer-actually-invoked-at-trial-start path. | **Fix scope:** ~3 LOC change in `app/marcus/cli/trial.py` (swap the import + adapt the call signature if it differs) + 1 adapter helper if needed + post-fix smoke test (re-launch Trial-3 against the same corpus + verify directive shape). Estimated effort: 1-2 hours including verification. **Substrate-amendment route required** because the edit touches `app/`-layer Python (TW-7c-4 will fire). Same `bmad-correct-course` Sprint Change Proposal pattern as the 2026-05-19 SCP precedent. The fix should EXTEND the existing SCP at `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md` (or author a sibling SCP) since it's the same substrate-amendment workflow. **Test surface:** after the wiring is fixed, the existing §02A composer test suite (5 files at `tests/composers/section_02a/`) should PASS unchanged + the Trial-2 forensic-regression test should PASS (it asserts non-byte-identicality with the broken-fixture directive that we just produced — `directive_digest 777d385b...`). Re-launch verification: re-run `trial start --preset production --input course-content/courses/tejal-apc-c1-m1-p2-trends/`; expect directive shape where slides are role=primary, quiz file is role=supporting, and expected_min_words is None or content-aware. **Reactivation:** immediate (TRIAL-3 cannot launch without this fix). NOT a post-Trial-3 candidate. **Priority over the 2026-05-19 SCP:** YES — this regression dominates the broad-regression cleanup work because Trial-3 dependence is binding. The pre-Trial-3 freeze can be safely amended for this single fix since the substrate is already structurally broken for Trial-3 (the freeze was protecting a substrate that turns out to be non-functional for its stated purpose). |
| **`trial-3-blocker-trial-cli-utf-8-launcher-wrapper-cmd`** | 2026-05-21 | **Secondary finding** surfaced during the same launch attempt: the operator launched the trial from `cmd.exe` (not PowerShell). The hot-start launch command prefix `$env:PYTHONIOENCODING="utf-8"` is PowerShell syntax; on CMD it returns "The filename, directory name, or volume label syntax is incorrect" and is silently no-op. Result: trial launched WITHOUT UTF-8 encoding set, re-introducing the Trial-2 finding #1 cp1252 crash vector for any Unicode-bearing corpus content. **Story 7c.2 was supposed to retire the operator-side workaround** by making the launcher UTF-8-safe by default; that closure clearly hasn't reached the CMD-invocation path. | **Fix scope:** make the trial CLI's main entry point (or the `_load_env_if_available` helper) set `os.environ["PYTHONIOENCODING"] = "utf-8"` and `sys.stdout.reconfigure(encoding="utf-8")` unconditionally before any I/O. ~5 LOC. Same substrate-amendment route. **Lower priority than the §02A wiring fix** but should be batched into the same Sprint Change Proposal if both are addressed together. Test surface: re-launch from CMD without any env-var prelude; expect clean UTF-8 output even on Unicode-bearing corpus content. |

**Convergent disposition recommendation:** the existing 2026-05-19 SCP at `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19.md` was a post-Trial-3 housekeeping motion. This Trial-3-blocking regression elevates it to a pre-Trial-3 binding amendment with extended scope. Either (a) extend the SCP §4 to include the §02A wiring fix + UTF-8 launcher fix alongside the original C1/C2/C3 phases, OR (b) author a NEW SCP scoped purely to Trial-3-blocking fixes and defer the SCP-2026-05-19 to post-Trial-3 as originally planned. Operator decides at next session start.

**Operator selected option (b) at 2026-05-21 session continuation:** narrow Trial-3-blocking-only SCP. New SCP file authored at `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-21-trial3-wiring.md` and party-mode-ratified (Round 1 4-of-4 APPROVE-with-amendments). Commits `12453bc..ab5562d` landed the substrate amendment + wiring fix + UTF-8 hardening + integration test. 2026-05-19 SCP remains queued for post-Trial-3 dispatch unchanged.

**Closure verdict 2026-05-21T22:00 re-launch (attempt-2):** §02A composer was successfully invoked at G0; LLM produced a real-judgment directive (digest `351a57fb...`; size 4192 bytes; preserved at `state/config/runs/6a3393f8-f369-4a30-b7c1-b50c60c1d1a2/directive.yaml`, gitignored). Operator confirmed at G0 with verb `c`. Trial then halted at first specialist dispatch (Texas wrangler exit 30) — DIFFERENT defect class, schema-drift between §02A output and Texas wrangler input. **The wiring fix worked. The integration boundary downstream is a separate Epic-scope issue filed below as `section-02a-downstream-consumer-compatibility-systemic-drift`.**

**Sibling entry `trial-3-blocker-trial-cli-utf-8-launcher-wrapper-cmd` also CLOSED 2026-05-21 via the same SCP (C2b commit `42ae4ec`):** trial CLI now invokes `_ensure_utf8_io()` at the top of `start_trial` to set `PYTHONIOENCODING=utf-8` + reconfigure stdout/stderr unconditionally before any I/O. CMD invocations work without the `$env:` prelude. Verified via 22:00 re-launch from cmd.exe.

---

## ~~CRITICAL Trial-3-blocking — §02A composer downstream-consumer schema-drift~~ (filed 2026-05-21T22:30; CLOSED 2026-05-22)

**Status:** **CLOSED 2026-05-22 via Epic 34** (`migration-34-1-section-02a-wrangler-integration-roundtrip-test` through `migration-34-7-translator-deletion-anti-pattern-entries-epic-close`; commit range `bc477ed..1b59487`). Phase B Quinn-synthesis Option 5 ratified the integration-ratchet-first sequence; Story 34-1 installed the Section 02A -> Texas wrangler subprocess ratchet (bc477ed); Stories 34-2 through 34-4 harmonized the wrangler/schema substrate (cabf850 + 08dfc4a + 16e36f7); Story 34-5 pinned translator shrinkage (e59b0f4); Story 34-6 deleted the legacy composer (55a4d25); Story 34-7 deleted the temporary translator scaffold + filed A23/P5 anti-pattern entries + this closure marker (1b59487). Trial-3 attempt-3 is unblocked on fully harmonized substrate with no temporary translator and no legacy directive composer.

~~Second occurrence in the same trial-launch arc of "tested module, untested integration" defect class. The first occurrence (§02A composer not wired into trial CLI) was patched via SCP-2026-05-21-trial3-wiring; the wiring fix REVEALED the second occurrence at the §02A composer → Texas wrangler integration boundary. **Operator-directed halt of Trial-3 attempt-2** at this finding (2026-05-21T22:30): rather than patch the symptom (one schema-drift gap) and risk discovering further gaps one-at-a-time, batch the full systemic fix.~~

**Observed at:** Trial-3 attempt-2 re-launch 2026-05-21T22:00 against `course-content/courses/tejal-apc-c1-m1-p2-trends/`. Run-id `6a3393f8-f369-4a30-b7c1-b50c60c1d1a2`. Operator confirmed `c` at G0. Texas dispatch raised `BundleDispatchError('texas wrangler reported hard error (exit 30); bundle not trusted')` from [app/specialists/texas/_act.py:323](../../app/specialists/texas/_act.py#L323).

**Root cause (verified by reproducing the wrangler subprocess directly):** the §02A composer's `Directive` schema diverges from the Texas wrangler's expected input schema along multiple field/enum boundaries. Three known incompatibilities surfaced so far; more may exist deeper in the wrangler validation, and additional drift is likely at other downstream surfaces (Texas bundle → Irene Pass-1, Irene packet → §04 G1, etc.) that this trial never reached.

| §02A composer emits | Texas wrangler expects | Wrangler enforcement | Severity |
|---|---|---|---|
| `src_id` | `ref_id` | [run_wrangler.py:319-348](../../skills/bmad-agent-texas/scripts/run_wrangler.py#L319-L348) | Hard fail at validation; this is what crashed attempt-2 |
| `role: supporting` | `role: supplementary` | [run_wrangler.py:328-338](../../skills/bmad-agent-texas/scripts/run_wrangler.py#L328-L338) (allowed set: primary / validation / supplementary / visual-primary / visual-supplementary) | Would hard-fail on the next gate even after `src_id` fix |
| `role: ignored` | not in wrangler's allowed set | Same line range | Would hard-fail if any ignored source appears (Tejal corpus has none; dodged for attempt-2) |

**Forensic evidence preserved (gitignored; local-only):**
- `state/config/runs/6a3393f8-f369-4a30-b7c1-b50c60c1d1a2/directive.yaml` — §02A-composed directive (sha256 `351a57fbe12aff4a49349c4a646618d92ae38a798ec53eee61668f74f8bbd703`; size 4192 bytes; 11 sources). Reference baseline for next-session diff comparison.
- `state/config/runs/6a3393f8-f369-4a30-b7c1-b50c60c1d1a2/run.json` — trial envelope at point of crash; `status: in-flight`, `completed_at: null`, `production_clone_launch_evidence_reason: registered-no-specialist-fired`.
- Reproduced wrangler error message: `[run_wrangler] directive error: sources[0] missing required field: ref_id`

**Why this is Epic-scope, not a one-line patch:** Story 7c.3a authored the §02A composer as a fresh schema without verifying it conforms to **any** downstream consumer. The §02A composer's own test suite (5 files, 12 tests) verifies the composer-in-isolation but never feeds output into the wrangler. AM-11 launch-permission token (52/52 GREEN) tests preflight + readiness fixtures, not end-to-end specialist dispatch. The Murat M-A1 wiring-contract test added in SCP-2026-05-21-trial3-wiring tests the adapter-to-composer contract but mocks `compose()` itself — it cannot detect output-shape drift. **Every §02A→downstream boundary is unprobed.**

**Resolution scope (Epic-level):**

**Phase A — comprehensive probe (~30-45 min next session):** Before any fix lands, audit ALL §02A→downstream integration surfaces. Targets:
- Texas wrangler input schema (this surface; surface more drift in the rest of `run_wrangler.py` beyond `ref_id` + `role`)
- Texas bundle output → Irene Pass-1 input (does Irene read fields that the new wrangler bundle produces in a different shape?)
- Irene packet → §04 G1 decision card input
- §04A per-plan-unit row schema
- §04.55 run-constants lock structure
- §07C storyboard build input/output
- §11 voice-selection input
- §15 final bundle output shape

Each surface gets a `grep` + `Read` audit; the result is a full integration-drift inventory (zero finding → schema is clean; N findings → batch into Phase B fix).

**Phase B — batched fix (1-2 sessions, scope-dependent):**
- **Decision point ratified at party-mode:** harmonize §02A→canonical or harmonize-downstream-to-§02A? The §02A schema is the NEWER authored deliverable (Story 7c.3a); the wrangler + downstream consumers reflect the pre-§02A canonical. Honoring "newer schema is canonical" → update wrangler + consumers. Honoring "minimize-blast-radius" → revert §02A to match the existing canonical.
- Either direction: requires substrate amendment (TW-7c-4 allowlist extension + lockstep test updates) via formal SCP route. Likely larger Epic than the wiring SCP (multi-file, multi-specialist).
- Consider authoring as an **integration-coherence Epic** via `bmad-create-epics-and-stories` with stories per affected surface.

**Phase C — closure (Trial-3 attempt-3 re-launch):** Once Phase B lands, re-launch from `course-content/courses/tejal-apc-c1-m1-p2-trends/`. Same corpus; same trial protocol. The Phase A audit + Phase B fix should de-risk additional integration-drift surprises within the §02A→downstream chain.

**Reactivation trigger:** immediate (next session is the BMAD sprint specifically dedicated to this Epic, per operator directive 2026-05-21T22:30: *"Start next session with a BMAD sprint to correct and address as many related issues as can be identified now and corrected proactively."*).

**Cross-reference:**
- Sibling-class finding: `marcus-interactive-experience-not-delivered-by-slab-7c` (Epic-scope; post-Trial-3 successor). That Epic is downstream of Trial-3 closure; THIS Epic is the gate before Trial-3 can re-launch successfully.
- Parent SCP (closes the wiring blocker that surfaced this gap): `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-21-trial3-wiring.md`.
- Pattern: this is the SECOND "tested module, untested integration" gap in the same trial-launch arc — worth flagging as a Mary-tier anti-pattern candidate for cross-trial-learnings.

---

## J-A1 follow-ons from SCP-2026-05-21-trial3-wiring (filed 2026-05-21T22:30)

Two seams the cli_adapter.py docstring + SCP §7 named explicitly; filed here per John J-A1 binding before re-launch (now retroactively filed since attempt-2 occurred without these on disk).

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| ~~**`trial-cli-effective-trial-id-vs-section-02a-composer-run-id-divergence`**~~ | 2026-05-21 | ~~The §02A composer's `Directive.run_id` is internally generated as `uuid4()` (per [directive_model.py:114](../../app/composers/section_02a/directive_model.py#L114)) independent of the trial CLI's `effective_trial_id`. The trial CLI passes a UUID to its run-dir path but does NOT thread it into the directive payload. **Observed in attempt-2:** stdout printed `run_id: c9f8c16a-1fd9-41fc-bc31-98167a8a8080` (composer's internal UUID); directive landed at `state\config\runs\6a3393f8-f369-4a30-b7c1-b50c60c1d1a2\directive.yaml` (trial CLI's `effective_trial_id`). Two different UUIDs. Downstream consumers reading the directive see a `run_id` that does not match the run-dir path.~~ **CLOSED via Story 34-3 (Codex ready-for-review handoff 2026-05-22):** `compose()` now requires caller-supplied `run_id: UUID`; `cli_adapter.compose_and_write(..., run_id=...)` threads it through; `app/marcus/cli/trial.py` passes `effective_trial_id`. CLOSED at commit `08dfc4a` (Claude T11 close 2026-05-22). | ~~**Resolution shape:** decide which UUID is canonical (likely trial CLI's `effective_trial_id`); thread it through `cli_adapter.compose_and_write(...)` to `compose(...)` as an explicit `run_id` parameter; update §02A composer to accept the caller-supplied UUID instead of generating its own. Likely 5-10 LOC plus test updates. **Reactivation:** fold into the §02A-downstream-consumer-compatibility Epic (Phase B) so it lands lockstep with the field-name + role-enum harmonization.~~ |
| ~~**`trial-cli-model-resolution-trail-not-appended-from-adapter`**~~ | 2026-05-21 | ~~`cli_adapter.compose_and_write` calls `make_chat_model("marcus")` which returns a `ChatModelHandle(chat, entry)` NamedTuple. The `.entry` field is the NFR-X4 `ModelResolutionEntry` audit record meant to be appended to `RunState.model_resolution_trail`. The adapter currently discards `.entry` because the trial CLI does not currently exercise `RunState.model_resolution_trail` append. **Result:** §02A composer LLM calls are NOT tracked in the run's reproducibility trail; NFR-X4 contract is silently bypassed at G0.~~ **CLOSED via Story 34-3 (Codex ready-for-review handoff 2026-05-22):** `cli_adapter.compose_and_write` persists `ChatModelHandle.entry` to `run_dir/model_resolution_trail.json` when the adapter resolves the default Marcus chat model. CLOSED at commit `08dfc4a` (Claude T11 close 2026-05-22). | ~~**Resolution shape:** thread the trail through trial.py state OR have the §02A composer write the entry to a sidecar JSON in `run_dir`. Decide at Epic-design time. **Reactivation:** fold into the §02A-downstream-consumer-compatibility Epic (Phase B).~~ |

---

## Documentation-currency drift (filed 2026-05-21T22:30; surfaced by operator-requested resource audit pre-attempt-2)

Five entries surfaced when the operator asked the orchestrator to verify resource currency + harmonization before Trial-3 re-launch. None blocked launch (the launch command I gave the operator correctly avoided the stale flag) but all represent doc drift that misleads readers.

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`g0-directive-composition-doc-stale-post-c2a`** | 2026-05-21 | [docs/conversational-gates/g0-directive-composition.md](../../docs/conversational-gates/g0-directive-composition.md) references the LEGACY composer behavior throughout: example directive at lines 13-22 shows the broken-fallback signature (`Auto-derived from corpus dir: X`, uniform `expected_min_words: 200`); mermaid diagram at lines 50-74 references `compose_directive()` + `materialize_directive` (the functions removed in C2a); paths at lines 10, 55, 107 cite `state/runs/<trial_id>/directive.yaml` but actual path is `state/config/runs/<trial-uuid>/directive.yaml`. | Rewrite the doc to reflect §02A composer behavior, current call-chain (`cli_adapter.compose_and_write` → `compose` → `write_directive_yaml`), and correct run-dir path. Land alongside the §02A-downstream-consumer-compatibility Epic OR as a separate cleanup commit. ~30 min docs work. |
| **`hil-verb-legend-s5-stub-paths-never-landed`** | 2026-05-21 | [docs/operator/hil-verb-legend.md](../../docs/operator/hil-verb-legend.md) cites 14+ `section-XX-operator-reference.md` files marked "(S5)" or "(S5 stub)" that never landed on disk — `section-02`, `section-03`, `section-04a`, `section-05`, `section-05b`, `section-10`, `section-11`, `section-13`, `section-14.5`, `section-15` operator-reference docs. Of the section-XX-operator-reference docs cited, only ~half exist on disk. Verb-set table information is still authoritative; file-pointer column is aspirational. | EITHER author the missing operator-reference docs OR remove the file-pointer references from the verb legend (recommend the latter; verb-sets are the load-bearing content). Land as part of pre-Trial-3 doc cleanup. |
| **`hil-verb-legend-section-path-dot-vs-dash-syntax-drift`** | 2026-05-21 | hil-verb-legend.md cites `section-04.5-operator-reference.md`, `section-04.55-operator-reference.md`, `section-05.5-operator-reference.md` (dotted) but actual filenames use dashes (`section-04-5-...`). Three citations affected (lines 37, 38, 40). | Trivial 3-line edit — change dots to dashes in the cited paths. ~5 min. |
| **`v5-pack-motion-enabled-flag-stale-across-launch-commands`** | 2026-05-21 (RE-CONFIRMED) | [docs/workflow/production-prompt-pack-v5-narrated-lesson-with-video-or-animation.md](../../docs/workflow/production-prompt-pack-v5-narrated-lesson-with-video-or-animation.md) lines 34, 42, 125, 150, 171 reference `--motion-enabled` as an active CLI flag; trial CLI rejects with `error: unrecognized arguments: --motion-enabled`. Operator-requested audit re-confirmed the v5 pack is canonical-but-stale at the launch command. **Pre-existing finding** also tracked in next-session-start-here ("~27 doc files still carry the stale flag; cleanup filed as a post-Trial-3 follow-on"). | Pattern-search-and-replace across all docs that reference `--motion-enabled` as an active CLI flag; remove flag from launch commands; preserve mention in historical docs (e.g., v4.2 frozen pack) with explicit deprecation note. ~1 hour cleanup. |
| **`g0-verb-set-mismatch-legend-vs-cli`** | 2026-05-21 | hil-verb-legend.md table line 33 (§02A G0 row) lists verbs as `approve / edit / reject`, but the CLI hardcoded prompt at [trial.py:92-103](../../app/marcus/cli/trial.py#L92-L103) emits `[c/e/s/x]` — confirm/edit/save/cancel. The legend has no equivalent of CLI's `s` (save-only). Semantic mapping is clean (approve↔c, edit↔e, reject↔x) but the verb-text differs and the legend is missing one verb. | Update the verb legend to reflect the CLI's actual verb-set OR update the CLI to use the legend's verbs (CLI-side change is heavier). Decide at doc cleanup time. |

**Cross-reference:** these doc drift entries are sibling-class to the integration-drift Epic (same root cause: substrate evolved post-Slab-7c without lockstep updates to operator-facing surfaces) but with smaller blast radius. Likely candidate for a single docs-cleanup batch after the integration-coherence Epic lands.

---

## Ruff SIM105 nit in C2b `_ensure_utf8_io` (filed 2026-05-21)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`trial-cli-sim105-utf8-io-try-except-pass`** | 2026-05-21 | Ruff flagged SIM105 on [trial.py:57-60](../../app/marcus/cli/trial.py#L57-L60): `try`-`except`-`pass` should be `with contextlib.suppress(ValueError, OSError):`. Recorded as deferred at SCP-2026-05-21-trial3-wiring session-WRAPUP Step 1 quality gate per the discipline "fix findings or record explicitly as deferred." | Trivial 4-line edit + `import contextlib` if not present. Land alongside the next substrate-amendment touching `trial.py` so we don't re-allowlist a new fragment edit. ~5 min. |

---

## POST-TRIAL-3 ARCHITECTURE — Marcus interactive experience not delivered by Slab 7c (filed 2026-05-21)

**Status:** **POST-TRIAL-3 ARCHITECTURE FOLLOW-ON.** Surfaced when the operator questioned the Trial-3 launch surface and named the discrepancy between the "Marcus conversationally mediates production runs" Slab-7c promise and the actual runtime path used at Trial-3 launch 2026-05-21. NOT Trial-3-blocking — Trial-3 ships against the CLI surface as-is. Reactivation trigger: **post-Trial-3 retrospective.** Likely Epic-scope, not single-story; needs `bmad-create-prd` → architecture → UX → stories chain.

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`marcus-interactive-experience-not-delivered-by-slab-7c`** | 2026-05-21 | **Architectural finding the operator surfaced post-Trial-3-cancellation review:** at trial launch the operator invokes `python -m app.marcus.cli trial start ...` directly; the operator-facing surface at G0 is a raw four-letter CLI prompt (`[c/e/s/x]`). Marcus-the-conversational-persona ([skills/bmad-agent-marcus/SKILL.md](skills/bmad-agent-marcus/SKILL.md)) is NOT in this runtime path. The SKILL.md promise — *"the operator should never need to think about agents, skills, APIs, or state; you are the interface; you carry the complexity"* — does not hold at the trial-launch surface. **Three distinct things named "Marcus" coexist and the naming collision masked the gap:** (1) Marcus-the-persona (BMAD skill at `skills/bmad-agent-marcus/`; sanctum at `_bmad/memory/bmad-agent-marcus/`; lives in Claude Code conversation space); (2) `app/marcus/` (Python package; LangGraph orchestrator runtime; trial CLI; gate runner; production runner); (3) `pre_gate_marcus` (LLM node inside `app/marcus/orchestrator/` that pre-fills gate proposals — a third meaning of the name). The Slab-7c work delivered (2) and (3) but did not bridge to (1). At the most operator-critical moments (G0, G1.5 IRREVERSIBLE lock, G3 IRREVERSIBLE four-artifact lock), the operator interacts with raw CLI verbs, not with Marcus-narrated context. **Implication:** the operator-mediation promise on which days of conversational-Marcus work were predicated has not been integration-delivered; the trial-launch surface is the CLI, with Marcus as a parallel doc/planning layer. | **Resolution scope:** Epic-level, not story-level. Likely shape: (a) `bmad-create-prd` for "Marcus-as-trial-launch-surface" Epic — operator says "Marcus, start a Trial-N run on corpus X" → Marcus reads the directive aloud → conversational G0 → Marcus invokes `app.marcus.cli` under the hood and narrates each gate transition with sanctum-loaded context; (b) Winston architecture pass on the bridge between Claude-Code-conversation-space (Marcus-skill) and Python-subprocess-space (`app/marcus/cli`) — IPC shape, state-sync, error-propagation; (c) Sally UX pass on the conversational gate surfaces (what does conversational G0 look like vs `[c/e/s/x]`? what does conversational G1.5 lock confirmation look like?); (d) story decomposition; (e) Trial-4 launches against the Marcus-mediated surface as success criterion. **Naming-collision sub-finding:** resolve the three-Marcuses ambiguity at architecture time. Candidate names: Marcus-Persona / Marcus-Runtime / Marcus-PreGate, or rename `app/marcus/` to something orthogonal. Architecture-level decision; not arbitrary. **Reactivation trigger:** post-Trial-3 retrospective consultation of this register per [CLAUDE.md §Deferred inventory governance](../../CLAUDE.md) binding-consultation-point #1. **Operator priority signal (2026-05-21):** "get to building out the promised but not delivered Marcus 'interactive' experience" — sequenced strictly AFTER CLI-only Trial-3 run completes. |

**Cross-reference:** complements the existing `slab-7c-marcus-cli-bypass` framing IF such an entry exists (none found at filing time — this is the first formal record of the gap). The Trial-3-blocking §02A composer wiring finding above is a RELATED but ORTHOGONAL gap: §02A wiring is the *specific* substrate-coherence defect that surfaced at G0; the Marcus-interactive gap is the *architectural* layer-mismatch finding that the §02A symptom revealed. Both will be cited as bidirectional reactivation triggers in the Trial-3 postmortem Shape A Q5 + Shape B harvest.

---

## POST-TRIAL-3 ARCHITECTURE — Tracy gap-fill lane not adopted by production-runner trial path (filed 2026-06-11)

**Status:** **POST-TRIAL-3 ARCHITECTURE FOLLOW-ON.** Surfaced during the 2026-06-11 attempt-4 wait window when the operator asked the consultant session to map how supplementary-research/gap-fill requests route to Tracy ("detective") and Texas ("wrangler"). Substrate study found the Epic-28 lane fully implemented and module-tested but **integration-orphaned on the production trial path**. NOT attempt-4-blocking — the trial proceeds on corpus material alone, which is consistent with the weed-clearing trial posture. Reactivation trigger: **post-Trial-3 retrospective** (same consultation point as `marcus-interactive-experience-not-delivered-by-slab-7c`).

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`tracy-gap-fill-lane-not-adopted-by-production-runner`** | 2026-06-11 | **Wiring-gap finding from read-only substrate study during the attempt-4 fix window.** The Epic-28 detective lane (Irene `IdentifiedGap`s with embellish/corroborate/gap_fill postures → plan-lock auto-dispatch via `IreneTracyBridge` → Tracy `suggested-resources.yaml` → operator approval at Marcus landing-point → Texas second-pass fetch → hard pre-Pass-2 gate on `tracy-complete.yaml`) is implemented and tested at module level (`skills/bmad_agent_tracy/scripts/irene_bridge.py`, `app/marcus/orchestrator/fanout.py::emit_plan_lock_fanout`, contracts at `tests/contracts/test_irene_tracy_bridge.py` + `tests/test_marcus_plan_lock_fanout.py`). However: (1) the ONLY production caller of `emit_plan_lock_fanout` is `app/marcus/facade.py` (4A lesson-plan lane), with `tracy_bridge` an **optional kwarg the trial CLI never passes**; (2) `app/marcus/orchestrator/production_runner.py` contains ZERO references to fanout/bridge/Tracy; (3) the v4.2 manifest has no Tracy node, while Tracy IS in `CANONICAL_SPECIALIST_IDS` (roster of 12). Net: no tracked trial on the current substrate can exercise gap-fill research dispatch; Irene Pass 2 (§08) proceeds on corpus material alone and the Epic-28 hard pre-Pass-2 gate (AC-S3) is vacuously satisfied because no Tracy dispatch ever opens. **Third instance of the A23/P5 "tested module, untested integration" pattern in the trial-launch arc** (after §02A→wrangler vocab fork and the resume-walker gate-pause gap). | **Resolution scope:** likely story-to-small-Epic, decided at retrospective. Candidate shape: (a) decide WHERE the lane attaches on the trial path — plan-lock at §04.55 (G1.5-lock) is the designed trigger point per Epic 28; production_runner would need to invoke the fanout+bridge there (or the manifest gains a Tracy node between §04.55 and §08); (b) decide the operator-approval surface — the Marcus landing-point implies a conversational HIL stop, which couples this to the `marcus-interactive-experience-not-delivered-by-slab-7c` Epic (the Irene→Marcus→Tracy→operator→Texas loop is inherently conversational); sequencing the two together at architecture time is the natural move; (c) wire the AC-S3 pre-Pass-2 receipt check into the §08 segment so the gate is real, not vacuous; (d) integration ratchet test FIRST per P5 (any Epic touching this producer-consumer contract starts with the green boundary test). **Direction may flip if substrate evolves** — if the Marcus-interactive Epic re-architects gate transport, the attach-point decision (runner-wired vs manifest-node vs conversational-lane) should be re-validated at dispatch, not executed blindly from this filing. |

**Cross-reference:** sibling-to and likely co-sequenced with `marcus-interactive-experience-not-delivered-by-slab-7c` (above) — both reactivate at the post-Trial-3 retrospective; the Tracy lane's operator-approval step is a concrete consumer of whatever conversational surface that Epic delivers. Pattern-tier: cite as third A23/P5 instance in the Trial-3 postmortem Shape B harvest + cross-trial-learnings entry. Epic-28 authority: `_bmad-output/implementation-artifacts/epic-28-tracy-detective.md` (AC spine, dispatch-vs-artifact ratification, runbook at `epic-28/_shared/runbook.md`).

---

## POST-TRIAL-3 CONTENT-QUALITY — reading-path narration enforcement machinery not absorbed at severance (filed 2026-06-11)

**Status:** **POST-TRIAL-3 CONTENT-QUALITY FOLLOW-ON.** Surfaced 2026-06-11 when the operator asked for a substrate study of the slide-clustering + reading-path-pattern capabilities ahead of a planned enhancement (expand the Z-pattern success to triptych / image-dominant-first / other scan patterns). Study found the capability **half-migrated**: prompt guidance survived; classification + contract + lint enforcement did not. Explains the standing user complaint that narration does not follow the natural scan of slide content. NOT attempt-4-blocking. Reactivation trigger: **post-Trial-3 retrospective**, with the operator's per-slide scan-order nit harvest from the attempt-4 G2C/G3 storyboard reviews as the evidence corpus.

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`reading-path-enforcement-machinery-not-absorbed-at-severance`** 🔴 ESCALATED | 2026-06-11 (🔴 operator-escalated DISASTER-LEVEL 2026-06-19 PM) | **🔴 2026-06-19 PM: MERGE with `beta-narration-hallucinates-nonexistent-illustrations` into ONE #1-priority "perception + reading-path narrative-grounding restoration & ENHANCEMENT" arc.** Operator declared the combined loss (perception + reading-path/Z-pattern visual-structure that directed narrative focus) a disaster-level regression; mandate is REPAIR *and* ENHANCE next session. The reading-path classification is the structure half; the perception layer is the grounding half — both lost at severance, both needed for narration that speaks to what's on the slide in the order the eye reads it. — **Capability-degradation finding from read-only substrate study.** The primary repo's reading-path system was a 5-part lockstep: enum registry (`state/config/reading-path-patterns.yaml`), `reading_path` sub-object in `segment-manifest.schema.json`, pattern-aware emission lint (`scripts/validators/pass_2_emission_lint.py`; `sequence_numbered` FAIL-CLOSED, others warning), heuristic pattern classifier (`scripts/utilities/reading_path_classifier.py` `_SCORERS`), and the worked-examples doc — kept aligned by `tests/contracts/test_reading_path_parity.py`. Per `upstream-severance-log.md` (2026-04-24), ONLY the worked-examples doc was absorbed (`skills/bmad-agent-content-creator/references/pass-2-grammar-riders-examples.md`, loaded as in-context expertise by `app/specialists/irene/graph.py:67-68`); registry, schema enum, lint, classifier, and parity test were explicitly NOT absorbed. The hybrid Pass-2 contract (`app/specialists/irene/authoring/pass_2_template.py::SegmentManifestSegment`) has NO `reading_path` field — scan-order narration is enforced only by LLM adherence to in-context exemplars ("classify → declare → lint" degraded to "suggest"). Perception substrate survived (`PerceptionArtifact.visual_elements`, `VisualReference.location_on_slide`, `traceable_visual_references` rule) and is the better v2 foundation. v1 repertoire (7 patterns): z_pattern, f_pattern, center_out, top_down, multi_column, grid_quadrant, sequence_numbered — `multi_column` and `center_out` already approximate the operator's triptych / hero-image-first ambitions. | **Resolution scope:** compact "reading-path v2" Epic or story family; operator priority signal 2026-06-11 (planned enhancement; addresses standing user complaint). Phased shape: **(1) restore the contract natively** (do NOT re-absorb upstream code — severance re-open is a party-mode governance exception; rewrite under the schema-story scaffold): `reading_path` sub-object on `SegmentManifestSegment` (closed pattern enum + declared scan order + classifier confidence) + regenerated schema + pattern-conformance check in Irene's 9-node **verify** node (replaces the old standalone lint); **(2) perception-grounded extraction**: classify layout pattern from Gary's rendered PNGs between §07 and §08 (vision call w/ bounding-box evidence into `PerceptionArtifact`), so Pass 2 consumes a declared pattern rather than guessing; **(3) evidence-first repertoire growth**: add `triptych` (vs multi_column) / `image_dominant_first` (vs center_out) ONLY where Storyboard-B review evidence shows the 7 mis-fit real slides; each addition costs the full parity set (registry/schema/lint-check/classifier-scorer/worked example); new patterns enter at warning severity. Governance: schema-shape story discipline (scaffold); any v4.2 pack/manifest touch goes through the pipeline-lockstep regime (Tier-2 bump = party-mode consensus BEFORE dev). Orthogonal to `marcus-interactive-experience-not-delivered-by-slab-7c` — can interleave. **Direction may flip if substrate evolves** — if a future pack version restructures Pass-2 authoring or the perception layer, re-validate the attach points (verify-node check; §07→§08 classification step) at dispatch rather than executing this shape blindly. |

**Cross-reference:** clustering (the operator's sibling capability in the same enhancement intent) needs NO inventory entry — fully migrated and live (cluster_id/role/position arc in `pass_2_template.py`; §05B/§6.2/§6.3/§7.5 pipeline sub-arc; `cluster_arc_continuity` + `bridge_cadence` validator-enforced). v2 work should couple per-cluster narration cadence to reading-path patterns where natural. Evidence-collection instruction for attempt-4: operator harvests per-slide scan-order nits at G2C (Storyboard A) + G3 (Storyboard B) reviews → postmortem Shape B → this entry's justification + test corpus.

---

## P2-2 T11 hand-back findings (filed 2026-06-20; party-mode 5/5, no impasse)

**Status:** ✅ **RESOLVED at re-T11 (2026-06-20 PM) — P2-2 CLOSED `done`.** The 4 MUST-FIX (F1 vacuous comparator calibration, F2 07G verbatim-extraction breach, M3 FIDELITY_GATE=warn over-broad catch, MF1 figure-regex false-positives) were all independently verified resolved; Option A for F2 fully implemented (allowlist + Check-9 meta-test + presence assertion). Full record: `p2-2-t11-code-review-2026-06-20.md` §6. The follow-ons below are the **standing process/governance guards** the party ratified that OUTLIVE the cycle (anti-pattern Category F harvested to `dev-agent-anti-patterns.md`; baseline-diff attestation + net-new-gen green-light checklist remain binding for future cycles).

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`verbatim-extraction-vs-net-new-gen-section-collision`** | 2026-06-20 | Party D2 (5/5 Option A). `test_33_1a_verbatim_extraction` requires every `-gen` section be verbatim-extractable from the FROZEN v4.2 source pack, but a net-new prose section (07G, the vision step) structurally cannot satisfy it — and the Tier-3 green-light never anticipated the collision. Caught only by a clean-HEAD baseline diff (Codex's handoff mislabeled the RED as "pre-existing drift"). | **07G handled now in the Codex cycle via Option A:** closed allowlist (section + why-verbatim-can't-hold + substitute invariant) + a meta-test asserting every excluded section is covered by L1 Check-9 regeneration-determinism (the structural lock) + a 07G presence/shape assertion + a formal rule comment (verbatim for frozen-origin sections; net-new `-gen` governed by Check-9). **Standing rule:** adding to the verbatim-exclusion allowlist is party-mode governance, not dev authority; expanding the verbatim-REQUIRED set is dev authority (safe direction). **Direction may flip if substrate evolves** — if a future pack version moves where `-gen` sections live, re-validate the allowlist/Check-9 coupling at dispatch. |
| **`codex-handoff-baseline-diff-attestation-required`** (anti-pattern: `mislabeled-regression-as-preexisting-drift`) | 2026-06-20 | Mary D1 process guard. Codex's P2-2 handoff asserted a P2-2-introduced regression (F2) was "unrelated pre-existing drift" — a false statement in the audit record that only surfaced because Claude ran a clean-HEAD baseline diff. The baseline diff cannot be the load-bearing safeguard (it depends on the reviewer choosing to run it). | **T11-gate checklist item (not advisory):** any test a Codex handoff labels "pre-existing / unrelated drift" MUST carry inline clean-HEAD RED evidence (`git stash`/checkout `<clean-HEAD>` + run the node). No attestation → label rejected, failure treated as in-scope by default (**burden of proof flips to dev**). Harvest the `mislabeled-regression-as-preexisting-drift` entry to `docs/dev-guide/dev-agent-anti-patterns.md` (or `specialist-anti-patterns.md`) + `cross-trial-learnings.md` at P2-2 re-close. |
| **`green-light-checklist-net-new-gen-section`** | 2026-06-20 | Mary D2 durable fix. The green-light gate missed F2 because "does this introduce a net-new `-gen` prose section?" was never a gate question. | Add to the Tier-3 / pack-touching green-light checklist: *"Does this story introduce any net-new `-gen` prose section? If yes, the verbatim-extraction test must register it as an exception (with Check-9 substitute coverage) BEFORE dev opens."* Converts a missed-anticipation into a standing gate question. |
| **`vision-comparator-anti-vacuity-pass-bar`** | 2026-06-20 | Murat D1 pass-bar (binding on P2-2 re-T11). | Genuine calibration = held-out labeled set (no `x==x` self-compare) + one negative control per threshold (bbox_iou / element_jaccard / text_edit_distance each tripping only its own knob) + a per-threshold mutation table in Completion Notes (degenerate knob → its test flips). Encoded in the remediation brief; recorded here so the anti-vacuity bar is reusable for any future tolerance-bearing comparator. |

---

## P2-3 green-light follow-ons (filed 2026-06-20; Tier-3 party-mode 5/5 GREEN-WITH-AMENDMENTS)

**Status:** P2-3 (Pass-2 consumes perceived visuals) spec `ready-for-dev`; Codex driver `codex-dev-prompt-p2-3-pass2-consumes-perceived-visuals.md`. These follow-ons were ratified at the green-light and are explicitly HELD OUT of P2-3 scope.

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`perception-artifact-two-model-fork`** (A23-class) | 2026-06-20 | Two `PerceptionArtifact` models coexist: rich `app/models/perception/perception_artifact.py` (vision-produced + detector-consumed) vs minimal `app/specialists/irene/authoring/pass_2_template.py::PerceptionArtifact` (authoring-time, anticipates perception but was unwired pre-P2-3). P2-3 wires the RICH model as Pass-2's grounding authority and makes the minimal model a subset projection (or leaves it untouched) — but does NOT unify them. Latent decoy: a future author "helpfully" populating the minimal model could silently re-arm brief-as-authority. | **Unify the two models under one story** (rich model canonical). **CLEANUP DIRECTION:** collapse the minimal authoring-envelope `PerceptionArtifact` into a view/projection of the rich model; delete the parallel definition. **Direction may flip if substrate evolves** — which model is canonical may invert once vision output stabilizes (cf. `app-marcus-stub-disposition` precedent); reactivation review re-validates direction. Guarded in P2-3 by a field-compatibility contract test + in-code decoy note. Reactivation trigger tied to P2-4 or first post-P2-3 trial. Bidirectionally referenced by the P2-3 grounding-leg strike. |
| **`perception-source-element-reference-promotion`** | 2026-06-20 | P2-3 keeps `visual_reference.perception_source = slide_id` (D3); the P2-1 detector enforces element-subset at slide granularity, which closes the regression. Promoting `perception_source` to a perceived-element reference is real value but expands the dp-schema + test surface beyond the regression fix. | **P2-4 successor** (couples naturally with reading-path element-level grounding). Promote `perception_source` to reference a specific perceived element; tighten `_assert_narration_joins_roster` to element granularity. |
| **`pass2-uncovered-slide-conservative-corpus-narration`** | 2026-06-20 | P2-3 D2 ships the floor: uncovered/low-confidence slides get an explicit detector-visible "UNVERIFIED" token (no silent brief-fallback). Synthesizing conservative narration from the corpus text for uncovered slides is net-new generation logic (John: P2-4-territory creep). | Upgrade the "UNVERIFIED" abstention to corpus-grounded conservative narration (still never brief-grounded). Post-P2-3 / P2-4. |

---

## ~~🔴 MUST-FIX-BEFORE-S5~~ **LANDED 2026-06-12** — v4.2 pack renderer broken since Slab-7a orchestration nodes; L1 regeneration guard vacuous (filed 2026-06-12; priority upgraded by party review 2026-06-12: Winston S1-A + Murat + John)

**LANDED 2026-06-12 (Claude-direct per cleanup-arc mode; Tier-1 — classification fix + guard hardening, no pack structural change):** shared `is_orchestration_only` predicate at `app/manifest/schema.py` consumed by BOTH the v4.2 generator and L1 (they can no longer drift apart); renderer skips the four runtime-only orchestration nodes (classification fix per the filing's lean, NOT placeholder templates); missing templates now raise `MissingSectionTemplateError` early instead of fabricating a name; the ratified S1 pack hand-edits (header/banner/§0.5 launchpad, PP-1 enrique, §3.g deletion, xref) folded back into templates per A12 counter-pattern; pack regenerated (2-line legitimate delta: S4-updated 07/07B rationale rows flowing through); **L1 Check 9 regeneration-determinism landed — render crash → exit 2 STRUCTURAL, SHA mismatch → exit 1, proven by Pin B red-tests** (`tests/generators/v42/test_renderer_classification_and_l1_fail_loud.py`: crash→2, hand-edit→1, green twin→0) and Pin A roster-derived all-node render loop. Six ambient failures healed (33_1a ×2, 33_3 ×2, full_pipeline_smoke ×2). **Per Murat's standing condition: Pin B is merged → L1-green is RESTORED to S5's evidence basis.**

**Priority determination (party review 2026-06-12, preserved):** a guard that cannot fail launders confidence (Winston); fix before any future story cites L1 as a verification gate. INSEPARABLE PAIR: L1 cannot be made render-aware while the renderer crashes (every closure hook would brick), so the renderer skip-or-template fix and the L1 fail-loud land as ONE story. Until it lands, **L1-green is EXCLUDED from S5's evidence basis** (Murat's alternate branch, recorded in the SCP).

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`v42-renderer-crashes-on-orchestration-nodes-l1-guard-vacuous`** | 2026-06-12 | Surfaced during S1 of SCP-2026-06-11 segment-data-plane when executing the regime's render step: `python -m scripts.generators.v42.render` crashes with `TemplateNotFound: sections/directive-composer-g0-...` — the four Slab-7a orchestration nodes (directive-composer, pre-gate-marcus, per-slide-subgraph, html-review-pack-emitter) were registered in the manifest with no section templates, and `pack.md.j2` includes a template per node unconditionally. Verified pre-existing via stash probe against the pre-edit manifest. Compounding: `check_pipeline_manifest_lockstep.py` still exits 0 — the Scenario-E regeneration-determinism guard is vacuous when rendering crashes, so hand-edits to the pack would currently NOT be caught by re-render SHA comparison. Same guard-exists-but-never-exercised class as the A23/P5 family. **AMENDED 2026-06-12 (drift-audit party round, 4-voice unanimous):** external drift audit independently re-found the 4 missing derived templates — FOLDED into this story, not filed separately. The ambient failure `tests/test_33_3_dc2_resolution.py::test_pack_section_sequence_matches_manifest_order` (pack ids missing the 4 orchestration sections) is this story's domain and becomes its healed-or-explained AC. | ONE story (inseparable pair stands): (a) renderer skips nodes without section templates (orchestration nodes are runtime-only, not pack prose) OR authors minimal section templates for the four — **decide classification-vs-generation explicitly in the spec** (Amelia trap b: placeholder templates may paper over a classification bug); declare Tier-1 vs Tier-2 pack governance up front (Amelia trap c). (b) L1 check made to FAIL (not pass) when regeneration itself crashes. (c) re-render and verify committed pack SHA. **Pins (Murat, binding):** Pin A — completeness test DERIVES the expected template set from the manifest/pack roster and renders ALL nodes in a loop (not a hardcoded list of the 4 known names; A23-with-extra-steps otherwise); Pin B — red-test removes a template in a tmp copy, runs L1, asserts NON-ZERO exit (the vacuous-guard kill shot; L1-green stays EXCLUDED from S5 evidence until Pin B is merged). Reactivate at the segment-data-plane arc close or next docs window, whichever first. |

---

## Party-review riders — segment-data-plane arc (filed 2026-06-12; SCP-2026-06-11 S0-S3 batch review, 4-voice APPROVE-with-amendments)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`manifest-edge-key-projection-s4`** | 2026-06-12 | Winston Deviation-2 ruling: the adapter dependency-map merge delivers whole-dict-per-key (wrong shape for granular consumers); the A-R3 seam spread is the documented BRIDGE (tombstone test cites this entry by name). End state: edge-level key PROJECTION declared on the manifest (producer output keys → consumer input keys), compile-validated against producer belt-pins + consumer CONSUMED_PAYLOAD_KEYS; §06→gary and 07B→quinn_r edges then move from quarantine to contracted; seam retires by deliberately breaking its tombstone. SEQUENCED AFTER the manifest vocabulary-version field (Winston S1-B: the manifest data plane currently has no versioned home; pack_version provably does not govern dependencies — determination at commit 2a617f5). John deadline: ruled mechanism lands before S5. | S4 story (with manifest version field first or same commit). Also at S4: builder specialist_id "marcus" → dedicated id (Winston c; latest_for_specialist("marcus") collision); walker shared-policy extraction (Winston d.2); seam-vs-dependency disagreement detector pairs with provenance plumbing (Murat b); GammaDispatchError → pause-not-cycle-death (Amelia trap 1). |
| **`ratchet-d-producer-write-keys-leg`** | 2026-06-12 | Murat: Ratchet-D is two-way (manifest↔consumer); the third leg (producer write keys match manifest claims) is unproven statically. Mitigated: S3's builder-output⊆contract pin instances it for the bitten edge; S0 fail-loud converts producer-drift starvation to a raise. | Rides — **binding trigger: lands BEFORE any NEW producer node is added to the manifest** (that is when drift gets authored). |
| **`gary-gamma-repo-root-export-default-retirement`** | 2026-06-12 | Amelia c: orchestrated Gary now exports under the run dir (state/config/runs/<id>/exports/gary — already gitignored via state/config/runs/; decision recorded, no .gitignore change needed). The old repo-root `runs/gary-gamma` default in generate_gamma_variants remains for standalone invocation. | Grep gary skill/references/tests for `runs/gary-gamma` readers; retire or redirect. **Direction may flip if substrate evolves** — standalone (non-orchestrated) Gary may legitimately keep a repo-root default; re-validate at dispatch. Also pin export-vs-attempt re-emit semantics when S4 touches exports (attempt+1 overwrite can desync disk exports from envelope attempt). |

**Also recorded:** Winston nit (kira empty-string video URL graduates to a typed raise if cycle-2 shows it surviving to a gate); Amelia trap 5 (pin the exact 21-test ambient-failure roster so S4 breakage shows as roster growth — fold into S5 prep); John (e) ruling: S4+S5 get their own lean party round.

---

## Codification-batch riders (filed 2026-06-12; party review of the Trial-3 cycle-2/3 codification batch, 4× APPROVE-WITH-AMENDMENTS; MUST-FIXes executed same-session)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`dp-v2-refinement-self-edge-vocabulary-plus-storyboard-b-roster`** | 2026-06-12 | Winston MUST-FIX 1: nodes 05/05B's refinement input (latest irene-pass1 plan) rides the RUNTIME FALLBACK because the compiler's cycle guard refuses a declared `upstream_output: irene-pass1` self-edge — an undeclared seam of exactly the class Ratchet-D exists to retire. Bundled per Winston: the G3-side Storyboard-B entry in `storyboard_publisher.STORYBOARD_GATES` is the same vocabulary-extension moment. Winston rider R2 rides here too: tighten `read_extracted_source`'s scan-every-dict tolerance to declared keys when dp-v2 lands. | dp-v2 vocabulary story: refinement/self-history projection form the compiler can validate without a cycle; extend STORYBOARD_GATES with the G3-side Storyboard-B entry; tighten the reader. Reactivate when the G2C→G3 segment enters scope. |
| **`storyboard-publish-idempotency-pin`** | 2026-06-12 | Amelia rider R2: recovery re-entering a publish-succeeded-then-pause-crashed state re-runs publish; the legacy routine archives/replaces same-run publishes — verified BY HAND at cycle 3, which is evidence, not a pin. | Re-publish-same-run test against the real routine (skills-side or handshake-suite leg). Low urgency; the routine is frozen. |
| **`conftest-publisher-stub-exemption-self-check`** | 2026-06-12 | Murat rider A: the nodeid exemption protecting the publisher's own suite from the autouse stub fails open on file rename. | One-line collection-time assertion that the exemption matched ≥1 test, or replace dir-wide autouse with an opt-out marker at next touch (Amelia rider R1 same surface). |

**Also recorded:** Murat rider B — the S5 negative test (broken brief → gate FAILS) is OWNED by the S5 fresh-run story; recorded here so the ownership claim cannot orphan at story close. ~~FIRED at dp-v1.1 (2026-06-12): `test_broken_brief_fails_the_gate_not_quality_theater` — empty plan_units → typed BuilderInputError, G2C card never issued, storyboard never published.~~ John riders — first error in the fresh run is stop-and-codify, never silent hot-fix; Mary's cross-trial harvest entries for cycles 2/3 file at the next methodology window.

**dp-v1.1 partial landing (2026-06-12):** the `dp-v2-refinement-self-edge-vocabulary-plus-storyboard-b-roster` bundle's **Storyboard-B roster half LANDED** at the cycle-4 08/08B remediation (`STORYBOARD_GATES` gains `G3B: storyboard-B`; Pass-2 narration overlay threads the legacy `--segment-manifest` leg; distinct `-b` pack slug). The self-edge vocabulary + `read_extracted_source` tightening halves REMAIN deferred to dp-v2 — note node 08 now also rides latest-contribution resolution for `lesson_plan` (same dp-v2 per-node-addressing gap; Amelia uniqueness mitigation recorded in the dp-v1.1 consensus).

## dp-v1.1 riders (filed 2026-06-12; cycle-4 08/08B remediation party consensus)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`grounding-contract-downstream-seam-hardening`** | 2026-06-12 | John scope ruling at the dp-v1.1 round: PIN-G1 (manifest-wide grounding audit) ships with a 21-row shrink-only GROUNDLESS_ALLOWLIST; rows for §11-§15 seams (elevenlabs 11B/12, compositor 14, quinn-r 13, vera 10, marcus gate nodes) defer per-specialist grounding + fail-loud hardening until those segments enter trial scope — don't harden seams the trial cannot reach. | When each downstream segment enters scope: declare projections for its nodes, publish CONSUMED_PAYLOAD_KEYS, retire the allowlist rows (test enforces shrink-only). Direction may flip if substrate evolves. |
| **`pass2-anchor-term-heuristic-pin`** | 2026-06-12 | Murat PIN-G4 (optional tier): deterministic checks catch structurally-empty grounding; "plausible content from the wrong corpus" stays semantically invisible. The slide-roster join check landed; a corpus-anchor-term heuristic (output must contain ≥N deck-title tokens) did not. **Murat review rider R1 rides here:** `_assert_narration_joins_roster` returns early on empty `narration_script` — a hollow-but-grounded Pass-2 output bypasses the join (G3B coverage likely nets it; "likely" is not a pin). | Warning-tier/xfail-tolerant heuristic pin if a fifth confabulation instance appears despite PIN-G1/G2; make the empty-narration case raise in the grounding family (or pin that downstream G3B coverage rejects it). |
| **`g3b-grounding-attestation-line`** | 2026-06-12 | John review rider R1 (dp-v1.1 review, 4× APPROVE): the operator should not forensically assemble trust at the G3B HIL gate — a one-line grounding attestation (which corpus/plan/brief/roster inputs node 08 consumed) belongs on the decision-card surface. INTERIM PROTOCOL (binding): the orchestrator verbally surfaces the node-08 input keys + B-pack publish record at every cycle-5+ G3B pause until this lands. | Lands with the NEXT G3B-touching story (concrete reactivation trigger). |
| **`mf6-sanctum-precondition-skip-not-fail`** | 2026-06-12 | Winston review rider R2: the llm_live cache-hit-rate baseline hard-FAILS on any populated-sanctum workstation (assert 50 == 0), forcing every batch to re-derive it as ambient. | Convert the MF6 sanctum-empty precondition to `pytest.skip` carrying the archive instruction. Warning-tier. |
| **`b-manifest-join-lossiness`** | 2026-06-12 | Amelia review rider R1 (also Winston R1 review-blindness): `_write_segment_manifest_for_b` joins each delta to its FIRST `perception_source` — a slide referenced only as a secondary ref passes quinn's G3B coverage yet renders narration-less in the published pack; `text_by_id.get(id, "")` publishes silently-empty narration on id mismatch. Bounded: the operator sees the pack online. | Fix the join (multi-slide deltas + id-mismatch raise) with the dp-v2 segment-manifest schema rework; if the transform grows selection logic, thread its output into quinn's review or add a post-publish check. |

## Roadmap-sequencing round rulings (filed 2026-06-12; party round Winston/Amelia/Murat/John on post-certification next steps)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`delivery-agent-role`** | 2026-06-12 | Operator ruling resolving the round's one disagreement (John wanted Descript rough-in at Wave 2; Winston/Amelia deferred): "the hand-off will be more than prepping Descript — a lesson plan might call for quizzes, worksheets, or other assets finished and/or deployed in places other than or in addition to Descript. We're really talking about another sophisticated agent role." Supersedes the narrow item-9 Descript framing. Desmond's automation advisory + the cycle-5/6 DESCRIPT-ASSEMBLY-GUIDE are the substrate evidence; Canvas connector exists for LMS-side deployment. | Design as a NEW AGENT (delivery/deployment specialist) when the platform's built-in 'add an agent to the workflow' capability is first exercised — that event is the reactivation trigger. The measured-durations rider feeds it (accurate rough-in timing). Direction may flip if substrate evolves. |
| **`roadmap-consensus-2026-06-12`** | 2026-06-12 | 4-of-4 party consensus on sequencing under the production-soon/no-breakage constraint. | Wave 0: storyboard correctness (glitches + blank fields + join lossiness) + phantom-delta fix + motion diagnosis + taxonomy re-base → closed by **Trial A** (literal text/visual slides + clustering, FROZEN engine — Murat: one-time old-engine baseline). Wave 1: pause-topology contract pin FIRST → fold-semantics gate-engine fix → wake G2B variant-pick + G4A voice-pick (folded mode stays default; pause opt-in) → closed by **Trial B** (certified Tejal corpus, both pauses exercised + unattended rerun). Witness→strict flip after A+B certify. Then Marcus SPOC thin slice (absorbs operator-interview/connectors item 7 + research item 5) on the fixed engine, write-API-only (Winston second-control-plane prohibition). Triggered entries: brand/theme packs at SME #2; Epic-15 learning at 5-10 banked production runs (Murat: non-stationarity blocked until then); visual-scan VO after motion proven; delivery-agent-role per its own entry. |

## dp-v1.2 riders (filed 2026-06-12; audio-segment arc party consensus, operator full-delegation directive)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`voice-selection-hil-fold-defect`** | 2026-06-12 | Winston/John (audio-arc): nodes 11/11B declare gate codes G4A/G4B with `fold_with: G4`, but G4 pauses BEFORE node 11 — the voice-selection HIL is structurally unable to pause ("HIL-in-name-only"); enrique silently defaults to the recommended voice. Acceptable under the operator's blanket delegation; fold-semantics fix (a gate that can pause after its parent) is gate-engine surface area requiring its own party round. | Reactivate at the first run where the operator wants a non-default voice. Direction may flip if substrate evolves. **Retirement tie (John R1, executed 2026-06-12):** when this lands, also retire the PIN-G1 `(11B, elevenlabs)` GROUNDLESS_ALLOWLIST row — `tests/contracts/test_manifest_grounding_contract.py::test_11b_allowlist_row_tied_to_voice_hil_rider` pins the linkage and fires if this entry closes while the row remains. |
| **`tagged-error-taxonomy-systematic-rebase`** | 2026-06-12 | PIN-AUD-3T's first run found 18 pre-existing bare tagged-error classes across app/specialists (gary, texas, kira, wanda, vera, cd, dan, desmond, tracy, aria/kim/mira/tamara/vyx parse errors) — each is the cycle-5 crash class waiting for its node. Seeded as a shrink-only EXCLUSIONS list. | Per-class re-base with catch-site greps (Amelia discipline), retiring exclusion rows as they land; prioritize classes on the live trial path (gary, texas, kira, vera). **LIVE-PATH TRANCHE EXECUTED 2026-06-12** (spec-taxonomy-rebase-live-path): GaryActError + ReceiptParseError + BundleParseError + KiraActError + FTRParseError re-based (catch-site audit: each caught once by name in its own act(); base is RuntimeError-derived so all handlers preserved); 5 exclusion rows retired (18→13); gary fabricated slide-01 roster KILLED (`gamma.slides.starved` raise) + ninth-seam regex widened to multi-key/multi-row. Remaining 13: cd/dan/desmond/tracy/wanda/aria/kim/mira/tamara/vyx parse errors + ElevenlabsDispatchError (legacy probe) + BuilderInputError (pre-dispatch runner leg) — next tranche at their seams' first live exercise or next substrate touch. **TRANCHE 2 EXECUTED 2026-06-17** (spec-taxonomy-rebase-tranche-2; party green-light + conflict-adjudication Winston/Amelia/Murat/John): `BuilderInputError` re-based onto SpecialistDispatchError (redundant ctor dropped — base ctor byte-identical; all 6 tagged raise-conditions preserved per-condition into error-pause.json) + BOTH `run_builder_node` call sites (`production_runner.py` start-walker + resume/recover-walker) wrapped in `except SpecialistDispatchError → _pause_at_error` under the `package_builder` identity. **Node-06 was edge-proven the LAST live-walk dispatch leg outside the error-pause family** — the live-path crash→error-pause invariant is now COMPLETE. Two party-ratified pins (`test_starved_resume_*`, `test_broken_brief_*`) migrated crash→error-pause with anti-quality-theater guarantees (no G2C card, no storyboard publish) preserved VERBATIM + recover-determinism MUST-FIX + kill-the-mutant verified; EXCLUSIONS 13→12 (reverse-existence red observed before row deletion). See **`tagged-error-taxonomy-tranche-3-offpath-sweep`** below for the remaining 12. |
| **`tagged-error-taxonomy-tranche-3-offpath-sweep`** | 2026-06-17 | John A7 hard precondition (tranche-2 close): the 12 EXCLUSIONS classes remaining after tranche 2 are all OFF the live walk path — re-basing them buys zero robustness on any trial runnable today, so they were deliberately deferred (NOT abandoned) rather than swept into tranche 2's tight single-anchor diff. Enumerated by name: `aria/kim/mira/tamara/vyx.OperatorInstructionsParseError`, `cd.CdDirectiveParseError`, `dan.DanAuxParseError`, `desmond.HandoffParseError`, `tracy.ManifestParseError`/`RetrievalIntentParseError`, `wanda._act.WandaActError`, `enrique.elevenlabs_dispatch.ElevenlabsDispatchError` (legacy-probe path only). | Per-class re-base with its OWN catch-site grep audit (tranche-1/2 discipline — Winston: adjudicate fail-loud-vs-pause INDIVIDUALLY per seam; a parser seam whose input IS the defect may legitimately want fail-loud, do NOT presume the pause family is the right home). Direction: **drive EXCLUSIONS toward empty, live-path-first** (Murat). Reactivation: any of the 12 jumps the queue the moment its seam goes live-path; otherwise a dedicated off-path sweep. EXCLUSIONS stays shrink-only + guarded by the reverse-existence pin (self-honest at any size — non-empty is hygiene, not a hazard). |
| **`enrique-measured-durations`** | 2026-06-12 | Enrique estimates durations from char count (len/14 ≈ 150wpm) — G5's WPM check is advisory-only against estimates (Winston: self-referential otherwise). Real measured durations need an audio probe (mp3 length). Winston review MUST-FIX 2 executed: payload-supplied durations are labeled "provided" (planned numbers), never "measured". | Probe synthesized mp3 duration post-TTS; set duration_source="measured"; G5 WPM raise re-arms automatically. |
| **`dp-v1.2-review-riders-bundle`** | 2026-06-12 | dp-v1.2 review (4× APPROVE): Winston R1 join-test hygiene (vacuous self-compare in test_join_policy; publisher test asserts presence not bytes) + R2 delete DEFAULT_BUNDLE_PATH constant after direct-caller audit; Amelia R1 phantom-delta silent audio gap (delta id matching no narration → empty-text row silently skips TTS yet counts as G5 coverage — refuse or drop pre-coverage, with pin) + R2 delete dead `_act_with_trail` in quinn_r/graph.py (zero callers, load-bearing-looking exception policy); Murat R1 generalize the ninth-seam inline-roster regex (whitespace/rename escapes) + R2 module-qualify taxonomy EXCLUSIONS keys (bare-name reuse inherits exclusion); John R1 tie the (11B, elevenlabs) allowlist row's retirement to the voice-HIL fold fix (per-node act discrimination) so the row cannot become a permanent fixture. | ~~Execute as a hygiene mini-batch at next substrate touch of the respective files; Amelia R1 is the highest-priority row (silent-gap family).~~ **EXECUTED 2026-06-12** (trial/4 branch): Amelia R1 at `ebe0c3f` (spec-phantom-delta-silent-audio-gap); remaining six rows in the follow-on hygiene-mini-batch commit (spec-dp-v1-2-hygiene-mini-batch). Murat R1 executed to its ratified scope (whitespace/rename); multi-key/multi-row regex widening deferred to the taxonomy re-base slice — its first catch is gary's live fabricated roster (`app/specialists/gary/_act.py:95`, two-key inline roster on the empty-input path), which is gary's re-base work (see deferred-work.md §2026-06-12). |

**Also recorded (dp-v1.1 review, next-touch tier):** Amelia R2 — `run_storyboard_b_review` treats missing `segment_manifest_deltas` as blocked-verdict rather than starvation (inconsistent with its docstring; align at next touch) + `_parse_pass_2_response` docstring claims a verify-node guard that is a stub (one-line truth-up). Murat R2 — **cycle-5 closes PIN-B4 explicitly**: the relaunch postmortem records G3B-published-before-pause with non-None segment manifest from the live run; if cycle 5 never reaches 08B, the deferred G3B walk test re-enters scope automatically. John R2 — **G2C delegated-approve on relaunch requires a MECHANICAL A-side identity check** (slide roster + content comparison vs the cycle-4 operator-approved pack); ANY content delta lapses the delegation and G2C reverts to a full operator pause.

## Storyboard-correctness dispatch — deferred riders (filed 2026-06-18; party-mode green-light of spec-storyboard-correctness-cover-shift)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`gamma-cover-suppression-spike`** | 2026-06-18 | John DECISION-4 hard ruling at the storyboard-correctness green-light: investigating whether `GammaClient.generate_deck` can request "no title/cover card" is pure schedule risk against the frozen-engine Trial A with zero payoff (the title-match + cover-drop backstop is permanent and load-bearing regardless). Cut from the dispatch entirely. | Post-Trial-A spike: probe Gamma API/prompt for reliable cover suppression across themes/templates. Even if reliable, the title-match backstop NEVER weakens (Winston: an upstream flag we don't control is not an invariant). Noise-reduction only. |
| **`storyboard-source-ref-provenance`** (Observation B) | 2026-06-18 | Cycle-6 review Observation B: `source_ref` blank on all storyboard rows (A and B) — no row→corpus-doc provenance link. John ruled DEFER at the green-light: provenance plumbing is a separate concern from the alignment contract, doesn't gate Trial A, doesn't fall out of the title-match work for free (unlike Obs A, which was folded in). | Own item: wire `source_ref` from the brief's corpus ref through the storyboard row. Low severity. |
| **`gary-export-over-count-recovery`** | 2026-06-18 | At the green-light, over-count beyond a single leading cover (e.g. 2 chrome pages, trailing "thank-you" page) is handled in the dispatch by FAIL-LOUD (`gamma.export.page-unmatched`), which falls out of the bijection assertion. Any RECOVERY logic (auto-classifying/dropping non-leading chrome) was explicitly deferred as creep (John). | Only if a real Trial-A/production deck demonstrates a recurring non-leading-chrome pattern that fail-loud makes too noisy. Until then, fail-loud is correct. Direction may flip if substrate evolves. |
| **`gamma-long-title-truncation-rule`** (CONDITIONAL) | 2026-06-18 | Spec T1 15-min check: if Gamma is DEMONSTRATED to truncate long titles in the export slug, a deterministic truncated-prefix match rule is IN the dispatch (else spurious `brief-unmatched` on a deck we expect to pass — Trial-A blocker). If only SPECULATIVE at T1, it is deferred here. | Reactivate if a briefed deck's long title is observed truncated in the `{N}_{Title}.png` slug. Deterministic prefix rule only — no fuzzy/truncation-tolerance engine (party: no fuzzy matching anywhere). |

---

## Drift-audit micro-batch riders (filed 2026-06-12; drift-audit party round + Dr. Quinn "witness, don't gate" synthesis, predicted 4/4 ACCEPT-WITH-CONDITION, operator GO)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`trial-envelope-validator-witness-to-strict-flip`** | 2026-06-12 | Lifecycle invariants on `ProductionTrialEnvelope` (paused-at-gate ⇒ paused_gate; paused-at-error ⇒ paused_error_tag; completed ⇒ completed_at) landed as a TWO-MODE validator per the Quinn synthesis: witness default at runtime (violations → `runs/<id>/anomalies.jsonl` sidecar + warning; never raises) and strict in tests/CI. Characterization corpus (7 live run dirs + golden) round-trips clean at landing. Closes detection half of composition-spec §12 limitation #9 (DFR-6.1-5). | **Flip ceremony (dated story, not an open-ended ghost — Winston condition):** flip the default witness→strict. Trigger: S5 fresh-launch acceptance COMPLETE **AND** every `anomalies.jsonl` produced during S5 reviewed clean. MUST close before any story relies on construction-time enforcement. The round-trip characterization suite (`tests/unit/runtime/test_production_trial_envelope_invariants.py`) is permanent and must stay green across the flip — a legacy artifact that violates post-flip gets a documented carve-out, never a silent loosening (Amelia condition, copied verbatim per her instruction). |

**Audit-process correction (recorded per party ruling, all four voices):** the 2026-06-12 external drift audit's finding "shared `SpecialistDispatchError` base gone; Gary back to `GammaDispatchError(RuntimeError)`" is **STRUCK — factually wrong**. The files left the dirty/untracked set because they were COMMITTED at `c4678ff`; verified at HEAD content: the base class is tracked and all six seam classes subclass it (pinned machine-answerably by `tests/integration/marcus/test_production_runner_error_pause_recover.py::test_dispatch_error_family_shares_recoverable_base`). Method failure named so it cannot recur as a finding: **the auditor inferred content from `git status`; drift-audit claims of regression/removal/revert must cite content-at-SHA (`git show <sha>:<path>` + file:line), never dirty-set membership — a claim without a content citation is an unverified observation and cannot drive disposition** (Murat sin-1 rule). Murat sin-2 rule (ambient-failure manifest; audits report only the delta) folds into the already-recorded Amelia trap 5 S5-prep rider above. The audit's other four findings verified real and were dispositioned same-day: templates folded into the 🔴 renderer/L1 story (amended above); stale `marcus.*` strings fixed + double-pinned; composition-spec rewritten as-built + lockstep-pinned; envelope validators landed witness-mode (entry above).

---

## Closed Entries — Archived (preserved for audit trail)

_24 closed entries archived 2026-05-07 at pre-Trial-3 cleanup S1 P0-IH per Mary AM-5 amendment. These rows previously lived intermixed with active entries throughout the inventory; segregated here for hot-start scannability of S2-S6 sessions. All content preserved verbatim — no entries deleted; this is a structural reorganization only._

Each row carries its original close-marker (`~~strikethrough~~` / `**CLOSED 2026-...**` / `**RESOLVED-AT-...**` / etc.) inline. Roll-up disposition into permanent registers (anti-pattern catalog / governance JSON / sprint-status) happens at S6 close.

| ~~**`vera-sanctum-sidecar-to-bmb-migration`**~~ | ~~Story 7b.3 T2 / AC-7b.3-B~~ | ~~Vera shipped with legacy `_bmad/memory/vera-sidecar/` 5-file sidecar before Slab 7b SG-4 enforcement.~~ | **CLOSED 2026-04-29 at Story 7b.3 T2.** Canonical `_bmad/memory/bmad-agent-vera/` six-file BMB sanctum landed; legacy sidecar preserved unchanged. |
| ~~**`fr105-tests-parity-per-specialist-subdir-decision`**~~ | ~~Slab 7b PRD Errata 4 / Story 7b.1 T1~~ | ~~Wave 1 must ratify `tests/parity/per_specialist/` subdir vs existing flat parity layout before first per-specialist activation contract test lands.~~ | **CLOSED 2026-04-29 at Story 7b.1 T1.** Verdict: flat layout is canonical; tests live at `tests/parity/test_<specialist_name>_activation_contract.py`. Recorded in `tests/parity/README.md` §"FR105 + Errata 4 layout decision". |
| ~~**`wanda-sanctum-test-expected-files-constant-drift`**~~ | ~~Story 7b.2 T11 review wider-regression sweep (2026-04-29)~~ | ~~`tests/specialists/wanda/test_wanda_sanctum_populated.py::test_sanctum_digest_nonempty_post_population` fails: `EXPECTED_SANCTUM_FILES` constant lists 5 files but disk reality has 6.~~ | **CLOSED 2026-04-29 at Story 7b.9 T2.** Wanda migrated from legacy `_bmad/memory/wanda-sidecar/` to canonical `_bmad/memory/bmad-agent-wanda/` six-file BMB (`INDEX/PERSONA/CREED/BOND/MEMORY/CAPABILITIES`), and the expected-files constant now matches that tuple. |
| ~~**`audit-pattern-T1-smoke-elaboration-for-extend-and-audit`**~~ | ~~Story 7c.5.G1 T11 review (2026-05-05)~~ | ~~G1's T1 backward-consumer audit ran static grep only and declared `drafted_proposal` + `evidence` as "drop because no audited consumer reads them after construction." T6 broad regression revealed live consumers exercised at smoke/integration time. Risk grows for G2C/G3/G4 with AMELIA-P3 staggering downstream pressure.~~ | **CLOSED 2026-05-05 via party-mode V6 amendment ratifying AMELIA-P5 (DROP-row Heavy gate)** + Winston per-row `audit_method` qualifier composition. Codified at `docs/dev-guide/migration-story-governance.json::extend_and_audit_t1_audit_method` (binding=hard validator rule: if disposition=DROP and audit_method=light/missing → REJECT). G2C+G3+G4 prompts inline AMELIA-P5 §audit_method requirement at T1. Q1 UNANIMOUS (John+Winston+Murat+Amelia all 4 voices ratified). |
| ~~**`bmad-memory-gitignore-force-add-policy`**~~ | ~~Story 7b.3 T11 review handoff (2026-04-29)~~ | ~~Codex's 7b.3 handoff surfaced that `_bmad/memory/bmad-agent-vera/` (sanctum migration target) is gitignored at the repo level. The 6 BMB files exist on disk but require `git add --force` to land in commits.~~ | **CLOSED 2026-04-30 by operator ratification.** `.gitignore` updated to `_bmad/*` + `!_bmad/memory/` so the entire `_bmad/memory/` subtree is now tracked normally. All sanctum content (BMB 6-file pattern, legacy sidecar variants, Class-C+/D2 4-file operational-metadata sidecars) lands in commits without `--force`. Slab 7b retrospective close commit will land the 60+ previously-hidden sanctum files atomically. |
| ~~**`2c-1-generator-auto-emit-retire-removal-support`**~~ | ~~Story 2c.1~~ | ~~Retiring a transient generator-validation specialist currently requires manual C3 row removal plus explanatory comment; repeated validation exercises would normalize manual string surgery.~~ | **RESOLVED-AT-2c.4.** Generator now supports `--retire <name>` with idempotent string-level Contract C3 row removal mirroring 2a.5 auto-emit surgery; it does not touch specialist trees or tests. |
| **`2c-3-m2-verdict-conditional-on-2c-2-live-artifact`** | Story 2c.3 | **RESOLVED 2026-04-27.** Operator executed M2 Wondercraft live-artifact ceremony 2026-04-27. One real podcast generated via `POST /podcast/scripted` against live Wondercraft API: artifact at `tests/fixtures/specialists/wanda/live_artifacts/2026-04-27/25dcc0554b12b3e5f99aa2290bcdb594d9b205d2a489fcdd16f6d87cad16b792.mp3` (1.13 MB; ~71 sec; ~$2.25 within $5 ceiling). Addendum pasted into `slab-2c-m2-acceptance-verdict.md` §Operator-Window Addendum. M2 promoted CONDITIONAL-GREEN → GREEN-LIGHT. Two A16 instances surfaced + handled in same session (WondercraftClient payload-shape defect + status-field-name mismatch); full client cleanup filed as new entry `5a-2-wondercraft-client-payload-shape-defect`. M5 condition #1 closes. | (Resolved.) |
| ~~**Cache-hit-rate harness re-enablement (FR54)**~~ | ~~Slab 1 Story 1.6 shipped the harness at `tests/end_to_end/test_cache_hit_rate_baseline.py` with `pytest.skip(...)`. The FR54 M1 acceptance bar (â‰¥60% cache hit rate on second invocation) was deferred per 2026-04-22 set-level consensus because Slab 1 ships passthrough specialists (zero LLM calls â†’ undefined cache rate).~~ | ~~First Slab 2 specialist that lands a real LLM call at its `act` node.~~ | **CLOSED 2026-04-25 at Story 2a.2 T7 waypoint.** Harness retargeted at Irene Pass 2; measured 95.33% median cache-hit-rate (vs 60% floor; +35.33pp slack). M1 ACCEPT-WITH-GAP fully retired. See `m1-acceptance-evidence-pack.md` Â§M1 Cache-Hit-Rate Clause for evidence block + Story 2a.2 Completion Notes for full measurement protocol. |
| ~~**`slab-3-m5-dispatch-registry-swap`**~~ | Story 2b.15 close + Story 4.5 inheritance | **RESOLVED-2026-04-26.** Verified every registered dispatch target imports cleanly, then promoted both `state/config/dispatch-registry.yaml` and `runtime/graphs/v42/dispatch-registry-snapshot.yaml` from `_status: interim` to `_status: production`. | Closed in the pre-trial Batch 1 Phase 1 pass; no further reactivation needed unless the registry re-enters a non-production state. |
| **`5a-2-production-graph-entrypoint-substrate-gap`** | Production-graph launcher investigation 2026-04-27 | **RESOLVED-2026-04-27.** Closed by Slab 6.0 substrate (`migration-6-0-production-envelope-substrate.md` per Path A-prime) + Slab 6.1 runner-consumes-substrate (`migration-6-1-production-graph-runner.md`); checkpoint resume patched 2026-04-27; operator-witnessed live gate-resume smoke passed in 30.54s. | (Resolved.) |
| ~~**`migration-6-2-promote-dependency-map-into-manifest`**~~ | Slab 6.1 bmad-code-review DFR-6.1-1 (AA-2) | **RESOLVED-2026-04-27 at Slab 6.2 close.** Implementation landed at commit `01631b6`; bmad-code-review at `8a780f1` + patch `c652747` + review record `da150a7`. Manifest nodes accept `dependencies: dict[str, str]`; compile-time circular-dependency validation (alias-normalized post-A9 patch); production runner resolves manifest first with PERMANENT runner-layer fallback for undeclared/empty nodes; `MissingUpstreamContributionError` fails loud per Composition Spec §3.6. All 8 BINDING + 3 NON-BLOCKING party-mode riders satisfied. Composition Spec §3.6 + §10 Decision Log + §12 known limitation #1 RESOLVED; migration guide §"Production Runner" augmented; governance JSON version bumped. | (Resolved.) |
| **`5a-5-m5-conditional-window-2026-05-03`** | Story 5a.5 | **REFRAMED 2026-04-27.** Original SHIP-CONDITIONAL conditional window superseded by 3-agent party-mode Option E reframe (Winston + Murat + Quinn-R unanimous GREEN-WITH-CONCERNS). Verdict promoted from SHIP-CONDITIONAL to SHIP for bounded-MVP scope; condition #3 (5a.2 production clone-launch equivalence) reframed as Slab 6 opener `migration-6-1-production-graph-runner.md`; condition #4 (Plausible-Token Substrate Contamination) RESOLVED 2026-04-27 via operator-side live-OpenAI cascade-tier smoke (3/3 PASS in 7.00s + bonus A16-instance discovery + same-session patch). **Two operator-presence items remain — no longer "conditional window," just final-mile close work:** M2 Wondercraft live-artifact ceremony + M3 Texas live-retrieval ceremony. | Reactivate when operator runs M2 ceremony + M3 ceremony (~30 min combined). When both close + their addenda paste into respective verdict artifacts, this tracking entry RESOLVES. Migration is then unconditionally shipped for bounded MVP scope; original demotion rule no longer applies (superseded by reframe). |
| **Generator auto-emit pyproject.toml C3 ignore_imports row (2a.1 follow-on defect)** | Story 2a.2 T2 dev-agent discovery (party-mode 2026-04-24 ratified) | **[CLOSED 2026-04-25 at Story 2a.5 â€” path (b) shipped]** Generator at `skills/bmad_create_specialist/scripts/generate.py` now atomically appends `app.specialists.<name>.graph -> app.gates.resume_api` to Contract C3 `ignore_imports` with generated comment marker, idempotency, dry-run no-op behavior, and rollback coupling to the 9-file specialist emission path. Manual `pyproject.toml` edits are no longer required for new specialist generations. | Closed by Story `migration-2a-5-generator-auto-emit-pyproject-c3-row` (single-gate, 2026-04-25). Slab 2b.1 kickoff hard gate 1 satisfied per `_bmad-output/implementation-artifacts/slab-2a-retrospective.md` closing-note update. |
| **AC-2a.4-B-OP live retrieval-dispatch reactivation (Texas)** (`2a.4-followon-ac-b-op-live-retrieval`) — **RESOLVED 2026-04-27** via Notion locator-shape ceremony (`scripts/operator/m3_texas_ceremony_notion.py`); 1 real page returned + evidence at `tests/fixtures/specialists/texas/live_retrieval/2026-04-27/437e2b249286c035611933270ffe9216ac7329b30318022b036587a27e019953.json`; M3 verdict promoted CONDITIONAL-GREEN → GREEN-LIGHT; addendum pasted in `slab-3-m3-acceptance-verdict.md` §Operator-Window Addendum. Scite-shape execution deferred as separate follow-on `5a-2-scite-mcp-oauth-integration` (third A16 instance discovered same day). | Story 2a.4 pre-T1 party-mode round 2026-04-25 (3-1 consensus: Winston + Murat + Paige â†’ Path 3 defer; Amelia â†’ Path 4 fallback compatible with defer) | `skills/bmad-agent-texas/scripts/run_wrangler.py:58` imports `marcus.dispatch.contract` which does not exist on hybrid (`marcus/dispatch/` absent â€” only `intake/lesson_plan/orchestrator/facade.py` shipped). Empirical grep gate (`grep -rn "marcus\.dispatch" skills/bmad-agent-texas/`) found marcus.dispatch.contract symbols **consumed on the runtime path** at `run_wrangler.py:120-125 _to_dispatch_outcome()` + `:1707-1709 build_dispatch_envelope(DispatchKind.TEXAS_RETRIEVAL)` + `:1717 build_dispatch_receipt()` + `:2021-2031 second-invocation-site` â€” NOT dead-on-arrival as initially theorized. Lazy-import shim (Path 2) would silently produce broken envelope shape â†’ vacuous-pass evidence (Murat M5 binding from 2a.3 P9 lesson). Forward-port marcus.dispatch.contract (Path 1, ~50-100 LOC) was rejected as Slab-3 architectural scope drift inside a scaffold-migration story. Defer (Path 3) is the disciplined call. **Mock-vs-live delta accepted at 2a.4** (Murat hard caveat point a): AC-B mockable wrapper shape verification is good but NOT live-wire proven â€” sha256 baselines (M2 sanctum lock + M3 NFR-I5 retrieval-contract) ARE live-proven (real files); subprocess-dispatch-latency (M4) is NOT measurable until live wire fires; bundle-parser parametrized parse-branch coverage (M1, 7 cases) is mock-proven against fixture bundle. **Deferral authority (Murat hard caveat point b):** Story 2a.4 pre-T1 party-mode round 2026-04-25 (3-1 consensus); see Story 2a.4 spec Â§"Filled-in evidence" Completion-Notes section + this inventory entry. | **Slab-3 reactivation gate (Murat hard caveat point c â€” BINDING; Slab-3 close cannot accept "AC-B-OP fires" alone):** before Slab-3 closes, re-execute the AC-B-OP path on **live wire** with the full M1-M5 test discipline applied: (i) 7 parametrized parse-branch cases against actual scite/Consensus MCP responses (not fixture bundles); (ii) two-sided assertions on parse outcome AND resolution-trail tag; (iii) M2/M3 sha256 baselines re-validated post-Slab-3-changes (sanctum lock + NFR-I5 retrieval-contract preservation); (iv) M4 subprocess-dispatch-latency p50/p95 measured + recorded in Â§12.10 retrospective as the substitute-metric baseline; (v) operator runs `.venv/Scripts/python.exe scripts/utilities/ac_b_op_texas_live_retrieval_evidence.py --directive tests/fixtures/specialists/texas/ac_b_op_directive.yaml --bundle-dir <path>` end-to-end + pastes filled-in evidence Markdown block into the Slab-3 reactivation story. Helper script + directive template are PRE-AUTHORED at 2a.4 and SHIP UNINVOKABLE-ON-HYBRID-TODAY; Slab-3 inherits without re-authoring. Anti-pattern harvest (Paige proposal at party-mode 2026-04-25): file as **A13 â€” "Premature live-wire AC against not-yet-ported substrate"** OR augment A9 (structural-name drift) sibling subsection at Slab-3 reactivation. The harvest is BETTER than codifying a lazy-import shim pattern (which would invite 14 Slab-2b specialists to reach for it reflexively). |
| ~~**`3-6-marcus-envelope-baseline-capture-for-future-regression-detection`**~~ | ~~Story 3.1 filed-forward rider W-R7~~ | ~~Slab 3 needed a frozen Marcus envelope on the first real local trial run so Slab 4+ regression tests would have a migration-native baseline instead of relying on pre-migration golden-trace assumptions.~~ | **RESOLVED-AT-3.6.** Story 3.6 captured `tests/fixtures/marcus/baseline_envelope/2026-04-26/{envelope.json,BASELINE_METADATA.md}` with schema version, provenance, and capture-environment hash. |
| **`slab-2b-mid-cross-cutting-importlib-loader-extraction`** | Story 2b.3 close (R2 third-occurrence trigger) | Quinn-R lands the third importlib-loader seam occurrence after Kira + Vera (and dual wrapper in the same story). Shared loader extraction is now mandatory per template R2. | **CLOSED at 2b.15 close.** Shared importlib loader extraction landed in `app/specialists/_scaffold/dispatch_loader.py` as part of dispatch-contract hardening. |
| **`SanctumLockViolation` cross-specialist refactor** (`slab-2a-close-followon-sanctum-lock-cross-cutting`) | Story 2a.4 G6 close (2026-04-25), flipped at Story 2b.3 close | 2a.4 introduced `SanctumLockViolation(RuntimeError)` named exception class for Texas with hard sha256 lock-and-verify protocol per Winston W1 rider; Irene (2a.2) and Kira (2a.3) currently raise generic `RuntimeError` on lock drift. Cross-specialist consistency would let any specialist's lock-and-verify tests assert the same exception class. | **CLOSED at 2b.15 close.** Sanctum exception extraction landed in `app/specialists/_scaffold/sanctum_exceptions.py` as part of dispatch-contract hardening. |
| **WondercraftClient payload-shape + status-field defects** (`5a-2-wondercraft-client-payload-shape-defect`) — **RESOLVED 2026-04-27** in operator session via direct production-code patch. `scripts/api_clients/wondercraft_client.py::create_scripted_podcast()` now accepts `script_segments: list[dict] | str` with correct `{"script": [{"text", "voice_id"}]}` payload (legacy string form supported with `voice_id` required + wrapped as single segment). `get_job_status()` now polls `/podcast/{job_id}` (corrected from `/jobs/{job_id}`). `wait_for_job()` now uses `finished` boolean + `error` boolean + `error_details` (corrected from non-existent `status` enum). Caller `app/specialists/wanda/wondercraft_dispatch.py::_as_audio_summary_produce` updated to require `voice_id` + use preferred segments form. `tests/test_wondercraft_client.py` rewritten with 9 tests asserting corrected contract — all GREEN. M2 ceremony 2026-04-27 in operator session | Two A16 (Composition-vs-Component Audit Gap) instances surfaced when M2 ceremony first ran against real Wondercraft API: (1) `WondercraftClient.create_scripted_podcast()` sent `script` as plain string + top-level `voiceId` field, but Wondercraft API requires `script` as an array of `{text, voice_id}` objects per `docs.wondercraft.ai/api-reference/endpoint/user_scripted` — broken since author; never caught because integration tests skip when key absent; (2) `WondercraftClient.wait_for_job()` polled `/jobs/{job_id}` but Wondercraft's actual status endpoint is `/podcast/{job_id}`, AND the response shape uses `finished` boolean + `url` field instead of `status` enum. Both defects FIXED in production code 2026-04-27. | (Resolved.) |
| **7a-1-deferred-directive-composer-manifest-node** | CLOSED-by-7a.2 2026-04-29 | Story 7a.1 close — manifest registration of the `directive-composer` orchestration node was deferred because lockstep set-equality required HUD/pack tolerance for orchestration-only nodes. | **CLOSED 2026-04-29 by Story 7a.2 AC-7.2-G** — orchestration-only-node lockstep tolerance landed in `scripts/utilities/check_pipeline_manifest_lockstep.py`; actual `directive-composer` node entry registered in `state/config/pipeline-manifest.yaml`. |
| ~~**slab-7a-trial-2-bs-2-readiness-confirmation-deferred-to-operator-trial-2-ceremony**~~ | 2026-04-29 (filed); 2026-05-04 (CLOSED) | ~~Slab 7a Story 7a.8 close via Path 1 (close 7a.8 done now; defer BS-2 ceremony). AC-7.8-I BS-2 readiness predicate (A-1..A-7) requires operator-witnessed Gate-2 ceremony (trial-2 OR trial-2 dry-run) per DUAL-GATE convention.~~ | **CLOSED 2026-05-04** at Trial-2 structured-stop ceremony. Slab 7a substrate readiness CONFIRMED end-to-end: `app.marcus.cli trial start --preset production --input <corpus-path>` launched cleanly, composed directive YAML, wrote run-state to disk, dispatched to Texas. Every step in the Slab 7a substrate chain is reachable with real corpus input. Evidence: 5 directive-YAML run dirs preserved under `state/config/runs/`; canonical Trial-2 evidence at run-id `db276994-edf4-47a2-83bc-771cc214c3c1`. Post-mortem: `_bmad-output/implementation-artifacts/trial-2-postmortem-2026-05-04.md`. |
| ~~**slab-7c-live-harness-evidence**~~ | 2026-05-01 (filed); 2026-05-07 (CLOSED) | ~~Slab 7b 7b.12 Gate-2 ceremony — AC-G.1 `run_cache_hit_harness.py` and AC-I `run_5_api_smoke.py` emitted `verdict: not_run` JSON evidence by design; live-dispatch authoring was deferred to Slab 7c / Trial-2 ceremony.~~ | **CLOSED 2026-05-07 via 7c.21a.** Live-dispatch authoring now lives in `scripts/utilities/run_cache_hit_harness.py` and `scripts/utilities/run_5_api_smoke.py`; default fail-closed `not_run` remains for CI, while operator-gated `--live-runs N` / `--live` paths invoke post-Slab-7c substrate clients when credentials are loaded. Verdict file: `_codex-handoff/7c-21a.ready-for-review.md`. Commit reference: pending 7c.21a review/close commit (working base `d2ba1c8`). |
| ~~**slab-7b-trial-2-ac-o-ac-p-readiness-confirmation-deferred-to-operator-trial-2-ceremony**~~ | 2026-05-01 (filed); 2026-05-04 (CLOSED-WITH-EVIDENCE) | ~~Slab 7b 7b.12 Gate-2 ceremony — AC-O MVP Exit Gate (G2 reached cleanly with real content from ≥9-of-11 specialists; ≥3 per class; SG-1+SG-2+SG-3 green) and AC-P Slab Close Gate (G3 cascade-reading 11 specialists; Wanda + Tracy via Pass-2 cascade audit logs) require operator-driven Trial-2 (or Trial-2 dry-run) execution.~~ | **CLOSED-WITH-EVIDENCE 2026-05-04** at Trial-2 structured-stop ceremony. AC-O / AC-P cannot complete with today's substrate because Slab 7c §02A (LLM-driven directive composition) has not landed; Texas correctly fail-loud-refused on the broken corpus-scan directive at attempt 8 (run-id `db276994-edf4-47a2-83bc-771cc214c3c1`). Three findings filed at `_bmad-output/implementation-artifacts/trial-2-postmortem-2026-05-04.md`. Trial-2 will re-run as a Slab 7c close gate; this entry RESOLVES into new follow-on `trial-2-finding-2-directive-composer-corpus-scan-fallback`. Slab 7b body activation is structurally validated end-to-end (Texas Story 7b.1 hardening engaged correctly on real content). |
| **trial-2-finding-1-g0-print-cp1252-crash** | 2026-05-04 | Trial-2 structured-stop ceremony attempt 1 (run-id `d44128e9-...`) — `app/marcus/cli/trial.py:123` default `print_fn = (lambda msg: print(msg))` crashes Windows console cp1252 stdout on U+202F NARROW NO-BREAK SPACE. Trigger: macOS-generated screenshot filenames using NNBSP between time and AM/PM (e.g. `differential diagnosis Screenshot 2026-02-10 at 5.38.36 PM.png`). Filenames read as UTF-8 correctly; YAML serialization preserves U+202F; `print()` to cp1252 console fails. Workaround validated: `PYTHONIOENCODING=utf-8` resolves at attempts 5 onward. Anti-pattern A11 (Windows-portability), already cataloged. Marcus MEMORY 2026-04-17 records the same class of bug in Texas wrangler `--help` output. | **CLOSED-BY Story 7c.2.** `_confirm_or_edit_directive` now defaults to a UTF-8 byte writer, directive text IO is pinned by structural coverage, and the cp1252 fixture-comparison utility plus no-`PYTHONIOENCODING` G0 print regression test retire the workaround as a structural dependency. |
| **trial-2-finding-2-directive-composer-corpus-scan-fallback** | 2026-05-04 | Trial-2 structured-stop ceremony attempts 1, 5, 6, 7, 8 (5 successive runs producing byte-identical corpus-scan directive output) — `app/marcus/orchestrator/directive_composer.compose_directive` produces a naive enumeration of every file in the corpus directory as `provider: local_file` with `description: 'Auto-derived from corpus dir: <filename>'` and `expected_min_words: 200`. `.gitkeep` promoted to `src-001 role: primary`. The actual primary lesson content (`APC C1-M1 Tejal 2026-03-29.docx`) demoted to `src-004 role: supporting`. Binary images (PNG/JPG/PPTX/PDF) all assigned `expected_min_words: 200`. Determinism across 5 runs confirms this is fallback corpus-scan logic, NOT LLM-driven composition. Story 7a.3 pre-gate-marcus shared LLM node infrastructure IS structurally landed (lockstep + tests pass; orchestration-only-node tolerance verified) — what's missing is the prompt + LLM call inside the node that converts a corpus-dir scan into a semantically-aware directive. Texas Story 7b.1 fail-loud guardrail engaged correctly (exit 30) when handed this broken directive — substrate honors the contract end-to-end. | **CLOSED-BY Story 7c.3a 2026-05-05.** §02A LLM-driven composer body landed at app/composers/section_02a/composer.py with locked D1-D11 contract; Trial-2 forensic regression test asserts non-byte-identicality with broken fixture + invariants on .gitkeep IGNORED + .docx PRIMARY + binary expected_min_words=None; TW-7c-2 not fired; cross-agent MANDATORY review PASS. Original recommendation was Author LLM-driven directive composition that: (a) reads corpus dir contents (existing); (b) constructs an LLM prompt that asks for semantic role assignment per file (primary/supporting/exemplar/out-of-scope); (c) discriminates by file format (skip `.gitkeep`; treat PNG/JPG/PPTX/PDF as visual references with no `expected_min_words`; identify the primary `.docx`/`.md`/`.pdf` by content sampling); (d) produces operator-reviewable directive YAML at G0; (e) supports the [e]dit branch for operator override. Already in Slab 7c scope per `next-session-start-here.md` line 32 ("§02A operator-directives poll surface"); this evidence elevates it from "scoped" to "first PRD priority within Slab 7c". Reactivates at Slab 7c PRD authoring. |

---

## Trial-4 2026-06-19 findings (added 2026-06-19; full-pipeline PASS, run `d7ad4dac`)

**Status:** Filed at Trial-4 postmortem close (`docs/trials/trial-4/postmortem.md`) per methodology §7 Q2 routing. Trial-4 PASSED (G0 → completion) after 2 substrate fixes (CLOSED this session) + 1 LLM re-roll. These three are the carry-forward follow-ons. **The binding-picker entry is the operator-named "big leap" for the next trial series toward BETA.**

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`trial-4-binding-variant-voice-picker`** 🟠 (the "big leap") | 2026-06-19 | **T4-F3 + T4-F6.** The woken G2B (variant) + G4A (voice) HIL gates are **accept/review checkpoints, NOT binding pick-from-N selectors.** On the card: `variant_candidates: []` / `voice_candidates: []`, `selected_variant_id`/`selected_voice_id` write-only, an `edit` verdict does NOT re-route downstream. This run was single-dispatch (one variant/slide). G4A DID carry real options in the specialist output (`enrique.voice_preview.voices` ×3 with `sample_audio_url`) — they were just never projected onto `card.voice_candidates`. Operator's stated mental model: review the published Storyboard-A HTML, pick which of N variants per slide goes downstream; audition voices, pick one — and have the pick take effect. The substrate wired the pause+review+approve/reject skeleton but deferred enumerate-and-select. | **Resolution scope: Epic/large-story — the headline of the next trial series.** Three coupled pieces: (a) **produce real N candidates** — Gary double-dispatch (≥2 distinct variant renderings per slide) surfaced into `G2BCard.variant_candidates`; enrique `voice_preview.voices` projected into `G4ACard.voice_candidates`; (b) **re-route on selection** — a downstream node reads `selected_variant_id`/`selected_voice_id` and binds that choice per slide/segment (the `edit` verdict's `edit_payload` becomes load-bearing); (c) **operator surface** — per-slide picker in the Storyboard-A HTML (couples to `marcus-interactive-experience-not-delivered-by-slab-7c`). Integration ratchet test FIRST per P5 (green pick→reroute→downstream boundary before building). Likely co-sequenced with the Marcus-interactive Epic. |
| **`trial-4-specialist-summary-artifact-list-reporting-gap`** 🟡 | 2026-06-19 | **T4-F1.** Specialist-summaries report `## Emitted artifacts: none` even when the on-disk bundle/output manifest lists real artifacts (Texas G1: 5-artifact `manifest.json` vs "none"; recurs for quinn-r/vera/enrique). Compounded by a **double Texas dispatch** (two identical summaries ~1.9s apart → `dispatch_exit_code: 10` + `duplicate_output_digest`). Net effect: the **G1 drafted_proposal recommended `reject`/halt-for-repair** (a false-negative) on confidence signals derived from the thin summary, over genuinely-present content — operator had to investigate to approve. | **Resolution scope: small story.** (a) Wire each specialist-summary's "Emitted artifacts" list to the producer's bundle/output manifest (single source of truth); (b) make the G1 drafted_proposal read the manifest, not just the summary, before recommending reject; (c) diagnose/de-dupe the Texas exit-10 double dispatch (understand whether the retry is intended and why `duplicate_output_digest` fires). NOT trial-blocking (operator/Claude can verify and approve). |
| **`trial-4-modemismatch-recoverable-family`** 🟡 | 2026-06-19 | **T4-F4 deferred hardening.** `ModeMismatchError(ValueError)` in `app/specialists/quinn_r/_act.py` is OUTSIDE the `SpecialistDispatchError` family, so a quinn-r mode-miss **crashes the trial** (uncaught) rather than error-pausing recoverably. Surfaced when node 07B dispatched with empty gate_id (the primary 07B gate_id bug was FIXED `1b629f3`; this is the residual robustness gap — a future mode-miss should not be a hard crash). | **Resolution scope: tiny (~5-15 LOC).** Re-base `ModeMismatchError` onto `SpecialistDispatchError` (RuntimeError-derived; preserves handlers) with a `tag` (e.g. `quinn_r.mode.unresolved`) so a mode-miss error-pauses + is `trial recover`-able. Add a catch-site test. Watch for test ripple (the class is a `ValueError` today; check existing `except ValueError`/`pytest.raises(ValueError)` callers). Sibling to the live-path error-taxonomy re-base tranches (`taxonomy-rebase-live-path`, `tagged-error-taxonomy-tranche-3-offpath-sweep`). |

**Closed this session (fixes landed + pushed; recorded for audit):** `trial-4-gamma-export-brief-unmatched` → CLOSED `10befac` (Gamma title-pinning + cardSplit; spec `spec-gamma-title-pinning-card-split.md`). `trial-4-node-07b-quinn-r-gate-id-crash` → CLOSED `1b629f3` (`_effective_quinn_r_gate_code`; spec `spec-quinn-r-07b-variant-gate-id.md`). Both cross-referenced in `docs/trials/cross-trial-learnings.md §Trial-4` with Q1 anti-pattern candidates flagged for Mary harvest-gate. **`trial-4-irene-pass2-perception-source-variance`** = watch-only (cleared by one re-roll; not filed as an active follow-on unless it recurs).

---

## BETA planning follow-ons (added 2026-06-19; from the BETA-scoping party consensus D8)

**Status:** Filed at BETA spec authoring (`_bmad-output/planning-artifacts/beta-spec-2026-06-19.md`) per Mary's D8 traceability asks. The BETA trial-sequence charter (`beta-trial-sequence-charter-2026-06-19.md`) is the execution authority; these capture the explicitly-deferred / newly-owned items so they don't become silent prerequisites.

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| **`beta-d-research-review-trial-path-attach`** 🟡 | 2026-06-19 | Capability (d) research-review had **no substrate owner** at BETA-scope time (Mary: the single biggest traceability hole). Epic-28 Tracy detective lane is built + module-tested but integration-orphaned (`tracy-gap-fill-lane-not-adopted-by-production-runner`). | **Owner = BETA charter T6(d).** Wire the Irene→Tracy plan-lock fanout onto the trial path (production_runner attach point at plan-lock) + Marcus review-only approval surface; `suggested-resources.yaml` surfaced, operator acknowledges (no auto-re-route for BETA). Non-blocking for the twice-gate. Integration-boundary ratchet test FIRST per P5. |
| **`beta-generality-across-corpora`** 🟡 | 2026-06-19 | "Error-free twice" is scoped to the FROZEN corpus (`tejal-apc-c1-m1-p2-trends`) to prove engine repeatability, NOT generalization (Mary/Murat: cross-corpus is a separate breadth claim that must not creep into the BETA gate). | **Post-BETA.** After the twice-green BETA, run the pipeline across ≥2 additional corpora + a 2nd instructional use case (concept-explainer / assessment) as generalization evidence. One cross-corpus run is recorded during BETA as non-gating breadth evidence. |
| **`beta-genuine-variant-distinctness`** 🟠 | 2026-06-19 | BETA ships variant-picker *mechanics* with only a distinctness-FLOOR (vary a non-title axis). Genuine distinctness (a real variation axis — segmentation/clustering/pacing divergence + title-pin relaxation) is the larger, manifest-likely half Amelia warned not to bundle into the twice-gate. | **Own governed arc, post-BETA-mechanics.** Define + parameterize the variation axis (segmentation is the hardest — two ways of chunking content into slides); relax/variant-scope title-pinning; each new axis costs a manifest/pack touch → pipeline-lockstep regime + party-mode. Couple per-cluster narration cadence to reading-path-v2 where natural. |
| **`beta-motion-synthesis-data-plane`** 🟡 | 2026-06-19 | BETA scopes motion to PLAN-review only (honest `motion_plan` node); synthesis (kling render) is out. Motion leg is structurally unproven (kira input-starved, `motion_receipts: []`). | **Post-BETA dp-v2-class arc.** Honest motion producer → manifest motion-plan node → kira synthesis → compositor consumption; close the four-layer silence (no producer / silent default / groundless G2F / tolerant compositor) from the cycle-5/6 motion-receipts investigation. |
| **`beta-marcus-namespace-collision-rename`** 🟢 | 2026-06-19 | Three things named "Marcus" coexist: Marcus-Persona (conversation), `app.marcus` (runtime package), `pre_gate_marcus` (gate-proposal node). BETA documents the distinction but does NOT rename (rename = large blast radius). | **Hygiene, post-BETA.** Architecture decision on canonical names (Marcus-Persona / Marcus-Runtime / Marcus-PreGate, or rename `app/marcus/`); large blast radius; coordinate with the SPOC architecture. |

## BETA execution follow-ons (added 2026-06-19 PM; from the BETA run/repair/rerun arc)

| Entry | Filed | Trigger context | Resolution shape |
|---|---|---|---|
| ~~**`beta-voice-select-wpm-qa-interaction`**~~ 🟢 **RESOLVED 2026-06-19 (P1)** — `spec-p1-voice-agnostic-wpm-floor.md`; party-mode §2 green-light + T11 3-lane PASS. G5 WPM check replaced with a voice-agnostic intelligibility band [110,200] (Murat §5 ruling); Sarah's 128 WPM now passes; logged break-glass `wpm_breach_override` retained. Lesson harvested → `cross-trial-learnings.md §"gate asserting the wrong invariant"`. | 2026-06-19 PM | A NON-default voice `select` (T5a-val-F5) trips G5 `quinn_r.g5.wpm-threshold` — measured WPM 128 vs voice-agnostic target 150 (band 130-170). Sarah's deliberate pace drops below the floor; the default (Roger) lands in-band so approve-path runs pass. Deterministic (recover won't help) → a non-default-voice run can't complete error-free without a decision. G5 is correctly enforcing its contract; the target is just voice-agnostic. | **Party-mode QA-semantics decision (do NOT unilaterally weaken QA):** (a) re-target WPM to the selected voice's expected pace, (b) make G5 operator-overridable post-select (the operator owns the pace via their voice pick — precedent: the existing `wpm_durations_estimated` advisory escape), or (c) widen tolerance for voice variance. Then a non-default-voice run completes error-free. Smallest unblock for full capability-e-to-completion. |
| ~~**`beta-irene-pass2-perception-source-autoretry-or-harden`**~~ 🟢 RESOLVED | 2026-06-19 PM | irene pass-2 `slide-join-failed` perception_source variance failed first-roll across Trial-4 + both T5a runs (rerun needed 3 re-rolls) — the #1 error-free-twice blocker. | **RESOLVED at `e9d20be` (S0.4 flake-budget).** `_invoke_specialist_with_retry` + `_RETRYABLE_DISPATCH_TAGS={irene.pass2.slide-join-failed}` + `_MAX_DISPATCH_RETRIES=3` absorb the variance IN-DISPATCH (no operator-manual recover). Validated: 4 error-free runs since (engine ×2 + SPOC ×2) needed zero manual irene recovers. If recurrence exceeds the budget, escalate to pass-2 prompt/schema hardening. |
| **`beta-spoc-narration-depth-c-d`** 🟡 | 2026-06-19 PM | The Marcus SPOC MVP narrates (c) lesson-plan + (d) research thinly ("staged"/"none"); the conversational frame + data are in place but the surfacing is shallow. | **Fast-follow story.** Surface real plan_units (objective/scope/rationale per unit) at the lesson-plan narration; surface Tracy suggested-resources when (d) is wired (`beta-d-research-review-trial-path-attach`). Optional: free-form NL turn-taking / LLM-mediated dialogue (deferred per operator directive "defer sophisticated ML"). |
| **`beta-narration-hallucinates-nonexistent-illustrations`** 🔴 (TOP PRIORITY next session) — **ROOT CAUSE CONFIRMED** | 2026-06-19 PM | **Operator-flagged content-fidelity defect; CONFIRMED by evidence.** Narration speaks to the CORRECT slide (binding/title-pin fixes worked) but **describes illustrations that DON'T EXIST on the rendered slide.** **Concrete evidence** (`_bmad-output/implementation-artifacts/evidence/narration-hallucinates-illustration-slide01-2026-06-19.png`, Storyboard-B card slide-01 "The Economic & Structural Reality"): RENDERED slide shows stat callouts **$4.5T / 74% / 3x + a building photo**; NARRATION says *"the upward **line** toward **$5.2 trillion** … the paired **bars** contrasting a shrinking Independent Practice column with a growing Hospital Employment column"* — a **dual-axis line+bar chart that was never rendered**, and the **wrong figure ($5.2T vs $4.5T)**. The narrated visual matches the ORIGINAL brief verbatim (`extracted.md` slide-1: "Dual-Axis Data Visualization … upward trending line graph … $5.2 Trillion … two comparing bar charts, Independent Practice vs Hospital Employment"). **Proof that Pass-2 grounds on the brief, not the render.** | **Fix: ground Pass-2 narration in the PERCEIVED rendered slide, not the brief.** Pass-2 currently builds `slide_roster` from gary's brief `visual_description`/prompt (the *requested* visuals); when Gamma renders different stat-callouts/photo (which it does — it owns layout + restyles figures), the narration describes the absent briefed illustration. The perception substrate exists (Vera `PerceptionArtifact.visual_elements`, `traceable_visual_references`) but Pass-2 doesn't consume it. Add a vision pass over Gary's exported PNGs (between §07 export and §08 Pass-2) → `PerceptionArtifact` of what's ACTUALLY on each slide → Pass-2 narrates from perceived visual_elements (+ perceived numbers like $4.5T), not the brief. Couples to `reading-path-enforcement-machinery-not-absorbed-at-severance`. Start: `app/specialists/irene/graph.py` (`_assemble_pass_2_prompt`, `_slide_roster`), `irene/authoring/pass_2_template.py`, Vera perception artifacts. Secondary win: also fixes narrated-figure drift ($5.2T→$4.5T). **🔴 OPERATOR-ESCALATED 2026-06-19 PM: DISASTER-LEVEL REGRESSION. MERGE with `reading-path-enforcement-machinery-not-absorbed-at-severance` into ONE combined "perception + reading-path narrative-grounding restoration & ENHANCEMENT" arc** — both are the same root (severance dropped the perception layer AND the reading-path/visual-structure classification that together directed narrative focus). Mandate: REPAIR (perception-grounded narration + restored reading-path classification) AND ENHANCE (grow pattern repertoire — triptych / image-dominant-first; couple per-cluster cadence to scan pattern). Likely `bmad-create-prd`/architecture scope, not a single story. This is the #1 next-session arc. |

## Variant Arc Follow-Ons (2026-06-23)

| Item | Source | What | Reactivation / fix |
|---|---|---|---|
| 🔴 **`variant-selection-downstream-consumption-gap`** — **NEXT SESSION (deck-wide fix chosen)** | Operator Q3 investigation 2026-06-23 (`variant-selection-downstream-gap-2026-06-23.md`, `7eeafbc`) | HIGH-severity 2-variant-trial BLOCKER: `selected_variant_id` is set at G2B but consumed NOWHERE downstream; Irene `_slide_roster` (`graph.py:118`) iterates ALL `gary_slide_output` rows unfiltered → 2 variants double the deck (12 roster rows / 6 slides); perception/reading-path/compositor assume 1 PNG/slide_id. Latent — single-variant runs unaffected. | **OPERATOR CHOSE deck-wide (Option 1):** filter `gary_slide_output` to the single `selected_variant_id` AFTER the G2B/G2C pick + BEFORE the vision-perception node (chokepoint); no-op for single-variant. NEW CYCLE: party green-light → Codex T1–T10 → Claude T11; RED-first test pinning the doubling bug. Also change `DEFAULT_VARIANT_PAIR` (`gary/_act.py:25`) per operator. Gates a 2-VARIANT trial only. |
| 🟡 **`variant-gamma-knobs-unwired`** | 2026-06-23 variant-arc T11 | `template` setting is advisory-only (echoed in additionalInstructions, not a real Gamma param — theming is `themeId`); Gamma's `imageOptions.model`/`.source` + `textOptions.audience`/`.language` are NOT wired per-variant (only style/amount/tone/theme). | Wire the missing Gamma knobs if a new default pair / distinctness axis needs the image model/source or audience. Small additive change to `gary/_act.py` `_image_options_for_variant` / `_text_options_for_variant`. |
| 🟡 **`variant-arbitrary-N`** | 2026-06-23 variant-arc (Amelia A1) | Variant production is hard-fixed to A/B (`_normalized_gamma_settings` rejects non-A/B). | Arbitrary-N variants with unique per-variant params — deferred; reactivate if >2 variants/slide is wanted (code change, party-scoped). |
