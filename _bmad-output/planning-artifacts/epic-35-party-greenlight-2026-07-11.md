# Epic 35 (Operator HUD v1) — Party-Mode Green-Light Record (2026-07-11)

**Gate:** epic/story green-light + AD-14 Tier-2 manifest-bump ratification + execution-plan sanction
**Party:** full-spawn subagent mode — Winston (architect), John (PM), Amelia (dev), Murat (TEA) + anti-consensus contrarians Splinter (consensus challenger), Level (claim checker). Operator goal directive 2026-07-11 16:30 binds this gate chain ("fully spawned party incl. contrarian on reviews/approvals/key decisions").
**Votes:** D1 story cut — 4× GO-WITH-AMENDMENTS + Splinter CHALLENGE-SUSTAINED-with-fence (resolved by adoption) + Level 8/9 claims CONFIRMED. D2 Tier-2 bump — 3× GO, 1× GO-WITH-AMENDMENTS, Splinter WITHDRAWN ("governance done properly"), Level CONFIRMED with precision nit. D3 4-hour plan — GO-WITH-AMENDMENTS across seats with Splinter's three fences adopted.
**Consensus: GO on D1, D2, D3 with the adopted amendment set below. Orchestrating agent concurs — per CLAUDE.md party-consensus-equals-approval, dev opens without a redundant human hold. Tier-2 manifest bump is hereby party-ratified per AD-14.**

## Adopted amendments (binding on story specs and dev)

1. **Coverage map corrected** (John): FR1/FR2 → 35.3 (produce) + 35.5 (render); FR17 adds 35.4.
2. **35.7 scoped-verdict schema** (Splinter D1-1 + Murat #3 + John #2): the party verdict MUST enumerate (a) pause classes witnessed live, (b) classes covered by L2 goldens + prior L3 witnesses, (c) per-class live-witness debt with named trigger. PASS language = "HUD performed to spec **on the witnessed surface**". Gate class mandatory live; error/batch induced if cheaply feasible else enumerated as debt. Brief SC5 (keep-it-open) is L4, explicitly NOT decidable at 35.7 — left open in the epic DoD.
3. **35.7 scoping reworded** (Murat #1 + Level #8 refutation): no 3-slide directive knob exists (`num_cards` is gate-tripped; slide count is lesson-plan-emergent; smallest precedent = 9 slides). AC becomes: "small run — `component_selection` deck+motion+workbook all true; slide count minimized via corpus/LO curation at the planning gates; emergent count accepted and recorded." The operator's "3 slides" is treated as smallness intent, not a literal knob.
4. **Sequencing pins** (Winston #3/#4, Amelia A1, Splinter D3-2): 35.2 → 35.3 STRICTLY SERIAL (same `production_runner.py` region); 35.8 STRICTLY AFTER 35.5 (`run_hud.py` collision); 35.3 ∥ 35.4 permitted with `app/hud/server.py` owned by 35.4 ONLY (35.3 passes identity via launch args; its L3 witness runs after 35.4 merges); 35.6 ∥ 35.5 permitted (disjoint trees). Any two agents touching the runner or the manifest are serialized by rule.
5. **35.0 precision** (Winston #1/#2, Level nit): the v4.3 bump enumerates exact paths — `app/models/runtime/operator_surface.py`, `app/models/schemas/operator-surface.v1.schema.json`, `app/marcus/orchestrator/operator_surface_assembler.py`, `app/hud/**`, `app/notify/**`, `tests/unit/models/test_operator_surface_shape_pin.py`, `tests/contracts/test_operator_surface_parity.py`, `tests/hud/**`, `tests/notify/**`, `app/models/runtime/production_trial_envelope.py` (AD-5 reverse tripwire), **and `app/marcus/orchestrator/production_runner.py` (explicit INCLUDE decision — it hosts the AD-2 emit seam; omission would be drift)**. The AC names which manifest field(s) bump (pack_version v4.2→v4.3 and/or schema_version per regime mechanics) with `check_pipeline_manifest_lockstep.py` exit-0 as arbiter; choice documented in story evidence.
6. **Formal ceremony preserved** (Splinter D3-1): each story dev runs formal `bmad-dev-story` discipline against a real story spec file; `docs/dev-guide/pipeline-manifest-regime.md` read at T1 is a per-story AC checkbox; readiness-check + sprint-planning execute before first dev dispatch; `bmad-code-review` before each story closes. No bmad-dev-auto on anything but §5.5's mechanical items.
7. **DoD-over-clock** (Splinter D3-3.1 + Amelia A5): per-story witness sets are non-compressible. If the 4-hour wall expires, stop at the last DoD-complete story and re-gate the remainder — never thin 35.7's evidence or party review. Honest estimate on record: serial ≈9h, critical path ≈5.5–6.5h; the E2E run's wall-clock may fall outside the window (operator goal's OR-clause governs).
8. **35.2 emit-never-raises** (Splinter D3-3.2): ANY exception inside `assembler.emit()` is caught, logged, never propagates into either walk (fault-injection test). `run.json` write semantics unchanged in passing (Amelia trap #1).
9. **E2E abort/continuity criterion** (Splinter D3-3.3): HUD/projection defect mid-run = logged 35.7 finding, run continues on the conversational SPOC surface; abort ONLY if the emission path corrupts run.json or blocks a walk; first-run-stands — no retry-to-green of the paid run.
10. **Honest trims — depth, never realness** (Amelia A2/A3/A4, Murat #4/#5, John #3): 35.5 v1 golden matrix = 7 statuses + 4 panel states (no-active-run, unrecognized, refuse-to-render, stale-veil), zone-scoped innerHTML replacement satisfies AD-12 v1, remaining panel-state fixtures fast-follow; 35.3 v1 live heartbeats = OpenAI + Gamma (+ LangSmith env presence), remaining platforms accrete under the AD-13 falter-surface regression rule; 35.6 L3 push witness via accountless ntfy with Pushover as config-swap once operator provisions token; replace-under-open-reader downgraded to deterministic smoke (one held reader + bounded-retry path + fault-injected exhaustion branch), concurrency hammer → deferred-inventory; DOM-preservation tested as render unit + 35.7 live witness checklist item. T+2h de-scope ladder pre-authorized: FR7 depth (model/cost attribution) → tile histories → trace-well richness; NEVER cut FR4/5/6/15/16/17/18.
11. **Narrowed-component-selection L2 fixture owner = 35.5** (Murat #2).
12. **35.3 spec reconciles start topology before dev** (Winston #5): the pinned AD-7 sequence must be mapped onto `trial.py::start_trial` + `run_production_trial` reality, including pre-envelope exits `cancelled-at-g0`/`saved-only` (outside the seven-status contract) — v1 decision: those exits leave the projection at `registered` with a terminal trace event; page shows the registered state honestly.
13. **Amelia's three trap designs adopted as story guidance**: stateless construct-on-demand assembler (read-merge-write keyed by trial_id+runs_root, no singleton); freshness tick try/finally context-managed in BOTH walks under the assembler lock; Windows child mechanics (`subprocess.Popen` + env + healthz poll; atexit+terminate for session-child; `DETACHED_PROCESS`/`CREATE_NEW_PROCESS_GROUP` budgeted into 35.6 for the survives-session claim).
14. **Murat's 10-item 35.7 party-review checklist adopted verbatim** as the objective PASS instrument (recorded in the 35.7 story spec).

## Claim-check record (Level)

4-failure baseline re-confirmed live (4 failed / 98 passed; empty-run failure visibly renders run 22b27500 — pollution class confirmed); lockstep checker exit 0 with PASS trace; manifest v4.2 + HUD trigger rows confirmed; `_persist_envelope`:221 + `run.json` bypass :3501 exact; seven-status literal :19–27 exact; both pin exemplars green (22 passed); fastapi/uvicorn/requests/watchdog present, apprise genuinely absent; env-only push creds hold. One refutation (claim 8) resolved by amendment 3.

## Readiness check (satisfied at this gate)

Inputs complete and mutually consistent: brief (final, operator-amended) + UX spine pair (final, operator-approved 2026-07-11) + architecture spine (final, 4-lens reviewed, 19 ADs) + this epic (amended). Six independent seats verified artifact-to-code fidelity at this gate (Level: 8/9 claims confirmed against live repo; Winston/Amelia/Murat spot-checks). Story specs (next artifact) embed the amendments; sprint-status.yaml updated at dispatch. This record constitutes the `bmad-check-implementation-readiness` disposition for Epic 35 under the party-consensus-equals-approval rule.

## Post-gate addendum — 35.0 close (2026-07-11, code-review S1 fold)

Story 35.0 landed the Tier-2 trigger-path extension **without a pack_version/schema_version bump** — the regime's recorded determination 2026-06-12 forbids flipping pack_version on a render-identical edit (L1 filter would drop all 47 nodes), and both on-tree precedents (Braid S4, composition-catalog S2) landed additive rows the same way. The party-ratified Tier-2 GOVERNANCE happened at this gate; the marker is the manifest's ratification comment. Amendment 5's "v4.3" phrasing and Story 35.8's "v4.3 envelope" are hereby read as "the 35.0 party-ratified trigger-path envelope". Code review APPROVE (no MUSTs); independent re-verification of checker exit-0, 100/2/0 pytest, and AD-14 hook classification on all 11 rows.

---

## KEY DECISION 2 — contract-enrichment (2026-07-11 ~20:30, focused party, contrarians included)

**Trigger:** story 35.5 render surfaced the shipped 35.1 operator-surface contract is thinner than the UX spine's §Projection Demands — no `decision_card` (gate_focus/operator_prompt/drafted_proposal/pick_context), no verbatim `error_message`, no `deliverables` section. FR4 + FR16 are on the never-cut list (amendment 10). Routed to party as a KEY DECISION per the operator goal directive.

**Seats + votes:** John (PM) A-widen-all · Winston (architect) A-widen-all · Splinter (contrarian) HYBRID (widen decision-card+error now, deliverables fast-follow, + new parity pin) · Level (contrarian claim-checker) CONFIRMED all facts (gap real; data exists but verb-conditional/synthesized; additive-safe no version bump; assembler already reads the cards at L316; 35.5 render helper pre-built).

**BINDING SYNTHESIS (4/4, Splinter dissent absorbed):**
1. Widen the 35.1 contract with three OPTIONAL sections (additive within v1 per AD-4 — NO schema_version bump; byte-pin + parity fixtures regenerate in lockstep): `decision_card` (gate_focus, operator_prompt, drafted_proposal{decision,confidence,rationale}, pick_context, evidence — ALL optional/verb-conditional: G1 has drafted_proposal+confidence but no operator_prompt; G4A/G2B have gate_focus/operator_prompt/pick_context but no drafted_proposal), `error_message` (verbatim `error-pause.json` message + node_index + tag), `deliverables` (component_selection booleans + total_cost_usd + cheaply-resolvable export paths = MINIMAL now).
2. Populate in the 35.2 assembler at the pause choke-point (decision_card+error, reusing the existing `run_dir/decision-card-{gate}.json` read at L316) and the completion emit (deliverables), all inside the existing log-and-swallow guard (amendment 8) — a missing/garbage artifact never sinks emit.
3. Extend the 35.5 render to read `projection.decision_card/.error_message/.deliverables`; DELETE the consumer-side `specialists[].last_artifact` deliverables synthesis (restores the AD-1 projection-is-sole-input spine — Winston's decisive point). 35.5's pre-built `_artifacts_block` helper renders pick_context/evidence.
4. NEW FENCE — §Projection-Demands parity pin (Splinter): a third contract gate mapping each of the ~10 EXPERIENCE.md §Projection Demands bullets to a contract section/field; FAILS RED if any bullet is unmapped or waived without a dated fast-follow ticket. Root-cause fix: the dual-pin only ever proved producer==consumer + status==envelope, never checked the contract against the UX spine — which is how the drift went unnoticed until render.
5. Registered dated waiver (the ONE allowed under the parity pin): rich per-artifact deliverables path enumeration (all exports/motion/specialist-summaries paths) → fast-follow ticket, carried visibly by 35.7's scoped verdict.

**Packaging:** discrete story **35.9 — operator-surface contract enrichment + Projection-Demands parity pin** (full dev + code-review; touches block_mode trigger paths operator_surface.py + assembler + render). Sequence: after 35.5 closes → 35.9 → 35.8 → 35.7. Keeps "~4 remaining" (35.3 closed; 35.9 takes its slot). 35.5 closes as honest-thin-v1 with its deliverables-from-last_artifact explicitly labeled a 1-story-lifetime stopgap superseded by 35.9 (visible, ticketed — not silently shipped).
