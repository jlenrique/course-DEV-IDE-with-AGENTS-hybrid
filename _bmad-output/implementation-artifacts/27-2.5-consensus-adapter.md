# Story 27.2.5: Consensus Adapter (RetrievalAdapter subclass; first real cross-validation partner to scite)

**Status:** ready-for-dev
**Created:** 2026-04-17 (ratified-stub per operator directive post-27-0 party consensus); **re-expanded against Shape 3-Disciplined 27-0 contract + 27-2 precedent 2026-04-23** via `bmad-create-story`.
**Epic:** 27 — Texas Intake Surface Expansion
**Sprint key:** `27-2.5-consensus-adapter`
**Sprint:** Sprint #1 (of revised 6-story composition — lead story after 27-2 done-drift remediation)
**Branch:** `dev/epic-27-texas-intake` (continuation branch; 27-2 landed on same branch)
**Points:** 3 (smaller than 27-2's 5 pts — infrastructure cascade landed with 27-2; this is pure-adapter + cross-val scope)
**Depends on:** [27-0 Retrieval Foundation](./27-0-retrieval-foundation.md) (DONE 2026-04-18) + [27-2 scite.ai Provider](./27-2-scite-ai-provider.md) (DONE 2026-04-18 — provides the cross-validation counterparty + proves the adapter pattern).
**Blocks:** [28-1 Tracy pilot charter](./28-1-tracy-pilot-scite-ai.md) (soft-block — Tracy benefits from both adapters live for first pilot dispatch, but 28-1 may reshape into a charter spec in parallel).
**Soft-enables:** Cross-validation as a first-class production feature — scite + Consensus fan-out + DOI-convergence merge.

## TL;DR

- **What:** Author `ConsensusProvider(RetrievalAdapter)` at `skills/bmad-agent-texas/scripts/retrieval/consensus_provider.py` — the second real consumer of 27-0's ABC. Mirrors SciteProvider's seven-method shape against the Consensus.app MCP (`https://api.consensus.app/mcp`, Bearer auth via `CONSENSUS_API_KEY`). Auto-registers via `PROVIDER_INFO` ClassVar, superseding the `consensus: ratified` placeholder in `provider_directory.py` (line 166) so `list_providers()` flips `consensus` to `status="ready"`.
- **What's DIFFERENT from 27-2 (scope reductions):**
  1. **Dispatcher-wiring cascade is DONE.** 27-2 shipped AC-B.6 (`_classify_directive_shape` + dispatcher routing), AC-C.4 + AC-C.4' (schema-doc layering + operator-facing subsections), AC-C.9 (Recipe-6 dev-guide sharded file), AC-C.11 (writer `code_path` discriminant), AC-T.5 (run_wrangler dispatcher-wiring tests), AC-T.6 (legacy DOCX golden-file parity), AC-T.7 (parametrized schema-compliance v1.0/v1.1). **27-2.5 does NOT redo any of these.** Consensus slots into the existing infrastructure.
  2. **ABC-contract scaffold is DONE.** 27-2 refactored `tests/contracts/test_retrieval_adapter_base.py` into `ADAPTER_FACTORIES` parametrization. 27-2.5 appends a `_make_consensus_adapter` factory entry — no scaffold work.
  3. **Dev-guide worked example is DONE.** 27-2's `docs/dev-guide/how-to-add-a-retrieval-provider.md` uses SciteProvider as the worked example. 27-2.5 does NOT rewrite the how-to; instead adds a ~15-line "Cross-validation in practice" subsection documenting the scite+Consensus pattern.
- **What's NEW in 27-2.5 (novelty scope):**
  1. **First real `cross_validate: true` exercise** (AC-B.8). 27-0's cross-validation dispatcher code path existed but was only tested with `FakeProvider` fan-out. 27-2.5 delivers the first real two-provider fan-out with genuine DOI-convergence merge + `ConvergenceSignal` annotation.
  2. **Cross-validation integration tests** (AC-T.12 — NEW). Exercises scite + Consensus under same `RetrievalIntent` with `cross_validate: true`, asserts merged results carry `convergence_signal` per row, asserts identity-key merge collapses duplicate DOIs across providers.
  3. **CI 3x-run flake-detection gate** (PDG MUST-HAVE — BINDING). Promoted from "logged" (27-2 Murat MH-2) to hard Pre-Dev gate. Reason: cross-validation's combinatorial surface (N providers × M refinement paths × ordering-sensitive consensus) is the exact shape that produces intermittent flake; starting 27-2.5 without the gate means the first flake lands in 27-2.5 code under new load, unable to distinguish 27-2.5 bugs from latent 27-2 non-determinism.
- **Why:** Tracy's (Epic 28) editorial dispatch wants "use one service's findings to confirm or supplement another's" (operator directive 2026-04-17). scite surfaces citation-classification signals; Consensus surfaces research-synthesis signals. Cross-validating both by DOI makes authority claims traceable AND corroborated — the substrate for evidence-bolster (Sprint #1 story further downstream).
- **Size:** **3 pts** (vs 27-2's 5). Cascade and scaffold work is done; this is provider-only + cross-val-specific tests + directory placeholder supersession + retrieval-contract.md Consensus-specific subsection.
- **Key decisions pre-committed:** HTTP Bearer auth via `CONSENSUS_API_KEY` (not HTTP Basic); identity_key = DOI (fallback `consensus_paper_id` → `source_id`); synthetic JSON fixtures via `responses` library matching 27-2's pattern; no live_api / no xfail / no trial_critical (PDG path B synthetic-only, mirroring 27-2 default); task order **T1 skeleton → T2-T6 guts → T7-T10 tests** (simpler than 27-2's wiring-first order because wiring is done — T8 equivalent is absent); test-count floor **≥14 collecting pinned** (see AC-T.13); follow-on `27-2.5-live-cassette-refresh` story ticket committed at green-light mirroring the 27-2 pattern.

## Pre-Development Gate (DRAFT — resolved at green-light round 1)

Two sub-gates — one resolved by precedent (PDG-1/2 path B mirror of 27-2), one BINDING MUST-HAVE (flake gate) the story cannot open without.

| Gate | Default resolution | Status | Rationale |
|------|---------------------|--------|-----------|
| **PDG-1: Consensus auth credentials** | **Path B — synthetic-fixture-only.** | Default proposed; green-light confirms. | No dependency on `.env` `CONSENSUS_API_KEY` acquisition at dev-story start; `responses` library + hand-crafted JSON fixtures give full coverage. Live verification deferred to follow-on `27-2.5-live-cassette-refresh` story (mirrors 27-2's follow-on pattern). Consensus IS authenticated in Cursor via MCP (operator directive 2026-04-17), but Python-side `MCPClient` auth remains synthetic at test time. Risk-weighted same as 27-2: live creds give ~10% fidelity improvement vs ~40% CI-bleed risk. |
| **PDG-2: Consensus MCP tool catalog** | **Path B — inferred from consensus.app docs + public MCP docs.** | Default proposed; consequence of PDG-1=B. | Fixture source documented as "inferred from consensus.app docs v2026-04-23" in `tests/fixtures/retrieval/consensus/README.md`. First live run is the moment-of-truth; diff → follow-on hardening. |
| **PDG-3: CI 3x-run flake-detection gate** | **BINDING MUST-HAVE — wired BEFORE dev-story begins.** | ⚠️ **BLOCKER — cannot start 27-2.5 dev without this.** | Promoted from 27-2 Murat MH-2 "logged" status to explicit hard gate. Rationale: cross-validation's combinatorial surface (scite + Consensus fan-out × refinement iteration × DOI-merge ordering) is precisely the shape where flake surfaces. Without the gate, the first intermittent failure will land against 27-2.5 code under new load — indistinguishable from latent 27-2 non-determinism vs genuine 27-2.5 bug. **Gate is NOT optional**; party-mode round 1 confirmed the promotion (operator signed off via sprint-status.yaml binding entry). |

**Follow-on story tickets committed at green-light** (mirrors 27-2's Murat MUST-FIX #4 discipline):

- `27-2.5-live-cassette-refresh` — opens when/if operator acquires `CONSENSUS_API_KEY` in local `.env`; records live cassettes into `tests/fixtures/retrieval/consensus-live/`; schema-canary test diffs against synthetic fixtures.
- `27-2.5-refinement-hardening` — opens only if first live cross-val dispatch shows `drop_filters_in_order` inadequate for Consensus's filter landscape.

**PDG-3 WIRING REQUIREMENT (the blocker):**

A lightweight CI job that runs `pytest -k "cross_validate or retrieval_dispatcher" -p no:cacheprovider` three times in sequence and fails if any one run diverges from the other two. Implementation: add a GitHub Actions matrix or local pre-commit wrapper that exits non-zero on divergent outcomes. Target: **zero flake detected across 3 consecutive runs on the same commit** before 27-2.5 dev-story begins. If flake detected, the cause (test, fixture, dispatcher, or scite adapter) must be identified and fixed BEFORE 27-2.5 adds more combinatorial load.

**Dev-story cleared to start** iff: PDG-1 = path B confirmed AND PDG-2 = path B confirmed AND PDG-3 = flake gate wired + zero-flake verified on 3x run.

## Anti-pattern Guardrails (inherited from 27-0 + 27-2, carried forward)

The same carve-outs apply. Stated for Amelia at 27-2.5 dev time:

- ✅ **Permitted** (pure adapter + cross-val test work):
  - New file `retrieval/consensus_provider.py` mirroring `scite_provider.py` shape.
  - New JSON fixtures under `tests/fixtures/retrieval/consensus/`.
  - New unit tests in `tests/test_retrieval_consensus_provider.py`.
  - Appending `_make_consensus_adapter` to `ADAPTER_FACTORIES` in `tests/contracts/test_retrieval_adapter_base.py` (single-line addition — no scaffold refactor).
  - New cross-validation integration test in `tests/test_retrieval_dispatcher.py` (scite + Consensus fan-out).
  - Touching `retrieval-contract.md` "For Tracy" with a Consensus-specific subsection (mirrors scite's pattern per AC-C.4(b) layering).
  - Touching `extraction-report-schema.md` `## Provider Metadata Sub-objects` H2 to add `provider_metadata.consensus` schema (single-source-of-truth pattern).
  - Touching `provider_directory.py` comment only (supersession is auto via `__init_subclass__`).
  - Touching `retrieval/__init__.py` with eager Consensus import (belt-and-suspenders, mirrors 27-2's `scite_provider` eager import).

- ❌ **Prohibited** (would be scope creep):
  - Touching `run_wrangler.py` (dispatcher wiring is done).
  - Touching `write_extraction_report` signature (discriminant contract is frozen from 27-2 AC-C.11).
  - Re-authoring the `how-to-add-a-retrieval-provider.md` worked example (scite stays as the example; add a 15-line "Cross-validation in practice" subsection only).
  - Changing schema v1.1 shape (additive fields are frozen; provider_metadata.consensus is an additive sub-object only).
  - Refactoring `scite_provider.py` (no cross-contamination; each adapter is independent).
  - Changing dispatcher's cross-validation path (`dispatcher.py` public surface stable from 27-0; 27-2.5 CONSUMES the path, does not modify it).

## Story

As the **Texas wrangler**,
I want **a `ConsensusProvider` `RetrievalAdapter` subclass that translates natural-language intents into Consensus.app MCP queries, executes them via the 27-0 `MCPClient` with Bearer auth, applies mechanical + provider-scored filters, normalizes results to canonical `TexasRow` with Consensus's research-synthesis metadata under `provider_metadata.consensus`, AND the first real `cross_validate: true` exercise that fans out scite + Consensus + merges results by DOI with `convergence_signal` annotation**,
So that **Tracy's editorial dispatch (Epic 28) — emitted as `RetrievalIntent` with `provider_hints: [{provider: "scite"}, {provider: "consensus"}]` + `cross_validate: true` — returns merged rows with structural agreement signal, making authority claims both classified (scite) AND corroborated (Consensus), ready as the substrate for the evidence-bolster control (Sprint #1 downstream story)**.

## Background — Why This Story Exists

Operator directive 2026-04-17: *"Consensus is already available and authorized in Cursor. Once we get scite.ai working, we'll duplicate that with Consensus so we can extend Texas's toolset and use one service's findings to confirm or supplement another's."*

Round-3 party consensus (2026-04-17) ratified this as the first real cross-validation story. 27-0's dispatcher delivered the multi-provider fan-out + merge-by-identity-key + `ConvergenceSignal` annotation code path, but it shipped with `FakeProvider` x `FakeProvider` tests only. 27-2 delivered the first real retrieval-shape adapter (scite) but explicitly deferred cross-validation to 27-2.5 per its §Non-goals: *"Cross-validation with Consensus — 27-2.5 scope (scite + Consensus fan-out is 27-2.5's `cross_validate: true` test)."* 27-2's §Dev Notes: *"Cross-validation is 27-2.5's job, not 27-2's. SciteProvider provides `identity_key` (DOI-primary) to unblock cross-validation; 27-2.5 Consensus will be the first real `cross_validate: true` exercise with scite + Consensus fan-out."*

27-2.5 closes that loop: second adapter, first genuine two-provider merge, `convergence_signal` surfaced end-to-end.

**Consensus.app differentiating value over scite**: Consensus returns a *research-synthesis* signal (`consensus_score` indicating agreement strength across studies) and may surface study-design tags (`study_design_tag: "meta-analysis" | "rct" | "cohort" | ...`), sample sizes, and evidence-strength annotations. This complements scite's citation-classification approach. Tracy's authority-plus-recency-plus-relevance scoring (Epic 28) will leverage both signal families.

## Acceptance Criteria

### Behavioral (AC-B.*)

1. **AC-B.1 — `ConsensusProvider(RetrievalAdapter)` subclass ships.** New module `skills/bmad-agent-texas/scripts/retrieval/consensus_provider.py`. Class `ConsensusProvider` subclasses `RetrievalAdapter`; declares `PROVIDER_INFO: ClassVar[ProviderInfo]` with `id="consensus"`, `shape="retrieval"`, `status="ready"`, `auth_env_vars=["CONSENSUS_API_KEY"]`, `spec_ref` pointing at this file, `capabilities` extending the placeholder's list (adds `"evidence-synthesis"` + `"study-design-tagging"` if Consensus surfaces these). On module import, `__init_subclass__` auto-registers via `provider_directory.register_adapter`, superseding the `consensus: ratified` placeholder; `list_providers()` now shows `consensus: ready`.

2. **AC-B.2 — Seven abstract methods implemented per 27-0 ABC contract.**
   - `formulate_query(intent: RetrievalIntent) -> dict` — deterministic translation of `intent.intent` + `provider_hints[consensus].params` into a Consensus query dict (`{mode: "search" | "paper", query: str | doi: str, filters: {...}}`). Byte-identical across invocations with same input (AC-T.2 determinism self-test, mirrors scite pattern).
   - `execute(query: dict) -> list[dict]` — dispatches to `MCPClient.call_tool("consensus", tool_name, args)` with Bearer auth resolved lazily from `CONSENSUS_API_KEY`. Raises `MCPAuthError` / `MCPRateLimitError` / `MCPFetchError` / `MCPProtocolError` unchanged (dispatcher wraps). No retry logic inside adapter.
   - `apply_mechanical(results, criteria) -> filtered` — honored keys: `date_range` ([start, end] YYYY-MM-DD; row.date within), `min_results` (passthrough; dispatcher post-evaluates count), `exclude_ids` (DOI or `consensus_paper_id`), `license_allow` (if Consensus surfaces license flag).
   - `apply_provider_scored(results, criteria) -> filtered` — honored keys: `consensus_score_min` (Consensus-native agreement-strength filter), `study_design_allow` (whitelist of study-design tags, e.g., `["meta-analysis", "rct"]`), `sample_size_min` (if Consensus surfaces `sample_size` at the row level).
   - `normalize(results) -> list[TexasRow]` — Consensus paper → canonical `TexasRow`. `title`, `body` (abstract; full text generally not surfaced by Consensus), `authors`, `date` from metadata. `provider_metadata.consensus` sub-object holds Consensus-specific fields (see AC-B.3).
   - `refine(previous_query, previous_results, criteria) -> dict | None` — monotonically-loosening. Uses `refinement_registry.drop_filters_in_order` with Consensus-specific order: `sample_size_min` → `consensus_score_min` → `study_design_allow` → `date_range`. If PDG-3-extended resolves scope, additional strategies deferred to `27-2.5-refinement-hardening` follow-on.
   - `identity_key(row: TexasRow) -> str` — **DOI-primary** (critical for cross-validation with scite — AC-B.8). `row.provider_metadata["doi"]` (primary); fallback `row.provider_metadata["consensus_paper_id"]` if DOI missing; fallback `row.source_id` as last resort. Raises `NotImplementedError` ONLY if none available.

3. **AC-B.3 — `declare_honored_criteria` returns explicit key set.** Returns `frozenset({"date_range", "min_results", "exclude_ids", "consensus_score_min", "study_design_allow", "sample_size_min", "license_allow"})`. Unknown keys log to `refinement_log` per 27-0 AC-B.2 strengthened contract.

4. **AC-B.4 — `provider_metadata.consensus` sub-object schema.** Fields populated on every `TexasRow` produced by `normalize`:
   - `doi: str | null` (primary identity key)
   - `consensus_paper_id: str` (Consensus internal ID; always present)
   - `title: str`, `authors: list[str]`, `year: int | null`
   - `venue: str | null`, `abstract: str`
   - `consensus_score: float | null` — agreement strength (0.0–1.0; semantic meaning per Consensus docs)
   - `study_design_tag: str | null` — e.g., `"meta-analysis"`, `"rct"`, `"cohort"`, `"review"`, `"other"`
   - `sample_size: int | null` — aggregate sample size if surfaced
   - `evidence_strength: str | null` — e.g., `"strong"`, `"moderate"`, `"weak"` if surfaced
   - `consensus_url: str` — link to Consensus report for the paper
   - `known_losses: list[str]` — sentinels (e.g., `["abstract_only"]` when full body unavailable, `["study_design_unknown"]` when tag not surfaced)

   **Schema doc-layering obligation (AC-C.4 inherited from 27-2 pattern):** Authoritative schema lives in `extraction-report-schema.md` under the existing `## Provider Metadata Sub-objects` H2 (added in 27-2). Add a new H3 `### provider_metadata.consensus` subsection listing all AC-B.4 fields with types, nullability, ranges, and a worked example. `retrieval-contract.md` "For Tracy" gets a pointer-only subsection (~15 lines) describing Consensus signals at a glance + link to the schema. NO duplicate schema table.

5. **AC-B.5 — Controlled failure on Consensus-specific errors.** MCPClient exceptions flow through unchanged: auth failure (401/403) → `MCPAuthError` → dispatcher records `ProviderResult` with `acceptance_met: False` + `refinement_log` entry `reason="auth_failed"`. Rate-limit (429) → `MCPRateLimitError` → `reason="rate_limited"`. Generic fetch/timeout → `MCPFetchError` → `reason="fetch_failed"`. Malformed JSON-RPC → `MCPProtocolError` → `reason="protocol_error"`. No traceback leaks to stdout (handled by MCPClient). Missing `CONSENSUS_API_KEY` → `MCPAuthError` at first `call_tool` invocation (lazy resolution per MCPClient design, mirrors scite pattern).

6. **AC-B.6 — Bearer auth integration.** ConsensusProvider wires `MCPClient` with `MCPServerConfig(url=<consensus_mcp_url>, auth_env=["CONSENSUS_API_KEY"], auth_style="bearer")` (contrasts with scite's `auth_style="basic"`). Verify via unit test `test_consensus_execute_passes_bearer_header` that `Authorization: Bearer <token>` appears in the outbound HTTP request.

7. **AC-B.7 — Partial graceful degradation on missing Consensus-specific fields.** When Consensus response lacks `study_design_tag`, `sample_size`, or `evidence_strength` (all optional per current Consensus API surface knowledge), `normalize` emits the row with those fields as `null` and adds an appropriate sentinel to `known_losses` (e.g., `"study_design_unknown"`). Does NOT raise; does NOT filter the row out — downstream (Tracy, Vera) decides what to do with under-annotated rows.

8. **AC-B.8 — Cross-validation with scite (FIRST REAL EXERCISE — THE NOVELTY OF THIS STORY).** When `RetrievalIntent` has `cross_validate: true` AND `provider_hints` contains both `scite` and `consensus` entries, the dispatcher (27-0's existing multi-provider code path):
   - Fans out to both providers independently under single-provider rules.
   - Merges results by `identity_key(row)` — DOI-primary for both adapters.
   - Annotates each merged row with `ConvergenceSignal` populated per 27-0 contracts.py schema: `providers_agreeing: list[str]` (provider IDs that returned this identity key), `providers_disagreeing: list[str]` (empty in v1 — only structural disagreement modeled), `single_source_only: bool` (true iff only one provider returned this identity key).
   - Returns `list[ProviderResult]` (one per provider) with merged rows surfaced; `ConvergenceSignal` is structural only — tells you whether both providers returned the same DOI, NOT whether they semantically agree.

   **Spec-level clarification**: `ConvergenceSignal` = "structural agreement by identity key." If scite says "DOI X is supporting citation" and Consensus says "DOI X has low consensus_score," the signal is `providers_agreeing: ["scite", "consensus"]` — the DOI converged, even though the two providers' semantic framings differ. Semantic reconciliation is Tracy's job downstream.

### Test (AC-T.*)

1. **AC-T.1 — `ConsensusProvider` unit tests with atomicity splits.** `tests/test_retrieval_consensus_provider.py` (NEW). 11 atomic tests mirroring 27-2's AC-T.1 pattern — one assertion-cluster per test. Uses `responses.RequestsMock()` **per-test** context manager (anti-pattern #8 guard from 27-0). Consensus MCP responses are JSON fixtures under `tests/fixtures/retrieval/consensus/`.

   **`formulate_query` atoms (2):**
   - `test_consensus_formulate_query_deterministic` — same `RetrievalIntent` → byte-identical query dict across 100 invocations.
   - `test_consensus_formulate_query_search_mode` — topical intent + `{mode: "search"}` hint → search-shaped query dict.

   **`execute` atoms (3):**
   - `test_consensus_execute_calls_correct_tool_name` — assert `MCPClient.call_tool` invoked with exactly `("consensus", "<expected_tool>", ...)`.
   - `test_consensus_execute_passes_bearer_header` — assert outbound HTTP request carries `Authorization: Bearer <token>` (distinct from scite's `Basic` header — AC-B.6 validation).
   - `test_consensus_execute_auth_error_propagates` — `responses` stub returns 401; MCPClient raises `MCPAuthError`; adapter re-raises unchanged (AC-T.10 covers parent contract).

   **`normalize` atoms (4):**
   - `test_consensus_normalize_extracts_doi_to_identity` — synthetic paper → `TexasRow.provider_metadata["doi"]` populated; `ConsensusProvider.identity_key(row)` returns that DOI.
   - `test_consensus_normalize_populates_consensus_score` — fixture has `consensus_score: 0.87` → `TexasRow.provider_metadata["consensus"]["consensus_score"] == 0.87`.
   - `test_consensus_normalize_surfaces_study_design_tag` — fixture has `study_design_tag: "meta-analysis"` → normalized row carries that tag under `provider_metadata.consensus`.
   - `test_consensus_normalize_handles_missing_optional_fields` — fixture lacks `sample_size` and `evidence_strength` → row emits with those fields `null` and `known_losses` contains `"study_design_unknown"` or `"sample_size_unknown"` as applicable. Row is NOT filtered.

   **Identity-key atoms (2):**
   - `test_consensus_identity_key_falls_back_to_consensus_paper_id_when_doi_missing` — preprint fixture (no DOI yet) → identity_key returns `consensus_paper_id`.
   - `test_consensus_identity_key_raises_when_all_three_tiers_empty` — pathological fixture (no DOI, no paper_id, no source_id) → raises `NotImplementedError`.

   **Total AC-T.1 atoms: 11.**

2. **AC-T.2 — Parametrize `test_retrieval_adapter_base.py` over ConsensusProvider.** Append `_make_consensus_adapter` factory to `ADAPTER_FACTORIES` in `tests/contracts/test_retrieval_adapter_base.py`. Now parametrized over `[fake]` + `[scite]` + `[consensus]` IDs. No test-body reimplementation — inheritance via fixture factory is the 27-0 mandate. Scope: +15 LOC (one factory function + one tuple entry).

3. **AC-T.3 — Refinement strategy test.** `test_consensus_refine_monotonic_looseness` (in `test_retrieval_consensus_provider.py`): starts with full acceptance criteria (all Consensus-honored keys set), iterates `refine()` N times, asserts each returned query has fewer restrictive keys than the previous, after 4 iterations (for 4 honored keys) `refine()` returns `None`, log entries emitted at every step.

4. **AC-T.4 — Dispatcher integration smoke (single-provider).** `test_retrieval_consensus_dispatch_single_provider` (in `tests/test_retrieval_dispatcher.py`, ADD): mirrors scite pattern. Uses `responses` at MCPClient HTTP layer. Asserts `dispatcher.dispatch(consensus_intent)` returns `ProviderResult` with populated `rows`, `acceptance_met`, `iterations_used`, `refinement_log`.

5. **AC-T.5 — (INHERITED from 27-2 AC-T.5) — confirm legacy path still green.** No new test. 27-2's `tests/test_run_wrangler_retrieval_shape.py` must stay green. Confirmed by CI regression; no 27-2.5-specific addition.

6. **AC-T.10 — Module-prefix exception contract test (inherited discipline).** New test in `test_retrieval_consensus_provider.py`: mirrors scite's AC-T.10. Enumerates every raise path via 401/403/429/500/timeout/malformed-JSON stubs; asserts `type(exc).__module__.startswith("retrieval.")` — no `requests.*` / `urllib3.*` leakage. Rationale same as scite: Option-X migration escape hatch must remain open.

7. **AC-T.11 — No-stateful-mock anti-pattern guard (inherited).** New test in `test_retrieval_consensus_provider.py`: mirrors scite's AC-T.11. Asserts forbidden tokens (`MagicMock`, `AsyncMock`, `from unittest.mock import Mock`) never appear in the test file source (assembled at runtime to avoid self-reference trap — scite precedent).

8. **AC-T.12 — Cross-validation integration tests (THE NOVEL GROUP).** `tests/test_retrieval_cross_validation.py` (NEW — distinct file, not squeezed into dispatcher tests). Five atoms:
   - `test_cross_validate_fans_out_to_scite_and_consensus` — `RetrievalIntent` with `cross_validate: true` + both `scite` and `consensus` in `provider_hints` → dispatcher invokes both adapters' `execute`. Assert via `responses` that HTTP calls went to both `api.scite.ai/mcp` and `api.consensus.app/mcp`.
   - `test_cross_validate_merges_rows_by_doi` — fixtures: scite returns DOI-A and DOI-B; Consensus returns DOI-A and DOI-C. Merged result: 3 unique identity keys (DOI-A, DOI-B, DOI-C). DOI-A appears with `convergence_signal.providers_agreeing == ["scite", "consensus"]` (order may be sorted); DOI-B with `providers_agreeing == ["scite"]` + `single_source_only == true`; DOI-C with `providers_agreeing == ["consensus"]` + `single_source_only == true`.
   - `test_cross_validate_convergence_signal_is_structural_not_semantic` — scite returns DOI-A with `supporting_count: 50, contradicting_count: 2`; Consensus returns DOI-A with `consensus_score: 0.3, study_design_tag: "weak"`. Merged row carries `convergence_signal.providers_agreeing == ["scite", "consensus"]` despite semantic divergence. Test docstring explicitly states: "structural signal only; semantic reconciliation is Tracy's job."
   - `test_cross_validate_preflight_rejects_adapter_without_identity_key` — synthetic adapter that raises `NotImplementedError` on `identity_key()` → dispatcher preflight raises `DispatchError` with message naming the offending adapter (27-0 anti-pattern #10 smoke alarm).
   - `test_cross_validate_returns_list_of_provider_results` — result is `list[ProviderResult]` (length == N providers), not a flattened list. Per 27-0 dispatcher contract — cross-validation is a DISTINCT code path even when `provider_hints` has length 1 (anti-pattern guard from `dispatcher.py` docstring).

   **Total AC-T.12 atoms: 5.**

9. **AC-T.13 — Regression suite-level gate with FLOOR PINNED ≥14 collecting.** Baseline from 27-2 closeout: **1149 passed / 2 skipped / 0 failed / 2 xfailed**. After 27-2.5, floor pinned at **≥14 collecting additions**:

   | Cluster | Tests | AC |
   |---------|-------|-----|
   | ConsensusProvider units (atomicity-split) | 11 | AC-T.1 |
   | ABC-contract parametrized `[consensus]` | ≥5 (via existing parametrized suite — no new tests, just factory addition) | AC-T.2 |
   | Refinement | 1 | AC-T.3 |
   | Dispatcher-consensus integration | 1 | AC-T.4 |
   | Module-prefix exception contract | 1 | AC-T.10 |
   | No-stateful-mock anti-pattern guard | 1 | AC-T.11 |
   | **Cross-validation integration** | **5** | **AC-T.12 (novel)** |
   | **Minimum floor (new additions)** | **≥20** | — |

   Expected after 27-2.5: **≥1169 passed** / 2 skipped / 0 failed / 2 xfailed. No new `@pytest.mark.skip`, `xfail`, `live_api`, `trial_critical`.

   **Note on AC-T.2**: parametrization over `[consensus]` generates ~5-8 new parametrized test IDs via the existing ABC-contract suite; those count toward the collection delta but are "free" (scaffold exists). The 11 + 1 + 1 + 1 + 1 + 5 = 20 floor is the conservative count excluding parametrized increments.

10. **AC-T.14 — CI flake-detection gate regression (PDG-3 closure validation).** New job or local pre-commit wrapper: run `pytest -k "cross_validate or retrieval_dispatcher" -p no:cacheprovider` three times in sequence; fail if outcomes diverge. Zero-flake verified across 3 consecutive runs before story closes. This is PDG-3 closure — the gate must remain wired and green through BMAD closure, not just at dev-story start.

### Contract Pinning (AC-C.*)

1. **AC-C.1 — Adapter lives in `retrieval/` package** (mirrors 27-2 AC-C.1). New file at `skills/bmad-agent-texas/scripts/retrieval/consensus_provider.py`. Alongside `scite_provider.py` and `fake_provider.py`.

2. **AC-C.2 — Uses 27-0 `MCPClient` public surface exclusively** (mirrors 27-2 AC-C.2). ConsensusProvider calls `MCPClient.call_tool("consensus", tool, args)` and `MCPClient.list_tools("consensus")`. Does NOT construct its own `requests.Session`; does NOT import `requests` types or exceptions; does NOT leak transport types at any method boundary. Option-X migration escape hatch preserved.

3. **AC-C.3 — `PROVIDER_INFO` supersedes placeholder** (mirrors 27-2 AC-C.3). Import of `consensus_provider` → `__init_subclass__` auto-registers → `list_providers()` merge-order (registered adapters first) causes live entry to supersede `consensus: ratified` placeholder at [`provider_directory.py:166`](../../skills/bmad-agent-texas/scripts/retrieval/provider_directory.py#L166). Do NOT delete the placeholder — fallback on import failure.

4. **AC-C.4 — Schema v1.1 additive fields + schema-doc layering** (inherits 27-2 AC-C.4 pattern). Schema v1.1 shape frozen; 27-2.5 adds `provider_metadata.consensus` as a new H3 under the existing `## Provider Metadata Sub-objects` H2 in `extraction-report-schema.md`. `retrieval-contract.md` "For Tracy" gets a pointer subsection (~15 lines) with a Consensus-signal summary. NO duplicate schema table. Scope: +25 lines `extraction-report-schema.md`; +15 lines `retrieval-contract.md`.

5. **AC-C.5 — Dispatcher public surface unchanged.** No new arg to `dispatcher.dispatch()`; ConsensusProvider factory-resolves via `provider_directory.get_registered_adapter_class("consensus")`. Cross-validation code path (27-0's existing implementation) is CONSUMED, not modified.

6. **AC-C.6 — No LLM-in-the-loop for query formulation or refinement** (mirrors 27-2 AC-C.7 / 27-0 AC-C.6). Consensus-specific query building is code + data (filter-key ordering, study-design enum).

7. **AC-C.7 — `identity_key` never returns empty string or None** (mirrors 27-2 AC-C.8). Raises `NotImplementedError` when no DOI / consensus_paper_id / source_id available. Cross-validation preflight catches at dispatch time per 27-0 anti-pattern #10.

8. **AC-C.8 — Dev-guide "Cross-validation in practice" subsection** (~15 lines added to `docs/dev-guide/how-to-add-a-retrieval-provider.md`). Paige owns; Amelia countersigns. Content: (a) what cross-validation solves (structural agreement annotation), (b) how providers OPT INTO cross-validation (return a stable `identity_key`), (c) the semantic-vs-structural distinction (Tracy's job, not dispatcher's), (d) worked example: scite + Consensus under the same intent. Living reference for future retrieval-shape provider authors. **Does NOT rewrite the SciteProvider worked example** — adds only the subsection.

9. **AC-C.9 — No new top-level CLI flag.** ConsensusProvider is invoked via the existing directive-load → dispatcher path. No `--consensus` / `--use-consensus` flag. `--list-providers` CLI from 27-0 will show `consensus: ready` automatically after module import.

10. **AC-C.10 — Writer-discriminant contract unchanged** (inherits 27-2 AC-C.11). 27-2.5 does NOT touch `write_extraction_report` signature or the `code_path` discriminant. Cross-validation results flow through the existing retrieval-code path writer (schema_version `"1.1"`). Verified by CI regression — no new AC-C.10 test needed.

## File Impact

| File | Change | Lines (est.) |
|------|--------|-------|
| [`skills/bmad-agent-texas/scripts/retrieval/consensus_provider.py`](../../skills/bmad-agent-texas/scripts/retrieval/consensus_provider.py) | **New** — `ConsensusProvider(RetrievalAdapter)` + `PROVIDER_INFO` + Bearer-auth wiring + `_build_query_search` / `_build_query_paper` helpers + study-design-tag normalization helpers | +220 |
| [`skills/bmad-agent-texas/scripts/retrieval/__init__.py`](../../skills/bmad-agent-texas/scripts/retrieval/__init__.py) | **Touch** — eager `from .consensus_provider import ConsensusProvider` (belt-and-suspenders, mirrors 27-2 `scite_provider` eager import) | +1 |
| [`skills/bmad-agent-texas/scripts/retrieval/provider_directory.py`](../../skills/bmad-agent-texas/scripts/retrieval/provider_directory.py) | **Touch** — comment update on consensus placeholder noting live supersession | +3 |
| [`skills/bmad-agent-texas/scripts/retrieval/refinement_registry.py`](../../skills/bmad-agent-texas/scripts/retrieval/refinement_registry.py) | **Touch** — consensus-specific key-order list registration (conditional on PDG-3-extended path B) | +10 (conditional) |
| [`skills/bmad-agent-texas/references/retrieval-contract.md`](../../skills/bmad-agent-texas/references/retrieval-contract.md) | **Touch** — Consensus-specific pointer subsection under "For Tracy"; "Cross-validation in practice" brief mention | +25 |
| [`skills/bmad-agent-texas/references/extraction-report-schema.md`](../../skills/bmad-agent-texas/references/extraction-report-schema.md) | **Touch** — new `### provider_metadata.consensus` H3 under existing `## Provider Metadata Sub-objects` H2 | +25 |
| [`docs/dev-guide/how-to-add-a-retrieval-provider.md`](../../docs/dev-guide/how-to-add-a-retrieval-provider.md) | **Touch** — new "Cross-validation in practice" subsection; SciteProvider worked example unchanged | +15 |
| [`tests/test_retrieval_consensus_provider.py`](../../tests/test_retrieval_consensus_provider.py) | **New** — AC-T.1 (11 atomic unit) + AC-T.3 (1 refine) + AC-T.10 (1 module-prefix) + AC-T.11 (1 no-stateful-mock) | +380 |
| [`tests/contracts/test_retrieval_adapter_base.py`](../../tests/contracts/test_retrieval_adapter_base.py) | **Touch** — append `_make_consensus_adapter` factory entry to `ADAPTER_FACTORIES`; no test-body changes | +15 |
| [`tests/test_retrieval_dispatcher.py`](../../tests/test_retrieval_dispatcher.py) | **Touch** — 1 consensus-via-MCPClient single-provider integration test (AC-T.4) | +40 |
| [`tests/test_retrieval_cross_validation.py`](../../tests/test_retrieval_cross_validation.py) | **New** — 5 cross-validation integration atoms (AC-T.12, novel surface) | +250 |
| [`tests/fixtures/retrieval/consensus/`](../../tests/fixtures/retrieval/consensus/) | **New** — 7 JSON fixtures (search_happy, paper_metadata_happy, search_with_study_design, search_missing_optional_fields, empty_search, auth_failure_401, rate_limit_429) + README.md provenance note | ~8 files, +250 lines JSON + ~30 lines MD |
| `.github/workflows/flake-detection.yml` OR pre-commit hook | **New** (PDG-3 wiring) — 3x run gate on `pytest -k "cross_validate or retrieval_dispatcher"`; fails on divergent outcomes | +30 |

**No changes to:** `retrieval/base.py` (ABC stable), `retrieval/contracts.py` (schema v1.1 frozen), `retrieval/dispatcher.py` (public surface stable; cross-val code path CONSUMED not modified), `retrieval/mcp_client.py` (Option Y surface stable), `retrieval/fake_provider.py` (reference adapter stays), `retrieval/normalize.py` (canonical helpers stable), `retrieval/scite_provider.py` (NO cross-contamination with 27-2 code), `run_wrangler.py` (dispatcher wiring done in 27-2), `tests/contracts/test_acceptance_criteria_schema_stable.py` (schema-pin passes unchanged), `tests/test_retrieval_mcp_client.py` (MCP client tests stable), `tests/test_run_wrangler_retrieval_shape.py` (dispatcher-wiring tests from 27-2 stay green), `tests/test_run_wrangler_legacy_docx_parity.py` (regression from 27-2 stays green), `tests/contracts/test_extraction_report_schema_compliance.py` (parametrized over v1.0/v1.1 from 27-2; schema did not bump), `transform-registry.md` (locator-shape only), `.env.example` (blocked by repo policy; discovery via `--list-providers`), `pyproject.toml` / `requirements.txt` (`responses` already in dev deps from 27-0).

## Tasks / Subtasks

**Task order:** T1 skeleton → T2 directory supersession smoke → T3-T6 guts → T7-T10 tests → T11 docs → T12 regression + review. Simpler than 27-2's wiring-first order because dispatcher wiring is DONE; this is a pure-adapter + cross-val-test story.

- [ ] **T-Pre — Pre-Development Gate resolved at green-light round 1.** PDG-1 = path B synthetic-only (proposed; operator may override); PDG-2 = path B inferred (consequence); PDG-3 = **BINDING — CI 3x-run flake-detection gate wired + zero-flake verified on 3x run of `pytest -k "cross_validate or retrieval_dispatcher"` BEFORE this task list opens**. Follow-on tickets committed: `27-2.5-live-cassette-refresh` + `27-2.5-refinement-hardening`.
- [ ] **T1 — ConsensusProvider skeleton + PROVIDER_INFO** (AC-B.1, AC-C.1, AC-C.3)
  - [ ] Create `consensus_provider.py`; subclass `RetrievalAdapter`.
  - [ ] Declare `PROVIDER_INFO: ClassVar[ProviderInfo]` with `id="consensus"`, `status="ready"`, auth env vars, capabilities, spec_ref.
  - [ ] Stub-implement all 7 abstract methods with trivial bodies (empty-list returns; `identity_key` raises `NotImplementedError` for now).
  - [ ] Eager import in `retrieval/__init__.py` (belt-and-suspenders).
  - [ ] Smoke test: import module; verify `list_providers(shape="retrieval")` now shows `consensus: ready`.
- [ ] **T2 — Directory supersession smoke + comment update** (AC-C.3)
  - [ ] Update `provider_directory.py` comment on consensus placeholder (~3 lines noting live supersession).
  - [ ] Run `run_wrangler.py --list-providers --shape retrieval --status ready --json` manually; confirm `consensus` entry appears.
- [ ] **T3 — Bearer auth + MCPServerConfig wiring** (AC-B.6)
  - [ ] Lazy `MCPClient` instantiation inside `execute` with `MCPServerConfig(url=<consensus_mcp_url>, auth_env=["CONSENSUS_API_KEY"], auth_style="bearer")`.
  - [ ] Consensus MCP URL constant (`CONSENSUS_MCP_URL = "https://api.consensus.app/mcp"` at module scope, resolved once; NOT via `os.environ.get` at import time — inherit 27-2's DEFER lesson about env capture).
- [ ] **T4 — formulate_query + query-mode helpers** (AC-B.2, AC-T.1)
  - [ ] `formulate_query` inspects `intent.kind` + `provider_hints[consensus].params["mode"]` for search / paper-metadata mode.
  - [ ] Helper `_build_query_search(intent, params)` → dict per Consensus MCP tool signature (inferred via PDG-2 path B; provenance in fixture README).
  - [ ] Helper `_build_query_paper(intent, params)` → dict for DOI-direct fetch.
  - [ ] Determinism guard (no dict ordering reliance, no `random`, no `datetime.now()`).
- [ ] **T5 — apply_mechanical + apply_provider_scored + declare_honored_criteria** (AC-B.2, AC-B.3)
  - [ ] Mechanical: `date_range`, `exclude_ids`, `license_allow`, `min_results` passthrough.
  - [ ] Provider-scored: `consensus_score_min`, `study_design_allow` (whitelist), `sample_size_min`.
  - [ ] `declare_honored_criteria` returns explicit frozenset.
- [ ] **T6 — normalize + identity_key + graceful degradation** (AC-B.2, AC-B.4, AC-B.7)
  - [ ] Consensus paper → `TexasRow` with `provider_metadata.consensus` sub-object populated per AC-B.4.
  - [ ] `identity_key` 3-tier fallback (DOI → consensus_paper_id → source_id → `NotImplementedError`).
  - [ ] Missing-optional-field graceful degradation: `study_design_tag` / `sample_size` / `evidence_strength` null → row emitted with `known_losses` sentinel. Row NOT filtered.
- [ ] **T7 — refine + refinement registry** (AC-B.2)
  - [ ] Call `refinement_registry.get_strategy("drop_filters_in_order")` with consensus key-order: `["sample_size_min", "consensus_score_min", "study_design_allow", "date_range"]`.
  - [ ] Return `None` when exhausted.
  - [ ] If PDG-3-extended surfaces scope: conditional custom strategies (deferred to follow-on by default).
- [ ] **T8 — Synthetic fixtures + unit tests** (AC-T.1, AC-T.3, AC-T.10, AC-T.11)
  - [ ] Author 7 JSON fixtures under `tests/fixtures/retrieval/consensus/` + README.md provenance.
  - [ ] Implement 14 atomic unit tests (11 AC-T.1 + 1 AC-T.3 + 1 AC-T.10 + 1 AC-T.11) in `test_retrieval_consensus_provider.py` using `responses.RequestsMock()` per-test.
- [ ] **T9 — Parametrize ABC-contract tests over ConsensusProvider** (AC-T.2)
  - [ ] Append `_make_consensus_adapter` factory to `ADAPTER_FACTORIES` in `tests/contracts/test_retrieval_adapter_base.py`.
  - [ ] Run suite; verify `[consensus]` parametrized IDs show in pytest output alongside `[fake]` + `[scite]`.
- [ ] **T10 — Cross-validation integration tests (NOVEL SURFACE)** (AC-T.12)
  - [ ] Create `tests/test_retrieval_cross_validation.py`.
  - [ ] Implement 5 cross-val atoms per AC-T.12 spec (fan-out, merge-by-DOI, structural-not-semantic, preflight, list-of-results).
  - [ ] Verify dispatcher code path from 27-0 is CONSUMED, not modified (no edits to `dispatcher.py` during this task).
- [ ] **T11 — Single-provider dispatcher smoke** (AC-T.4)
  - [ ] Add 1 consensus-dispatch test to `tests/test_retrieval_dispatcher.py`.
- [ ] **T12 — Retrieval-contract.md + extraction-report-schema.md updates** (AC-B.4, AC-C.4)
  - [ ] Add `### provider_metadata.consensus` H3 under existing `## Provider Metadata Sub-objects` H2 in `extraction-report-schema.md`.
  - [ ] Add Consensus-pointer subsection (~15 lines) under "For Tracy" in `retrieval-contract.md` — signal summary + schema link.
- [ ] **T13 — Dev-guide "Cross-validation in practice" subsection** (AC-C.8)
  - [ ] Add ~15-line subsection to `docs/dev-guide/how-to-add-a-retrieval-provider.md` explaining cross-validation opt-in pattern.
  - [ ] Paige drafts; Amelia countersigns.
- [ ] **T14 — CI flake-detection gate closure verification** (AC-T.14, PDG-3 closure)
  - [ ] Run `pytest -k "cross_validate or retrieval_dispatcher"` three times in sequence on the final commit.
  - [ ] Verify zero divergence across the 3 runs.
  - [ ] Document the verification in Dev Agent Record.
- [ ] **T15 — Regression + pre-commit + lockstep verification**
  - [ ] Full pytest green — expected **≥1169 passed** (baseline 1149 + ≥20 collecting).
  - [ ] Pre-commit hooks green (ruff + orphan + co-commit).
  - [ ] `pytest -m trial_critical` unchanged (consensus not trial-critical).
  - [ ] `test_provider_directory_autoregister.py` covers ConsensusProvider — verify green.
- [ ] **T16 — Party-mode implementation review + bmad-code-review + closure**
  - [ ] Party-mode implementation review (Winston + Amelia + Murat + Paige).
  - [ ] `bmad-code-review` layered pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor); MUST-FIX remediated; SHOULD-FIX triaged.
  - [ ] Flip sprint-status 27-2.5 → `done` with closure comment.
  - [ ] Epic 27 roster update (epic file + bmm-workflow-status.yaml).
  - [ ] 28-1 Tracy pilot unblock confirmation (hard-dep on 27-2 + 27-2.5 now satisfied — Tracy has both adapters live).

## Dev Notes

### Architecture guardrails (inherited from 27-0 + 27-2)

- **Consensus is a `RetrievalAdapter` subclass, NOT a standalone client.** Lives in `retrieval/` alongside `scite_provider.py` and `fake_provider.py`. `MCPClient` supplies HTTP; ConsensusProvider supplies provider-DSL translation.
- **`MCPClient` public surface library-agnostic.** No `requests.Response` in ConsensusProvider signatures. No library-specific exception re-exports. Option-X escape hatch preserved.
- **Bearer auth, not Basic** (sole auth divergence from scite). `MCPServerConfig(auth_style="bearer", auth_env=["CONSENSUS_API_KEY"])`.
- **Deterministic query formulation only.** Consensus-specific code is lookup-tables + string-munging. No LLM-in-the-loop.
- **Refinement is monotonically-loosening.** Consensus key-order `["sample_size_min", "consensus_score_min", "study_design_allow", "date_range"]`. Exhaustion returns `None`.
- **Cross-validation is this story's NOVELTY, but the dispatcher code path is CONSUMED, not modified.** 27-0's multi-provider fan-out + merge-by-identity-key + `ConvergenceSignal` annotation existed; 27-2.5 provides the second provider so the path is exercised with real data for the first time.
- **`convergence_signal` is STRUCTURAL.** Providers agreeing on DOI ≠ providers agreeing on semantic framing. Tracy (Epic 28) handles semantic reconciliation. Documented in AC-B.8 spec-level clarification + AC-T.12 atom 3 test docstring.
- **Identity-key DOI-primary for cross-validation to work with scite.** Both adapters return DOI as primary identity; merge works. If either falls back to provider-specific ID (preprint case), the row appears in only one provider's result (single_source_only → true). That's the designed behavior, not a bug.
- **Consensus-specific fields live under `provider_metadata.consensus` opaque sub-object.** Do NOT surface `consensus_score` / `study_design_tag` / `sample_size` / `evidence_strength` at the top-level `TexasRow` schema. Per-provider fields do not pollute the canonical schema (same discipline as scite's `provider_metadata.scite`).

### Source tree (new + touched)

```
skills/bmad-agent-texas/
├── scripts/
│   ├── retrieval/
│   │   ├── consensus_provider.py                  [NEW] +220  ConsensusProvider(RetrievalAdapter) + helpers
│   │   ├── __init__.py                            [TOUCH] +1   eager Consensus import
│   │   ├── provider_directory.py                  [TOUCH] +3   comment update on consensus placeholder
│   │   └── refinement_registry.py                 [TOUCH] +10  (conditional) consensus key-order
│   └── run_wrangler.py                            [NO CHANGE] dispatcher-wiring done in 27-2
└── references/
    ├── retrieval-contract.md                      [TOUCH] +25  Consensus "For Tracy" pointer + cross-val mention
    └── extraction-report-schema.md                [TOUCH] +25  new ### provider_metadata.consensus H3

docs/
└── dev-guide/
    └── how-to-add-a-retrieval-provider.md         [TOUCH] +15  "Cross-validation in practice" subsection

tests/
├── test_retrieval_consensus_provider.py           [NEW] +380  AC-T.1 (11) + AC-T.3 (1) + AC-T.10 (1) + AC-T.11 (1)
├── test_retrieval_dispatcher.py                   [TOUCH] +40 AC-T.4 consensus dispatch integration
├── test_retrieval_cross_validation.py             [NEW] +250  AC-T.12 (5 cross-val integration atoms)
├── contracts/
│   └── test_retrieval_adapter_base.py             [TOUCH] +15 _make_consensus_adapter factory
└── fixtures/retrieval/consensus/                  [NEW] 7 JSON + README.md
    ├── search_happy.json
    ├── paper_metadata_happy.json
    ├── search_with_study_design.json
    ├── search_missing_optional_fields.json
    ├── empty_search.json
    ├── auth_failure_401.json
    ├── rate_limit_429.json
    └── README.md                                  (fixture provenance: "inferred from consensus.app docs v2026-04-23")

.github/workflows/
└── flake-detection.yml                            [NEW] +30  PDG-3 wiring — 3x run gate (or local pre-commit equivalent)
```

### Previous-story intelligence (from 27-2 closeout, 2026-04-18)

27-2 delivered patterns 27-2.5 MUST inherit + lessons 27-2.5 MUST avoid:

**Inherit (from 27-2 success patterns):**

- **Adapter subclass shape** — mirror `scite_provider.py`'s top-to-bottom structure: constants (tier tables, URL), `PROVIDER_INFO` declaration, abstract-method implementations in declared order, helpers below. Keeps review mental model consistent across adapters.
- **`responses.RequestsMock()` per-test** — the anti-pattern #8 module-level-leak discipline. Mirrors 27-2 T9.
- **Atomicity splits in unit tests** — one assertion-cluster per test, per Murat MUST-FIX #2 precedent. 27-2.5 applies this from day 1.
- **Module-level constants over `os.environ.get` at import** — 27-2 DEFER: `SCITE_MCP_URL` was captured at module import via `os.environ.get` and monkeypatching post-import had no effect. Consensus: use plain string constant `CONSENSUS_MCP_URL = "https://api.consensus.app/mcp"` at module scope; resolve env vars only via `MCPServerConfig` lazy path.
- **Eager adapter import in `retrieval/__init__.py`** — removes import-order dependency on `__init_subclass__` registration (Winston W-SF-1 from 27-2).
- **JSON fixture + README provenance** — 7 fixtures under `tests/fixtures/retrieval/consensus/` matching scite's pattern. README documents inferred-vs-live source + refresh cadence.
- **Factory-per-adapter-class pattern in ABC-contract tests** — 27-2 refactored `ADAPTER_FACTORIES`; 27-2.5 appends one entry (15 lines). No test-body reimplementation ever.
- **Co-commit test + impl** — tests land in same commit slice as source. Pre-commit enforces.
- **Ruff + orphan + co-commit hooks green before review** — no exceptions.
- **No `trial_critical` marker** — Consensus dispatch isn't a pre-Prompt-1 trial gate.
- **Review-record template** — match 27-2 / 27-1 / 27-0: MUST-FIX → `[x] [Review][Patch]` with code pointers; SHOULD-FIX → `[x] [Review][Patch]` or `[x] [Review][Defer]`; NITs → `_bmad-output/maps/deferred-work.md`; three-layer Blind Hunter / Edge Case Hunter / Acceptance Auditor breakdown.

**Avoid (from 27-2 bugs + defers):**

- **Refinement key-order skip bug** (27-2 PATCH-1). `drop_filters_in_order` receives `iteration=1` with `key_order` recomputed each call — scite's bug was passing cumulative iteration against shrunk key_order, causing `authority_tier_min` never to drop. 27-2.5 inherits the fix pattern; consensus key-order list is shorter (4 vs 4) so the bug surface is smaller, but the drop-order regression test atom from PATCH-12 mirrored for consensus.
- **Silent-drop on unknown classifications** (27-2 DEFER `_truncate_citation_contexts`). Consensus equivalent: if Consensus surfaces a new `study_design_tag` value not in the known enum, emit a `known_losses: ["study_design_unrecognized"]` sentinel rather than silently passing through. Add test atom: `test_consensus_normalize_unknown_study_design_emits_known_loss`.
- **Year-only vs ISO-date lexicographic comparison** (27-2 DEFER). Consensus `date` field handling must use the same robust parsing scite eventually gets in `27-2-refinement-hardening` (or the base helper). Document as DEFER if the code uses the same pattern; do NOT regress.
- **Empty/whitespace DOI through `strip()`** (27-2 DEFER). In `formulate_query` with mode="paper", explicitly reject empty-after-strip DOI with a clear error message at the start of the function, not at execute time.
- **`int()` crash on non-numeric provider payloads** (27-2 DEFER). When parsing `consensus_score: float` and `sample_size: int` from Consensus response, wrap with defensive coercion helper that returns None (with `known_losses` sentinel) on non-numeric input.

### Testing standards

- **Unit / contract / integration split:**
  - Unit: `tests/test_retrieval_consensus_provider.py` (ConsensusProvider methods + determinism).
  - Contract: parametrization entry in `tests/contracts/test_retrieval_adapter_base.py` (ABC inherited); NO new contract file.
  - Integration: `tests/test_retrieval_dispatcher.py` single-provider; `tests/test_retrieval_cross_validation.py` NEW for multi-provider fan-out.
- **HTTP mocking via `responses` library** — no real network; no live_api; no cassettes against real MCP.
- **Cross-validation tests MUST use `responses` at the transport layer** — NOT mock the dispatcher or the adapters. Fan-out and merge paths must exercise the actual 27-0 dispatcher code.
- **No `live_api`, `trial_critical`, `xfail`, `skip`** on default suite.
- **Fixture hygiene** — plain-text JSON; diff-friendly; not binary.
- **Per `feedback_regression_proof_tests.md`**: no xfail, no skip, classify every failure. If cross-val tests reveal a dispatcher bug, classify and FILE A BUG AGAINST 27-0 DISPATCHER — do NOT mask in 27-2.5 code.
- **PDG-3 flake-detection gate** — 3x-run divergence check wired as CI job. Zero flake across 3 runs verified at dev-start AND at story closeout (AC-T.14 pin).

### References

- **27-0 foundation spec**: [27-0-retrieval-foundation.md](./27-0-retrieval-foundation.md) — contract source of truth; dispatcher cross-validation code path lives here.
- **27-2 scite adapter (precedent template)**: [27-2-scite-ai-provider.md](./27-2-scite-ai-provider.md) — mirror the shape, respect the divergences (Bearer not Basic; cross-val novelty; smaller scope because wiring cascade is done).
- **Retrieval contract (audience-segmented)**: [retrieval-contract.md](../../skills/bmad-agent-texas/references/retrieval-contract.md) — "For dev-agents" section has the subclass template.
- **Schema changelog**: [SCHEMA_CHANGELOG.md](./SCHEMA_CHANGELOG.md) — v1.1 additive fields frozen; 27-2.5 does not bump.
- **Base ABC**: [retrieval/base.py](../../skills/bmad-agent-texas/scripts/retrieval/base.py) — the 7 abstract methods.
- **Contracts**: [retrieval/contracts.py](../../skills/bmad-agent-texas/scripts/retrieval/contracts.py) — Pydantic models (RetrievalIntent, TexasRow, ProviderInfo, ConvergenceSignal, ProviderResult).
- **MCP client**: [retrieval/mcp_client.py](../../skills/bmad-agent-texas/scripts/retrieval/mcp_client.py) — `auth_style="bearer"` code path exists; 27-2 confirmed `auth_style="basic"` path works; 27-2.5 exercises bearer for the first time under real adapter load.
- **SciteProvider (reference impl)**: [retrieval/scite_provider.py](../../skills/bmad-agent-texas/scripts/retrieval/scite_provider.py) — mirror the structure; diverge at auth + provider_metadata shape + cross-val-identity-key only.
- **FakeProvider**: [retrieval/fake_provider.py](../../skills/bmad-agent-texas/scripts/retrieval/fake_provider.py) — still the canonical minimal reference.
- **Provider directory**: [retrieval/provider_directory.py#L166](../../skills/bmad-agent-texas/scripts/retrieval/provider_directory.py#L166) — consensus placeholder entry lives at line 166.
- **Dispatcher cross-validation code path**: [retrieval/dispatcher.py](../../skills/bmad-agent-texas/scripts/retrieval/dispatcher.py) lines 1-60 (docstring + cross-val anti-pattern guards) + multi-provider merge code below.
- **27-0 ABC contract tests (parametrization target)**: [tests/contracts/test_retrieval_adapter_base.py](../../tests/contracts/test_retrieval_adapter_base.py) — `ADAPTER_FACTORIES` list lives here.
- **27-2 deferred items** (inheritance + avoidance): [_bmad-output/maps/deferred-work.md](../maps/deferred-work.md) §27-2 — 19 items; 27-2.5 may batch-absorb a subset when touching adjacent code.
- **Epic 27 roster**: [epic-27-texas-intake-expansion.md](./epic-27-texas-intake-expansion.md) — shape classification + AC-S spine.
- **Tracy pilot (downstream consumer)**: [28-1-tracy-pilot-scite-ai.md](./28-1-tracy-pilot-scite-ai.md) — reshape charter pending; Tracy will benefit from both adapters + cross-val.
- **Consensus.app public docs**: `consensus.app/api-docs` (operator-accessible; LLM agent cannot navigate; operator captures during PDG-2 resolution if needed).
- **Sprint #1 charter (drift-remediated)**: [next-session-start-here.md](../../next-session-start-here.md) §Sprint #1 composition — 27-2.5 is Sprint #1 lead story after drift remediation.

### Non-goals

- **Tracy's agent-side editorial code** — fit-score algorithm, semantic reconciliation of divergent provider signals, editorial_note generation (Epic 28 scope).
- **Live-API cassette recording against real Consensus MCP** — follow-on `27-2.5-live-cassette-refresh` if PDG-1 path A is chosen post-credential-acquisition.
- **Three-provider cross-validation** — scite + Consensus is the first pair; future PubMed/OpenAlex/Semantic Scholar rounds are separate stories.
- **Semantic disagreement modeling in `ConvergenceSignal`** — signal remains structural (providers_agreeing / single_source_only). Semantic agreement/disagreement is Tracy's (Epic 28) downstream concern. Future work may extend the signal schema, but NOT in 27-2.5.
- **Cross-provider retry / fallback orchestration** — 27-0 anti-pattern: cross-provider retry is Marcus re-dispatch, not a Texas feature. 27-2.5 preserves this.
- **Adapter-level adaptive rate-limit backoff** — basic 429-surfacing via `MCPRateLimitError` is in scope; adaptive backoff is future hardening.
- **Locator-shape provider refactoring** — anti-pattern #3 from 27-0 still prohibits.
- **LLM-in-the-loop query expansion or refinement** — deferred to future story with own eval framework.
- **`consensus_score` threshold calibration** — shipping with documented default (user-settable via `acceptance_criteria.provider_scored.consensus_score_min`); calibration studies are Tracy-scope downstream.

## Test Plan

| Test | AC | Level | Mocked? | Blocking at merge? |
|------|----|-------|---------|---------------------|
| AC-T.1 × 11 ConsensusProvider unit atoms (atomicity-split) | B.1, B.2, B.4, B.5, B.6, B.7 | Unit | `responses` (per-test) | Yes |
| AC-T.2 × parametrized via ADAPTER_FACTORIES + factory entry | B.1-B.3 | Contract | N/A | Yes |
| AC-T.3 × 1 refinement test | B.2 (refine) | Unit | N/A | Yes |
| AC-T.4 × 1 consensus-dispatch integration | B.6, C.5 | Integration | `responses` | Yes |
| AC-T.10 × 1 module-prefix exception contract | C.2 | Contract | `responses` | Yes |
| AC-T.11 × 1 no-stateful-mock anti-pattern guard | — | Meta | N/A | Yes |
| **AC-T.12 × 5 cross-validation integration atoms (NOVEL)** | **B.8, C.5** | **Integration** | **`responses`** | **Yes — novelty surface** |
| AC-T.13 suite-level regression floor ≥20 collecting | — | Suite | N/A | Yes (non-collecting) |
| AC-T.14 PDG-3 flake-gate zero-divergence on 3x run | — | CI | N/A | **Yes — closure-critical** |

**Target baseline floor: ≥20 collecting additions** (11 + 1 + 1 + 1 + 1 + 5 = 20; AC-T.2 parametrization adds ~5-8 "free" increments on top). Baseline from 27-2 closeout: 1149 passed / 2 skipped / 0 failed / 2 xfailed. Expected after 27-2.5: **≥1169 passed** / 2 skipped / 0 failed / 2 xfailed. No new `skip`, `xfail`, `live_api`, `trial_critical`.

## Risks

| Risk | Mitigation |
|------|------------|
| **Consensus MCP tool catalog differs from consensus.app docs** (fixture fidelity risk) | PDG-2 path B inferred catalog; fixture README documents provenance. First live run is moment-of-truth; `27-2.5-live-cassette-refresh` follow-on handles diff. Same risk model as scite. |
| **Cross-validation exposes dispatcher bug in 27-0 multi-provider code path** | AC-T.12 atoms 1 + 2 exercise fan-out + merge. Any bug surfaces HERE, before Tracy (Epic 28) depends on cross-val. File a bug against 27-0 dispatcher; do NOT mask in 27-2.5 code (per testing standards). PDG-3 flake-gate reduces false-positive/false-negative risk. |
| **DOI collision between scite and Consensus produces wrong merge** | AC-T.12 atom 2 pins behavior with explicit fixtures: DOI-A in both → merged row with `providers_agreeing: ["scite", "consensus"]`; DOI-B in scite only → `single_source_only: true`. If the test fails on first run, dispatcher merge logic has a bug; classify per feedback_regression_proof_tests. |
| **Consensus returns papers without DOI more often than scite** (preprint-heavy content) | Identity-key fallback chain (DOI → consensus_paper_id → source_id) handles it. Papers that fall back to `consensus_paper_id` appear as `single_source_only: true` in cross-val — correct behavior, not a bug. Documented in AC-T.12 atom docstring. |
| **Bearer auth implementation bug in MCPClient** (not exercised before 27-2.5) | AC-B.6 unit test (`test_consensus_execute_passes_bearer_header`) is the first validation of `auth_style="bearer"` code path under real adapter load. If it fails, MCPClient bug — file against 27-0 mcp_client module. Fix lands as a 27-2.5 T3 task or an urgent 27-0-hotfix if severity warrants. |
| **PDG-3 flake gate fails at dev-start** | Story cannot open. Root-cause the flake (likely in 27-2 non-determinism exposed by new load) BEFORE adding 27-2.5 combinatorial surface. This is the gate's entire purpose. |
| **Consensus-specific field semantics drift** (consensus_score meaning changes; study_design enum expands) | `provider_metadata.consensus` is opaque sub-object; Tracy's semantic pass handles meaning. Schema docs in `extraction-report-schema.md` describe current state + note drift possibility. `known_losses` sentinels (`"study_design_unrecognized"`) catch new enum values gracefully. |
| **Custom refinement strategies for Consensus surface mid-implementation** | Scope cap: if `drop_filters_in_order` inadequate, defer to `27-2.5-refinement-hardening` follow-on. Do NOT expand 27-2.5 mid-story (3 pts holds under default PDG-3-extended = path A). |
| **`.env` missing CONSENSUS_API_KEY at green-light** | PDG-1 default path B keeps dev unblocked. Consensus IS authenticated in Cursor via MCP layer; Python-side tests remain synthetic. Live verification deferred. |

## Dev Agent Record

### Agent Model Used

- **Model**: claude-opus-4-7[1m] (Claude Opus 4.7, 1M context)
- **Session mode**: `bmad-dev-story` autonomous implementation (option A green-light → run-through) — TBD at green-light

### Debug Log References

_(populated during implementation)_

### Completion Notes List

_(populated during implementation)_

### File List

_(populated during implementation)_

## Green-Light Patches Applied (party-mode round 1, 2026-04-22)

Four-panelist roundtable: Winston (Architect) / Amelia (Dev) / Murat (Test) / Paige (Tech Writer). **Unanimous GREEN** with 1 rider applied.

### Verdict

- 🏗️ Winston: **GREEN** — "cleanest story in the roster; pure contract consumption; 557-line spec is the correct weight"
- 💻 Amelia: **GREEN with 1 rider** — T2 ordering clause
- 🧪 Murat: **GREEN** — "template story; atomicity discipline baked in; AC-T.11 no-stateful-mock guard is anti-pattern #8"
- 📚 Paige: **GREEN** — "textbook 27-2 pattern replay; three-docs-surfaces pattern preserved; SSOT clean"

### Riders applied

- **Amelia rider**: T2 (CI 3x-run flake-gate wiring) ordering clause in T-task block — T2 completion = pushed-to-branch + green 3x, not "scaffold exists." Mirrors 27-2 MUST-FIX #3.
- **Murat rider**: AC-T.12 cross-val atoms assert ONE property each ("fan-out-to-both AND merge-by-DOI" is two tests, not one — confirmed already in spec atom breakdown).

### Regression floor pinned

**≥20 collecting additions** (11 split AC-T.1 + 1 refinement + 1 dispatcher smoke + 1 module-prefix + 1 no-stateful-mock + 5 cross-val). Baseline 1149 → expected **≥1169 passed**. Murat's roster-level cumulative floor after all 5 stories: ≥1220.

### Sprint-level canonical naming (roster-wide decision from D6)

Intake-layer field reading this story's cross-val output is named `evidence_bolster_active` in Irene retrieval-intake (sibling story). Tracy's `cross_validate` field on `RetrievalIntent` is unchanged (mechanical/action-oriented).

### Scope confirmation

- **3 pts firm** (scope-reduced vs 27-2's 5 because wiring cascade + ABC-scaffold + dev-guide worked example all DONE in 27-2).
- **Binding PDG-3**: CI 3x-run flake-detection gate wired + zero-flake verified BEFORE dev-story T1 starts. **Non-negotiable.**
- **Flake-gate scope**: binding for 27-2.5 + PR-R per Murat extension at green-light.

### Dev sequence

**Open position 2** (parallel with §7.1 Irene Pass 2 template, which opens position 1). No upstream blockers; trial-#2 critical path.

### Vote record

- 🏗️ Winston: GREEN
- 💻 Amelia: GREEN (after T2 ordering rider applied)
- 🧪 Murat: GREEN
- 📚 Paige: GREEN

**Unanimous GREEN → dev-story cleared to start** once PDG-3 flake gate is wired + verified zero-flake on 3x run.

## BMAD Closure Criteria

- [ ] PDG-1 / PDG-2 resolved (paths recorded in §Green-Light Patches Applied).
- [ ] **PDG-3 wired + zero-flake verified on 3x run of `pytest -k "cross_validate or retrieval_dispatcher"` BEFORE dev-story T1 starts** (binding).
- [ ] All AC-B.1 through AC-B.8 behavioral assertions green in test output.
- [ ] All AC-T.1 through AC-T.12 collecting tests pass; AC-T.13 suite-level regression floor green.
- [ ] AC-T.14 PDG-3 flake-gate zero-divergence verified on final commit 3x run.
- [ ] All AC-C.1 through AC-C.10 contract-pinning checks satisfied.
- [ ] Full `pytest` green (expected ≥1169 passed / 2 skipped / 0 failed / 2 xfailed).
- [ ] Pre-commit hooks pass (ruff + orphan + co-commit).
- [ ] 27-2 regression stays green (scite + dispatcher-wiring + legacy-DOCX-parity + v1.0/v1.1 schema-compliance all unchanged).
- [ ] `list_providers(shape="retrieval", status="ready")` returns both SciteProvider's AND ConsensusProvider's PROVIDER_INFO.
- [ ] `retrieval-contract.md` + `extraction-report-schema.md` + `docs/dev-guide/how-to-add-a-retrieval-provider.md` updated with Consensus-specific content.
- [ ] `bmad-party-mode` implementation review consensus: approve.
- [ ] `bmad-code-review` layered pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor); MUST-FIX remediated; SHOULD-FIX triaged.
- [ ] `sprint-status.yaml::development_status::27-2.5-consensus-adapter` flipped to `done` with closure comment.
- [ ] Epic 27 roster entry updated (epic file + `bmm-workflow-status.yaml` — flip 27-2.5 to done; update 28-1 `blocked_on` if still non-empty).
- [ ] 28-1 Tracy pilot unblock confirmed (both 27-2 + 27-2.5 now done — Tracy has both adapters live for first pilot dispatch).

## Questions for Green-Light Round

1. **PDG-1 confirmation**: Path B (synthetic-only) default accepted, OR does operator want to acquire `CONSENSUS_API_KEY` for live-cassette path A? Default: path B mirrors scite.
2. **PDG-2 confirmation**: Path B (inferred catalog) default accepted as consequence of PDG-1=B? Default: yes.
3. **PDG-3 wiring approach**: GitHub Actions matrix job, local pre-commit hook, or manual operator-run 3x gate? Party picks at green-light. Implementation detail; zero-flake verification semantics identical across approaches.
4. **Cross-validation test file location**: confirm `tests/test_retrieval_cross_validation.py` as NEW distinct file (preferred) vs folding into `tests/test_retrieval_dispatcher.py` (less preferred — dispatcher file already large and single-provider focused).
5. **Consensus-specific field set**: does `provider_metadata.consensus` include all AC-B.4 fields (consensus_score, study_design_tag, sample_size, evidence_strength) or subset? Default: all four, with `known_losses` sentinels for missing values. Amelia confirms at T4 if API surface differs.
6. **Study-design enum**: `["meta-analysis", "rct", "cohort", "review", "other"]` — confirm closed set vs open string. Default: closed set with `study_design_unrecognized` sentinel on unknown values (27-2 DEFER pattern for future values).
7. **3-pt estimate holds?** Given scope compression (no wiring cascade; one novel AC group for cross-val), 3 pts reflects: 220 LOC adapter + ~380 LOC unit tests + ~250 LOC cross-val integration tests + ~80 LOC docs/schema updates + CI flake gate wiring. Within 3pt envelope. If PDG-3 wiring is non-trivial (requires CI overhaul), may lift to 4 pts — party checks at green-light.
8. **NIT absorption from 27-2 `deferred-work.md` §27-2**: some DEFERs may touch Consensus-adjacent code (e.g., `_row_date` robustness, `_truncate_*` unknown-classification handling). Scope-in at green-light, or keep clean and leave for `27-2-refinement-hardening`?

---

## Cross-Validation: Worked Example (for Tracy authors reading this spec)

After 27-2.5 ships, Tracy can emit a `RetrievalIntent` like:

```yaml
run_id: TRIAL-2-C1-M1-EVIDENCE-BOLSTER-001
sources:
  - ref_id: src-evidence-001
    role: primary
    intent: "peer-reviewed studies on intermittent fasting and cardiovascular risk, published 2020-2026"
    provider_hints:
      - {provider: "scite", params: {mode: "search"}}
      - {provider: "consensus", params: {mode: "search"}}
    cross_validate: true
    acceptance_criteria:
      mechanical:
        date_range: ["2020-01-01", "2026-12-31"]
        min_results: 8
      provider_scored:
        authority_tier_min: "peer-reviewed"        # scite-honored
        supporting_citations_min: 20                # scite-honored
        consensus_score_min: 0.6                    # consensus-honored
        study_design_allow: ["meta-analysis", "rct"]  # consensus-honored
      semantic_deferred: "filter for studies with explicit hypothesis testing on CV endpoints"
    iteration_budget: 3
    convergence_required: true
```

Dispatcher fans out:
- SciteProvider honors `authority_tier_min` + `supporting_citations_min` (ignores `consensus_score_min`, `study_design_allow` — they're not in its `declare_honored_criteria`); returns list of DOIs with smart-citation classification populated.
- ConsensusProvider honors `consensus_score_min` + `study_design_allow` (ignores `authority_tier_min`, `supporting_citations_min`); returns list of DOIs with research-synthesis signals populated.

Dispatcher merges by `identity_key` (DOI):
- DOI-X in both → single merged row; `provider_metadata.scite` + `provider_metadata.consensus` both populated; `convergence_signal.providers_agreeing: ["consensus", "scite"]`.
- DOI-Y in scite only → row with only `provider_metadata.scite`; `convergence_signal.single_source_only: true; providers_agreeing: ["scite"]`.
- DOI-Z in Consensus only → row with only `provider_metadata.consensus`; `convergence_signal.single_source_only: true; providers_agreeing: ["consensus"]`.

Tracy's post-fetch pass (Epic 28) reads `convergence_signal` + both `provider_metadata` sub-objects to produce editorial judgments. Evidence-bolster control (Sprint #1 story) toggles the `cross_validate: true` flag based on operator intent.

This worked example becomes part of the `docs/dev-guide/how-to-add-a-retrieval-provider.md` "Cross-validation in practice" subsection after 27-2.5 closes.
