# P2-2 — Claude T11 Code Review Record (2026-06-20)

**Story:** `p2-2-perception-artifact-vision-node`
**Branch:** `fidelity-perception-arc-2026-06-19`
**Reviewer:** Claude (T11), NEW CYCLE — Codex owns T1–T10 dev; Claude reviews/closes or hands back.
**Codex handoff:** `_codex-handoff/p2-2-perception-artifact-vision-node.ready-for-review.md`
**Disposition:** **HAND BACK TO CODEX** (T11 STOP condition b) — see §Disposition.

---

## 1. Verification battery (independent re-run, not handoff-trusted)

| Check | Result | Notes |
|---|---|---|
| Focused P2-2 suites (vision, quinn_r, models/perception, integration-marcus, audit, manifest-payload contracts) | **328 passed, 1 skipped** ✅ | Core deliverable green |
| `lint-imports` | **15 kept, 0 broken** ✅ | New vision-provider isolation contract present |
| Pipeline-manifest lockstep L1 (`check_pipeline_manifest_lockstep.py`) | **exit 0 PASS** ✅ | Internal manifest↔generator↔pack registration consistent for 07G |
| Frozen-pack-SHA contract (`test_frozen_pack_shas.py`) | **4 passed, 1 skipped** ✅ | Confirms frozen v4.2 source pack untouched (→ corroborates F2) |
| `tests/parity` | 5 failed | **All 5 ⊆ clean-HEAD failures** (no P2-2-introduced parity regression) |
| `tests/contracts` | 15 failed | 14 pre-existing; **1 P2-2-INTRODUCED = F2** (see below) |

**Pre-existing-failure verification method (load-bearing):** created a detached worktree at clean HEAD `4455c04` and ran `tests/parity tests/contracts` there for a baseline diff. 13 of 15 contracts failures + all 5 parity failures reproduce on clean HEAD → genuinely pre-existing. **`test_33_1a_verbatim_extraction` is the sole exception: RED on the P2-2 tree, GREEN on clean HEAD** (confirmed in isolation, ruling out cross-suite flakiness). Codex's handoff bucketed this among "15 unrelated pre-existing failures" — incorrect.

---

## 2. Binding-amendment verification (from the Tier-3 green-light)

| Amendment | Verdict | Evidence |
|---|---|---|
| Tripwire flipped to ONE two-sided enforce test; old deleted not skipped | ✅ MET | `test_fidelity_detector.py`: RED (`test_slide01_red_fixture_blocks_orphan_visual_reference`) + GREEN (`test_green_corpus_is_two_sided_and_hash_frozen`); old dormant test replaced by `test_g5_manifest_supplies_perception_projection` |
| Additive schema keeps P2-1 green | ✅ MET | `extra="forbid"` retained; new fields Optional/defaulted; shape-pin updated to exact `==`; P2-1 fixture still validates |
| Schema EXCLUDES provenance; two-sided divergence test | ✅ MET | `Field(exclude=True)+SkipJsonSchema`; parity test asserts on-model AND absent-from-schema |
| Detector tag NOT in `_RETRYABLE_DISPATCH_TAGS` (standing guard) | ✅ MET | set = `{"irene.pass2.slide-join-failed"}`; `test_fidelity_detector_tag_is_not_retryable` pins both fidelity tags out |
| `dp-v1.3` bump; `pack_version: v4.2` unchanged; no v4.3 | ✅ MET | manifest `data_plane_vocabulary_version: dp-v1.3`; no v4.3 lineage |
| `FIDELITY_GATE=warn` override present, default-enforce | ⚠️ PARTIAL → **M3** | present + annotated, BUT catch scope too broad (see M3) |
| Comparator θ/d calibrated against held-out set + negative control (M-3/M-4) | ❌ NOT MET → **F1** | calibration is vacuous (see F1) |

---

## 3. Three-lane adversarial review (fully-spawned subagents) + self-validation

### MUST-FIX (4)

**F1 — Comparator θ/d calibration is VACUOUS** (`app/specialists/vision/repeatability.py`, `tests/specialists/vision/test_vision_repeatability.py`). The "pass case" compares a golden fixture **to itself** (tautology — stable for any non-degenerate thresholds). The "negative control" only asserts `element_jaccard` fired, and `element_jaccard_min=1.0` is exact-equality (any element delta trips it). The genuinely tolerance-bearing thresholds `bbox_iou_min=0.90` and `text_edit_distance_max=8.0` are **never exercised at any boundary** — they could be set to 0.0 / 10000 and both tests still pass. No held-out labeled set. Violates the binding M-3/M-4 anti-vacuity amendment; operator designated vacuous tolerances a MUST-FIX. *Self-validated: read both files + ran the suite.*

**F2 — Section 07G breaks `test_33_1a_verbatim_extraction`** (lockstep / `block_mode_trigger_paths` pack-generator contract). The test asserts every `-gen` section's prose appears verbatim in the FROZEN v4.2 source pack. 07G is net-new prose ("Marcus, run the Vision specialist…") absent from the frozen source → `AssertionError: Template prose drift for 07G`. RED on P2-2 tree, GREEN on clean HEAD. Codex did not touch the frozen source (frozen-SHA passes) and mislabeled this as pre-existing drift. *Self-validated: isolation runs on both trees.*

**M3 — `FIDELITY_GATE=warn` swallows STRUCTURAL integrity failures** (`app/specialists/quinn_r/_act.py:176-192`). The `except FidelityError` wraps the entire `detect_fidelity` call, so the operator break-glass downgrades schema-drift / duplicate-artifact / missing-artifact errors (all raised with `ORPHAN_TAG` from `_artifact_map`) to a "narration unverified" advisory — silently disabling substrate-integrity guards, not just the narration-fidelity check AC-17 scoped it to. Should be scoped to narration-fidelity tags only. *Self-validated: read `_act.py:176-192`; convergent (Edge M3 + Blind SF3). Non-`warn` env values correctly fail-closed.*

**MF1 — Core detector `_FIGURE_RE` captures stray trailing letters** (`app/specialists/quinn_r/fidelity_detector.py:18-19`). `\$\s*\d+(?:\.\d+)?\s*(?:t|trillion|b|billion)?` has no trailing boundary and allows whitespace before the unit. `_figures("$5 to enroll")` → `{'money-trillion:5'}` (= "$5 trillion"); a slide showing bare "$5" → `{'money-bare:5'}` → spurious `FIGURE_TAG` Class-A block, and it conflates "$5 to…" with "$5 trillion." False positives in a Class-A gate are "poison" (the code's own visual-groups comment). *Self-validated: ran `_figures`/`_FIGURE_RE` live on the corpus.*

### SHOULD-FIX

- **AC-12 — `quarantined` pytest marker is COSMETIC.** Default `addopts = -m 'not live and not serial'` does NOT exclude `quarantined`; the repeatability tests run on the BLOCKING lane (verified: 2 passed in default run). The two-lane CI guarantee is not wired.
- **AC-15 — silent-drift canary cannot detect drift.** Its only test is `compare_artifacts(payload, payload)` (self-compare) — stable by construction; proves nothing. Capability named, unproven.
- **SF2 (Blind) — provider `slide_id` accepted verbatim** via `setdefault` (`provider.py`) with no equality check → perception can be mislabeled onto the wrong slide / collapse to duplicate slide_ids surfaced late at G5.
- **SF4 (Blind) — pinned model-id not enforced on response** + `model_config.yaml` (`gpt-5`) disagrees with the provider's hard-pinned `DEFAULT_MODEL_ID="vision-fixture-v1"`; the act path never honors the model_config-resolved model.
- **M1/M2 (Edge) — retry coverage gaps:** 429/408 and connection errors (`ConnectError`/`ReadError`) are not retried — only `>=5xx` and `TimeoutException` — contradicting the "bounded transport-only retry" intent for the most common transient failures.

### NIT (selected)
S1 `confidence_score` accepted but never consulted (can contradict categorical `confidence`); S3 partial-batch failure aborts the whole node, discarding already-perceived slides; S4 unbounded `_levenshtein` / regex over `extracted_text` (perf); S5 photo-only (text-free) faithfully-perceived slide false-positives on any narrated figure; N2 PNG path has no run-dir containment check.

---

## 4. AC roll-up (Acceptance lane)

13 MET · 1 UNMET (AC-14-B operator live round-trip evidence — op-gated, openly deferred) · 4 PARTIAL (AC-11=F2, AC-12 cosmetic quarantine, AC-13=F1, AC-15 vacuous canary).

---

## 5. Disposition — party-mode consensus (UNANIMOUS 5/5; no impasse)

Fully-spawned party-mode (Winston/John/Murat/Mary/Amelia), same team that green-lit P2-2.

**D1 — HAND P2-2 BACK TO CODEX (5/5).** One consolidated T1–T10 remediation cycle, then re-run Claude T11. P2-2 does **not** flip `done`; Codex's uncommitted T1–T10 work stays in the working tree for the re-work. No bounded-Claude close: all 4 MUST-FIX are guard-defeating production dev code; Claude remediating the substrate it reviews would collapse the NEW CYCLE author/reviewer separation. T11 STOP condition (b).

**D2 — F2 via Option A (5/5):** register 07G as a net-new-section exception in `test_33_1a`; **reject** editing the frozen mapping-axis pack. Ownership resolved 4-to-1 → **Codex implements within the consolidated cycle** (Claude specifies what; Codex authors). Conditions (complementary, all binding on the hand-back):
- closed allowlist (not regex), each entry = section + why-verbatim-can't-hold + substitute invariant (Murat);
- meta-test: every excluded section is covered by L1 Check-9 regeneration-determinism — the structural lock (Murat);
- 07G presence/shape assertion: non-empty body + correct ordinal vs 07F/08 (Amelia);
- formal rule amendment: verbatim-extraction applies to frozen-origin sections; net-new `-gen` sections are governed by Check-9 (Winston);
- additions to the allowlist = party-mode governance, not dev authority (Murat).

**Pass-bar for F1 (Murat, binding on re-run T11):** held-out labeled fixture set (no `x==x` self-compare); one negative control per threshold (bbox_iou / element_jaccard / text_edit_distance each tripping only its own knob); a per-threshold **mutation table in Completion Notes** proving each constant is load-bearing (set degenerate → its test flips). **MF1:** adversarial false-positive corpus ("$5 to enroll", ordinals, version numbers, page refs) must be green-silent while a true money-trillion case still fires.

**Process guards (Mary + Murat):** (1) mandatory baseline-diff attestation in every Codex handoff — any "pre-existing failure" label requires pasted clean-HEAD RED evidence; burden flips to dev. (2) green-light checklist question: "does this story add a net-new `-gen` prose section? → register the exception before dev opens." (3) harvest anti-pattern `mislabeled-regression-as-preexisting-drift`.

**Consolidated remediation brief for Codex:** `_bmad-output/implementation-artifacts/codex-remediation-prompt-p2-2-t11.md`.

**Next:** operator dispatches Codex on the remediation prompt (T1–T10), Codex re-delivers a handoff, Claude re-runs T11. P2-3 prep stays BLOCKED until P2-2 closes on a real, reviewed PerceptionArtifact.
