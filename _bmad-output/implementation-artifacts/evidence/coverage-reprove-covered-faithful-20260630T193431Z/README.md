# Coverage interlock — COVERED arm, FAITHFUL positive-control FRESH walk (option C) — NOT-CONVERGED → fallback A

**Verdict: NOT-CONVERGED** (both bounded attempts error-paused on INFRASTRUCTURE flakiness BEFORE the G3 coverage seam — never a coverage/narration outcome). $0 ElevenLabs. Per Round-8 + John condition-6 + operator-authorized "one bounded re-attempt; fail/block again → close A", this triggers the **close on path A**.

## What option C was trying to prove
A real-runner walk over a small *faithful positive-control* corpus (`course-content/courses/coverage-faithful-probe/`, single `70%` focal slide whose speaker notes repeat `70%` 5×) producing a real-run figure-token-VERIFIED **COVERED** must-cover point that PASSES `enforce_coverage_gate_before_audio(enrique)` — exercising the gate's PASS arm on the real runner (which it never had; only the BLOCK arm was live-proven).

## Two bounded attempts (first-run-stands; NO retry-to-green; corpus FROZEN)
- **v1** (run `825136d0-...`, 229s): live gpt-5 G0-enrichment succeeded (real `corpus_fingerprint`; slide-2 source points all carrying `percent:70`), walk approved G0E→G0R, then **error-paused at Texas node 02** — `dispatch_retrieval missing required input: directive_path` (`bundle.dispatch.input-missing`). Root cause: the driver launched `run_production_trial` WITHOUT `directive_path` (a launch-recipe omission, NOT a coverage finding). Driver: `_bmad-output/.../coverage-reprove-covered-faithful-20260630T193322Z/driver.py`.
- **v2** (run `38e91d16-...`, 330s; CORRECTED): authored the launch directive (run_id-bound source-config; corpus FROZEN, not re-engineered) + passed `directive_path`. The fix WORKED — Texas dispatched; walk approved **G0E→G0R→G1** cleanly — then **error-paused at the Gamma deck export** (`gamma.export.brief-unmatched`) BEFORE G2/G3. A known Gamma export flake (deferred-inventory notes an auto-retry net exists; the manual gate-drive driver did not engage it). Driver: `driver_v2.py` (here). Result: `faithful_driver_v2_result.json`; log: `faithful_driver_v2.log`.

## Honest determination
The coverage gate's PASS arm could not be exercised this session via a FRESH full walk: G0→G3 traverses several flaky infrastructure nodes (Texas directive plumbing, then Gamma brief-matching) — each fixable but **compounding** (the exact characteristic the prior SESSION-HANDOFF documented, and why that session used API/infra-independent **rewind-recover** instead). NEITHER attempt reached the coverage seam, so NEITHER says anything about the narration figure-fidelity question — that remains open and is routed to Leg-4 regardless.

## Disposition
Close Leg-1 on **path A**: BLOCK arm proven-LIVE (`coverage-reprove-block-recover-20260630T131517Z/`, $0 ElevenLabs) + COVERED arm proven-OFFLINE (both-arms regression fixture `tests/unit/marcus/test_coverage_figure_facet.py`: real `$5.3T→one-fifth` drift BLOCKS / `18%` paraphrase COVERS-verified). The real-run COVERED-PASS becomes a **Leg-4 / Leg-1b confirmation follow-on** — cheaply achievable via the proven rewind-recover mechanism on a faithful corpus, or once the fresh-walk infra flakes (`gamma.export.brief-unmatched` auto-retry in gate-drive; Texas directive auto-compose) are hardened. Close-note stays honest: numeral/figure-token facet ONLY; production narration NOT proven figure-faithful (OPEN in Leg-4).
