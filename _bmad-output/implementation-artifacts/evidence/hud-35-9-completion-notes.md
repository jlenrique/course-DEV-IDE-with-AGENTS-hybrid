# Story 35.9 — Operator-surface contract enrichment + §Projection-Demands parity pin — completion notes

**Story:** 35.9 (Epic 35, party KEY DECISION 2). **Branch:** `dev/hud-revival-2026-07-11`. **Dev:** fresh BMAD dev agent. Working tree only — no commits (orchestrator commits post-review).

**Binding source:** `epic-35-party-greenlight-2026-07-11.md` §KEY DECISION 2 (4/4 synthesis). **ADs:** 1, 3, 4 (additive-only within v1), 15.

## Result summary

- Contract widened with THREE optional sections, additive within v1 — **NO `schema_version` bump** (stays `"v1"`). Byte-pin schema + parity/shape fixtures regenerated in lockstep; existing goldens still validate; lenient reader tolerates the widened document.
- Assembler populates `decision_card` + `error_message` at the pause choke-point and `deliverables` on the completion emit — every read inside its own log-and-swallow guard (amendment 8); fault-injection tests prove a missing/garbage artifact never sinks emit.
- Render (page.py + client.py) renders the three new sections; the consumer-side `specialists[].last_artifact` deliverables synthesis is DELETED (AD-1 restoration, grep-proven). The three 35.5-review client folds are landed (UTC clock, projection-sourced health budget, cold→live field parity).
- NEW FENCE: `tests/contracts/test_operator_surface_projection_demands_parity.py` — maps every §Projection-Demands bullet to a contract section/field; RED on any unmapped/renamed field or unregistered waiver. Exactly ONE waiver registered.

## Files changed

Production (all in `block_mode_trigger_paths`):
- `app/models/runtime/operator_surface.py` — 3 new optional sections + `DraftedProposal` / `DeliverableComponents` sub-models; wired into `OperatorSurfaceProjection`; `__all__` updated.
- `app/models/schemas/operator-surface.v1.schema.json` — regenerated byte-pin (955 → 1245 lines).
- `app/marcus/orchestrator/operator_surface_assembler.py` — `_decision_card_dict` / `_error_message_dict` / `_deliverables_dict` (+ `_read_component_selection` / `_read_total_cost` / `_read_export_paths` / `_summarize_context_entry`); wired into `emit()`; `import json` + `import yaml` added.
- `app/hud/render/page.py` — `_ctx_gate` renders decision_card; `_ctx_error` renders verbatim message; `_ctx_completed` renders `projection.deliverables` (synthesis deleted); docstring updated; server `_view` honors projection health budget.
- `app/hud/render/client.py` — UTC `clock()`; `artifactsBlock`/`labelledArtifacts` helpers; gate/error/completed branches rewritten; map Step X/Y dur; projection-sourced health budget.
- `app/hud/render/styles.py` — `.art-label` + `.components`.

Tests:
- `tests/contracts/test_operator_surface_parity.py` — new sections added to `_SECTION_DEFS`; additive round-trip / optionality / lenient-tolerance tests.
- `tests/contracts/test_operator_surface_projection_demands_parity.py` — NEW parity-pin fence.
- `tests/hud/_render_fixtures.py` — decision_card / error_message / deliverables fixtures.
- `tests/hud/test_render_goldens.py` — decision-card / error / deliverables goldens; deliverables-not-synthesized-from-roster test.
- `tests/unit/marcus/orchestrator/test_operator_surface_assembler.py` — 8 new tests (G1/G4A shapes, error pause, completion, fault injection, partial artifacts, clear-on-resume).

## Contract diff summary (additive within v1 — AD-4)

| Section (`| None = None` at root) | Inner fields (all optional / verb-conditional) |
|---|---|
| `DecisionCardSection` | `gate_focus`, `operator_prompt`, `drafted_proposal: DraftedProposal | None` (`decision`, `confidence∈[0,1]`, `rationale`), `pick_context: list[str]`, `evidence: list[str]` |
| `ErrorMessageSection` | `message`, `node_index: int`, `tag` |
| `DeliverablesSection` | `components: DeliverableComponents | None` (`deck`/`motion`/`workbook` bool), `total_cost_usd: float≥0`, `export_paths: list[str]` |

No field removed/renamed/retyped → no version bump (AD-4 additive-only). Byte-pin test green; required/optional bidirectional parity green for all new $defs.

## Per-verb assembler field mapping (1:1 artifact → field, no new derivation)

| Trigger status | Source artifact (in `run_dir`) | Section | Field mapping |
|---|---|---|---|
| `paused-at-gate` | `decision-card-{paused_gate}.json` → `card` | `decision_card` | `card.gate_focus`→`gate_focus`; `card.operator_prompt`→`operator_prompt`; `card.drafted_proposal.{decision,confidence,rationale}`→`drafted_proposal`; `card.pick_context[]`→`pick_context` (summarized string per entry); `card.evidence[]`→`evidence` (same) |
| `paused-at-error` | `error-pause.json` (top-level) | `error_message` | `message`→`message` (verbatim); `node_index`→`node_index`; `tag`→`tag` |
| `completed` | `run_summary.yaml` + `cost-report.json` + `exports/` | `deliverables` | `component_selection.{deck,motion,workbook}`→`components`; `total_cost_usd`→`total_cost_usd`; top-level `exports/` entries→`export_paths` (MINIMAL, capped 40) |

Verb-conditional confirmed against real fixtures in `state/config/runs/22b27500-…`: G1 card has `drafted_proposal`+`confidence` / no `operator_prompt`; G4A card has `gate_focus`/`operator_prompt`/`pick_context` / no `drafted_proposal`. Every read guarded independently (a missing cost report never suppresses the component booleans); `decision_card`/`error_message`/`deliverables` each CLEAR (set to `None`) off their trigger status, matching `next_action` resume semantics.

`pick_context`/`evidence` entry summarization precedence (1:1, bounded to 240 chars): artifact `path` > voice-option names (`voice options: …`) > `kind (node_id)` > raw. This is a display-string projection (one entry → one string), not new derivation of state/confidence.

## Parity-pin bullet → field map (the new fence)

`tests/contracts/test_operator_surface_projection_demands_parity.py` — explicit `PROJECTION_DEMANDS` + `WAIVERS` constants. Every demand is satisfied iff all mapped `Model.field` entries resolve OR it cites a registered dated waiver.

| Demand id | §Projection-Demands bullet | Mapped to |
|---|---|---|
| `envelope-status-pause-timestamps` | envelope status + pause metadata + timestamps | `EnvelopeSection.{status,paused_gate,paused_error_tag,waiting_batch_id,completed_at,as_of}` |
| `run-identity` | run identity | `IdentitySection.{trial_id,lesson,preset,operator_id}` |
| `preflight-heartbeat-items` | pre-flight/heartbeat items | `PreflightItem.{name,state,output,latency_ms,quota_reading,quota_confidence}` |
| `two-stage-step-list` | two-stage step list | `StepsSection.{walk_index,manifest_digest}` + `StepEntry.{stage,status,conditions,blockers,locked_artifact_summary}` |
| `decision-card-and-next-action` | active decision card + exact next-action command | `DecisionCardSection.{gate_focus,operator_prompt,drafted_proposal,pick_context,evidence}` + `DraftedProposal.{decision,confidence}` + `NextActionSection.{command,pause_class}` |
| `specialist-roster` | specialist roster activity | `SpecialistEntry.{name,status,current_node,model,last_artifact,cost_usd}` |
| `health-readings` | health readings + history | `HealthTile.{label,value,unit,confidence,threshold_state,history}` + `HealthReading.{at,value}` |
| `modality-readings` | modality readings | `ModalitiesSection.{llm_execution_mode,detective_disposition,styleguide,styleguide_provenance}` |
| `state-trace-events` | state-trace events | `TraceSection.events` + `TraceEvent.{at,event,detail}` |
| `notification-echo-and-heartbeat` | notification config echo + watchdog heartbeat | `NotificationsEchoSection.{config,parse_status}` + `OperatorSurfaceProjection.{progress_seq,last_progress_at}` |
| `error-briefing` (briefing row) | error briefing: message verbatim + node + re-entry | `ErrorMessageSection.{message,node_index,tag}` |
| `deliverables-summary` (briefing row) | deliverables list + final cost | `DeliverablesSection.{components,total_cost_usd,export_paths}` + `DeliverableComponents.{deck,motion,workbook}` |
| `deliverables-rich-enumeration` | RICH per-artifact enumeration | **WAIVED** → `rich-per-artifact-deliverables-enumeration` |

### The one registered waiver

`WAIVERS = { "rich-per-artifact-deliverables-enumeration": Waiver(date="2026-07-11", ticket="35.7", reason=…) }` — exactly one, dated, citing the 35.7 fast-follow (KEY DECISION 2 §5). Tests enforce: exactly one waiver, correct date, ticket cites 35.7, no dangling waiver.

## Grep proof — `specialists[].last_artifact` deliverables synthesis removed (AD-1)

```
$ grep -n "last_artifact" app/hud/render/page.py app/hud/render/client.py
page.py:30    # docstring: "…synthesis is DELETED (AD-1…"
page.py:480   # SPECIALIST CHIP roster field (legit, §Projection-Demands bullet 6)
page.py:732   # comment documenting the deletion
client.py:209 # SPECIALIST CHIP roster field (legit)
client.py:279 # comment documenting the deletion
```

The completed-briefing (`_ctx_completed` / client completed branch) no longer reads `last_artifact` — deliverables come solely from `projection.deliverables`. The only functional `last_artifact` uses are the specialist chip (a legitimate roster field), not deliverables synthesis.

## 35.5-review client folds (task 3)

- **UTC clock**: `client.py::clock()` now uses `getUTCHours/getUTCMinutes/getUTCSeconds` to match the server's UTC `strftime` → kills the cold-load-vs-first-poll clock jump.
- **Health budget**: `renderZones` reads `d.health_budget_seconds` (falls back to 60); server `_view` prefers a projection-carried budget too, so cold-load and poll agree. (No `health_budget_seconds` field is emitted in v1, so both currently default to 60 — the hook is additive-tolerant and requires no schema change. Documented interpretation: the review's "projection health_budget_seconds" is honored via a top-level projection read, defaulting to 60; adding a dedicated field was out of scope and would have bumped the byte-pin.)
- **Cold→live field parity**: client completed branch renders the final-cost row + component chips; error branch renders the re-entry row + verbatim message + node index; active map node renders "Step X/Y" — none vanish on the first poll.

## Verification

- `pytest tests/unit/models tests/contracts tests/hud tests/unit/marcus/orchestrator` → **981 passed, 1 skipped, 2 failed**. The 2 failures are PRE-EXISTING and unrelated to 35.9:
  - `test_30_1_zero_test_edits::test_no_preexisting_test_files_modified_in_30_1` — a committed **commit-range** diff (`0f3fee72..HEAD`) flagging Epic 35 test files already on the branch; invisible to my uncommitted working tree.
  - `test_transform_registry_lockstep::test_every_format_covered_or_exempted` — about `transform-registry.md` format sections; my diff touches zero transform files.
- `ruff check` (all changed files) → clean.
- `lint-imports` → 17 kept, 0 broken (HUD1 KEPT — render stays app.hud + contract only).
- `check_pipeline_manifest_lockstep.py` → exit 0 (trace `reports/dev-coherence/2026-07-12-0041/…PASS.yaml`).
- Byte-pin schema regenerated and green; JS asset `node --check` valid.

## Deviations

1. **Health-budget fold** interpreted conservatively (see task-3 note above) — projection-read + 60 default, no new contract field, no schema bump. Documented for reviewer visibility.
2. `pick_context`/`evidence` stored as `list[str]` (pre-summarized display strings) rather than raw nested objects — keeps the projection small (AD-16) and feeds the pre-built `_artifacts_block` helper directly; the summarization is a 1:1 entry→string projection, not new state derivation.
