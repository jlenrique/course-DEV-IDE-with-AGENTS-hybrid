# Story 43-5 — Plan-unit + estimator + run-constants renderers

**Epic:** 43 · **Slab:** 2 · **Status:** ready-for-dev · **Gate mode:** dual-gate.

Follows the **43-3/43-4 pattern** (bespoke renderer + `GATE_TO_CONTENT_TYPE` entry + synthetic fixture + allowlist→registry move + TW-7c-4 registration). Three content types in one story (all Irene/plan-band gates the operator crosses on a normal run).

## Story

As the **operator**, at **G1A (plan-unit ratification)**, **G1.5 (run-budget estimator)**, and **G1.5 (run-constants lock)**, I want each shown as a purpose-built table rather than the generic dump.

## T1 required readings

- `app/marcus/cli/hil_tabular_projector.py` — registry + `GATE_TO_CONTENT_TYPE`/`resolve_content_type` + the 43-3/43-4 bespoke renderers (the pattern) + `_md_table`/`_truncate_cell` + `KNOWN_UNRENDERED_ALLOWLIST`.
- `app/gates/section_04a/poll_surface.py::display_plan_unit` (:145) — PlanUnit shape.
- `app/gates/section_04_5/poll_surface.py::display_estimator` (:73) — run-budget estimator shape.
- `app/gates/section_04_55/poll_surface.py::display_run_constants` (:109) — run-constants shape.
- `tests/marcus/cli/test_variant_mode_renderers_43_3.py` (test pattern); `tests/fixtures/hil_projector/README.md`; the TW-7c-4 Epic-43 block.

## Acceptance criteria

**AC-1 — three bespoke renderers**, each registered under its canonical key:
- `render_plan_unit` → `content_type="plan_unit"` (G1A): table the PlanUnit fields (unit id, scope, LOs, weather band — whatever section_04a carries).
- `render_estimator` → `content_type="estimator"` (G1.5): table the estimator line items (parent slide count, token/cost projections — per section_04_5).
- `render_run_constants` → `content_type="run_constants"` (G1.5): table the run-constants key/values (per section_04_55).
Reuse `_md_table`/`_truncate_cell`; tolerate nested-display + bare-payload shapes; name each gate ("G1A plan-unit ratification", "G1.5 run-budget estimator", "G1.5 run-constants lock") in tests.

**AC-2 — bridge entries.** Add the gate keys → the three content types in `GATE_TO_CONTENT_TYPE` (discover the actual gate identifiers from the poll_surfaces — G1A, and the two G1.5 gates; note two content types share the G1.5 gate, so key by the disambiguating paused_gate/surface_id string as 43-3 did for G2B/G2M). Routing tests prove each fixture reaches its bespoke renderer.

**AC-3 — three synthetic fixtures** (`poll-plan-unit-synthetic.json`, `poll-estimator-synthetic.json`, `poll-run-constants-synthetic.json`), generated by invoking the real `display_*` functions; README notes.

**AC-4 — ratchet moves.** Remove `plan_unit`, `estimator`, `run_constants` from `KNOWN_UNRENDERED_ALLOWLIST`; update the ratchet state-pin to the new registered set. Green.

**AC-5 — invariants.** stdout/stderr split; additive-within-v1; projector stdlib-pure; TW-7c-4 register new test .py file; ruff + import-linter clean; no manifest touch.

## Test / process

- New test file(s) under `tests/marcus/cli/`; routing + shape tests per content type; ratchet green; TW-7c-4 scope test green. Use `-n 0`.
- Do NOT run the full suite. Do NOT commit or stash.

## Definition of done

Three renderers registered + tabling their synthetic fixtures; each gate routes correctly; all three moved allowlist→registry with 43-10 green; TW-7c-4 registered; ruff/import-linter clean; ready for orchestrator review → `bmad-code-review`.
