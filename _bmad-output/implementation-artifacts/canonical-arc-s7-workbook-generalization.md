# Story S7 (Phase 1) — Workbook producer generalization + live in-graph proof

**Arc:** Canonical Production Conversation (party record §1 S7 row + §9 operator checkpoint). Gated after S6 (COMPLETE). **The workbook-generalization phase; the "lesson-plan-as-rationale directs-production" spine is Phase-2, deferred (see `deferred-inventory.md §Lesson-Plan-as-Rationale Platform`).**
**Status:** ✅ **DONE — FORMALLY CLOSED 2026-07-08 (session 20): fresh 4-seat party concurrence 4/4 (Winston/John/Murat CONCUR-W-FINDINGS, Amelia CONCUR-W-FINDINGS), all findings bound into this record; full verdicts in the arc monitor ledger close entry.**
**FORMAL CLOSE RECORD (binding language — party-ratified):**
- **Two-story close (John A1, honored):** *the first in-graph workbook from a DIFFERENT lesson (Tejal Part 3) was produced only after the upstream Irene Pass-2 id-integrity fix (`irene-pass2-slidejoin-id-integrity-gate`) — both stories close together on the single fresh post-fix AC-L witness (trial `4c64db93`).*
- **Claim (SOP-034/A3):** **"off-frozen-lesson generalization — different LESSON, same SME; cross-SME = Phase-2."** Never "first non-tejal." NOT learner-ready — scaffold-honest per the S7 fence (S2 REVOICE-REQUIRED markers stay; prose → S8 arc; word-form-numeral + semantic-audit gate gaps stay named-deferred).
- **Witness provenance (F-3801):** a **fresh post-fix AC-L witness (full re-walk, ~$0.55), recover-labeled; the surgical 08→08B→07W re-entry was NOT performed** (`recover_production_trial` hardcodes failed-node re-entry; `recover-with-reenter-node-affordance` filed). First-run-stands held: driver frozen at 20:56:00, trial minted 20:56:03Z, one sequential pass, no re-roll (Murat timeline-verified).
- **F-4001 numeric audit (CLOSED, stronger than expected):** `numeric_audit_status: FAIL` on the witness is the frozen-neck's **zero-denominator "un-auditable" non-event recording** — independently re-verified by two close seats: **0 symbol figure tokens** in the workbook body (word-form figure scan also 0); the raise-path (`FAIL ∧ narration_token_count>0`, `workbook_producer.py:326`) is intact; **no unsourced numeral exists because no numeral exists.** Cites `braid-workbook-wordform-numeral-gap` (+ new status-vocabulary rider filed on that entry: FAIL conflates "violation" with "nothing to audit").
- **F-4002 DOI clause (PASS-with-recorded-nuance, Murat ruling adopted):** the anti-fabrication teeth are affirmatively satisfied (3 entries traced to the `research_wiring` contribution, `source_ref=retrieval:scite:…`, never a `_FURTHER_READING` constant; rendered UNDER G2, `citation_unsourced: 0`). doi.org **dereferences** the primary `10.5465/ambpp.2019.19399abstract` to the correct AOM landing path — the publisher 403s automated fetchers (access nuance, not a citation defect; a dead DOI yields doi.org not-found, never a publisher 403). **Two other rendered DOIs resolved HTTP 200** (`10.3389/fresc.2024.1336559`, `10.48550/arxiv.2604.06331`, monitor-verified SOP-040). The strong form (doi.org→200 content-inspected) was live-proven at S6 (`10.1038/ijo.2010.252`). `research-quality-resolvable-doi-yield` **now actually FILED** in deferred-inventory (was named-only — John close-Finding 1; the session-19 checklist overstated).
- **F-4201:** canonical DOCX witness = the committed `_bmad-output/artifacts/workbooks/u01@1.docx`; the 12MB evidence-dir duplicate is **deliberately untracked**. **F-2802:** standing stray convention holds. **F-4302:** resolved by this close. **F-3401:** 5 stale-wording fixes applied 2026-07-08 (in this file) + Amelia close-F1 (stale "frozen tejal deck input" comment at `app/marcus/lesson_plan/workbook_producer.py:107`) fixed in the close batch.
- **Residuals routed:** LO *statements* render unresolved-with-recorded-provenance (the D2-pinned per-slot degrade — bindings/bloom/sections are Irene's real collateral; no fabrication) → statement-resolution rides the S8 prose arc. MD figure links point into gitignored `state/config/runs/` (dangle in a fresh clone; the DOCX embeds them) — observation only. Winston close rider `run-envelope-corrupt-vs-absent-fail-loud` filed.
**Pre-close status (historical):** AMENDED — green-light round complete + SOP-026 + seam-verification folded. §9 operator gate EXECUTED 2026-07-07 (session 19); design ratified in `lesson-plan-rationale-platform-positioning-2026-07-07.md`. Party green-light 4/4 GO-WITH-AMENDMENTS (Winston/John/Murat/Amelia) + Codex SOP-026 CONCUR-WITH-FINDINGS — all amendments folded below. **Seam RESOLVED (Tier-1 confirmed).**
**Operator-ratified design (§9 gate, binding):** the artifact is a **"narrated-deck companion workbook"** — one member of a **collateral** family; **"collateral" = the family umbrella** (schema `CollateralSpec`). Add a `kind` discriminant now (on the artifact); build NO family machinery. Prose + gate-gaps deferred. Irene owns rationale/intent; producer+adapters+enrichment own resolution — **S7 CONSUMES Irene's real emitted collateral + proves it live** (no parallel adapter authoring path).
**Size:** M · **Tier:** **Tier-1 CONFIRMED** (seam-verification: producer disk-reads `run.json` via `app.models.*` model classes — no `pipeline-manifest.yaml` edit, no `block_mode_trigger_path` touched, M3 intact). STOP-and-report if any edit forces a manifest change. · **Gate mode:** single-gate structural (3-lane at T11) · **Branch:** `dev/workbook-2026-07-06`.
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, `-n 0`, **no `git stash`** (throwaway worktrees for baseline). Do NOT commit — orchestrator commits after the LIVE witness.

## Party scope (binding — Phase-1 fence)
S7 Phase-1 = **the producer generalizes off tejal + produces a real workbook off a DIFFERENT lesson than the frozen one, live, in-graph (off-frozen-lesson, same SME allowed — NOT “non-tejal”, per SOP-034)**. IN: retire the tejal-hardcoded constant assembly (incl. the `_plan_unit_and_context` unit_id/event_type metadata); consume Irene's `lesson_plan["collateral"]` as the section/objective/depth-delta authority (enrichment = resolution overlay); wire the S6-cited `research_entries` → the workbook DOI section under G2; add the `kind` discriminant. **OUT (each its own arc):** learner-ready re-voiced prose (`workbook-learner-ready-prose-uplift`, S8 arc); the word-form-numeral + semantic-audit gate gaps; the `collateral → ComponentSelection` derivation edge + course/SME registry + course_purpose inputs + projector family (all Phase-2). **STOP-and-report (John A1):** if the verified work requires net-new `collateral → ComponentSelection` *derivation machinery* (beyond consuming `collateral.workbook` for content), STOP and re-green-light — that is the Phase-2 spine, not S7.

## Context (subsystem map — RESOLVED; verified at green-light + SOP-026 + seam-verification)
- **07W producer act:** `app/specialists/workbook_producer/_act.py`. `produce_workbook` → `build_workbook_inputs(run_dir, config, run_id)` → `WorkbookProducer.produce(...)`. Deterministic (no model client — pinned invariant). Terminal node; `insertion_after:15`; last gate before it is G5 (node 13).
- **⚠️ SSOT FORK — RESOLVED (Winston/Amelia, code-verified).** Today `build_workbook_inputs` (`_act.py:545-736`) has two paths and reads **neither** `lesson_plan["collateral"]`: (1) enriched path (`load_enrichment_card` → `project_enrichment_to_workbook_inputs`, `workbook_enrichment.py:296`) **synthesizes** sections from `provisional_los` (fabricates `depth_delta` at `:348-357`) — it does NOT reflect Irene's authored collateral; (2) tejal-constant fallback (`_act.py:591-736`). **Verdict: consuming Irene's collateral is 0% done; the enrichment projection IS the parallel-adapter authoring path the §9 design forbids.** So the producer must NEWLY read `lesson_plan["collateral"]`; the enrichment projection is demoted to a resolution overlay.
- **⚠️ ENVELOPE→PRODUCER SEAM — RESOLVED (seam-verification, Tier-1).** Both `collateral` and `research_entries` live only in the ProductionEnvelope / persisted `run.json`. Contract M3 forbids `app.specialists` importing `app.marcus.orchestrator` (so the producer may NOT call `research_entries_from_envelope`). **Mechanism (pinned): the producer disk-reads `<run_dir>/run.json`** (mirrors `load_enrichment_card`), deserialized via the **`app.models.*` model classes** — `ProductionTrialEnvelope` (`app/models/runtime/production_trial_envelope.py:40`) → `.production_envelope` → contributions. M3-safe (imports the MODEL, not the orchestrator; uses LOCAL literal constants `"irene_pass1"` / `"research_wiring"` / `"04.55"` / `"research_entries"`). **run.json is FRESH at 07W:** collateral rides the `irene_pass1` `lesson_plan` contribution (planning-time); research_entries land at node `04.55` (upstream of G2); every gate through G5 re-persists run.json before 07W; resume walks reload from run.json. No stale-read risk. Readers:
  - collateral → `envelope.latest_for_specialist("irene_pass1").output["lesson_plan"]["collateral"]`
  - research_entries → `envelope.get_contribution("research_wiring", node_id="04.55").output["research_entries"]`
  - **Recommended thinness (Amelia):** add pure disk-readers next to `load_enrichment_card` in `workbook_enrichment.py` (an M3-allowed import for the producer) — `load_run_envelope` / `collateral_from_run` / `research_entries_from_run` — keep `_act.py` thin.
- **⚠️ THE TEJAL STRING THAT SURVIVES TO THE LIVE ARTIFACT (Murat).** `_plan_unit_and_context` (`_act.py:74, 487-505`) hardcodes `unit_id="tejal-apc-c1-m1-p2-trends"`, `event_type="present-trends"`, `source_fitness_diagnosis="…macro-trends…"` on **both** paths → renders into the MD header (`workbook_producer.py:498` H1 title `Workbook: {event_type}`, `:507` `Unit ID: {unit_id}`). The enriched (live) path never emits the KC/further-reading constants — so the ONLY tejal leak on the live path is this header. D1/AC-1 MUST generalize it.
- **⚠️ G2 does NOT audit the DOI entries today (Codex F-2601).** `WorkbookProducer.produce()` runs G2 only over `citations` + `source_ref_manifest` (`workbook_producer.py:891-895`); the DOI block renders separately from `research_entries` (`:690-702`). D4/AC-4 must bring rendered research DOIs under G2.
- **⚠️ `declaration=="none"` under a selected workbook bundle (Codex F-2602).** S7 leaves `collateral→ComponentSelection` to Phase-2; the operator picks the `narrated-deck-with-workbook` bundle (`bundle_catalog.py:228-232`) which selects 07W. So 07W runs even when the plan declares deck-only — D3 must specify producer behavior.
- **Gates already present (do NOT rebuild):** G1 numeric (symbol-only), G2 citation, G3 exercise, AC-5 coverage, AC-8 dual-coding superset, S1 orphan/phantom. `research_entries=inputs.research_entries` is already forwarded to `produce()` (`_act.py:770`) — only the population is missing.
- **`kind` discriminant home (Amelia nit — RULED):** put `kind` on the **artifact** (`WorkbookSpec`), NOT on the `CollateralSpec` umbrella — the umbrella will hold multiple typed artifacts in Phase-2's projector family; typing the artifact keeps that seam clean. `CollateralSpec` (`collateral_spec.py:399-450`) has no `kind` today.

## Deliverables
### D1 — Retire ALL tejal-hardcoded content (constants AND plan_unit metadata)
Remove the producer's dependence on the tejal constants (`_CH2_KC`/`_CH3_KC`/`_FURTHER_READING`, the 3 hardcoded sections + 3 objectives) **and generalize `_plan_unit_and_context`** so `unit_id` / `event_type` / `source_fitness_diagnosis` derive from the run's real lesson plan / corpus, never the `tejal…`/`present-trends` config-default (Murat A1). A run off the frozen lesson must not emit frozen-lesson (p2-trends) Chapter-2/3 content NOR a frozen-lesson header.

### D2 — Consume Irene's `lesson_plan["collateral"]` as the blueprint authority (pinned mechanism + precedence rule)
The producer reads `lesson_plan["collateral"]` (from the `run.json` disk-read, seam above) as the **authoritative source of sections / depth-deltas / objectives / titles**. The G0 enrichment projection is **DEMOTED to a resolution overlay** (bloom leveling, `citation_resolutions → further_reading`, answer-key resolution) — it may NOT author sections. **Precedence rule (F-2603 / Winston / Amelia — binding):** *CollateralSpec owns intended artifact structure + LO/section bindings; G0 enrichment supplies deterministic resolved content slots (exercises, readings, citations); structural mismatch (declaration=="present" but no/absent collateral, or orphan section) fails loud at zero-blueprint; per-section missing data degrades with recorded in-workbook provenance.* **⚠️ MECHANICAL RESOLUTION ONLY** — do NOT author new pedagogical content in the producer (Irene's rationale role; out of scope).

### D3 — Honor `declaration` at the producer (fail-loud vs no-op-skip — two distinct cases)
- **`declaration=="present"` + blueprint absent/unresolvable** ⇒ fail-loud `WorkbookProducerActError` (recoverable dispatch-family) BEFORE producing a scaffold.
- **`declaration=="none"` (or absent collateral) under a selected workbook bundle (F-2602)** ⇒ the producer emits an **explicit skipped/no-op contribution** — a recorded "workbook declared none; no artifact produced" marker, a VALID (empty) specialist return contract, **NO stale artifact, NO fabricated scaffold**. (S7 does NOT build the `collateral→ComponentSelection` prune — that's Phase-2; the producer honors declaration itself.)

### D4 — Wire S6 `research_entries` → the DOI section, UNDER G2 (retire `research_entries=()`)
The producer reads the run's cited `research_entries` (run.json disk-read) and passes them to `produce(research_entries=...)`, populating `_act.py:538` and `:732`. **F-2601 (binding): every rendered `research_entry.source_ref` MUST be added to the G2 citation audit manifest; G2 FAILS if a rendered research DOI lacks its source_ref/hash** (update BOTH the 07W read AND the producer G2 contract — Codex open Q2). **F-2604 (binding): render DOI with `source_ref`/`provider`/`citation_id` + `supports_segment_id` ONLY if already present on the entry** — do NOT infer claim/segment support (that's the deferred semantic arc). Zero rows ⇒ recorded explicitly-empty WITH reason (mirror S6 D4), NOT the hardcoded deferred note.

### D5 — Add the `kind` discriminant on `WorkbookSpec` (forward-compat; no family machinery)
Add `kind: Literal["deck-companion-workbook"] = "deck-companion-workbook"` to `WorkbookSpec`, additive + back-compat (absent → default), bump `SCHEMA_VERSION` 1.0→1.1, **regenerate the emitted schema artifact** (`app/marcus/lesson_plan/schema/collateral_spec*.schema.json` — confirm whether the `.v1.` filename versions) + SCHEMA_CHANGELOG entry. Build NO drill/job-aid machinery.

### D6 — Retire the side-door driver as evidence
`scripts/utilities/produce_tejal_workbook.py` is DEAD as acceptance evidence — S7 proves 07W via a real composed graph run through the dispatcher (AC-L).

## Acceptance criteria
- **AC-1 (no tejal leakage — teeth on the REACHABLE vector; Murat A2/A3):** for a corpus other than the frozen lesson the produced MD contains none of the tejal KC prompts / `_FURTHER_READING` / section text **AND its Overview `Unit ID:` line + H1 title carry no `tejal-apc-c1-m1-p2`/`present-trends`/`macro-trends` substring** (frozen-LESSON identifiers — bare `tejal` is the SME name and appears legitimately in a same-SME different-lesson corpus slug; matches the AC-L witness facts) and trace to the run's real corpus. RED-first on BOTH paths: KC/further-reading via a forced no-card fallback unit test; the plan_unit-metadata leak via a card-present enriched test (that's the vector that survives live).
- **AC-2 (blueprint provenance — Irene's authored strings; Winston A3):** produced `section_id`/`title`/`depth_delta.deferred_depth` trace to `lesson_plan["collateral"].workbook.sections[*]` (Irene's authored strings), NOT `provisional_los` synthesis. Precedence witnesses (F-2603): collateral section absent from enrichment; enrichment extra LO/section; title/depth mismatch → the pinned fail-loud/degrade behavior.
- **AC-3 (declaration honored):** `present`+absent-blueprint ⇒ `WorkbookProducerActError` (RED-first); `none` under a selected workbook bundle ⇒ explicit skipped/no-op contribution, negative twin proving NO stale artifact + valid return contract.
- **AC-4 (research_entries under G2 — F-2601/F-2604):** a run whose envelope carries ≥1 cited `research_entry` renders its DOI (`https://doi.org/{DOI}`) with `source_ref`/`provider`/`citation_id`; **its `source_ref` is in the G2 manifest and G2 FAILS if corrupted/absent** (RED test deletes/corrupts one research source_ref → G2 red). `supports_segment_id` rendered only if present (no inference). Zero rows ⇒ recorded explicitly-empty with reason. Deterministic empty/degrade negative twin (Murat B2).
- **AC-5 (`kind` on WorkbookSpec):** defaults to `"deck-companion-workbook"`; absent-key round-trips to default; `SCHEMA_VERSION` bumped; schema artifact regenerated; SCHEMA_CHANGELOG entry; closed-enum triple red-rejection.
- **AC-6 (full-tree sweep + baseline — Murat C1/C2):** mechanical recorded token-sweep across ALL `tests/`: `_CH2_KC`,`_CH3_KC`,`ex-ch2-q`,`ex-ch3-q`,`nhe-fact-sheet`,`tejal-apc-c1-m1`,**`present-trends`**,**`Unit ID:`**,`research_entries=()`,`live-research leg deferred` → emit a table (file → subject-tejal? → migrate|pin). Capture the pre-existing-reds baseline from a **clean throwaway worktree BEFORE the change** (prove "zero flip-caused reds" by diff). `tests/ -n 0` green except known baseline reds.
- **AC-7 (Tier-1 + invariants):** `git diff -- state/config/pipeline-manifest.yaml` EMPTY; `figure_tokens.py` byte-frozen (word-form gap stays named-deferred); no VO↔on-screen / source-detail regression.
- **AC-L (LIVE witness — orchestrator, PASS required; first workbook off a DIFFERENT lesson — Tejal Part 3 — in-graph; NOT "non-tejal" per SOP-034):** a REAL composed walk on the **operator-named different-lesson corpus `tejal-c1m1-p3-opportunity` (Tejal Part 3; same SME, different lesson from the frozen p2-trends — see AC-L corpus section)** with enrichment + research dispatch default-ON → reaches 07W through the dispatcher (NOT the side-door) → real DOCX + MD whose content derives from the run's collateral (AC-2), NO tejal leakage incl. header (AC-1), ≥1 figure, ≥3 Bloom-spanning exercises, and **≥1 real resolvable-DOI `research_entry`** in S6 that **traces to the `research_wiring` envelope contribution — NOT a `_FURTHER_READING` constant DOI** (Murat L1) — content-inspected, `https://doi.org/{DOI}` resolves, G1/G2/G3/AC-5/AC-8 pass, **the live judge inspects the header for the tejal `unit_id`** (Murat L3). First-run-stands; frozen judge; foreground/watchdog; cleanup APPEND. **Precondition:** Irene must emit a usable `collateral` (+ ≥1 research_goal) on the fresh run BEFORE judging — a zero-collateral run is INVALID → re-run, not a fail. **L2 (no degrade alibi):** empty S6 section on a **creds-present** run is a **FAIL** (DOI clause unmet), not an honest-degrade pass. **One live lane + one deterministic degrade twin** (Murat) — the degrade RENDER branch is the AC-4 negative twin, not a second live run. **Quality guardrail (John A2 — tightened):** scaffold prose is acceptable ONLY for the S2 re-voiced-narrative section + word-form numerals; the depth-delta narrative, objectives, exercises, figures, and DOIs MUST be real (AC-1/2/4/8) and are NOT covered by the guardrail.

## RED-first plan
0. **T1 (FIRST — full-tree sweep, AC-6):** mechanical token-sweep (incl. `present-trends` + `Unit ID:`) + clean-worktree baseline. 1. AC-1 no-tejal-leakage teeth on both paths (RED). 2. AC-2 collateral-provenance + precedence witnesses. 3. AC-3 declaration fail-loud + no-op-skip twin. 4. AC-4 research_entries under G2 (RED: `()` renders the note; RED: corrupt a research source_ref → G2 must fail). 5. AC-5 `kind`. 6. migrate every dormant producer test + causation-isolation (zero new reds). 7. AC-L live in-graph proof (orchestrator). **First dev tasks prioritize the RED sets for F-2601/F-2602/F-2603 (Codex).**

## Out of scope (Phase-1 fence)
Learner-ready re-voiced prose (`workbook-learner-ready-prose-uplift`, S8) · word-form-numeral gate (`braid-workbook-wordform-numeral-gap`) · semantic claim↔source audit (`braid-workbook-l2-semantic-claim-citation-audit`) · the `collateral → ComponentSelection` derivation edge · course/SME registry · `course_purpose` inputs · the collateral projector family — all Phase-2 · any `pipeline-manifest.yaml` edit (Tier-1) · Irene producer edits (Irene owns rationale — S7 consumes) · S8 composed proof · rebuilding the research_entries→workbook contract (AC-D-pinned; S7 populates + brings it under G2).

## AC-L corpus — RESOLVED (operator-named 2026-07-07): Tejal **Part 3**
**Corpus:** `course-content/courses/tejal-c1m1-p3-opportunity/part3-opportunity-clinician-innovator.md` — Tejal's Module-1 **Part 3: The Opportunity & The Clinician as an Innovator** (a DIFFERENT lesson from the baked-in Part 2 "Trends"; same SME/course — NOT a new-SME onboarding, which is Phase-2). Extract Part 3 "as with Part 1 and Part 2" (slide descriptions + detailed design notes for most slides; discussion questions / Knowledge Checks parsed and used if/as needed). Proves producer-generalization off the specific frozen lesson.
**LO derivation (operator guidance — binding for the live run):** Part 3 has **no native LOs**. Derive them either (a) inferred from the Part-3 Knowledge Checks (5 Bloom-tagged Qs w/ answers + explanations in the source), or (b) from the **10 Core Conceptual Pillars of Module 1** (below). This feeds Irene Pass-1 / the G0R LO-ratification gate on the live run.
**The 10 Core Conceptual Pillars of Module 1 (operator-provided; LO source for the Part-3 run):**
1. **The Intrapreneurial Mandate** — innovating *inside* the hospital (navigating politics/budgets/legacy systems), not a standalone startup.
2. **Kind vs. Wicked Environments (Epstein's Range)** — clinical medicine (Kind: repetitive, clear rules) vs. healthcare innovation (Wicked: ambiguous, changing variables).
3. **Functional Flexibility** — abandon rigid specialty tools; borrow frameworks from operations, business, psychology.
4. **Clinical Diagnosis vs. Systemic Diagnosis** — don't buy a million-dollar system without diagnosing the operational root cause.
5. **Falling in Love with the Problem** — the core ethos; innovators fail when they love their *solution* and ignore the *problem*.
6. **Symptom vs. Root Cause** — separate visible pain (ED wait times) from invisible system failure (misaligned staffing).
7. **The "Lead Domino" Concept** — solving the true root cause knocks down downstream symptoms; finding it is the goal of problem framing.
8. **The "5 Whys" Framework** — the module's primary operational tool (ask "Why?" five times to a root cause).
9. **The "Opportunity Radar" (Past, Present, Future)** — audit past frustrations, present workarounds, future trends.
10. **Psychological Safety as a Prerequisite** — you can't diagnose a broken system if staff fear retaliation; safety precedes innovation.
**DOI-leg note:** Part 3 is more conceptual than Part 2 (cites a business book + a blog, fewer journal-empirical claims) — the resolvable-DOI leg MAY not fire. Per AC-L precondition, a genuine "no research_goal" outcome on this conceptual corpus is a valid generalization witness (the DOI-populated case is already S6-proven on tejal); it is NOT a producer fail. If the operator wants the DOI leg witnessed in the same run, a more empirically-citable different lesson can be named instead.

## Close-language honesty guard (John A3 + Codex SOP-034 P1 — binding)
The S7 close / party record / status lines state **"producer-generalization proven off the frozen `tejal-apc-c1-m1-p2-trends` LESSON — first workbook produced from a DIFFERENT lesson (Tejal Part 3, `tejal-c1m1-p3-opportunity`) in-graph."** ⛔ Do **NOT** claim **"first non-tejal workbook"** — the AC-L corpus is a different LESSON of the SAME SME (Tejal), not a genuinely non-Tejal corpus. It proves generalization off the specific baked lesson; **cross-SME/cross-course ("genuinely non-Tejal") generalization remains Phase-2** (`course-and-sme-registry`; no non-Tejal corpus exists on disk). Also NOT "learner-ready workbook" (S2 re-voiced prose + the two gate gaps stay named-deferred with their follow-on IDs).

## T11 gates
Monitor pre-dispatch poll DONE (Codex SOP-026 CONCUR-WITH-FINDINGS + party 4/4 — all folded) → FRESH dev RED-first → monitor dev-poll + 3-lane `bmad-code-review` → RED-first remediation → orchestrator re-verify → **AC-L LIVE (first off-frozen-lesson in-graph workbook w/ real DOI under G2) PASS** → commit (strays excluded) → monitor close poll → **fully-spawned bmad-party-mode concurrence (S7 Phase-1 close)**. Story flips done ONLY after the live AC-L PASS.
