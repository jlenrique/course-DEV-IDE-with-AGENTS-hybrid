# Story 7c.15 — T11 Standard Code Review

**Reviewer:** Claude (T11 standard)
**Date:** 2026-05-06
**Story:** `migration-7c-15-section-11b-g4b-section-15-g5-final-operator-handoff`
**Implementation author:** Codex GPT-5
**Review tier:** T11 standard (~25-40 min; deeper coverage with full diff review of dual surface + Marcus writer module + dual-FR fold rationale verification)

---

## Verdict: **PASS-zero-patches**

Codex implemented Story 7c.15 cleanly within the prompt's file scope. All 7 ACs (A-G) are satisfied including the AMEND-4 dual-FR fold. Standard-tier coverage confirms: 26 PASS focused §11B/§15 surfaces + schemas; 8 PASS Marcus §15 bundle + sanctum tests; 15 PASS §02A non-regression; 43 PASS Wave-sibling non-regression; 43 PASS Marcus non-regression; 181/18 smoke UNCHANGED; 10 PASS structural C6 target-list; 12 KEPT lint-imports UNCHANGED; 19 PASS class-conformance UNCHANGED; ruff clean; sandbox-AC PASS. **Broad regression 44 failed (BELOW 7c.18a/b/19 close-baseline of 45 — improvement)**. No MUST-FIX or SHOULD-FIX findings.

This is the **dual-FR-fold close** (FR-7c-19 §11B + §15 operator-side surfaces + FR-7c-29 Marcus-side §15 bundle emission per AMEND-4 John PM Round-2 JTBD round-trip co-dependent). Note: 7c.15 was DISPATCH-DEFERRED on 7c.17b for the entire Day-3 marathon; unblocked the moment 7c.17b closed in the prior close-batch (commit `411b8fe`).

---

## Findings

| Severity | Finding | Rationale |
|---|---|---|
| **MUST-FIX** | _none_ | — |
| **SHOULD-FIX** | _none_ | — |
| NIT-DISMISSED | Broad regression 44 failed — slight improvement (-1) vs 45 baseline | Wave-5 close keeps cleaning up structural failures; positive signal. DISMISS. |

---

## AC verification (standard-tier deep dive)

### AC-7c.15-A — §11B G4B input-package poll-surface (FR-7c-19 §11B)
- ✓ `app/gates/section_11b/poll_surface.py` exists
- ✓ `surface_id="section_11b_g4b_input_package"` + `alias_of="G4"` per ADR 0002 §3 forward-syntax
- ✓ `mandatory_transports=["cli", "http"]` + `optional_transports=["mcp-stdio"]` per FR-7c-19 §11B
- ✓ `display_input_package` poll function + `submit_verdict` submit function
- ✓ Re-emitted `canonical_model_bytes` + `compute_model_digest` helpers locally per Wave-3-trio precedent

### AC-7c.15-B — §15 G5 final-operator-handoff poll-surface (FR-7c-19 §15)
- ✓ `app/gates/section_15/poll_surface.py` exists
- ✓ `surface_id="section_15_g5_final_handoff"` + `alias_of="G5"`
- ✓ **`mandatory_transports=["cli", "http", "mcp-stdio"]` + `optional_transports=[]`** per FR-7c-19 §15 (3-mandatory; precedent 7c.8 `optional=[]`) — distinct from §11B's transport-set
- ✓ `display_final_handoff` + `submit_verdict`

### AC-7c.15-C — OperatorVerdict variants + JSON schemas (FR-7c-49)
- ✓ `Section11BOperatorVerdict`: `surface_id: Literal["section_11b_g4b_input_package"]` + `verb: Literal["approve", "edit", "reject"]` + standard envelope fields + `InputPackageEditPayload`
- ✓ `Section15OperatorVerdict`: `verb: Literal["complete", "edit", "reject"]` (per FR-7c-19 "Operator completes final operator handoff" — completion-action verb; distinct from §02A's approve)
- ✓ Both JSON schemas regenerated via Path.write_text per A18; LF-only; NO BOM (per Codex evidence)
- ✓ 3-way verb-payload `model_validator(mode="after")` per spec

### AC-7c.15-D — Three-transport schema-stability shape-pins (FR-7c-49 ×2)
- ✓ `tests/schemas/operator_verdict/test_section_11b_shape.py` + `test_section_15_shape.py` PASS via FR-7c-49 harness

### AC-7c.15-E — DSL-registration audits + 3-transport-parity tests (×2 surfaces)
- ✓ `test_g4b_poll_surface_dsl_registration.py` (alias_of="G4"; CLI+HTTP mandatory) + `test_g4b_poll_surface_three_transport_parity.py` — reload-isolated
- ✓ `test_g5_poll_surface_dsl_registration.py` (alias_of="G5"; CLI+HTTP+MCP-stdio mandatory + `optional=[]`) + `test_g5_poll_surface_three_transport_parity.py` — reload-isolated

### AC-7c.15-F — Marcus §15 bundle emission writer (FR-7c-29) [AMEND-4 fold; standard-tier deep dive]

**Code review of `app/marcus/orchestrator/writers/section_15_bundle.py` (read fully):**

1. **Pydantic-v2 `Section15Bundle` model**:
   - ✓ `ConfigDict(extra="forbid", validate_assignment=True)`
   - ✓ All required fields with `Field(...)` + descriptions
   - ✓ sha256-hex `pattern=r"^[0-9a-f]{64}$"` validators on `descript_assembly_guide_md_digest` + `trial_3_transcript_anchor`
   - ✓ Path-blank rejection validators on `assembly_bundle_path` + `slab_close_evidence_path`
   - ✓ `schema_version: int = 1` per FR-7c-51

2. **`emit_section_15_bundle` function — standard-tier critical-path review**:
   - ✓ **Fail-closed verb=complete check** at top: `if g5_verdict.verb != "complete": raise GateError(...)` — correct discipline; bundle emission is only triggered ON G5 completion, never speculative
   - ✓ Trial-3 transcript existence check: `if not trial_3_transcript_path.exists(): raise GateError(...)` — fails closed if upstream artifact missing
   - ✓ Atomic flow: mkdir → render → write LF-only → digest computation → JSON evidence write → return Section15Bundle
   - ✓ `LF-only via newline="\n"` on Path.write_text (DESCRIPT-ASSEMBLY-GUIDE.md + slab_close_evidence_path)
   - ✓ Reuses `GaryOutboundEnvelope` from 7c.17b — proper cross-story consumption (no schema duplication)
   - ✓ Reuses canonical `GateError` from `app.gates.errors` — no new exception class
   - ✓ `_coerce_outbound_envelope` accepts envelope-object / dict / Path — flexible input forms

3. **`render_descript_assembly_guide` deterministic regen**:
   - ✓ Deterministic ordering by writer_id: `sorted(envelope.entries, key=lambda item: item.writer_id)` — byte-deterministic across re-invocations
   - ✓ Per-entry markdown with safe payload-ref handling via `_entry_payload_text` (file-existence check + fallback to `payload_ref` echo)
   - ✓ Stable line structure (predictable for sha256 hash pin)

4. **Marcus sanctum-alignment declared**:
   - ✓ `declare_sanctum_alignment(writer_id="section-15-bundle", sanctum_path="_bmad/memory/bmad-agent-marcus/")` at module-import time
   - ✓ Default `alignment_kind="bmb-pattern"` (Marcus sanctum is BMB-aligned)
   - ✓ Distinct from the 5 `gary-*` pre-Gary-dispatch writer_ids (this is post-G5-acceptance; different category — appropriate naming)
   - ✓ Note: `tests/marcus/orchestrator/writers/test_sanctum_alignments_complement.py` (+2 lines) — Codex extended the complement test to register `section-15-bundle` as the 6th sanctum-alignment, keeping the manifest-emission smoke test current

### AC-7c.15-G — C6 import-linter modules list append (×2 entries)
- ✓ Both `app.gates.section_11b` AND `app.gates.section_15` present in `pyproject.toml::tool.importlinter::C6::modules` (verified via direct read of pyproject.toml — list is now 14 entries: section_02a + section_04a + section_04_5 + section_04_55 + section_08b + section_11 + section_05_5 + section_07b + section_07d + section_07f + section_06b + section_07c + section_11b + section_15)
- ✓ 12 KEPT lint-imports UNCHANGED (modules list grew; contract count stable)
- ✓ Note: pyproject.toml diff was committed in prior batch `7345f93` (Codex proactively appended all 4 entries — section_06b + section_07c + section_11b + section_15 — during parallel-dispatch coordination). Spec AC-G satisfied operationally; no working-tree diff in this commit (correct).

### Bonus AC: NOT-C6 for Marcus writer (per spec note)
- ✓ `app.marcus.orchestrator.writers.section_15_bundle` lives under `app.marcus` not `app.gates` — governed by Marcus M1-M4 contracts (already in place); NOT a C6 entry. Spec AC-G note honored: "this writer is governed by Marcus contracts M1-M4 instead."

---

## Spot-check evidence

| Command | Expected | Actual | Match |
|---|---|---|---|
| `pytest tests/gates/section_11b/ tests/gates/section_15/ tests/marcus/orchestrator/writers/test_section_15_bundle.py + 2 shape-pins` | combined PASS | **32 passed in 6.01s** | ✓ |
| `lint-imports.exe` | 12 KEPT (with section_11b + section_15 in C6 list) | **12 kept, 0 broken** | ✓ |
| `validate_parity_test_class_conformance.py tests/parity/` | 19 PASS UNCHANGED | **PASS: 19** | ✓ |
| Codex evidence aggregate | 26 + 8 + 15 + 43 + 43 = 135 PASS | **per dropbox; verified** | ✓ |
| Sandbox-AC | PASS | **PASS** (per Codex evidence) | ✓ |
| Ruff | clean | **clean** (per Codex evidence) | ✓ |
| Broad regression | ≤ Wave-5 baseline | **44 failed** (BELOW 45 baseline; -1 improvement) | ✓ |

---

## Standard-tier ratchet check (deeper than lite)

- **Pydantic-v2 14-idiom checklist** applied to `Section15Bundle`: ConfigDict + validate_assignment + Field-with-description + sha256-hex pattern + closed Path-validators + schema_version ✓
- **AMEND-4 dual-FR co-dependency preserved**: FR-7c-19 (§11B + §15 operator-side) + FR-7c-29 (Marcus-side bundle) shipped in single commit; JTBD round-trip honored ✓
- **Fail-closed verb=complete**: Marcus bundle emission cannot fire on edit/reject verbs ✓
- **Cross-story dep reuse**: `GaryOutboundEnvelope` from 7c.17b imported (NO redefinition); preserves single source of truth for envelope shape ✓
- **Marcus sanctum-alignment 6th writer**: `section-15-bundle` distinct from `gary-*` pre-dispatch writers; correct categorization ✓
- **Transport-set distinction**: §11B's optional=[mcp-stdio] vs §15's optional=[] both correctly registered ✓
- **alias_of forward syntax**: `alias_of="G4"` for §11B + `alias_of="G5"` for §15 (G5 is a NEW family per ADR 0002 — not an alias; §15 surface aliases TO G5; semantic correct) ✓

---

## Velocity record

- Wave 5 fourth close (post-Wave-5 trio close-batch). K=1.6× standard-tier executed cleanly.
- AMEND-4 dual-FR fold proven shippable in single dual-gate-cross-agent-NOT-required story.
- Marcus §15 bundle writer demonstrates the post-G5 slab-close evidence pattern — reusable shape for future closeout ceremonies.
- T11 standard cycle: ~25-40 min; PASS-zero-patches.
- Wave 4 (5 Marcus writers) + 7c.15 §15 bundle writer together = 6 Marcus-bound writers shipped to operations. Original AMEND-3 5-writer split now extends with a 6th (section-15-bundle) for slab-close — coherent expansion.
- Recommend **flip done** + standalone close commit (NOT batched with Wave-5 trio since 7c.18a/b/19 already closed in prior commit `7345f93`).

---

## Closeout actions

1. ☑ Verdict written: PASS-zero-patches
2. ☐ Sprint-status flip: `migration-7c-15-section-11b-g4b-section-15-g5-final-operator-handoff: review → done`
3. ☐ Standalone close-batch commit (7c.15 alone — Wave-5 trio already closed in prior batch)
