# Story pre-S5 — g0-enrichment battery baseline (the BINDING S5-open precondition)

**Arc:** Canonical Production Conversation (party record **§8** S2-close addendum — binding precondition: *S5 may NOT open until the g0-enrichment substrate baseline is green*). Gated after S4 (DONE `63369c99`).
**Status:** **DONE** (2026-07-07 session 18; SOP-014 CONCUR-W-F → fresh dev RED-first → orchestrator re-verify: **18→2** with exactly the 2 named pre-existing drift reds remaining (`pack_hash` + `roster_count`, both filed follow-ons, untouched), 466 passed, `git diff -- app/` + manifest EMPTY, fresh-clone/throwaway-worktree GREEN (88 passed on the copied fixture+pin), ruff 0-new, pin refreshed to the full exact tuple. `studio-smoke-min/lesson.md` = 1 H1 + 3 H2 + 3 body lines → 4 typed components, 1 provisional LO, tracked (not gitignored). **S5-open precondition SATISFIED — S5 unblocks.** Offline-only: no live witness (S5 owns the live G0 proof); no 3-lane (no production code — SOP-014 + orchestrator re-verify are the proportionate independent verification per §3 intent).) · **Size:** XS (test-fixture + one test-pin refresh; NO production-code change expected) · **Gate mode:** single-gate structural; **offline-only** (no live API surface — the battery runs `dispatch_live=False`; S5 owns the live G0 proof) · **Branch:** `dev/workbook-2026-07-06`.
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, `-n 0`, **no `git stash`** (throwaway worktree for baseline). Do not commit — orchestrator commits after re-verify.

## Why (party record §8, verbatim binding)
S5's default-flip witnesses are meaningless against a red, fixture-less baseline. The 13 `test_g0_enrichment_brick.py` reds + the stale gate-inventory pin must go green from a fresh clone before S5's spec may be authored. This story is that precondition — nothing more (SOP-001's "specs authored on stale premises" lesson applied prospectively).

## Current RED baseline (verified 2026-07-07 at `63369c99`; SOP-014 re-counted — 18 reds at HEAD, not the "~21" prior estimate)
The two governing dirs (`tests/integration/marcus/` + `tests/unit/marcus/cli/`) show **18 reds**, by cause:
- **15 corpus-absence reds** (ALL fixture-resolved by D1's `lesson.md`): **13** in `tests/integration/marcus/test_g0_enrichment_brick.py` (all `FileNotFoundError`/`DirectiveCompositionError: …/course-content/courses/studio-smoke-min`, `production_runner.py:2346`) **+ 2** in `tests/integration/marcus/test_irene_refinement_brick.py` (SOP-014 F-1401 — same `studio-smoke-min` cause: `test_ac_s3_6_wiring_reads_gate1_provisional_los_and_refines` (composer `DirectiveCompositionError`) and `test_full_two_gate_offline_refines_and_ratifies` (`FileNotFoundError`)). These two ARE the two-gate offline refine+ratify path — they are the concrete evidence that discharges the `b59679ce` "T11 owed" tail, so they MUST be in the green set. The corpus was never committed (party record §8 cause (a)); the untracked `course-content/courses/tejal-c1m1-studio-min/` holds a `.docx` (gitignored by `.gitignore:77` anyway), NOT the `lesson.md` the tests require — CREATE, not re-point.
- **1 stale-pin red** in `tests/unit/marcus/cli/test_shim_parser_factory.py::test_active_terminal_gates_canonical_inventory` — the code's `ACTIVE_TERMINAL_GATES` (`_shim_parser.py:26-27` = `("G0E","G0R","G1","G2B","G2C","G3","G4A","G4")`) legitimately ADDED `G0R` (G0-S3 Irene LO ratify-gate #2, `b59679ce` 2026-06-26); the pin was maintained at `e5e24ad3` (when G0E landed) but NOT at `b59679ce` → **code current, test pin stale** (§8 cause (c)).
- **2 INDEPENDENT pre-existing drift reds — NOT this story's job, expected residual after the fix (SOP-014 F-1402):** `test_front_door_selection_threading.py::test_run_summary_pack_hash_default_is_byte_identical_to_raw` (manifest pack_hash mismatch `54c398a…`≠`e0e52b9…`) and `test_run_summary_yaml_emit.py::test_clean_trial_run_summary_populated` (`specialist_roster_count 17≠12` roster drift). These reach REAL assertions (independent of `studio-smoke-min`). **The dev MUST NOT touch them** (see AC-4/AC-5). Filed as follow-ons.
- The `b59679ce` "T11 owed" tail (cause (b)) is discharged PRACTICALLY by greening the battery from a fresh clone (the 13 g0-brick + the 2 irene-brick two-gate tests exercise the g0-enrichment + G0R substrate). A full formal re-review of the 676-line `irene_refinement.py` is OUT OF SCOPE of this precondition (§8's "small pre-S5 story").

## Deliverables
### D1 — Create + commit the `studio-smoke-min` corpus fixture
`course-content/courses/studio-smoke-min/lesson.md` — a minimal synthetic outline: **exactly 1 H1 + 3 H2 sections, each H2 with ≥1 verbatim body line** (so component split → ≥4 typed components with real excerpts, and the offline pre-pass extracts ≥1 provisional LO with resolvable source-ref provenance + quoted spans). The tests define the exact shape — the dev works it RED-first. Constraints the fixture MUST satisfy (from the battery):
- `build_enrichment_result(corpus_dir=studio-smoke-min, dispatch_live=False)`: `typed_components ≥ 4`, each with `parent_source_id` ∈ enumerated files + `component_id` + `locator` (heading breadcrumb) + `excerpt` (verbatim body line, or the heading label when bodyless); component ids unique.
- `provisional_los` non-empty; each LO `status=provisional`, `adequacy is None`, every `source_ref.source_id` ∈ `enumeration_provenance` ids with a non-empty `quoted_span`.
- `reconcile`: `n_files_in == len(enumeration_provenance)`, `n_files_covered == n_files_in`, `n_files_ignored == 0`, `n_components == len(typed_components) ≥ n_files_in`.
- `traversal_roots[0].kind == "corpus_dir"`; each `enumeration_provenance` entry has `root_id` + `locator`.
- Dissent: a real run-level dissent present (`against`/`marcus_position` non-empty); its fingerprint-derived `marcus_position` differs from `tests/fixtures/trial_corpus`'s (distinct corpus fingerprint — automatic from distinct content).
- `start_trial`/`run_g0_enrichment(corpus_dir=..., dispatch_live=False)` succeeds through the G0E confirm-gate (the woken-gate + cache tests use the real trial machinery on this corpus).
- **A one-file corpus is sufficient** (the tests say "the lone outline file"); do NOT over-build. Keep it instruction-neutral synthetic content (not real lesson prose — this is a smoke fixture).
- Commit under `course-content/courses/` (the canonical corpus path); ensure it is TRACKED (not gitignored) so a fresh clone greens.

### D2 — Refresh the stale gate-inventory pin
Update `test_shim_parser_factory.py::test_active_terminal_gates_canonical_inventory`'s expected tuple to match the current code (`("G0E","G0R","G1","G2B","G2C","G3","G4A","G4")`) — G0R was a legitimate 2026-06-26 addition. This is a TEST-PIN refresh to current substrate, NOT a code change; the test must still assert the FULL exact inventory (extend the pin, don't weaken it to a subset).

## Acceptance criteria
- **AC-1:** all **15 corpus-absence reds** GREEN via the committed fixture — the **13** `test_g0_enrichment_brick.py` tests **AND the 2** `test_irene_refinement_brick.py` tests (`test_ac_s3_6_wiring_reads_gate1_provisional_los_and_refines`, `test_full_two_gate_offline_refines_and_ratifies`) (SOP-014 F-1401). No test edited to pass — the fixture satisfies the assertions honestly; the monitor verifies non-gaming.
- **AC-2:** `test_active_terminal_gates_canonical_inventory` GREEN via the pin refresh; the pin still asserts the exact full tuple (not loosened).
- **AC-3:** the fixture is TRACKED + greens **from a fresh clone / throwaway worktree** (party record §8's "from a fresh clone" — the whole point; verified not gitignored — `.md` is not excluded, SOP-014 F-1405).
- **AC-4:** no regression AND honest residual accounting — after the fix, the ONLY remaining reds in `tests/integration/marcus/` + `tests/unit/marcus/cli/` are **exactly the 2 pre-existing independent drift reds** (`test_front_door_selection_threading::test_run_summary_pack_hash_default_is_byte_identical_to_raw` + `test_run_summary_yaml_emit::test_clean_trial_run_summary_populated`, SOP-014 F-1402). Re-count and confirm **18 → 2**. These 2 are **OUT OF SCOPE and MUST be left untouched** — do NOT edit the pipeline manifest or the specialist roster to green them (that is a lockstep-regime path and an AC-5 violation); they are filed as separate follow-ons.
- **AC-5:** NO production-code change. If the dev finds one is genuinely needed to green a corpus-absence test honestly, STOP and report — that would mean the substrate, not the fixture, is the gap, escalating scope (and would trip the pipeline-lockstep regime).

## RED-first plan
1. Confirm the 15 corpus-absence reds (13 g0-brick + 2 irene-brick) + 1 pin red RED at HEAD (`-n 0`); note the 2 pre-existing drift reds as the expected untouched residual.
2. Create `studio-smoke-min/lesson.md`; iterate against BOTH `test_g0_enrichment_brick.py` and `test_irene_refinement_brick.py` until all 15 green (RED→GREEN per assertion).
3. Refresh the pin; confirm green.
4. Fresh-clone / throwaway-worktree verify (AC-3); confirm the residual is EXACTLY the 2 drift reds (18 → 2), untouched.

## Out of scope
S5 G0-canonical default-flip (this only greens the baseline) · live G0-enrichment dispatch (S5) · full formal T11 re-review of `b59679ce`'s `irene_refinement.py` (discharged practically by the green battery; follow-on only if a real gap surfaces) · re-pointing to `tejal-c1m1-studio-min` (wrong shape — a `.docx`, not a structured `lesson.md`) · any production-code change (AC-5).

## T11 gates (proportionate — XS, offline, test-fixture only)
SOP-014 monitor pre-dispatch poll (verify the fixture requirements are correctly read + the pin refresh is legitimate-not-gamed) → FRESH dev RED-first → orchestrator re-verify (13+1 green, fresh-clone green, no regression, NO production-code diff, ruff clean) → SOP-015 close poll → commit (strays excluded). No live witness (no runtime API surface; S5 owns the live G0 proof). No 3-lane review (no production code to adversarially hunt; the two monitor polls + orchestrator re-verify provide proportionate independent verification per §3 intent; escalate to 3-lane only if AC-5 trips).
