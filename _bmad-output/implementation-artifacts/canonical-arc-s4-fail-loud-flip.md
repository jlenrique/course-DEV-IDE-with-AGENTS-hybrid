# Story S4 — FAIL-LOUD flip (styleguide-less WARN-seed → hard failure; parity divergence WARN → ERROR)

**Arc:** Canonical Production Conversation (party record §1 S4 row + §7 re-scope S4 row; gated on **S1 ∧ S2 ∧ S3**, all CLOSED live-proven).
**Status:** **DONE** (2026-07-07 session 18; full gate chain: SOP-011 CONCUR-W-F → fresh dev RED-first (all offline ACs witnessed) → SOP-012 CONCUR-W-F + 3-lane T11 (Auditor PASS all ACs / Blind clean / Edge 1 SHOULD-FIX; AC-9 teeth PRESERVED, independently confirmed) → RED-first remediation R1-R6 (the false "both envelopes ride the receipt" divergence message → now carries all five digest values + honest halt-path survivability) → orchestrator re-verify (parity.py + production_runner.py frozen, 348 green, ruff 0-new, lint-imports 16/0) → **AC-L LIVE 3-LEG WITNESS: ALL PASS, frozen judges verbatim, first-run-stands, NO anomalies** — Leg-3 happy 15/15 (real Gamma deck `npkNxi1NwrKbfPj3NU61I`, `ok/match/clock_eligible=true`, walk proceeded — anti-outage proven), Leg-2 divergence→ERROR 16/16 (the EXACT S3 rewind-recover that WARN-proceeded now HALTS `paused-at-error/gamma.styleguide.parity-divergence`, $0, same-bytes divergence with all three directive digests equal), Leg-1 unbound 12/12 (`paused-at-error/gamma.styleguide.unbound`, names variant A, $0); evidence `s4-acl-liveproof-20260707T041058Z`; spend 1 deck + ~$0.32) · **Size:** S · **Gate mode:** single-gate structural (3-lane T11) · **SOP-011 findings F-1101/F-1102 applied pre-dispatch; F-1103/F-1104 honored** (two localized action-flips; NO comparator-logic change) · **Gate mode:** single-gate structural (3-lane review at T11) · **Branch:** `dev/workbook-2026-07-06` (S1 @ `c24308f7`, S2 @ `037be6d3`, matcher @ `59a9a48a`, S3 @ `7630d091`, session-17 wrapup @ `13792617`).
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, xdist rule (`-n 0` confirm before triaging new parallel reds), **no `git stash` ever** (baseline via throwaway worktrees). Do not commit — orchestrator commits after the live AC-L PASS.

---

## The one-sentence scope

S4 changes **two ACTIONS on already-classified conditions** — nothing else. It flips (A) Gary's styleguide-less `DEFAULT_VARIANT_PAIR` honesty **WARN-seed → a pre-spend hard failure**, and (B) the S3 parity **`divergence` outcome from WARN-and-proceed → a pre-spend hard failure (error-pause)**. Both raises fire **before any paid Gamma dispatch**. The S3 comparator (`app/styleguide/parity.py`) and its trichotomy are the **frozen input contract** — S4 imports its verdict and acts on it; it does not re-classify. The **authority S-flip stays deferred** (parity clock at tick 1 of 3-clean+1-cross-deck; Gary keeps its own resolve authority).

---

## SOP-011 obligations — discharged in this spec (the four the monitor will verify)

**(a) — the E4 defer, explicitly weighed (does the flip require full THREE-way attestation, or does two-way suffice?).**
E4 (S3 Review Findings, deferred): `clock_eligible=True` currently accrues on **two-way** matches — a gateless start / single-file trial writes `directive_digest: null`, so `trial_start_directive_digest` is `None` and the comparator's F-703 `comparable` set is `{cd_directive_digest, gary_directive_digest}` (two members collapsing to one on a match). The routed question: should the S4 flip **require** the full three-way attestation (trial-start digest present + equal) before it fires?

**Ruling weighed for S4: TWO-WAY SUFFICES; S4 adds NO new attestation gate; the three-way clock-strength question stays with the deferred S-flip story.** Reasoning, on the record for SOP-011/party ratification:
- The parity **ERROR** flip (Flip B) fires on the **`divergence` outcome**, which the S3 comparator only ever returns when the directive digests it *can* compare are **equal** (F-703: any disagreement among comparable digests classifies as `expected-ordering-gap/directive-drift`, never `divergence`). A `cd_directive_digest == gary_directive_digest` match already proves the directive **bytes were identical at CD-authoring read and Gary-dispatch read** — which is *exactly* the condition that makes a resolution disagreement a genuine Gary-vs-CD defect. The trial-start digest is a **third** attestation that strengthens the S-flip **clock** (accumulating trust for the *authority* flip); it is **not** necessary to call a same-bytes resolution disagreement a real defect worth halting on.
- Requiring three-way for the ERROR would **leave gateless / single-file production runs unprotected** against a genuine resolver divergence — the *opposite* of fail-loud intent, and a silent hole exactly like the F-801 two-way-degrade the monitor caught in S3.
- **`clock_eligible` semantics are NOT touched by S4** (it is an S-flip concern; parity.py is byte-frozen — obligation b). S4's ERROR rides the comparator's existing (S3-ratified) `divergence` classification; it neither reads nor tightens `clock_eligible`. The three-way-vs-two-way *clock-strength* decision therefore remains open **for S-flip**, where it belongs (S-flip's entry gate already demands "zero digest divergences" across 3-clean+1-cross-deck — the natural place to decide whether a null trial-start digest disqualifies a clean run from *counting*).

Alternative (recorded, not adopted): tighten the ERROR to fire only on three-way-attested divergence, downgrading two-way-attested divergence to WARN. Rejected because it re-opens the F-801 silent-two-way-degrade hole on the *control* plane and weakens fail-loud on gateless runs. **SOP-011 + the green-light party may overturn this ruling before dev dispatch.**

**(b) — scope == the §7 re-scoped S4 row, with the committed v1 receipt schema cited as the input contract.**
Scope is verbatim the §7 S4 row: *"Flips: Gary's `DEFAULT_VARIANT_PAIR` WARN-seed → hard failure; parity WARN → ERROR. The authority flip is NOT S4."* The **frozen input contract** S4 builds on is the COMMITTED `app/styleguide/parity.py` (S3 @ `7630d091`):
- the **13-key receipt** returned by `_receipt(...)`: `{schema_version, outcome, reason, cd_status, clock_eligible, cd_resolution_digest, gary_resolution_digest, cd_directive_digest, gary_directive_digest, trial_start_directive_digest, cd_bound_guides, gary_bound_guides, detail}`;
- the **10-key CD emission contract** `CD_STYLEGUIDE_RESOLUTION_V1_KEYS`;
- the **outcome trichotomy** `ok` / `expected-ordering-gap` / `divergence` with its `reason` sub-cases.
**S4 makes ZERO edits to `parity.py`** (no logic, no schema, no new field). It reads `receipt["outcome"]` and acts. The S3 determinism/AC-4 byte-identity pins inherit unbroken.

**(c) — F-705 WARN-seed ownership formally transfers to S4, WITH a byte-diff witness planned.**
S3 kept the styleguide-less WARN-seed **byte-untouched** and stamped "S4 owns the flip (F-705)." **S4 now OWNS it.** AC-2 (below) is the planned **byte-diff witness**: (i) the ONLY substantive change to the `else` branch of the styleguide seed loop in `_normalized_gamma_settings` is WARN-log-replaced-by-`raise`; (ii) the old honesty-WARN log record **no longer fires** on the styleguide-less path (asserted absent); (iii) the raise fires with the pinned tag; (iv) a `git diff` review confirms the change is localized to that branch (+ the parity-caller branch for Flip B) and touches no resolve/merge/dispatch bytes on the success path.

**(d) — the §10 deferred filings stay FILED, not silently absorbed.**
Both §10 filings remain in `deferred-inventory.md`, untouched by S4 and named in **Out of scope**:
- `gamma-dispatch-retry-taxonomy-deterministic-variance` — **relevant but NOT fixed here.** S4's new raises are **pre-spend** (before any paid Gamma call), so an auto-retry would burn **$0** and re-fail deterministically. S4's only obligation toward this filing is **AC-4**: assert the new fail-loud tags surface as a **deterministic error-pause**, not a retryable variance — i.e., S4 must not *newly* route a governance error into the retryable family. The taxonomy overhaul itself stays deferred.
- `run-artifact-split-across-runs-roots` — untouched, unrelated to the flip; stays filed.

---

## Context (landed substrate — re-verify at T1 against HEAD `13792617`; line numbers may drift)

- **Flip-A surface — Gary's styleguide-less honesty WARN-seed.** In `_normalized_gamma_settings` (`app/specialists/gary/_act.py`), the per-variant loop's styleguide seed has two branches: a variant naming a `styleguide` resolves the CD-owned library base (the `if styleguide_name is not None:` branch, ~:561-586); a variant with **no** `styleguide` key falls to the **`else` branch (~:587-597)** which logs `_LOGGER.warning("variant %s present with no bound styleguide; seeding from DEFAULT_VARIANT_PAIR base — fail-loud deferred to cd-envelope-authoring", ...)` and sets `merged = dict(by_variant[variant_id])` (the `DEFAULT_VARIANT_PAIR` smoke seed defined at ~:232). This whole method runs at the TOP of `generate_gamma_variants` (~:1114), **before** the paid dispatch loop (`for variant in variants:` ~:1135) — so a raise here is **pre-spend**.
- **Flip-B surface — the parity divergence action.** `_styleguide_parity_receipt` (`_act.py:~1019-1096`) computes the S3 receipt via `compare_styleguide_resolution` and logs by outcome (divergence → `_LOGGER.warning(... "observability-only — dispatch proceeds; WARN->ERROR flip is S4" ...)`, ~:1048-1056; gap → INFO; ok → silent). Its caller `generate_gamma_variants` invokes it at **~:1115**, immediately after `_normalized_gamma_settings` and **before** the variants dispatch loop (~:1135) — again **pre-spend**. The receipt already rides Gary's contribution as `styleguide_parity` (S3 D3); on `divergence` the receipt's `detail` carries **both envelopes** (CD block verbatim + gary_view) per S3 D3.
- **Trichotomy protects legacy/rewind bundles (F-802 inheritance).** A `None`/absent CD block classifies `expected-ordering-gap/cd-envelope-absent-legacy`; a newer schema classifies `expected-ordering-gap/cd-schema-newer`; a mid-walk directive mutation classifies `expected-ordering-gap/directive-drift`. **None of these are `divergence`** — so Flip B never fires on them, and rewind-recovered golden bundles / legacy envelopes still dispatch cleanly. This is why S4 can flip the whole `divergence` outcome without a legacy-tolerance carve-out.
- **`GaryActError` is the fail-loud vehicle.** The surrounding guards in `_normalized_gamma_settings` already `raise GaryActError(msg, tag=...)` (e.g. `gamma.settings.invalid`, `gamma.styleguide.unknown`) — a `GaryActError` becomes a specialist **error-pause** at the runner (deterministic, not retryable when tagged as a settings/governance error). S4's raises use the same vehicle.
- **§06 presence guarantee (inherited).** `run_builder_node` fails loud on a missing cd contribution (`builder.gary.upstream-missing`) before §07 dispatches — unchanged; S4 rides it.
- **Picker is canonical at trial-start (S2).** `run_picker_preflight` runs the Beat-1 ceremony at interactive trial-start (`037be6d3`); a pick is always OFFERED before the run proceeds. This is the precondition that makes Flip A safe rather than a "manufactured outage" (Winston, §7): flipping styleguide-less to fail-loud only after S2 means the operator always had a place to pick.

## Binding inheritances (carry into green-criteria; verify HONORED at SOP-011/SOP-012)

- **parity.py is FROZEN (obligation b).** No edit to `app/styleguide/parity.py`. All S3 pins on it stay green **unmodified** (the AC-1 trio, AC-2 status-keying, AC-3 drift, AC-6 contract teeth, AC-7 CD byte-stability).
- **F-705 — S4 owns the WARN-seed; byte-diff witness mandatory (AC-2).**
- **F-802 — legacy/rewind tolerance preserved by the trichotomy** (AC-5 witnesses it: a legacy/absent CD block dispatches CLEANLY post-flip, does NOT raise).
- **🔒 Protected invariant — source-detail→Gamma conveyance + VO↔on-screen** ([[feedback_source_detail_to_gamma_conveyance_protected]], [[feedback_vo_onscreen_alignment_protected_invariant]]): the flips are pre-dispatch control-flow only; on the SUCCESS (non-raising) path Gary's normalized settings + merged per-variant overrides + dispatched packet are **byte-identical to `7630d091`** (AC-3). No styling/keyword/prompt byte moves.
- **X3 — no new ceremony.** The flips raise into the EXISTING error-pause machinery; no new gate, pause surface, or HIL beat is introduced.
- **F-705 environment constraints** (public `read_pick_events` if any test touches the sidecar; `trial_id.hex` keys; carry the ~21 F-605 baseline reds in green-criteria — S4 neither fixes nor touches them; `-n 0` before triaging any NEW red).

---

## Deliverables

### D1 — Flip A: styleguide-less seed → pre-spend hard failure (F-705 cash-out)
Replace the `else` branch's WARN-and-seed (`_act.py:~587-597`) with a **fail-loud raise**:
```
raise GaryActError(
    "variant <X> is present with no bound styleguide; a present variant "
    "without a bound styleguide is a governance error (S4 fail-loud flip; "
    "the styleguide pick is canonical on every run)",
    tag="gamma.styleguide.unbound",
)
```
- The `DEFAULT_VARIANT_PAIR` constant (~:232) **stays defined** (it remains the offline smoke seed for tests and the `by_variant` projection map at ~:511-513; do NOT delete it — deleting it is out of scope and would ripple). Only the **production `else`-branch consumption** of it as a live seed is retired.
- Tag `gamma.styleguide.unbound` is NEW; it must classify as a **deterministic error-pause** (see D3), never a retryable variance.
- **Scope boundary — what Flip A does and does NOT close (SOP-011 F-1101, adopted narrow):** the raise lives in the per-item `else` branch, so it fires ONLY on a **variant that is PRESENT in `gamma_settings` but carries no `styleguide` key**. It does **NOT** fire when `gamma_settings` is **empty/absent** — that path falls back to `("A",)`/`("A","B")` (`_act.py:~1116-1118`) and dispatches with `variant_settings=None` (styleguide-less, no raise), and the parity comparator then classifies the both-pickless run `ok/status-keyed-no-picks` (a legitimate proceed, preserved by AC-5). **This is deliberate and honest:** S4 flips exactly the WARN-seed §7 names (the named-variant `else` branch); it does NOT expand into the empty/declined-pick default-A path. Broadening to a whole-dispatch "no variant may ship styleguide-less" chokepoint was **weighed and rejected** for this story — it would risk false-failing legacy/rewind-recovered styleguide-less bundles (violating the W5-morph "golden bundles must not false-fail", §7) and reaches beyond the named WARN-seed. The empty/declined-pick leg of §7 D-skip ("'No pick' is NEVER a skip path post-S4") is closed **separately and earlier** by a **deferred trial-start pre-check** (`styleguide-mandatory-pick-trial-start-precheck`, filed in deferred-inventory this session) — the correct home for it (fail before the run starts, not at §07), and out of S4's scope. `parity.py` stays total/tolerant, unchanged.

### D2 — Flip B: parity `divergence` → pre-spend hard failure (parity WARN → ERROR)
In `generate_gamma_variants`, immediately after the `styleguide_parity = _styleguide_parity_receipt(...)` call (~:1115) and **before** the variants dispatch loop, add:
```
if styleguide_parity.get("outcome") == "divergence":
    raise GaryActError(
        "styleguide parity DIVERGENCE (<reason>): CD's authoring-time "
        "resolution disagrees with Gary's dispatch-time resolution (directive "
        "attestations agree where both are present); halting pre-spend (S4 "
        "WARN->ERROR flip). Both envelopes ride the styleguide_parity receipt.",
        tag="gamma.styleguide.parity-divergence",
    )
```
- Fires on **all three `divergence` reasons** (`resolution-mismatch`, `cd-unresolvable-but-gary-resolved`, `contract-violation`) — the S3 trichotomy already reserved `divergence` for genuine defects; the tolerated states are `expected-ordering-gap` (stays INFO, proceeds) and `ok` (silent, proceeds). **No new carve-out needed.** (F-1103: the message says "attestations agree where both are present" rather than "same bytes" — `resolution-mismatch` is reachable with only one directive digest present, `parity.py:321-327/350-358`; the halt is still correct, the wording just doesn't overstate.)
- **`divergence/contract-violation` INCLUDES the total-comparator crash fallback — halting on it is INTENDED (SOP-011 F-1102 ruling, on the record):** the comparator is total/never-raise and folds any internal surprise (RecursionError, a latent `_json_safe`/`_classify` bug) into `divergence/contract-violation` (`parity.py:181-195`). Post-flip, such a comparator self-crash HALTS the run pre-spend rather than shipping on unverified parity. This is the intended fail-loud posture (§ *bundle carrier must be extraordinarily robust* — [[feedback_bundle_carrier_extraordinarily_robust]]): $0 pre-spend, `trial recover`-able, and a broken auditor SURFACES instead of silently passing. The comparator's "loud in data plane, silent in control plane" S3 stance is deliberately inverted **for the flip only** — `parity.py` itself is byte-unchanged. AC-4 witnesses the crash-fallback halt explicitly.
- The receipt still rides the contribution for triage; ADDITIONALLY, the divergence `reason` + the three digests + both-envelope `detail` must be reachable from the error context (put `reason` in the message; the receipt is already persisted on the contribution — confirm the error-pause path preserves the contribution-so-far, else attach a compact digest summary to the error). T1 verification task.
- **Log-line reconciliation:** update `_styleguide_parity_receipt`'s divergence branch (~:1048-1056) message — drop the now-false "dispatch proceeds; WARN->ERROR flip is S4" future-tense; the helper may keep an ERROR-level log for observability, but the **control-flow raise lives in the caller** (`generate_gamma_variants`), keeping the helper's return-a-receipt contract intact. Minimal-diff: helper logs, caller raises.

### D3 — Error taxonomy: new tags are deterministic error-pauses (obligation d interaction)
Both new tags (`gamma.styleguide.unbound`, `gamma.styleguide.parity-divergence`) must resolve to a **deterministic error-pause** at the runner, **not** the retryable-variance family that `gamma.export.brief-unmatched` wrongly sits in. **Verified feasible (SOP-011 may re-confirm):** the runner's retry gate is an **allowlist** — `_RETRYABLE_DISPATCH_TAGS` (`production_runner.py:~1790-1798`, currently exactly `{irene.pass2.slide-join-failed, gamma.export.brief-unmatched}`); `_invoke_specialist_with_retry` (~:1802) retries ONLY tags in that set and its comment states *"everything else fails loud immediately."* So both new tags land **non-retryable by default** — S4 adds NOTHING to the allowlist. AC-4 pins this (neither new tag ∈ `_RETRYABLE_DISPATCH_TAGS`). **Do NOT** refactor the retry taxonomy (that stays the filed §10 follow-on).

### D4 — E4 weighing recorded in-spec (obligation a)
No code deliverable — the E4 ruling above is the artifact. The dev agent makes **no `clock_eligible` change** and **no `parity.py` change**; if a reviewer or the monitor overturns the two-way ruling, that becomes a comparator-or-clock change and is re-scoped **out of S4** into S-flip. A one-line assertion pins intent: a test documents that `clock_eligible` is read by NEITHER flip (Flip A/B branch on the styleguide-key presence and the `outcome` field only).

---

## Acceptance criteria

- **AC-1 (Flip A — NAMED-variant-styleguide-less raises pre-spend; SOP-011 F-1101):** a payload with a variant **PRESENT in `gamma_settings` but carrying NO `styleguide` key** (NOT empty/absent gamma_settings — that path is out of scope, AC-5) ⇒ `_normalized_gamma_settings` raises `GaryActError(tag="gamma.styleguide.unbound")`; **zero** `GammaClient` generate/create-from-template calls (pre-spend witness — assert the generative methods are never invoked); the message names the offending variant. RED against HEAD (today it WARNs + seeds). **Companion negative:** empty/absent `gamma_settings` ⇒ NO `gamma.styleguide.unbound` raise (default-A proceeds; closed later by the deferred trial-start pre-check).
- **AC-2 (F-705 byte-diff witness — obligation c):** (i) the styleguide-less honesty-WARN **log record no longer fires** on that path (assert absent); (ii) the raise fires with the pinned tag; (iii) `DEFAULT_VARIANT_PAIR` is still importable/defined and the `by_variant` projection is intact (no accidental deletion); (iv) a diff-scoped test/inspection confirms the resolve/merge/success-path bytes are untouched.
- **AC-3 (observability→control only; protected invariant — success path byte-identical):** on the SUCCESS (all-variants-styleguided, `ok/match`) path, Gary's normalized settings, merged per-variant overrides, and dispatched packet are **byte-identical to `7630d091`** on identical inputs (golden-comparison witness); no styling/keyword/prompt byte moved by either flip.
- **AC-4 (Flip B — parity divergence raises pre-spend, deterministic error-pause):** a `divergence/resolution-mismatch` receipt (forced via CD-vs-Gary SSOT disagreement on the SAME directive bytes, per S3's AC-L leg-2 technique) ⇒ `generate_gamma_variants` raises `GaryActError(tag="gamma.styleguide.parity-divergence")` **before** any generative dispatch call; the raise carries the divergence `reason`; both envelopes remain reachable for triage; the tag classifies **deterministic error-pause, NOT retryable** — assert `"gamma.styleguide.parity-divergence" not in _RETRYABLE_DISPATCH_TAGS`. Parametrized over all three `divergence` reasons: `resolution-mismatch`, `cd-unresolvable-but-gary-resolved`, and **`contract-violation` — including the total-comparator crash fallback (F-1102): a fixture that drives the comparator to its crash path (e.g. an unserializable/recursive view value) yields `divergence/contract-violation` and MUST halt** (witnesses the intended halt-on-auditor-self-crash ruling).
- **AC-5 (tolerated states still proceed — F-802 + trichotomy):** each of `ok/match`, `ok/status-keyed-no-picks` (offline/legacy surface), `expected-ordering-gap/*` (all four reasons), and a `cd_styleguide_resolution: None` legacy/absent block ⇒ **NO raise from Flip B**, dispatch proceeds; a legacy/rewind-recovered envelope (cd contribution predating S1) dispatches CLEANLY (mirrors S3 AC-6 F-802 witness). This is the anti-over-fire pin.
- **AC-6 (parity.py frozen — obligation b):** every S3 pin on `app/styleguide/parity.py` passes **UNMODIFIED**; `git diff` shows **zero** changes to `parity.py`; the S3 CD determinism pin (`tests/specialists/cd/test_styleguide_resolution_emission.py`) stays green unmodified.
- **AC-7 (no clock coupling — E4):** a test documents/asserts that neither flip reads `clock_eligible`; Flip A branches on styleguide-key presence, Flip B on `receipt["outcome"]` only. (Pins the E4 two-way ruling as "S4 introduced no attestation gate.")
- **AC-8 (both-walks):** parametrized witness over start-walk and continuation-walk harness contexts (S1/S2/S3 walk-pin pattern, shared dispatch site, slides path per F-805): Flip A raises in BOTH walks on a styleguide-less payload; Flip B raises in BOTH on a divergence; the happy path proceeds in BOTH.
- **AC-9 (pins extended, never weakened):** any exact-shape/behavioral pin that trips on the flipped branches is EXTENDED (F-205 discipline), never loosened; T11 Acceptance Auditor rejects any weakened equality/assert. Ratchet/contract pins stay green via superset.
- **AC-L (live fail-loud teeth — orchestrator runs it; PASS required, skip never counts):**
  - **Leg 1 (named-variant-styleguide-less fail-loud, $0; SOP-011 F-1101):** one REAL run whose directive carries a **NAMED variant in `gamma_settings` with NO `styleguide` key** (NOT empty gamma_settings — that would fall back to default-A and NOT fire the flip, a false negative) reaching §07 Gary dispatch ⇒ error-pause at `gamma.styleguide.unbound`, **zero Gamma generative spend**, walk halted (not WARN-proceeded); the persisted run shows the error tag. Construct the directive deliberately (e.g. hand-edit / scripted-pick a variant then strip its styleguide binding) so the flip surface is exercised.
  - **Leg 2 (parity divergence → ERROR, $0):** the S3 AC-L leg-2 setup (mutate a RESOLVER-EMITTED field of the picked guide in `state/config/gamma-style-guides.yaml` AFTER CD's resolution and BEFORE §07, directive bytes untouched so digests MATCH, then git-restore) ⇒ now **error-pause** at `gamma.styleguide.parity-divergence` (S3 WARN-proceeded; S4 halts), **zero Gamma spend**, both envelopes in the receipt/error context, walk halted.
  - **Leg 3 (happy path still dispatches — anti-outage proof, ~1 deck):** one REAL run WITH a valid committed pick whose CD and Gary resolutions match ⇒ `ok/match`, **§07 dispatches a real Gamma deck**, walk proceeds to the next gate (proves the flip did not manufacture an outage on the canonical happy path). Expected spend: 1× Gamma deck + 1× CD fast-tier — cost not a constraint.
  - **"Zero Gamma spend" scope (F-1104):** `client.list_themes()` (`_act.py:~1107`) is a non-generative metadata read that runs before both flips — it is a PERMITTED call and does not count as spend; the pre-spend assertions target the **generate / create-from-template** methods specifically (legs 1 & 2 invoke NEITHER).
  - First-run-stands; frozen judges; **cleanup receipts APPEND, never overwrite (F-701)**; live legs driven FOREGROUND with a hard timeout OR an orchestrator-side single-notification watcher on the run's terminal state (session-17 gotcha (a) — do NOT park a subagent on a watcher).

## RED-first plan
1. `tests/specialists/gary/test_styleguide_fail_loud_flip.py` — AC-1 (Flip A raise + zero-dispatch), AC-2 (byte-diff witness: WARN-absent + tag + DEFAULT_VARIANT_PAIR intact), AC-3 (success-path byte-identity), AC-7 (no clock read) — RED against HEAD.
2. `tests/specialists/gary/test_styleguide_parity_error_flip.py` — AC-4 (Flip B raise pre-dispatch, all three divergence reasons, deterministic-not-retryable), AC-5 (tolerated states proceed + legacy clean) — RED.
3. Walk-pin extension (S1/S2/S3 harness) — AC-8 both-walks raise/proceed — RED until D1/D2 land.
4. `parity.py` S3 suites + CD determinism pin — GREEN UNMODIFIED (AC-6 constraint, not new tests).
5. Runner error-classification witness — AC-4's "deterministic error-pause, not retryable" — RED/pin against `production_runner` classifier.
6. Pre-existing battery: green UNTOUCHED except the ~21 F-605 baseline reds (`-n 0` before triaging any NEW red).

## Out of scope
Authority flip / Gary consuming CD's envelope resolution / Gary yaml-read collapse (**S-flip**, deferred-inventory entry gate — clock at tick 1) · any `clock_eligible` / three-way-attestation-strength change (**S-flip**, per the E4 ruling) · ANY edit to `app/styleguide/parity.py` (frozen input contract) · the retry-taxonomy overhaul (`gamma-dispatch-retry-taxonomy-deterministic-variance` — stays filed; S4 only ensures its own tags don't land retryable) · `run-artifact-split-across-runs-roots` (stays filed) · deleting `DEFAULT_VARIANT_PAIR` (retained for offline/smoke) · the **empty/declined-pick default-A leg** of §7 D-skip — closed by the deferred `styleguide-mandatory-pick-trial-start-precheck` follow-on (filed this session), NOT S4; a trial-start pre-check is the correct home (fail before the run starts, not at §07) and avoids the W5-morph legacy-bundle false-fail risk that a §07 whole-dispatch chokepoint would carry · g0_enrichment battery greens (S5 precondition) · Tracy (S6) · workbook (S7) · the legacy `dispatch_to_gamma` directive-path branch (F-805 — no styleguide resolve, no receipt, no flip).

## T11 gates
SOP-011 monitor pre-dispatch poll (verify the four obligations above BEFORE dev dispatch) → FRESH dev RED-first → SOP-012 at dev-complete + 3-lane `bmad-code-review` on the diff → RED-first remediation → orchestrator re-verify (ruff 0-new on touched; lint-imports 16+/0; `git diff --check` clean; focused + walk/trial battery green with the F-605 baseline carried; **`parity.py` diff empty**) → **AC-L live 3-leg PASS (2 fail-loud $0 legs + 1 happy-path deck)** → orchestrator commit (strays excluded: `_bmad-output/artifacts/workbooks-test/`, the 3 `claude-shadow-monitor-*` ledgers, `runs/a18c2a86.../`) → SOP close poll. Story flips **done ONLY after** the live AC-L PASS.

## Review Findings (SOP-011 + SOP-012 + 3-lane T11 + AC-L, 2026-07-07 — full text in the monitor ledger `canonical-arc-claude-shadow-monitor-2026-07-06.md`)

**SOP-011 (pre-dispatch): CONCUR-W-F** — obligations (a)-(d) HONORED; F-1100 feasibility trace (GaryActError→non-retryable error-pause) confirmed in code. Applied pre-dispatch: **F-1101** (Flip A scoped to the named-variant `else`-branch; the empty/declined-pick default-A leg is out of scope, filed as `styleguide-mandatory-pick-trial-start-precheck`), **F-1102** (halt-on-comparator-self-crash made an explicit intended ruling), F-1103 (message precision), F-1104 (list_themes permitted non-generative).

**SOP-012 (dev-complete): CONCUR-W-F** — parity.py byte-frozen; both flips pre-spend; F-1102 crash-halt witnessed via the REAL comparator crash path; both tags non-retryable; **AC-9 collateral teeth PRESERVED** (the 31+5 test adaptations verified as honest byte-identical-seed superset discipline, the one relocated F-804 teeth confirmed intact at `test_parity_comparator.py`). Findings F-1200 (autouse-fixture blanketing — mitigated R6) + F-1201 (baseline reds carry) — both NOTES.

**3-lane T11:** Acceptance Auditor **PASS** (all ACs MET, no weakened assertions; 1 NIT) · Blind Hunter **CLEAN** (NIT-1/NIT-2 stale-comment + thin-triage) · Edge Case Hunter **1 SHOULD-FIX + NITs** (no MUST-FIX; over-fire hunt clean, legacy/None tolerance verified). Convergent finding: the divergence-halt discarded the receipt yet the message falsely claimed "both envelopes ride the receipt."

**Remediation (fresh dev agent, RED-first): R1-R6 all GREEN.** R1 [SHOULD-FIX] the Flip-B raise now carries all five digest values + states honest halt-path survivability (no false receipt claim); R2 the E4 two-way-divergence raise witnessed at the flip surface (proven to discriminate against base); R3 mixed-variant A-bound+B-naked raises unbound; R4 empty-list proceeds; R5 dead studio-comment corrected; R6 autouse-fixture guard comment. Orchestrator re-verify: 348 green, parity.py + production_runner.py frozen, ruff 0-new, lint-imports 16/0.

**AC-L LIVE (orchestrator, frozen judges first-run-stands, NO anomalies):** Leg-1 unbound **12/12** · Leg-2 divergence→ERROR **16/16** (decisive S3 contrast: same rewind-recover, WARN-proceed→HALT) · Leg-3 happy **15/15** (real deck, anti-outage). Evidence `evidence/s4-acl-liveproof-20260707T041058Z/`. Spend 1 deck + ~$0.32.
