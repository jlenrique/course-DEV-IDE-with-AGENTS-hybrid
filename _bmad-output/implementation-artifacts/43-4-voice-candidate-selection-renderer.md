# Story 43-4 — G4A voice-candidate selection renderer

**Epic:** 43 · **Slab:** 2 · **Status:** ready-for-dev · **Gate mode:** dual-gate (paused-at-gate emit + registry).

Follows the **43-3 pattern exactly** (bespoke renderer + GATE_TO_CONTENT_TYPE entry + synthetic fixture + allowlist→registry move + TW-7c-4 registration). The gate→renderer bridge already exists — this story just adds one gate.

## Story

As the **operator**, at **G4A (voice selection)**, I want the candidate voices shown as a comparison table (name + the distinguishing attributes) instead of the generic Field|Value dump, so I can choose a voice on the merits.

## T1 required readings

- `_bmad-output/planning-artifacts/epic-43-hil-surface-tabular-coverage.md` §2/§4b.
- `app/marcus/cli/hil_tabular_projector.py` — the registry, `GATE_TO_CONTENT_TYPE`, `resolve_content_type`, `render_variant_ab`/`render_per_slide_mode` (43-3, the exact pattern to copy), `_md_table`, `_truncate_cell`, `KNOWN_UNRENDERED_ALLOWLIST`.
- `app/gates/section_11/poll_surface.py` — `display_voice_candidates` (:81): the exact dict shape to render + to synthesize a fixture from (its `voice_selection_payload` / candidate list).
- `tests/fixtures/hil_projector/README.md`; `tests/audit/test_audit_tw_7c_4_no_live_dispatch_scope_creep.py` (Epic-43 block).

## Acceptance criteria

**AC-1 — `render_voice_candidates`** registered under `content_type="voice_candidates"`; tables the candidate voices (one row per candidate) with their distinguishing fields (name + whatever the section_11 payload carries — voice_id, provider, style, etc.). Reuse `_md_table` + `_truncate_cell`; tolerate both the nested display shape and the bare payload sub-map (as 43-3's renderers do). Names "G4A voice-candidate selection" in a test.

**AC-2 — bridge entry.** Add the G4A gate key(s) → `voice_candidates` to `GATE_TO_CONTENT_TYPE` (use the same discovery approach 43-3 used — check the paused_gate string and/or surface_id; read section_11 to find the actual gate identifier). A routing test proves a paused G4A fixture reaches `render_voice_candidates`, not the generic fallback.

**AC-3 — synthetic fixture** under `tests/fixtures/hil_projector/` (e.g. `poll-voice-candidates-synthetic.json`), generated by invoking the real `display_voice_candidates` against a constructed input (schema-faithful, zero spend). README note: SYNTHETIC, source poll_surface named.

**AC-4 — ratchet move.** Remove `voice_candidates` from `KNOWN_UNRENDERED_ALLOWLIST`; update the `test_projector_coverage_ratchet_43_10.py` state-pin to the new registered set. Ratchet invariants stay green.

**AC-5 — invariants.** stdout/stderr split (R4); additive-within-v1 (R7); projector stdlib-pure; TW-7c-4 register the new test .py file; ruff + import-linter clean; no manifest touch.

## Test / process

- New test file under `tests/marcus/cli/`; routing + table-shape tests vs the synthetic fixture; ratchet green; TW-7c-4 scope test green.
- Run ONLY touched test files + ratchet + TW-7c-4 scope (use `-n 0` — the repo's pytest addopts use xdist). Do NOT run the full suite. Do NOT commit or stash.

## Definition of done

`render_voice_candidates` registered + tabling its synthetic fixture; G4A routes to it (proven); moved allowlist→registry with 43-10 green; TW-7c-4 registered; ruff/import-linter clean; ready for orchestrator review → `bmad-code-review`.
