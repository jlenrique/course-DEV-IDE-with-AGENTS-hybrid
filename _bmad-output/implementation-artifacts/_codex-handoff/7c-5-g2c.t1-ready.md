# 7c.5.G2C T1 Ready

Story: `migration-7c-5-g2c-decision-card-extend-and-audit`

Codex has completed T0/T1 only and is halted before T2 pending the PRE-T2 cross-agent review verdict.

## Deliverables

| Deliverable | Status | Evidence |
| --- | --- | --- |
| Frozen-hash pre-check | PASS | On-disk `app/models/decision_cards/g2c.py` sha256 and `FROZEN_AT_SHIP_HASHES["g2c"]` both equal `237ce7d1b6c228cea5bc3653027cb40e50e40f316681a736d0692612dc7ba72a`. |
| Contract diff | PASS | `_bmad-output/implementation-artifacts/migration-7c-5-g2c-contract-diff.md` authored with AMELIA-P5 `audit_method` qualifiers. All DROP rows are `audit_method=heavy`. |
| Backward-consumer audit | PASS | `_bmad-output/implementation-artifacts/migration-7c-5-g2c-backward-consumer-audit.md` authored with per-call-site verdicts. |
| T1 class-conformance baseline | PASS | `PASS: 16 parity contract file(s) conform (11 activation + 5 decision-card shape-pin)`. |
| Sandbox-AC | PASS | `PASS - no sandbox-AC violations across 1 story file(s)`. |
| T1 regression baseline | PASS-modulo-inherited | `870 passed, 1 failed, 18 skipped`; sole failure is inherited NFR-CG6 governance-version drift from V6/V7 amendment commit `57b92b2`, not G2C body work. |

## AMELIA-P5 Summary

Contract-diff section 2 uses schema `| field | disposition | audit_method | rationale |`.

G2C DROP rows with heavy evidence:

- `drafted_proposal`: collect run found no `-k drafted_proposal` tests in required roots; targeted direct reads are G1-only and passed when executed.
- `evidence`: collect run found 7 `-k evidence` tests; execution passed 7 tests. Targeted direct reads are G1-only and passed when executed.
- `risks`: collect run found no tests in required roots; targeted access grep found no direct reads.

Metadata DROP rows also carry `audit_method=heavy`:

- `reject_rate`, `party_mode_contributions`, `consolidated_at`, `sanctum_warnings`: collect runs found no tests in required roots and targeted grep found no required-root reads. Separate `tests/integration/gates/` reads of party-mode metadata are for `G-PARTY`, not G2C.

## T2 Update Surface Identified

- `app/marcus/orchestrator/production_runner.py:434`
- `marcus/orchestrator/m3_trial.py:169`
- G2C four-file lockstep: `app/models/decision_cards/g2c.py`, `schema/g2c.v1.schema.json`, `tests/fixtures/decision_cards/g2c_golden.json`, `tests/parity/test_decision_card_g2c_shape.py`

2-class-regime substrate already accepts both `DecisionCard` and `DecisionCardBase` after G1: `app/manifest/compiler.py`, `app/manifest/refs.py`, `tests/unit/models/decision_cards/test_manifest_dotted_reference_resolver.py`, and `app/gates/resume_api.py`.

## Halt

Codex must not proceed to T2 until cross-agent T1 review drops a PASS verdict at `_bmad-output/implementation-artifacts/7c-5-g2c-t1-cross-agent-review-2026-05-NN.md`.
