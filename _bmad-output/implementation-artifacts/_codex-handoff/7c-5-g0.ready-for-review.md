# Story 7c.5.G0 Ready for Review

**Status:** ready for T11 standard `bmad-code-review`  
**Story:** `migration-7c-5-g0-decision-card-fresh-author`  
**Date:** 2026-05-05  

## Summary

Codex implemented the G0 trial-open / corpus-confirm DecisionCard as a four-file lockstep set:

- `app/models/decision_cards/g0.py`
- `app/models/decision_cards/schema/g0.v1.schema.json`
- `tests/parity/test_decision_card_g0_shape.py`
- `tests/fixtures/decision_cards/g0_golden.json`

G0 consumes the 7c.4b `_base.py:DecisionCardBase` substrate and explicitly carries the required operator-facing fields: `card_id`, `trial_id`, `gate_id`, `gate_focus`, `created_at`, `corpus_paths_confirmed`, `directive_run_id`, `corpus_confirmation_summary`, `verb`, `meta`, `decision_card_digest`, and `schema_version`.

## Verification

- G0 shape-pin: `10 passed`
- G0 + G2A + TW-7c-3 structural pins: `23 passed`
- DecisionCard model regression: `36 passed`
- Gate/DecisionCard integration slice: `27 passed`
- Section 02A regression: `15 passed`
- Smoke: `181 passed, 18 skipped`
- Class-conformance: `PASS: 13 parity contract file(s) conform (11 activation + 2 decision-card shape-pin)`
- Import-linter: `12 kept, 0 broken`
- Sandbox-AC: `PASS`
- Ruff: `All checks passed`
- Broad regression: `44 failed, 4132 passed, 27 skipped, 2 xfailed`; failures are checkout-level inherited surfaces, not G0/G2A focused failures.

## Notes

- AMEND-7d-i compliance: the shape-pin imports `LOCKSTEP_CHECK` by reference and does not rederive `FOUR_FILE_GLOBS`.
- Windows portability: `corpus_paths_confirmed` serializes via `Path.as_posix()` so the golden fixture remains deterministic on Windows.
- Shared validator extension: `validate_parity_test_class_conformance.py` now counts per-gate DecisionCard shape-pins, producing the required G0/G2A delta.

## Self-Review

Blind Hunter:
- The new G0 card uses `DecisionCardBase`, not the legacy `DecisionCard` substrate.
- The four-file set is present and cited by the single-source lockstep check.

Edge Case Hunter:
- Closed `gate_id` rejects `G1`, `G99`, and non-string values.
- Closed `gate_focus` rejects unrelated values and `None`.
- Empty `corpus_paths_confirmed` is rejected.

Acceptance Auditor:
- AC-A through AC-E have direct model, schema, fixture, and shape-pin coverage.
- Section 02A verdict schema stability passed unchanged.
