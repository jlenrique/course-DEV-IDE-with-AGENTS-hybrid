# Story 7c.5.G2A Ready for Review

**Status:** ready for T11 standard `bmad-code-review`  
**Story:** `migration-7c-5-g2a-decision-card-fresh-author`  
**Date:** 2026-05-05  

## Summary

Codex implemented the G2A plan-unit-ratification DecisionCard as a four-file lockstep set:

- `app/models/decision_cards/g2a.py`
- `app/models/decision_cards/schema/g2a.v1.schema.json`
- `tests/parity/test_decision_card_g2a_shape.py`
- `tests/fixtures/decision_cards/g2a_golden.json`

The G2A worker produced the initial model/test/fixture; the main thread completed the missing schema, removed an AMEND-7d-i AST-scan violation, integrated shared exports, and ran verification.

## Verification

- G2A shape-pin: included in combined `23 passed` G0/G2A/TW-7c-3 run.
- DecisionCard model regression: `36 passed`
- Gate/DecisionCard integration slice: `27 passed`
- Smoke: `181 passed, 18 skipped`
- Class-conformance: `PASS: 13 parity contract file(s) conform (11 activation + 2 decision-card shape-pin)`
- Import-linter: `12 kept, 0 broken`
- Sandbox-AC: `PASS`
- Ruff: `All checks passed`
- Broad regression: `44 failed, 4132 passed, 27 skipped, 2 xfailed`; failures are checkout-level inherited surfaces, not G0/G2A focused failures.

## Notes

- AMEND-7d-i compliance: the G2A shape-pin imports `LOCKSTEP_CHECK` by reference and no longer references `all_four_present` directly.
- Class-conformance baseline at close: G0 + G2A shape-pins raise the validator count from `11` activation contracts to `13` total parity contracts.
- Existing G1/G2C/G3/G4 construction and union tests passed unchanged.

## Self-Review

Blind Hunter:
- G2A uses the new `DecisionCardBase` substrate and stays out of the legacy G1-G4 body code.
- Shared `__init__.py` union export includes G2A after the model exists.

Edge Case Hunter:
- Closed `gate_id` rejects `G1`, `G99`, and non-string values.
- Closed `gate_focus` rejects unrelated values and `None`.
- Empty `plan_unit_summary` and empty `ratification_evidence` are rejected.

Acceptance Auditor:
- AC-A through AC-E have direct model, schema, fixture, validator, and regression coverage.
- Cross-gate non-regression passed through the existing decision-card test suite.
