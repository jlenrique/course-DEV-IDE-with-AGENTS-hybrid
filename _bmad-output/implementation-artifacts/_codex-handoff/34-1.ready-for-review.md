# Story 34-1 Ready For Review

## Blind Hunter

- PASS: The integration test invokes `run_wrangler.py` through a real `subprocess.run([sys.executable, ...])` call.
- PASS: The test writes a real `tmp_path / "directive.yaml"` after calling the real `translate_directive_for_wrangler`.
- PASS: The forensic fixture sha256 is asserted before model validation or subprocess execution.
- PASS: The A9 mitigation is present: `len(result_yaml["materials"]) >= 1` is asserted before primary-row shape assertions.
- PASS: Paths are `Path` objects, and the subprocess `cwd` is the fixture directive's `corpus_dir` so the original relative locators resolve without adding translator scope.

## Edge Case Hunter

- Missing fixture: the test explicitly skips with a diagnostic if the committed fixture is absent.
- Fixture drift: the test fails on sha256 mismatch against `351a57fbe12aff4a49349c4a646618d92ae38a798ec53eee61668f74f8bbd703`.
- Wrangler hang: subprocess has `timeout=60`.
- Windows/POSIX: subprocess uses `sys.executable`, absolute script/directive/bundle paths, `Path` objects, and text capture.
- Exit-code taxonomy: per amended contract, exit `0` and `10` are accepted only with `status in {"complete", "complete_with_warnings"}` and `blocking_issues == []`; exits `20` and `30` fail the schema-seam ratchet.
- Translator mappings: `translate_directive_for_wrangler` reads `TRANSLATOR_ACTIVE_MAPPINGS` inside each mapping branch, keeping the constant load-bearing for Story 34-5.

## Acceptance Auditor

- AC-34-1-A: PASS. Real subprocess, real directive write, non-empty materials assertion before row shape, primary row has `word_count` and `content_path`.
- AC-34-1-B: PASS. Byte-identical forensic fixture copy is committed and dynamically hash-checked.
- AC-34-1-C: PASS. `_wrangler_translator.py` contains `__epic_34_scaffolding__ = True` and `DELETE-AT-EPIC-34-CLOSE`.
- AC-34-1-D: PASS. Test module docstring enumerates the mock surface; no mocks are used.
- AC-34-1-E: PASS. Focused integration test reports 3 passed.
- AC-34-1-F: NOT RUN. Operator-gated live-LLM exercise is out of dev-agent scope.

## Mock-Surface Audit

| Mock | Used? | Rationale |
|---|---:|---|
| Upstream LLM call | No | Fixture replay uses the committed Trial-3 attempt-2 directive; no live LLM is exercised in dev-agent scope. |
| Wrangler subprocess | No | Real subprocess is invoked via `subprocess.run([sys.executable, run_wrangler.py, ...])`. |
| Directive write path | No | Test writes a real YAML file under `tmp_path`. |
| Translator function | No | Test imports and calls the real `translate_directive_for_wrangler`. |
| Section 02A Directive model | No | Test validates fixture through the real `Directive.model_validate`. |

## Verification

- `.\.venv\Scripts\python.exe -m pytest tests\integration\test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> 3 passed.
- `.\.venv\Scripts\python.exe -m ruff check tests\integration\test_section_02a_to_wrangler_subprocess_roundtrip.py app\composers\section_02a\_wrangler_translator.py` -> passed.
- `.\.venv\Scripts\lint-imports.exe --config pyproject.toml` -> 13 kept, 0 broken.
- `.\.venv\Scripts\python.exe -m pytest tests\composers\section_02a tests\marcus_cli\test_compose_section_02a_directive_adapter.py -q` -> 16 passed.
- `.\.venv\Scripts\python.exe scripts\utilities\validate_migration_story_sandbox_acs.py _bmad-output\implementation-artifacts\migration-34-1-section-02a-wrangler-integration-roundtrip-test.md` -> PASS.
- `.\.venv\Scripts\python.exe -m pytest tests\audit\test_audit_tw_7c_4_no_live_dispatch_scope_creep.py -q` -> 5 passed.
- `PYTHONPATH=.; .\.venv\Scripts\python.exe skills\bmad-agent-cora\scripts\preclosure_hook.py` -> passed.
- `.\.venv\Scripts\python.exe -m pytest --tb=no -q` -> 95 failed, 4442 passed, 27 skipped, 2 xfailed. Delta vs 88 baseline is +7, within the Story 34-1 abort threshold.

## Notes For T11

- The story contract was amended during T1 to defer `metadata.json::sme_refs[]` assertions to Story 34-4.
- The story contract was amended during T1 to accept wrangler exit `10` for `complete_with_warnings` when `blocking_issues == []`.
- Two empty package markers named in early story text, `tests/integration/__init__.py` and `tests/fixtures/integration/__init__.py`, were not created because TW-7c-4 C1 does not allowlist them. Pytest collection does not require them.
