# Story 34-2 Ready For Review

**Story:** `migration-34-2-wrangler-six-role-union-excluded-reason`  
**Status:** `review`  
**Date:** 2026-05-22  
**T11 requested:** Claude standard `bmad-code-review` (single-gate; not cross-agent)

## Summary

Story 34-2 implementation is present in commit `bc477ed` and verified in this pass. The final closeout edits in this pass are the BMAD status flip and this handoff.

Implemented contract coverage:

- D1: `run_wrangler.py` has module-level `_ALLOWED_ROLES` with the 7-element union: `primary`, `supporting`, `ignored`, `validation`, `supplementary`, `visual-primary`, `visual-supplementary`.
- D2: `_ALLOWED_EXCLUDED_REASONS` and `excluded_reason` validation are enforced for `role=ignored`; non-ignored sources reject `excluded_reason`.
- D3: §02A cross-field invariants migrated to wrangler-side validation: ignored forbids `expected_min_words`, text source roles require `expected_min_words`, binary supporting sources forbid it.
- D4: locator-shape wrangler run filters `role=ignored` rows before materialization and logs each filtered row to stderr.
- D5: primary-presence behavior is unchanged and operates after ignored-row filtering.
- T4: `_wrangler_translator.py::TRANSLATOR_ACTIVE_MAPPINGS` is reduced to `frozenset({"src-id-to-ref-id"})`; retired mappings are no longer applied.

## Files

Modified:

- `skills/bmad-agent-texas/scripts/run_wrangler.py`
- `app/composers/section_02a/_wrangler_translator.py`
- `_bmad-output/implementation-artifacts/migration-34-2-wrangler-six-role-union-excluded-reason.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

New:

- `skills/bmad-agent-texas/scripts/tests/test_run_wrangler_role_enum_union_and_excluded_reason.py`
- `_bmad-output/implementation-artifacts/_codex-handoff/34-2.ready-for-review.md`

Untouched by this pass:

- `app/composers/section_02a/directive_model.py`
- `app/composers/section_02a/composer.py`
- Story 34-1 integration test

## Verification

- T1 C1 ancestry: `3159a0e` is an ancestor of `HEAD`.
- T1 predecessor: Story 34-1 is `done` in `sprint-status.yaml`.
- T1 Story 34-1 ratchet: `.venv/Scripts/python.exe -m pytest tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> `3 passed`.
- T6 ruff: `.venv/Scripts/python.exe -m ruff check skills/bmad-agent-texas/scripts/run_wrangler.py skills/bmad-agent-texas/scripts/tests/test_run_wrangler_role_enum_union_and_excluded_reason.py app/composers/section_02a/_wrangler_translator.py` -> PASS.
- T6 lint-imports: `.venv/Scripts/lint-imports.exe` -> `13 kept, 0 broken`.
- T7 focused Story 34-2 + ratchet: `.venv/Scripts/python.exe -m pytest skills/bmad-agent-texas/scripts/tests/test_run_wrangler_role_enum_union_and_excluded_reason.py tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> `23 passed`.
- T7 Texas focused surface: `.venv/Scripts/python.exe -m pytest skills/bmad-agent-texas/scripts/tests tests/specialists/texas tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> `122 passed, 1 skipped`.
- T8 broad regression: `.venv/Scripts/python.exe -m pytest --tb=no -q --no-header` -> `86 failed, 4471 passed, 27 skipped, 2 xfailed`.

Broad-regression delta vs requested 88-baseline: `-2` failures. No Story 34-2-specific focused failures observed.

Tooling note: `.venv/Scripts/python.exe -m lint_imports` is unavailable in this venv (`No module named lint_imports`); the installed console command `.venv/Scripts/lint-imports.exe` passes with 13 contracts kept.

## G6 Layered Self-Review

Blind Hunter:

- Finding: D1/D2 constants and validator path are present and closed-list based. No patch needed.
- Finding: D4 filtering uses post-validation `directive["sources"]` and feeds only `non_ignored_sources` to materialization/cross-validation. No patch needed.
- Finding: The existing primary-presence check remains in `_load_directive`, so an only-ignored directive still fails before runtime materialization. This matches AC-34-2-C.

Edge Case Hunter:

- Unknown role rejects before cross-field checks.
- Missing, blank, and unknown `excluded_reason` fail for ignored sources.
- Non-ignored `excluded_reason` fails.
- Binary `supporting` with `expected_min_words` fails.
- Ignored rows are absent from `materials`, `metadata.json::provenance`, and `extracted.md`, with stderr audit logging.

Acceptance Auditor:

- AC-34-2-A: PASS.
- AC-34-2-B: PASS.
- AC-34-2-C: PASS.
- AC-34-2-D: PASS.
- AC-34-2-E: PASS via focused + Texas suite.
- AC-34-2-F: PASS via Story 34-1 ratchet.

## Review Notes

The worktree had an unrelated dirty file before closeout: `runs/cache-harness/irene-pass1.md`. It was not read for implementation, not modified by this pass, and should be ignored for Story 34-2 T11 unless the operator says otherwise.
