# 7c.5.G4 T1 Ready

Story: `migration-7c-5-g4-decision-card-extend-and-audit`

Codex has completed T0/T1 only and is halted before T2 pending the PRE-T2 cross-agent review verdict.

## Dispatch Note

The operator instructed Codex to proceed. Local sprint status still records G2C and G3 as `in-progress`, so this dispatch is treated as the operator (E)-elasticity override allowed by the G4 prompt. The local class-conformance baseline is 16 rather than the prompt's expected "likely 18".

## Deliverables

| Deliverable | Status | Evidence |
| --- | --- | --- |
| Frozen-hash pre-check | PASS | On-disk `app/models/decision_cards/g4.py` sha256 and `FROZEN_AT_SHIP_HASHES["g4"]` both equal `98536d2ab845972f96e8d374b08bb3929fb8d02024cbc56616c90424061c6b5a`. |
| Contract diff | PASS | `_bmad-output/implementation-artifacts/migration-7c-5-g4-contract-diff.md` authored with clean semantic-fit statement and AMELIA-P5 `audit_method` qualifiers. All DROP rows are `audit_method=heavy`. |
| Backward-consumer audit | PASS | `_bmad-output/implementation-artifacts/migration-7c-5-g4-backward-consumer-audit.md` authored with per-call-site verdicts. |
| T1 class-conformance baseline | PASS | `PASS: 16 parity contract file(s) conform (11 activation + 5 decision-card shape-pin)`. |
| Sandbox-AC | PASS | `PASS - no sandbox-AC violations across 1 story file(s)`. |
| T1 regression baseline | PASS-modulo-inherited | `870 passed, 1 failed, 18 skipped`; sole failure is inherited NFR-CG6 governance-version drift from V6/V7 amendment commit `57b92b2`, not G4 body work. |

## Semantic Alignment Verdict

G4 legacy closeout fields fit ADR 0002's `fidelity_gate` framing cleanly. T1 verdict: preserve `final_status`, `artifact_paths`, and `outcome_summary`; add `gate_focus: Literal["fidelity_gate"]` at T2 as the family taxonomy marker. No semantic-divergence follow-on is required by this T1 audit.

## AMELIA-P5 Summary

Contract-diff section 2 uses schema `| field | disposition | audit_method | rationale |`.

G4 DROP rows with heavy evidence:

- `drafted_proposal`: collect run found no `-k drafted_proposal` tests in required roots; targeted direct reads are G1-only and passed when executed.
- `evidence`: collect run found 7 `-k evidence` tests; execution passed 7 tests. Targeted direct reads are G1-only and passed when executed.
- `risks`: collect run found no tests in required roots; targeted access grep found no direct reads.

Metadata DROP rows also carry `audit_method=heavy`:

- `reject_rate`, `party_mode_contributions`, `consolidated_at`, `sanctum_warnings`: collect runs found no tests in required roots and targeted grep found no required-root reads. Separate `tests/integration/gates/` reads of party-mode metadata are for `G-PARTY`, not G4.

## T2 Update Surface Identified

- `app/marcus/orchestrator/production_runner.py:449`
- `marcus/orchestrator/m3_trial.py:185`
- G4 four-file lockstep: `app/models/decision_cards/g4.py`, `schema/g4.v1.schema.json`, `tests/fixtures/decision_cards/g4_golden.json`, `tests/parity/test_decision_card_g4_shape.py`

2-class-regime substrate already accepts both `DecisionCard` and `DecisionCardBase` after G1: `app/manifest/compiler.py`, `app/manifest/refs.py`, `tests/unit/models/decision_cards/test_manifest_dotted_reference_resolver.py`, and `app/gates/resume_api.py`.

## V7 Note

G4 is the final extend-and-audit story in the V7 P2-clean-x3 baseline. T10 must report whether G2C, G3, and G4 freshness remained clean enough to unlock Wave-3 N+3.

## Halt

Codex must not proceed to T2 until cross-agent T1 review drops a PASS verdict at `_bmad-output/implementation-artifacts/7c-5-g4-t1-cross-agent-review-2026-05-NN.md`.
