# Story 7c.0a Ready for Review

Story: `migration-7c-0a-decision-foundation`
Status: `review`
Date: 2026-05-04
Next required step: Claude T11 `bmad-code-review` in a cold context.

## Summary

Codex completed the 7c.0a decision foundation deliverables:

- ADR 0001 for the parity-contract DSL with the six required decision sections and Appendix A audit-chain conceptual design.
- Import-linter contracts C4/C5/C6 in `pyproject.toml`, with empty `forbidden_modules` lists and a comment block citing 7c.0a / Winston W2 / downstream populating stories.
- `TripwireLedgerEntry`, `TripwireId`, and `TripwireSeverity` in `app/models/tripwire_ledger.py`, exported from `app/models/__init__.py`.
- Shape and structural tests for the model, ADR, and import-linter contracts.

## Files

- `docs/dev-guide/adr/0001-parity-contract-dsl.md`
- `pyproject.toml`
- `app/models/tripwire_ledger.py`
- `app/models/__init__.py`
- `tests/parity/test_tripwire_ledger_entry_shape.py`
- `tests/structural/test_adr_0001_parity_contract_dsl_present.py`
- `tests/structural/test_import_linter_contracts_c4_c5_c6_present.py`
- `_bmad-output/implementation-artifacts/migration-7c-0a-decision-foundation.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

## T1 Resolutions

- ADR slot `0001-parity-contract-dsl.md` was free because `docs/dev-guide/adr/` did not exist at T1.
- `tests/fixtures/frozen_paths/` did not exist and was created during T1.
- `app/audit/` did not exist. This is acceptable for 7c.0a because ADR Appendix A is conceptual only; `app/audit/errors.py` lands in 7c.0b.
- The prompt referenced `app/models/override_event.py`, but this checkout contains `app/models/decision_cards/override_event.py`. TripwireLedgerEntry remains correctly placed under `app/models/`.
- C4/C5/C6 source packages do not exist yet in this decision-tier story. The contracts use wildcard source expressions with empty forbidden lists so `lint-imports` is active without creating 7c.0b scaffold files early.

## Verification

- Focused tests:
  `.venv/Scripts/python.exe -m pytest tests/parity/test_tripwire_ledger_entry_shape.py tests/structural/test_adr_0001_parity_contract_dsl_present.py tests/structural/test_import_linter_contracts_c4_c5_c6_present.py -p no:randomly -q --tb=short`
  Result: `27 passed in 0.52s`.
- Ruff touched-files check:
  Result: `All checks passed!`.
- Import-linter:
  Result: `Contracts: 12 kept, 0 broken` (baseline 9 + C4/C5/C6).
- Class conformance:
  Result: `PASS: 11 activation contract file(s) conform`.
- Sandbox-AC validator:
  Result: `PASS - no sandbox-AC violations across 1 story file(s).`
- Broad regression:
  `.venv/Scripts/python.exe -m pytest -p no:randomly -q --tb=line`
  Result: `38 failed, 3988 passed, 27 skipped, 46 deselected, 2 xfailed`.

Broad-regression note: the full-suite failures are existing checkout/environment drift outside 7c.0a touched files. A spot check of `tests/contracts/test_30_1_zero_test_edits.py` shows it fails from historical `d7fd520..HEAD` drift, not from the uncommitted 7c.0a files.

## Codex Self-Review

Blind Hunter:

- No specialist body, HIL surface module, pipeline manifest, composition spec, or prior ADR was modified.
- Import-linter count increased from 9 to 12 and remains green.
- The main review risk is the wildcard source expression adaptation for future source packages; this is deliberate to avoid creating executable/scaffold packages before 7c.0b.

Edge Case Hunter:

- TripwireLedgerEntry rejects naive datetimes, non-UUID4 trace IDs, extra fields, invalid tripwire IDs, invalid severity tiers, and invalid assignment mutations.
- The shape test checks JSON Schema `additionalProperties: false` and enum closure.
- The import-linter subprocess test decodes output with `errors="replace"` to avoid Windows console-byte failures without relying on `PYTHONIOENCODING`.

Acceptance Auditor:

- AC-A: C4/C5/C6 present, empty forbidden lists, lint-imports green at 12 kept.
- AC-B: ADR exists at canonical 0001 path and structural test asserts the six required sections.
- AC-C: Pydantic-v2 model and exports landed with shape-pin tests.
- AC-D: ADR Appendix A names append-only, monotonic timestamp, parent-trace linkage, error hierarchy, `app/audit/errors.py`, and 7c.0b forward pointer.

Review attention requested:

- Decide whether the wildcard source expression adaptation is acceptable for 7c.0a or whether Claude wants to change the contract config after creating non-executable placeholder packages. Codex did not create placeholder packages because that would expand the file scope and preempt 7c.0b.
- Treat broad-regression red as a repo-state blocker to adjudicate, not as focused story evidence. Focused story checks are green.
