# 7c.5.G3 T1 Ready

Story: `migration-7c-5-g3-decision-card-extend-and-audit`

Codex has completed T0/T1 only and is halted before T2 pending the PRE-T2 cross-agent review verdict.

## Dispatch Note

The operator declared G2C reviewed and closed for G3 dispatch. The local checkout still shows `HEAD=57b92b2` and does not contain a migrated G2C body or G2C close review artifacts, so this handoff records the local class-conformance baseline as 16 rather than the prompt's expected "likely 17".

## Deliverables

| Deliverable | Status | Evidence |
| --- | --- | --- |
| Frozen-hash pre-check | PASS | On-disk `app/models/decision_cards/g3.py` sha256 and `FROZEN_AT_SHIP_HASHES["g3"]` both equal `bcfe2865df5e7071cf43ada563e29a8b6fc5dfa1cb3abdf99f78fa5d2d0fddf3`. |
| Contract diff | PASS | `_bmad-output/implementation-artifacts/migration-7c-5-g3-contract-diff.md` authored with required semantic-alignment statement and AMELIA-P5 `audit_method` qualifiers. All DROP rows are `audit_method=heavy`. |
| Backward-consumer audit | PASS | `_bmad-output/implementation-artifacts/migration-7c-5-g3-backward-consumer-audit.md` authored with per-call-site verdicts. |
| T1 class-conformance baseline | PASS | `PASS: 16 parity contract file(s) conform (11 activation + 5 decision-card shape-pin)`. |
| Sandbox-AC | PASS | `PASS - no sandbox-AC violations across 1 story file(s)`. |
| T1 regression baseline | PASS-modulo-inherited | `870 passed, 1 failed, 18 skipped`; sole failure is inherited NFR-CG6 governance-version drift from V6/V7 amendment commit `57b92b2`, not G3 body work. |

## Semantic Alignment Verdict

G3 legacy fields describe mid-trial in-flight review, while ADR 0002 names the family "motion-clip approval". T1 verdict: preserve `progress_percent`, `active_node_id`, `pending_nodes`, and `operator_prompt` verbatim for backward consumers; add `gate_focus: Literal["motion_clip_approval"]` at T2 as the family taxonomy marker; defer any operator semantic refinement.

## AMELIA-P5 Summary

Contract-diff section 2 uses schema `| field | disposition | audit_method | rationale |`.

G3 DROP rows with heavy evidence:

- `drafted_proposal`: collect run found no `-k drafted_proposal` tests in required roots; targeted direct reads are G1-only and passed when executed.
- `evidence`: collect run found 7 `-k evidence` tests; execution passed 7 tests. Targeted direct reads are G1-only and passed when executed.
- `risks`: collect run found no tests in required roots; targeted access grep found no direct reads.

Metadata DROP rows also carry `audit_method=heavy`:

- `reject_rate`, `party_mode_contributions`, `consolidated_at`, `sanctum_warnings`: collect runs found no tests in required roots and targeted grep found no required-root reads. Separate `tests/integration/gates/` reads of party-mode metadata are for `G-PARTY`, not G3.

## T2 Update Surface Identified

- `app/marcus/orchestrator/production_runner.py:441`
- `marcus/orchestrator/m3_trial.py:177`
- G3 four-file lockstep: `app/models/decision_cards/g3.py`, `schema/g3.v1.schema.json`, `tests/fixtures/decision_cards/g3_golden.json`, `tests/parity/test_decision_card_g3_shape.py`

2-class-regime substrate already accepts both `DecisionCard` and `DecisionCardBase` after G1: `app/manifest/compiler.py`, `app/manifest/refs.py`, `tests/unit/models/decision_cards/test_manifest_dotted_reference_resolver.py`, and `app/gates/resume_api.py`.

## Halt

Codex must not proceed to T2 until cross-agent T1 review drops a PASS verdict at `_bmad-output/implementation-artifacts/7c-5-g3-t1-cross-agent-review-2026-05-NN.md`.
