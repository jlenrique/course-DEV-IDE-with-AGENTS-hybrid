# 7c.5.G6 Codex Handoff: Ready for Review

Story: `migration-7c-5-g6-decision-card-fresh-author`
Date: 2026-05-05
Worker: Codex G6 parallel-dispatch lane

## Changed Files

- `app/models/decision_cards/g6.py`
- `app/models/decision_cards/schema/g6.v1.schema.json`
- `tests/parity/test_decision_card_g6_shape.py`
- `tests/fixtures/decision_cards/g6_golden.json`
- `_bmad-output/implementation-artifacts/_codex-handoff/7c-5-g6.ready-for-review.md`

No shared export/status file was edited in this worker lane.

## Implementation Summary

- Added `G6Card(DecisionCardBase)` importing `DecisionCardBase` from `app.models.decision_cards._base`.
- Added UUID4-typed `card_id`, `trial_id`, and `closing_run_id` annotations.
- Added G6 closed literals: `gate_id: Literal["G6"]` and `gate_focus: Literal["slab_close_ceremony"]`.
- Added `slab_id` regex validation for `^[0-9]+[a-z]?$`.
- Added strip-then-non-empty validation for `slab_close_summary`.
- Added POSIX path serializers for `retrospective_path` and `closing_artifact_paths`.
- Generated deterministic sorted JSON Schema with LF line endings.
- Added deterministic golden fixture and G6 shape-pin.

## Four-File Lockstep

`LOCKSTEP_CHECK("G6", repo_root=REPO_ROOT)` passes inside `test_g6_four_file_lockstep_is_satisfied` with:

```python
{
    "golden_fixture": True,
    "model": True,
    "schema": True,
    "shape_pin": True,
}
```

## AMEND-7d-i Evidence

Exact import line:

```python
from app.parity.contracts.tw_7c_3_firing import LOCKSTEP_CHECK
```

Guardrail self-grep:

```text
rg -n "FOUR_FILE_GLOBS|all_four_present" tests/parity/test_decision_card_g6_shape.py
```

Result: zero matches. The shape-pin does not import `FOUR_FILE_GLOBS` and does not reference `all_four_present` in test-body code.

## Slab ID Matrix

Accepted cases in the shape-pin:

```python
["7", "7c", "12", "12b", "5a"]
```

Rejected cases in the shape-pin:

```python
["", "slab-7c", "7C", "7-c", "7cd", "7ab"]
```

## Verification Results

T1 readiness:

```text
DecisionCardBase DecisionCardBase
DecisionCardMeta DecisionCardMeta
LOCKSTEP_CHECK True
FOUR_FILE_GLOBS True
```

Class-conformance baseline at story-open:

```text
PASS: 13 parity contract file(s) conform (11 activation + 2 decision-card shape-pin)
```

Post-G6 class-conformance:

```text
PASS: 14 parity contract file(s) conform (11 activation + 3 decision-card shape-pin)
```

Arithmetic at the time of the G6 postcheck: G6 landed alone in this worker checkout; G5 artifacts were not present. Expected delta was `13 -> 14` (`+1`).

Final class-conformance rerun after concurrent G5 artifacts appeared in the shared checkout:

```text
PASS: 15 parity contract file(s) conform (11 activation + 4 decision-card shape-pin)
```

Concurrent arithmetic now visible in the shared checkout: `13 -> 15` (`+2`) across G5+G6.

Focused owned test:

```text
.venv/Scripts/python.exe -m pytest tests/parity/test_decision_card_g6_shape.py -p no:randomly -q --tb=short
23 passed in 3.44s
```

TW-7c-3 structural AST-scan:

```text
.venv/Scripts/python.exe -m pytest tests/structural/test_tw_7c_3_firing_spec_single_source.py -p no:randomly -q --tb=short
2 passed in 3.21s
```

Sandbox-AC:

```text
.venv/Scripts/python.exe scripts/utilities/validate_migration_story_sandbox_acs.py _bmad-output/implementation-artifacts/migration-7c-5-g6-decision-card-fresh-author.md
PASS - no sandbox-AC violations across 1 story file(s).
```

Ruff:

```text
.venv/Scripts/python.exe -m ruff check app/models/decision_cards/g6.py tests/parity/test_decision_card_g6_shape.py
All checks passed!
```

Import-linter:

```text
.venv/Scripts/lint-imports.exe
Contracts: 12 kept, 0 broken.
```

Cross-gate parity slice:

```text
.venv/Scripts/python.exe -m pytest tests/parity/ tests/parametrized_harness/ -p no:randomly -q --tb=short
1 failed, 279 passed, 18 skipped in 9.99s
```

Failure observed:

```text
tests/parity/test_nfr_cg_block_aggregated.py::test_nfr_cg_block_structural_evidence[NFR-CG6]
AssertionError: assert '2026-05-04-velocity-amendments-bundle' == '2026-04-29-slab7b-twelve-stories'
```

Attribution: this matches the inherited NFR-CG6 governance-version drift called out in the dispatch prompt. It is not caused by G6 model/schema/fixture/shape-pin changes.

Not run in this worker lane: `pytest --smoke` and full broad regression. The user-requested focused verification battery was completed, and cross-gate parity plus import-linter were added as extra signal.

## Self-Review

Blind review:

- The new model inherits `DecisionCardBase`, not the legacy `DecisionCard`.
- The UUID identity fields are annotated with `UUID4`.
- Path serialization uses `Path.as_posix()` for both the single retrospective path and the artifact-path list.
- The schema is generated from the model and byte-pinned by the shape test.

Edge-case review:

- `slab_close_summary=""` and whitespace-only summaries are rejected.
- `closing_artifact_paths=[]` is rejected.
- Uppercase, hyphenated, prefixed, and multi-letter slab IDs are rejected.
- Frozen mutation of `gate_id` is rejected by inherited config.

Acceptance audit:

- AC-A: four G6 files authored and lockstep-pinned.
- AC-B: shape-pin cites `LOCKSTEP_CHECK` by reference without re-deriving the rule.
- AC-C: class-conformance count increased `13 -> 14`.
- AC-D: cross-gate run has one inherited NFR-CG6 failure; no G6-attributable failures observed.
- AC-E: Pydantic-v2 model uses inherited `extra="forbid"`, `validate_assignment=True`, `frozen=True`, closed literals, timezone validator, UUID4 validation, non-empty validators, slab regex validator, path serializers, field descriptions, and `schema_version: Literal["v1"]`.

## Needed Main-Agent Integration

- Completed by main thread: `G6Card` was added to `app/models/decision_cards/__init__.py` flat exports and `AnyDecisionCard` union together with concurrent `G5Card`.
- Completed by main thread: sprint status moved to `review`.

## Main-Thread Final Verification Addendum

- G5 + G6 + TW-7c-3 structural pins: `39 passed`
- Existing DecisionCard model regression: `36 passed`
- Final class-conformance: `PASS: 15 parity contract file(s) conform (11 activation + 4 decision-card shape-pin)`
- Ruff including shared export: `All checks passed`
- Cross-gate parity + parametrized harness: `1 failed, 293 passed, 18 skipped`; sole failure is inherited NFR-CG6 governance-version drift.
- Smoke: `181 passed, 18 skipped`
- Import-linter: `12 kept, 0 broken`
- Sandbox-AC for G5+G6 specs: `PASS`
- Broad regression: T1 `44 failed, 4132 passed`; T9 `43 failed, 4170 passed`, delta `-1 failed / +38 passed`.
