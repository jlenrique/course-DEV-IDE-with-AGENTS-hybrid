# Story 7c.3a Ready for Review

Status: ready for Claude T11 cross-agent mandatory `bmad-code-review`.

Story: `migration-7c-3a-section-02a-llm-directive-composer-body`

## Diff Summary

Implemented the Section 02A LLM-driven directive composer body:

- New `app/composers/section_02a/` package with directive models, cache,
  prompt helpers, composer body, and UTF-8 directive YAML writer.
- New Jinja2 prompt template at `docs/conversational-gates/section-02a-composer.j2`.
- New focused tests under `tests/composers/section_02a/`.
- C5 import-linter target list populated in `pyproject.toml`.
- UTF-8 AST scan extended to the new composer writer.
- Existing structural pins updated so C5 is expected populated while C6 remains empty.

## Contract Compliance

| Decision | Verdict | Evidence |
| --- | --- | --- |
| D1 module path | PASS | `app/composers/section_02a/composer.py` |
| D2 model path | PASS | `app/composers/section_02a/directive_model.py` |
| D3 signature | PASS | `compose(corpus_dir: Path, *, llm: BaseChatModel, cache: ComposerCache | None = None) -> Directive` |
| D4 Pydantic model | PASS | Triple-layer `DirectiveRole` red-rejection, `extra="forbid"`, `validate_assignment=True`, UUID4 and tz-aware validators, binary-file invariant |
| D5 excluded reasons | PASS | `.gitkeep`, `.DS_Store`, `Thumbs.db` rule-excluded before LLM |
| D6 prompt template | PASS | `docs/conversational-gates/section-02a-composer.j2` |
| D7 cache | PASS | `ComposerCache`, prompt normalization regex, SHA256 cache keys, hit/miss tests |
| D8 TW-7c-2 trigger | PASS | Trial-2 regression test passes; no TW-7c-2 firing |
| D9 C5 target list | PASS | C5 populated with the locked three forbidden modules; lint-imports 12 KEPT |
| D10 live LLM marker | PASS | No live LLM tests added; focused tests use fixture-replay `BaseChatModel` |
| D11 fixture path | PASS | Regression test reads canonical Trial-2 fixture and skips explicitly if absent |

No contract deviations were taken.

## Verification

- Focused/impact tests:
  `.venv/Scripts/python.exe -m pytest tests/composers/section_02a/ tests/structural/test_import_linter_c5_target_list_populated.py tests/structural/test_directive_io_uses_utf8_explicit.py tests/structural/test_import_linter_contracts_c4_c5_c6_present.py tests/structural/test_import_linter_c4_target_list_populated.py -p no:randomly -q --tb=short`
  - Result: 21 passed.
- R3 broad regression:
  `.venv/Scripts/python.exe -m pytest -p no:randomly -q --tb=line`
  - T1 baseline before 7c.3a edits: 39 failed, 4063 passed, 27 skipped, 2 xfailed, 11 warnings.
  - Final result: 39 failed, 4077 passed, 27 skipped, 2 xfailed, 11 warnings.
  - Delta: +14 passes; known checkout-level 39 failures preserved; no new 7c.3a failures.
- Class conformance:
  - Result: PASS, 11 activation contract files conform.
- Import linter:
  - Result: 12 kept, 0 broken.
- Sandbox AC:
  - Result: PASS.
- Ruff:
  - Result: clean.

## TW-7c-2

TW-7c-2 did not fire. The Trial-2 regression test verifies that the new
composer does not reproduce the broken corpus-scan directive:

- `.gitkeep` becomes `ignored` with `git-marker-file`.
- `APC C1-M1 Tejal 2026-03-29.docx` becomes `primary`.
- PNG/JPG/PDF/PPTX binaries keep `expected_min_words=None`.

## Self-Review

Blind layer:

- Verified no import of `app.marcus.orchestrator.directive_composer`.
- Verified production changes are isolated under `app/composers/section_02a`.
- Verified no live LLM call path is used by default tests.

Edge layer:

- Cache test proves second identical compose call avoids a second LLM invocation.
- Rule-excluded metadata files skip the LLM and therefore do not consume cache.
- The old C5-empty structural expectations were updated because C5 target
  population is the explicit D9 deliverable.

Auditor layer:

- AC-A through AC-F are covered by focused tests and structural pins.
- T1-T10 are checked in the story record. T11 remains for Claude.
- Optional A11 catalog augmentation was deferred to preserve K-target.

## Deferred Follow-Ons

- At T11/story-close, update the deferred-inventory entry
  `trial-2-finding-2-directive-composer-corpus-scan-fallback` to closed-by-7c.3a
  if Claude accepts the implementation.
- Live LLM cache-hit evidence remains a later operator/live-run concern; this
  story proves deterministic fixture-replay cache behavior.
