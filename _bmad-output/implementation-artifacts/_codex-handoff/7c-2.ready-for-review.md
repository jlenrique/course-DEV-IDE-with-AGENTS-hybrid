# Story 7c.2 Ready for Review

**Story:** `migration-7c-2-cp1252-windows-portability-fix`  
**Status:** Codex T1-T10 complete; story flipped to `review`.  
**Date:** 2026-05-04  
**Gate:** single-gate; Claude `bmad-code-review` still required by BMAD sprint governance.

## Implementation Summary

- Replaced the G0 directive-review default print callback in `app/marcus/cli/trial.py` with Option A: `_utf8_safe_print()`, which writes UTF-8 bytes to `sys.stdout.buffer` with a text-stream fallback.
- Added `app/marcus/utils/cp1252_fixture_compare.py`, a byte-level fixture comparator with CRLF/LF normalization for comparison only, divergence diagnostics, and `python -m` JSON CLI output.
- Added focused cp1252/Unicode regression coverage for U+202F NNBSP plus related typographic/macOS filename characters, explicitly removing `PYTHONIOENCODING`.
- Added structural guards for directive UTF-8 IO, A11 catalog coverage, and 7c.2 AMELIA-P1 path isolation.
- Updated `docs/dev-guide/specialist-anti-patterns.md` A11 with the Slab 7c §02A cp1252 worked example.
- Updated `_bmad-output/planning-artifacts/deferred-inventory.md` to close Trial-2 finding #1 by Story 7c.2.

## File List

- Modified: `app/marcus/cli/trial.py`
- New: `app/marcus/utils/cp1252_fixture_compare.py`
- New: `tests/integration/marcus/cli/test_trial_g0_print_nnbsp.py`
- New: `tests/integration/marcus/cli/test_trial_print_utf8_safe.py`
- New: `tests/unit/marcus/utils/test_cp1252_fixture_compare.py`
- New: `tests/structural/test_directive_io_uses_utf8_explicit.py`
- New: `tests/structural/test_anti_pattern_a11_slab_7c_example_present.py`
- New: `tests/structural/test_7c_2_path_isolation_honored.py`
- Modified: `docs/dev-guide/specialist-anti-patterns.md`
- Modified: `_bmad-output/planning-artifacts/deferred-inventory.md`
- Modified: `_bmad-output/implementation-artifacts/migration-7c-2-cp1252-windows-portability-fix.md`
- Modified: `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Verification

- Focused 7c.2 tests: PASS, `16 passed`.
- Ruff on touched Python files: PASS, `All checks passed!`.
- Import-linter: PASS, `12 kept, 0 broken`; KEPT count unchanged from post-7c.0a baseline.
- Sandbox-AC validator: PASS.
- Parity class-conformance validator: PASS, `11 activation contract file(s) conform`.
- Broad regression: executed and remains red from checkout/environment drift outside 7c.2 scope: `38 failed, 4005 passed, 27 skipped, 46 deselected, 2 xfailed`.

## Path Isolation

AMELIA-P1 honored. 7c.2 did not touch `app/parity/contracts/**`, `app/models/tripwire_ledger.py`, `app/audit/**`, audit chain tests, ADR 0001/0002, or `pyproject.toml` import-linter contracts. The structural guard accepts the two valid review-lane states observed during development: clean committed 7c.0a or the original dirty 7c.0a diff baseline; any other forbidden-path diff fails.

## T10 Self-Review

**Blind Hunter:** No direct regression found in the cp1252 path. `_confirm_or_edit_directive()` still supports injected `print_fn`, so existing tests and callers can bypass real stdout when needed. The default path now uses UTF-8 bytes and no longer depends on operator environment variables.

**Edge Case Hunter:** Covered NNBSP, NBSP, em dash, curly quotes, CRLF/LF fixture normalization, cp1252 re-encoding drift, and JSON CLI exit behavior. One notable edge is pytest capture: `_utf8_safe_print()` includes a text-stream fallback for stdout objects without `.buffer`.

**Acceptance Auditor:** ACs A-E satisfied. T1 decisions documented: `specialist-anti-patterns.md` chosen as canonical A11 home; `app/marcus/utils/` chosen for the runtime-adjacent comparator; Option A selected for stdout. T9 broad-regression gate is executed but red for pre-existing failures, not 7c.2 scope.

## Review Notes

The broad suite still includes historical failures unrelated to this story: v42 template/manifest drift, populated sanctum/cache expectations, replay pack hash drift, Windows psycopg ProactorEventLoop incompatibility, model catalog drift, run HUD drift, and older migration status assertions. The focused 7c.2 tests pass both standalone and within the broad run.
