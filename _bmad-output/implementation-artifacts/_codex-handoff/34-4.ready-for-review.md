# Story 34-4 Ready For Review

**Story:** `migration-34-4-wrangler-metadata-sme-refs-additive-emission`  
**Status:** `review`  
**Date:** 2026-05-22  
**T11 requested:** Claude standard `bmad-code-review` (single-gate)

## Summary

Story 34-4 implementation is ready for T11 review.

Implemented contract coverage:

- D1: `_write_metadata_json()` preserves `provenance` and additively emits `sme_refs`.
- D2: `content_digest` is computed per source from `SourceOutcome.content_text.encode("utf-8")`, not from the whole bundle.
- D3: `sme_refs[].path` is the locator for `local_file` sources and `null` for non-local providers.
- D4: ignored-row exclusion is inherited from Story 34-2 filtering before materialization; no extra filter was added.
- D5/AC-34-4-A-EXT: the Story 34-1 integration ratchet now asserts `metadata.json::sme_refs[]` presence, non-empty shape, provenance preservation, and one-to-one source IDs.

T5 consumer verification:

- `app/marcus/intake/pre_packet.py::_build_sme_refs` already prefers non-empty `metadata["sme_refs"]` and parses each entry into `SourceRef`.
- `app/marcus/lesson_plan/log.py::SourceRef` shape is satisfied by the emitted keys `{source_id, path, content_digest}`.
- No `pre_packet.py` or `SourceRef` changes were needed.

## Files

Modified:

- `skills/bmad-agent-texas/scripts/run_wrangler.py`
- `tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py`
- `_bmad-output/implementation-artifacts/migration-34-4-wrangler-metadata-sme-refs-additive-emission.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

New:

- `skills/bmad-agent-texas/scripts/tests/test_run_wrangler_sme_refs_emission.py`
- `_bmad-output/implementation-artifacts/_codex-handoff/34-4.ready-for-review.md`

Untouched as required:

- `app/composers/section_02a/_wrangler_translator.py`
- `app/composers/section_02a/*`
- `app/marcus/intake/pre_packet.py`
- `app/marcus/lesson_plan/log.py`

## Verification

- T1 C1 ancestry: `3159a0e` is an ancestor of `HEAD`.
- T1 predecessors: Stories 34-1, 34-2, and 34-3 are `done` in `sprint-status.yaml`.
- T1 Story 34-1 ratchet before edits: `.venv/Scripts/python.exe -m pytest tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> `3 passed`.
- T1 TW-7c-4 audit: `.venv/Scripts/python.exe -m pytest tests/audit/test_audit_tw_7c_4_no_live_dispatch_scope_creep.py -v` -> `5 passed`.
- Focused Story 34-4 + ratchet: `.venv/Scripts/python.exe -m pytest skills/bmad-agent-texas/scripts/tests/test_run_wrangler_sme_refs_emission.py tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> `6 passed`.
- Ruff: `.venv/Scripts/python.exe -m ruff check skills/bmad-agent-texas/scripts/run_wrangler.py skills/bmad-agent-texas/scripts/tests/test_run_wrangler_sme_refs_emission.py tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py` -> PASS.
- Combined 34-2/34-4/audit/ratchet surface: `.venv/Scripts/python.exe -m pytest tests/audit/test_audit_tw_7c_4_no_live_dispatch_scope_creep.py skills/bmad-agent-texas/scripts/tests/test_run_wrangler_role_enum_union_and_excluded_reason.py skills/bmad-agent-texas/scripts/tests/test_run_wrangler_sme_refs_emission.py tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py -v` -> `31 passed`.
- lint-imports: `.venv/Scripts/lint-imports.exe` -> `13 kept, 0 broken`.
- T8 broad regression: `.venv/Scripts/python.exe -m pytest --tb=no -q --no-header` -> `85 failed, 4477 passed, 27 skipped, 2 xfailed`.

Broad-regression delta vs requested 88-baseline: `-3` failures.

## G6 Layered Self-Review

Blind Hunter:

- Finding: `provenance` remains unchanged and `sme_refs` is additive in `metadata.json`. No patch needed.
- Finding: digest derivation uses per-outcome `content_text` bytes. No patch needed.

Edge Case Hunter:

- Non-local provider path is `None`.
- Local provider path is preserved from locator.
- Empty/vacuous `sme_refs` is caught by the extended integration ratchet.
- Ignored rows are absent from both `provenance` and `sme_refs` through the inherited Story 34-2 filter.

Acceptance Auditor:

- AC-34-4-A: PASS.
- AC-34-4-A-EXT: PASS.
- AC-34-4-B: PASS by source verification; no consumer code change required.
- AC-34-4-C: n/a; translator untouched.
- AC-34-4-D: PASS via new ignored-row test.

## Review Notes

The worktree still contains the unrelated pre-existing dirty file `runs/cache-harness/irene-pass1.md`. It was not part of this story and should be ignored for Story 34-4 T11 unless the operator says otherwise.
