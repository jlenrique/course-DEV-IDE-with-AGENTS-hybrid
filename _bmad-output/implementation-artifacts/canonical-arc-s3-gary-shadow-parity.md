# Story S3 — Gary shadow-parity WARN + §06 fold + both-walks receipts

**Arc:** Canonical Production Conversation (party record §7 re-scoped S3 row; SOP-007 F-702..F-705 + carried F-402/F-304 inherited constraints).
**Status:** **DONE** (2026-07-07; full gate chain: SOP-008 CONCUR-W-F → RED-first dev (all offline ACs witnessed) → SOP-009 CONCUR-W-F + 3-lane T11 (0 decision / 7 patch / 1 defer / 4 dismissed; Auditor ACCEPT-W-NOTES all ACs MET, Blind no-Crit/High, Edge no-MUST-FIX) → RED-first remediation 7/7 → orchestrator re-verify 725-green → **AC-L LIVE WITNESS: BOTH LEGS PASS, frozen judges verbatim, first-run-stands** — leg-1 judge-1 13/13 (trial `a18c2a86`: real pick → live CD → real Gamma §07 → `ok/match`, `clock_eligible: true`, three-way digest attestation equal + non-null; **S-flip parity clock tick 1**); leg-2 judge-2 10/10 (rewind-recover trial `4d465677`, SSOT `amount: minimal→concise` mutated post-CD/pre-gary: `divergence/resolution-mismatch` WARN w/ both envelopes, digests EQUAL = genuine same-bytes disagreement per F-703/F-806, walk NOT halted, SSOT git-restored clean); evidence `s3-acl-liveproof-20260707T011735Z`. **Mid-witness discovery:** pre-existing matcher defect (apostrophe tokenization) fixed on its own merits at `59a9a48a` per party record §10 — judges never executed pre-fix, no retry-to-green) · **Size:** S→M (per §7 re-scope) · **Gate mode:** single-gate structural (3-lane review at T11) · **Branch:** `dev/workbook-2026-07-06` (S1 @ `c24308f7`, S2 @ `037be6d3`, matcher fix @ `59a9a48a`).
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, xdist rule (`-n 0` confirm before triaging new parallel reds), **no `git stash` ever** (session-16 ledger incident — baseline via throwaway worktrees). Do not commit.

## Context (landed substrate — verified 2026-07-07 at `d049453b`; line claims re-verified by SOP-008)

- **CD's audit block (S1) is the schema-v1 SSOT — F-402 (binding):** the COMMITTED `_styleguide_resolution_from_projection` emission (`app/specialists/cd/graph.py:390-586`), NOT the S1 spec's D3 sketch (stale-marked). Full key set (byte-verified at :572-586): `{schema_version, status, input_picks, bound_guides, resolved, layering_manifest, resolution_digest, directive_digest, default_provenance, errors}`. `status ∈ {resolved, no_picks_at_authoring, unresolvable_pick}`; `bound_guides[]` entries carry `{name, ssot_digest, lifecycle[, visibility]}`; `resolution_digest = sha256(canonical_json(resolved))`; errors accumulate in pick order; the block is total (never raises), a SIBLING of the load-bearing `cd_directive`. **F-304 (binding): the comparator treats this FULL emitted key set as the contract** — v1 blocks missing keys are a contract violation, not a tolerable degrade.
- **Gary's resolve site:** `_normalized_gamma_settings` (`app/specialists/gary/_act.py:437` onward) resolves per-variant via `resolve_styleguide` (:514, imported through the thin re-export `gary/styleguide_library.py` → `app/styleguide/resolver.py`); the styleguide-less branch seeds `DEFAULT_VARIANT_PAIR` with the honesty WARN (:518-528) whose marker says "fail-loud deferred to cd-envelope-authoring". **WARN-seed UNTOUCHED — S4 owns the flip (F-705).** Gary's inline variant vocabulary `{"A","B"}` lives at :464. **F-805 fence:** the resolve runs only on the `generate_gamma_variants` path (`_act.py:1182-1183`); the legacy `dispatch_to_gamma` directive-path branch (:1185) never calls it and carries NO receipt — production §07 always takes the slides path (projections deliver `slides`/`prompt`), so the parity clock is unaffected; AC-5's harness contexts MUST drive the slides path.
- **§06 fail-loud presence guarantee:** `run_builder_node` (`app/marcus/orchestrator/package_builders.py:195-247`) fails loud on a missing cd contribution (`builder.gary.upstream-missing`, :227-231) before §07 can dispatch — this is the presence guarantee S3 rides; it is EXISTING behavior (cite/pin, don't rebuild). The §06→07 manifest data plane (`pipeline-manifest.yaml:497-523`, keys `slides/prompt/additional_instructions`) is UNTOUCHED by this story (F-802 ruling, D1).
- **Runner seams:** ONE shared dispatch site (`_dispatch_specialist_at_node`, both walks); gary payload branch at `production_runner.py:1436-1443` (`export_dir` + `gamma_settings = _gamma_settings_from_directive(directive_path)`; `runs_root`/`trial_id` are in scope — they guard the branch); cd's `directive_projection` branch at :1444-1499 with the :852-854 sha256 pattern. Runner-supplied context is CHARTERED per the gary `gamma_settings` + cd `directive_projection` precedents; `dispatch_adapter` raises on runner-vs-projection key collision (:113-119) — none of the new keys collide (no projection change).
- **Trial-start attestation (S2 P10/F-604) — F-801 CORRECTED:** `start_trial` re-digests AFTER the confirm/edit loop (`trial.py:470`); the persisted `directive_digest` lands ONLY in **`run_dir/"trial-start.json"`** (written at `trial.py:536`, key built at :533). **`run.json` is the persisted ProductionTrialEnvelope and carries NO directive/digest field** — sourcing the digest from run.json would silently degrade F-703's three-way cross-check to two-way on every production run. `trial-start.json` exists before any continuation-walk §07 dispatch; absent (legacy runs, some start-walk harness contexts) ⇒ `None` honestly.
- **Import boundary:** pyproject S1 contract "cd + app.styleguide never import app.specialists.gary" (`pyproject.toml:328-332`); picker↔cd production import structurally forbidden (F-403 AST guard + lint); the F-403 test-level lockstep pin lives at `tests/marcus/orchestrator/test_picker_cd_vocabulary_lockstep.py` (two-way today).
- **Pick events:** public reader `read_pick_events` (`styleguide_picker.py:173`; `_read_pick_events` back-compat alias :205). `_RUN_TAG_RE = ^[A-Za-z0-9_]+$` (`picker_html_emitter.py:70`) — hyphens forbidden, `trial_id.hex` rule.

## Binding inheritances (SOP-008 verified all six HONORED; F-703 via the F-801 amendment)

- **F-702 — the parity clock is STATUS-KEYED.** A run counts toward the S-flip clock iff CD's envelope `styleguide_resolution.status == "resolved"` — regardless of tty/interactive-ness. Pick-presence is determined from the **directive/envelope at CD-read time** (CD's `input_picks` echo + Gary's payload), **NEVER from sidecar pick events** — an edit at the G0 gate can drop a committed pick (`_warn_if_pick_dropped_by_edit`, trial.py:472). The receipt carries an explicit `clock_eligible` boolean: true iff CD status `resolved` AND outcome `ok`.
- **F-703 — digest cross-check discriminates drift from divergence.** The comparator cross-checks trial-start `directive_digest` (from `trial-start.json`, per F-801) == CD's envelope `directive_digest` == the digest of the bytes Gary's settings were extracted from. Any mismatch ⇒ the directive mutated mid-walk ⇒ INFO (`expected-ordering-gap` family), NEVER a divergence WARN. Only same-bytes disagreement is genuine Gary-vs-CD divergence.
- **F-704 — pickable ≠ resolvable.** The comparator resolves through the shared resolver (`app/styleguide/resolver.py`) ONLY — it never imports picker pickability (`_assert_pickable_guide`, lifecycle/visibility enforcement stays pick-time). A deprecated-but-resolvable guide must compare clean. Any new Gary-side variant-vocabulary constant extends the F-403 lockstep pin (D5).
- **F-705 — environment constraints.** Use public `read_pick_events` (never the alias) if any S3 code/test touches the sidecar; tolerate heterogeneous sidecar schemas (legacy events lack the F-502 course field); `trial_id.hex` for run_tag-shaped keys; the F-605 baseline reds (13 g0-brick + gate-pin + Texas-internal ≈ 21 at HEAD; 14 reproduced live by SOP-008 in the two named suites) are carried in green-criteria — S3 neither fixes nor touches them (S5 precondition owns them); **WARN-seed untouched (S4 owns the flip).**
- **F-402/F-304** — see Context bullet 1.

## Deliverables

### D1 — CD's block reaches Gary via CHARTERED runner context (F-802 ruling, recorded)
**F-802 ruling (on the record, F-501-reinterpretation precedent):** the ratified letter's manifest-projection transport is UNSAFE — the projection machinery HARD-RAISES on an absent producer key (`dispatch_adapter.py:97-101`), and `run_builder_node`'s idempotent skip (:217-218) means a rewind-recovered/legacy envelope whose `package_builder` contribution predates S3 would reach §07 with the key absent ⇒ dispatch hard-fails, violating the ratified "rewind-recovered golden bundles must not false-fail" (W5-morph, §7). **Adopted: SOP-008 option (i)** — the gary payload branch (`production_runner.py:1436-1443`) supplies `cd_styleguide_resolution` as CHARTERED runner context, sourced from the envelope's **latest cd contribution** (`latest_for_specialist("cd")` — same selector §06 uses): the committed `styleguide_resolution` block verbatim, or `None` when the cd contribution is absent-at-dispatch-time or predates S1 (legacy; never a raise). This matches the established runner-context pattern for exactly this data family (`gamma_settings`, cd's `directive_projection`). **NO `pipeline-manifest.yaml` edit; NO `build_gary_briefs` package change; the lockstep regime is NOT triggered** (if dev finds a manifest edit unavoidable after all, STOP: the originally-recorded Tier-1 ruling + regime-doc-read-at-T1 + F-802 witness obligations apply, and the deviation is reported). The ratified "same fail-loud path" half of the letter is preserved by §06's EXISTING `builder.gary.upstream-missing` guarantee — cite the existing pin (or add one if none exists) rather than rebuilding it. T1 verification task: confirm the envelope (or the latest-cd lookup) is reachable at the gary payload call site; if the site can't see it, thread it through the shared dispatch chain within `production_runner.py` (single-module signature extension) — never via a shadow store.

### D2 — Shared parity module (neutral home, F-704-clean)
NEW `app/styleguide/parity.py`: (a) `canonical_resolution_digest(resolved) -> str` — the sha256-over-canonical-JSON helper, extracted so CD and the comparator digest identically; CD's `_canonical_json`/`resolution_digest` emission refactors to call it **byte-identically** (S1 determinism pin `tests/specialists/cd/test_styleguide_resolution_emission.py` must stay green UNMODIFIED — AC-7); (b) `compare_styleguide_resolution(cd_block, gary_view) -> receipt` — a PURE, total (never-raise) comparator taking CD's block plus Gary's view `{picks: {variant: guide_name}, resolved_base: {variant: resolver_output}, ssot_digest, directive_digest, trial_start_directive_digest}` and returning the receipt (D3 schema). It compares **base-layer resolutions** (Gary's `resolved_base` BEFORE per-variant explicit-key overrides — Amelia's on-record parity semantics: parity instruments INPUT drift across time, not resolver logic; per-variant overrides are Gary-legitimate and must not false-diverge) via `canonical_resolution_digest` + pick-set equality + `bound_guides` name/ssot_digest comparison. Import direction: gary → `app.styleguide` (already chartered via the resolver re-export); cd → `app.styleguide` (existing); **parity.py imports NOTHING from cd, gary, or app.marcus** (the existing pyproject:328-332 contract inherits over the new module; extend contracts if needed — never weaken).

### D3 — Gary audit helper at the resolve site (three-outcome, observability-only)
Immediately after `_normalized_gamma_settings` completes its resolve loop (slides/`generate_gamma_variants` path only — F-805 fence, Context bullet 2), Gary's act builds the `gary_view` and calls the comparator; the receipt rides Gary's contribution output as a sibling key `styleguide_parity` (provenance in the carrier — no shadow store, no new file; Gary's output assembly at `_act.py:1160-1172` is additive-safe). **Read-once alignment:** Gary reads the SSOT bytes ONCE (mirroring CD's T5 discipline via `load_style_guides(path, content=ssot_bytes)`) and passes `guides=` into its existing `resolve_styleguide` calls (`resolve_styleguide(name, *, guides=None, path=None)` — signature-compatible, resolver.py:432-452) — resolved OUTPUT byte-identical (AC-4 pins it), and the read's sha256 feeds the comparator's `gary_view.ssot_digest` — surfacing in the receipt via the `gary_bound_guides` entries (and `detail` on divergence), NOT as a top-level receipt field (external-monitor F-003 lockstep fix 2026-07-07: the receipt schema below is the authority; this prose previously misnamed a top-level `gary_ssot_digest`).

**Receipt schema (v1):** `{schema_version: 1, outcome, reason, cd_status, clock_eligible, cd_resolution_digest, gary_resolution_digest, cd_directive_digest, gary_directive_digest, trial_start_directive_digest, cd_bound_guides, gary_bound_guides, detail}` — on `divergence` only, `detail` carries both envelopes (CD block verbatim + gary_view) per §7's "WARN receipt carrying both envelopes + digests".

**Three-outcome classification (ratified trichotomy; `reason` carries the sub-case; NEVER raise, NEVER alter dispatch):**
- `ok` (silent — receipt data only, no log): `reason=match` — CD `resolved`, digests aligned, base resolutions byte-equal (`clock_eligible: true`); `reason=status-keyed-no-picks` — CD `no_picks_at_authoring` AND Gary pickless: the F-202 asymmetry (Gary seeds `DEFAULT_VARIANT_PAIR`; CD binds default-A) is keyed on status and NOT settings-compared (`clock_eligible: false`).
- `expected-ordering-gap` (INFO log, never cry-wolf WARN): `reason=cd-saw-no-picks` — CD `no_picks_at_authoring` but Gary's payload carries picks (pick landed after 4.75 — legacy/G2B-era flow); `reason=directive-drift` — F-703 digest mismatch (any of the three digests disagree, `None` treated as not-comparable never as mismatch): the directive mutated mid-walk, comparison of resolutions is not meaningful; `reason=cd-envelope-absent-legacy` — `cd_styleguide_resolution` is None (pre-S1 bundle / legacy); `reason=cd-schema-newer` — `schema_version > 1` (forward-compat, never WARN-spam).
- `divergence` (WARN log via Gary's logger — distinct message from the :523 WARN-seed; never raise): `reason=resolution-mismatch` — same directive bytes (digests equal), CD `resolved`, but pick sets / bound guides / base-resolution digests disagree (includes mid-run SSOT drift — `ssot_digest` comparison in the receipt discriminates it for triage); `reason=cd-unresolvable-but-gary-resolved` — CD `unresolvable_pick` yet Gary resolved the SAME bytes cleanly (resolver disagreement = real defect); `reason=contract-violation` — a v1 block violating the F-304 full key set.

**Fences:** the audit is observability-ONLY — Gary's dispatched Gamma packet and merged settings are byte-identical with the audit present (AC-4; 🔒 source-detail→Gamma conveyance untouched); the WARN-seed branch (:518-528) is byte-untouched; no new ceremony (X3 — receipts are data; WARN is a log line, not a pause).

### D4 — Runner digest threading (F-703 inputs; chartered runner context; F-801 amended)
The gary payload branch (`production_runner.py:1436-1443`) additionally supplies: `directive_digest` — sha256 of the SAME bytes `_gamma_settings_from_directive` parsed (read-once: refactor the helper to read bytes once and hand both out; :852-854 pattern); `trial_start_directive_digest` — **from `run_dir/"trial-start.json"`'s `directive_digest` key (F-801: NOT `run.json`, which is the ProductionTrialEnvelope and has no digest field)**, `None` when the file or key is absent (legacy runs; comparator then classifies on the remaining two digests, recording the third as not-comparable). All three new payload keys (`cd_styleguide_resolution`, `directive_digest`, `trial_start_directive_digest`) join `CONSUMED_PAYLOAD_KEYS` in `app/specialists/gary/payload_contract.py` (F-804: that is the source constant; `GARY_CONSUMED_PAYLOAD_KEYS` is only the import alias at `package_builders.py:35`); none collides with a projection key (collision guard stands; no projection change). Extend the :1405-1412 seam docstring enumeration (F-203 precedent).

### D5 — F-403 lockstep pin extension (F-704 rider)
Gary's inline `{"A","B"}` (:464) is promoted to a named module constant; `test_picker_cd_vocabulary_lockstep.py` extends to a THREE-way set-equality pin (picker `_VARIANT_IDS` == cd `_PICK_VARIANT_VOCABULARY` == gary constant), keeping the comment naming the structural no-production-import rule. Production import coupling between the three modules stays forbidden (existing AST guard + lint contracts untouched).

## Acceptance criteria

- **AC-1 (non-vacuity trio — three fixtures, three DISTINCT outcomes):** (1) divergence-MUST-WARN: same directive bytes, SSOT content differing between CD's resolution and Gary's read (via `ssot_path`/`content` injection) ⇒ `divergence/resolution-mismatch` receipt with both envelopes + a WARN log record asserted present; (2) match-silent: same picks, same SSOT ⇒ `ok/match`, `clock_eligible: true`, NO log record; (3) gap-INFO: CD block with `status=no_picks_at_authoring` + Gary payload WITH picks ⇒ `expected-ordering-gap/cd-saw-no-picks`, INFO not WARN.
- **AC-2 (F-702 status-keying):** both-pickless run ⇒ `ok/status-keyed-no-picks`, `clock_eligible: false`, and the test asserts (F-804) Gary's `DEFAULT_VARIANT_PAIR` seed was NOT compared against CD's default-A resolution; `clock_eligible` is true ONLY on `resolved`+`ok/match` (parametrized witness over all outcome/status combinations).
- **AC-3 (F-703 drift discrimination + F-801 non-vacuity):** trial-start digest ≠ CD digest (or CD ≠ Gary-read) with otherwise-divergent resolutions ⇒ `expected-ordering-gap/directive-drift`, NEVER `divergence`; all three digests echoed in the receipt. **F-801 sub-witness (mandatory):** a fixture with a REAL `trial-start.json` on disk yields a receipt whose `trial_start_directive_digest` is NON-null and equal to that file's `directive_digest` — the three-way check must be proven three-way, not silently two-way.
- **AC-4 (observability-only, protected invariant):** with the audit active, Gary's normalized settings, merged per-variant overrides, and dispatched packet are byte-identical to `037be6d3` behavior on identical inputs (golden-comparison witness); the WARN-seed branch text/behavior byte-untouched; `resolve_styleguide(name, guides=...)` output byte-identical to the path-reading call.
- **AC-5 (both-walks receipts):** parametrized witness over the start-walk and continuation-walk harness contexts (S1/S2 walk-pin pattern, shared dispatch site), driving the slides/`generate_gamma_variants` path (F-805): Gary's contribution carries `styleguide_parity` in BOTH; resume of a receipted run does not re-audit into a shadow store (idempotent per the walkers' skip rule).
- **AC-6 (F-304/F-402 contract teeth + F-802 legacy tolerance):** a v1 CD block missing any SSOT key ⇒ `divergence/contract-violation`; `schema_version: 2` ⇒ `expected-ordering-gap/cd-schema-newer`; `cd_styleguide_resolution: None` (legacy/absent cd) ⇒ `expected-ordering-gap/cd-envelope-absent-legacy` — all three witnessed. **F-802 witness (mandatory):** a legacy envelope whose package_builder/cd contributions predate S3 dispatches §07 CLEANLY (no raise, receipt honest) — rewind-recovered golden bundles must not false-fail.
- **AC-7 (CD byte-stability through the D2 refactor):** the S1 determinism pin passes UNMODIFIED; CD's emission before/after the `canonical_resolution_digest` extraction is byte-identical on a fixed projection.
- **AC-8 (pins extended, never weakened):** every exact-shape pin that trips on the new payload keys is EXTENDED (F-205 discipline) — the known trip-points are the gary projection key-set pins `tests/integration/marcus/test_package_builders.py:261,516` IF they pin payload composition (T1: verify; the §06 package pin at :212 must NOT trip — this story does not touch the package); T11 Acceptance Auditor rejects weakened equality assertions. Ratchet-D (`tests/contracts/test_manifest_payload_contracts.py:31,51`) stays green via the contract superset.
- **AC-9 (lockstep pin):** the three-way vocabulary pin is green; no production import between picker/cd/gary styleguide modules (existing guards green).
- **AC-L (live parity teeth-witness — orchestrator runs it; PASS required, skip never counts):** **Leg 1 (matching-pick PASS):** one REAL run with a committed pick crossing 4.75 (live CD dispatch) and §07 (live Gary/Gamma dispatch) — golden-bundle rewind-recover (rewind point per the F-802 ruling: any point works, legacy §06 packages are tolerated) or a fresh minimal corpus, orchestrator's call — asserting `styleguide_parity.outcome == ok/match` + `clock_eligible: true` in the persisted contribution. **Leg 2 (forced-divergence WARN):** same-shape run where `state/config/gamma-style-guides.yaml` is deliberately mutated AFTER CD's resolution and BEFORE §07, then git-restored — **F-806: mutate a RESOLVER-EMITTED field of the picked guide (e.g., a `prompt_configuration` value), so BOTH `ssot_digest` AND the base-resolution digest diverge** (a metadata-only mutation witnesses only half the comparator) — asserting `divergence/resolution-mismatch` + WARN log + both envelopes in the receipt, run NOT halted. Directive bytes untouched in leg 2 (digests must MATCH — that is what makes the divergence genuine per F-703). First-run-stands; **cleanup receipts APPEND, never overwrite (F-701)**; expected spend: 2× (CD fast-tier + Gamma deck) — cost not a constraint; live-leg driven FOREGROUND with hard timeout (session-16 gotcha (a)).

## RED-first plan
1. `tests/styleguide/test_parity_comparator.py` — the AC-1 trio + AC-2/AC-3/AC-6 classification table against the pure comparator (incl. the F-801 non-null-digest and F-703 None-is-not-mismatch cases) — ALL RED (module absent).
2. `tests/specialists/gary/test_styleguide_parity_receipt.py::test_receipt_rides_contribution` — RED (no audit wired); `::test_dispatch_bytes_identical_with_audit` (AC-4) — RED-by-construction.
3. `tests/specialists/cd/test_styleguide_resolution_emission.py` — GREEN UNMODIFIED through D2 (AC-7 golden constraint, not a new test).
4. Walk-pin extension (S1/S2 harness): both-walks receipt presence (AC-5) + the F-802 legacy-envelope clean-dispatch witness (AC-6) — RED until D1+D4 thread the data.
5. `test_picker_cd_vocabulary_lockstep.py::test_three_way_vocabulary_identical` — RED (gary constant absent).
6. `CONSUMED_PAYLOAD_KEYS` extension + any tripped projection pins extended (AC-8) — RED where exact-set-pinned.
7. Pre-existing battery: green UNTOUCHED except the F-705 baseline reds (SOP-008 reproduced 14 in the two named suites at HEAD; `-n 0` before triaging any NEW red).

## Out of scope
WARN-seed flip + parity-WARN→ERROR (S4) · authority flip / Gary consuming CD's envelope / yaml-read collapse (S-flip, deferred-inventory entry gate) · g0_enrichment battery greens (S5 precondition) · G0 beats (S5) · Tracy (S6) · workbook (S7) · picker/emitter changes · variant-vocabulary changes (F-403 forbids without lockstep ceremony) · any sidecar-event-derived pick-presence (F-702 forbids) · `pipeline-manifest.yaml` / §06 package changes (F-802 ruling — runner-context transport) · the legacy `dispatch_to_gamma` directive-path branch (F-805 — carries no receipt by design).

## Review Findings (T11 3-lane + SOP-009, 2026-07-07 — triaged: 0 decision-needed / 7 patch / 1 defer / 4 dismissed / 3 notes)

**Patch batch (RED-first, fresh dev agent):**
- [x] [Review][Patch] P1 (MED, blind): the trial-start attestation witness fabricates the file — add a producer-side test driving the REAL `start_trial` write and asserting `_trial_start_directive_digest(run_dir)` returns the digest it persisted (closes the last silent-degrade seam on F-703: a producer-side key rename must go RED). [tests/orchestrator/test_gary_parity_payload_seam.py]
- [x] [Review][Patch] P2 (LOW, blind+edge+auditor): `schema_version` type gate — `True`/`1.0` currently pass as v1 (bool/float coercion) and `"2"` lands contract-violation. Fix: version claim is valid iff `type(x) is int`; int > 1 ⇒ `cd-schema-newer`; any non-int claim ⇒ `contract-violation` (Auditor's ruling: a non-int is not a valid forward-compat claim). Witnesses for `True`, `1.0`, `"2"`. [app/styleguide/parity.py]
- [x] [Review][Patch] P3 (LOW, edge): the `expected-ordering-gap` INFO log says "expected pre-S2/legacy state" for ALL four gap reasons incl. `directive-drift` (a mid-walk directive mutation — triage-worthy, not legacy). Reason-keyed message; no "legacy" editorializing on drift/schema-newer. [app/specialists/gary/_act.py]
- [x] [Review][Patch] P4 (LOW, edge+blind): serialization-safe, decoupled receipt payloads — (a) divergence/contract `detail` envelopes round-tripped through canonical JSON (deep-copied; unserializable values guarded to a bounded repr excerpt) so the never-raise guarantee extends through envelope persistence and the receipt never aliases the live cd contribution; (b) `cd_styleguide_resolution` payload value + `resolved_base` capture decoupled (deepcopy at capture); (c) crash-path fallback `detail` carries best-effort digests alongside `comparator_error`. Witness: post-receipt mutation of source objects leaves the receipt unchanged; an unserializable view value still yields a persistable receipt. [parity.py, production_runner.py, _act.py]
- [x] [Review][Patch] P5 (LOW, blind): `_trial_start_directive_digest` docstring promises WARN on malformed content but a present-but-non-string/empty `directive_digest` returns None silently — WARN-log that path (a producer-side type regression must be operator-visible). [production_runner.py]
- [x] [Review][Patch] P6 (LOW, blind): AC-4 canonicalizer soft teeth — drop `default=str` (type-differing packets must not canonicalize equal) and assert `generate_calls` non-empty in the byte-identity test. [tests/specialists/gary/test_styleguide_parity_receipt.py]
- [x] [Review][Patch] P7 (LOW, auditor): double-resume witness — resume a run whose §07 already carries a receipt; assert `gary_dispatch_count` stays 1 and the persisted receipt is byte-unchanged (AC-5's "never re-audit" clause gets teeth). [tests/composition/test_gary_parity_walk_pin.py]

**Defer:**
- [x] [Review][Defer] E4 (edge, INFO): `clock_eligible=True` accrues on two-way matches (`trial_start_directive_digest=None` — gateless starts, single-file trials writing `directive_digest: null`). F-702-letter-compliant today (SPOC product path always writes the attestation); whether the S4 flip should require the full three-way attestation is an S4 spec-time design decision — routed to the S4 spec author via deferred-work.md.

**Dismissed:** bound_guides same-guide-both-variants false-WARN (blind — execution-REFUTED by the Edge lane's probe through the real CD emission + real Gary loop: sorted-pair compare neutralizes it); resolver kwargs/imports invisible in diff (blind — verified landed by SOP-009 + Auditor, suites green); adapter collision-surface widening (edge INFO — intended fail-loud, note for manifest authors); pre-existing directive-parse crash family + pre-audit `list_themes` ordering (edge INFO — pre-existing, already filed at deferred-work.md:52).

**Notes recorded (no action):** AC-4 equivalence is within-tree A/B + resolver identity, not a cross-commit golden — ruled honestly covered by BOTH SOP-009 (F-901) and the Auditor independently; AC-L leg 1 is the de-facto cross-version confirmation. Comparator precedence: status-keying evaluates before drift (both classes non-WARN + clock-ineligible — no F-702/F-703 harm; revisit wording only if S4 tightens). Close-commit bookkeeping: 16 touched/new .py files, not 15 (SOP-009 F-902).

## T11 gates
SOP-008 CONCUR-W-F satisfied (F-801/F-802 applied above; F-803 moot — no manifest edit; F-804 applied; F-805/F-806 encoded); SOP-009 at dev-complete (must verify F-801/F-802 landed as amended + AC-L rewind point consistent with the F-802 ruling); 3-lane `bmad-code-review` on the diff; ruff 0-new on touched; lint-imports contracts kept (16+/0); focused suites + trial/walk battery green with the F-705 baseline carried; story flips done ONLY after the live AC-L both-leg PASS.
