# Claude Shadow Monitor - Operator HUD Revival - 2026-07-11

Independent shadow ledger for the Operator-HUD revival arc (branch `dev/hud-revival-2026-07-11`): brief → bmad-ux → bmad-architecture → epics/stories, per the adopted party plan §5.

## Standing Watchpoints

- **Monitor boundary:** inspect the repo read-only and write only this shadow ledger. Do not edit production code, tests, runtime state, commits, branches, or BMAD-owned artifacts.
- **Product boundary:** the product is the Marcus-SPOC runtime's operator-facing instrument panel. The HUD serves the SPOC-orchestrated runtime; nothing may be designed for proofing/concierge vehicles (CLAUDE.md framing guardrail).
- **Binding SSOT:** `_bmad-output/planning-artifacts/briefs/brief-operator-hud-revival-2026-07-11/brief.md` (status final, operator amendments applied) + `addendum.md`; workflow chain + evidence strategy from `_bmad-output/planning-artifacts/hud-revival-party-assessment-and-plan-2026-07-11.md` §5. **The brief supersedes the party plan's §3 wherever they diverge** — the §7 binding fresh-requirements interview gate was satisfied at the brief phase (addendum §A).
- **Core invariants (from the brief, binding):** read-only zero-button (next actions are copy-paste commands, digest pre-filled); zero-lie rule (never contradict the envelope; unrecognized renders as unrecognized; every tile admits its age; dead feed → explicit STALE); self-updating locally served page; glanceable dark-cockpit; no-mocks heartbeats (real live calls, cheap paid pings fine); configurable notifications incl. phone push + run-stalled watchdog in v1.
- **Scope fence (brief IN/OUT):** IN — pre-flight + heartbeat visualization, two-stage progress map with drill-down, gate briefings, specialist/service icons with briefings, read-only state-trace window, health strip, modality indicators, YAML-configured notifications with phone push + stall watchdog, locally served page. OUT — any interactive affordance beyond copy-paste text, fleet/multi-run views, historical run browsing, email/webhook, trace-waterfall re-implementation (link out to LangSmith), eval/drift panels, Dev-Cycle/sprint panels, M5 relic panel.
- **Claim hygiene:** UX spines may DEMAND a runtime-owned operator-surface projection contract from architecture; they may not ASSERT one exists. The projection contract (versioned, written by BOTH runner walks, dual shape-pins) is an architecture-phase deliverable per brief §Constraints.
- **Carry-through set:** pre-flight contract (falter-surface coverage definition), seven-status completeness against `app/models/runtime/production_trial_envelope.py`, Sally non-negotiables (answer-first 5-second rule, every-pause-renders-a-next-action, artifacts-under-judgment inline, staleness honesty, Dev-Cycle/M5 out), `hud_per_step_summary.py` urgency auto-expand contract verbatim.
- **Assumption discipline:** team-delegated decisions (notification defaults, staleness budgets, watchdog budget, iconography, health thresholds) must carry [ASSUMPTION] tags surfaced for operator review at the UX gate — never silently pinned.
- **Governance lookahead (dev phase, not UX):** `run_hud.py`, `progress_map.py`, and their tests are pipeline-lockstep `block_mode_trigger_paths` (pipeline-manifest:63-67); all dev on them is manifest-regime governed. Model policy: production pipeline stays OpenAI GPT-5 family; Fable/Claude are agent/IDE models only.
- **Evidence bar (from party plan §5, Murat):** L1 enum contract test vs envelope vocabulary, L2 golden run-dir fixtures per pause class + one legacy dir rendering "unrecognized", L3 first-run-stands live witness per pause class. Applies at dev, but UX/architecture artifacts must not pre-claim it.

---

### SOP-HUD001 — UX-phase gate poll (bmad-ux finalize) - 2026-07-11T13:51:16-04:00

**Scope reviewed:** brief.md + addendum.md (final, operator-amended); party plan §5 adopted workflow + §7 interview gate; the bmad-ux CREATE run output at `_bmad-output/planning-artifacts/ux-designs/ux-operator-hud-2026-07-11/` — `EXPERIENCE.md` (236 lines), `DESIGN.md` (220 lines), `.memlog.md`, `mockups/flight-deck-{preflight,in-flight,paused-at-gate}.html`; envelope status vocabulary independently verified at `app/models/runtime/production_trial_envelope.py:19-27`; mockups grepped for interactive elements; `git status`/`git log`. No tests run. This ledger file is the only write.

**1. Sequence integrity — CLEAN.** The run follows the adopted §5 order: brief (final, committed at `59349de2`) → bmad-ux (this output) → architecture next. Both spines list the brief + addendum + party plan as sources, and `.memlog.md` opens with the correct precedence decision: "Brief supersedes party plan where they diverge (fresh operator interview)." The §7 binding operator-interview gate was satisfied at the brief phase (addendum §A interview record), so an autonomous UX run with [ASSUMPTION] tags for delegated detail is the sanctioned path — the memlog records exactly that posture. One apparent divergence is a legitimate supersession, not drift: party plan §5.2 prescribed "moment-based IA (returning/watching/deciding/closing — NOT a panel inventory)," while EXPERIENCE.md's IA is five fixed zones. The brief's interview demoted the returning-to-a-paused-run job to a supporting moment and pinned the real-time cockpit ("actively piloting this jet, mid-flight"); the moments survive as the six Key Flows. Recorded as supersession (F-HUD-0006).

**2. Scope fidelity — CLEAN.** Read-only zero-button holds throughout: EXPERIENCE.md's Interaction Primitives section enumerates the entire interactivity budget (disclosure, text selection, links out, scroll) and explicitly bans buttons/forms/inputs/verdict affordances; the three mockups contain zero `<button>`/`<form>`/`<input>`/`onclick` elements (only comments asserting "zero buttons"). Dev-Cycle/M5 panels, fleet views, historical browsing, and trace-waterfall re-implementation are all explicitly REJECTED in the Inspiration & Anti-patterns section (LangSmith link-out honored). Phone push is IN with per-event defaults; email/webhook stated OUT. Stall watchdog present with copy pattern and the addendum §F shared-notifier architecture note carried. No silent drops found; the health-tile "history-this-run" drill is within-run detail, not historical run browsing (small internal reconciliation noted at F-HUD-0004). The seven-vs-six status expansion is the one scope *extension*: the brief's criterion 1 enumerates six statuses, the envelope model defines seven (`registered` included) — this monitor independently verified the seven at `production_trial_envelope.py:19-27`. EXPERIENCE.md fences the expansion transparently under the brief's own "every envelope status" clause. Correct call, correctly disclosed (F-HUD-0002).

**3. Claim hygiene — CLEAN with one caution.** The Projection Demands section is properly demand-phrased: "What each zone requires the runtime-owned operator-surface projection to carry — the UX-side half of the contract; architecture owns its schema, versioning, and dual-walk write points." No assertion that the projection exists. The zero-lie restatement is behavioral and testable ("the HUD performs no inference, no fallback resolution, and composes no command strings"), and the command block explicitly renders only "the exact command string the runtime projection supplies." Flow commands and fixture facts are grounded in the real run-dir of trial `22b27500…` (memlog fixture-grounding entries), not invented. One tension: the modality-chip component spec names raw file sources directly ("from `run.json` `llm_execution_mode`", detective sidecar presence, `directive.yaml` styleguide) — phrased as if the HUD reads run-dir files, which would contradict zero-lie rule 1. The Projection Demands list does carry "modality readings (llm_execution_mode, detective disposition, styleguide + provenance)", so the parentheticals should be read as provenance documentation; architecture should pin this (F-HUD-0003).

**4. Binding-principle carry-through — CLEAN.** Zero-lie: dedicated section with five testable rules, plus unknown ≠ bad ≠ stale as three distinct renderings and the "confidence-tagged green only" rule. Staleness honesty: mandatory per-tile stamps, budgets, stale veil, feed-lost escalation, missed-heartbeat as its own alarm. Pre-flight contract: rendered per-item as tested, heartbeat rows split connectivity from sufficiency with confidence tags, never-false-green honored; the falter-surface item inventory itself correctly remains an architecture deliverable per the brief. Seven-status completeness: full state table with distinct annunciator/panel/next-action per status. Sally non-negotiables all present: five-second rule promoted to a layout law (above-the-fold at 1440×900, restated as an accessibility zoom constraint); every pause class renders a next-action command block (with the honest `failed` fallback "no automated recovery — see SPOC"); artifacts-under-judgment inline in gate briefings (G2B thumbnails, G4A voice rows); urgency auto-expand contract carried **verbatim** from `hud_per_step_summary.py` including auto-expand-beats-remembered-collapse; Dev-Cycle/M5 ruled out. The April HUD's silent wrong-run fallback is explicitly killed ("RUN IDENTITY UNCERTAIN — refusing to render").

**5. Assumption discipline — CLEAN with one caution.** [ASSUMPTION] tags are present in the spine at the delegated points: staleness budgets (5s run state / 60s health tiles), notification defaults (whole table column tagged), watchdog budget (10 min). All tags carry "operator reviews at UX gate." The memlog additionally logs the workspace-naming assumption, zone-layout assumption, and iconography assumption ("Operator may swap for pictographic icons at review"). Caution: the iconography assumption lives only in `.memlog.md` — the specialist-chip specs in EXPERIENCE.md/DESIGN.md are untagged, so an operator reviewing only the spines would not see that decision flagged as theirs to overturn (F-HUD-0005). The mock-vs-spine token conflicts were adjudicated with explicit "spine wins" rulings folded into DESIGN.md — good hygiene.

**6. Worktree/branch state:** on `dev/hud-revival-2026-07-11`, HEAD `59349de2` (brief finalize) in sync with origin (0 ahead); the entire UX output tree (`_bmad-output/planning-artifacts/ux-designs/`) plus this ledger are **untracked local files only** — nothing of the UX phase is committed or pushed; remaining dirty state is inherited ambient material (runs/*, _tmp-regression*, prior monitor ledger edit).

**Findings:**
**F-HUD-0001 [P1] UX-phase output is not durable.** The two spines, three mockups, and memlog exist only as untracked files; last push was 13:03, files written through 13:49. Per push-cadence policy (safety-checkpoint trigger), the UX gate crossing should include commit + push of the ux-designs tree before architecture opens.
**F-HUD-0002 [P2] Seven-status expansion — verified correct; propagate downstream.** The brief's criterion 1 lists six statuses; the envelope defines seven (`registered`) — monitor-verified at `production_trial_envelope.py:19-27`. EXPERIENCE.md's expansion under the brief's "every envelope status" clause is right; epics/stories and the L1 enum contract test must cite seven, and the brief's six-item enumeration should be treated as superseded by its own clause (or amended) so criterion 1 can't be "passed" at six.
**F-HUD-0003 [P2] Modality-chip file-source phrasing vs zero-lie rule 1.** Component specs read as direct run-dir reads (`run.json`, sidecar, `directive.yaml`); zero-lie rule 1 says the HUD renders only from the projection. Architecture must pin that modality readings arrive via the projection with provenance fields — the parentheticals are provenance, not a read path.
**F-HUD-0004 [P3] Health-tile drill/demand gap.** The IA promises tile expansion to "source-of-reading + confidence + history-this-run", but the Projection Demands health line does not demand per-tile reading history. Reconcile at architecture: add the demand or trim the drill.
**F-HUD-0005 [P3] Iconography assumption not surfaced in the spines.** Delegated iconography (monogram chips vs pictographic) is tagged only in `.memlog.md`; add it to the operator review packet or tag the specialist-chip spec so the delegated decision is visibly reviewable at the gate.
**F-HUD-0006 [P3] Party-plan §5.2 moment-based-IA divergence is a sanctioned supersession, not drift.** The brief's interview (addendum §A) demoted the returning-moment framing; zone IA + moment-based Key Flows is consistent with the superseding requirements source. Record it as such in any gate summary to pre-empt relitigating.

**Residual fencing at this gate:** operator review of the [ASSUMPTION] set (notification defaults, staleness budgets, watchdog budget, iconography, health thresholds) — delegated decisions are reviewable, not ratified; the projection contract, transport detail, phone-push mechanism, pre-flight item inventory, and prior-asset reuse tier all remain architecture-phase deliverables; no dev, tests, or lockstep-path edits should occur before that phase's own gates.

**Verdict: UX GATE CROSSING CLEAN-WITH-CAUTIONS.** Sequence, scope, claim hygiene, binding-principle carry-through, and assumption discipline all hold against the operator-amended brief. Cross after (1) committing + pushing the UX output tree (F-HUD-0001) and (2) putting the [ASSUMPTION] register — including memlog-only iconography — in front of the operator at review. F-HUD-0002/0003/0004 travel to the architecture phase as pinned reconciliation items.
