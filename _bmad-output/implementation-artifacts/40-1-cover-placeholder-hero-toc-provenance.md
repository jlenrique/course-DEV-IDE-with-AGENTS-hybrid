---
id: 40-1
epic: 40
status: ready-for-dev
depends_on: 39-2  # DAG: … 38.2 → 39.2 → 40.1 — 40-1 opens dev only after 39-2 lands; shared-file serialization is RUNNER-BAR-ONLY (see Provenance)
anchor_provenance: post-38-2-T4 tree 19c3e73e  # every line anchor below verified against this tree; re-verify against the post-39-2 landed tree at dev-open
baseline_commit: 19c3e73e
---

# Story 40.1: Cover producer — placeholder hero + art-brief, journey-TOC, provenance block

Status: ready-for-dev

## Story

As the learner,
I want an eye-grabbing, navigable, provenanced cover — an honest placeholder hero with a machine-readable art-brief, a journey table of contents, and a trust/registry provenance block,
so that the workbook has a front door I trust and can navigate, and a later Gamma step can drop in real art without a layout change.

## Provenance & Dependencies (BINDING)

- **Epic authority:** `_bmad-output/planning-artifacts/epics-presentation-support-workbook-2026-07-12.md` §Epic 40 / Story 40.1 — three Given/When/Then ACs (§12 Part 1 hero = named placeholder slot + deterministic art-brief, visibly a placeholder, no fabricated final-art claim, later Gamma swap with no layout change; §12 Part 2 creative journey-TOC generated deterministically from the §10 section model; §12 Part 3 provenance = unit/objective binding, SME, run id, date, deck reference, G2 citation/fidelity stamp, how-to-use dual-coding note, deterministic from run metadata). FR13 maps to this story alone.
- **Design authority:** `_bmad-output/planning-artifacts/workbook-presentation-support-redesign-2026-07-12.md` §12 (cover design, three parts) + §10 (consolidated section model; Cover = order 0) + §13.2 (Gamma illustration generation is an explicit NON-GOAL this pass).
- **Wave protocol authority:** `_bmad-output/planning-artifacts/wave-3940-kickoff-party-record-2026-07-15.md` §D3 — planks 1–6 + seven amendments. This story is **fully deterministic: the placeholder hero means NO Gamma call and NO new LLM surface ⇒ NO component probe owed.** Same-diff deliverable-bar extension (plank 5) with negative-witness pins (M-D3-2b) is AC 6.
- **DAG position:** `36 → {37.1, 37.3, 37.4, 37.2a} → 38.1 → 37.2b → 38.3 → 39.1 → 38.2 → 39.2 → 40.1` (green-light amendment A1). **40-1 lands after 39-2 lands.**
- **Serialize-on-runner-file (the ONLY shared file):** 39-2 touches `app/marcus/lesson_plan/trends_projection.py` + the runner bar; 40-1 touches `app/marcus/lesson_plan/workbook_producer.py` / `app/specialists/workbook_producer/_act.py` + the runner bar. The single shared file is **`scripts/utilities/marcus_spoc_live_test_runner.py`** (both stories append a clause to `_assert_completed_workbook_deliverable`). Discipline (green-light W-3 + J-3, merged): 40-1 does not edit the runner file until 39-2's clause is landed; the 40-1 cover clause appends AFTER 39-2's trends clause in the bar's call chain; every runner-file line anchor below is re-verified against the post-39-2 tree at dev-open; **the dev-open re-anchor duty additionally confirms 39-2's LANDED diff left `workbook_producer.py`/`_act.py` untouched** (not just the pre-authoring expectation that it would). `trends_projection.py` is NOT touched by 40-1 (it is read-only consumed via the existing `compose_workbook` import); the expectation that `workbook_producer.py`/`_act.py` are NOT touched by 39-2 is now a dev-open CHECK, not an assumption. The 39-2 spec EXISTS and carries the mirrored rider line in its Green-Light Round Record (W-3 side edit; the earlier "39-2 spec not yet authored" note is stale and struck).
- **Run-B boarding rider:** this story boards **governed run B** (after 38-2 + 39-2 + 40-1) on **suite-green alone** — no probe of its own (fully deterministic; no LLM surface; D3 boarding rule "only fixes independently greened offline may join a batch" is satisfied by deterministic suite + review green). Run B's evidence pack carries **this story's own per-story verdict line** keyed to the terminal `07W` render + the cover deliverable-bar clause: REACHED+PASS ⇒ flip to `done` citing the run id; NOT-REACHED ⇒ claim stays OPEN (no-evidence — never pass, never fail). This story never crosses two batch boundaries unwitnessed.
- **Status vocabulary (John F2):** on suite-green + review-green this story flips to `done-awaiting-live-witness  # deterministic+review green; no probe owed (fully deterministic); full-run witness owed by batch run B`.

## T1 Readiness (BINDING readings before any code)

1. **Epic AC text (verbatim)** — `epics-presentation-support-workbook-2026-07-12.md` §Story 40.1 (three Given/When/Then clauses) + the green-light amendments A6 (mode-parity copy: mechanism identical, only encounter-label copy differs — the TOC's [presentation] divider uses `encounter_mode`) and A9 (golden semantics: **byte-identical** on deterministic beats — this whole story is a deterministic beat).
2. **Design doc §12 + §10** — `workbook-presentation-support-redesign-2026-07-12.md`: the three cover parts, "spec-in-place, never an empty box," "visibly a placeholder in trial runs," §10 journey rhythm (Before you watch → [presentation] → After you watch), and the §13.2 non-goal fence (no Gamma generation).
3. **Wave party record §D3** — planks 4/5/6 + M-D3-2b (negative-witness pins; bar modules get Blind+Edge review on change) + M-D3-3/John F1 (batch attribution, run-B verdict line).
4. **Landed renderer substrate** — `app/marcus/lesson_plan/workbook_producer.py`: `_ComposedDoc` (L527–540, the (level, heading, body) block model), `compose_workbook` (L625–1047; pre-work prepend L679–696; S0 Overview L698–735), `render_markdown` (L1050–1063, appends the Human Review footer OUTSIDE `doc.blocks`), `render_docx_from_model` (L1066–1093), B8 determinism pins (L75–88 + `_pin_docx_zip_determinism` L1096–1117), `_render_docx_body` (L1120–1144 — **skips `![…](…)` image-syntax lines at L1126–1127**), `WorkbookSidecar` (L1183–1213), `produce()` (L1236–1475).
5. **Intake substrate** — `app/specialists/workbook_producer/_act.py`: `build_workbook_inputs` (L901–1338), `_derive_display_title` (L393–408), `_derive_plan_unit_fields` (L355–382), `WorkbookInputs` (L558–594), `_sidecar_refs` (L1577–1598, the structured channel into the run.json 07W contribution), `produce_workbook` output-root default `<run_dir>/exports/workbooks` (L1403–1419). Plus `app/marcus/lesson_plan/workbook_enrichment.py`: `load_run_envelope` (L675), `lesson_plan_from_run` (L718), `corpus_root_from_run` (L783 — the LAYERING precedent for a new trial-envelope reader, NOT the fail-shape precedent: it swallows corrupt→None and has no symlink guard; the new reader's fail-shape contract is stricter, see W-1 in Dev Notes).
6. **Runner bar idioms** — `scripts/utilities/marcus_spoc_live_test_runner.py`: `_assert_completed_workbook_deliverable` (L1882–1939, the clause call chain to append to), `_assert_exercise_composition_conformant` (L1600–1786 — **the structured-first pattern to mirror**: run.json 07W contribution receipt first, MD floor second, R3-style tolerance for receipt-absent shapes, callout-without-record = fabricated claim ⇒ REFUSE), `_assert_conformant_workbook_markdown` (L1203–1215 — the **H1-first MD floor**: first non-blank line must start `# `), `_md_section_body` (L1282–1291), the presentation-support sentinel (L1239). Re-anchor ALL of these against the post-39-2 tree.
7. **Lockstep check** — `docs/dev-guide/pipeline-manifest-regime.md` against `state/config/pipeline-manifest.yaml::block_mode_trigger_paths` (L60–110): NO scoped file is a trigger row (see Lockstep declaration). **`app/models/runtime/production_trial_envelope.py` IS a trigger row (manifest L105) — it is READ-ONLY substrate here and MUST NOT be edited.** Mid-dev drift into any trigger row is a STOP, party-gate.

## Acceptance Criteria

1. **Cover renders FIRST (Part 1 — identity + honest placeholder hero).** On the `presentation_support` render profile, `compose_workbook` prepends three cover blocks at the HEAD of `doc.blocks` (a deterministic post-composition insert, so the TOC in AC 3 derives from the FINAL composed section set): `## Cover` → `## Contents` → `## About This Workbook`, rendering before Pre-work/Overview in both MD and DOCX. The document H1 (`# Workbook: <display title>`) stays the first line (the runner's H1-first MD floor, L1213–1215, is untouched). The `## Cover` block carries: the display title + lesson/course identity (unit id, `fulfills` target, course/corpus identity), and the **hero placeholder** — an explicit, honest, TEXT placeholder block (exported module constant, e.g. `COVER_HERO_PLACEHOLDER_MARKER`, imported by the runner clause as the single source of the marker text — the label-drift idiom of `PRACTICE_GROUP_LABEL`): visibly a placeholder ("HERO ILLUSTRATION — PLACEHOLDER — Gamma generation deferred"), with deterministic alt-text derived from the lesson scenario/topics and a pointer to the art-brief sidecar (path + digest). **Never image syntax, never a fabricated final-art claim** — a later Gamma step swaps the image into this named slot with no layout change. New heading names collide with NO existing H2 (`Pre-work`, `Overview`, `Learning Objectives`, `Deep Dive`, `Depth-delta narrative` (emitted on BOTH profiles — W-2 corrected inventory), `Exercises`, `Answer Key`, `Research Glossary`, `References`, `Research Trends`, `Human Review Checkpoint` — verified distinct) so every `_md_section_body`-scoped bar check keeps its scope.
2. **Machine-readable art-brief sidecar (deterministic JSON).** `produce()` writes `<stem>.cover-art-brief.json` beside the MD/DOCX in the same output root (trial-scoped `<run_dir>/exports/workbooks/` in production): `schema_version: "workbook-cover-art-brief.v1"` (closed literal; bump tripwire per D3 drift-flags), workbook stem, display title, lesson identity (unit_id, event_type), **deterministically derived content hints** — themes (from section titles + LO statements + the pre-work Scene brief when present — run-artifact inputs only), palette hints (a small fixed deterministic mapping, dev latitude on the mapping, pinned once chosen), alt_text (same string the hero block renders), `hero_slot: "placeholder"` — and a **self-digest** (sha256 over the canonical serialization of every other field: `sort_keys=True, ensure_ascii=True, separators=(",", ":")` — the `cache_prefix` idiom). **Digest-stable:** double-produce on identical inputs yields byte-identical JSON and an identical digest (pinned). The art-brief is the machine contract for the FUTURE Gamma hero step; this story makes no generation claim of any kind.
3. **Journey-TOC deterministic from the composed `doc.blocks`.** The `## Contents` block is generated from the FINAL composed block list (never a hardcoded section list): every level-2 heading in `doc.blocks` (excluding the three cover headings themselves) gets **exactly one** TOC entry, plus one fixed entry for the renderer-appended `## Human Review Checkpoint` footer (which lives OUTSIDE `doc.blocks` — L1059/L1083, both renderers append it). Entries are grouped by the §10 journey rhythm via a fixed deterministic heading→phase map: **"Before you watch"** (Pre-work-derived headings — map keys enumerated as the pre-work sub-headings **Scene / Friction Scale / Promise** plus the `Pre-work` container heading itself (W-2); label copy keys on `encounter_mode` exactly as the existing pre-work label does — A6: mechanism identical, only encounter-label copy differs) → **"[The presentation]"** divider (recorded/live copy; **each encounter-mode copy variant is exactly ONE line, and the recorded/live variants have EQUAL line counts — preserves the parity test's `zip(strict=True)`, A-3**) → **"After you watch"** (review/backmatter headings; `Depth-delta narrative` — present on BOTH profiles, W-2 — is a DECLARED map entry falling to "After you watch", not an accidental unknown). An UNKNOWN heading deterministically falls into "After you watch" — **never dropped** (this feeds matrix row k / M-4). Friendly labels come from a fixed heading→label map ("Test yourself" for Exercises, etc.) with the verbatim heading as fallback; each entry carries the friendly label AND the exact rendered heading text — **anchor-consistency = the entry's target heading string matches a rendered `## ` heading verbatim** (exact-string anchor; MD hyperlink affordance is an explicit NON-goal this pass — `_render_docx_body` would render link syntax as raw text, verified L1120–1144). TOC entries render as list items only — **never** lines beginning `## ` (which would corrupt `_md_section_body` scoping). Byte-deterministic; **updates when the section set changes — pinned** (matrix rows c/d).
4. **Provenance block (Part 3) — from RUN ARTIFACTS, never wall-clock; rendered as a learner-plain colophon (J-1).** The `## About This Workbook` block renders as learner-facing colophon copy — plain language, no internal gate ids, no `date_source` keys, no raw pipeline internals in the rendered text; ALL machine/internal detail (gate ids, `date_source: "run.json:started_at"`, raw envelope fields) rides the receipt/sidecar channel ONLY. Every rendered line is deterministically derived from persisted run artifacts: (i) unit/objective binding (`plan_unit.unit_id`, `fulfills` = `unit_id@revision`, the distinct bound `learning_objective_id`s); (ii) **run id** (the `run_id`/`run_dir.name` the intake already threads — `_act.py` L1231/L1290-area — cross-checked against `run.json` `trial_id` by the bar, pin (c)); (iii) corpus/lesson identity (display title + the trial envelope's `corpus_path`); (iv) **generation date read from `run.json` `started_at`** (`ProductionTrialEnvelope.started_at`, tz-aware, L58 — NEVER `datetime.now()`; note the run is mid-flight at 07W so `completed_at` is None by construction); (v) SME — from the run's corpus bundle metadata (`sme_name`, `app/marcus/course_source/input_bundle.py:34`) when cheaply derivable READ-ONLY, else an explicit honest "SME not recorded in this run's artifacts" line — never fabricated; (vi) deck/source-bundle reference (the configured `segment_manifest_relpath` + bundle identity); (vii) **models used — STRUCK from the rendered block (J-2, ORCHESTRATOR RULING): receipt-only.** The sorted distinct (`specialist_id`/`node_id` → `model_used`) pairs off `run.json` `production_envelope.contributions` (`SpecialistContribution.model_used`, L55) are still collected and persisted in the cover receipt/sidecar, but do NOT render in `## About This Workbook` (conservative reading of the epic AC; reversible later by party); (viii) the **G2/G1 fidelity stamp rendered as an enforced-gate declaration in learner-plain copy (J-1)**, not a post-hoc result (audits run AFTER compose — `produce()` L1322–1381): the rendered copy says the workbook is covered by "numeric-fidelity and citation checks — a shipped workbook has passed by construction" (no gate ids like "G1"/"G2" in the rendered text; the structured sidecar/receipt carries the gate ids and the actual audit outcome); (ix) the "how to use this workbook with the deck" dual-coding note (§12 Part 3). A run whose envelope is absent/corrupt/symlink-suspect degrades every envelope-derived line to an explicit honest absent-REASON — the reason lines DISTINGUISH absent vs corrupt vs symlink per the W-1 reader contract (matrix row g asserts the reason lines, not just degradation) — never fabricated, never wall-clock backfilled. **Byte-determinism is with respect to the run artifacts:** double-render of the same run dir at different wall-clock times is byte-identical (matrix row f).
5. **DOCX parity from the same composed model.** Cover/TOC/provenance ride `doc.blocks`, so `render_markdown` and `render_docx_from_model` both emit them with zero parallel-render code. The hero placeholder is plain text (AC 1) — REQUIRED for parity, since `_render_docx_body` silently skips `![…](…)` lines (L1126–1127): an image-syntax "placeholder" would vanish from the DOCX. B8 byte-determinism (pinned core-properties + zip mtimes) holds unchanged; the DOCX double-render is byte-identical (matrix row f).
6. **Same-diff runner-bar cover clause + negative pins (D3 plank 5 + M-D3-2b).** Extend `_assert_completed_workbook_deliverable` with `_assert_cover_conformant(run_dir, md_paths)` in THIS story's diff, mirroring the exercise clause's structured-first idiom (L1600–1786): the persisted **cover receipt** on the 07W `workbook_producer` contribution in `run.json` (new `cover` key in `_sidecar_refs` — TOC entry list with target headings, `hero: "placeholder"`, art-brief path + digest, provenance `{run_id, date_source: "run.json:started_at", …}`) is the primary assertion surface; the MD is the conformance floor. **Anti-tautology fence (M-1, verbatim-in-substance):** the bar clause imports ONLY exported constants from the producer module (marker/heading strings, the schema-version literal) and takes ALL assertion data from persisted artifacts (`run.json` receipt, rendered MD, art-brief sidecar file) — it NEVER calls `compose_workbook` and NEVER reads `doc.blocks`; the bar must be able to fail the producer, so it may not re-derive expectations from the producer's own composition path. Asserts: (i) **TOC coverage, BIDIRECTIONAL (M-2)** — every rendered `## ` section heading (excluding the three cover headings; including the footer) has a matching TOC entry in receipt AND render, AND every TOC entry's target heading names a verbatim-matching rendered `## ` heading; a rendered section with no TOC entry = REJECT; a phantom TOC entry naming no rendered heading = REJECT; duplicate-H2 ambiguity is resolved FAIL-LOUD (or shown impossible with documented evidence in the dev diff); (ii) **hero honesty** — the cover block carries the exported placeholder marker and NO image reference (`![`) inside the `## Cover` section body while the receipt claims `placeholder`; an image reference where the placeholder is expected = REJECT (fabricated hero claim); (iii) **provenance run-id match** — the receipt/rendered run id equals `run.json` `trial_id`; mismatch = REJECT; (iv) the art-brief sidecar file exists at the receipt path, parses, carries the pinned `schema_version`, and its self-digest recomputes (a stale/corrupt brief = REJECT); (v) **date VALUE-match (M-3)** — the rendered and receipt generation dates both equal the `started_at` value of the envelope the clause itself loads from `run.json` — a source-of-truth VALUE comparison, not a format check. **Receipt-present branch only (A-2, BLOCKING):** the cover asserts (i)–(v) execute ONLY in the receipt-present branch; receipt-absent presentation-support MDs — `RENDERED_WORKBOOK_FIXTURE` and ALL existing bar-test fixtures — keep passing UNCHANGED (R3-style tolerance), and the existing 37_2b/39_1/39_1b bar tests are the regression bar proving it. **Negative-witness pins in the same diff** (mutated copies of frozen live shapes; each must REJECT): **(a)** a rendered section deleted from the TOC (missing TOC entry); **(b)** the hero placeholder replaced by an `![…](…)` image reference while the receipt still claims `placeholder`; **(c)** the provenance run id mutated to mismatch `run.json` `trial_id`; **(d)** a wall-clock-mutated date (rendered/receipt date differing from the envelope `started_at`) ⇒ REJECT (M-3); **(e)** a phantom TOC entry naming no verbatim-matching rendered heading ⇒ REJECT (M-2). Bar-module change ⇒ Blind+Edge review (M-D3-2b).
7. **Deterministic I/O matrix — every row a pinned deterministic test** (see matrix below; 11 rows a–k: profile/pre-work section-set variance flowing into the TOC, empty-honest sections, long titles, envelope-absent honesty with distinguished absent-reasons, byte-determinism double-render, phantom-TOC-entry rejection, unmapped-H2 fallback witness).
8. **Existing bars and floors unaffected.** The deep-dive (L1788), glossary (L1312), LO-overlay (L1505), and exercise (L1600) clauses re-run green on covered renders: the cover introduces NO `ask-a-cite-` tokens, NO `Exercise overlay loss:`/`Enrichment overlay loss:` callout text, NO `### ` headings inside any existing `_md_section_body` scope (the glossary entry-heading regex is section-scoped — cover sub-content uses list items/paragraphs, not `### `), and does not move the presentation-support sentinel (it stays in the S0 Overview body — the deep-dive clause only asserts `in text`). The H1-first MD floor holds (AC 1). G1 is verified quiet by construction: the `_FIGURE_RE` tokenizer is `%`/`$`/`Nx`-only (`app/specialists/_shared/figure_tokens.py:12–22`), so the provenance date/run-id/count numerals are invisible to the symbol-only gate — any theme/hint string that DOES carry a symbol figure must ride the `_authored_prose_figure_supplements` declaration idiom (B5), pinned. **CLOSED flip inventory (A-1, BLOCKING — enumerated PRE-dev, replacing the earlier "enumerated in the dev diff" deferral):** exactly two intentional pin flips are authorized:
   - **FLIP-1:** `tests/marcus/lesson_plan/test_workbook_s0_s7.py::test_presentation_support_frontmatter_and_fr17_cut` L178 — `headings[:4]` expectation flips to `["Cover", "Contents", "About This Workbook", "Pre-work"]`.
   - **FLIP-2:** same file, `::test_recorded_live_parity_is_exactly_one_label_in_md_and_docx` L201–234 — consciously EXTENDED for the TOC's [presentation] divider + group-label lines (the A-3 one-line/equal-line-count constraint keeps `zip(strict=True)` valid).

   **Verified NON-flip list (A-1, seat-verified — these pins must survive the diff untouched):** the H1 pins; section-index scoping; the pre-work order pin; the AC-8 dual-coding double-fence; the G1 regex-quiet pins; the glossary-render witness family (digest surface = glossary section body) — verified under `WITNESS_REPLAY_STRICT=1`. Any flip outside FLIP-1/FLIP-2 is drive-by and BLOCKS. **Named battery test (M-5):** the AC 8 "existing bars re-run green" claim is pinned as a NAMED test executing the FULL `_assert_completed_workbook_deliverable` clause chain — every clause, INCLUDING 39-2's trends clause — against a cover-bearing derived fixture built from the frozen run `runs/8b275e5b-ed8a-4720-8217-8ddaca4c6627` (skip-if-absent on the live run dir + a committed derived fixture, per the 39-1b precedent).

## Deterministic I/O matrix (every row a pinned test; byte-identical golden semantics per A9)

| # | Input condition | Verdict / outcome |
|---|---|---|
| a | presentation_support + pre-work brief present (baseline) | H1 first line; then `## Cover` → `## Contents` → `## About This Workbook` → `## Pre-work` …; hero = exported placeholder marker + alt-text + art-brief pointer; no `![` in the Cover section |
| b | TOC coverage on baseline (BIDIRECTIONAL, M-2) | every rendered H2 (+ the renderer-appended Human Review footer) has exactly one TOC entry with a verbatim-matching target heading, AND every TOC entry's target heading matches a rendered H2 verbatim (no phantom entries — the reverse direction is row j's REJECT surface); journey grouping Before → [presentation] → After; Before-you-watch map keys = Scene / Friction Scale / Promise (+ `Pre-work`, W-2); friendly-label map applied, verbatim fallback pinned |
| c | section-set variance: `legacy` vs `presentation_support` profile ± pre-work | the TOC entry set follows the composed blocks exactly (legacy adds Transcript-narrative / Transcript of Record / Figures; presentation_support adds Deep Dive; `Depth-delta narrative` present on BOTH profiles — W-2 corrected inventory; pre-work headings only when the brief is present) — TOC "updates when sections change" pinned as a set-difference assertion. NOTE: cover blocks + receipt emit on `presentation_support` only; a legacy render emits NO cover and NO receipt (R3 tolerance branch) |
| d | empty-honest sections (glossary explicitly-empty, trends explicitly-empty) | `Research Glossary` / `Research Trends` headings render UNCONDITIONALLY with empty-honest bodies (substrate fact — compose L906–917/L1034–1045), so their TOC entries persist; body-level emptiness never drops a TOC entry |
| e | long display title (>120-char lesson_summary, `_derive_display_title` truncation L393–408) + unicode | cover identity + art-brief title carry the truncated display title; H1/cover/art-brief agree; byte-deterministic; no layout break |
| f | byte-determinism double-render (same run dir, different wall-clock) | MD bytes, DOCX bytes (B8), art-brief JSON bytes + self-digest all identical across two `produce()` calls |
| g | run.json absent vs corrupt vs symlink (fixtures = MUTATED COPIES of the frozen live run dir `runs/8b275e5b…`, digest-bound with bump tripwire — NEVER synthetic, M-6) | provenance renders explicit honest absent-REASON lines that DISTINGUISH absent vs corrupt vs symlink (W-1 reader fail-shape contract; the test asserts the reason lines, not mere degradation) for run-id/date/corpus fields (models are receipt-only per J-2) — never wall-clock, never fabricated; art-brief still emits (its inputs are compose-side); receipt records the degrade |
| h | encounter_mode `recorded` vs `live` | ONLY the [presentation] divider + Before-you-watch label copy differ (A6 parity: mechanism identical); each copy variant is exactly ONE line with EQUAL line counts across modes (A-3, preserves `zip(strict=True)`); pinned as a two-render diff limited to the encounter-copy strings |
| i | art-brief schema/digest tripwires | mutated `schema_version` or content-vs-digest mismatch fails the digest recompute (consumed by bar clause AC 6(iv)); pinned negative |
| j | phantom TOC entry (M-2): a TOC entry mutated to name a heading no rendered `## ` line matches verbatim | REJECT (bar clause AC 6(i) reverse direction / negative pin (e)); duplicate-H2 ambiguity fail-loud or documented-impossible with evidence |
| k | unmapped H2 injected at compose level (M-4) | the unknown heading lands under "After you watch" with a verbatim-fallback label — the never-dropped witness; exactly one TOC entry, no drop, no crash |

## Batch-run AC — governed run B

- **No probe.** Fully deterministic story (placeholder hero = NO Gamma call; no LLM surface). Boarding rule satisfied by suite-green + `bmad-code-review` green alone.
- On suite-green + review-green: flip to `done-awaiting-live-witness  # deterministic+review green; no probe owed (fully deterministic); full-run witness owed by batch run B (after 38-2 + 39-2 + 40-1)`.
- Run B's evidence pack carries **this story's own per-story verdict line** keyed to the terminal `07W` render + the cover bar clause: REACHED+PASS ⇒ `done` citing the run id; NOT-REACHED ⇒ OPEN (never pass, never fail). "Aboard" ≠ tested.
- `WITNESS_REPLAY_STRICT=1` pre-flight green (0 skipped) before run B per D3 plank 1 / Amelia F1.

## Scope Fences (hard NO)

- **NO Gamma calls, NO Gamma wiring, NO image generation of any kind** (§13.2 non-goal; the art-brief is a sidecar contract for a FUTURE story).
- **NO new LLM surface** (no probe exists because none is owed — adding one voids this story's boarding rider).
- **NO section content changes beyond the three new cover blocks.** S0 Overview and the S7 Human Review footer stay in place UNCHANGED this pass — §12's "absorbs old S0 + S7" is satisfied additively (the dual-coding note also appears in the provenance block); retiring/deduplicating S0/S7 is a named follow-on — **FILED (J-4) 2026-07-16 in `_bmad-output/planning-artifacts/deferred-inventory.md` §Named-But-Not-Filed Follow-Ons as `workbook-cover-s0-s7-dual-coding-dedupe`**, NOT this diff.
- **NO manifest/wiring/runner-walk changes:** no edits to `workbook_wiring.py`, `research_packet.py`, `production_runner.py`, `workflow_runner.py`, `state/config/pipeline-manifest.yaml`, **or `app/models/runtime/production_trial_envelope.py` (trigger row — read-only substrate)**. Any drift = undeclared lockstep expansion → STOP, party-gate.
- **NO trends work** (`trends_projection.py` is 39-2's file — read-only here); NO glossary/exercise/deep-dive render changes (39-1/39-1b/37-2b surfaces — their bars must re-run green untouched); NO References redesign (37.5); NO mode-parity story work beyond the encounter-copy TOC divider (37.6).
- **NO weakening of:** the terminal `07W` model-free pin, the H1-first MD floor, B8 DOCX byte-determinism, G1/G2 FAIL-mode gates, the existing four bar clauses, VO↔on-screen protected invariants.
- **NO MD hyperlink TOC affordance this pass** (declared non-goal — the DOCX body projector renders link syntax raw; anchor-consistency is exact-heading-string, AC 3).
- **NO pin flips outside the CLOSED A-1 inventory** (FLIP-1 + FLIP-2 in AC 8; enumerated pre-dev, not in the dev diff).

## Lockstep declaration

Checked every scoped file against `state/config/pipeline-manifest.yaml::block_mode_trigger_paths` (L60–110):

| Scoped file | Trigger-path entry? | Touched? |
|---|---|---|
| `app/marcus/lesson_plan/workbook_producer.py` | not listed | yes (cover blocks, hero marker constants, art-brief writer, TOC composer, sidecar `cover` field) |
| `app/specialists/workbook_producer/_act.py` | not listed | yes (cover inputs: run-identity read, receipt into `_sidecar_refs`) |
| `app/marcus/lesson_plan/workbook_enrichment.py` | not listed | yes (new READ-ONLY trial-envelope identity reader with the W-1 precise fail-shape contract — reasoned non-raising degrades distinguishing absent/corrupt/symlink) |
| `scripts/utilities/marcus_spoc_live_test_runner.py` | not listed | yes (cover bar clause + 3 negative pins, same diff; AFTER 39-2's clause lands) |
| `app/models/runtime/production_trial_envelope.py` | **trigger (manifest L105)** | **NO — read-only** |
| `app/marcus/orchestrator/workbook_wiring.py` | **trigger** | **NO** |
| `app/marcus/orchestrator/production_runner.py` | **trigger** | **NO** |
| `app/marcus/lesson_plan/research_packet.py` | **trigger** | **NO** |
| `state/config/pipeline-manifest.yaml` | **trigger** | **NO** |

**Verdict: zero trigger paths touched — lockstep regime not triggered.** Any drift into a trigger row is a STOP.

## Dev Notes

### Code map (anchors verified against the post-38-2-T4 tree `19c3e73e`; re-verify against the post-39-2 tree at dev-open — the re-anchor duty ALSO confirms 39-2's LANDED diff left `workbook_producer.py`/`_act.py` untouched, W-3)

- **UPDATE `app/marcus/lesson_plan/workbook_producer.py`:** new exported constants (`COVER_HEADING`/`CONTENTS_HEADING`/`ABOUT_HEADING`, `COVER_HERO_PLACEHOLDER_MARKER`, `COVER_ART_BRIEF_SCHEMA_VERSION` — runner clause imports these, the L551–555 label idiom); a `_compose_cover_blocks(doc, cover_inputs)` post-pass invoked at the END of `compose_workbook` (after L1045) that inserts the three blocks at `doc.blocks[0:0]` so the TOC walks the FINAL section set; the art-brief builder + canonical-JSON writer (write in `produce()` beside md/docx, L1421–1427 region); `WorkbookSidecar` gains `cover: dict | None` + `art_brief_path: str | None` (L1183–1213); `produce()` threads the new inputs and returns the receipt (mirror `exercise_composition`, L1471–1474 — derive the receipt FROM the same composed blocks the render walks, so structure cannot diverge from the deliverable).
- **UPDATE `app/specialists/workbook_producer/_act.py`:** `WorkbookInputs` gains the cover input struct (run identity, SME, deck reference, models-used pairs — all read-only derivations inside `build_workbook_inputs`, L901–1338, presentation_support branch only); `_sidecar_refs` (L1577–1598) gains `"cover"`.
- **UPDATE `app/marcus/lesson_plan/workbook_enrichment.py`:** new `run_identity_from_run(run_dir)` READ-ONLY reader returning `{trial_id, started_at, corpus_path, models_used}` off `run.json` (models_used feeds the RECEIPT ONLY per J-2 — never the rendered block) — **note the layering surprise:** `load_run_envelope` (L675) returns the inner `ProductionEnvelope` (contributions — source of `model_used` pairs, `production_envelope.py:55`), while `started_at`/`trial_id`/`corpus_path` live on the OUTER `ProductionTrialEnvelope` (L54–58), which only `corpus_root_from_run` (L783) currently deserializes. **Precise fail-shape contract (W-1, replacing the earlier inaccurate "mirror `corpus_root_from_run`" instruction — that helper swallows corrupt→None and has NO symlink guard):** the new reader is REASONED NON-RAISING — it never raises on bad input and returns a degrade result that DISTINGUISHES (1) `run.json` absent, (2) `run.json` corrupt/unparseable, (3) symlink-suspect path — so the provenance renderer can emit the distinct honest absent-REASON lines matrix row g asserts. Do NOT widen `load_run_envelope`'s contract.
- **UPDATE `scripts/utilities/marcus_spoc_live_test_runner.py`:** `_assert_cover_conformant` appended to the L1915–1939 clause chain (after 39-2's trends clause); structured-first per the exercise-clause idiom (L1600–1786); 3 negative pins per AC 6.
- **Tests:** `tests/unit/marcus/lesson_plan/` (cover compose/TOC/art-brief/matrix rows) + `tests/specialists/workbook_producer/` (intake + receipt) + the bar-clause module with its negative pins; fixtures = mutated copies of frozen live shapes (live-shape rule; digest-bound with bump tripwire) — this INCLUDES matrix row g's absent/corrupt/symlink run.json fixtures, which are MUTATED COPIES of the frozen live run dir, never synthetic (M-6). Frozen substrate: `runs/8b275e5b-ed8a-4720-8217-8ddaca4c6627` (the 39-1b replay substrate) for a real-shape cover render pin AND the M-5 named full-battery test (the complete `_assert_completed_workbook_deliverable` clause chain, incl. 39-2's trends clause, against a cover-bearing derived fixture; skip-if-absent + committed derived fixture, per the 39-1b precedent). Pin flips limited to the A-1 CLOSED inventory (FLIP-1/FLIP-2, AC 8).

### Decisions + named risks (pre-resolved for dev)

- **Cover is presentation_support-profile-only** (the §10 model IS the presentation-support workbook; legacy renders are the frozen pre-redesign artifact). Receipt-absent legacy shapes take the bar's R3 tolerance branch. This minimizes pin flips.
- **Hero must be text, not image syntax** — `_render_docx_body` L1126–1127 silently drops `![` lines (DOCX parity would break), and negative pin (b) rejects image syntax as a fabricated hero claim.
- **G2 stamp chicken-and-egg resolved as gate-declaration, rendered learner-plain (J-1):** audits run after compose (`produce()` L1322–1381); the cover renders the FAIL-mode contract statement in colophon copy — "numeric-fidelity and citation checks — a shipped workbook has passed by construction" — with NO gate ids/internals in the rendered text; the actual audit outcome + gate ids ride the sidecar/receipt (AC 4(viii)). Never render a claimed-pass the compose step cannot know.
- **Models-used is receipt-only (J-2, orchestrator ruling):** collected from contributions, persisted in the cover receipt/sidecar, NEVER rendered in `## About This Workbook`. Conservative epic-AC reading; reversible later by party.
- **Date source:** `started_at` (the run is mid-flight at 07W; `completed_at` is None by construction — `production_trial_envelope.py:59/115`). Never `datetime.now()`; B8 discipline extends to the cover.
- **G1 verified quiet:** `_FIGURE_RE` is `%`/`$`/`Nx`-only (`figure_tokens.py:12–22`) — dates/run-ids/counts don't tokenize. Only symbol-bearing theme strings would need the B5 supplement declaration.
- **Guardrails:** product work only (SPOC deliverable); terminal `07W` stays deterministic-consume + model-free; carrier robustness (schema-versioned, additive-safe, fail-loud art-brief; honest degrades everywhere); enumerated pin flips only.

### References

- `_bmad-output/planning-artifacts/epics-presentation-support-workbook-2026-07-12.md` — §Epic 40 / Story 40.1 ACs (binding), FR13, green-light amendments A6/A9
- `_bmad-output/planning-artifacts/workbook-presentation-support-redesign-2026-07-12.md` — §12 (cover, three parts), §10 (section model / journey rhythm), §13.2 (non-goals)
- `_bmad-output/planning-artifacts/wave-3940-kickoff-party-record-2026-07-15.md` — §D3 planks + amendments (binding wave protocol), wave run plan (run B)
- `_bmad-output/implementation-artifacts/39-1b-exercise-merge-composition.md` — the receipt + bar-clause + negative-pin precedent this story mirrors
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — `40-1-cover-placeholder-hero-toc-provenance: backlog  # Gamma hero = placeholder + art-brief; TOC + provenance deterministic`
- `docs/dev-guide/pipeline-manifest-regime.md` · `state/config/pipeline-manifest.yaml::block_mode_trigger_paths`

## Green-Light Round Record (2026-07-16)

**Verdict: 4/4 GREEN-WITH-AMENDMENTS** — Winston (Architect) / John (PM) / Amelia (Dev) / Murat (TEA), heard via two combined seats (Winston+John architecture/product seat; Amelia+Murat dev/test seat); orchestrator concurred. Per the ratified party-consensus-=-approval policy (CLAUDE.md §Solo operator), consensus + orchestrator concurrence = approval; no redundant human hold. **Orchestrator ruling on J-2** (models-used struck from the rendered provenance block — receipt-only; conservative reading of the epic AC, reversible later by party) recorded below and folded at AC 4(vii). All amendments folded 2026-07-16; status flipped to `ready-for-dev`.

Per-id fold map:

| Id | Amendment (substance) | Folded at |
|---|---|---|
| W-1 | `run_identity_from_run` precise fail-shape contract: reasoned non-raising degrades distinguishing absent vs corrupt vs symlink (NOT "mirror `corpus_root_from_run`" — that helper swallows corrupt→None, no symlink guard); row g asserts the absent-REASON lines | T1 item 5; AC 4 (degrade sentence); matrix row g; Lockstep table (workbook_enrichment row); Dev Notes code map (workbook_enrichment bullet) |
| W-2 | Heading inventory corrected: `## Depth-delta narrative` (both profiles) added to collision list, row c, and the phase map (declared fallback to "After you watch"); Before-you-watch map keys enumerated = Scene / Friction Scale / Promise | AC 1 (collision list); AC 3 (phase map); matrix rows b + c |
| W-3 | Rider refresh: stale "39-2 spec does not exist yet" note struck; mirror line added to 39-2's Green-Light Round Record (side edit); dev-open re-anchor duty extended to CONFIRM 39-2's landed diff left `workbook_producer.py`/`_act.py` untouched | Provenance serialize bullet; Dev Notes code-map header; this record (replacing the stale PENDING item iv) |
| A-1 (BLOCKING) | CLOSED flip inventory pre-dev: FLIP-1 `test_workbook_s0_s7.py::test_presentation_support_frontmatter_and_fr17_cut` L178 headings[:4] → ["Cover", "Contents", "About This Workbook", "Pre-work"]; FLIP-2 `::test_recorded_live_parity_is_exactly_one_label_in_md_and_docx` L201–234 (consciously extended per A-3); + verified NON-flip list (H1 pins, section-index scoping, prework order pin, AC-8 dual-coding double-fence, G1 regex-quiet, glossary witness digest surface) | AC 8 (replaces the "enumerated in the dev diff" deferral); Scope Fences (pin-flip fence); Dev Notes tests bullet |
| A-2 (BLOCKING) | Cover asserts execute ONLY in the receipt-present branch; receipt-absent presentation-support MDs (RENDERED_WORKBOOK_FIXTURE + all existing bar-test fixtures) keep passing unchanged (R3-style tolerance); 37_2b/39_1/39_1b bar tests = the regression bar | AC 6 (receipt-present-branch clause, superseding the unconditional REFUSE-on-unreceipted-cover phrasing) |
| A-3 | Encounter-mode copy variants = one line each, equal line counts across recorded/live (preserves `zip(strict=True)`) | AC 3 (divider); matrix row h; cited in A-1 FLIP-2 |
| J-1 | Provenance rendered as learner-plain colophon; gate ids/date_source/raw internals ride receipt/sidecar only; rendered copy: "numeric-fidelity and citation checks — a shipped workbook has passed by construction" | AC 4 (intro framing + clause (viii)); Dev Notes (G2-stamp decision) |
| J-2 | **ORCHESTRATOR RULING:** models-used STRUCK from the rendered block — receipt-only; conservative epic-AC reading, reversible later by party | AC 4(vii); matrix row g; Dev Notes (new decision bullet + reader bullet) |
| J-3 | = the serialize-rider items — merged with W-3, both cited | Provenance serialize bullet (cites W-3 + J-3) |
| J-4 | S0/S7 dual-coding-note dedupe follow-on FILED in deferred-inventory §Named-But-Not-Filed, sourced to 40-1 | Scope Fences (S0/S7 fence, marked FILED); `deferred-inventory.md` side edit |
| M-1 | Anti-tautology fence verbatim-in-substance: bar clause imports ONLY exported constants (marker/heading strings, schema-version literal); ALL assertion data from persisted artifacts — never `compose_workbook`, never `doc.blocks` | AC 6 (fence, immediately after the assertion-surface sentence) |
| M-2 | Bidirectional TOC: row b tightened + new row j (phantom TOC entry ⇒ REJECT) + negative pin (e); duplicate-H2 ambiguity fail-loud or documented impossibility with evidence | AC 6 clause (i) + pin (e); matrix rows b + j |
| M-3 | Date VALUE-match: rendered/receipt date equals the envelope `started_at` the clause loads; negative pin (d) wall-clock-mutated date ⇒ REJECT; AC 6 gains clause (v) | AC 6 clause (v) + pin (d) |
| M-4 | New matrix row: unmapped H2 injected at compose level lands under "After you watch" with verbatim-fallback label — the never-dropped witness | Matrix row k; AC 3 (unknown-heading sentence now cites row k) |
| M-5 | AC 8 battery pinned as a NAMED test: FULL `_assert_completed_workbook_deliverable` clause chain (incl. 39-2's trends) against a cover-bearing derived fixture from frozen run `runs/8b275e5b` (skip-if-absent + committed derived fixture, 39-1b precedent) | AC 8 (named battery test); Dev Notes tests bullet |
| M-6 | Row g fixtures = MUTATED COPIES of the frozen live run dir, digest-bound with bump tripwire — never synthetic | Matrix row g; Dev Notes tests bullet |

Items from the draft's flagged list, disposed: (i) presentation_support-only cover — RATIFIED as drafted; (ii) exact-heading-string anchor semantics, MD hyperlinks deferred — RATIFIED as drafted; (iii) S0/S7 additive-not-absorbed fence — RATIFIED, follow-on now FILED (J-4); (iv) serialize rider — refreshed and mirrored (W-3/J-3).

## Dev Agent Record

*(empty — story not yet dispatched)*
