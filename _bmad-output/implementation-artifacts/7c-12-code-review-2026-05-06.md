# Story 7c.12 — T11 Lite Cross-Agent Code Review

**Reviewer:** Claude (T11 lite cross-agent)
**Date:** 2026-05-06
**Story:** `migration-7c-12-section-07f-g2f-motion-gate`
**Implementation author:** Codex GPT-5
**Review tier:** T11 lite (~10-15 min single-pass; aggressive DISMISS on cosmetic NITs per `docs/dev-guide/story-cycle-efficiency.md`)
**Closest sibling:** 7c.13 (identical CLI+HTTP-mandatory transport-set + identical approve/edit/reject verb-pattern)

---

## Verdict: **PASS-zero-patches**

Codex implemented Story 7c.12 cleanly within the prompt's file scope. All 5 ACs (A-E) are satisfied. All focused / non-regression / validator commands PASS exactly as Codex reported. The flagged inherited broad-regression failures (`test_no_unauthorized_callers` family + Slab-6 baseline) are confirmed PRE-EXISTING — not introduced by 7c.12. No MUST-FIX or SHOULD-FIX findings. The §07F G2F motion-gate surface is structurally indistinguishable from 7c.13's §08B G3B sibling pattern (transport-set + verb-pattern + alias_of mechanic), with the appropriate G2C-aliased-fanout re-targeting (alias_of="G2C", surface_id="section_07f_g2f_motion_gate", motion-clip nomenclature throughout).

---

## Findings

| Severity | Finding | Rationale |
|---|---|---|
| **MUST-FIX** | _none_ | — |
| **SHOULD-FIX** | _none_ | — |
| NIT-DISMISSED | `test_no_unauthorized_callers` family (broad-regression noise) | Pre-existing structural flaw in the scanner (substring matches CLASS DEFINITIONS like `class Section07FOperatorVerdict(BaseModel):`, not direct constructor calls). Already failing pre-7c.12 with multiple offenders across §02A + Wave-3 trio + 7c.13/7c.14. 7c.12 inherits the breakage; does not introduce a new failure category. **DISMISS as pre-existing per 7c.13 + 7c.14 review precedent.** |
| NIT-DISMISSED | §02A DSL-registration tests may flake under broad-run xdist but PASS in isolation (15 passed isolated) | Shared-registry xdist ordering flake; §02A test files unchanged since 7c.3b. The §07F registration test is correctly reload-isolated via `_clear_registered_surfaces_for_tests()` + `importlib.reload`. NOT a 7c.12 regression. |

---

## Verb-pattern verification (7c.12 = approve/edit/reject + 2-way model_validator)

**Verified at `app/models/operator_verdict_section_07f.py:21,141-157`:**

- `Section07FVerdictVerb = Literal["approve", "edit", "reject"]` — closed 3-verb set ✓
- `_enforce_verb_payload_consistency` `model_validator(mode="after")` enforces:
  - `verb=="edit"` → requires `edit_payload`, forbids `reject_reason`
  - `verb=="reject"` → requires `reject_reason`, forbids `edit_payload`
  - `verb=="approve"` → forbids BOTH `edit_payload` AND `reject_reason`
- Pattern is byte-equivalent to §02A + 7c.13 §08B `_enforce_verb_payload_consistency` precedent. **2-way verb-payload consistency MATCHED.**
- `MotionClipEditPayload` mirrors `DirectiveEditPayload` shape (mandatory `edits: dict` with `min_length=1`, blank-key rejection, `extra="forbid"`, `frozen=True`). ✓

---

## Transport-set verification (7c.12-specific: CLI+HTTP mandatory; MCP-stdio optional)

**Verified at `app/gates/section_07f/poll_surface.py:27-32`:**

```python
@parity_contract(
    surface_id="section_07f_g2f_motion_gate",
    mandatory_transports=["cli", "http"],
    optional_transports=["mcp-stdio"],
    alias_of="G2C",
)
```

| Aspect | 7c.12 (this story) | 7c.13 (closest sibling) | Match |
|---|---|---|---|
| `mandatory_transports` | `["cli", "http"]` | `["cli", "http"]` | ✓ IDENTICAL |
| `optional_transports` | `["mcp-stdio"]` | `["mcp-stdio"]` | ✓ IDENTICAL |
| `surface_id` | `"section_07f_g2f_motion_gate"` | `"section_08b_g3b_poll"` | ✓ section-distinct as required |
| `alias_of` | `"G2C"` | `"G3"` | ✓ section-distinct (G2F aliases G2C per ADR 0002 §2; 7c.13 G3B aliases G3) |
| Verb set | `["approve", "edit", "reject"]` | `["approve", "edit", "reject"]` | ✓ IDENTICAL |
| 2-way model_validator | edit/reject/approve consistency enforced | edit/reject/approve consistency enforced | ✓ IDENTICAL pattern |

**Critically NOT CLI-only** (the 7c.9/7c.10/7c.11 pattern); 7c.12 follows the **dual-mandatory-transport** pattern of 7c.13. Confirmed at the decorator-level + at the DSL-registration test asserts (`test_g2f_surface_registers_declared_transports_and_alias`).

DSL-registration test asserts:
- `declaration.mandatory_transports == ["cli", "http"]` ✓
- `declaration.optional_transports == ["mcp-stdio"]` ✓
- `declaration.alias_of == "G2C"` ✓

---

## C6 import-linter modules list verification (10-entry post-4-way-union)

**Verified at `pyproject.toml::tool.importlinter::contracts::C6::modules` (lines 256-267):**

```toml
modules = [
    "app.gates.section_02a",        # 7c.3b
    "app.gates.section_04a",        # Wave-3 trio (7c.6)
    "app.gates.section_04_5",       # Wave-3 trio (7c.7)
    "app.gates.section_04_55",      # Wave-3 trio (7c.8)
    "app.gates.section_08b",        # 7c.13
    "app.gates.section_11",         # 7c.14
    "app.gates.section_05_5",       # 7c.9 (4-way union)
    "app.gates.section_07b",        # 7c.10 (4-way union)
    "app.gates.section_07d",        # 7c.11 (4-way union)
    "app.gates.section_07f",        # 7c.12 (4-way union — THIS STORY)
]
```

**10 entries confirmed.** PARALLEL-DISPATCH GUARDRAIL #3 (4-way union with 7c.9/7c.10/7c.11) honored. Lint-imports `12 KEPT, 0 broken` UNCHANGED.

---

## Spot-check evidence

| Command | Expected | Actual | Match |
|---|---|---|---|
| `pytest tests/gates/section_07f/ tests/schemas/operator_verdict/test_section_07f_shape.py` | 13 passed | **13 passed in 26.74s** | ✓ |
| `pytest tests/gates/section_02a/ tests/schemas/operator_verdict/test_section_02a_shape.py` | 15 passed | **15 passed in 51.39s** | ✓ |
| `pytest tests/gates/section_04a/ tests/gates/section_04_5/ tests/gates/section_04_55/ tests/gates/section_08b/ tests/gates/section_11/` | 43 passed | **43 passed in 20.77s** | ✓ |
| `lint-imports.exe` | 12 KEPT | **Contracts: 12 kept, 0 broken** | ✓ |
| `validate_parity_test_class_conformance.py tests/parity/` | 19 PASS | **PASS: 19 parity contract file(s) conform** | ✓ |
| `validate_migration_story_sandbox_acs.py <story>` | PASS | **PASS — no sandbox-AC violations** | ✓ |
| `ruff check app/gates/section_07f/ app/models/operator_verdict_section_07f.py tests/gates/section_07f/ tests/schemas/operator_verdict/test_section_07f_shape.py` | clean | **All checks passed!** | ✓ |
| `operator_verdict_section_07f.v1.schema.json` LF-only / no BOM | LF-only / no BOM | **BOM=False; CRLF count=0; LF count=106** | ✓ |
| `pyproject.toml::tool.importlinter::C6::modules` includes `app.gates.section_07f` (10-entry post-state) | 10 entries | **VERIFIED** (lines 256-267) | ✓ |

**Pattern-parity vs 7c.13 (`Section07FOperatorVerdict` shape audit):**
- UUID4 `run_id` validator via `enforce_uuid4_version` ✓
- Tz-aware `submitted_at` validator via `enforce_tz_aware` ✓
- sha256-hex `decision_card_digest` validator (`re.fullmatch(r"[0-9a-f]{64}", ...)`) ✓
- Strip-then-non-empty validators on `motion_clip_id` + `operator_id` (mode="before") ✓
- Verb-payload consistency `model_validator(mode="after")` enforces edit↔edit_payload + reject↔reject_reason + approve forbids both ✓
- `model_config = ConfigDict(extra="forbid", validate_assignment=True, frozen=True)` ✓
- Closed `Section07FVerdictVerb = Literal["approve", "edit", "reject"]` ✓
- Closed `Section07FSurfaceId = Literal["section_07f_g2f_motion_gate"]` ✓
- `parity_contract` decorator: `surface_id="section_07f_g2f_motion_gate"` + `mandatory_transports=["cli", "http"]` + `optional_transports=["mcp-stdio"]` + `alias_of="G2C"` ✓ (per FR-7c-16 + ADR 0002 §2)
- Re-emitted `canonical_model_bytes` + `compute_model_digest` helpers locally (C6 cross-§section independence) ✓
- DSL-registration test reload-isolated via `_clear_registered_surfaces_for_tests()` + `importlib.reload` ✓
- 3-transport-parity test uses canonical `assert_operator_verdict_schema_stable_across_transports` harness + adds tampered-digest rejection + motion_clip_id-mismatch rejection + YAML loader UTF-8 path ✓

---

## Broad-regression attribution

Codex reports `4352 passed, 43 failed, 27 skipped, 2 xfailed` with inherited failure count UNCHANGED.

5 spot-checked failures with git-log attribution:

| # | Failing test | Most-recent touching commit | Attribution |
|---|---|---|---|
| 1 | `tests/test_run_hud.py::TestScanBundleArtifacts::test_lists_files_with_sizes` | `162d129` (Slab 6 trial experience bundle) | Pre-Slab-7c. NOT 7c.12's diff. |
| 2 | `tests/contracts/test_30_1_zero_test_edits.py::test_no_preexisting_test_files_modified_in_30_1` | `2ba1e32` (Epic-33 Pipeline Lockstep close) | Pre-Slab-7c structural ledger. NOT 7c.12's diff. |
| 3 | `tests/migration/test_m5_verdict_consequence_path.py::test_m5_verdict_consequence_path` | `728f739` (working-tree spillover Slab 6.0+6.1) | Pre-Slab-7c. NOT 7c.12's diff. |
| 4 | `tests/end_to_end/test_full_pipeline_smoke.py::test_migrated_manifest_has_33_nodes` | `eb2adb0` (Slab 1 Story 1.6) | Pre-Slab-7c manifest migration test. NOT 7c.12's diff. |
| 5 | `tests/test_check_pipeline_manifest_lockstep.py::test_red_path_fixtures_fail_correctly_schema_only` | `d4defdc` (4.1 graph manifest lockstep ci) | Pre-Slab-7c. NOT 7c.12's diff. |

**Conclusion:** All 5 spot-checked failures attribute to commits prior to Wave-3. None implicate §07F code. 7c.12 introduces ZERO new regressions per V7 v1.1 broad-regression baseline-shift policy.

---

## AC-by-AC verdict

| AC | Status | Evidence |
|---|---|---|
| AC-7c.12-A (§section package + parity_contract registration) | PASS | `app/gates/section_07f/poll_surface.py` declares `SURFACE_ID = SECTION_07F_SURFACE_ID`, decorates module-level `_parity_contract_registration` with `@parity_contract(surface_id="section_07f_g2f_motion_gate", mandatory_transports=["cli", "http"], optional_transports=["mcp-stdio"], alias_of="G2C")`, implements `display_motion_clip` + `submit_verdict` + `resume_from_verdict` + `load_motion_clip`, re-emits `canonical_model_bytes` + `compute_model_digest` helpers locally per Wave-3-trio + 7c.13 precedent. |
| AC-7c.12-B (OperatorVerdict variant + JSON schema regen) | PASS | `Section07FOperatorVerdict` + `MotionClipEditPayload` + closed `Literal["approve", "edit", "reject"]` verb + `motion_clip_id` strip-non-empty + UUID4/tz-aware/sha256 validators + verb-payload consistency. Schema test `test_section_07f_operator_verdict_schema_file_matches_model` enforces on-disk equality with model-derived JSON; A18 Path.write_text discipline honored (LF-only verified at byte level: BOM=False, CRLF=0, LF=106). |
| AC-7c.12-C (3-transport schema-stability shape-pin) | PASS | `test_section_07f_shape.py` invokes the canonical `assert_operator_verdict_schema_stable_across_transports(verdict_class=Section07FOperatorVerdict, surface_id=SECTION_07F_SURFACE_ID, transports=["cli", "http", "mcp-stdio"])` from `tests/schemas/operator_verdict/_harness.py`, plus an in-file schema-hash-stable assert across all three transports. |
| AC-7c.12-D (DSL-registration audit + 3-transport-parity test) | PASS | `test_g2f_poll_surface_dsl_registration.py` AST-asserts exactly one module-level `parity_contract` + reload-and-introspect-asserts `mandatory_transports=["cli", "http"]` + `optional_transports=["mcp-stdio"]` + `alias_of="G2C"` + `run_self_registration_audit` PASS. `test_g2f_poll_surface_three_transport_parity.py` round-trips a sample G2F motion-clip approval verdict across all three transports with payload + digest equality assertions, plus tampered-digest rejection + motion_clip_id-mismatch rejection + YAML loader UTF-8 path. |
| AC-7c.12-E (C6 import-linter modules list append, parallel-dispatch coordinated) | PASS | `pyproject.toml` C6 modules list grew to 10 entries via 4-way union with concurrent 7c.9/7c.10/7c.11. PARALLEL-DISPATCH GUARDRAIL #3 honored. Lint-imports `12 kept, 0 broken` UNCHANGED. |

---

## Recommendation

**Flip 7c.12 to `done` in `_bmad-output/implementation-artifacts/sprint-status.yaml`.**

No follow-on work required. Story closes cleanly under the Wave-3-next-batch lite-tier follower pattern. The pre-existing inherited-failure NIT remains common across the entire G2C-aliased-fanout 4-way batch — would be addressed by a separate test-hardening story (out of 7c.12 scope).

---

**Time spent:** ~13 min (within T11 lite budget).
