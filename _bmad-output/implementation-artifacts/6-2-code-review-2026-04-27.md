# Slab 6.2 Code Review - dependency_map manifest promotion

**Review date:** 2026-04-27  
**Reviewed implementation commit:** `01631b6` (`feat(slab-6.2): promote dependency_map declaration into pipeline manifest`)  
**Review fix commit:** `c652747` (`fix(slab-6.2): normalize manifest dependency ids`)  
**Mode:** full, three-layer review (Blind Hunter + Edge Case Hunter + Acceptance Auditor)

## Summary

Review found one substantive patch item: dependency cycle validation used raw specialist ids while production dispatch uses canonical specialist ids (`quinn-r` -> `quinn_r`, `elevenlabs` -> `enrique`). That could let alias-mediated dependency cycles compile. Commit `c652747` fixes the compiler and runner normalization path and adds regression coverage.

No `decision_needed` items surfaced. No new deferred-inventory entries were filed from this review. Five findings were dismissed as false positives, out-of-scope hardening, or already covered.

## Per-layer findings

### Blind Hunter

| ID | Finding | Triage | Resolution |
|---|---|---|---|
| BH-1 | Kira dependency appears to reference `quinn_r` while manifest node says `quinn-r`. | dismiss | False positive after project-context review: dispatch canonicalizes `quinn-r` to `quinn_r`; commit `c652747` also makes visible alias dependency values resolve to canonical contributions. |
| BH-2 | Manifest dependency values can reference nonexistent upstream specialist ids without compile failure. | dismiss | Spec requires fail-loud at dependency resolution boundary, not compile-time membership validation. Existing M-R1 test covers phantom upstream at trial start. |
| BH-3 | Resume path changed without resume-specific new test coverage. | dismiss | Existing resume integration slices exercise `resume_production_trial`; post-patch run passed 9/9. Adding duplicate tests would not materially improve coverage. |

### Edge Case Hunter

| ID | Finding | Triage | Resolution |
|---|---|---|---|
| EC-1 | Alias-normalized circular dependencies can compile because `_validate_dependency_cycles` used raw `node.specialist_id`. | patch | Fixed in `c652747`; compiler now canonicalizes node ids and dependency values before cycle traversal. |
| EC-2 | Visible manifest ids like `quinn-r` / `elevenlabs` could be accepted by schema but fail at runtime against canonical contribution ids. | patch | Fixed in `c652747`; runner normalizes dependency map entries to existing canonical contributions before fail-loud check. |
| EC-3 | `MissingUpstreamContributionError` is only reachable in live-call branch. | dismiss | Dependency resolution is only meaningful when a specialist invocation is attempted. Offline/no-live paths do not cross the dependency resolution boundary. |
| EC-4 | `dependencies` accepts empty string keys/values. | dismiss | The accepted schema is `dict[str, str]`; no min-length constraint is specified. Empty upstream values still fail loud if absent. |

### Acceptance Auditor

| ID | Finding | Violated constraint | Triage | Resolution |
|---|---|---|---|---|
| AA-1 | Alias-normalized circular dependencies can compile. | AC-Slab-6.2-A; W-R3 circular-check verification; N5 state-flow contract. | patch | Fixed in `c652747`; regression added at `tests/integration/manifest/test_manifest_dependencies_field.py::test_schema_rejects_alias_normalized_circular_dependencies_at_compile_time`. |

## Triaged punch list

### Patch

| Patch ID | Sources | Issue | Addressing commit |
|---|---|---|---|
| P-1 | EC-1 + EC-2 + AA-1 | Manifest dependency id handling was not consistently canonicalized across compiler cycle checks and runner dependency resolution. | `c652747` |

Patch evidence:
- `app/manifest/compiler.py:291` canonicalizes specialist ids and dependency values before cycle validation.
- `app/marcus/orchestrator/production_runner.py:247` normalizes dependency-map upstream ids to canonical contribution ids when present.
- `tests/integration/manifest/test_manifest_dependencies_field.py:76` pins alias-mediated cycle rejection.
- `tests/integration/marcus/test_production_runner_dependency_resolution.py:192` pins visible alias dependency resolution.

### Defer

None.

### Dismiss

| Dismiss ID | Sources | Justification |
|---|---|---|
| D-1 | BH-1 | Kira's `quinn_r` dependency is correct for the runtime contribution id; patched alias handling also supports visible `quinn-r` inputs. |
| D-2 | BH-2 | Compile-time membership validation was not specified; the binding fail-loud rule is runtime dependency resolution, already tested. |
| D-3 | BH-3 | Resume behavior is covered by existing resume integration tests, which passed post-patch. |
| D-4 | EC-3 | Offline/no-live execution does not invoke specialists and therefore does not resolve dependencies. |
| D-5 | EC-4 | Empty-string dependency keys/values are outside the specified `dict[str, str]` shape constraints and do not create a current behavioral regression. |

### Decision Needed

None.

## Acceptance trace

| Area | Verdict | Evidence |
|---|---|---|
| NodeSpec.dependencies shape | PASS | `app/manifest/schema.py:115` keeps `dict[str, str] \| None` with `default=None`; tests cover empty, declared, and non-dict rejection. |
| Circular dependency validation | PASS | `app/manifest/compiler.py:291` called by both compiler entry points; `c652747` adds alias-normalized cycle rejection. |
| Manifest-first runner precedence + permanent fallback | PASS | `_resolve_dependency_map` reads `node.dependencies` first and falls back to `_default_dependency_map_for`; fallback docstring remains permanent/non-transitional. |
| MissingUpstreamContributionError | PASS | `app/marcus/orchestrator/production_runner.py:52` stores upstream id, downstream input key, and downstream specialist id; raised before adapter invocation. |
| Fail-loud missing upstream tests | PASS | `test_missing_upstream_dependency_fails_loud` and `test_upstream_ran_but_no_contribution_fails_loud` assert exception type and key fields. |
| Pipeline manifest declarations | PASS | Current explicit entries match fallback behavior; at least one existing specialist node omits `dependencies` and exercises fallback. |
| W-R2 fallback regression test | PASS | `test_fallback_path_unchanged_for_undeclared_existing_manifest_node` uses the real manifest path and asserts call order, fallback map, and contributions. |
| M-R4 isolated unit test | PASS | `test_resolve_dependency_map_prefers_declared_map` calls `_resolve_dependency_map` directly. |
| Composition Spec updates | PASS | Section 3.6 states fallback retention is permanent; Section 10 has Slab 6.2 decision row; Section 12 item #1 is RESOLVED. |
| Migration guide update | PASS | Production Runner section documents manifest dependency source, permanent fallback, and `MissingUpstreamContributionError`. |
| Schema-pin drift refresh | PASS | `tests/unit/manifest/test_schema_pin.py` still pins NodeSpec/PipelineManifest; fixtures include `dependencies`. |
| Path A-prime invariants | PASS | No Slab 6.0/6.1 substrate, specialist graph, or M3 harness files were modified by `01631b6` or `c652747`. |

## Substrate Inventory Checklist Trace

| N-item | Applicability | Verdict | Trace evidence |
|---|---|---|---|
| N1 - External-provider resource ID validity | N/A | N/A | No new external provider ids, model ids, endpoints, auth surfaces, or resource ids introduced. |
| N2 - Composition exercise before vote | N/A | N/A | No new composition-shape vote; Slab 6.2 is a minor manifest schema extension with fallback retention. |
| N3 - Live-API smoke before MVP close | N/A | N/A | Migration already shipped; this story does not add an MVP or provider-live gate. |
| N4 - Per-component isolation invariant preserved | applicable | PASS | No `app/specialists/**`, `ProductionEnvelope`, `dispatch_adapter`, `production_trial_envelope`, or M3 harness changes; `pytest tests/composition/test_specialist_isolation_preserved.py -q --tb=short` -> 16 passed. |
| N5 - Cross-component state-flow contract | applicable | PASS | Manifest dependencies now make state-flow explicit; `c652747` closes alias drift in compiler + runner; focused dependency tests 13 passed. |
| N6 - Gate boundary scope hierarchy | N/A | N/A | No gate hierarchy or gate precedence behavior changed. |
| N7 - Replay regression verifies execution path | applicable | PASS | Slab 6.2 touched no replay path. Replay slice attempted; failures are the pre-existing deferred pack-hash drift `013b7ef -> 19cde78`, which dispatch explicitly excludes from new findings. |
| N8 - Cost machinery integration with real trace data | N/A | N/A | No cost machinery, pricing, trace aggregation, or economics behavior changed. |
| N9 - Operator-witnessed evidence at M-gate vote | applicable | PASS | Single-gate story: dev-side evidence is recorded here; operator confirmation remains the close trigger before `review -> done`. |
| N10 - Anti-pattern catalog read at architecture-authoring time | N/A | N/A | No new architecture authoring; A12 remains partially mitigated and generator-emit follow-on stays deferred. |
| N11 - Composition mode declared alongside isolated mode | N/A | N/A | No composition mode change; existing Composition Spec Section 3.6 governs dependency-map sourcing. |
| N12 - Auth model verified via probe | N/A | N/A | No new external auth surface or provider probe requirement introduced. |

## Verification

- `.\.venv\Scripts\python.exe -m pytest tests/integration/manifest/test_manifest_dependencies_field.py tests/integration/marcus/test_production_runner_dependency_resolution.py -q --tb=short` -> 13 passed.
- `.\.venv\Scripts\python.exe -m pytest tests/unit/manifest/test_schema_pin.py tests/unit/manifest/test_schema.py tests/unit/manifest/test_compiler.py tests/integration/manifest/test_manifest_dependencies_field.py tests/integration/marcus/test_production_runner_dependency_resolution.py -q --tb=short` -> 50 passed.
- `.\.venv\Scripts\python.exe -m pytest tests/integration/manifest tests/unit/manifest tests/integration/marcus/test_production_runner_dependency_resolution.py tests/integration/marcus/test_production_runner_gate_pause_resume.py tests/integration/marcus/test_production_runner_resume_continues_execution.py tests/integration/marcus/test_production_runner_resume_invariants.py tests/integration/marcus/test_production_runner_invocation.py tests/end_to_end/test_full_pipeline_smoke.py -q --tb=short` -> 88 passed.
- `.\.venv\Scripts\python.exe -m pytest tests/composition/test_specialist_isolation_preserved.py -q --tb=short` -> 16 passed.
- `.\.venv\Scripts\python.exe -m ruff check app/manifest/compiler.py app/marcus/orchestrator/production_runner.py tests/integration/manifest/test_manifest_dependencies_field.py tests/integration/marcus/test_production_runner_dependency_resolution.py` -> All checks passed.
- Manifest parse check -> 33 nodes / 34 edges.
- Replay regression slice attempted; still blocked by known pre-existing `PackHashDriftError` (`013b7ef...` expected, `19cde78...` actual), not introduced by Slab 6.2.

All `patch` items addressed in commits; all `defer` items filed in deferred-inventory; all `dismiss` items justified above; `decision_needed` items surfaced to operator; substrate inventory trace complete with N-items 1-12 verdicts recorded. Slab 6.2 ready for `review -> done` flip pending operator confirmation.
