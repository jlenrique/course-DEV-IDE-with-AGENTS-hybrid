# Slab 3 Marcus Invariant Stub

Seed stub for the Slab 5a invariant-audit matrix. Marcus + gate surfaces are
recorded here so the later matrix story can absorb them without re-discovering
the Slab 3 close state.

- **Marcus**: Single operator-facing surface remains `marcus.facade.get_facade()`; local M3 trial proves manifest-driven supervision plus baseline-envelope capture.
- **G1**: Trial-open DecisionCard emitted and resumed through the canonical verdict path with healthy cache-state metadata.
- **G2C**: Pre-execution DecisionCard accepted an `edit` verdict and propagated the edit payload into downstream trial state.
- **G3**: Mid-trial DecisionCard emitted after the runtime override applied; cache-state surface flipped to `mixed`.
- **G4**: Closeout DecisionCard emitted on the same verdict authority path and recorded the local artifact set.
- **OperatorVerdict**: Trial-bound digest + nonce replay protection remain the only accepted gate-resume receipt shape.
- **resume_api**: Registered-card digest validation and transport-neutral resume payload remain the sole authority boundary for gate continuation.
