# Defect fix — Irene Pass-2 slide-join id-integrity gate

**Type:** production-defect fix (SPOC runtime), surfaced by the S7 AC-L live run (first-ever in-graph reach to 07W). **Unblocks + carries:** the S7 AC-L witness (see §Two-story close). Its own small story — NOT part of S7's producer scope.
**Status:** AMENDED — focused green-light 3/3 GO-WITH-AMENDMENTS (Winston/Murat/John), all folded below.
**Branch:** `dev/workbook-2026-07-06`. **Execution:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, `-n 0`, no `git stash`, do NOT commit (orchestrator commits the fix + the AC-L witness together AFTER the live witness).
**Governance (John A5):** substrate change on the protected VO/narration path → not Claude-direct. Dev reads `docs/dev-guide/pipeline-manifest-regime.md` at T1 (precautionary). **Correction (Winston 4):** `app/specialists/irene/graph.py` is NOT a `block_mode_trigger_paths` member — no block-mode lockstep hook gates this path. Expected Tier-1 (NO `pipeline-manifest.yaml` edit; AC-5 git-diff-empty is belt-and-suspenders). Monitor poll at dispatch + close; `bmad-code-review` before done (mandatory); party concurrence at close mirroring S7.

## Defect (confirmed from run `40f3a90a` frozen node-08 contribution)
Pass-2 (gpt-5, node 08) authored 9 distinct, well-grounded per-slide narrations but emitted **`id`/`segment_id` = None on every narration segment AND every delta**. Cascade:
1. `backfill_delta_ids` (`graph.py:534`, called `:2069`/`:2073`) cannot repair — needs a `segment_id` (absent) or narration_script ids (all None).
2. `_assert_narration_joins_roster` (`graph.py:673-702`, called `:2085`) **passes** — validates `perception_source`→roster grounding, NEVER id-join integrity. **The gap.**
3. `join_narration_segments` (`narration_join.py:20-62`) keys narration text by `id` (`:34`) + deltas by `id` (`:39`) → all empty → single `''` bucket → **last-write-wins = slide-09's summary** → 9 rows, distinct `slide_id`, empty ids, IDENTICAL summary narration. `phantom_segment_ids` (`:65-79`) misses it (collapsed text non-empty).
4. `_write_segment_manifest_for_b` (`storyboard_publisher.py:154-208`, only guards `if not segments`) writes it; node 07W `_load_segments` (`_act.py:227-229,257-261`) drops all → `workbook-producer.segment-manifest.empty` — **3 nodes too late.**

**General fragility** — any id-less Pass-2 emission on any deck collapses identically; corrupts the whole lesson's narration (Enrique audio pre-spend, G5 QA, VO — not just the workbook). Part-3 (5 slides → 9-card deck w/ summary tail) merely triggered it.

## Deliverable — D1: id-integrity gate at the Pass-2 output boundary
In `app/specialists/irene/graph.py`, AFTER the backfill calls and alongside `_assert_narration_joins_roster` (~`:2073`–`:2085`), add a **directly-callable pure function** (e.g. `_assert_join_id_integrity(parsed)`) that raises `Pass2GroundingError(tag="irene.pass2.slide-join-failed", ...)` when, AFTER backfill, **either**:
- **(a) id-absence** — any narration_script segment or delta still lacks a usable `id` (empty/None), **or**
- **(b) non-bijective id-join** — the id-join is not 1:1: `len({usable ids across narration_script segments}) < len(narration_script)` (**positional cardinality of id-bearing segments — NEVER keyed on `narration_text`**; Murat A4 + Winston 1). This is the `text_by_id`-overwrite collapse vector (`narration_join.py:34`).

**Distinct detail per condition (John A2 — also the anti-vacuity discriminator):** the raised `Pass2GroundingError` **detail string must name which condition fired** — e.g. `"id-less after backfill"` (a) vs `"id-join non-bijective"` (b) — DISTINCT from the pre-existing `_assert_narration_joins_roster` grounding message (`graph.py:701`). The tag `irene.pass2.slide-join-failed` is already in `_RETRYABLE_DISPATCH_TAGS` (`production_runner.py:1803`, `_MAX_DISPATCH_RETRIES=3`) → auto-retry self-heals an id-bearing re-roll (do NOT mint a new tag / edit the retryable set). **Enforcement stays at the Pass-2 boundary — do NOT touch the frozen import-identity-pinned `join_narration_segments` (`narration_join.py`).**

## Acceptance criteria
- **AC-1 (id-less → fail-loud, RED-first, non-vacuous):** a Pass-2 output with **valid roster-matching `perception_source`s** but empty narration/delta ids (the true `40f3a90a` shape) → `_assert_narration_joins_roster` returns SILENT, and the new `_assert_join_id_integrity` RAISES `Pass2GroundingError` with the id-absence detail substring. RED against pre-fix: the fixture passes the roster gate and silently degrades. **Assert BOTH the roster-gate-silent AND the new-gate-fires** (Murat A1) — tag-only assertion is insufficient (shared tag).
- **AC-2 (non-bijective collapse → fail-loud):** a fixture with **duplicate/empty non-empty narration ids** (distinct segments sharing a join key — the `text_by_id` overwrite path) → new gate raises with the non-bijective detail substring. RED-first; deterministic; mirrors `40f3a90a` collapse.
- **AC-3 (no regression + FALSE-FIRE GUARD):** (i) a valid id-bearing Pass-2 output (known-good tejal shape — reuse `tests/specialists/irene/conftest.py:71-85 joined_pass2_response()`, distinct `segment_id`s, bijective) passes the new gate unchanged; existing Pass-2/narration-join suites green. (ii) **False-fire guard:** two segments with **identical `narration_text` but DISTINCT ids** → PASS (legitimate repeated prose); only a **shared non-empty id** → FIRE.
- **AC-4 (deterministic collapse proof — the real hole):** a required pure-function test asserting pre-fix `join_narration_segments(<id-less fixture>)` yields the degenerate flood (distinct `slide_id`, IDENTICAL `narration_text` across all rows). Proves the gate closes a live collapse, not a hypothetical.
- **AC-5 (retryable + protected-path):** `irene.pass2.slide-join-failed` ∈ `_RETRYABLE_DISPATCH_TAGS` (assert membership). `narration_join.py` byte-frozen; no VO↔on-screen / source-detail regression; `git diff -- state/config/pipeline-manifest.yaml` EMPTY (Tier-1, belt-and-suspenders).
- **AC-6 (full-tree sweep):** causation-isolated; zero new reds beyond the known baseline.
- **Test co-location (John A3):** extend `tests/specialists/irene/test_irene_pass2_grounding_fail_loud.py` (already asserts this tag at `:138`), not a new orphan module.

## Recover path (orchestrator, after the fix — the S7 AC-L witness)
`recover_production_trial` re-enters at the failed node index (46 = 07W) → re-reads the frozen bad manifest → dead-end. The fix is upstream (node 08), so recover **surgically re-dispatch the `08 → 08B → 07W` chain** (Winston 2 — NOT a forward walk, which re-runs 09-15 and re-spends ElevenLabs audio at node 12, which 07W does not consume). **Drop BOTH the node-08 `irene` AND the node-08B contributions** from the persisted envelope (Winston 3 — else 08B is skipped via latest-contribution reuse and the stale bad manifest stays on disk), then re-dispatch: Pass-2 re-rolls (now gated → id-bearing) → 08B G3-publish re-derives a valid storyboard-B manifest → 07W produces the workbook. Re-spends only Pass-2 (gpt-5); the real Gary deck + vision + motion + audio assets are reused.
**Stop rule (Murat A6 — binding):** the DETERMINISTIC unit proof (AC-1..AC-6) is the binding acceptance. The live recover is a **CONFIRMATION** witness contingent on gpt-5 emitting ids within the bounded retries (1+3). If ALL attempts emit id-less segments → the run error-pauses (correct fail-loud) → **STOP and report**; do NOT hand-inject ids into the envelope, do NOT retry-to-green (that fabricates the witness). A persistent id-less re-roll is a Pass-2 *authoring* defect harvested separately.
**Witness inspection (= S7 AC-L):** no tejal leakage (header + body), Part-3 sections from Irene's collateral, ≥1 figure, ≥3 Bloom exercises, and the resolved DOI `10.3991/ijac.v17i2.45555` if the DOI leg fires.

## Two-story close (John A1 — honest sequencing; binding)
S7 did NOT independently flip done — its AC-L (first in-graph workbook off a different lesson) error-paused on THIS defect. S7's producer CODE stays done/reviewed (integration-boundary-proven; real — do not undo). The **single recover witness closes BOTH stories**, cross-cited: S7's close record + status line must state *"producer-generalization proven off the frozen `tejal-apc-c1-m1-p2-trends` lesson; the first in-graph workbook from a DIFFERENT lesson (Tejal Part 3) was produced only after the upstream Irene id-integrity fix (this story)."* **⛔ Codex SOP-034 P1: do NOT claim "first non-tejal workbook" — Part 3 is a different LESSON of the SAME SME (Tejal); cross-SME/cross-course generalization is Phase-2.** Preserve S7's "not learner-ready workbook" honesty guard. This story's DONE = live AC-L workbook produced + inspected.

## Deferred follow-ons to file (John A4 — before this story flips)
- **`join-narration-segments-silent-collapse-hardening`** — a pure non-raising `collapsed_segment_ids`/non-bijective DETECTOR (sibling to `phantom_segment_ids`) all three join consumers can consult; the collapse is invisible to Enrique pre-spend + G5 QA + publisher, not just 07W. Aligns with the bundle-carrier fail-loud invariant.
- **`recover-with-reenter-node-affordance`** — `recover_production_trial` hardcodes re-entry at the failed node; a defect whose fix is UPSTREAM needs a `recover(..., reenter_at_node=…)` / drop-contribution-and-rewind affordance (couples to the golden-bundle rewind-recover pattern).

## Out of scope
Widening `join_narration_segments` (frozen shared policy) · the workbook producer (S7) · any `pipeline-manifest.yaml` edit (Tier-1) · Pass-2 authoring quality beyond id-integrity.
