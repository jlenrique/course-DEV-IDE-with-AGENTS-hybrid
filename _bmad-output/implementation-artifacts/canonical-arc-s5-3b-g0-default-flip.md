# Story S5-3b — G0-enrichment default flip + canonical G0E/G0R witness (S5 split, part b)

**Arc:** Canonical Production Conversation (party record §1 S5 row; §7(d) Tier-1 ruling; M1 migration-before-flip; M-wit S5 live witness). Gated after S5-3a (DONE `721cce04`).
**Status:** **DONE** (2026-07-07 session 18; live-proven both lanes). Gate chain: SOP-018 CONCUR-W-F → **F-1801 party-ratified 4/4 RATIFY-A** (Winston/John/Murat/Dan — structure-always + content-armed; `--g0-dispatch-live` CLI arm; fail-loud mode-stamp; Dan's affordance) → the 3b flip-attempt STOP surfaced a 2nd migration wave → **re-scope party 3/3** → **S5-3a.2 pre-migrated the whole blast radius** (`3eff93da`, SOP-020: zero flip-caused reds remain) → fresh dev RED-first re-implement on the flip-ready baseline → orchestrator re-verify (truth-table exact, manifest+parity frozen, zero new reds) → **SOP-021 CONCUR** (D1-D4 faithful; mode-stamp fail-loud content-derived; CLI default-OFF no production-default-ON; un-skip cashed) → **AC-L TWO-LANE LIVE WITNESS: BOTH PASS, frozen judges first-run-stands** — Lane-a 8/8 (env proven absent after dotenv → studio-smoke-min DIRECTORY → **first pause G0E** not G1 → G0E→G0R→G1 real resume; receipt `deterministic-recorded`; $0 pre-pass) + Lane-b 4/4 (armed `--g0-dispatch-live` → genuine gpt-5 G0 pre-pass; receipt `live`; richer content; divergence guard holds); evidence `s5-3b-acl-liveproof-20260707T080341Z`; spend ~$0.0003. **The operator directive "G0 enrichment canonical — performed on every run with HIL input" is REALIZED (Reading A): G0E/G0R HIL gates + deterministic enrichment on every unset-env run; live enrichment operator-armed.** · **Size:** M/L · **Tier-1** (no manifest edit) · **un-skip cashed** (`test_ac_s3_6_default_on_parity_with_s2`) · **Filed follow-ons:** `g0-production-default-dispatch-live-decision` (Tier-2), the 2 SOP-021 cosmetic NITs (postmortem). · **Gate mode:** single-gate structural (3-lane at T11) · **Branch:** `dev/workbook-2026-07-06`.
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, `-n 0`, **no `git stash`**. Do not commit — orchestrator commits after re-verify + LIVE witness.

## What 3b does (the operator directive cashes out here)
Operator directive (party record, binding): "the G0 enrichment loop should be canonical — performed on every run with HIL input." 3a made the 8 first-pause-G1 suites env-independent; 3b **flips the default** so the CANONICAL (unset-env) Marcus-SPOC run wakes G0E + G0R as standing gates, and proves it live.

## The flip (exact — verified at `721cce04`)
`g0_enrichment_active()` (`app/marcus/orchestrator/g0_enrichment_wiring.py:123-130`) currently returns True ONLY on explicit truthy env; UNSET → False (dormant). **Flip: UNSET → True (canonical ON); kill-switch = explicit falsy** (`"0"/"false"/"no"/"off"` → False). This is the one-line-family change that makes 3a's `setenv("0")` pins the kill-switch escape-hatch and every unset-env walk wake G0E→G0R→G1. `G0R` rides the same env (`irene_refinement_active()` delegates) — one flip wakes both. **`g0_dispatch_live()` (`:141-148`) is NOT flipped here** — it stays default-OFF (deterministic recorded pre-pass) so the default test path never spends; production arms it (see D4).

## Deliverables
### D1 — The default flip + kill-switch (Tier-1)
Flip `g0_enrichment_active()` to default-ON with an explicit-falsy kill-switch; update the module comment (`:112-119`) — the "follow-on once the existing integration suite is migrated" note is now CASHED (3a migrated it). **Tier ruling (§7(d), pre-decided): Tier-1** — the flip lives in `g0_enrichment_wiring.py` (+ possibly `production_runner.py`), neither a `block_mode_trigger_paths` member; NO `pipeline-manifest.yaml` edit; nodes/edges already compiled. **STOP-and-report if any manifest edit becomes unavoidable** (that re-opens the lockstep regime + the T6c question).

### D2 — T1 flip-and-sweep: enumerate + migrate the TRUE residual (M1)
**T1 task #1 (before ACs freeze):** apply the flip locally, run the FULL offline suite (`tests/ -n 0`), and enumerate EVERY test that breaks — not just the 3a-migrated 8. The flip adds G0E/G0R pauses to every unset-env full walk, so tests that (a) resume/assert a gate SEQUENCE, (b) count pauses, or (c) implicitly assume the first pause was G1 may break beyond the 3a set. Apply the **same M1 disposition rubric** (D-migrate-canonical pin ON + assert the G0E-first canonical walk / D-kill-switch pin OFF / D-delete) with a **test-disposition ledger** in Completion Notes. **Scope signal (count breakage CLASSES, not raw test count — F-1803):** one shared-walk-helper or fixture fix can clear many tests, so enumerate by CLASS (first-pause / gate-SEQUENCE / pause-COUNT / CLI-gate-shim-resume-path). SOP-018 corroborated the residual is likely small (~0-5): most runner tests build synthetic single-gate manifests (no G0E/G0R nodes → immune), and the real-manifest full-walk pins were already migrated in 3a. Explicitly INCLUDE in the sweep the (a) gate-SEQUENCE / pause-COUNT asserters and (b) CLI-gate-shim resume-path tests — the two classes a first-pause-only grep (3a) could have missed. If the residual beyond the 3a-8 is small (≤~5), migrate in 3b; if large (a broad implicit dependency), STOP and report (scope conversation, not silent thrash). The canonical-path tests (pin ON) need a real corpus DIRECTORY (e.g. `course-content/courses/studio-smoke-min` from the pre-S5 story, or `tests/fixtures/trial_corpus`) — the README-file offline suites cannot drive the ON path (3a finding).

### D3 — G0E/G0R standing-gate narration (Beats 2-3) + the "live-available" affordance (Dan's binding caveat)
The canonical run now pauses at G0E (source-enrichment confirm) then G0R (LO ratify) before G1. Add the operator-facing kickoff narration Beats 2-3 (Beat-1 = the S2 picker ceremony) so the operator sees a coherent "here is what I extracted / here are the LOs to ratify" surface at G0E/G0R — mirroring S2's Beat-1 framing. Content-neutral wiring; the G0E/G0R decision cards already exist (`g0r.py`, the enrichment card). Keep it a narration/HIL-display layer, not new gate logic.
- **Dan's NON-NEGOTIABLE content caveat (party-ratified):** when the enrichment mode is `deterministic-recorded` (the unarmed default), the G0E/G0R narration MUST surface an explicit affordance — *"this is the deterministic scaffold; richer live semantic enrichment is available — arm `--g0-dispatch-live`."* Without it the operator cannot distinguish a deliberately-shallow cost-conscious default from the system's best effort and may ratify thin LOs believing that's the ceiling (a silent content-quality ceiling). The affordance rides the mode-stamp (D4) — the same receipt that records `deterministic-recorded` drives the operator-facing "live available" note.

### D4 — `MARCUS_G0_DISPATCH_LIVE`: structure-always + content-ARMED (F-1801 — PARTY-RATIFIED Reading A)
**Party ratification (focused round, 2026-07-07, Winston/John/Murat/Dan — verdict + reasoning in the monitor ledger):** the operator directive "canonical — performed on every run with HIL input" binds on the **HIL GATES (structure)**, NOT on live-LLM content every run. **Reading A adopted:** G0E/G0R HIL gates + the deterministic recorded enrichment are canonical on EVERY run; the **live LLM enrichment is operator-ARMED** (`g0_dispatch_live()` stays default-OFF — matches the `MARCUS_RESEARCH_DISPATCH_LIVE` precedent; keeps 3b Tier-1; the default/CI/fake-key path never spends and stays reproducible). Reading B (forced live every run) was rejected as scope-creep that corrupts the hermetic default and breaks the very ceremonies being canonicalized.
- **The F-1801 silent-deterministic trap is fixed by LEGIBILITY + FAIL-LOUD, not by forcing live (Winston):** STAMP the resolved enrichment mode (`live` | `deterministic-recorded`) into the run bundle/ledger receipt on EVERY canonical run, so a run can never silently serve deterministic content while the operator believes it is live; fail-loud on ambiguity.
- **Concrete production arming (John+Winston — first-class product surface, NOT a raw env export):** surface an explicit `marcus_spoc` CLI flag / preset field (e.g. `--g0-dispatch-live`, mirroring the research flag's surface) that resolves to `MARCUS_G0_DISPATCH_LIVE`; the env var is retained as the underlying impl seam + a dev/evidence-driver escape hatch. The operator-facing production arm is the CLI/preset flag (auditable in the run record).
- **Future Tier-2 filing (NOT 3b — out of scope):** whether canonical *production* should ever DEFAULT `dispatch_live=ON` is a separate operator-vision Tier-2 governance decision — file it, do not smuggle it through this default flip (`g0-production-default-dispatch-live-decision`).

### D5 — fast-path machinery default-on (already built; activate + witness)
The fingerprint-gated G0E cache-reuse + conservative-G0R machinery already exists (`build_enrichment_result` cache, exercised by the g0-battery). 3b confirms it is active on the canonical path (fingerprint-keyed G0E reuse across replays; G0R conservative re-ratify) with a witness — no new engine, just default-on coverage.

### D6 — T6c decision (in-arc rider vs deferral) — RECOMMEND DEFERRAL (keep Tier-1)
Party record §7(d): if 3b absorbs the T6c post-Pass-1 plan-review G1A wake, THAT edits the manifest (`fold_with` clear) → lockstep regime + party re-consent. **Recommendation (weigh at SOP-018 / party): DEFER T6c** to its filed follow-on `t6c-post-pass1-plan-review-wake` — keeping 3b Tier-1 and manifest-untouched. G0R already ratifies the LO contract before Pass-1 (T6c is PARTIAL-covered); the post-Pass-1 plan-unit review stays folded (G1A/04A). Deferring keeps the arc-close Batch-LLM GO carrying T6c as a named open item (J3 conservative ruling), not an automatic block. If the party wants T6c in-arc, it becomes a manifest-editing sub-story with the lockstep regime — out of this spec's Tier-1 scope.

## Acceptance criteria
- **AC-1 (the flip — explicit truth-table CONTRACT, F-1802):** `g0_enrichment_active()` must ENUMERATE the falsy kill-switch set `{"0","false","no","off"}` (stripped/lowered) → False, and **default EVERYTHING ELSE to True** — NOT `not in truthy_set` (which would wrongly flip empty/whitespace/unrecognized to OFF). Parametrized witness pinning: **unset → True**; **empty-string `""` → True**; **whitespace-only → True**; **unrecognized (`"maybe"`) → True**; each of `{0,false,no,off}` (and case/whitespace variants) → False; each of `{1,true,yes,on}` → True. `irene_refinement_active()` (G0R) delegates → one env wakes both (assert the delegation). This makes the kill-switch a real code contract, not an accident of set-membership.
- **AC-2 (3a pins hold):** all 8 3a-migrated suites stay GREEN post-flip (their `setenv("0")` kill-switch pins keep them on the G1-first dormant path). Re-run them.
- **AC-3 (residual migrated — M1 ledger):** every test surfaced by the D2 flip-and-sweep is dispositioned + green; the full offline suite (`tests/ -n 0`) is green except the KNOWN pre-existing baseline reds (2 drift + Texas-internal + slab_7a unicode); a test-disposition ledger accounts for each.
- **AC-4 (Tier-1 held):** `git diff -- state/config/pipeline-manifest.yaml` EMPTY; the flip touches only `g0_enrichment_wiring.py` (+ `production_runner.py`/narration wiring as needed); no pack prose change.
- **AC-5 (canonical narration):** G0E/G0R Beats 2-3 present on the woken canonical walk (operator-facing surface witnessed); no VO↔on-screen or source-detail regression (protected invariants — [[feedback_vo_onscreen_alignment_protected_invariant]], [[feedback_source_detail_to_gamma_conveyance_protected]]).
- **AC-6 (dispatch_live discipline — two-lane, Murat):** the DEFAULT path is deterministic + hermetic (a fake-key default run wakes G0E/G0R, spends ZERO, and emits a receipt naming the mode `deterministic-recorded` — assert ON THE RECEIPT, not just absence of error); the resolved mode is STAMPED into the bundle (D4); the armed path flips the REAL production switch (the `--g0-dispatch-live` CLI flag / preset, NOT a pytest marker) and dispatches a real G0 pre-pass. Divergence guard: the ONLY behavioral delta between the two lanes is the enrichment content SOURCE — gate structure, pause points, and carrier shape are identical.
- **AC-L (LIVE witness — orchestrator, PASS required; two lanes per Murat):**
  - **(a) canonical default-ENV walk (the flip proof):** a REAL run with `MARCUS_G0_ENRICHMENT_ACTIVE` verified ABSENT **after** any dotenv load (by exact var name — F-1804, closes the dotenv-override false-positive-default window) on a real corpus DIRECTORY (e.g. `studio-smoke-min`): pauses first at **G0E**, then (on approve) **G0R**, then G1 — the canonical G0E→G0R→G1 sequence live-witnessed via the **real CLI resume path** (confirm→ratify, not only the runner API — F-1805; the shim already accepts G0E/G0R verbs), enrichment result + LO-ratify cards real, and the receipt stamps `deterministic-recorded` + the "live-available" affordance (D3) present at the gate.
  - **(b) dispatch_live ARMED live-witness:** an armed run via the REAL `--g0-dispatch-live` production switch — real key — dispatches a genuine live-LLM G0 pre-pass (real model id + response artifact), receipt stamps `live`, and the G0E/G0R gates consume the LIVE content; the divergence guard holds (only the content source differs from lane (a)). $0 on lane (a); modest live spend on lane (b) — cost not a constraint.
  - First-run-stands; foreground/watchdog; cleanup APPEND (F-701).

## RED-first plan
1. **T1 flip-and-sweep** (D2): apply the flip, run `tests/ -n 0`, enumerate the TRUE broken set; RED-first per residual test.
2. Flip code (D1) + AC-1 parametrized env witness — RED first (unset currently → False).
3. Migrate the residual (D2) + confirm the 3a-8 hold (AC-2).
4. Beats 2-3 (D3) + dispatch_live discipline (D4/AC-6) witnesses.
5. Full offline suite green except known baseline reds (AC-3).
6. AC-L live legs (orchestrator).

## Out of scope
The T6c manifest-editing G1A wake (D6 — deferred; separate lockstep sub-story if the party wants it in-arc) · **whether canonical PRODUCTION should ever DEFAULT `dispatch_live=ON`** (party-filed Tier-2 operator-vision follow-on `g0-production-default-dispatch-live-decision` — 3b ships Reading A: default-OFF/armed) · S6 Tracy · S7 workbook · any `pipeline-manifest.yaml` edit (Tier-1) · new enrichment/G0R engine logic (the machinery exists — 3b activates + witnesses it) · the picker/CD/gary styleguide surfaces (S1-S4 closed).

## T11 gates
SOP-018 monitor pre-dispatch poll (verify: the flip semantics exact; the D2 flip-and-sweep will enumerate the TRUE residual not just the 3a-8; T6c deferral sound + Tier-1 held; dispatch_live production-arming defined; AC-L assert-unset preamble is a real default-witness not a set-value) → FRESH dev RED-first → SOP-019 + 3-lane `bmad-code-review` (production flip = real adversarial surface: over-broad flip, kill-switch gaps, narration regressions, dispatch_live spend leaks) → RED-first remediation → orchestrator re-verify → **AC-L LIVE both legs PASS** → commit (strays excluded) → SOP close poll. Story flips done ONLY after the live AC-L PASS.
