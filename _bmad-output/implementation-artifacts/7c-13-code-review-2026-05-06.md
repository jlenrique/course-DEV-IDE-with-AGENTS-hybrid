# Story 7c.13 — T11 Lite Cross-Agent Code Review

**Reviewer:** Claude (T11 lite cross-agent)
**Date:** 2026-05-06
**Story:** `migration-7c-13-section-08b-g3b-storyboard-b-live-url`
**Implementation author:** Codex GPT-5
**Review tier:** T11 lite (~10-15 min single-pass; aggressive DISMISS on cosmetic NITs per `docs/dev-guide/story-cycle-efficiency.md`)

---

## Verdict: **PASS-zero-patches**

Codex implemented Story 7c.13 cleanly within the prompt's file scope. All 5 ACs (A-E) are satisfied. All focused / non-regression / validator commands PASS exactly as Codex reported. The flagged inherited failure (`test_no_unauthorized_callers`) is confirmed PRE-EXISTING noise — not introduced by 7c.13. No MUST-FIX or SHOULD-FIX findings.

---

## Findings

| Severity | Finding | Rationale |
|---|---|---|
| **MUST-FIX** | _none_ | — |
| **SHOULD-FIX** | _none_ | — |
| NIT-DISMISSED | `test_no_unauthorized_callers` lists `app/models/operator_verdict_section_08b.py` in its scanner output | Pre-existing structural flaw in the test (substring scan matches CLASS DEFINITIONS like `class Section08BOperatorVerdict(BaseModel):`, not direct constructor calls). Already failing pre-7c.13 with 6 offenders (§02A + Wave-3 trio + concurrent 7c.14). 7c.13 inherits the breakage; does not introduce a new failure category. See dedicated analysis below. DISMISS as pre-existing. |
| NIT-DISMISSED | §02A DSL-registration tests (`test_poll_surface_registers_three_mandatory_transports`, `test_self_registration_audit_passes_floor_10`) FAIL under broad-run xdist but PASS in isolation | Shared-registry xdist ordering flake; §02A test files unchanged since 7c.3b (commit `f8fc1a8`). The §08b registration test is correctly reload-isolated per 7c.6 Codex precedent. NOT a 7c.13 regression. |

---

## `test_no_unauthorized_callers` analysis (verdict: PRE-EXISTING NOISE — DISMISS)

**Test mechanism (read at `tests/integration/gates/test_resume_api_authority.py:18-34`):**
The test scans `app/**/*.py` for the literal substring `OperatorVerdict(` and asserts the offender list is empty (excluding three allowlisted bridge modules + `operator_verdict.py`).

**Critical flaw:** the substring `OperatorVerdict(` appears in EVERY OperatorVerdict class definition (`class Section02AOperatorVerdict(BaseModel):` etc.) — the test pattern matches class signatures, not direct constructor invocations. It was authored at commit `9e441f7` (2026-04-26) before §section variants existed and has not been updated since.

**Pre-7c.13 offender list (from isolated run):**
1. `app/models/operator_verdict_section_02a.py` — added by 7c.3b (`f8fc1a8`)
2. `app/models/operator_verdict_section_04a.py` — Wave-3 trio (`636fbff`)
3. `app/models/operator_verdict_section_04_5.py` — Wave-3 trio (`636fbff`)
4. `app/models/operator_verdict_section_04_55.py` — Wave-3 trio (`636fbff`)
5. `app/models/operator_verdict_section_11.py` — concurrent 7c.14 (NOT 7c.13)
6. `app/gates/section_02a/poll_surface.py:160` (literal `Section02AOperatorVerdict(...)` constructor call) — 7c.3b

**7c.13 contribution:** `app/models/operator_verdict_section_08b.py` — same class-definition pattern; one drop in an existing flood of identical-shape "offenders."

**Verdict:** PRE-EXISTING NOISE. The test was already failing with 6 offenders BEFORE 7c.13 landed. The proper fix is to narrow the scanner to detect actual direct constructor calls (separate test-hardening story; not in 7c.13 scope). 7c.13 introduces no new structural concern beyond what §02A and Wave-3 trio already established as the §section-package pattern. **DISMISS.**

Codex's claim ("not introduced as a new failing test count by 7c.13") is **VERIFIED**.

---

## Spot-check evidence

| Command | Expected | Actual | Match |
|---|---|---|---|
| `pytest tests/gates/section_08b/ tests/schemas/operator_verdict/test_section_08b_shape.py` | 13 passed | **13 passed in 6.51s** | ✓ |
| `pytest tests/gates/section_02a/ tests/schemas/operator_verdict/test_section_02a_shape.py` | 15 passed | **15 passed in 7.79s** | ✓ |
| `pytest tests/gates/section_04a/ tests/gates/section_04_5/ tests/gates/section_04_55/` | 25 passed | **25 passed in 6.03s** | ✓ |
| `lint-imports.exe` | 12 KEPT | **Contracts: 12 kept, 0 broken** | ✓ |
| `validate_parity_test_class_conformance.py tests/parity/` | 19 PASS | **PASS: 19 parity contract file(s) conform** | ✓ |
| `validate_migration_story_sandbox_acs.py <story-file>` | PASS | **PASS — no sandbox-AC violations** | ✓ |
| `ruff check app/gates/section_08b/ app/models/operator_verdict_section_08b.py tests/gates/section_08b/ tests/schemas/operator_verdict/test_section_08b_shape.py` | clean | **All checks passed!** | ✓ |
| `operator_verdict_section_08b.v1.schema.json` LF-only / no BOM | LF-only / no BOM | **BOM=False; CRLF count=0; LF count=106** | ✓ |
| `pyproject.toml::tool.importlinter::contracts::C6::modules` includes `app.gates.section_08b` AND `app.gates.section_11` | 2-way union | **VERIFIED** (lines 256-263: union of 6 sections incl. both 08b + 11) | ✓ |

**Pattern-parity vs Wave-3 trio (`Section08BOperatorVerdict` shape audit):**
- UUID4 `run_id` validator via `enforce_uuid4_version` ✓
- Tz-aware `submitted_at` validator via `enforce_tz_aware` ✓
- sha256-hex `decision_card_digest` validator (`re.fullmatch(r"[0-9a-f]{64}", ...)`) ✓
- Strip-then-non-empty validators on `storyboard_id` + `operator_id` (mode="before") ✓
- Verb-payload consistency `model_validator(mode="after")` enforces edit↔edit_payload + reject↔reject_reason + approve forbids both ✓
- `model_config = ConfigDict(extra="forbid", validate_assignment=True, frozen=True)` ✓
- Closed `Section08BVerdictVerb = Literal["approve", "edit", "reject"]` ✓
- Closed `Section08BSurfaceId = Literal["section_08b_g3b_poll"]` ✓
- `parity_contract` decorator: `surface_id="section_08b_g3b_poll"` + `mandatory_transports=["cli", "http"]` + `optional_transports=["mcp-stdio"]` + `alias_of="G3"` ✓ (per FR-7c-17 + ADR 0002 §3)
- Re-emitted `canonical_model_bytes` + `compute_model_digest` helpers locally (C6 cross-§section independence) ✓
- DSL-registration test reload-isolated via `_clear_registered_surfaces_for_tests()` + `importlib.reload` ✓

---

## Broad-regression attribution

Reviewer-side broad run: **43 failed, 4300 passed, 27 skipped, 2 xfailed in 224.33s** — exact match with Codex's reported counts.

5 spot-checked failures with git-log attribution:

| # | Failing test | Most-recent touching commit | Attribution |
|---|---|---|---|
| 1 | `tests/test_run_hud.py::TestScanBundleArtifacts::test_lists_files_with_sizes` | `162d129` (Slab 6 trial experience bundle, pre-Slab-7c) | Pre-Slab-7c. NOT 7c.13's diff. |
| 2 | `tests/contracts/test_30_1_zero_test_edits.py::test_no_preexisting_test_files_modified_in_30_1` | `162d129` (Slab 6) | Pre-Slab-7c structural ledger contract. NOT 7c.13's diff. |
| 3 | `tests/migration/test_m5_verdict_consequence_path.py::test_m5_verdict_consequence_path` | `728f739` (working-tree spillover Slab 6.0+6.1) | Pre-Slab-7c. NOT 7c.13's diff. |
| 4 | `tests/end_to_end/test_full_pipeline_smoke.py::test_migrated_manifest_has_33_nodes` | `eb2adb0` (Slab 1 Story 1.6) | Pre-Slab-7c manifest migration test. NOT 7c.13's diff. |
| 5 | `tests/test_check_pipeline_manifest_lockstep.py::test_red_path_fixtures_fail_correctly_schema_only` | `d4defdc` (4.1 graph manifest lockstep ci) | Pre-Slab-7c. NOT 7c.13's diff. |

**Conclusion:** All 5 spot-checked failures attribute to commits prior to Wave-3. None implicate §08B code. Failure count 43 is BELOW the inherited Wave-3 trio band of 46-50 (per Codex's note + 7c.6/7c.7/7c.8 closeouts) — actually a small improvement, consistent with §08B introducing no new failures and possibly mooting one prior xdist flake. 7c.13 introduces ZERO new regressions per V7 v1.1 broad-regression baseline-shift policy.

---

## AC-by-AC verdict

| AC | Status | Evidence |
|---|---|---|
| AC-7c.13-A (§section package + parity_contract registration) | PASS | `app/gates/section_08b/poll_surface.py` declares `SURFACE_ID = SECTION_08B_SURFACE_ID`, decorates module-level `_parity_contract_registration` with `@parity_contract(surface_id="section_08b_g3b_poll", mandatory_transports=["cli", "http"], optional_transports=["mcp-stdio"], alias_of="G3")`, implements `display_storyboard_b` + `submit_verdict` + `resume_from_verdict` + `load_storyboard_b`, re-emits `canonical_model_bytes` + `compute_model_digest` helpers locally per Wave-3-trio precedent. |
| AC-7c.13-B (OperatorVerdict variant + JSON schema regen) | PASS | `Section08BOperatorVerdict` + `StoryboardBEditPayload` + closed `Literal["approve", "edit", "reject"]` verb + `storyboard_id` strip-non-empty + UUID4/tz-aware/sha256 validators + verb-payload consistency. Schema test `test_section_08b_operator_verdict_schema_file_matches_model` enforces on-disk equality with model-derived JSON; A18 Path.write_text discipline honored (LF-only, no BOM verified at byte level). |
| AC-7c.13-C (3-transport schema-stability shape-pin) | PASS | `test_section_08b_shape.py` invokes the canonical `assert_operator_verdict_schema_stable_across_transports(verdict_class=Section08BOperatorVerdict, surface_id=SECTION_08B_SURFACE_ID, transports=["cli", "http", "mcp-stdio"])` from `tests/schemas/operator_verdict/_harness.py`. |
| AC-7c.13-D (DSL-registration audit + 3-transport-parity test) | PASS | `test_g3b_poll_surface_dsl_registration.py` AST-asserts exactly one module-level `parity_contract` + reload-and-introspect-asserts `mandatory_transports=["cli", "http"]` + `optional_transports=["mcp-stdio"]` + `alias_of="G3"` + `run_self_registration_audit` PASS. `test_g3b_poll_surface_three_transport_parity.py` round-trips a sample G3B verdict across all three transports with payload + digest equality assertions, plus tampered-digest rejection + storyboard_id-mismatch rejection + YAML loader UTF-8 path. |
| AC-7c.13-E (C6 import-linter modules list append, parallel-dispatch coordinated) | PASS | `pyproject.toml` C6 modules list grew from `[app.gates.section_02a, app.gates.section_04a, app.gates.section_04_5, app.gates.section_04_55]` to include both `app.gates.section_08b` AND `app.gates.section_11` — PARALLEL-DISPATCH GUARDRAIL #3 2-way union honored with concurrent 7c.14. Lint-imports `12 kept, 0 broken` UNCHANGED. |

---

## Recommendation

**Flip 7c.13 to `done` in `_bmad-output/implementation-artifacts/sprint-status.yaml`.**

No follow-on work required. Story closes cleanly under the Wave-3 lite-tier follower pattern. The two pre-existing xdist-flake / structural-test-flaw NITs remain inherited from Wave-3 trio close — they would be addressed by separate test-hardening stories (out of 7c.13 scope).

---

**Time spent:** ~14 min (within T11 lite budget).
