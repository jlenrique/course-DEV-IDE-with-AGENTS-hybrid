# Leg-C — `min_cluster_floor` scripted class (Irene Pass-1 cluster floor, 07G-gated)

**Arc:** Gamma Styleguide Library (branch `dev/gamma-styleguide-library-2026-07-01`).
**Class:** S. **Status:** **in-progress** (offline delta built + 3-lane `bmad-code-review` + P1–P8 remediated RED-first, 62 Leg-C tests green / ruff clean; D1/D2 folded to the live Part-3 baseline GO/NO-GO; live ACs #9–13 + dual-gate CLOSE pending). **Phase:** 4-implementation. **Parent:** Leg-C (arc leg). **Depends on:** Leg-A (done) only — NOT Leg-E.
**Gate mode:** **DUAL-GATE + the 07G VO↔on-screen perception gate NON-WAIVABLE** — 🧪 Murat (structural: contract shape, differential-effect-fired proof, dead-config detector, determinism, hermetic units) + 🎙️ Irene (content: the soft-floor/legitimate-seams/figure-atomicity safety + 07G non-regression). 🎨 Dan advisory on the CD-intent contract.
**GREEN-LIGHT:** `leg-c-min-cluster-floor-greenlight-party-record-2026-07-01.md` — **6/6 RATIFY-WITH-AMENDMENTS**; binding-time investigation DISCHARGED (EARLY-BOUND, HIGH confidence).

## Why (product value / guardrail)
Give a styleguide a way to declare a **style-identity** property that is genuinely NOT expressible declaratively: "this style reads as a multi-beat narrative walk; below N beats it collapses to a stub deck." That intent is honored by feeding Irene's Pass-1 clustering a **minimum cluster floor**, improving VO **follow-along** on multi-beat styles (dense source chunked so narration can track it — [[project_chunking_via_clustering_followalong]]). PRODUCT-VALID: the floor must earn its place in follow-along/perception terms, **never** to make a proofing run produce a particular deck ([[feedback_spoc_is_goal_not_concierge_proofing_runs]]). **The floor proposes; the non-waivable 07G gate disposes.**

## Ratified architecture (green-light — binding)
- **Contract (Dan/Winston):** `scripted` is a **sealed, registry-bound closed-vocab namespace** in the CD-owned SSOT — NOT an engine/interpreter ("a switch with one case"). v1 registry = exactly `{min_cluster_floor}`; **adding a class is a party-mode governance act.** The style declares a **declarative value** (positive int), NEVER a mechanism; one hardwired coded consumer. Mandatory **style-identity `rationale`** in provenance (bars content/pedagogy smuggling — a rationale reading like "this lesson is hard" is a review-catchable smell; only "this style reads as multi-beat" is authorable). Envelope-authored, validator-gated, **NO CD-direct-write**.
- **Leak guard (Winston/Gary):** `scripted.*` reaches Irene ONLY via a **scripted-only typed accessor with NO path to `gamma_settings[]`** — symmetric to Gary's existing metadata-quarantine (`styleguide_library.py` drops `scripted` from `RESOLVED_API_KEYS`).
- **Seam (Winston/Amelia — binding-time DISCHARGED):** floor consumed **strictly at Pass-1 clustering time, upstream of per-sub-slide narration**; it is an *input to* the same clustering the 07G gate already validates, NEVER a post-hoc reshaper of an already-narrated deck. Orchestrator reads the SSOT yaml **directly** (mirror `_gamma_settings_from_directive`) to respect the specialist/orchestrator import boundary.
- **Honoring mechanism (Amelia, binding):** enforce as a **deterministic POST-clustering, split-only, monotone** constraint — SUBDIVIDE existing clusters only; **NEVER merge/reorder/reassign canonical text order; NEVER re-parameterize the LLM clustering objective** (letting the model re-partition freely is a 07G-regression path and is out of bounds).
- **Safety (Irene, central):** the floor is a **SOFT target with source-content veto.** Pass-1 may return best-honest M < floor and emit a **styleguide-vs-content mismatch signal** — *fail loud on the mismatch, never by forcing a bad split.* **Figure↔narration atomicity is inviolable** (explicit pre-check; a figure and its explaining narration are one indivisible unit). Split only along **legitimate internal source-structure seams** (never mid-component); **re-run 07G per resulting sub-slide.**
- **Honesty banner (Dan, binding):** Leg-C proves the *scripted channel/pattern + ONE class conveyed faithfully under 07G supremacy.* It DEFERS: `scripted` as a general intent-language; per-style floor-correctness for any named style; any general reading-quality claim.

## Scout ground truth (confirmed — binding-time investigation, file:line cited)
- **Node ordering (EARLY-BOUND):** `directive-composer` (pipeline start) → `04A/05/05B irene-pass1` (05B = "Cluster Plan G1.5" clustering node) → … → `07 gary` (`state/config/pipeline-manifest.yaml:1047-1052`). Irene Pass-1 **strictly precedes** Gary; the run-level `directive.yaml` (carrying Leg-A's `styleguide` key) is materialized before the node loop (`app/marcus/orchestrator/production_runner.py:2049/2066/2124`) and threaded to every node.
- **Carrier seam EXISTS:** `_runner_payload_for_specialist(...)` (`production_runner.py:1378`) already threads directive-derived config to Gary (`gamma_settings` via `_gamma_settings_from_directive`, `:1418-1425/:1490`); returns `None` for `irene-pass1` today. Payload flows runner → dispatch_adapter merge (collision-guarded, `dispatch_adapter.py:108-120`) → `cache_prefix` → Irene `_decode_envelope_payload` (`app/specialists/irene/graph.py:1550-1558`) → `_act_pass_1` (`graph.py:1561-1587`).
- **⚠️ No existing honored floor (net-new logic):** the `floor`/`cluster` grep hit in `irene/graph.py` is a **prompt-token floor** (`:2165`, unrelated). Pass-1 clustering is **LLM-emergent** (fixed system message `:1569-1572`; `envelope_payload` as user content `:1582`; nothing enforces a minimum cluster count). Honoring ≥N is **net-new** deterministic logic — this is the leg's real work (Task 2).
- **SSOT status (INERT):** `scripted` block schema-declared but inert (`state/config/gamma-style-guides.yaml:210-217`); `min_cluster_floor` is NOT in `scripted_enum` (only `publication_target`/`descript_bound`, `:218-221`); resolver deliberately DROPS `scripted` (`app/specialists/gary/styleguide_library.py:249-263`, assert `:261-262`). A NEW scripted-only accessor is required.

## The build (Task 0/1/2)
**Task 0 — SSOT prereqs + contract.**
1. Add `min_cluster_floor` to `scripted_enum` in `state/config/gamma-style-guides.yaml`; a `scripted` entry carries `{class: min_cluster_floor, value: <positive int>, rationale: <style-identity string>, provenance{authoring_styleguide, envelope_write_stamp}}`.
2. Add a **scripted-only accessor** (e.g. `resolve_scripted(record_or_resolved, class_name)`) — returns the typed scalar; has NO path to `RESOLVED_API_KEYS`/`gamma_settings[]`.
3. Extend the hermetic validator `scripts/utilities/validate_gamma_style_guides.py`: closed-enum membership (unregistered `scripted` class → RED `gamma.scripted.unknown-class`), positive-int value, **mandatory style-identity `rationale`** present (empty/missing → RED), envelope-provenance present.

**Task 1 — threading (feasible).**
4. Add an `if specialist_id == "irene-pass1":` branch in `_runner_payload_for_specialist` (`production_runner.py:1378`) that reads `directive.yaml`'s `styleguide` name, resolves the SSOT `scripted.min_cluster_floor` (orchestrator reads the yaml directly), and returns `{"min_cluster_floor": N}` (absent styleguide / absent scripted → no key, clean). Classify explicitly as RUNNER CONTEXT (resolved run-config scalar, peer of `gamma_settings`) to satisfy the adapter collision-guard rationale (`:1400-1404`).

**Task 2 — the real work: deterministic split-only floor-honoring in `_act_pass_1`.**
5. After Pass-1 emits its LLM-honest clustering (count M), if `min_cluster_floor = N` is present and M < N: **subdivide** existing clusters ONLY at legitimate internal source-structure boundaries (component/source-point boundaries the LLM already grouped) to reach ≥N. **Never** split mid-component and **never** across a figure↔narration bond (explicit atomicity pre-check).
6. If legitimate seams cannot reach N (atomic/incompressible content): emit a **`styleguide-vs-content mismatch` fail-loud signal** — do NOT force a split. (Soft target; source content retains veto.)
7. Re-run the 07G VO↔on-screen read-path-match gate **per resulting sub-slide.**

## Acceptance criteria (RED-first; DUAL-GATE + 07G non-waivable)
**Contract / structural (Murat):**
- **AC#1 [scripted enum + accessor]** `min_cluster_floor` is a registered `scripted_enum` class; the scripted-only accessor returns the typed positive-int; an unregistered `scripted` class → RED `gamma.scripted.unknown-class`; a missing/empty `rationale` → RED. A synthetic non-int/negative value → RED.
- **AC#2 [leak guard]** `scripted.*` NEVER appears in the resolved `gamma_settings[]` surface — a test asserts the accessor and the Gary resolver are non-overlapping projections (a `min_cluster_floor` in a styleguide does not reach any Gamma payload key).
- **AC#3 [threading — early-bound]** with a directive binding a styleguide whose `scripted.min_cluster_floor = N`, `_runner_payload_for_specialist("irene-pass1", …)` returns `{"min_cluster_floor": N}`; the value arrives in Irene's `envelope_payload` at `_act_pass_1`. Absent styleguide/scripted → no key (no crash, no `None`-leak).

**Honoring / safety (Irene + Amelia):**
- **AC#4 [split-only, byte-identity — the 07G guard in unit form]** feed a known Pass-1 output (M clusters), apply floor N > M: assert (a) result has ≥N clusters AND (b) the **flattened text sequence is byte-identical to pre-floor** (subdivide-only; no merge/reorder/reassign).
- **AC#5 [figure↔narration atomicity]** a cluster containing a figure + its explaining narration, under floor pressure, is NEVER split across that bond (explicit pre-check test); the bond survives intact.
- **AC#6 [soft floor / content veto]** floor N on content whose legitimate seams top out at M < N → emit `styleguide-vs-content mismatch` fail-loud, and DO NOT over-fragment (no forced bad split). The mismatch is the failure surface, not a manufactured split.
- **AC#7 [dead-config detector — narrow, IN-SCOPE]** a `min_cluster_floor` set on a bound styleguide but never consulted at Pass-1 → **fail loud** (discriminating pair: consulted → quiet; set-but-never-read → raise). An unknown `scripted` key → RED (covered by AC#1; assert it's not silently tolerated as "dead").
- **AC#8 [learned-store-stays-empty pin]** Leg-C introduces ZERO learned entries; the Leg-B2 learned store + manifest remain empty; the only floor in play came from the documented `scripted` class.

**Live proof (no mocks, real Irene Pass-1, first-run-stands):**
- **AC#9 [differential-effect-fired pair]** same corpus + seed, floor-off (control K) vs floor-on (treatment T); arbiter = cluster count read from the **on-disk Pass-1 artifact** (never a driver's inline claim). **Anti-vacuous: `count_K < N ≤ count_T`** — the control MUST cluster below the floor. (If no real corpus clusters below N with legitimate seams to reach N → the effect is unfalsifiable → BLOCK.)
- **AC#10 [determinism pinned]** run K twice → `count_K == count_K'` before attributing the T−K delta to the floor.
- **AC#11 [07G with teeth]** the read-path-match gate returns a real PASS **verdict artifact** on the floored deck T; a **teeth-witness** (deliberately desynced floored deck — one cluster's VO shuffled against its on-screen text) is FLAGGED RED by 07G. ("07G didn't error" is rejected as vacuous.)
- **AC#12 [figure-bond-survives, live]** a real cluster containing a figure + its reading, floor applied, demonstrated intact on-disk.
- **AC#13 [adversarial floor-too-high → honest refusal — Irene, non-waivable]** a floor set above what the corpus can honestly support emits the mismatch signal and **declines to over-fragment** (no orphaned figure, no forced cut). This case is required, not just the happy path.

## T1 Readiness
- **Binding-time proof: DISCHARGED** (cited above — node ordering `pipeline-manifest.yaml:1047-1052`; carrier seam `production_runner.py:1378`; net-new honoring logic; SSOT inert). No re-investigation needed.
- **Required reads:** this spec; the Leg-C green-light record; `docs/dev-guide/dev-agent-anti-patterns.md`; `docs/dev-guide/story-cycle-efficiency.md`; `docs/dev-guide/pydantic-v2-schema-checklist.md` **iff** any Pydantic model is added; the protected invariants [[feedback_vo_onscreen_alignment_protected_invariant]] + [[feedback_source_detail_to_gamma_conveyance_protected]]; the two-walks gotcha [[project_production_runner_two_walks]] (the floor is read on the start walk before G1 — confirm both walks if any gate-pause side-effect is touched). **NOT** a Lesson-Planner MVP story (Epics 28-32) nor a migration story → the lesson-planner / migration-sandbox governance validators do **NOT** apply.
- **Corpus selection (Murat/Irene criteria — a T1 deliverable):** (a) a **control-below-N** corpus WITH legitimate internal sub-seams to reach N (the AC#9 supportive case); (b) a **figure-bond** corpus (AC#12); (c) a **floor-too-high / atomic-content** corpus with no legitimate seam to reach N (AC#13). Real course source (Part-3 candidate slugs per the scope addendum — party/operator to confirm).
- **Dev vehicle:** spawned dev agent, RED-first ([[feedback_bmad_workflows_party_and_dev_agent]]). Live proof real-API, no mocks, first-run-stands; live-key recipe (pop `sk-subst` sentinel + `load_dotenv(override=True)`) if a live LLM call is needed for Pass-1.
- **Scope fence:** ONE scripted class (`min_cluster_floor`). NO open engine/interpreter. NO 2nd scripted class. NO general dead/unreachable-config detector (narrow set-but-never-read + unknown-key only; general detector = follow-on). NO picker (Leg-D). NO live-doc audit (Leg-E). NO roster growth.

## Follow-ons to file (deferred-inventory)
- 🟡 `gamma-scripted-general-dead-config-detector` — the broad dead/unreachable-styleguide-config detector (beyond Leg-C's narrow set-but-never-read + unknown-key guards); John's surface-doubling concern → its own slot.
- 🟢 `gamma-scripted-2nd-class-admission` — the next `scripted` class re-earns its place via party governance; registry stays closed at one member until a genuinely-non-declarative 2nd instance appears with two concrete cases on the table. LEADING CANDIDATE = the operator's targeted visual-disaggregation rule ("point to specific slides for clustering treatment").

## Review Findings (3-lane bmad-code-review, 2026-07-01 — offline delta)

Lanes: Blind Hunter (diff-only) + Edge Case Hunter (diff+project) + Acceptance Auditor (diff+spec). 10 findings after dedup; 3 dismissed; 1 deferred. Offline AC#1/#2/#3/#8 CONFIRMED genuinely satisfied; no off-by-one in the subdivision math; tests non-tautological EXCEPT w.r.t. the unpinned Pass-1 output contract (see D1).

**decision-needed:**
- [ ] [Review][Decision] **D1 — Member-seam + figure/narration contract is GUESSED, unverified vs real Pass-1.** Honoring rests on `_MEMBER_KEYS` (cluster_floor.py:37) + `kind`/`type` role tags the Pass-1 prompt never requests (graph.py:1574-1587). Offline AC#4/#5/#6 pass tautologically w.r.t. the fabricated `source_points`/`kind` fixtures. If real Pass-1 lacks splittable sub-structure or role tags → every floor becomes an unreachable mismatch AND atomicity silently no-ops (figure severable → 07G regression). RESOLUTION = live Part-3 baseline pins the real contract; if absent → re-scope to EXTEND the Pass-1 output contract. GO/NO-GO for the honoring approach.
- [ ] [Review][Decision] **D2 — Soft-veto over-blocks visual disaggregation.** figure↔narration adjacency is ALWAYS bonded (cluster_floor.py:529-568) → a graph(figure)+caption(narration) slide is declared un-splittable — the exact disaggregation the operator wants; a fused single-member slide has zero seams → refused. Decision: refine Leg-C atomicity (protect figure-to-ITS-narration, allow splitting distinct figure+narration UNITS apart) now, OR defer targeted visual disaggregation to the 2nd scripted class.

**Decision resolution (operator, 2026-07-01):** BOTH D1 + D2 → **folded into the live Part-3 baseline as its GO/NO-GO first task.** Reframing: **there are no "fused slides" at Pass-1** — Pass-1 clusters SOURCE material BEFORE any slide/screen is generated (Gamma/Studio renders screens later), so nothing is pre-fused; "disaggregation" = choosing source-granularity. Design goal: **redesign/rewire so the desired disaggregations are POSSIBLE — drivable BOTH on-principles (semantic rules) AND directly-specified ("X source-unit must yield ≥Y clusters").** Operator PRE-AUTHORIZES the rewire (extend the Pass-1 output contract to expose the sub-structure + roles the split needs) if the baseline shows the current split-only-at-existing-seams approach can't achieve it. Atomicity still protects figure-to-its-own-narration; the targeted principled/direct disaggregation levers are the 2nd-class candidate. **P1's "strip floor from the LLM payload / deterministic post-hoc enforcement" stays correct** (07G-protection; the levers are deterministic, NOT LLM re-parameterization).

## D1 ADJUDICATED — party UNANIMOUS 6/6 (2026-07-01 session 03): NO-GO on the current wiring → REWIRE R3-with-R2-veto (record: `leg-c-d1-adjudication-party-record-2026-07-01.md`)

**The sharpened finding (file:line, zero live spend):** the honoring is wired into `app/specialists/irene/graph.py::_act_pass_1` behind `pass_phase=="pass-1"` — a branch PRODUCTION NEVER DISPATCHES (`pass_phase` nowhere in manifest/runner; 04A/05/05B dispatch the SEPARATE `app/specialists/irene_pass1/` module, which has ZERO floor references). **D-0 (worse than dead config):** the real path has NO hidden-key strip — `irene_pass1/_act.py:154-155` embeds the payload into the LLM prompt, so a bound floor LEAKS to the model (live re-parameterization violation). The real Pass-1 contract is flat `plan_units[]` (cluster vocabulary: head/interstitial/positions/narrative_arc/cluster_interstitial_count) — no member sub-structure, no role tags; `cluster_floor.py`'s `_MEMBER_KEYS` shape is a straight type mismatch. The 66 offline tests exercised real code in an unreachable branch.

**RATIFIED REWIRE (R3 + R2-veto):** irene_pass1 additionally emits per-unit **`source_refs[]`** (verbatim ANCHORS into source it already had in context — citations, not new structure; never-re-parameterize holds). Deterministic honoring PORTED (not rewritten — A-3) to the real path: seams derived FROM SOURCE at the anchors; splits land as interstitial plan_units in the existing cluster vocabulary; role vocabulary fixed {figure, narration, claim} (I-3); unresolvable/ambiguous/role-unverifiable anchor → REFUSE via soft-mismatch (W-1; Murat: "remove the veto and my concurrence goes with it"); atomicity + keep-dense + assessment-exempt (I-4) inviolable; 05/05B carry refs forward, refuse if absent (A-4).

**New blocking ACs:** **AC#14 (M-3/J-4, lands FIRST)** — production-dispatch-path pin: manifest specialist_id set resolved to real modules; the floor-consumer must be in the resolved set; non-waivable. **AC#15 (D-0)** — hidden-key strip in irene_pass1 keyed off the scripted NAMESPACE (not a hand-list), as a DISTINCT commit (J-3); AC#7's discriminating pair rewritten against irene_pass1 + anchors carry no scripted value/hint (byte-identical model input with/without a bound floor — D-3). **D-2** CI import-reachability assertion (a registered scripted class with an unreachable consumer fails CI). **M-2** rewired AC#4/5/6 fixtures generated FROM the live baseline's on-disk output (sha256-pinned, drift-checked); teeth-witness re-runs post-rewire.

**Honesty disclaimer (Dan, verbatim, binding):** "The scripted channel conveys CD style-identity intent (SSOT → validator → accessor → leak-guard → runner threading) faithfully to the runtime boundary; per-class consumption at the real Pass-1 surface is corrected and live-proven only for `min_cluster_floor`, and scripted-as-general-intent-language remains out of scope." **Product framing (Irene):** chunk-by-default means well-behaved Pass-1 already lands near the floor — the floor is a BACKSTOP for styleguide/content mismatches, not the mechanism.

**Sequencing (ratified):** floor-off live baseline (Part-3 corpus `tejal-c1m1-p3-opportunity`, walk to G2, capture 04A/05 verbatim, count_K, zero-floor-leak assert — Murat-5) → AC#14 pin → R3 rewire RED-first (spawned dev) → floor-on differential + K′ + AC#9–13 → dual-gate CLOSE. **Follow-ons to file:** W-2 (delete the dead `_act_pass_1` floor plumbing once the port is live-proven); the 2nd-class rider on `gamma-scripted-2nd-class-admission` (R1-style member emission re-evaluated only at TWO concrete classes).

## Review Findings — original patch list (P1–P8 applied offline; P1/P3/P4/P5 must be RE-ASSERTED on the ported real-path module per the adjudication above)

**patch — MUST-FIX:**
- [ ] [Review][Patch] **P1 — Strip `min_cluster_floor`/scripted from the LLM-visible `envelope_payload`** before the Pass-1 model call (re-parameterization of the clustering objective — VIOLATES the binding "never re-parameterize the LLM objective" amendment + contaminates the live differential's control-vs-treatment input) [graph.py:1578-1587]
- [ ] [Review][Patch] **P2 — Fork-#2 false-positive halt:** non-list `structural_outline` handling must not diverge on floor-binding; reclassify from `DeadFloorConfigError` to a content-mismatch/upstream signal (a bound floor must not turn a tolerated outline shape into a run halt) [graph.py:1602-1611]
- [ ] [Review][Patch] **P3 — Fail-loud must re-base `SpecialistDispatchError`:** `honor_min_cluster_floor` raises bare `ValueError`/`TypeError` that bypass the recoverable error-pause (reachable via the SPOC/direct-state path) [cluster_floor.py:612-614]
- [ ] [Review][Patch] **P4 — Sub-clusters must get DISTINCT identities:** `_split_cluster` shallow-copy duplicates cluster-level ids (07G per-sub-slide join collision) + aliases nested metadata + duplicates non-chosen member-key lists [cluster_floor.py:594-599]
- [ ] [Review][Patch] **P5 — Atomicity fail-safe:** if figure/narration roles can't be verified for a bound floor, do NOT split blind — fail-safe to the soft mismatch (protects the 07G VO↔on-screen invariant) [cluster_floor.py:529-541]

**patch — SHOULD-FIX:**
- [ ] [Review][Patch] **P6 — Validator flags duplicate `scripted` classes** (two `min_cluster_floor` entries silently first-win) [validate_gamma_style_guides.py `_validate_scripted`]
- [ ] [Review][Patch] **P7 — Runtime must not silently drop a present-but-malformed floor** (WARN/fail-loud instead of returning `None`) [production_runner.py:62-64; styleguide_library.py `resolve_scripted`]
- [ ] [Review][Patch] **P8 — Parity test** pinning the orchestrator local literal `_SCRIPTED_MIN_CLUSTER_FLOOR_CLASS` ∈ `SCRIPTED_ENUM_CLASSES` + value-rule parity [production_runner.py:16]

**defer:**
- [x] [Review][Defer] Non-contiguous `bond_group` honored only pairwise [cluster_floor.py:544-568] — deferred, edge-of-edge (contiguous groups correct; matters only for malformed non-contiguous groups)

**dismissed (3):** missing-styleguide silent floor drop (relies on gamma_settings validation elsewhere); `envelope_payload` NameError (false-positive — 511 tests pass); nested-metadata aliasing (subsumed by P4).
