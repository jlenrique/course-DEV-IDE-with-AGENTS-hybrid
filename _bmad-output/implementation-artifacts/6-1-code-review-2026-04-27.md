# Slab 6.1 Code Review — Production Runner Consumes Substrate

**Review date:** 2026-04-27  
**Reviewed diff:** `d5cfad8` (`feat(slab-6.1): rewire production runner to consume Slab 6.0 substrate`)  
**Review mode:** full (`bmad-code-review`: Blind Hunter + Edge Case Hunter + Acceptance Auditor)  
**Patch commit from this review:** `6ca5f43` (`fix(slab-6.1): harden runner envelope evidence review findings`)

## Verification Run During Review

- `pytest tests/unit/runtime/test_production_trial_envelope_strict.py tests/integration/marcus/test_dispatch_adapter.py tests/integration/manifest/test_compiler_real_dispatch.py tests/integration/marcus/test_production_runner_invocation.py tests/integration/marcus/test_production_runner_gate_pause_resume.py tests/integration/marcus/test_production_clone_launch_evidence_discipline.py tests/integration/runtime/test_production_runner_trace_binding.py tests/integration/marcus/test_trial_cli.py tests/composition/test_specialist_isolation_preserved.py -q --tb=short` → `54 passed`.
- `ruff check` on touched Slab 6.1 review-fix files → pass.
- Replay sweep attempt: `pytest tests/integration/replay tests/trial_replay tests/migration/test_compiled_graph_digest_stable.py -q --tb=short` timed out at 124s before producing a stable failure set. This remains covered by DFR-6.1-3 below.

## Per-Layer Findings

### Blind Hunter

| ID | Finding | Triage | Resolution |
|---|---|---|---|
| BH-1 | Production evidence could be synthesized without LangSmith evidence. | patch | Partially patched in `6ca5f43`: `production_clone_launch_evidence` now requires `LANGSMITH_API_KEY` + `LANGSMITH_PROJECT`, and live smoke skips without both. Remaining real-trace requirement is DN-1. |
| BH-2 | Missing dispatch registry entries silently become no-op production nodes. | dismiss | Current manifest has intentional orchestration nodes; AC-A test pins explicit `orchestration` handlers rather than passthrough. No missing active migrated specialist was found in the Slab 6.1 diff. |
| BH-3 | `--allow-offline-cost-report` also disables CLI gate pauses. | dismiss | Offline mode is explicitly non-production evidence and cannot close clone-launch equivalence; no evidence path flips true in offline mode. |
| BH-4 | Live smoke did not require `LANGSMITH_PROJECT`. | patch | Addressed in `6ca5f43`; live smoke now requires `OPENAI_API_KEY`, `LANGSMITH_API_KEY`, and `LANGSMITH_PROJECT`. |

### Edge Case Hunter

| ID | Finding | Triage | Resolution |
|---|---|---|---|
| EC-1 | First specialist input was never bound to the corpus because adapter replaced base `cache_state` with `{}` for empty dependency maps. | patch | Addressed in `6ca5f43`; adapter preserves base `cache_state` for first-specialist empty dependency maps and test pins corpus payload preservation. |
| EC-2 | Same-specialist manifest nodes consume duplicate IDs; later nodes skip. | patch | Addressed as ratified Path Z shape in `6ca5f43`; skip is explicit, logged, and tested. Path X/Y remains DFR-6.1-2. |
| EC-3 | Resume marks trial completed without continuing execution. | RESOLVED-AT-PATCH | Checkpoint-resume patch validates the verdict, reloads checkpoint state, applies the verdict payload, invokes downstream manifest specialists through `ProductionDispatchAdapter`, and finalizes the trial envelope after real continuation. Patch commit: `fix(slab-6.1): wire checkpoint resume to continue graph execution from gate node`. |
| EC-4 | Paused trials cannot resume across process boundaries because DecisionCard registration is in-memory. | RESOLVED-AT-PATCH | Decision-card files now persist `issued_at` with nonce/digest and resume rehydrates the in-memory registry from disk before validation. Conflict/multi-checkpoint semantics remain out of scope. |
| EC-5 | Production evidence can be true without LangSmith env. | patch | Addressed in `6ca5f43`; evidence predicate requires LangSmith env. Remaining real-trace proof is DN-1. |
| EC-6 | Runner ignores compiled graph edges and iterates manifest order. | defer | DFR-6.1-4. Current v4.2 manifest is linear; non-linear graph traversal should be a follow-on before branch/conditional production use. |
| EC-7 | `ProductionTrialEnvelope` permits impossible lifecycle states. | defer | DFR-6.1-5. Real issue, but not introduced by the 6.1 rewire patch and not needed to validate the substrate rewire. |

### Acceptance Auditor

| ID | Finding | Triage | Resolution |
|---|---|---|---|
| AA-1 | Runner does not invoke the parent compiled LangGraph with `graph.invoke`. | dismiss | Re-scoped Path A-prime runner uses `compile_run_graph` for manifest node resolution and invokes each full specialist subgraph through `ProductionDispatchAdapter`; parent graph invocation is not the accepted close shape for Slab 6.1. |
| AA-2 | `dependency_map` uses deterministic fallback rather than manifest source. | patch | Binding operator disposition accepts fallback. `6ca5f43` renamed/documented fallback and added an isolated contract test; manifest promotion remains DFR-6.1-1. |
| AA-3 | First-contribution-wins skips later same-specialist nodes. | patch | Binding Path Z disposition accepts first contribution wins. `6ca5f43` makes the skip explicit and logged with a test; Path X/Y remains DFR-6.1-2. |
| AA-4 | Per-specialist gate interrupts are recorded by adapter but not asserted by runner. | dismiss | Slab 6.0 adapter tests assert full 9-node scaffold and `last_interrupts`; Slab 6.1 runner test uses the adapter path. Production-level gate precedence remains documented in Composition Spec and no bypass of G1/G2C/G3/G4 was found. |
| AA-5 | LangSmith trace binding is synthetic, not real trace evidence. | decision_needed | DN-1. Current cost report still uses local `SimpleNamespace` trace fixtures, not live LangSmith fetch. |
| AA-6 | Evidence predicate did not require LangSmith binding. | patch | Addressed in `6ca5f43`; evidence requires LangSmith env. |
| AA-7 | Replay/pack-hash execution-path verification missing. | defer | DFR-6.1-3. Operator already ratified pre-existing pack-hash drift as deferred, not Slab 6.1 blocker; local replay command timed out during review. |

## Triaged Punch List

### Patch

| ID | Item | Commit |
|---|---|---|
| P-1 | Preserve trial corpus/base `cache_state` for first specialist dispatch with empty dependency map. | `6ca5f43` |
| P-2 | Require `ProductionTrialEnvelope.production_envelope` as full embedded `ProductionEnvelope`; regenerate JSON Schema and add schema parity assertion. | `6ca5f43` |
| P-3 | Name, document, and test deterministic `dependency_map` fallback. | `6ca5f43` |
| P-4 | Make Path Z duplicate specialist skip explicit, logged, and tested. | `6ca5f43` |
| P-5 | Require LangSmith env for `production_clone_launch_evidence=True` and for live smoke execution. | `6ca5f43` |

### Defer

| ID | Item | Deferred-inventory entry |
|---|---|---|
| DFR-6.1-1 | Promote `dependency_map` declaration into manifest or registry before Tier A. | `tier-a-0-promote-dependency-map-into-manifest` |
| DFR-6.1-2 | Replace Path Z with Path X node-scoped contribution identity or Path Y per-pass envelope when real multi-pass production need emerges. | `slab-6-1-multi-pass-envelope-path-x-or-y` |
| DFR-6.1-3 | Investigate replay-regression pack-hash drift and add execution-path assertions once baseline is refreshed. | `replay-regression-pack-hash-drift-pre-slab-6.1` |
| DFR-6.1-4 | Add graph-edge/conditional traversal execution path before non-linear production manifests are used. | `slab-6-1-runner-compiled-edge-traversal` |
| DFR-6.1-5 | Add lifecycle cross-field validators to `ProductionTrialEnvelope` (`completed_at`, `paused_gate`, `cost_report_path` state matrix). | `production-trial-envelope-lifecycle-invariants` |

### Dismiss

| ID | Item | Justification |
|---|---|---|
| DIS-1 | Parent `graph.invoke` not used. | Slab 6.1 re-scope chose Path A-prime adapter traversal; full specialist subgraphs are invoked through the adapter. |
| DIS-2 | Orchestration nodes are no-op handlers. | They are explicit structural nodes, not passthrough specialists; compiler tests pin this distinction. |
| DIS-3 | Offline mode bypasses gate pauses. | Offline mode is local harness-only and cannot set production evidence true. |

### Decision Needed

| ID | Decision | Options / tradeoff |
|---|---|---|
| DN-1 | Real LangSmith cost/trace evidence remains synthetic in runner code. | **Option A:** require `measure_trial_cost(trial_id)` live LangSmith fetch before evidence can be true; strongest N8/AC-E compliance, may need retry/backoff for LangSmith ingestion latency. **Option B:** accept current local trace fixture as Slab 6.1 close evidence while operator live smoke remains the real witness; fastest, but N8 remains PASS-WITH-GAP rather than PASS. |
| DN-2 | Resume semantics are currently verdict-validation + command persistence, not true continued execution from checkpoint. | **RESOLVED-AT-PATCH:** Option A implemented. `resume_production_trial(...)` now continues from `checkpoint.next_node_index`, marshals edit verdicts into post-gate `RunState.cache_state`, preserves pre-pause contributions, invokes post-resume specialists through the adapter, writes trace/cost artifacts, and completes the trial. Patch commit: `fix(slab-6.1): wire checkpoint resume to continue graph execution from gate node`. |

## Operator-Ratified Disposition Trace

| Disposition | Trace result | Verdict |
|---|---|---|
| Dependency map fallback accepted for Slab 6.1 | Fallback is now named `_default_dependency_map_for`, documented with Tier-A follow-on, and covered by an isolated unit assertion in `tests/integration/marcus/test_production_runner_invocation.py`. | PASS-WITH-DEFER |
| Multi-pass Path Z first contribution wins | Duplicate contribution skip now logs an explicit Path Z message and has a focused test. | PASS-WITH-DEFER |
| Replay-regression pack-hash drift deferred | Review attempted replay sweep but timed out; no new Slab 6.1-specific replay failure set was established. Deferred entry required. | PASS-WITH-DEFER |

## Substrate Inventory Checklist Trace

| N-item | Applicability to Slab 6.1 | Trace evidence | Verdict |
|---|---|---|---|
| N1 — External-provider resource ID validity | not-applicable | Slab 6.1 introduced no new provider IDs; OpenAI/LangSmith env paths are inherited. | N/A |
| N2 — Composition exercise before vote | applicable | Codex-side live smoke reported by dispatch: `tests/live/test_production_trial_smoke.py -m live -q` passed in 31.75s with `texas` + `irene` contributions; local non-live adapter suite now `54 passed`. | PASS |
| N3 — Live-API smoke before MVP close | applicable | Same Codex-side live smoke reported; operator-side gate-2 still pending. | PASS-PENDING-OPERATOR |
| N4 — Per-component isolation invariant preserved post-composition | applicable | `tests/composition/test_specialist_isolation_preserved.py` included in local review run; focused suite `54 passed`. | PASS |
| N5 — Cross-component state-flow contract | applicable | `ProductionEnvelope` full embed required; adapter preserves first input state; fallback dependency map test covers Texas/CD/default. Manifest promotion deferred. | PASS-WITH-DEFER |
| N6 — Gate boundary scope hierarchy | applicable | Adapter invokes full specialist graph and records interrupts; runner pauses on production G1/G2C/G3/G4; checkpoint-resume patch now validates an operator verdict and continues through post-gate adapter invocations to trial completion. | PASS |
| N7 — Replay regression verifies execution path | applicable | Checkpoint-resume patch added execution-continuation assertions. Replay command now returns the known pre-Slab-6.1 pack-hash drift (`33333333-3333-4333-8333-333333333333` expected `013b...`, got `19cd...`), tracked as deferred item `replay-regression-pack-hash-drift-pre-slab-6.1`. | PASS-WITH-DEFER |
| N8 — Cost machinery integration with real trace data | applicable | Current runner still passes synthetic trace roots to `measure_trial_cost`; evidence predicate now requires LangSmith env but real trace fetch is DN-1. | decision_needed |
| N9 — Operator-witnessed evidence at M-gate vote | applicable | Codex-side original smoke passed; checkpoint-resume live smoke passed with `texas` + `irene`, cost `$0.1020115`. Operator-side dual-gate gate-2 remains required before close. | PASS-PENDING-OPERATOR |
| N10 — Anti-pattern catalog read at architecture-authoring time | not-applicable | No new architecture authoring in this review; A15/A16/A17/P3 already governed the Slab 6.1 dispatch. | N/A |
| N11 — Composition mode declared alongside isolated mode | applicable | Path A-prime / Option B documented in `docs/dev-guide/composition-specification.md`; runner consumes adapter/envelope substrate. | PASS |
| N12 — Auth model verified via probe | not-applicable | No new external auth model introduced; `OPENAI_API_KEY`, `LANGSMITH_API_KEY`, and `LANGSMITH_PROJECT` paths are inherited. | N/A |

## Close Verdict

Operator decision needed on DN-1 only before `done` flip can proceed; DN-2 is RESOLVED-AT-PATCH by checkpoint-resume continuation. Substrate inventory trace now shows 0 FAIL / 1 decision_needed.
