# Story S6 — Tracy/research Scite-canonical (live Scite dispatch on every production run)

**Arc:** Canonical Production Conversation (party record §1 S6 row + J1 scope-fence + T6d + X4/W-deg degrade + M-wit). Gated after S5 (COMPLETE). **The FINAL substrate story of the arc before S7 (workbook, ⛔ operator checkpoint) + S8 (composed proof).**
**Status:** **DONE** (2026-07-07 session 18; live-proven both lanes after an honest-RED→party→D7→recover cycle). Gate chain: scout → SOP-023 CONCUR-W-F → fresh dev RED-first (safe, 0 live calls) → **SOP-024 CONCUR-W-F + 3-lane** (caught 2 real defects: the self-defeating degrade-recovery + the Basic-creds crash path) → RED-first remediation R1-R5 → re-verify → **AC-L LIVE = HONEST RED** (the dispatch was structurally UNREACHABLE — `has_research_goals`/bridge read `plan_units[].identified_gaps` but real Irene emits `collateral.research_goals[]`, never bridged; research enrichment non-functional on the canonical run) → **re-scope party 3/3** (Winston Fix-A / John RATIFY-A in-scope / Murat anti-regression) → **D7 bridge fix** (consumer-side dual-read, mechanical field-carry, J1 quality-fenced) + AC-7 (real-Irene-shape tests, smoke-harness-injection banned) → re-verify → **AC-L RECOVER WITNESS BOTH LANES PASS, fresh frozen judges first-run-stands**: Lane-a 8/8 (real Scite dispatch via the D7 `collateral.research_goals` path — 0 identified_gaps + 4 Irene-emergent research_goals, provenance-proven; **resolvable DOI `10.1038/ijo.2010.252` doi.org-200 content-inspected**, source_ref `retrieval:scite:{DOI}`, G2 passed, narration surfaced) + Lane-b 4/4 + R1 re-dispatch bonus (creds-absent degrade → restore → re-dispatch); evidence `s6-acl-recover-liveproof-20260707T101919Z` (+ the honest-RED `s6-acl-liveproof-20260707T093933Z`); spend ~$0.95 LLM total. **The operator directive "Tracy/research canonical — performed on every run" is REALIZED (J1 narrow: Scite-canonical dispatch functional on every run; Consensus deferred with skip-witness; research QUALITY fenced out).** · **Size:** S→M (D7 added) · **Tier-1** · **Filed follow-ons:** `research-quality-resolvable-doi-yield` (J1 quality), the SOP-024 NITs. · **SOP-023 CONCUR-W-F record: — both load-bearing premises code-CONFIRMED: the `ready[:2]` selector bug real [`['consensus','gamma_docs','scite']`→picks consensus+gamma_docs, excludes Scite] + D2 fix seam-located [scope `selected` to `['scite']` at `:128`], M-wit live-feasible NOW [Scite ready, token refreshed today, source_id=DOI], one-flag structure + Tier-1 + J1 fence all hold; F-2304 blast-radius undercount [+the `test_ac_d2_two_walk_parity` dormant, ⚠️ live-paid-call safety] + F-2305 [D4 seam named] applied) · **Size:** S (party-sized S) · **Gate mode:** single-gate structural (3-lane at T11) · **Branch:** `dev/workbook-2026-07-06`.
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, `-n 0`, **no `git stash`**. Do not commit — orchestrator commits after the LIVE witness.

## Party scope (binding — J1 narrow fence)
Party row (`...party-record...:23`): *"S6 = Tracy/research canonical | `MARCUS_RESEARCH_DISPATCH_LIVE` default-ON, **narrow scope: Scite-canonical; Consensus explicitly deferred with a skip-witness**; resume idempotency (no double-dispatch); degrade-when-creds-absent = recorded envelope outcome + intake-time narration; cited `research_entries` → workbook DOI section (contract already AC-D-pinned)."* **J1:** *"dispatch-ON + envelope threading; research QUALITY is OUT."* This is dispatch-canonicalization, NOT a research-quality story.

## Context (subsystem map — SOP-023 to re-verify at code)
- **Tracy vs Texas:** Tracy = Pass-2 research-intent shaper (`skills/bmad-agent-tracy/`, importable pkg `bmad_agent_tracy/`); Texas = fetcher (per-provider `RetrievalAdapter`). The Irene→Tracy→Texas bridge (`bmad_agent_tracy/scripts/irene_bridge.py`) turns plan-unit gaps → `RetrievalIntent`s → Texas → `TexasRow`s → `CitedResearchEntry` (`research_entries`).
- **⚠️ ONE flag, not two (KEY difference from S5):** research has NO `research_active()` structure gate. `run_research_wiring(...)` fires UNCONDITIONALLY at node **04.55** on BOTH walks (`production_runner.py:~2573-2592` start + `~3303-3322` continuation) — only `dispatch_live=_research_dispatch_live()` (`production_runner.py:~107-121`, `MARCUS_RESEARCH_DISPATCH_LIVE`, default-OFF via truthy-set) gates the live Scite call. So STRUCTURE is already canonical (empty-but-present `research_entries`); S6 flips the **live-dispatch** flag.
- **THE MUST-FIX (load-bearing):** `DeterministicPostureSelector.select_posture` (`research_wiring.py:~118-128`) selects `ready[:2]` of SORTED ready providers = `['consensus','gamma_docs','scite']` → picks `['consensus','gamma_docs']` — **Scite excluded, Consensus (party-deferred) included, gamma_docs is a Gamma-doc-audit provider with NO DOIs.** Flipping the flag alone dispatches to consensus+gamma_docs → zero DOI rows → M-wit UNREACHABLE.
- **Scite reachability:** `SciteProvider(RetrievalAdapter)` `status="ready"` (`scite_provider.py:213`, eagerly registered), MCP `https://api.scite.ai/mcp`, OAuth Bearer via `scite_oauth_token.py` (`secrets/scite_oauth_token.json` present, refreshed today) + Basic fallback (`.env` `SCITE_USER_NAME`/`SCITE_PASSWORD`). **Live-reachable now.**
- **DOI:** no dedicated field — `TexasRow.source_id` IS the DOI (`contracts.py:101`; `scite_provider.py:225` "Identity key: DOI"). Minted `source_ref = "retrieval:scite:{DOI}"` (`research_citation.py:88`) → resolvable `https://doi.org/{DOI}`.
- **Gates:** a G2 citation-fidelity FAIL gate already exists inline (`production_runner.py:~2593`, `gate_citation_fidelity`). No new gate.
- **Two-walk timing:** node 04.55 sits AFTER G1 — only the CONTINUATION walk reaches it on the trial path (`research_wiring.py:~23-30`). The live witness must resume past G1.
- **Current degrade (fail-soft, party-insufficient):** a missing token → silent Basic fallback → `DispatchError` caught fail-soft (`research_wiring.py:~265-275`) → row silently drops. NO intake-time narration. Party X4/W-deg require this be VISIBLE + recorded.

## Deliverables
### D1 — Flip `MARCUS_RESEARCH_DISPATCH_LIVE` default OFF→ON (kill-switch model; Tier-1)
`_research_dispatch_live()` (`production_runner.py:~107-121`) → default-ON with an EXPLICIT falsy kill-switch (`{"0","false","no","off"}` → OFF), mirroring S5's `g0_enrichment_active` F-1802 contract (NOT `not in truthy_set`). Unset/truthy → live dispatch ON. **Tier ruling (mirror S5 W3): expected Tier-1** — neither `research_wiring.py` nor `production_runner.py` is a `block_mode_trigger_paths` member; NO `pipeline-manifest.yaml` edit; STOP-and-report if a manifest edit becomes unavoidable.

### D2 — Scite-canonical provider selection (THE MUST-FIX) + Consensus skip-witness
Scope the research dispatch to **Scite** for the literature-shape retrieval — EXCLUDE `consensus` (party-deferred) and `gamma_docs` (Gamma-doc-audit, no DOIs). Land it at the selection seam (`DeterministicPostureSelector.select_posture` / the research_wiring dispatch) — the dev chooses the cleanest mechanism (a literature-provider filter, or pinning scite for the research posture), but the OUTCOME is pinned: **a live research dispatch reaches SciteProvider, never consensus/gamma_docs.** Add a hard `pytest.skip(reason=...)` witness NAMING `consensus-provider-live-enablement` (the deferred-inventory entry) so Consensus's deferral is explicit + traceable, not silent. Keep the selector deterministic (no ordering flakiness).

### D3 — Resume idempotency (no double-dispatch)
A resumed/recovered run whose 04.55 already produced `research_entries` must NOT re-dispatch to Scite (no double paid call, no duplicate rows) — idempotent re-entry (mirror the S3 walk-pin "never re-audit" discipline). Witness: a double-resume asserts the Scite dispatch count stays at its first-pass value + the entries are byte-stable.

### D4 — Degrade-when-creds-absent contract (net-new; X4/W-deg — VISIBLE, recorded, never silent)
When live dispatch is ON (default) but Scite creds are absent/token-unrefreshable: instead of today's SILENT fail-soft drop (the `DispatchError`-catch → `continue` at `research_wiring.py:~265-275`), record a VISIBLE envelope outcome marker (e.g. `"research enrichment skipped — credentials unavailable"`), carry recorded-empty provenance (the `research_entries` section is present + explicitly-empty with the reason, NOT a silent drop), and surface a headed-relogin offer. **Never halt the lesson** (W-deg: degrade-with-recorded-empty). **Seam (SOP-023 F-2305 — name it explicitly):** a **creds precondition at `run_research_wiring` ENTRY** — when `dispatch_live=True`, check creds (`load_bearer_token()` + `SCITE_AUTH_ENV_VARS` presence) BEFORE `_dispatch_intents_to_texas`; on absent → short-circuit to present+explicitly-empty `research_entries` with the reason marker + relogin offer. RED-test at that seam. ("Intake" = this research-node-entry precondition, not the upstream SPOC conversation.)

### D5 — SPOC post-04.55 research narration (T6d, review-only, non-blocking)
The SPOC narrates the dispatched/cited research result AFTER node 04.55 (review-only display: "N cited sources found" / the degrade marker) — non-blocking, no new gate. Narration/HIL-display only (mirror S5 D3 discipline).

### D7 — research-intent→dispatch bridge (Fix A; party-ratified 2026-07-07 from the AC-L honest-RED; makes the dispatch REACHABLE)
**Defect (AC-L live witness, code-verified):** the §04.55 dispatch is UNREACHABLE on any real run — `has_research_goals()` (`research_wiring.py:277-279`) + `IreneTracyBridge.process_plan_locked` (`irene_bridge.py:27`) gate on `plan_units[].identified_gaps` (Gagne `IdentifiedGap`), but real Irene-Pass-1 emits research intent as **`collateral.research_goals[]`** (`irene_pass1/_act.py:290-314`) and never fills `identified_gaps` (only the smoke harness / manual G1A does). So research enrichment is structurally non-functional on the canonical run — a hollow "canonical" flip. **Fix A (consumer-side dual-read, Winston):** make `has_research_goals()` AND the Tracy bridge ALSO read `collateral.research_goals[]`, mapping each `{goal_id, pedagogical_intent, binds_to_objective_id}` → a `RetrievalIntent`. **⚠️ MECHANICAL FIELD-CARRY ONLY (binding J1 fence):** carry the pedagogical_intent SEED (never a query), the `binds_to_objective_id` target, and `goal_id` provenance — then STOP. The instant the mapping decides research KIND / relevance / gap_type, it crosses into the J1-fenced QUALITY zone — let the existing downstream posture-selection + Texas fetch decide quality UNCHANGED. Additive only: do NOT touch the Irene producer, the Gagne `IdentifiedGap` model, or `plan_units[]` (Fix B — synthesizing gaps — was REJECTED: provenance loss + forced quality decisions). If a corpus carries BOTH `identified_gaps` (real G1A-curated) AND `collateral.research_goals`, both are honored (union), provenance-distinct.

### D6 — cited `research_entries` → workbook DOI section (contract already AC-D-pinned; witness)
The contract threading cited `research_entries` through the envelope to the workbook's DOI/citation section is ALREADY AC-D-pinned (Concierge era). S6 does not rebuild it — it WITNESSES that a live-cited entry (from the AC-L run) flows to the workbook DOI section (content-inspected). If the thread is broken, STOP + report (that's a regression, out of S6's flip scope).

## Acceptance criteria
- **AC-1 (the flip):** `_research_dispatch_live()` returns True on unset/truthy; False only on explicit falsy `{0,false,no,off}` (parametrized truth-table). Both hook sites (start + continuation) read the toggle (the `src.count("dispatch_live=_research_dispatch_live()") == 2` static pin stays green).
- **AC-2 (Scite-canonical selection — THE teeth):** a live research dispatch selects **Scite** and NOT consensus/gamma_docs (unit/integration witness on the selection seam); the Consensus `pytest.skip` witness names `consensus-provider-live-enablement`. RED-first: pre-fix, the selector picks consensus+gamma_docs (the current `ready[:2]` bug).
- **AC-3 (resume idempotency):** double-resume of a research-completed 04.55 does NOT re-dispatch (dispatch count stable, entries byte-stable).
- **AC-4 (creds-absent degrade — X4/W-deg):** creds/token absent ⇒ intake-time detection + a VISIBLE recorded envelope marker ("research enrichment skipped — credentials unavailable") + recorded-empty provenance (present, explicitly-empty, reason-carrying) + relogin offer; the walk PROCEEDS (never halts). Witnessed with a forced-absent-creds fixture.
- **AC-5 (test migration + no new reds — FULL-TREE SWEEP, the S5 lesson):** the "~3 files + 1 dormant" estimate is a FLOOR, not the enumeration. **T1: run the S5-style full-tree causation-isolation sweep** (`MARCUS_RESEARCH_DISPATCH_LIVE=1` vs `=0` across ALL of `tests/`, isolated-re-run filter) to enumerate EVERY test that walks `run_production_trial`+resume through node 04.55 with a research gap and an unpinned flag. Known migration set so far: (a) `test_m1_toggle_off_by_default_threads_false_to_both_walks` → kill-switch/escape-hatch role (explicit `setenv("0")`); **(b) SOP-023 F-2304 — `test_ac_d2_two_walk_parity_fires_on_real_continuation` (`test_braid_s3_research_wiring.py:237`): runs a REAL walk through 04.55 with an in-scope gap, does NOT pin the flag, and stubs only `ProductionDispatchAdapter` NOT the retrieval/Scite adapter.** ⚠️ **SAFETY: post-flip (default-ON) + the live `.env` Scite creds → this would fire a REAL/PAID Scite call (or a nondeterministic network hang) inside the offline integration suite.** Migrate it (pin `setenv("0")` OR install a fake retrieval adapter) — a default-ON integration test MUST NOT reach live Scite. The toggle-ON + `src.count(...)==2` static pins + `test_m2_fail_mode` (pins the flag False) + `test_ac_d3_intent_names_only_registered_providers` (subset assert — survives the scite-scoping) stay green unchanged. `tests/ -n 0` green except the KNOWN pre-existing baseline reds (causation-isolate any new ON-only red; the flip adds zero). **Every dormant-real-walk test the sweep finds gets a migration row.**
- **AC-6 (Tier-1 + narration):** `git diff -- state/config/pipeline-manifest.yaml` EMPTY; D5 SPOC post-04.55 narration present (review-only); no VO↔on-screen / source-detail regression.
- **AC-7 (D7 anti-regression — Murat, the offline suite was a FALSE witness):** `test_real_irene_pass1_shape_reaches_research_bridge` — build an envelope with `collateral.research_goals = [≥1 goal]` AND `plan_units[].identified_gaps == []` **asserted explicitly empty** (so the test is meaningless if a future fixture back-fills gaps) → assert `has_research_goals()` is True AND the §04.55 dispatch fires AND the dispatched intent's provenance traces to `collateral.research_goals` (NOT identified_gaps). Negative twin `test_empty_research_goals_does_not_dispatch` (zero goals → no dispatch). **BAN the smoke-harness `identified_gaps` injection from this test module** so it can never again validate a path the real producer doesn't take. (RED-first: pre-D7 the real-Irene-shape envelope yields `has_research_goals()==False`.)
- **AC-L (LIVE witness — orchestrator, PASS required; the M-wit — RECOVER-the-witness after D7, party-ratified as error-pause→recover NOT retry-to-green; frozen judges never ran against a fixed dispatch):** a REAL production walk on a literature-rich corpus (units with research-enrichment goals) with `MARCUS_RESEARCH_DISPATCH_LIVE` on (default post-flip) + Scite creds live: resume past G1 → node 04.55 → a REAL Scite dispatch → **≥1 cited `TexasRow` with a resolvable DOI** (`source_id` a real DOI, `https://doi.org/{DOI}` resolves) minted into `research_entries` → G2 citation-fidelity passes → the cited entry flows to the workbook DOI section (D6). **Content-inspected, NOT row-counted** (read the actual cited row + its DOI). First-run-stands; frozen judge; foreground/watchdog; cleanup APPEND (F-701). ALSO witness the degrade path once (forced-absent-creds → visible marker, $0, walk proceeds) — the two-lane honesty (real-cite // honest-degrade). Corpus: orchestrator names a literature-rich one (party §3; `tejal-apc-c1-m1-p2-trends` yielded 5 real research_goals in the first witness). **Recover-witness PRECONDITION (Murat): Irene-Pass-1 must emit ≥1 `collateral.research_goals` on the fresh live run BEFORE judging dispatch** (LLM non-deterministic — a zero-goal run is an INVALID witness → re-run the corpus/Irene, NOT a fail). **Quality guardrail (John/J1): if the recovered witness DISPATCHES (goals reach Scite) but yields no resolvable DOI, that is a research-QUALITY finding — OUT of S6, harvest to `research-quality-resolvable-doi-yield` postmortem, revisit separately; it does NOT block the S6 dispatch-canonical close.**

## RED-first plan
0. **T1 (do FIRST — full-tree sweep, F-2304):** `MARCUS_RESEARCH_DISPATCH_LIVE=1` vs `=0` across ALL `tests/`, causation-isolate, enumerate EVERY dormant real-walk-through-04.55 test (⚠️ FIRST migrate/pin the two known [`test_m1_toggle_off…`, `test_ac_d2_two_walk_parity…`] so the sweep itself does not fire a live paid Scite call). 1. AC-1 flag truth-table (RED: unset currently → OFF). 2. AC-2 selection teeth (RED: current `ready[:2]` picks consensus+gamma_docs, not scite → scope `selected` to `['scite']` at `research_wiring.py:128`). 3. AC-3 double-resume idempotency. 4. AC-4 creds-absent degrade at the `run_research_wiring`-entry seam (RED: today silent-drops). 5. migrate every dormant test the sweep found + full-suite causation-isolation (zero new reds). 6. AC-L live legs (orchestrator).

## Out of scope (J1 fence)
Research QUALITY (intent-shaping accuracy, ranking) · **Consensus provider live-enablement** (party-deferred; `consensus-provider-live-enablement`, S6 ships the skip-witness) · gamma_docs in the research literature path · the flag RETIREMENT (`research-dispatch-flag-retirement`, triggers at S8 green) · any `pipeline-manifest.yaml` edit (Tier-1) · a new research gate (04.55 + G2-fidelity already exist) · S7 workbook / S8 composed proof · rebuilding the research_entries→workbook contract (AC-D-pinned; S6 only witnesses it).

## T11 gates
SOP-023 monitor pre-dispatch poll (verify the subsystem map — esp. the one-flag structure + the `ready[:2]` selector bug + Scite reachability; confirm the D2 Scite-canonical seam is right + the D4 degrade contract is well-defined + Tier-1 holds) → FRESH dev RED-first → SOP-024 + 3-lane `bmad-code-review` (real surface: the flag flip, the selector scoping, the degrade contract, the idempotency) → RED-first remediation → orchestrator re-verify → **AC-L LIVE (real resolvable-DOI cited row + degrade lane) PASS** → commit (strays excluded) → SOP close poll → **fully-spawned bmad-party-mode concurrence (the arc's final-substrate-story close + the goal's DONE-SIGNAL for S6)**. Story flips done ONLY after the live AC-L PASS.
