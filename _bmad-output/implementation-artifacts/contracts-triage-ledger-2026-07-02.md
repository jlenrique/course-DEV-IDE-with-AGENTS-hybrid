# Contracts-Tree Triage Ledger — 2026-07-02 (finding d, D-D1)

**Charge:** party record `carried-findings-remediation-greenlight-party-record-2026-07-02.md` §D-D1 — READ-ONLY adjudication of the 20 pre-existing `tests/contracts` failures at HEAD, then apply only adjudicated L/T/O rows within the 90-minute box.
**Branch:** `dev/carried-findings-enum-refresh-2026-07-02` (off consolidated master `c0ce283b`).
**Reproduction at HEAD (this session, before any edit):** `pytest tests/contracts` → **20 failed / 293 passed / 1 skipped** (matches the party evidence pack).
**Categories (binding):** L legitimate-drift→repin (named commit + governing story/party artifact required) · R regression→keep test, production fix exits batch · T test-defect→rewrite with property-preservation justification · O obsolete→delete · E deferred-unadjudicated.
**Prior-art evidence:** `_bmad-output/implementation-artifacts/ambient-contract-debt-triage-2026-06-23.md` (diagnosed 14 of these 20; never applied; treated as corroborating evidence, NOT as governing authority — each row re-adjudicated here). The 6 rows it did not cover: modality_registry ×4 (drift landed 2026-06-25, after that triage), 33-1a 07W (landed 2026-06-26), specialist_error_taxonomy (classes landed 2026-06-27+).

**Standing observation (Winston cross-finding pattern, confirmed here):** the dominant root cause is pin/allowlist edits missed inside the large mechanical sweep commits of the s2-marcus-collapse (`accd226d` updated each test's *walk root / import* but not its *path-literal / allowlist frozenset* — verified per-file diffs below). Same family as finding (a)'s `195be7c9` pin-flip. Secondary root cause: governed stories that widened substrate (Braid S2 workbook, Braid S4 overlay, 07W brick, Slab-1 stub) without lockstep-updating the contract pins that reference it.

**Note on root `marcus/` tree:** a stale `marcus/{intake,lesson_plan,orchestrator,__pycache__}` directory exists at repo root but is **fully untracked** (`git ls-files marcus/` → 0 entries) and contains no schema files. Local residue only; flagged for cleanup hygiene, no contract semantics.

---

## Per-test ledger

### Tier-1 family A — fit_report (6 tests, 2 files; one root cause, per-test verdicts)

Family analysis: Story 29-1 pinned the fit-report schema + canonical-caller invariant against the old repo-root `marcus/` tree. The s2-marcus-collapse (pre-Trial-3 cleanup arc S2 dispatch 2026-05-07; deferred-inventory entry `migration-tech-debt-app-marcus-stub-disposition`, direction-flip precedent codified in CLAUDE.md §Deferred-inventory governance) moved the tree to `app/marcus/` (`343220f6` copy → `accd226d` sweep → `195be7c9` legacy delete). `accd226d` ("test sweep + path-string updates, 110 + 29 files") updated **only the import line** in `test_fit_report_v1_schema_stable.py` and **only the rglob root** in `test_fit_report_canonical_caller.py` (verified via `git show accd226d -- <file>`; each diff = exactly 1 line), leaving `SCHEMA_PATH` and `ALLOWED_PRODUCTION_IMPORT_PREFIXES` pointing at the deleted root tree. First-bad commit for the RED: **`195be7c9`** (Phase-5 legacy delete removed the last file the stale path resolved to). Schema file confirmed present and current at `app/marcus/lesson_plan/schema/fit_report.v1.schema.json`.

| # | Test | Failure summary | First-bad evidence | Cat | Citations | Action |
|---|---|---|---|---|---|---|
| 1 | `test_fit_report_v1_schema_stable.py::test_fit_report_schema_file_exists` | `SCHEMA_PATH` (`parents[2]/"marcus"/…`) does not exist | `accd226d` half-sweep + `195be7c9` legacy delete; schema lives at `app/marcus/lesson_plan/schema/fit_report.v1.schema.json` | **L** | s2-marcus-collapse commits `343220f6`/`accd226d`/`195be7c9`; CLAUDE.md `migration-tech-debt-app-marcus-stub-disposition` S2-dispatch precedent; ambient-triage row 1 | Repin `SCHEMA_PATH` → `app/marcus/...` |
| 2 | `…::test_fit_report_schema_title_and_version` | FileNotFoundError on same stale path | same | **L** | same; ambient-triage row 2 | same repin (shared constant) |
| 3 | `…::test_fit_report_property_coverage_matches_pydantic` | FileNotFoundError on same stale path | same | **L** | same; ambient-triage row 3 | same repin |
| 4 | `…::test_fit_diagnosis_property_coverage_matches_pydantic` | FileNotFoundError on same stale path | same | **L** | same; ambient-triage row 4 | same repin |
| 5 | `…::test_fit_diagnosis_fitness_enum_matches` | FileNotFoundError on same stale path | same | **L** | same; ambient-triage row 5 | same repin |
| 6 | `test_fit_report_canonical_caller.py::test_emit_fit_report_not_imported_outside_canonical_callers` | Walk root is `app/marcus` but `ALLOWED_PRODUCTION_IMPORT_PREFIXES` are root-`marcus/` → allow-set never matches; all 8 "offenders" are the previously-sanctioned surfaces themselves (defining module `fit_report.py`, package re-export `__init__.py`, an orchestrator *comment* in `write_api.py:151`). **No genuine new importer exists** — invariant substance intact. | `accd226d` (updated rglob root only; allowlist frozenset untouched) | **L** | same collapse provenance; ambient-triage row 7; offender list inspected this session | Repin the 3 allowlist prefixes → `app/marcus/...` (teeth preserved: any specialist import still flagged) |

### Tier-1 family B — modality_registry (4 tests; one root cause, per-test verdicts)

Family analysis: Braid S2 (`2cac27b9`, 2026-06-25, party-CLOSED per braid arc; spec `spec-braid-s2-workbook-producer.md`) shipped the `workbook` modality as an **explicitly governed AC-C.4 widening**: `SCHEMA_CHANGELOG.md` §"Modality Registry v1.1 — 2026-06-25 — Braid S2 Workbook Producer" documents the schema-version bump 1.0→1.1, the additive `ModalityRef` value, and the `ready` producer entry, and states the widening protocol ("bump + changelog entry + registry edit + tests — NOT a free edit"). The contracts-tree pins were the missed lockstep piece. This is exactly the L pattern: named commit + governing changelog/spec/party artifacts; production substrate correct.

| # | Test | Failure summary | First-bad evidence | Cat | Citations | Action |
|---|---|---|---|---|---|---|
| 7 | `test_modality_registry_stable.py::test_schema_version_is_1_0` | `SCHEMA_VERSION` is `"1.1"`, pin says `"1.0"` | `2cac27b9` (Braid S2) | **L** | SCHEMA_CHANGELOG §Modality Registry v1.1; `spec-braid-s2-workbook-producer.md`; memory: braid S2 party-CLOSED | Repin to `"1.1"` (rename test to `test_schema_version_is_1_1`) |
| 8 | `…::test_modality_ref_literal_closed_set` | `workbook` extra vs expected closed set | `2cac27b9` | **L** | same | Add `"workbook"` to `EXPECTED_MODALITY_REFS` |
| 9 | `…::test_modality_registry_key_set_matches_expected` | registry keys include `workbook` | `2cac27b9` | **L** | same | same constant repin |
| 10 | `…::test_modality_registry_status_split_matches_expected` | `workbook` is `ready`, not in expected-ready set | `2cac27b9` (registry entry `status="ready"` documented in changelog) | **L** | same | Add `"workbook"` to `EXPECTED_READY_MODALITIES` |

### Tier-2 (heightened suspicion — adjudicated individually)

| # | Test | Failure summary | First-bad evidence | Cat | Citations | Action |
|---|---|---|---|---|---|---|
| 11 | `test_marcus_single_writer_routing.py::test_intake_and_orchestrator_packages_route_through_write_api` | 3 violations: `write_api.py:161` + `dispatch.py:142` (both allowlisted at OLD root-`marcus/` paths — pure stale prefix) **plus `app/marcus/intake/__init__.py:104`, which is NOT allowlisted under either scheme** | Prefix staleness: `accd226d` (updated scan roots only). Intake call: S2-merge 2026-05-07 (`343220f6` era) merged a legacy flat-module `extract_pre_packet` that calls `write_api.append_event(state, event)` — a **state-machine test helper that appends to `state.events` and provably never touches `LessonPlanLog`** (write_api.py:164-181 documents "NOT a production write surface for Lesson Plan log events") | **T** | write_api.py:98-161 (real single-writer surface intact), write_api.py:170-181 (helper), intake/__init__.py:49-57+72-105 read this session; ambient-triage row 9 (SHALLOW — it missed intake:104; corrected here) | Rewrite: repin allowlist prefixes → `app/marcus/...` AND exempt bare-name calls that resolve (by import) to the write_api state-helper. **Property preserved:** attribute-form `<log>.append_event(...)` calls and bare `append_event` imported from anywhere else remain flagged everywhere outside the allowlist; the exempted helper by construction cannot write the Lesson Plan log. **Follow-on filed (see footer):** rename the confusingly-named S2-merge helper so the scanner needs no exemption. |
| 12 | `test_30_2b_dispatch_monopoly.py::test_dispatch_is_sole_orchestrator_caller` | Sole "offender" `dispatch.py:90` **is the sanctioned caller itself** (`dispatch_intake_pre_packet` → `emit_pre_packet_snapshot`), allowlisted at the OLD root path | `accd226d` (updated walk root; `_ALLOWED_CALLERS` frozenset untouched) | **L** | collapse provenance as rows 1–6; dispatch.py:90-94 read this session; ambient-triage row 8 | Repin `_ALLOWED_CALLERS` → `app/marcus/orchestrator/{dispatch,write_api}.py`. Teeth intact: any other orchestrator module calling emit still flagged. **Winston suspicion answered: no real violation — the only flagged caller is the sanctioned seam.** |
| 13 | `test_33_2_state_config_disjoint_keys.py::test_pipeline_manifest_keys_do_not_shadow_other_state_configs` | Two colliders: (i) `pipeline-manifest-substrate-stub.yaml` shares 7 manifest keys; (ii) `capability-overlay.yaml` shares `generator_ref` | (i) `eb2adb01` — **Slab 1 Story 1.6** deliverable; file header self-identifies as the deliberate single-node smoke stub for `app.smoke_test.run_smoke()` / 1.1d transport-parity; consumed strictly by explicit filename (app/smoke_test.py, tests/unit/manifest). (ii) `cce6df19` — **Braid S4**; `spec-braid-s4-marcus-capability-overlay.md` line 60 **explicitly specifies** `generator_ref: scripts/utilities/generate_capability_overlay.py` in the overlay shape (reviewed spec, not self-justifying output) | **L** | eb2adb01 + stub header; cce6df19 + spec-braid-s4 (generator_ref in ratified shape); no `state/config/*.yaml` glob-loader exists (test itself is the only globber); ambient-triage row 11 (stub only — overlay post-dates it) | Repin with **narrow** exemptions: skip `pipeline-manifest-substrate-stub.yaml` by name (it IS a second manifest by design) + per-file-per-key allowance `capability-overlay.yaml:{generator_ref}`. All other files/keys keep full teeth. **Winston suspicion answered: both collisions are governed artifacts, not accidental shadowing; residual honesty — both stories missed lockstep-amending this contract (same cross-finding pattern).** |
| 14 | `test_transform_registry_lockstep.py::test_every_format_covered_or_exempted` | 2 registry sections unclassified: "Box (fetch layer)" and "Image (intake via sensory-bridges)" | transform-registry.md gained both sections with REAL wiring in `run_wrangler.py` (`wrangle_box_file` @765; image dispatch @768 — per ambient-triage row 12 verification); the test's lockstep constants were not updated | **L** | ambient-triage row 12 + Batch-3 remediation plan (add REGISTRY_METHOD_TO_EXTRACTOR entry for Image's `wrangle_local_image`; Box fetch-layer fits the documented exemption rule "remote fetch, not a wrangle_local_* extractor" like the existing `html \ url` row); registry sections + wiring confirmed | Apply if box time remains (moderate, regex-against-source contract): Image → `REGISTRY_METHOD_TO_EXTRACTOR` entry; Box → `LOCKSTEP_EXEMPTIONS` rationale entry. Flag to Acceptance-Auditor + Texas-owner sanity check per prior triage's care note. |

### Tier-3

| # | Test | Failure summary | First-bad evidence | Cat | Citations | Action |
|---|---|---|---|---|---|---|
| 15 | `test_provider_directory_roster_placeholders.py::test_roster_placeholder_present_with_expected_status[consensus-retrieval-ratified]` | consensus status is `ready`, pin says `ratified` | `835e6503` (2026-04-24 upstream-absorption at severance) brought `consensus_provider.py` — **Story 27-2.5** ConsensusProvider adapter (module docstring names the story). Shipped adapter supersedes placeholder → status flips, the exact pattern the test itself documents for scite (lines 18-23: "Every future retrieval-shape story will flip its row here the same way") | **L** | `835e6503`; consensus_provider.py:1-3 (Story 27-2.5); test's own supersession contract doc; ambient-triage row 13 | Repin parametrize row → `("consensus", "retrieval", "ready")` |
| 16 | `test_quinn_r_gate_no_log_boundary.py::test_quinn_r_gate_does_not_call_lesson_plan_log_write_surface` | FileNotFoundError: CWD-relative `Path("marcus/lesson_plan/quinn_r_gate.py")`; file lives at `app/marcus/lesson_plan/quinn_r_gate.py` | s2-marcus-collapse (as rows 1–6); this file was never touched by the `accd226d` sweep at all | **L** | collapse provenance; ambient-triage row 10 | Repin path (anchored to repo root via `__file__`, not CWD) AND update the forbidden-token `"from marcus.lesson_plan.log import"` → `"from app.marcus.lesson_plan.log import"` so the token keeps teeth against the current import namespace |
| 17 | `test_33_1a_verbatim_extraction.py::test_existing_sections_match_source_pack_byte_identical` | "Template prose drift for 07W" — 07W is a net-new `-gen` section with no frozen v4.2 source-pack origin, never enrolled in the closed exclusion allowlist | `b914bb9e` (2026-06-26, "07W in-graph workbook brick — 3-bundle goal DONE") added the 07W manifest node + `07W-companion-workbook-producer.md.j2` template + story spec `spec-07w-workbook-producer-brick.md`, but did not enroll 07W in `_VERBATIM_EXCLUDED_SECTIONS` (the 07D.5 story `d96ff3cd` DID enroll its section — the established pattern) | **L** | `b914bb9e` + `spec-07w-workbook-producer-brick.md` (in-commit story spec); test's own party-ratified enrollment mechanism (P2-2 T11 Option A, 2026-06-20, cited in-file); 07D.5 precedent | Enroll `"07W"` as `net-new-prose` / Check-9-governed; update the closed-set pin in `test_verbatim_exclusions_are_closed_and_check9_governed` (same file, currently green — explicit auditor flag). **Observation for auditor:** `pack_version` stayed `"v4.2"` at `b914bb9e` despite adding a pipeline step (Tier-2 pack change per pipeline-manifest regime) — governance question outside this batch, noted honestly. |
| 18 | `test_30_1_zero_test_edits.py::test_no_preexisting_test_files_modified_in_30_1` | Zero-test-edit invariant vs baseline `4911fc4`: ~965 test files touched since, across every subsequent epic + the app/ migration | Baseline lapsed cumulatively; the test's own docstring mandates per-story rollforward that was never executed (ambient-triage row 14 concurs) | **E** | ambient-triage row 14 + Batch-4 ("needs governance/owner decision: rollforward vs retire — do NOT silently expand the allowlist") | **DEFERRED-UNADJUDICATED (honest defer):** disposition requires the Marcus-duality lane owner / governance to choose rollforward vs retire — precisely the "gate-mode / owner decision" class this batch must escalate, not decide unilaterally. |
| 19 | `test_blueprint_producer_registry_contract.py::test_no_schema_bump_and_public_surface_stays_expected` | `modality_registry.SCHEMA_VERSION` `"1.1"` vs pinned `"1.0"` (31-4-era "no bump yet" statement) | Same governed Braid-S2 v1.1 bump as rows 7–10 (`2cac27b9`) | **L** | SCHEMA_CHANGELOG §Modality Registry v1.1; `spec-braid-s2-workbook-producer.md` | Repin modality expectation to `"1.1"`; `produced_asset` stays pinned `"1.0"` and the `__all__` surface pin stays byte-identical (verified at apply) |
| 20 | `test_specialist_error_taxonomy.py::test_every_tagged_error_class_is_dispatch_family` | Two tagged (tag-kwarg-convention) error classes are NOT `SpecialistDispatchError` subclasses: `app.specialists._shared.voice_provider_text.VoiceProviderTextError` and `app.specialists.gary.styleguide_library.StyleguideError` | Born non-compliant: `077d68e2` (enhanced-VO v3 tag-only compiler, 2026-06-29) and `bd0003d3` (gamma-styleguide Leg-A library spine, 2026-06-30). The ratchet (PIN-AUD-3T, audio-arc 2026-06-12) is explicit: shrink-only exclusion seed list; "New tagged classes get NO exclusion — they must be born dispatch-family" (kills the cycle-5 walk-dead-instead-of-error-pause crash class) | **R** | test docstring + shrink-only EXCLUSIONS policy read this session; both classes on LIVE paths (voice provider-text = narration; styleguide = Gamma leg) | **KEEP TEST RED. No production edit in this batch.** Proposed follow-on story (footer): re-base both classes onto `SpecialistDispatchError` + per-class catch-site grep per the ratchet's documented re-base protocol. This is the genuine defect the contracts tree caught — a walk-killing bare-exception class on two live production paths. |

---

## Category counts (Phase-1 adjudication)

| Category | Count | Rows |
|---|---|---|
| L (legitimate-drift → repin) | 17 | 1–10, 12–17, 19 |
| T (test-defect → rewrite) | 1 | 11 |
| R (regression → keep red, follow-on) | 1 | 20 |
| O (obsolete → delete) | 0 | — |
| E (deferred-unadjudicated) | 1 | 18 |

## Phase-2 application record

**RED-at-old-pin:** captured for every applied row in the Phase-1 reproductions above (all 20 red at HEAD before any edit; per-family assertion output recorded this session).

**Applied (17 rows → GREEN, verified individually then full-suite):**

| Rows | File edited | Change |
|---|---|---|
| 1–5 | `tests/contracts/test_fit_report_v1_schema_stable.py` | `SCHEMA_PATH` repinned `marcus/` → `app/marcus/` |
| 6 | `tests/contracts/test_fit_report_canonical_caller.py` | 3 allowlist prefixes repinned → `app/marcus/...` |
| 7–10 | `tests/contracts/test_modality_registry_stable.py` | `workbook` added to `EXPECTED_MODALITY_REFS` + `EXPECTED_READY_MODALITIES`; version pin → `"1.1"` (test renamed `test_schema_version_is_1_1`); provenance cited in-file |
| 11 (T) | `tests/contracts/test_marcus_single_writer_routing.py` | Allowlist repinned → `app/marcus/...`; bare-name calls resolving (by sole-import-source) to the write_api state-machine helper exempted with full in-file justification; attribute-form log writes + bare imports from any other module keep full teeth |
| 12 | `tests/contracts/test_30_2b_dispatch_monopoly.py` | `_ALLOWED_CALLERS` repinned → `app/marcus/orchestrator/...` |
| 13 | `tests/contracts/test_33_2_state_config_disjoint_keys.py` | Narrow exemptions: `pipeline-manifest-substrate-stub.yaml` skipped by name (deliberate second manifest, Story 1.6) + per-file-per-key allowance `capability-overlay.yaml:{generator_ref}` (spec-braid-s4 line 60); synthetic-collision detector test untouched and still green |
| 15 | `tests/contracts/test_provider_directory_roster_placeholders.py` | consensus row `ratified` → `ready` (Story 27-2.5 supersession) |
| 16 | `tests/contracts/test_quinn_r_gate_no_log_boundary.py` | Path repinned + repo-root-anchored; forbidden import token updated to `app.marcus` namespace (teeth restored) |
| 17 | `tests/contracts/test_33_1a_verbatim_extraction.py` | `07W` enrolled as `net-new-prose` exclusion; closed-set pin updated to 5 ids (companion green test edited — flagged for auditor) |
| 19 | `tests/contracts/test_blueprint_producer_registry_contract.py` | modality version pin → `"1.1"`; produced_asset pin and `__all__` surface pin unchanged and green |

**Adjudicated but NOT applied (application deferred at box expiry):**

- Row 14 (`test_transform_registry_lockstep`) — verdict **L stands**; the correct fix (REGISTRY_METHOD_TO_EXTRACTOR entry for Image + Box fetch-layer exemption, dispatch-presence regexes against `_fetch_source` source text) is a source-inspection contract the prior triage explicitly gated on a **Texas-owner sanity check of the regex shapes**; rushing it at box expiry risked a wrong-regex quiet-pass. Remains RED with fix plan on record.

**Intentionally left RED:** row 20 (R — the taxonomy ratchet caught a real defect; production fix exits this batch), row 18 (E — owner/governance decision), row 14 (above).

**Suite result after application:** `pytest tests/contracts` → **3 failed / 310 passed / 1 skipped** (was 20 failed / 293 passed / 1 skipped). The 3 remaining failures are exactly rows 14, 18, 20.

**Time-box disposition:** Phase-1 complete for all 20 rows within the box (0 rows landed E by expiry; row 18's E is an adjudicated escalation, not a box-expiry default). Phase-2 applied 17 of the 18 L/T rows; row 14's application (not its verdict) deferred at expiry.

## Footer

### Proposed deferred-inventory AGGREGATE entry (orchestrator applies; do not edit deferred-inventory.md from this lane)

> **contracts-triage-2026-07-02-residuals** — Aggregate pointer to `_bmad-output/implementation-artifacts/contracts-triage-ledger-2026-07-02.md` (finding-d D-D1 ledger). Open items: **R-row 20** `test_specialist_error_taxonomy` — re-base `VoiceProviderTextError` (`077d68e2`) and `StyleguideError` (`bd0003d3`) onto `SpecialistDispatchError` + catch-site audit per the PIN-AUD-3T shrink-only ratchet (test intentionally left RED as the reminder); **E-row 18** `test_30_1_zero_test_edits` baseline rollforward-vs-retire (owner/governance decision; ambient-triage Batch-4); **L-row 14 application** `test_transform_registry_lockstep` — apply the adjudicated fix (Image extractor entry + Box fetch-layer exemption) with Texas-owner sanity check on the dispatch-presence regexes; **follow-on from T-row 11**: rename the S2-merge `write_api.append_event(state, event)` state-machine helper (production) so it no longer name-collides with the Lesson Plan log write surface; **observation from row 17**: 07W landed without a `pack_version` bump (Tier-2 pack-versioning question); **hygiene**: untracked stale root `marcus/` directory. Direction may flip if substrate evolves.

### Acceptance-Auditor countersign list (per gate-mode amendment: ALL L rows + the T row)

Rows 1–10, 12–17, 19 (L) and row 11 (T). Highest-attention items: row 13 (verify spec-braid-s4 line 60 + Story 1.6 stub header authorize the narrow exemptions), row 17 (verify 07W enrollment matches the P2-2 T11 Option-A governance and note the pack_version observation), row 11 (verify the helper-exemption rewrite preserves the single-writer teeth), row 14 (Texas-owner sanity check on the regex/exemption shapes).

---

## Appendix — 195be7c9 sweep-victim adjudications (a-batch audit)

**Provenance:** the carried-findings a-batch audit of `195be7c9` (s2-marcus-collapse Phase-5/6/7, 2026-05-07) found the mechanical import-linter contract-count sweep — its commit message documents "6 import-linter contract count tests: 12 -> 13 contracts kept" and explicitly disclaims semantics changes elsewhere ("string-literal … AST scan targets" / "30-1 identities preserved verbatim") — **substring-over-matched** the `12`→`13` edit onto unrelated numeric pins in two additional test files. Both live **outside `tests/contracts` (tests/ root)**; recorded in this ledger per D-A amendment 3 (all sweep-victim adjudications land in the finding-d ledger regardless of tree). Both remediated this session (branch `dev/carried-findings-enum-refresh-2026-07-02`), RED-first.

| # | Test (tests/ root) | Failure summary | Corrupting commit | True-pin birth | Cat | RED / GREEN evidence | Action |
|---|---|---|---|---|---|---|---|
| A1 | `tests/test_marcus_facade_roundtrip.py::test_get_facade_thread_safe_fresh_instances` (line 74) | Pin asserted `138` unique instances but the test spawns `range(128)` — an **impossible pin** (max distinct ids = 128). RED at HEAD. | `195be7c9` — diff hunk `- == 128` / `+ == 138` (the `12`→`13` sweep matched the `12` inside `128`) | `7d9a9bf6` (2026-04-26, "feat(3.1): land marcus orchestration foundation") introduced the fresh-instances semantics with `range(128)` + `== 128` (predecessor `46f68a13` asserted `== 1` under old singleton semantics) | **L** | RED captured this session: `assert 128 == 138 … 1 failed`. Reverted `138`→`128` → GREEN: `2 passed` (joint run with A2) | Revert pin to `128` (matches spawner arity; impossible-pin removed) |
| A2 | `tests/test_structural_walk.py::test_manifest_loader_reads_repo_manifest_contract` (line 421) | `sequence_doc_parity_specs` count pin `13` but the loader returns **12** and the in-file enumerating comment lists exactly 12 specs ("12 parity specs after v4.2 + 04A lockstep checks: … (x2), narration-and-manifest"). RED at HEAD. | `195be7c9` — diff hunk `- == 12` / `+ == 13` (swept alongside the genuine import-linter 12→13 count bumps; this pin is a manifest parity-spec count, not an import-linter contract count) | `b98aad6e` (2026-04-19, "chore(lockstep): enforce 04A workflow parity and venv structural-walk usage") set the true value 12 (predecessor `5e073cff` had 10 pre-04A) | **L** | RED captured this session: `AssertionError: assert 12 == 13 … 1 failed`. Reverted `13`→`12` → target test GREEN: `2 passed` (joint run with A1) | Revert pin to `12` (matches loader output + the enumerating comment) |

**Scope note:** `tests/test_structural_walk.py` also carries an unrelated pre-existing RED (`test_motion_dry_run_preview_adds_marcus_motion_sequence`) — explicitly out of scope for this adjudication and left untouched.

### Countersign verdict (durable record — Murat gate condition C-1)

**Acceptance-Auditor lane, 2026-07-02 (session 05):** rows 1–10, 12–17, 19 (L) + row 11 (T) — **FULL COUNTERSIGN, no hand-backs.** Every citation chain opened and verified against the named commits + governing artifacts; no repin overshoots its authorization; the two mechanical-sweep commits (`accd226d`, `195be7c9`) appear only as the *cause* of staleness, never as the *justification* for a repin. **T-row-11 mutation teeth-probe: PASS** (attribute-form write, bare log-module import, local-def bare call, and mixed-import-source cases all still flagged; only the sole-import-from-write_api-helper case is exempt via exact-set equality — a genuine second writer cannot ride the exemption). Negative adjudications independently CONFIRMED with fresh artifacts (dispatch-monopoly sole-caller; 33-2 both colliders governed: Story 1.6 stub `eb2adb01` + spec-braid-s4 `generator_ref`, spec line 59). Row-17 pack_version Tier-2 governance observation CONFIRMED real (07W landed at `b914bb9e` without a bump) — filed in the residuals aggregate. Spot-runs: 36 passed across the 10 edited files; full contracts tree 3 failed / 310 passed / 1 skipped matching the adjudication record.
