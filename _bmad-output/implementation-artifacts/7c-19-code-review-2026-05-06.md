# Story 7c.19 — T11 Lite Code Review

**Reviewer:** Claude (T11 lite)
**Date:** 2026-05-06
**Story:** `migration-7c-19-section-09-four-artifact-lock-semantics`
**Implementation author:** Codex GPT-5
**Review tier:** T11 lite (~10-15 min single-pass; aggressive DISMISS on cosmetic NITs per `docs/dev-guide/story-cycle-efficiency.md`)

---

## Verdict: **PASS-zero-patches**

Codex implemented Story 7c.19 cleanly within the prompt's file scope. All 5 ACs (A-E) are satisfied. All focused / non-regression / validator commands PASS exactly as Codex reported. Spot-checks confirm 35 PASS across the Wave-5 trio focused suite; lint-imports 12 KEPT UNCHANGED; class-conformance 19 PASS UNCHANGED; ruff clean. No MUST-FIX or SHOULD-FIX findings.

This is the **third Wave-5 close** (Marcus-side enforcement function — first non-HIL-surface story in Wave 5). One T1-T9 decision flagged for review: specialist-side Pydantic-v2 producer model gap; mitigation acceptable with documented follow-on.

---

## Findings

| Severity | Finding | Rationale |
|---|---|---|
| **MUST-FIX** | _none_ | — |
| **SHOULD-FIX-DEFERRED** | Lock-ready Pydantic-v2 producer models not found for Kira motion-plan / Vera fidelity-verdict / Quinn-R QA-verdict under `app/specialists/`; Codex used a local minimal lock model with `plan_unit_id` + permissive structural fields | Codex's spec-anticipated fallback ("If absent, document the gap and use a minimal local model that captures the lock-relevant fields"). Acceptable for §09 lock semantics scope (lock checks are about presence + plan_unit_id consistency, not deep specialist-output validation). **Deferred to follow-on**: file `seven-c-nineteen-specialist-producer-models-future-hardening` for next idle cycle when specialist-side producer Pydantic models are wanted. NOT a 7c.19 regression — it is a documented scope-bound. |
| NIT-DISMISSED | Broad regression 45 failed (same as 7c.18a/b; shared run) | UNCHANGED from prior close-batch baseline. Inherited noise. DISMISS. |

---

## AC verification

| AC | Description | Verified |
|---|---|---|
| A | `app/marcus/orchestrator/section_09_lock.py` + `enforce_section_09_lock(plan_unit_id, artifact_paths, *, tripwire_log_path)` function reads 4 artifacts, validates structural shape, validates plan_unit_id consistency across all 4, returns `Section09LockResult` on success or raises `GateError` with structured failure context on failure | ✓ Happy + 4 absent + parse-error + validation-error + plan-unit mismatch tests PASS |
| B | `Section09LockArtifactPaths` + `Section09LockArtifactRef` (closed-enum `artifact_kind` Literal of 4 producers + sha256-hex `artifact_digest`) + `Section09LockResult` (success-only `lock_status: Literal["locked"]`; failure raises GateError) | ✓ Pydantic-v2 ConfigDict + closed-enum + sha256 pattern verified |
| C | Tripwire-ledger entry on lock failure: deterministic JSONL append via `_append_jsonl`-style helper; entry shape includes `event` + `plan_unit_id` + `failure_kind` + `failed_artifact_kind` + `fired_at` + `schema_version: 1`; raises GateError after ledger write (audit-trail + visible failure both honored) | ✓ Per dropbox + tripwire determinism test PASS |
| D | Tests: happy + 4 absent-artifact + parse-error + validation-error + plan-unit mismatch + tripwire determinism + schema-hash STABLE pin | ✓ All tests PASS; schema hash for `Section09LockResult` pinned at `b26b0fb68a4bba3b06e57d764c2c16e4ea36b868d2d348485857cdd1db447f39` (sha256 hex confirmed) |
| E | Marcus M1-M4 import-linter contracts hold UNCHANGED; 12 KEPT post-implementation | ✓ Confirmed |

---

## Spot-check evidence

| Command | Expected | Actual | Match |
|---|---|---|---|
| Wave-5 trio focused | combined PASS | **35 passed in 6.81s** | ✓ |
| `lint-imports.exe` | 12 KEPT UNCHANGED (no pyproject.toml edit by 7c.19) | **12 kept, 0 broken** | ✓ |
| `validate_parity_test_class_conformance.py` | 19 PASS UNCHANGED (no parity_contract; Marcus enforcement is NOT an HIL surface) | **PASS: 19** | ✓ |
| Sandbox-AC | PASS | **PASS** (per Codex evidence) | ✓ |
| Ruff | clean | **clean** | ✓ |

---

## Codex T1-T9 decisions captured

- **Specialist-producer-model gap**: surfaced explicitly in dropbox review notes. Kira motion-plan / Vera fidelity-verdict / Quinn-R QA-verdict do NOT have lock-ready Pydantic-v2 producer models under `app/specialists/` per Slab-7b body-activation deliverables. Codex used a local minimal lock model with `plan_unit_id` + permissive structural fields. **Spec-anticipated fallback honored** — story spec AC-A bullet 3 explicitly allowed this path: "if absent, document the gap and use a minimal local model that captures the lock-relevant fields."
- **Tripwire log path**: default `_artifacts/section_09_lock_tripwire.jsonl` (per spec default).
- **GateError raised AFTER ledger write**: audit-trail completeness preserved; failure visibility preserved.
- **Section09LockResult schema hash pinned**: `b26b0fb68a4bba3b06e57d764c2c16e4ea36b868d2d348485857cdd1db447f39`.

---

## Pattern-parity ratchet check

- Pydantic-v2 ConfigDict(extra=forbid, validate_assignment=True) on all 3 models ✓
- Closed-enum Literal on `artifact_kind` (4 producers) + `lock_status` (`"locked"` only) ✓
- strip-then-non-empty validators ✓
- sha256-hex pattern on `artifact_digest` ✓
- Timezone-aware datetime for `locked_at` ✓
- `schema_version: int = 1` per FR-7c-51 ✓
- GateError imported from `app.gates.errors` (canonical pattern; no new exception class) ✓
- Tripwire-ledger append-only + JSONL deterministic ordering ✓
- No parity_contract decorator (Marcus enforcement; not HIL surface); class-conformance UNCHANGED ✓
- No pyproject.toml edit; M1-M4 contracts inherited by construction ✓

---

## Velocity record

- Wave 5 third close (Marcus-side enforcement; first non-HIL story in Wave 5).
- Specialist-producer-model gap surfaced + documented as deferred follow-on (spec-anticipated path honored).
- Tripwire-ledger linkage per NFR-7c-R3 + FR-7c-50 audit-chain integrity invariants successfully demonstrated.
- T11 lite cycle: ~10-15 min; PASS-zero-patches.
- Recommend **flip done** + close-batch with 7c.18a + 7c.18b.

---

## Follow-on filed

`seven-c-nineteen-specialist-producer-models-future-hardening`: when specialist-side Pydantic-v2 producer models for Kira motion-plan / Vera fidelity-verdict / Quinn-R QA-verdict land under `app/specialists/`, replace the local minimal lock model in `app/marcus/orchestrator/section_09_lock.py` with the producer-model imports for stricter structural validation. NOT a regression; documented scope-bound. Add to `_bmad-output/planning-artifacts/deferred-inventory.md` § Named-But-Not-Filed Follow-Ons at next session-WRAPUP per CLAUDE.md deferred-inventory governance.

---

## Closeout actions

1. ☑ Verdict written: PASS-zero-patches (with 1 SHOULD-FIX-DEFERRED follow-on filed)
2. ☐ Sprint-status flip: `migration-7c-19-section-09-four-artifact-lock-semantics: review → done`
3. ☐ Close-batch commit (combined with 7c.18a + 7c.18b PASS verdicts)
