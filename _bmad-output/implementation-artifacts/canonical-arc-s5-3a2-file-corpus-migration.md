# Story S5-3a.2 — file-corpus dormant-path migration (S5 split, part a.2 — the second migration wave before 3b)

**Arc:** Canonical Production Conversation (S5 split; party re-scope round 2026-07-07 — John RATIFY-B / Murat Q1(i)+Q2 / Winston split+prod-follow-on; M1 migration-before-flip). Gated after S5-3a (DONE `721cce04`), lands BEFORE S5-3b (the flip).
**Status:** **DONE** (2026-07-07 session 18; SOP-019 CONCUR-W-F → fresh dev RED-first (dev applied all 25 migrations across 10 files then stopped mid-final-sweep) → **orchestrator completed the verification**: `git diff -- app/ state/config/` EMPTY (test-only); the 25 flip-caused tests are ENV-INDEPENDENT (green at forced `MARCUS_G0_ENRICHMENT_ACTIVE=1` among the 10 migrated files: 111 passed / 1 skipped [= the irene skip-until-3b witness] / 3 failed — ALL 3 baseline-verified PRE-EXISTING at clean HEAD `721cce04`: pack_hash drift + roster_count drift + `test_trial_475_silent_bypass…` Texas-internal `BundleDispatchError`, none migration/flip-caused); trial_475 pinned (the F-1901 23rd); the 2 re-contracts genuine (+66/-4, not mechanical pins). Full-tree AC-6 causation-isolation → SOP-020. **3b UNBLOCKS.** — original SOP-019 CONCUR-W-F record: — an exhaustive full-tree causation-isolated sweep caught the undercount **F-1901** (25 not 24 — a 23rd downstream test `test_trial_475…` in `tests/parity/`, a SECOND flip mechanism = first-pause-relocation not file-crash) + F-1902/F-1903/F-1905/F-1904 all applied; dispositions/orthogonality/scope-fence confirmed sound, completeness proven (sweep 23 + read 2 delenv = 25); dev dispatch cleared) · **Size:** M · **Gate mode:** single-gate structural · **Branch:** `dev/workbook-2026-07-06`.
**Execution convention:** FRESH dev agent, RED-first, `PYTHONIOENCODING=utf-8`, `-n 0`, **no `git stash`**. Do not commit — orchestrator commits after re-verify.

## Why (party re-scope, 2026-07-07 — discovered at the 3b flip-attempt)
S5-3b's T1 flip-and-sweep discovered the flip's TRUE blast radius is **24 tests / 9 suites BEYOND the 3a-8** (SOP-018's ~0-5 estimate was blind to the class). ALL 24 share ONE root cause: they pass a README **file** as `corpus_path`, which only worked while G0-enrichment was a dormant no-op; the 3b flip wakes G0E's corpus-**DIRECTORY** enumeration → `DirectiveCompositionError: corpus path is not a directory` pre-gate. Per M1 (migration-before-flip) + strangler-fig discipline (Winston), these consumers migrate to env-independence NOW (flag still OFF) so 3b collapses to a bisectable one-default-flip commit. **This story does NOT flip the default** — it only removes the silent dormant-default reliance.

## The residual (25 tests — SOP-019 causation-isolated full-tree sweep; TWO flip mechanisms, not one)
SOP-019 F-1907 proved completeness: the process-env `=1` sim is a faithful proxy for the real code-default flip for every env-untouched test; there are EXACTLY 2 `delenv` sites tree-wide (the 2 feature-flag tests) + no conftest env manipulation → **sweep(23 env-untouched) + read(2 delenv) = 25 complete.** **The dev's T1 MUST re-derive this by a FULL-`tests/`-TREE causation-isolated sweep** (`MARCUS_G0_ENRICHMENT_ACTIVE=1` vs `=0`, isolated-re-run filter: flip-caused iff isolated-ON-fail ∧ isolated-OFF-pass) — do NOT trust this list or a file-corpus grep (F-1904; the earlier greps missed `tests/parity/` twice).
- **23 downstream-subject (D-kill-switch pin `setenv("0")`):**
  - **file-corpus-crash mechanism (22):** `test_production_runner_gate_pause_resume.py` (9) · `test_production_clone_launch_evidence_discipline.py` (4) · `test_resume_rehydrates_selection.py` (3) · `test_front_door_selection_threading.py` (2) · `test_trial_cli.py` (2) · `test_run_summary_yaml_emit.py::test_paused_at_g1…` (1) · `test_runner_threads_pre_fill_to_decision_card.py` (1) — these pass a README FILE → the flip wakes G0E's directory enumeration → `DirectiveCompositionError` (`composer.py:37-38`).
  - **first-pause-relocation mechanism (1 — SOP-019 F-1901/F-1902):** `tests/parity/test_trial_475_directive_composition_regression.py::test_start_trial_threads_composed_directive_to_texas_dispatch` — uses a VALID DIRECTORY corpus (`tests/fixtures/trials/trial_475_mini_corpus/`), NOT a file; flip-caused because the walk now first-pauses at G0E BEFORE Texas dispatches → `texas_payloads == []` (:391). Disposition is a `setenv("0")` pin (its subject = directive→Texas threading, downstream/G0-orthogonal — NOT a directory fixture; it already has a directory).
- **2 feature-flag-contract (D-RE-CONTRACT; the ONLY 2 `delenv` sites tree-wide):** `test_g0_enrichment_brick.py::test_ac_s2_5_default_off_is_byte_identical_first_pause_stays_g1` (`delenv`→file→asserts `paused_gate != "G0E"`) · `test_irene_refinement_brick.py::test_ac_s3_6_feature_flag_parity_with_s2` (`delenv`→`irene_refinement_active() is False`) — the flip INVERTS both premises.

## Dispositions (party-ratified — Murat, binding)
- **D-kill-switch (the 23):** per-suite explicit file-scoped autouse `monkeypatch.setenv("MARCUS_G0_ENRICHMENT_ACTIVE","0")` — exactly the 3a pattern. Subject-preserving (they keep testing the dormant G1-first path — their subject is downstream, enrichment-orthogonal, whether they crash on a file corpus OR relocate-first-pause on a directory corpus). **NOT** a corpus-directory fixture (that forces the woken path = coupling, not coverage) and **NOT** a broad directory-wide autouse (over-pin risk). **Per-suite guardrail:** the dev CONFIRMS each of the 23 is genuinely enrichment-orthogonal before pinning (SOP-019 spot-checked and confirmed all downstream); any test that actually asserts canonical-path behavior is MISFILED → give it a directory fixture, do not pin it (SOP-019 found NONE misfiled).
- **D-re-contract (the 2):** a mechanical pin is PROHIBITED (both-worlds-green vanity — the premise is inverted). For each: (a) re-home the legacy "default-OFF ⇒ byte-identical ⇒ G1-first" assertion behind an explicit `setenv("0")`, re-titled as the legacy/kill-switch contract; AND (b) add a NEW positive witness asserting the DEFAULT wakes G0E/G0R. **F-1905 (SOP-019, binding per-test):**
  - **g0 `test_ac_s2_5`:** the new default-ON witness uses the explicit `setenv("1")` form (green NOW, no skip-debt) **but MUST use a DIRECTORY corpus** (`CORPUS`, not the README file) — a `setenv("1")` witness on the file would crash with `DirectiveCompositionError` instead of proving the G0E pause. A woken directory witness already exists as the pattern (`test_ac_s2_5_woken_pauses_at_g0e_with_real_card`).
  - **irene `test_ac_s3_6`:** the MEANINGFUL new assertion is that the UNSET default now returns `irene_refinement_active() is True` (that IS "feature-flag parity"); a `setenv("1")` witness does not prove the *default*. → **skip-until-3b** (`@pytest.mark.skip(reason="asserts the 3b default-ON parity; un-skips at the 3b flip")`) with the un-skip obligation carried into the 3b spec.

## Acceptance criteria
- **AC-1 (env-independence — the 23):** each of the 23 downstream tests pins `setenv("0")` explicitly and passes at default-OFF (today) AND under a forced-default-ON simulation (`MARCUS_G0_ENRICHMENT_ACTIVE=1` at the process level) — env-independence, not both-worlds-green vanity (each asserts the DISTINCT dormant-path walk its pin drives). RED-first (the load-bearing proof, per the TWO mechanisms F-1902): at HEAD forced-ON, the 22 file-corpus tests FAIL with `DirectiveCompositionError`, and trial_475 FAILs with `texas_payloads == []` (empty-Texas-dispatch, its directory corpus intact) — capture each RED before pinning.
- **AC-2 (re-contract — the 2):** the legacy assertion survives behind explicit `setenv("0")` (re-titled); a NEW default-ON positive witness exists per Murat (explicit `setenv("1")` green now, or skip-until-3b). No mechanical pin of the inverted premise.
- **AC-3 (25-row test-disposition ledger — binding):** Completion Notes carry one row per affected test (23 pins + 2 re-contracts): `id · class (downstream-subject | feature-flag-contract) · mechanism (file-corpus-crash | first-pause-relocation | delenv-premise-inverted) · disposition (explicit-pin | re-contract) · replacement-witness (for the 2 re-contracts)`. No test reaches green without a row.
- **AC-4 (NO flip, NO production change):** `git diff -- app/ state/config/` EMPTY; the default stays OFF; codebase behavior byte-unchanged. This is test-only. (The production single-file-corpus legibility fix is the FILED follow-on `g0-single-file-corpus-opaque-crash-legibility`, NOT this story.)
- **AC-5 (3a-8 + full suite hold):** the 8 3a-migrated suites stay green; the full `tests/ -n 0` is green except the KNOWN pre-existing baseline reds (2 drift + Texas-internal + slab_7a unicode + psycopg/env-gated); re-count so the residual is exactly those known reds (no new).
- **AC-6 (flip-readiness proof — scored by CAUSATION-ISOLATION, F-1903):** with ALL 25 migrated + the 3a-8, a forced-default-ON full-suite simulation shows ZERO **flip-caused** reds remain. **Scoring rule (binding):** any red under full forced-ON is re-run ISOLATED both ON and OFF; it counts as a flip-caused failure ONLY if isolated-ON-fail ∧ isolated-OFF-pass. The raw full-ON suite carries KNOWN non-flip-caused NOISE that must NOT be scored as AC-6 failures: `test_workbook_producer.py::test_ac11_exercise_has_bloom_and_source_ref` (cross-suite pollution → spurious README crash), `test_workbook_s0_s7.py::test_real_tejal_workbook_docx_witness` (Windows docx `OSError` writing to the untracked `_bmad-output/artifacts/workbooks-test/` stray), and `test_ac13_created_at_is_tz_aware` (tz flake). This is the "3b collapses to a clean flip" witness.

## RED-first plan
1. **T1 (full-`tests/`-tree, F-1904):** re-derive the 25-test set by a FULL-TREE causation-isolated sweep (`MARCUS_G0_ENRICHMENT_ACTIVE=1` vs `=0` across ALL of `tests/`, then isolated-re-run filter: flip-caused iff isolated-ON-fail ∧ isolated-OFF-pass) — do NOT trust the list or a file-corpus grep. Reconcile against the 25 above (report any 26th). Confirm each downstream test's enrichment-orthogonality.
2. Per suite: capture the forced-ON RED (the `DirectiveCompositionError`), apply the pin, confirm green at OFF and forced-ON.
3. Re-contract the 2 feature-flag tests (legacy behind `setenv 0` + new default-ON witness).
4. Assemble the 24-row ledger.
5. AC-6: forced-default-ON full-suite simulation shows zero flip-caused reds (flip-readiness).

## Out of scope
The default flip itself (S5-3b — unparks after this closes; the flip code is preserved) · the production single-file-corpus legibility fix (filed follow-on) · G0E/G0R narration + dispatch_live (3b) · any production/`app/`/manifest change · the picker/CD/gary surfaces (S1-S4 closed).

## T11 gates
SOP-019 monitor pre-dispatch poll (verify the 24-set is complete + Murat's dispositions honestly applied — pins subject-preserving, re-contracts genuine not mechanical, the per-suite orthogonality guardrail respected) → FRESH dev RED-first → orchestrator re-verify (forced-ON zero-flip-reds AC-6; `git diff -- app/` empty; ledger complete; no new baseline reds) → SOP-020 close poll → commit (strays excluded). **No live witness** (test-only migration, no runtime surface — 3b owns the live G0E/G0R witness); no separate 3-lane (test-only — SOP-019 + SOP-020 + orchestrator re-verify are the proportionate independent verification, same call as 3a/g0-battery; escalate to 3-lane only if AC-4 trips). Story flips done after re-verify + close poll.
