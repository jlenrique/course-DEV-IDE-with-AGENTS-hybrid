GOAL - Complete remaining S8 work to full arc close (2026-07-08)

OBJECTIVE
Finish S8 on the Marcus-SPOC local runtime product. The first selection-edge
runtime slice is already CLOSED at `282ea82f`. The planning-input selection
contract checkpoint is implemented, reviewed, and live-witnessed, but still
uncommitted and explicitly NOT "S8 complete." This session must land that
checkpoint, complete the remaining S8 prose/workflow-direction lane, live-test
each authored/edited component against the local host-machine runtime, and
obtain fully-spawned BMAD party concurrence that S8 itself is closed.

PRODUCT BOUNDARY
- Target = Marcus-SPOC local runtime orchestrator only.
- Not an externally hosted app.
- Not a concierge/proofing-run convenience surface.
- Not real HAI/PHS ingestion, PHS B-2 renames, LO-ratification UX, course/SME
  routing completion, collateral projector families, Batch LLM/LiteLLM mode, or
  trust-complete hardening. Keep those deferred unless the operator explicitly
  re-scopes.

CURRENT STATE TO HONOR
- Branch: `dev/workbook-2026-07-06`
- Closed: first S8 selection-edge slice at `282ea82f`
  (`ratified collateral intent -> BUNDLE_CATALOG -> ComponentSelection ->
  local production runner`)
- In flight / uncommitted checkpoint:
  - `app/marcus/lesson_plan/collateral_selection.py`
  - `tests/marcus/lesson_plan/test_collateral_selection.py`
  - `tests/integration/marcus/test_trial_cli.py`
  - `docs/STATE-OF-THE-APP.md`
  - `docs/project-context.md`
  - `_bmad-output/implementation-artifacts/s8-planning-input-selection-contract-2026-07-08.md`
- Party close on that checkpoint already said: do NOT claim "S8 complete."
- Remaining S8 frontier named in status docs:
  lesson-plan rationale, asset-building task list, workflow-selection language,
  and downstream-consumer clarity.

TASKS
1) Orient first.
   Review:
   - active shadow-monitor ledgers under
     `_bmad-output/implementation-artifacts/`
     (especially `claude-shadow-monitor-s8-selection-edge-2026-07-08.md` and
     `grok-shadow-monitor-s8-roadmap-2026-07-08.md`)
   - `docs/STATE-OF-THE-APP.md`
   - `docs/project-context.md`
   - `SESSION-HANDOFF.md`
   - `s8-planning-input-selection-contract-2026-07-08.md`
   - current worktree / HEAD / origin sync
2) Convene a fully spawned BMAD party-mode team to confirm the remaining-S8
   done-bar before further implementation:
   - what must be committed now vs authored next
   - whether composed live proof (operator-named corpus + HIL) is in-scope for
     this S8 close or a named immediate follow-on
   - trigger-path governance for
     `app/marcus/lesson_plan/composition.py` and
     `app/models/state/component_selection.py`
   - explicit non-scope list
3) Land the planning-input selection checkpoint cleanly.
   - Re-verify focused tests + ruff + local CLI witness as needed.
   - Commit and push by explicit path list only.
   - Exclude known strays: workbooks-test/, runs/*, shadow-monitor ledgers,
     goal launcher files, duplicate evidence docx, and any .tmp witness dirs
     unless the operator explicitly asks to track a specific evidence artifact.
4) Complete the remaining S8 prose / workflow-direction lane.
   Author or refine the smallest useful artifacts that make the lesson-plan ->
   production path operator-usable and downstream-clear:
   - lesson-plan rationale
   - asset-building task list
   - workflow-selection language / direction
   - downstream-consumer clarity for the already-built selection edge
   Prefer BMAD story/spec discipline. Do not invent a projector family or
   course-specific shortcut.
5) Live-test every component as it is authored or edited.
   Offline/unit tests are required where appropriate, but local host-machine
   Marcus-SPOC / trial runtime execution is indispensable and must not be
   deferred to the end.
6) Review shadow-monitor notes at every major decision point:
   pre-dispatch, scope/done-bar, implementation/documentation checkpoint,
   live-test witness, review triage, commit, and close.
7) Keep deferred work explicit and parked with rationale unless deliberately
   selected by BMAD/operator consensus:
   - real HAI video/slide/readings ingestion
   - real PHS Confluence/Canvas ingestion
   - PHS B-2 module renames
   - operator LO-ratification surface
   - course/SME styleguide/voice/attribution/approval routing
   - collateral projector families beyond workbook/deck/motion selection
   - Batch LLM/LiteLLM token-efficiency mode
   - trust-complete hardening follow-ons
   - KG/onboarding regen and stale tracker cleanup
8) Run BMAD close gates for FULL S8, not merely the planning-input checkpoint:
   - code review where code warrants
   - focused remediation
   - re-verification
   - live/runtime witness
   - fully spawned party-mode close concurrence that S8 is complete
9) Leave the next exact gate clear after S8 close.
   At minimum, name the immediate post-S8 lane and any trailing S8-triggered
   follow-ons that become ripe only after close, including:
   - `workbook-learner-ready-prose-uplift`
   - `g0-enrichment-flag-retirement`
   - `research-dispatch-flag-retirement`
   Do not silently absorb those into the S8 done claim unless the party
   explicitly expands scope.

WORKING RULES
- BMAD-first governance; fully spawned party mode for green-light, major
  design choices, and close.
- Trigger-path governance is mandatory; avoid
  `composition.py` / `component_selection.py` unless a party-ratified
  lockstep/Tier event is opened.
- Preserve the Marcus-SPOC product guardrail.
- Explicit-path staging only; never `git add -A`.
- Do not claim "S8 complete" until the full remaining lane above is closed by
  party concurrence.

COMPLETION CONDITION
This goal is complete only when a fully spawned BMAD party-mode team convened
for this purpose concurs that remaining S8 work is accomplished, live-tested
against the local production runtime where applicable, validated, committed/
pushed as a clean checkpoint excluding known strays, and honestly closed as
S8-complete with the next post-S8 gate named.
