# Project Context: Multi-Agent Course Content Production System

**Project Name:** course-DEV-IDE-with-AGENTS
**Phase:** 4-Implementation — **Epic 34 §02A Downstream-Consumer Schema Coherence FULLY COMPLETE 2026-05-22** (7/7 stories DONE; Trial-3 attempt-3 launch UNBLOCKED on fully harmonized substrate). Slab 7c brownfield migration completed 2026-05-07 (36/36 dev-stories DONE) and Epic 34 then harmonized the §02A composer ↔ Texas wrangler integration boundary that Trial-3 attempt-2 surfaced as silently broken.
**Architecture Status:** 15 legacy epics complete + Epics 19-21 done + Epic 34 done, **125 FRs + 13 FR-E34**, Complete Architecture with Interstitial Cluster Extensions; **Hybrid LangChain/LangGraph re-platform: SHIPPED at M5 (2026-04-27) + Slab 7c orchestrational tail FULLY COMPLETE 2026-05-07 + Epic 34 §02A coherence FULLY COMPLETE 2026-05-22**

**2026-07-08 Update (session 20, operator-present; Class S [upgraded from P at the close batch — one comment-only code edit] — 🎯 S7 Phase-1 + Irene Pass-2 FORMALLY CLOSED 4/4 + Phase-2 course-source spec RATIFIED story-ready).** Branch `dev/workbook-2026-07-06` @ `8924bd78` (pushed; close batch `7e2ace2e`, 93 files). **(1) Formal close executed** (was pending "operator notes first"; operator indefinitely deferred the notes — minor workbook-content points → S8 prose lane — and directed spec completion instead): fresh 4-seat close-concurrence party (Winston/John/Murat/Amelia, independent code-and-evidence-verifying agents) **4/4 CONCUR(-W-FINDINGS)** on the single cross-cited recover witness (trial `4c64db93`, John-A1 two-story close honored); all 8 carried Codex-monitor findings (F-4001..F-4701) resolved or filed; both story files flipped ✅ DONE with binding close records; ledger SOP-048 (close record) + SOP-049 (post-commit verification poll — CLOSE COMMIT VERIFIED / CONCUR, zero findings). Key close rulings: `numeric_audit=FAIL` = the zero-denominator un-auditable NON-EVENT (0 symbol figure tokens, verified independently twice — "no unsourced numeral exists because no numeral exists"); DOI clause = PASS-with-recorded-nuance (2 rendered DOIs HTTP-200; the primary `10.5465/...` doi.org-dereferences to the correct AOM page which bot-403s); witness provenance = fresh post-fix AC-L (recover-labeled, NOT surgical re-entry); Pass-2 witnessed the gate's PASS path live (FIRE path = 14-test suite + the `40f3a90a` honest-RED; "self-healed" phrasing retired). Wording-honesty batch: 5 SOP-034 fixes in the S7 spec + Amelia close-F1 (stale "frozen tejal deck input" comment, `workbook_producer.py:107`). John close-catch: `research-quality-resolvable-doi-yield` was NAMED-only, never filed — now a real inventory entry; +`run-envelope-corrupt-vs-absent-fail-loud` (Winston rider) + wordform status-vocab rider. **(2) Phase-2 ratified:** fully-spawned party (Winston/John/Murat/Amelia) **4/4 GO-W-AMENDMENTS** over the operator's brief `s7-phase2-course-source-management-brief-2026-07-08.md` → party record `s7-phase2-party-record-2026-07-08.md` + **story-ready spec `canonical-arc-s7-phase2-course-source-stories.md`** (Stories A→{B∥C}→D: A = course-source registry `app/marcus/course_source/` + deterministic manifest scan + gap ledger + **the broad-root refusal guard FIRST** at the single ingestion chokepoint `_walk_corpus_files` [`composer.py:34-43`, covers both runner walks]; B = syllabus extraction PROPOSAL-only [renames = operator-gated leg B-2]; C = canonical asset record schema-shape story [closed enums, `source_grounded`-requires-content-refs teeth, projector-family excluded]; D = lesson-planning input bundle w/ `ComponentSelection` contract test + hard negative fence; spine filed by name at D-close). Reconciliation: `course-and-sme-registry` SPLIT (identity→A; styleguide/routing deferred, blocks HAI-510 production); `course-purpose…lo-inputs` EXPANDED (residue re-triggers at D); spine HARDENED-in-binding; projector family untouched. Amelia ground-truths: PHS 620 ".doc" = MIME/MHTML (stdlib-parseable, zero new deps); seeded `course.yaml` = de-facto v0.1 schema; all stories Tier-1. Committed as fixtures (F-4501): both course containers (PHS 620 15-module + HAI 510 4-module) + the brief; NOTE: HAI syllabus `.docx` is gitignored (`course-content/**/*.docx`) — local-only, spec B-D4 handles (frozen extracted-text fixtures + optional narrow exception at B's pre-dispatch poll). **NEXT: Class S — Story A dev dispatch (the close gate is OPEN).** HOLD-for-operator register in spec §0 (slug renames, .doc policy, HAI reading list, lesson granularity, LO-ratification surface, first production course, gap-ledger disposition, extra kinds). Records: SESSION-HANDOFF §session-20 · SOP-048/049 · party record · spec.

**2026-07-07 Update (session 19, operator-present; Class S — 🎯 S7 WORKBOOK GENERALIZATION LIVE-PROVEN: first-ever in-graph workbook off a DIFFERENT lesson).** Branch `dev/workbook-2026-07-06` @ session-19 wrapup. Executed the §9 operator design gate → ratified S7 design (positioning artifact `lesson-plan-rationale-platform-positioning-2026-07-07.md`) → S7 producer generalization (retire tejal constants + generalize `_plan_unit_and_context`; consume Irene `lesson_plan["collateral"]` via M3-safe `run.json` disk-read [Tier-1]; enrichment→resolution-overlay; S6 `research_entries` under G2; `WorkbookSpec.kind`) via party 4/4 + Codex SOP-026/027/028 + dev RED-first + 3-lane review + remediation. **First-ever in-graph composed walk to node 07W** (trial `40f3a90a`, 8 gates) → **honest-RED** (`workbook-producer.segment-manifest.empty`, root cause **Irene Pass-2 slide-join degeneracy** — id-less narrations collapsed the shared join to one bucket). **Fixed on merits as its own story** (`irene-pass2-slidejoin-id-integrity-gate`: `_assert_join_id_integrity` at `graph.py:705`, conditions a/b/c, raises the retryable tag → auto-retry self-heals; green-light 3/3 + review MUST-FIX delta-id gap + remediation). **RECOVER witness (trial `4c64db93`) COMPLETED → produced `_bmad-output/artifacts/workbooks/u01@1.{md,docx}`** (12.16MB) off Tejal **Part 3** — no tejal leakage, 6 real Part-3 sections, 5 Bloom exercises, DOI populated. ⛔ **"off-frozen-lesson generalization," NOT "non-tejal"** (Part 3 = different LESSON, same SME; cross-SME = Phase-2; SOP-034 P1). **Live-proven but NOT yet formally closed** (operator notes + party close pending). NEXT: (1) elicit operator notes, (2) build Phase 2 (lesson-plan-as-rationale platform, 4 filed items) amended per notes. Filed follow-ons: Phase-2 platform ×4, ingestion-robustness, Pass-2 ×2 (`join-narration-segments-silent-collapse-hardening`, `recover-with-reenter-node-affordance`), research-quality DOI. KG/ONBOARDING regen ~4 sessions OWED. (**Session-18** catch-up: Canonical Production Conversation arc S0–S6 CLOSED + DONE-SIGNAL-MET — see SESSION-HANDOFF §session-18.)

**2026-07-07 Update (session 17, operator-present; Class S — canonical-arc S3 CLOSED LIVE-PROVEN BOTH LEGS + party-ratified gamma-matcher contract amendment).** Branch `dev/workbook-2026-07-06` @ `7630d091` (+wrapup; pushed). **S3 "Gary shadow-parity" DONE** — full gate chain SOP-008 (CONCUR-W-F; caught the run.json-has-no-digest F-801 trap + the F-802 legacy-projection hard-fail → ruled runner-context transport, zero manifest/§06 changes) → fresh dev RED-first (16 .py: NEW pure comparator `app/styleguide/parity.py`; gary `styleguide_parity` receipt at the resolve site [three-outcome ok / expected-ordering-gap INFO / divergence WARN, never-raise, observability-only, WARN-seed byte-untouched — S4 owns the flip]; runner digest-triple threading incl. the `trial-start.json` attestation; three-way picker/CD/gary vocabulary lockstep pin) → SOP-009 CONCUR-W-F + 3-lane T11 (0 decision / 7 patch / 1 defer / 4 dismissed) → RED-first remediation 7/7 → orchestrator re-verify 725-green → **AC-L LIVE both legs, frozen judges verbatim, first-run-stands**: leg-1 13/13 (trial `a18c2a86`: real pick → live CD → real Gamma §07 → `ok/match` + `clock_eligible: true` + three-way digest attestation; **S-flip parity clock tick 1**) + leg-2 10/10 (rewind trial `4d465677`, SSOT mutated post-CD/pre-gary: `divergence/resolution-mismatch` WARN carrying both envelopes, directive digests EQUAL = genuine same-bytes disagreement, walk NOT halted, SSOT git-restored clean) → SOP-010 close audit CLEAN (timeline-verified: no judge output predates the fix; S3 DONE STANDS, **S4 UNBLOCKS**). **Mid-witness product discovery:** the frozen title-matcher tokenized apostrophes as boundaries while Gamma's slugger deletes them → deterministic `brief-unmatched` for ANY apostrophe title (the 2026-07-01 `gamma-title-matcher-punctuation-robustness` backlog entry's trigger fired; would have blocked S8 + real lessons). Fixed on its own merits at `59a9a48a` per a focused 3-seat round (party record **§10**: dual-pass enumerated apostrophe-family deletion; the collide-loud witness exposed a pre-fix SILENT wrong-slot binding). **Governance adds:** party record **§9** = operator-mandated S7 workbook-spec review/edit/approval checkpoint (purpose/contents/design-structure/sources — operator authority, overrides autonomous-party for that one gate); new deferred filings: `gamma-dispatch-retry-taxonomy-deterministic-variance` (brief-unmatched retried ×3 deterministically — 8 wasted paid decks), `run-artifact-split-across-runs-roots` wart, E4 clock-attestation-strength decision → S4 spec author. **NEXT: S4 FAIL-LOUD flip** (opens with SOP-011 on its spec — four obligations at the ledger tail). Records: S3 story file (done) · monitor ledger SOP-008..010 + orchestrator T11/AC-L records · party record §9/§10 · evidence `s3-acl-liveproof-20260707T011735Z` (committed) · SESSION-HANDOFF §session-17. KG/ONBOARDING regen OWED (grew again: parity module + matcher fix + ~16 files).

**2026-07-06 Update (session 16, operator-present; Class S — CANONICAL PRODUCTION CONVERSATION arc OPENED + S0/S1/S2 CLOSED, both stories LIVE-PROVEN).** Branch `dev/workbook-2026-07-06` @ `037be6d3`. Operator ratified the arc theme: every built-but-dormant ceremony becomes a STANDING property of a normal Marcus-SPOC run (picker, G0 enrichment, CD, Tracy research, generalized workbook, one composed nothing-bypassed proof). 6/6 green-light party, then a 3-seat re-scope round after the session's NEW **Claude shadow-monitor lane** (`canonical-arc-claude-shadow-monitor-2026-07-06.md`, SOP-000..007 — fresh agent per poll at every major interval, operator-directed) shredded the ratified premise on its FIRST poll: CD was never routed-around; it is LIVE and LOAD-BEARING at §06 (registry prose stale in both directions; fixed in S1). Re-scoped spine: **S0 ✅** (charter: W2 seam contract, T6b/c/d mapping gating the Batch-LLM GO, Tier-1 ruling) → **S1 ✅** (`c24308f7` — CD emits the deterministic sibling `styleguide_resolution` audit block; shared resolver re-homed `app/styleguide/resolver.py` w/ gary thin re-export; runner `directive_projection` seam; registry/overlay cd→wired; **live AC-L PASS**) → **S2 ✅** (`037be6d3` — picker canonical at interactive trial-start: Beat-1 kickoff ceremony, pick persisted via `write_pick_to_directive` [F-404], F-502 course-keyed pick events, scripted `--selection-code`, `TrialAlreadyExistsError` guard, ONE ceremony budget, pickability-filtered recommendations; **AC-L LIVE 8/8 happy-publish**: real gh-pages publish 200 → real selection-code round-trip → walk paused-at-G1; evidence `s2-acl-liveproof-20260706T211912Z`; <$0.05) → NEXT **S3** Gary shadow-parity (spec MUST inherit F-702..F-705 + F-402 + F-304 — see monitor ledger SOP-007) → S4 FAIL-LOUD (gated S1∧S2∧S3) → S5 G0 canonical (**BINDING precondition, party record §8**: green the 13-red g0_enrichment battery first — never-committed corpus fixture + stale ACTIVE_TERMINAL_GATES pin) → S6 Tracy Scite-canonical → S7 workbook generalization → S8 composed proof (operator names corpus). Gate rhythm per story (held twice, works): spec → monitor pre-dispatch poll → fresh dev RED-first → monitor dev-poll + 3-lane review → RED-first remediation → orchestrator re-verify → **LIVE witness (PASS required, never close on offline-green)** → commit → monitor close poll. Records: `canonical-production-conversation-arc-greenlight-party-record-2026-07-06.md` (+§6/§7/§8) · amended `styleguide-binding-cd-contract-2026-07-06.md` · monitor ledger · S1/S2 story files (done) · deferred-inventory §Canonical-arc (S-flip, F-102 dead-ceremony defect, flag retirements) · SESSION-HANDOFF §session-16. KG/ONBOARDING regen RECOMMENDED (new app/styleguide package + ~10 app/tests files).

**2026-07-03 Update (session 11, operator-present styleguide-picker gh-pages publish; Class S) — PICKER BUILT + REVIEWED + HARDENED + INTERACTIVE-MECHANICS REAL-BROWSER-PROVEN; public-URL deploy blocked on an EXTERNAL GitHub Pages backend bug.** Branch `dev/gamma-styleguide-phase2-2026-07-02` (arc commit `daa8a50d` pushed). Made the Leg-D style Picker a client-facing gh-pages surface (mirrors the Storyboard-A chooser: static page emits a copyable `SGP-{run_tag}-A:{slug}[ B:{slug}]` SELECTION CODE the client pastes back → `marcus_spoc` decodes into the run directive). Full BMAD spine: party GREEN-LIGHT 4/4 (Winston/Sally/Murat/Marcus; 8-amendment ledger) → dev RED-first → **4-lane code review** (Blind + Edge + Acceptance Auditor + independent shadow-monitor) → RED-first patches (MUST-1..5: producer-side run_tag validation [traversal/injection/hyphen], token-leak scrub, sidecar idempotency, SOP-204 versions-inconsistency, enforced confirm-gate) → **Track-1 hardening** (verify-after-push FAIL-LOUD — the publisher never returns a URL it didn't confirm 200; + pack sanitize). 3 seams: `picker_html_emitter.py` (new), `picker_publisher.py` (new), `styleguide_picker.py` + `marcus_spoc.py` (`run_picker_preflight` enforced product path). ~130 tests green, ruff clean, validator exit 0. **Interactive mechanics + zero-mis-mapping round-trip PROVEN on a REAL headed browser** (`evidence/picker-mechanics-realbrowser-20260703T202858Z`: A→A/B→B, SOP-204 live, stale-run_tag reject, idempotent commit) — operator's PRIMARY bar MET. **THE ONE OPEN ITEM:** the public gh-pages URL never served — blocked SOLELY on an **external GitHub Pages backend bug** (deploys fail at `syncing_files`, reproduced on a proven-healthy 467–475 MB tree → not content/size/history; stuck rejecting state). FIX = operator repo-admin Source-toggle (our token lacks Pages:write) / GitHub Support. En route: shared `jlenrique.github.io` pruned 847.5→456 MB (operator-approved, **10-day retention rule ratified**); gh-pages self-maintenance-routine spec + Actions-migration workflow staged; the duplicated `_git_publish_dir` (picker/chooser/storyboard/gamma) flagged for consolidation. **Design/lessons (binding):** "publish succeeded" was a lie until verify-after-push; a green git push ≠ a live page (Pages has a separate opaque deploy backend); real-browser mechanics proof > sum of unit tests; the operator's independent ~10-min Codex shadow-monitor corroborated both the idempotency gap and the deploy diagnosis. **NEXT:** operator Source-toggle → confirm picker public URL 200 → close task #4; build the self-maintenance routine (bmad-quick-dev + party); complete the Actions migration. Records: SESSION-HANDOFF §session-11 + `picker-gh-pages-build-failure-diagnosis-2026-07-03.md` (UPDATE-3) + `gh-pages-site-maintenance-routine-spec-2026-07-03.md` + deferred-inventory. KG/ONBOARDING regen RECOMMENDED (new picker module).

**2026-07-03 Update (session 10, operator-present studio-via-API override plumbing; Class S) — PRODUCTION STUDIO PATH CARRIES PER-RECORD theme_id + image_model OVERRIDES (step-1 plumbing, `2530f4f4` pushed) + REFERENCE Classic→Studio TRANSFORMATION LIVE-PROVEN + TRANSFORMER-SKILL SPEC delivered.** Branch `dev/gamma-styleguide-phase2-2026-07-02`. Full BMAD spine + an operator-added independent SHADOW-MONITOR lane consulted at every gate. **Design (operator-ratified, binding):** `production_mode` is a DECLARATIVE type tag, NOT an orthogonal switch; a Studio companion is a DISTINCT self-contained record (Approach 1); the Classic→Studio (later →videographic) TRANSFORMER is an authoring-time tool that GENERATES the companion record — not a runtime overlay engine (orthogonal Approach 2 rejected as too hard + limiting). **Landed:** (1) **step-1 plumbing** — resolver `_expand_studio` + dispatch `_generate_studio_variant` + validator carry `studio_template.{theme_id,image_model}` overrides (distinct from Classic `theme`/`image_model`; `GammaClient` already accepted the params → reuse, no client change); fail-loud on non-string, runtime enum-guard before any paid call, `derived_from` inert (resolver never reads it), set-equality drift guard; 230 tests / ruff clean / validator exit 0. Green-light 5/5 GO-WITH-AMENDMENTS (12-ledger) → dev RED-first → **3-lane code review** (Blind + Edge + shadow-monitor SOP-001) 0 MUST-FIX + **6 patches applied RED-first** (incl. the 2 paid-path robustness gaps the shadow-monitor surfaced) → hard checkpoint. (2) **step-2 reference transformation** — authored `hil-2026-apc-crossroads-blueprint-studio` (candidate; derived_from + 6-pt promotion_criteria + runner-up + known_limitation) + **PRODUCTION-SEAM live proof** (2 cards through `generate_gamma_variants`; wire receipts prove `themeId=e8tz1vxb9v1urqp` + `imageOptions={model:recraft-v3}` on both from-template calls; 16:9; first-run-stands). (3) **transformer-skill SPEC** = the 3-column MAPS/DROPPED/STUDIO-ONLY hand-mapping (`classic-to-studio-transform-hand-mapping-2026-07-03.md`). Dual-gate party **CLOSE-WITH-CONDITIONS** (Murat structural + Dan content); all conditions executed on-substrate. **KEY FINDING:** recraft-v3 renders crisp hero titles + clean Western illustration but **GARBLES small embedded body text** (Dan's predicted criterion-3 risk, real on both cards) → promotion held OUT; directed refinement filed (Arm-1 hero/large-callouts-only; NOT accept-limitation). **NEXT:** studio-candidate promotion (micro-text refinement), the Classic→Studio transformer skill (operator's step 3, built against the spec), Murat close-conditions (F6/F11/SOP-003-a). Records: SESSION-HANDOFF §session-10 + shadow-monitor log (SOP-001/002/003) + deferred-inventory §Session-10. KG/ONBOARDING regen RECOMMENDED (studio seams).

**2026-07-03 Update (session 09, operator-present Phase-2 roster BUILD-OUT; Class S) — 7-STYLE GAMMA REGISTRY BUILT + DECLUTTERED + MODEL-HUNT RESOLVED + STYLE PICKER BMAD-REVIEWED TO UNANIMOUS CONCURRENCE.** Branch `dev/gamma-styleguide-phase2-2026-07-02`, commits `89af093b`→`535f3b47`→`6849be98`→`b1435bba` pushed. **Landed:** (1) **7-style roster, all `permanent` + thumbnailed** — crossroads-classic (A) / digital-collage / magazine (Nano Banana 2 Mini = `gemini-3.1-flash-image-mini`) / blueprint-B (recraft-v3) / bread-and-butter (preserve / `gpt-image-2`) / videographic-glance (recraft-v3) / studio-image-card; 5 carry operator exemplar URLs. (2) **Declutter:** deleted legacy `classic-freeform-x-cards` + `hil-2026-apc-blueprint-classic` (+ orphaned thumbs); 9 test files repointed; aspect-honesty chip test decoupled via a committed portrait fixture. (3) **3 review findings** (from a review of the session-08 branch): matrix→`page_settings.ui_only_reference`; resolver fails loud on present-but-malformed `additional_instructions` (mirrors validator, preserves empty=unset); oversized-POST 413 Windows body-drain. (4) **Model-hunt resolved:** operator picked recraft-v3; winner protocol (double-pull, keywords ON) proved recraft-v3 clean+deterministic but with a Japanese-motif drift → operator-directed Westernization steer cleared it → minted `videographic-glance`. **gpt-image-2-mini's Japanese-anime prior resists even a hardened language lock for plain illustration — recraft-v3 is the clean-Western fix** (art-styles + full `gpt-image-2` escape it). (5) **matrix §6 free-text `imageOptions.style` PROVEN** for named styles (digital-collage "Digital Collage" on the wire). (6) **Style PICKER production-reviewed to UNANIMOUS CONCURRENCE** — a 3-seat BMAD review (Sally/UX + Murat/reliability + Winston/architecture) → dev-agent remediation (crash-atomic directive write [temp+fsync+os.replace]; 413 suppress; Content-Length guard; +regression tests; reject deprecated/probe at the mutation point; degrade broken thumbnail to placeholder; keyboard/focus/aria/selected-state/display-name/surfaced pros-cons/no-dead-click) + a **"View live example ↗"** per-card link to the operator's public exemplar decks → re-review Sally+Murat+Winston all CONCUR. 52 picker tests + 406 broad battery green. (7) **Studio-via-API override capability PROVEN** (from-template accepts `themeId`+`imageOptions={model}` overrides; a Blueprint-themed recraft-v3 Studio deck rendered live) — production plumbing filed as `studio-via-api-override-plumbing`. **NEXT:** the Studio override plumbing (ripe dev arc), or the §6 stylePreset-vs-`style` equivalence sub-test. Records: SESSION-HANDOFF §session-09 + control-map matrix + deferred-inventory §Session-09. KG/ONBOARDING regen RECOMMENDED (heavy picker/registry churn).

**2026-07-03 Update (session 08, operator-present Phase-2 style-creation RESTART; Class S) — STANDARD-'A' CROSSROADS STYLE CANONICALIZED + LIVE-VALIDATED + UI-VOCAB LAYER + CONTROL-MAP MATRIX.** Branch `dev/gamma-styleguide-phase2-2026-07-02`, commit `dfae2c47` pushed. The operator restarted style creation on matrix-grounded machinery. **Landed:** (1) new standard 'A' `hil-2026-apc-crossroads-classic` authored field-by-field (3-layer Gamma-UI↔API↔Gary walkthrough), **live-validated through Gary's AUTHENTIC seam** (gold-standard briefed slide rendered clean/English/on-message), canonicalized permanent + in the Leg-D picker + curated thumbnail; (2) **amount UI→API vocab layer** — registry stores Gamma UI vocab (`minimal`/`concise`), code translates to API (`brief`/`medium`) at `_normalized_gamma_settings`+`_text_options_for_variant`, validator accepts UI vocab (operator-ratified: registry=UI-vocab, code-translates); (3) **`prompt_configuration.additional_instructions`** activated as a first-class complement-not-overwrite style channel (ported onto correct HEAD after a background dev-agent worktree was found branched from a stale base); (4) **`skills/gamma-api-mastery/references/gamma-style-control-map.md`** — a 3-layer traceability matrix (UI↔API↔Gary packet, owner/consumer/gaps, §5b vocab-normalization, §6 art-style channel [`style` canonical / `stylePreset` legacy], §8 style-dev lessons) as the SSOT the tooling draws on; (5) A/B/collage **candidates** `hil-2026-apc-crossroads-blueprint` (theme swap) + `hil-2026-apc-crossroads-digital-collage` (art style via free-text `imageOptions.style`), unrendered, for next-session eye; (6) cleanups: `status`/`export_as` dropped, decorative page-setup → `ui_only_reference`, picker hides `deprecated`. **KEY LESSON (matrix §8):** image quality rides in the slide CONTENT's `Illustration:` brief (protected source-detail→Gamma conveyance), NOT the style config — the CJK-in-image finding (gpt-image-2-mini) is a content-brief gap proven by rendering the identical config clean once briefed. **Standard-A still uses the PROVEN legacy `style_preset: illustration`;** its migration to the canonical `style` channel is gated on a proof render (deliberately not migrated blind). **NEXT:** retry the videographic MODE/style on this machinery. Records: SESSION-HANDOFF §session-08 + the control-map matrix + `gamma-style-additional-instructions-channel*` party record/spec. KG/ONBOARDING regen RECOMMENDED (cumulative).

**2026-07-02 Update (session 07, operator-present Phase-2 style building; Class S) — LIFECYCLE SCHEMA + VIDEOGRAPHIC REGISTER PROVEN + MODEL HUNT (verdict pending).** Branch `dev/gamma-styleguide-phase2-2026-07-02`. Party green-light 5/5 RATIFY-W-AMENDMENTS (A1–A14). **Landed:** (1) registry **lifecycle schema** `candidate|permanent|deprecated` enforced at the validator write-gate (candidate requires promotion_criteria + authored_session; picker badges candidates; 5 pre-session records retro-marked permanent; promotion gate = B-corpus stress test + re-run reliability, operator-ratified); (2) frozen 4-slide probe corpus `style-test-strip-v1` (instruction-neutral, verbatim Tejal C1-M1); (3) **videographic register proven live** across 5 renders (forced `cardSplit=inputTextBreaks` split 5/5 reliable; large-type narrative text recipe; prose-continuity uncanny-valley finding → forced relationship-building DROPPED; cluster-head grammar adopted; one-generation-one-visual-brief proven) — `videographic-glance-track` candidate v2 banked; (4) first-bind witness DISCHARGED (`gpt-image-2-mini` verified live but anime/Japanese in-image text — swapped to recraft-v4, unrendered); (5) 5 corpus-stamped ledger observations; (6) **model hunt v1/v2** through Gary's authentic seam (operator directive; v1 proved the house look is INSTRUCTION-dominant — keyword homogenizer converges models; v2 = discrimination protocol with RecordingClient wire receipts) — **operator verdict PENDING**. Load-bearing operator decisions: videographic = future MODE/overlay orthogonal to style (schema home at close gate); interstitial rename queued (B-companion earmark STAYS with it); style-dev via authentic seam + simulated-Irene briefs (never count toward permanence). Records: SESSION-HANDOFF §session-07 + `gamma-styleguide-phase2-session07-greenlight-party-record-2026-07-02.md` + deferred-inventory §Session-07.

**2026-07-02 Update (session 06, operator-present orientation; Class P) — GAMMA STYLEGUIDE PHASE-2 STAGED: master consolidated, Phase-2 branch cut, roster reviewed, A/B semantics confirmed; ready to BUILD the first new styleguide next session.** Light no-substrate session. Master fast-forwarded to session-05 HEAD (`c0ce283b..f3c8c8df`) + pushed — **master is now current** — and today's style-dev branch `dev/gamma-styleguide-phase2-2026-07-02` was cut off it (branch == master, origin 0/0). Reviewed the Leg-D picker roster live (review-only server, wrote nothing): **3 production guides** (`classic-freeform-x-cards`, `hil-2026-apc-blueprint-classic`, `hil-2026-apc-studio-image-card`, all with real curated thumbnails) = 3 of ~8 target, plus **2 hidden Leg-C floor probes** (`leg-c-part3-floor-probe` floor=8 reachable / `leg-c-part3-floor-toohigh` floor=20 unreachable-honest-refusal; `thumbnail_ref: null` by design → correct "no live render" placeholder). **Semantics confirmed (load-bearing for Phase-2):** the app does NOT insist on A/B — decks generated = variants named (`gary/_act.py:847-850`); bare default = single deck (variant A); one style in one picker slot = a single-deck/primary-only run (single-variant-binds-one per the `retire-default-variant-pair` fix); two slots = an A/B pair; `double_dispatch` is the only opt-in that forces two absent explicit settings. **NEXT: activate Marcus → author the first new guide** in `state/config/gamma-style-guides.yaml` via the full BMAD spine (party green-light → author + `validate_gamma_style_guides.py` exit 0 → live Gamma A/B the operator eyes → dual-gate close → curate thumbnail). Honest residuals from session 05 carried unchanged (contracts 2 failed; C3 lint-imports; vision anchor variance now `--run-live`-gated; `/harmonize` unregistered 6th session — nothing to harmonize, no diff). Record: SESSION-HANDOFF §session-06 + next-session-start-here §session-06.

**2026-07-02 Update (session 05, autonomous overnight) — CARRIED-FINDINGS REMEDIATION + 🔴 gamma-image-model-enum-refresh BOTH CLOSED + row-20 error-taxonomy re-base (goal done; operator returns AM for Phase-2).** Branch `dev/carried-findings-enum-refresh-2026-07-02` (fresh off master `c0ce283b`; commits `e69b309c`→`2b963bc3`→`87f38b5c`→`95fd42d9`→`8698c17e`→`2b9215bc`, all pushed). Full BMAD spine per change (green-light parties 4/4 + 6/6 + Task-3 pick 3/3; spawned devs RED-first; 3-lane reviews; live proofs; Murat gates + Dan CD checklist). **(1) Carried findings a–d remediated:** (a) pr_rc 135→125 revert — ROOT CAUSE: `195be7c9`'s "contract count 12→13" sweep over-matched substrings (`12x`→`13x`), corrupting THREE numeric pins (all reverted: 125, 128, 12; sibling victims ledgered); (b) publisher post-join export projection lifted byte-identically into `enrich_segments_for_export` + anti-tautology oracle (byte-equality + literal anchors + hand-written-literal projection tests; anchor-RED witnessed); (c) **`llm_live` now requires `--run-live`** (was key-presence-only → real gpt-5.5 spend on every default run with a real key; global Pass-1 deselection, Pass-2 key-skip retained, 5-test gate contract + rot guard; capture layer writes repo-relative posix, fail-loud; W1 $0 deselection witness + W2 single real recapture ALL-PORTABLE, anchor flake reported-not-retried); (d) **20 contracts-tree reds triaged** into `contracts-triage-ledger-2026-07-02.md` (L=17 repins applied, auditor FULL-COUNTERSIGN incl. mutation teeth-probe; T=1; R=1; E=1). **(2) enum-refresh dual-gate CLOSED** (`2b963bc3`): IMAGE_MODEL_VALUES strict doc-parity (−3 stale/+11 documented) across `_act.py` + creative-directive schema yaml/json + parameter-catalog (machine-readable `unverified-in-production` markings); NEW three-way lockstep pin (RED-witnessed mid-sequence) + synthetic-drift RED-path fixture; LIVE proof exit 0 first-run-stands (enum-parity-image-model → confirmed; ledger byte-untouched — driver write monopoly held; resolution-row-kind gap filed). **(3) Task-3 pick (A): row-20 R fix closed** — `VoiceProviderTextError` + `StyleguideError` re-based onto `SpecialistDispatchError` (live-path crash→error-pause; 12-row catch-site audit table; de-risks Phase-2 authoring); row-14 DROPPED by Texas-owner check (adjudicated fix ruled UNSOUND — would flip a green companion test; corrected exemptions-only shape ledgered). Contracts tree now **2 failed** (row-18 E owner decision + row-14 re-adjudication) vs 20 at session start; TW-7c-4 red on 10 pre-existing ambient scratchpad drivers only. Records: `carried-findings-remediation-greenlight-party-record-2026-07-02.md` (+Task-3 addendum) + `gamma-image-model-enum-refresh-greenlight-party-record-2026-07-02.md` + story (done) + ledger. **NEXT: Phase-2 interactive roster growth with the operator** (Leg-D picker surface; first new-model bind = designated live availability witness). (B) keywords→imageOptions.style + (C) promotion-path ceremony deliberately left for operator-present sessions.

**2026-07-02 Update (session 04) — GAMMA STYLEGUIDE LIBRARY arc: 🏁 PHASE-1 MACHINERY COMPLETE — W-2 dead-branch deletion + Leg-E `gamma_docs` live-doc audit BOTH CLOSED (/goal done; final concurrence 3/3 John/Murat/Dan).** Branch `dev/gamma-styleguide-leg-e-2026-07-02` (fresh off master `e0f50f41`; master ff-consolidated at close). **W-2** (`6b1a6a48`): RED-first deletion of the unreachable irene pass-1 floor plumbing + orphaned old engine (−837/+46; the live port at `irene_pass1/` untouched); Murat single-gate B1–B6 PASS. **Leg-E** (final Phase-1 leg; `06c39623`+`5730997f`+`ea0c3294`): Texas Shape-3 `gamma_docs` RetrievalAdapter (kind=direct_ref, 3 declared degeneracies, pure leaf, status=ready flipped WITH the live proof) + deterministic audit driver (`scripts/utilities/audit_gamma_docs.py`; 3 terminal states w/ totality; S-1 write mapping + wording-triple gate; exit tiers 0/10/20 over REAL items; VOID never writes) + pointer-table manifest (dotted code_refs covering every `_ENUM_CHECKS` frozenset; 1 labeled probe). Full spine: green-light 6/6 (4 tensions synthesized S-1..S-4; Amelia's live probe found real image-model drift pre-story) → dev RED-first → 3-lane review (2 MUST-FIX: VOID-run SSOT writes; blank-expected false-CONFIRMED — remediated RED-first; 2 ratified spec amendments P6/P10) → **LIVE PROOF**: 15/15 terminal states vs real developers.gamma.app (serves raw markdown via `.md`/`llms.txt` — no scraping needed); **FIRST REAL learned-store writes** (ledger 3→6, candidate-only, wording-triple 0 rejections): image-model doc-drift + coverage-gap (→ 🔴 `gamma-image-model-enum-refresh` follow-on) + the 429 standing-candidate resolution (docs still say 429; the 401 live observation STANDS — authority precedence held under real fire); probe teeth live (doc-restructure, zero writes); idempotent re-run byte-identical; `gamma-learned-rules.lock` byte-untouched → dual-gate: Murat HAND-BACK F-1 (stale duplicate status pin, caught by the pin's own teeth) → remediated → **Murat CLOSE no-conditions + Dan CD + Texas contract CLOSE**. Field-surface Qs answered (NO discrete imageOptions tags field; NO documented stylePreset⊕style composition) → keywords-routing trigger MET; promotion-path trigger FIRED (noted, not acted). Carried findings: 3 START-gate reds (pr_rc fixture drift; narration-join golden drift; vision live test UNGATED in the default profile + absolute-path recapture side-effect) + the 20 pre-existing contracts-tree baseline reds (proven at HEAD; candidate triage story). **NEXT: Phase-2** interactive roster growth (~8 styles, Storyboard-B only; the Leg-D picker is the surface); batch-LLM epic ⏸ at arc close. Records: `leg-e-gamma-docs-audit-greenlight-party-record-2026-07-02.md` (incl. close addendum + 3/3 concurrence) + story `leg-e-gamma-docs-live-doc-audit.md` (done) + SESSION-HANDOFF §session-04. KG/ONBOARDING regen recommended (gamma_docs adapter + audit driver + W-2 deletions).

**2026-07-02 Update — GAMMA STYLEGUIDE LIBRARY arc: Leg-C `min_cluster_floor` CLOSED LIVE + Leg-D HTML style-picker CLOSED (both dual-gate; /goal complete, final party concurrence 3/3).** Branch `dev/gamma-styleguide-leg-c-live-2026-07-01` (master consolidated first at `fa9485a9`; all session work pushed through `14b32595`). **Leg-C:** the D1 adjudication party (6 seats, unanimous) found the offline honoring UNREACHABLE (wrong module: the `irene/graph.py` pass_phase branch vs the real `app/specialists/irene_pass1/`; the floor also LEAKED into the LLM prompt) → ratified R3 rewire (per-unit `source_refs[]` verbatim ANCHOR emission + deterministic seam-splitting with R2 veto discipline; splits mint `#f` clusters at model-declared unit boundaries) → live proof on the Part-3 corpus: **AC#9 7<8≤8 on the clean T3 arbiter** (04A=10 no-op → 05 refinement consolidation → honoring FIRED, minted `c-u04#f1` — latest-pass honoring live-proven), AC#10 determinism pin, AC#13 honest refusal LIVE at $0. THREE live-only defects fixed RED-first: F1 specialist-id alias-form threading (floor never threaded in ANY real walk), F2 anchor markdown/quote debris (false veto), the T2 double-walk checkpoint race (now the pause-write-atomicity live fixture). Claims shrunk: 07G floored-deck coverage = 🔴 non-waivable follow-on `leg-c-floored-deck-07g-live-walk` (chained on the gary export-matcher fix); AC#12 live-vacuous (figure-role anchors never emitted on this corpus). **Leg-D:** green-light 6/6 chose Fork A (PRE-RUN local picker — the directive IS the binding surface; in-run gate rejected). Shipped `app/marcus/orchestrator/styleguide_picker.py` (stdlib same-origin http.server; slot-bar A/B UX + receipt panel; probes default-hidden; sidecar `gamma-styleguide-picks.jsonl` — SSOT `presentation.last_used` null forever; curated real-render thumbnails + data-driven aspect-honesty chip). 41 tests + AC-8 LOCAL live leg 17/17 (the pick threaded the floor probe's `min_cluster_floor: 8` through the REAL runner branch). 3-lane review MUST-FIXes (CORS null-origin; blueprint-thumbnail honesty) remediated RED-first. Records: `leg-c-d1-adjudication-party-record-2026-07-01.md` + `leg-d-styleguide-picker-greenlight-party-record-2026-07-02.md` + both stories (done) + `evidence/leg-c-part3-baseline-20260701T232209Z/AC-DOSSIER.md`. **NEXT: Leg-E** (live-doc audit; own green-light; first REAL learned-store writes) → Phase-2 roster growth (the picker is Phase-2's surface). KG/ONBOARDING regen RECOMMENDED (new `irene_pass1/cluster_floor.py` + `styleguide_picker.py` + heavy test additions).

**2026-07-01(g) Update — GAMMA STYLEGUIDE LIBRARY arc: Leg-C `min_cluster_floor` scripted class OFFLINE-built + 3-lane reviewed + P1–P8 remediated (in-progress; `c933404d`).** Full BMAD spine: party GREEN-LIGHT 6/6 (two tensions synthesized — sealed `scripted` namespace not a bare field; dead-config split narrow-in-scope + general-follow-on) → binding-time investigation DISCHARGED (EARLY-BOUND: directive threaded before `05B irene-pass1` which precedes `07 gary`; honoring is NET-NEW logic since Pass-1 is LLM-emergent with no existing floor) → offline dev RED-first (sealed `scripted` closed-vocab v1=`min_cluster_floor` + scripted-only accessor [no `gamma_settings[]` path] + validator rules + irene-pass1 threading [orchestrator reads SSOT yaml directly, no gary import] + NEW `app/specialists/irene/cluster_floor.py` deterministic split-only honoring; 8 ACs, 45 tests) → 3-lane `bmad-code-review` (Blind+Edge+Auditor; 2 decisions + 8 patches) → **P1–P8 RED-first remediation** (P1 strip floor from LLM-visible payload [binding re-parameterization fix], P2 fork-#2 false-positive-halt removed, P3 fail-loud→recoverable, P4 distinct sub-cluster identities, P5 atomicity fail-safe, P6 dup-class validator, P7 no silent-drop, P8 parity guard; 62 Leg-C tests green, 507 sweep, ruff clean, invariants held). **D1/D2 folded to the LIVE PART-3 BASELINE GO/NO-GO:** D1 = the honoring rests on a GUESSED Pass-1 output contract (member-keys + role tags the prompt never requests; offline ACs tautological w.r.t. fixtures) — the baseline captures a real Pass-1 output → proceed or invoke the operator-pre-authorized Pass-1 output-contract extension (P5's fail-safe makes it decisive: role-less clusters refuse floors). D2 = visual disaggregation reframed (no "fused slides" at Pass-1 — source-granularity, drivable on-principles + directly-specified "X→≥Y clusters"; the targeted rule = the 2nd scripted class). **NEXT: the Leg-C live Part-3 baseline → live proof AC#9–13 → dual-gate CLOSE. 🔥 FABLE 5 regained (`claude-fable-5`) — bake into upcoming sessions: P1 evaluate on Irene Pass-1 (may resolve D1), P2 narration-fidelity flag-ON, P3 hard design arcs (bundle-carrier, positive-carry) — see SESSION-HANDOFF §(g).** Records: leg-c greenlight-party-record + story `leg-c-min-cluster-floor-scripted.md` (Review Findings + Decision resolution). KG/ONBOARDING regen recommended.

**2026-07-01(f) Update — GAMMA STYLEGUIDE LIBRARY arc: Leg-B COMPLETE — `leg-b2-learned-store-scaffold` DUAL-GATE CLOSED (`2391974e`).** B2 = the hermetic learned-store scaffold: NEW `app/specialists/gary/learned_dependencies.py` (declarative interpreter [closed predicate ops, no eval] + digest-idempotent append-only JSONL observations ledger + identity-manifest pin [id-set superset + predicate_hash + fixture-existence, NO count]) wired into `validate_style_guides_full`; ships EMPTY manifest + 3 `status:candidate` OBSERVATIONS (zero active rules, non-enforcing). Two-store split real in code; promotion machinery + live CD envelope-authoring ceremony + memory-sidecar projection DEFERRED + honestly disclaimed (validated-by-fixture, NOT exercised-live; refuses Leg-A AC#6 over-claim). NOT the Postgres `app/ledger` (hermetic requires a file store). Spine: dev RED-first (20 failed pre-fix) → Murat structural CLOSE 8/8 (anti-vacuous hole provably plugged) + Dan CD content CLOSE-WITH-CONDITIONS (Dan-C1 discharged). 47 tests green (22 B2 + 25 B1), 3 seeds clean, hermetic, ruff clean. 2 follow-ons filed. Records: leg-b2 story (done) + SESSION-HANDOFF §2026-07-01(f). **NEXT: Leg-C** (`min_cluster_floor` scripted→Irene Pass-1, 07G-gated).

**2026-07-01(e) Update — GAMMA STYLEGUIDE LIBRARY arc: Leg-B GREEN-LIT (SPLIT B1/B2) + Leg-B1 dependency rules DUAL-GATE CLOSED (`d7ec3207`).** Leg-B green-light 6/6 split the leg into B1 (documented rules — done) + B2 (learned-store scaffold — next); the learned-store promotion machinery + live CD envelope-authoring ceremony are DEFERRED (premature before Leg-E; honesty-disclaim per Dan). **B1** extended the hermetic validator `scripts/utilities/validate_gamma_style_guides.py` with a warnings channel + Rule 2 (ERROR `gamma.dep.image-model-source`) + Rule 3 (WARN `gamma.dep.preset-style-subordinated`). The guardrail shrank the claim honestly: rules 5/7 dropped (runtime-request surface), from-template⊕theme_id + rule 6 deduped (already `surface-violation`), narrow non-contradiction skipped (seed #1's "16:9-as-excluded" rationale defeats the only deterministic pair). Spine: GREEN-LIGHT 6/6 → dev RED-first (7 failed pre-fix) → Murat structural CLOSE 8/8 + Gary/Dan content CLOSE-WITH-CONDITIONS (Rule-3 wording tightened; 4 follow-ons filed). 25/25 validator tests, 3 seeds clean (0 err/0 warn), hermetic no-network guard, ruff clean. Records: leg-b greenlight-party-record + leg-b1 story (done). **NEXT: Leg-B2** learned-store scaffold (schema + append-only ledger + identity-manifest pin, empty-by-design).

**2026-07-01(d) Update — GAMMA STYLEGUIDE LIBRARY arc: `gamma-instructions-channel-cleanup` DUAL-GATE CLOSED (`bb33852d`).** Operator-surfaced from live Gamma runs: `_instructions_for_variant` (`app/specialists/gary/_act.py`) dumped variant style settings as PROSE into `additionalInstructions` (image_style_preset/amount/tone) even though they travel structurally via imageOptions.stylePreset/textOptions — redundant + wrong-channel + a literal `image_style=None` bug. **Operator principle (binding):** additionalInstructions is a legitimate/primary style channel; it must never merely echo a structured param nor contradict one. Fix: removed the redundant settings-dump + None bug + vestigial `template=`; de-tokenized keywords → "Emphasize this imagery: X, Y." prose (kept in-channel, source-wins precedence preserved). Full BMAD spine, dual-gate: GREEN-LIGHT 6/6 (Winston/John/Murat/Amelia/Gary/Vera) → dev RED-first (6 failed pre-fix) → **AC#8 live real-Gamma A/B** (wire captured: no dump/None; stylePreset=illustration + amount/tone travel structurally; source note verbatim+leading; source keywords WON over styleguide base live — `evidence/instructions-cleanup-ac8-20260701T182809Z/`) → **DUAL-GATE CLOSE** (Murat structural 8/8 + Vera fidelity 6/6). 201 tests green, ruff clean. 3 follow-ons filed (keywords→imageOptions.style structured routing; prose-vs-param non-contradiction validator; single-slide title-matcher flake). Records: greenlight-party-record + story (done) + SESSION-HANDOFF §2026-07-01(d). **NEXT: Leg-B** dependency enforcement.

**2026-07-01(c) Update — GAMMA STYLEGUIDE LIBRARY arc: RIPE `styleguide-retire-default-variant-pair` CLOSED (`7b42dede`).** On branch `dev/gamma-styleguide-library-2026-07-01`. `_normalized_gamma_settings` (`app/specialists/gary/_act.py`) hardcoded `return [by_variant["A"], by_variant["B"]]` — always emitting both variants regardless of what the payload named, so a single styleguide binding paid-dispatched an unbound fixture-B deck (Edge#8). Retired: now `return [by_variant[v] for v in present_ids]` (projects only payload-present variants, payload order). Full BMAD spine, single-gate: **party GREEN-LIGHT 6/6** RATIFY-WITH-AMENDMENTS (Winston/John/Murat/Amelia/Gary/Dan; the DEFAULT_VARIANT_PAIR-disposition contention reconciled to Dan's synthesis — retain base seed + WARN + defer fail-loud flip) → dev RED-first → **3-lane `bmad-code-review`** (0 correctness MUST-FIX + RED-first R1–R4 fail-loud remediation: dup→raise, non-empty-zero-valid `[None]`→raise, double_dispatch WARN, negative-WARN+companion tests) → **AC#8 live real-Gamma proof** (exactly 1 real `generate_deck` call, variant A only, negative-twin zero fixture-B; discriminating deterministic block; re-judged from on-disk artifacts — `evidence/retire-variant-pair-ac8-20260701T180148Z/`) → **Murat single-gate structural CLOSE 6/6**. 200 gary/styleguide tests green, ruff clean. Consumer `generate_gamma_variants` needed zero edits (already iteration-based). New follow-on filed: `styleguide-retire-default-variant-pair-fail-loud-flip` (flip styleguide-less WARN-seed → FAIL-LOUD once cd-envelope-authoring lands). Records: greenlight-party-record + story (done) + SESSION-HANDOFF §2026-07-01(c). **NEXT: Leg-B** (dependency enforcement).

**2026-07-01(b) Update — GAMMA STYLEGUIDE LIBRARY arc OPENED; Leg-A (library spine) DONE + pushed (`bd0003d3`).** New CD-substrate arc on branch `dev/gamma-styleguide-library-2026-07-01` (fresh off master `d02f456c`). **Operator two-phase shape:** Phase-1 machinery (Legs A–E, autonomous BMAD spine) + Phase-2 interactive-with-Marcus roster growth to ~8 styles (Storyboard-B-terminating, no Descript); **Option-A DONE-bar = machinery proven, not roster-complete.** The operator added **3 features** to the party-ratified §7 design (`gamma-styleguide-library-briefing-2026-06-30.md`): a styleguide **AUDIT** (→Leg-E, Texas live-doc fetch + dependency enforcement), an **HTML style-picker menu** (→Leg-D, CD-entry gate + last_used/thumbnail/narrative), and a per-style **`scripted`** imperative channel (→Leg-C, `min_cluster_floor`→Irene). **Binding operator directive:** REMOVE baked-in generation-time overrides so the library config is the SOLE determinant of Gamma output. **Leg-A CLOSED:** CD-owned SSOT `state/config/gamma-style-guides.yaml` + hermetic `scripts/utilities/validate_gamma_style_guides.py` write-gate (inverted-polarity completeness / frozen-enum reuse / studio discriminated-union surface-violation / coherence-triad / roster floor) + Gary `styleguide:<name>` **base-layer** seam in `_normalized_gamma_settings` (source-derived instructions/keywords WIN — protected invariant [[feedback_source_detail_to_gamma_conveyance_protected]]) + **unconditional 16:9 generation override REMOVED** (`_act.py`; library `dimensions` is sole determinant; anti-crop → publication-boundary follow-on) + 3 seed guides (2 Classic + 1 Studio). Full BMAD spine: GREEN-LIGHT 6/6 → dev RED-first → 3-lane `bmad-code-review` (found + closed a fail-open cluster) → 10-item RED-first remediation → **AC#5 live differential PROVEN** (Classic `classic-freeform-x-cards` vs Studio `hil-2026-apc-studio-image-card` → measurably different real Gamma output, 16:9 Studio spanned, no-vacuous-green; evidence `evidence/leg-a-styleguide-differential-20260701T164108Z/`) → **dual-gate party CLOSE** (Murat structural + Dan/CD + Vera/fidelity). 118 passed / 1 skipped, validator exit 0 offline, ruff clean. Codex shadow-monitor consulted at every gate (Poll 11 independently concurred on AC#5). **RIPE next-dev: `styleguide-retire-default-variant-pair`** (single binding still paid-dispatches an unbound fixture-B; spec pre-authorized retirement after the now-satisfied green live differential); then Legs B–E. **8 follow-ons** in `deferred-inventory.md §Gamma Styleguide Library Leg-A`. Full-repo `/harmonize` at session START = COHERENT (`reports/dev-coherence/2026-07-01-1027/`; pre-existing C3 import-break + 2 baseline-red tests unchanged). Records: greenlight-party-record + scope-addendum + story `gamma-styleguide-leg-a-library-spine.md`. **KG/ONBOARDING regen recommended** (new app/ files: `styleguide_library.py` + `_act.py` seam).

**2026-07-01 Update — CONCIERGE PRODUCTION SUBSTRATE arc COMPLETE (Leg-2 motion + Leg-3 clustering + Leg-4 asset/fidelity ledgers, all party-CLOSED + live-proven; autonomous /goal done-signal met).** On branch `dev/concierge-production-substrate-2026-06-29` (HEAD `594f838a`, origin 0/0). Three legs across the full BMAD spine (fully-spawned party per leg; live testing indispensable, no mocks; SPOC-is-the-goal guardrail held). **Leg-2** (`4e3b77a5`): per-run kira motion-artifact isolation (run-scoped `bundle_path` unconditional) — closes a real cross-run data-integrity defect; LIVE-proven via 2 real Kling renders + two-run coexistence through the continuation walk; party 5/5→4/4, 3-lane review 0 MUST-FIX. **Leg-3** (`7f8db3c9`): confirm-spike found clustering ALREADY CORRECT → **zero production code** (cleanest guardrail outcome); the non-waivable 07G VO↔on-screen per-sub-slide invariant live-proven on a real chunked slide (distinct perception per sub-slide + a trip-wire raising the real gate); party 6/6→verify-only 4/4; callback-on-cluster filed OPEN. **Leg-4** (`9866b42f`+`a7091081`): DONE-SIGNAL = UDAC anti-tautology **broken-asset HALT live-proven** (corrupted `g0-enrichment.json` → `udac.asset-stale` at node 07/gary, $0 before the paid Gamma call, discriminating control sailed, digest-based; all 6 Murat bars PASS; driver-plus-flag, zero prod code) + Irene `narration⊆source` fail-loud conflict-gate (flag `MARCUS_NARRATION_FIGURE_FIDELITY_ACTIVE` default-OFF; protects VO↔on-screen by blocking on source-vs-deck conflict; 3-lane review 0 MUST-FIX; teeth-witnessed on real gpt-5.5; frozen `figure_tokens` neck untouched). **FINAL party CONCURRENCE 6/6** (`3c88d90c`) = the explicit done-signal. **HONEST SCOPE: trust-GATED, NOT trust-COMPLETE** — the fidelity flag stays default-OFF until the precision+activation follow-on lands; narration positive-carry, word-form neck (frozen-neck governance), carrier bundle-validator, UDAC universality, callback-on-cluster all filed OPEN in `deferred-inventory.md`. **Side task** (`594f838a`): Gamma styleguide registry exemplars folded into the spec (briefing §8) + 2 draft artifacts tracked (seed, not runtime-active). Records: SESSION-HANDOFF §2026-07-01; story specs `concierge-leg{2,3,4}-*.md`; Codex shadow-monitor `marcus-leg2-through-leg4-shadow-monitor-2026-07-01.md`. **KG/ONBOARDING regen recommended** (≥20 app/ files across the arc); full `/harmonize` deferred to next START.

**2026-06-30 Update — CONCIERGE SUBSTRATE arc: coverage-interlock Leg-1 CLOSED + Leg-1b CLOSED + 2 production-hardening down-payments; 2 architectural guardrails codified.** On branch `dev/concierge-production-substrate-2026-06-29` (HEAD `2a878ca4`, origin 0/0). **(1) Coverage-assurance interlock (Leg-1) DUAL-GATE CLOSED path A** (`53eb4a85`) — a fail-loud-before-audio-spend coverage gate; BLOCK arm proven LIVE ($0 ElevenLabs), COVERED arm proven OFFLINE; honest numeral/figure-token facet; narration-figure-fidelity gap routed to Leg-4. **(2) Leg-1b DUAL-GATE CLOSED** (`097f7423`, party 5/5) — `warm_callback` Pass-2 authoring + Vera-R7 source-containment WITH TEETH (block-by-omission; audit rides the production-envelope carrier); grounded to strictly-prior teachable material; frozen `figure_tokens` neck READ-ONLY; LIVE close-bar real gpt-5 ALL_PASS ×2; a 3-lane `bmad-code-review` caught + remediated 4 MUST-FIX RED-first (crash-on-malformed-grounding, figure-gate bypass, both-flags role-clobber, silent partial-ship); flag-OFF by default; production activation = filed follow-ons. **(3) Down-payments:** unconditional Classic-branch `cardOptions.dimensions=16x9` (`27309271`, live-proven real Gamma 2400×1350/1.7778 — closes the default-run crop; strangler-fig Phase-0 of the future CD-owned Gamma Styleguide Library) + a fail-loud Descript publication-receipt asserting `composition.duration ≈ expected` before "published" (`3dd908f4`, live-proven on the real assembled Part-1 composition — closes the Underlord duration-0 detachment). **(4) Two binding architectural guardrails codified repo-wide** (CLAUDE.md + STATE-OF-THE-APP + session-START §0 + AGENTS.md + Cursor/Copilot mirrors): **the goal is the Marcus-SPOC runtime PRODUCT — concierge/proofing runs are off-the-books discovery vehicles, never a design target** (`10f541e3`); and **the production-envelope/bundle carrier that threads all state must be EXTRAORDINARILY ROBUST** (schema-validated/versioned/append-only/fail-loud/single-SSOT/resumable-both-walks) because every deferred consumer piles onto it (`4c59180d`). STATE-OF-THE-APP reconciled forward (`930b3518` — the braid COMPLETE + P5/directed-voice/enhanced-VO arcs shipped; frontier = the concierge substrate arc). **NEXT: Leg-2 (motion B2/B3 live)** → Leg-3 → Leg-4. Records: SESSION-HANDOFF §2026-06-30(d)/(e); party record `concierge-substrate-party-record-2026-06-29.md` (Rounds 1-8). **KG/ONBOARDING regen recommended** (≥10 app/ substrate files changed).

**2026-06-29 Update — ENHANCED-VO arc DONE (clip-level); master consolidated.** On the now-consolidated `master` (`96309e10`): two BMAD stories shipped via the full spine (party green-light → create-story → dev RED-first → 3-layer code-review → party CLOSE). (1) **enhanced-vo-1** (`d4455e4f`) — a stable per-segment `slide_key` identity join replacing the fail-open ordinal role→slide match (derives source-slide identity via `slide_briefs.source_ref` → Pass-1 plan-unit; M3-clean; byte-identical off). (2) **enhanced-vo-2** (`077d68e2`) — a NEW pure leaf `app/specialists/_shared/voice_provider_text.py`: a TAG-ONLY `eleven_v3` provider-text compiler (frozen 8-tag allowlist; byte-exact `strip_tags(provider)==canonical` firewall) + four sha256'd text channels (canonical/provider/display/captions) + captions zero-tag-leak hard gate + model-aware branch on `effective_model==eleven_v3`; v2 numeric mapper frozen. Live-proven (real eleven_v3 two-arm A/B + ASR no-leak PASS). **Finding:** operator blind A/B = **B but SUBTLE** (intonation>pace; below the audition "confidential-tone" effect) → the v3 tag channel is a real-but-subtle control surface, not sufficient dramatic expressiveness alone. ⚠️ Descript publish FAILED (clip-level A/B only; full-mix cross-confirm owed). Records: SESSION-HANDOFF §2026-06-29 close; `_bmad-output/planning-artifacts/enhanced-vo-party-consensus-2026-06-29.md` + `enhanced-vo-retrospective-2026-06-29.md`. **KG/ONBOARDING regen recommended** (≥10 substrate files + Epic close).

**2026-06-24 EVE Update — figure-citation caveat fix DONE + clustering re-activation arc opened (Phase-1a green-lit).** On `fidelity-perception-arc-2026-06-19`: (1) `pass2-figure-citation-gate` closed (`5cf0684`, NEW CYCLE Codex→Claude T11, party-CLOSE 3/3) — a shared `figure_tokens` extractor (Irene + G5 share one definition) + input-side brief-figure redaction + output-side `irene.pass2.figure-contradiction` post-check, so narration cannot cite a figure absent from the chosen variant's slide. (2) Dev-plan revisit filed (`forward-development-sequence-2026-06-24.md` + autonomous goal `goal-clustering-followalong-trial.txt`): VO follow-along to be fixed by chunking dense slides via the existing cluster/grouping model (Epics 19-21/20c/23). (3) **Believed-green save:** clustering verified DORMANT — ran for real in the old pipeline (April manifest 74 cluster fields) but the LLM-first Pass-1 rebuild dropped cluster emission; downstream cluster machinery survives unexercised. Phase 1 (re-wire Pass-1 emission → re-verify downstream → small adds) green-lit 4/4. (4) Descript API + asset-upload client tucked away (parked). Next session: operator launches the goal. Records: SESSION-HANDOFF.md §2026-06-24-EVE; STATE-OF-THE-APP §EVE banner.

**2026-06-21 Update — VISION PERCEPTION NOW LIVE (gpt-5.5) + reading-path patterns catalog at operator-review checkpoint.** Two BMAD-governed advances on `fidelity-perception-arc-2026-06-19`: (1) **`vision-perceiver-real`** — the vision specialist (`app/specialists/vision/provider.py`) was a fixture-backed contract (`vision-fixture-v1`, unconfigured endpoint); rewired to a real live **gpt-5.5** multimodal call via the house `ChatOpenAI`/cascade (party 5/5 green-light → dev agent → 3-layer `bmad-code-review` → 4/4 CLOSE; `gpt-5.5` added to registry/pricing/cascade; live AC-8 over the 6 frozen PNGs). (2) **Reading-path patterns catalog** — party-mode design round ratified a HYBRID (c) pattern-ID architecture (deterministic geometry = authoritative scan-order producer + default; perceiver in-band hint; catalog-guided gpt-5.5 escalation on ambiguity; default = `top_down` position-order, not Z). A live gpt-5.5 scan of 54 operator slides produced a v0-draft catalog (`reading-path-patterns-catalog.md`): `_looks_z` over-claims z (43/54, 24 false-pos); admitted `image_dominant` (15) + `diagram_driven` (9) by rubric; `two_up`/`triptych`/`grid` got zero fits (not admitted). Awaiting operator review rounds → hybrid classifier build → P2-4b calibration. Governance: no-mocks/real-live directive; anti-pattern G1 (fixture-backed contract mistaken for live capability) + deferred-inventory `believed-green-tracker-audit`. Records: SESSION-HANDOFF.md §2026-06-21; STATE-OF-THE-APP §11.

**2026-06-19 Update — BETA ARC: error-free production run ×2 + Marcus conversational SPOC demonstrating a–g ×2 (branch `trial/4-2026-06-12`).** Goal-driven autonomous session (plan→run→repair→rerun). **Planning (party-reviewed):** 5-agent BETA-scoping party (8 decisions D1–D8); `beta-spec-2026-06-19.md` + `beta-trial-sequence-charter-2026-06-19.md`. **Substrate (each tested/ruff/lint-imports-13/pushed):** S0.1 crash-taxonomy — `ModeMismatchError`→error-pause (`5c9cbea`); S0.2 ingestion-report wired to bundle manifest (`6497514`); S0.3 G2B/G4A cards carry structured variant/voice candidates (`a0d85a8`); **T5b `select` verb** — surgical picker overlay, party-ratified Option B, `edit` full-replace preserved (`c1fc663`); T5a-F3 voice re-route — operator pick threads to synthesis (`3b5eec0`); S0.4 picker-contract ratchet (`b87bc2d`); **S0.4 flake-budget auto-retry** — irene pass-2 LLM variance absorbed in-dispatch, the error-free keystone (`e9d20be`); **Marcus SPOC** conversational layer `app/marcus/cli/marcus_spoc.py` narrating a–g (`9ec7a40`). **Validated live:** picker binding (T5a rerun `710684c0` — operator-selected Sarah synthesized at node 12); **error-free production run ×2** (`b7919f65`+`bb76170c`, no substrate change between, auto-retry kept variance non-blocking); **Marcus SPOC drove a–g to error-free completion ×2** (`e2291039`+`74f72a4c`). **Scope (honest):** SPOC is an MVP structured conversational surface (free-form NL/LLM dialogue deferred per operator directive); non-default-voice-to-completion gated on open `beta-voice-select-wpm-qa-interaction` (G5 WPM voice-agnostic); (c)/(d) narration thin; (f) motion review-only (synthesis deferred). Records: `marcus-spoc-ag-demonstration-2026-06-19.md`, `beta-error-free-twice-milestone-2026-06-19.md`, SESSION-HANDOFF.md §2026-06-19. Frozen corpus: `tejal-apc-c1-m1-p2-trends`.

**2026-06-13 Update — WAVE 0 ROBUSTNESS ARC: 4 of 6 items landed on the certified frozen engine (branch `trial/4-2026-06-12`).** Post-certification roadmap (`roadmap-consensus-2026-06-12`) opened. Engine FROZEN for Trial A; all work is correctness/honesty hardening with zero production-walk behavior change. Landed this session (each: quick-dev spec → 3-lane code review blind/edge/acceptance → push): (1) **phantom-delta silent-audio gap CLOSED** (`ebe0c3f`) — a segment-manifest delta with no matching narration joined empty-text, silently skipped TTS yet counted as G5 coverage; enrique now refuses pre-spend (`elevenlabs.join.empty-narration-text`) + G5 drops pre-coverage so `CoverageGapError` names the silent slide; detection single-homed in `narration_join.phantom_segment_ids`. (2) **dp-v1.2 hygiene mini-batch** (`6b4c9c4`) — six review riders: enrique `DEFAULT_BUNDLE_PATH` retired (fail-loud `elevenlabs.bundle.path-missing`), dead `_act_with_trail`+4 orphaned quinn_r helpers deleted, ninth-seam regex generalized, EXCLUSIONS module-qualified + reverse-existence pin, (11B,elevenlabs) allowlist row machine-tied to the voice-HIL rider, join-test honesty fixes. (3) **motion-receipts diagnosis** (`e9edc61`, HIGH confidence, case file at `_bmad-output/implementation-artifacts/investigations/motion-receipts-cycle-5-6-investigation.md`) — kira node 07E ran in both certified runs but was INPUT-STARVED (no manifest producer for a motion plan); `_load_motion_plan` empty-default → zero-iteration loop → `motion_receipts: []` + `kling.dispatch.ok` + `provenance: real`; four-layer silence (no producer / silent default / folded groundless G2F gate / tolerant compositor). Certification stands for the narrated-deck deliverable; motion leg is structurally unproven (fix = motion data-plane arc, dp-v2-class, post-Trial-A). (4) **taxonomy re-base — live-path tranche** (`37f8323`) — GaryActError/ReceiptParseError/BundleParseError/KiraActError/FTRParseError re-based onto `SpecialistDispatchError` (crash→error-pause; base is RuntimeError-derived so all handlers preserved); gary fabricated slide-01 roster KILLED (`gamma.slides.starved`); EXCLUSIONS 18→13. Edge-review caught the next priority: `BuilderInputError` (node 06) is the last live-walk dispatch leg NOT in the error-pause family — node-06 starvation still kills a trial. Remaining WAVE 0: storyboard-correctness (awaiting operator cycle-6 content review for the glitch list; VO-slide sync = item #1 = `b-manifest-join-lossiness` rider) → Trial A (literal text/visual slides + clustering, frozen-engine baseline). Records: SESSION-HANDOFF.md §2026-06-13. Specs at `_bmad-output/implementation-artifacts/spec-{phantom-delta-silent-audio-gap,dp-v1-2-hygiene-mini-batch,taxonomy-rebase-live-path}.md`.

**2026-06-12 Update — 🏆🏆 FIRST COMPLETE PRODUCTION RUNS: FULL PIPELINE G0 → COMPOSITION HAND-OFF, CERTIFIED.** Two-session arc (2026-06-11/12) closed the Trial-3 campaign milestone. Six trial cycles total; the segment-data-plane SCP's fix→fresh-launch method ran four party-designed + party-reviewed remediation rounds (S0-S4p2 data plane; dp-v1.1 node-08 grounding + storyboard-B body after the cycle-4 sepsis-confabulation/08B-crash pair; G3 fold-target publisher roster fix in-situ; dp-v1.2 audio arc after the cycle-5 G5 crash cluster — enrique grounding + fabricated-roster kill + error-taxonomy re-base; economics deterministic-marker fix). **Cycle 5 (`036e7ff8`)**: first complete production run — operator-approved ONLINE Storyboards A and B (criterion 7 proven both sides), 6 real ElevenLabs narration segments, G5 QA on real data, compositor assembly bundle + DESCRIPT guide + desmond hand-off brief, status `completed`. **Cycle 6 (`f8da20ae`)**: FRESH CERTIFICATION E2E under the operator's blanket delegation — G0 → `completed` with ZERO ad-hoc fixes, 20/20 provenance:real, both storyboards auto-published online, per-gate delegation-exercise log at the run dir. S5 criteria 1-7 ALL CLOSED. Substrate now enforces: manifest-wide grounding audit (PIN-G1, shrink-only allowlist), specialist error-taxonomy ratchet (PIN-AUD-3T), per-node envelope keying, fail-loud + error-pause/recover on the live path, shared narration-join policy module, storyboard publisher keyed to pausing gates. Key inventory debt: voice-HIL fold defect, 18-class tagged-error re-base, measured audio durations, dp-v2 self-edge vocabulary, phantom-delta silent-audio guard. Records: SESSION-HANDOFF.md §2026-06-12; SCP 2026-06-11 segment-data-plane closure paragraph. Commits `f3185b4 c6f9d7a 6dc7f94 8b306b1 4a654d5`.

**2026-06-11 Update — 🏆 TRIAL-3 LIVE-FIRE: FIRST MULTI-GATE CROSSING IN PLATFORM HISTORY; ATTEMPT-4 ALIVE AT G1.** Two-day live-fire session (2026-06-10 readiness verification + /goal confidence scrub; 2026-06-11 trial launches). Attempt-3 ran across 3 instances and harvested **9 findings**: 5 substrate fixes landed Claude-direct under party-mode-ratified guardrails (G0 composer no-primary template fix `bb81b6f`; Texas dispatch cwd fork + exit-10 bundle-discard `919b16d`; irene_pass1 roster adoption + `_pause_at_gate` extraction `cd31b33`; resume-walker multi-gate pause wiring `d727248` — closing the known-deferred `7a-2-deferred-resume-mode-multi-gate-pause` follow-on; **no live trial had EVER crossed gate-to-gate before**), 1 config fix (gpt-5.4 pricing row `08d5e34`), 3 filed to deferred-inventory (pause-write atomicity; max_specialist_calls cap semantics — killed instance `a0d31fc0` via quinn_r→kira starvation AFTER it achieved the historic G1→G2C crossing; CD directive-validator mismatch 🔴 — systematic 2/2, blocks attempt-4 resume). **Trial `50b7d353` (attempt-4) is ALIVE, cleanly paused-at-G1, and is the first structurally-completable trial ever** — resume after the CD fix with `--max-specialist-calls 12` per segment. MANDATORY before attempt-5: batch `bmad-code-review` of the 5-fix delta (party-mode guardrail #3). Operator playbook at `trial-3-attempt-3-operator-playbook.md`; interim trial record in `docs/trials/cross-trial-learnings.md §Trial-3 attempts 3-4`; formal postmortem + Epic-34 retrospective pending next session. Marcus runner battery: 133 passed (3 pre-existing env/staleness failures, stash-attributed).

**2026-05-22 Update — 🎉 EPIC 34 §02A DOWNSTREAM-CONSUMER SCHEMA COHERENCE FULLY COMPLETE: 7/7 STORIES DONE.** Single-session marathon closed via Quinn-synthesis Option 5 ("Round-Trip First, Then Harmonize") — story order inverted so integration ratchet test lands BEFORE substrate harmonization, with temporary in-tree translator scaffold bridging during the harmonization arc + deleted at Epic close. Closure ledger: 34-1 forensic integration ratchet @ bc477ed (T11 PASS-WITH-NITS; tests/integration/test_section_02a_to_wrangler_subprocess_roundtrip.py + forensic-fixture binding sha256 `351a57f...`); 34-2 wrangler 6-role union + excluded_reason + ignored-row filter + cross-field invariants @ cabf850 (T11 PASS / 1 DEFER-NIT); 34-3 §02A src_id→ref_id rename + J-A1(a)/(b) cli_adapter completion (run_id thread-through + model_resolution_trail.json sidecar) @ 08dfc4a (T11 PASS / 1 PATCH applied — `_accept_legacy_source_id_key` docstring); 34-4 wrangler metadata.json sme_refs[] additive emission + Story 34-1 ratchet extension per AC-34-4-A-EXT @ 16e36f7 (T11 PASS / 0 NITs); 34-5 translator-shrinkage carrier ratchet @ e59b0f4 (T11 PASS / 0 NITs); 34-6 legacy `app/marcus/orchestrator/directive_composer.py` DELETION + 7-file test rewire/delete (2 DELETE + 5 REWIRE) + 2 structural-orphan cleanups @ 55a4d25 (T11 PASS / 0 NITs; substrate-audit at session-START predicted ALL 7 hit counts EXACTLY 20/23/5/2/2/2/2); 34-7 translator deletion + A23/P5 anti-pattern entries + Epic close ceremony @ 1b59487 (T11 PASS / 0 NITs; AC-34-7-H forensic grep-sweep PERFECT). Two doctrine entries filed at `docs/dev-guide/specialist-anti-patterns.md`: **A23** "Two-source-of-truth vocab fork latent across N-year-old integration boundary" (sibling-to-A17 but vocab-forked at untested boundary) + **P5** "Schema-coherence Epic without integration-boundary green test is governance failure" (process-tier — any producer-consumer Epic MUST install integration-boundary green test as FIRST story per AC-34-N-EXT extension pattern). Deferred-inventory closures: section-02a-downstream-consumer-compatibility-systemic-drift + J-A1(a) + J-A1(b) all CLOSED at commit range bc477ed..1b59487. Substrate state: §02A composer emits `ref_id` natively (renamed from `src_id`); Texas wrangler accepts 7-role union {primary, supporting, ignored, validation, supplementary, visual-primary, visual-supplementary} + excluded_reason enum + cross-field invariants + sme_refs metadata; no temporary translator; no legacy directive composer; integration-boundary green test installed and EXTENDED through 34-4 + ratified through 34-7's direct-ratchet simplification. **Trial-3 attempt-3 LAUNCH READY** on fully harmonized substrate. Per CLAUDE.md §Deferred-inventory governance #1, `bmad-retrospective` on Epic 34 is a binding consultation point — operator-discretion whether retrospective precedes Trial-3 launch or follows it.

**2026-05-07 Update — SLAB 7C BROWNFIELD MIGRATION FULLY COMPLETE.** All 36 Slab 7c dev-stories closed across 7 Waves (0/1/2/3/4/5/6) under NEW CYCLE discipline (Claude pre-author → Codex T1-T10 → Claude T11). V7 v2 Murat triple-condition gated parallel-dispatch successfully throughout the marathon; AMEND-7c percentage-threshold gap-rate prevented noisy closeouts; AMEND-7d-iii STOP-on-TW-7c-6-fire branch never triggered (ceremony PASS at synthetic-zero-fail-reference baseline). Trial3Transcript schema landed (FR-7c-51); Trial-3 readiness PASS for development closeout. SG-1+SG-2+SG-3+SG-4 standing-guardrails AGGREGATED structurally enforced at slab close. TW-7c-1 (gap-rate detection) verdict: not_fired @ 1.35% combined gap rate across AUDIT-AC trio. TW-7c-6 (parity flake) verdict: not_fired. D12 cross-slab governance protocol three-line set landed at 7c.21: invariant-preservation note + anti-pattern A19 (class-definition substring scanners go stale) + §Slab 7c migration-guide section. 7c.21a strict-last cleanup closed Epic 3 in-place via 7a+7b+7c retirement record + live-dispatch wiring in `run_cache_hit_harness.py` + `run_5_api_smoke.py` (default fail-closed; `--live-runs N` / `--live` operator-gated). **Critical-path next:** operator-driven Gate-2 ceremony for 7c.21 (`bmad-retrospective` + party-mode-ratified mapping-checklist row-flips per FR-7c-42 + per-tripwire firing-rate review per FR-7c-41) → Trial-3 launch (`_bmad-output/implementation-artifacts/trial-3-readiness-checklist.md`) → Epic 15 (Learning) reactivation post-Trial-3 PASS. 4 housekeeping stories pre-authored at lookahead_tier=1/2 for parallel post-Gate-2 dispatch (digest-helpers extract / scanner-staleness AST-rewrite / specialist-side producer models / legacy sidecar cleanup). 3 new anti-pattern entries landed at session-WRAPUP (A20 cross-package helper duplication under import-isolation contracts + P1 facilitator-skill-as-dev-agent-action + P2 DISMISS-thread-as-anti-pattern-signal).
**2026-04-05 Update:** Epics 13 and 14 are now complete, tested, and internally reviewed. Production prompt packs now split by workflow template: `production-prompt-pack-v4.1-narrated-deck-video-export.md` for standard narrated runs and `production-prompt-pack-v4.2-narrated-lesson-with-video-or-animation.md` for motion-enabled narrated runs. `DOUBLE_DISPATCH` remains an inline branch in either pack.
**2026-04-08 Update:** Storyboard B and downstream motion contracts are now explicit. Motion segments keep the approved still slide in `visual_file`, the approved MP4 in `motion_asset_path`, Storyboard B renders both for review, and motion-first narration should orient briefly to the slide and then speak primarily to the visible action in the approved clip. First complete motion-enabled production run `C1-M1-PRES-20260406` has been executed through all 15 prompts of prompt pack v4.2, with assembly bundle fully packaged for Descript composition.
**2026-04-11 Update:** **Desmond** (`skills/bmad-agent-desmond/`) — memory-backed agent for Descript-specific operator steps, local Descript doc cache refresh, and mandatory **Automation Advisory** on APP assembly handoffs; `DESCRIPT_API_KEY` used only via `.env` for API verification/tooling (never commit).

**2026-04-12 Update:** Cluster dispatch trial (Storyboard A) validated end-to-end — MVP gate PASSED. Wave 1 foundation complete (20b-3, 22-1, 21-5). Wave 2 Irene intelligence iteration active: Epic 20c (template library, content-aware selection, density intelligence, master arc composition, potential Pax/Lens agents) with A/B trial methodology (`docs/workflow/operator-script-v4.2-irene-ab-loop.md`). Pass 2 initially in structural-coherence-check mode (now superseded — Epic 23 shipped; live mode is cluster-aware-refinement). PRD expanded to FR125. 229 cluster tests passing at time of entry.

**2026-04-14 Update:** **Wave 2 REPLAN** (Party Mode consensus). A/B trials SKIPPED — replaced by experience-profile-driven trial runs. Three parameter families identified: Run Constants (operational), Narration-time (Irene script controls), Assembly-time (Compositor timing). **Creative Director (CD) agent** planned as second pillar alongside Marcus — owns creative frame, parameter orchestration, gate-triggered review ("is this right?" vs Quinn-R's "is this good?"). CD uses Option B interaction: params for deterministic executors + params AND structured briefs for LLM interpreters. Two extreme experience profiles (Visual-Led + Text-Led) as proof of concept. 8 new stories added (20c-7 through 20c-14): parameter registry, assembly timing, narration expansion, CD agent, directive schema, profiles, resolver, E2E validation. 20c-2 marked done. 20c-4/5/6 + 23-1 deferred — reactivate after profile runs reveal gaps. Master parameter directory (`docs/parameter-directory.md`) planned as ongoing reference for CD, specialists, and operators.
**2026-04-14 Closeout Update:** Wave 2B execution advanced from planning into implementation. `20c-7` and `20c-8` are complete; `20c-9/10/11/12` are in-progress with concrete artifacts now present: expanded narration profile controls, CD skill scaffold (`skills/bmad-agent-cd/`), creative directive schemas (`state/config/schemas/creative-directive.schema.*`), experience profile targets (`state/config/experience-profiles.yaml`), strict `slide_mode_proportions` run-constants validation, and a dedicated creative directive validator (`scripts/utilities/creative_directive_validator.py`). Immediate next step: `20c-13` profile resolver wiring.
**2026-04-15 Review Closure Update:** `20c-3`, `20c-13`, and `20c-14` are now formally reviewed clean and complete. Static density resolution is profile-driven through the canonical experience-profile registry, Marcus/profile contract hardening is in place across the Pass 2 seam, proof artifacts now match canonical profile state, and the full repo suite is green at `670 passed, 1 skipped, 27 deselected`. Immediate next focus: formally review the remaining Wave 2B foundation stories (`20c-9` through `20c-12`) before reactivating deferred downstream work.
**2026-04-15 Wave 2B Review Closeout:** `20c-9`, `20c-10`, `20c-11`, and `20c-12` are now formally reviewed and closed. Review fixes hardened `creative_directive_validator.py` to use repo-anchored schema/profile paths and expanded the Creative Director contract seam so the full 11-key `narration_profile_controls` surface is aligned across narration config, parameter registry, creative-directive schemas, and `experience-profiles.yaml`. Focus can now shift to choosing the next deferred/reactivation slice (`23-1` or `20c-4/5/6`).
**2026-04-15 Epic 23 Reactivation Update:** `23-1` is now in implementation as a disciplined BMAD contract-hardening story. Irene Pass 2 is no longer treated as structural-coherence-only in the A/B workflow docs; the live target is cluster-aware refinement with head/interstitial calibration, tension pivots, cluster-boundary seams, and cluster-level behavioral-intent coherence explicitly in scope.
**2026-04-15 23-1 Review Remediation Closure:** `23-1` is now formally reviewed clean and closed to done after a late review-patch pass closed five findings: pivot-only tension bridges, pivot spoken-cue enforcement, master behavioral-intent subordination checks, bridge-type registry/doc parity, and tracker-state reconciliation. The focused closure suite is green at `147 passed`; the next approved implementation target is `23-2`.
**2026-04-15 23-2 Formal Review Closure:** `23-2` is now formally reviewed clean and closed to done after a review-fix pass closed three findings: interstitial master derivation now resolves from the cluster head row, the story AC now matches the authoritative cluster arc schema order (`establish -> tension -> develop -> resolve`), and contract tests now assert `severity` plus `description` for `G4-16..19`. Closure validation is green at `151 passed`; full repo regression is green at `693 passed, 1 skipped, 27 deselected`. Immediate next approved implementation target: `23-3`.
**2026-04-15 Epic 23 Closure:** All three Epic 23 stories (`23-1`, `23-2`, `23-3`) are formally reviewed clean and closed. G4-16 through G4-19 are now codified in `g4-narration-script.yaml` (19 total G4 criteria). Cluster-aware narration rules are production-ready: dual-channel grounding, gate extension, bridge cadence adaptation. Wave 3 (codify) is complete. Next: Wave 4 downstream implementation starting with `22-2`.
**2026-04-16 Closeout Update:** Wave 4 stories `22-2` (Storyboard B cluster view with script context) and `20c-15` (profile-aware parent slide count / runtime estimator, `slide_count_runtime_estimator.py` + `parent_slide_count` propagation) are **done** per `sprint-status.yaml`. Prompt pack **v4.2f** adds extraction-completeness validation and per-dimension gate evidence; trial run `C1-M1-PRES-20260415` paused after Prompt 4 (stub `extracted.md`) — **not resumable**; next operator focus is a **fresh trial** per `next-session-start-here.md`. Source Wrangler agent vision captured in `_bmad-output/planning-artifacts/source-wrangler-agent-vision.md` (future epic). BMAD **done-story coverage** audit script: `maintenance/audit_done_bmad_coverage.py` (optional `reports/audit-done-bmad-coverage.txt` output).

**2026-04-17 Update — Epic 27 Texas Intake Expansion + Shape 3-Disciplined Retrieval Architecture.** Story 27-1 DOCX provider wiring BMAD-closed (python-docx 1.2.0; Tejal cross-validation 100% key-term coverage; full suite 1036/2/0). **Three-round party-mode debate ratified Shape 3-Disciplined retrieval architecture** (Dr. Quinn's knowledge-locality partitioning): Tracy owns editorial (intent + three-tier acceptance criteria + provider choice); per-provider adapters own mechanical query translation + fetch + iteration + normalization; thin Texas dispatcher owns routing + cross-validation merger. Cross-validation is v1 first-class (scite + Consensus convergence_signal). **Story 27-0 Retrieval Foundation** opened + green-lit (ratified-stub → full BMAD spec → unanimous Option Y for Python MCP client: hand-rolled JSON-RPC-over-`requests`, NOT `mcp` PyPI pre-1.0). 27-0 blocks 27-2 / 27-2.5 / 27-3 / 27-4 / 28-1 Tracy. New **shape classification**: retrieval-shape providers (scite / Consensus / YouTube / image) use `RetrievalIntent` contract; locator-shape providers (DOCX done, Notion / Box / Playwright) keep existing directive shape — operator-direct is degenerate case of Shape 3 contract; both shapes route through the same dispatcher. Epic 27 roster: 9 stories, ~31 pts. Critical path: 27-1 done → 27-0 → 27-2 → 27-2.5 → unblocks Epic 28. **Story 28-1 Tracy** reshape banner added (points drop 9 → ~7; scite-DSL knowledge relocates to 27-2 scite adapter).

**2026-04-29 Update (HYBRID CLONE) — Slab 7b PRD RATIFIED + Wave 0 foundational artifacts LANDED.** Slab 7b PRD authored end-to-end via `bmad-create-prd` workflow with 9 party-mode rounds (R1-R9; 4 voices/round; 0 re-opens; 4/4 unanimous on R9 close). Slab 7b activates eleven specialist body roles (Texas/Quinn-R/Vera Class-A hardening; Irene Pass-1 Class-B refresh; Tracy Class-C+ port-shape with sidecar emission; Gary/Kira/Wanda/Enrique Class-C API-bound port-shapes; Dan Class-D1 LLM-greenfield; Compositor Class-D2 pipeline-greenfield) into the Slab 7a orchestration substrate (CLOSED 2026-04-29 at `95c81b0`). 26 new FRs (FR88-FR113); 24 new NFRs (NFR-T9..T12 / NFR-CG12..CG20 / NFR-I9..I13 / NFR-OD3..OD6); 10 new ADRs (D14-D23); SG-4 (BMB sanctum alignment per body) added as 4th standing guardrail with closed-allowlist option-b path. 5-class taxonomy A/B/C/C+/D1/D2 per D14. **Class-D2 sidecar variant (Compositor) is canonical inside BMB regime per D20 — NOT exception.** Implementation-readiness Steps 1-3 closed READY-WITH-MINOR-AMENDMENTS-AND-NAMED-PRECONDITIONS. Three named preconditions (foundational artifacts FR108-FR112 + sandbox-AC inventory FR107 + scaffold-v0.2-D2-pipeline FR111) ALL LANDED in Wave-0 atomic-merge commit `9ed6fcb`: 6 artifacts (`docs/dev-guide/bmb-sanctum-alignment-checklist.md` 8-section TOC + `docs/dev-guide/sanctum-exception-categories.json` closed allowlist with sidecar-hook initial entry per Cora precedent + `docs/dev-guide/operator-control-parity-template.md` form-not-prose template + `docs/dev-guide/scaffolds/scaffold-v0.2-D2-pipeline/` 7-file scaffold dir + `skills/bmad-agent-cora/SKILL.md §Sanctum exception` anchor with HTML grep marker + `docs/dev-guide/migration-ac-sandbox-inventory.json` +5 entries gamma/kling/elevenlabs/wondercraft/dan-api-tbd-pending). PRD errata addendum at commit `ddcd1b1` captures P0 path-sweep findings (FR111 path correction + FR101 contract realignment + SanctumParityTestBase Wave-1 deliverable + tests/parity/per_specialist subdir TBD). 696 passed/19 skipped regression baseline holds (no regression). **Wave 1 (Class-A hardening: Texas/Quinn-R/Vera) UNBLOCKED** — next workflow is `bmad-create-epics-and-stories` to produce `epics-slab-7b-specialist-activation-eleven.md` with 1 Epic + 12 stories. NFR-CG17 deviation noted: FR107 sandbox-AC inventory authored by Claude (not Codex) per R1 party-mode scoping consensus — Wave 0 is foundational scaffolding pre-Wave-1, NOT inside Codex's Class-C/C+ port-shape scope; Codex deployment activates at Wave-1 story open.

**2026-04-25 Update (HYBRID CLONE) — Story 2a.2 BMAD-CLOSED + M1 ACCEPT-WITH-GAP CACHE-HIT-RATE CLAUSE RETIRED.** First REAL LLM-invoking specialist migration on the LangChain/LangGraph stack landed. Irene Pass 2 9-node scaffold runs live against `gpt-5.4` via `app/specialists/irene/{__init__,graph,state,model_config,expertise/README}` (`_act` 64 LOC vs AC-B 150 LOC ceiling; 4 deterministic helpers — sanctum digest sorted-by-as_posix + reference bundle + sort_keys+ensure_ascii+separators-pinned envelope serialization + JSON-fenced response parser; Winston discriminator-check on `_act` trail-read). Cache-hit-rate harness retargeted at Irene; T7 measurement: prompt_tokens stable at 9399 across N=10 (>>1024 MF2 floor); **median[2:]=95.33%** (vs 60% MF1 threshold; +35.33pp slack); MF6 sanctum lock-and-verify pre/per/post=0/0/0. Wall-clock 230s. Cost ~$0.30. T8 dual-path regression: real-key 361/5/0 (+40 margin above 321 floor); placeholder-key 360/7/0 (SF3 anti-erosion verified — both `@pytest.mark.llm_live` tests auto-skip on placeholder sentinel). 28 tests across 11 files. Ruff clean; import-linter 3/3 KEPT (Irene C3 ignore_imports row added at T2 per A12 procedural-coupling pattern). Doc deliverables: NEW [`docs/dev-guide/sanctum-reference-conventions.md`](dev-guide/sanctum-reference-conventions.md) (MF7 — activation-baseline-vs-steady-state epoch split); [`specialist-anti-patterns.md`](dev-guide/specialist-anti-patterns.md) augmented (A9 second example + A10/A11/A12 NEW); [`langgraph-migration-guide.md §12`](dev-guide/langgraph-migration-guide.md) real-Irene worked example. Pre-T1 sanctum archived to `_bmad/memory/_archive/bmad-agent-content-creator-pre-2a2-2026-04-24/` (46 files preserved); active sanctum empty per D2 SYNTHESIS verdict. 4 party-mode waypoint reviews honored (T2/T4/T7/T8) + G6 layered self-review (3 PATCH applied / 2 DISMISSED / 4 DEFERRED). FRs closed: FR9–FR12 (per-specialist) + FR15 (first sanctum cold-read) + FR16 (first resolution-trail) + **FR54** (cache-hit-rate ACTIVATED + MEASURED). Slab 2a momentum: 2a.1 ✅ → 2a.2 ✅ → 2a.3 (Kira motion) next → 2a.4 (Texas) → Slab 2b.1 TEMPLATE batch (gated on filing A12 generator-auto-emit follow-on as 2a.1 defect story before 2b.1 opens). Deferred-inventory: 1 CLOSED + 4 NEW (net 17 named follow-ons).

**2026-04-22 Update (HYBRID CLONE) — LangChain + LangGraph Migration Planning Complete.** On the hybrid clone (`dev/langchain-langgraph-foundation` branch, `origin=course-DEV-IDE-with-AGENTS-hybrid`, `upstream=course-DEV-IDE-with-AGENTS` read-only), the full planning chain for the re-platform migration is now complete in one session: (a) implementation-readiness report (`_bmad-output/planning-artifacts/implementation-readiness-report-2026-04-22.md`) — READY-WITH-MINOR-AMENDMENTS verdict, 5 findings staged; (b) migration architecture (`_bmad-output/planning-artifacts/architecture-langchain-langgraph-migration.md`) — 8/8 steps, 13 decisions locked (D1 sanctum hybrid / D2 model cascade / D3 HIL tamper-evidence / D4-D13), party-mode Round 1 green-lit with 9 amendments, 15/15 substrate invariants preserved; (c) epics + stories (`_bmad-output/planning-artifacts/epics-langchain-langgraph-migration.md`) — 9 epics / 56 stories / ~184 pts with M1–M5 acceptance bars expressed as story-level ACs. All 65 FRs + 43 NFRs have per-story coverage. PR-R (primary Sprint #1 Marcus dispatch reshaping) staged as forward-port convergence in migration-guide §8. Authoritative NFR count reconciled 38→43. Ready for Slab 1 Story 1.1a kickoff (Runtime Substrate Environment + Dependencies — uv venv + core deps + lockfile) per Amendment F strict-serial split 1a→1b→1c. Legacy primary-repo epics.md / architecture.md / prd.md remain frozen reference; migration artifacts are hybrid-scope and coexist under distinct filenames. Primary repo continues production-shift work unchanged per Decision 2 (bounded big-bang in clone).

**2026-04-18 Update — Story 27-0 Retrieval Foundation BMAD-CLOSED.** Shape 3-Disciplined foundation landed: 8 new retrieval modules under `skills/bmad-agent-texas/scripts/retrieval/` (`contracts`, `base`, `dispatcher`, `mcp_client`, `normalize`, `refinement_registry`, `fake_provider`, `provider_directory`); schema v1.1 additive bump (`extraction-report-schema.md` + new `SCHEMA_CHANGELOG.md` gate artifact); audience-segmented `skills/bmad-agent-texas/references/retrieval-contract.md`; `--list-providers` CLI on `run_wrangler.py`; repo-level `.cursor/mcp.json` + `.mcp.json` scite + Consensus URL entries; CLAUDE.md pointer; Marcus `external-specialist-registry.md` breadcrumb. **Post-green-light operator amendment**: Provider Directory fold added AC-B.8 / B.9 / T.9 / T.10 / T.11 — runtime `list_providers()` surface with 16 entries (11 locator ready/ratified + 5 retrieval including `openai_chatgpt: backlog` forward placeholder per operator directive). Tests +70 collecting (target +34); full suite **1106 passed / 2 skipped / 0 failed / 2 xfailed**. Ruff clean on all 27-0 code. Gates: party-mode implementation review 3 GREEN + 1 YELLOW→GREEN; bmad-code-review layered 5 MUST-FIX + 9 SHOULD-FIX applied, 4 DISMISSED with rationale (AC-B.7 literal dispatcher-wiring deferred to 27-2 per anti-pattern #3 shape-separation; dual-emit writer + log-stream parity + parametrized schema-compliance cascade-deferred), ~22 NITs logged. **UNBLOCKS 27-2 scite adapter and 27-2.5 Consensus adapter.** Forward design note captured in user memory: three distinct parameter knobs for future work — enrichment degree (Irene aspirational depth beyond SME), gap-filling (derivative-artifact content demands), evidence-bolster (corroboration of existing claims via cross-validation).
**Current Implementation Status:** Use the dated update above as the source of truth for epic completion; the detailed historical implementation notes below remain as project chronology.
**Implementation Status:** Epics 1-14 + SB all COMPLETE (75 stories). Epics 19-21 + 23 COMPLETE (cluster schema, Irene planning, Gary dispatch, cluster-aware narration — 20 stories). Epic 20c IN-PROGRESS (cluster intelligence + creative control — iterative; 20c-1 in-progress; 20c-4/5/6 deferred; Wave 2B closure stories including `20c-15` done). Epics 22, 24 IN-PROGRESS (`22-2` done; `22-3`/`22-4` and Epic 24 stories queued). Epics 15-18 BACKLOG (learning, autonomy, research, assets).

## Purpose

Build a persistent collaborative intelligence infrastructure for systematically scaling creative expertise in online course content production. A custom master orchestrator agent (created via `bmad-agent-builder`) provides a conversational interface ("general contractor" experience) coordinating specialist agents that manipulate professional media tools through skills backed by Python scripts for API calls, while systematically capturing creative decision-making patterns in BMad memory sidecars for iterative refinement and reuse.

## Critical Implementation Model

**Agents are skill directories** created through `bmad-agent-builder` six-phase conversational discovery process, following BMad SKILL.md standard. They live under `skills/bmad-agent-{name}/` and are discovered through the skills layer.

**Skills are SKILL.md directories** providing tool-specific capabilities with progressive disclosure (`references/`), Python code execution (`scripts/`), and output templates (`assets/`).

**Python infrastructure** provides supporting code for API clients, state management, and file operations - invoked from agent skills when code execution is required.

**Cursor plugin packaging** via `.cursor-plugin/plugin.json` enables native IDE integration with auto-discovery of skills, rules, commands, hooks, and MCP servers.

**BMad memory sidecars** provide persistent agent learning through `_bmad/memory/{skillName}-sidecar/` with index.md (context), patterns.md (learned preferences), chronology.md (history), and access-boundaries.md (scope control).

## Key Decisions From Planning

### Agent Architecture (Confirmed)
- **Custom Master Orchestrator agent** (.md file created via `bmad-agent-builder`) as single conversational point of contact
- **Custom specialist agents** (.md files created via `bmad-agent-builder`) for tool mastery (Gamma, ElevenLabs, Canvas, etc.)
- **Custom skills** (SKILL.md + references/ + scripts/) for tool expertise, parameter intelligence, coordination
- **Reuse existing BMad agents** for writing, editing, review, documentation
- **Python infrastructure** in scripts/ for API clients, state management, file operations

### Cursor Plugin Architecture (Confirmed)
- `.cursor-plugin/plugin.json` manifest with auto-discovery of skills/, rules/
- `.mcp.json` for tool server definitions bundled in plugin
- `hooks/hooks.json` for event-driven automation (sessionStart → pre-flight, afterFileEdit → quality, sessionEnd → reporting)
- `rules/*.mdc` for persistent agent behavior guidance
- `commands/*.md` for agent-executable actions

### BMad Memory System (Confirmed)
- Agent memory sidecars at `_bmad/memory/{skillName}-sidecar/`
- `index.md` for essential context loaded on activation
- `patterns.md` for systematic expertise crystallization (append-only, periodically condensed)
- `chronology.md` for session and production run history
- `access-boundaries.md` for agent scope control (read/write/deny zones)

### Operational Model (Confirmed)
- **Run presets**: `explore`, `draft`, `production`, `regulated` with parameter overrides
- **Asset-lesson pairing invariant**: every educational artifact paired with instructional context
- **Tool parameter mastery**: Specialty agents master complete API parameter spaces, preferences stored in style guide YAML
- **Exemplar-driven development**: Each specialist agent proves competence by reproducing real exemplar artifacts programmatically via API/MCP, scored against a structured rubric. Exemplars serve as both design aids and acceptance tests. See `resources/exemplars/_shared/woodshed-workflow.md`
- **Woodshed skill**: Shared skill (`skills/woodshed/`) provides study → reproduce → compare → reflect → register workflow with detailed run logging, downloaded artifact retention for every attempt (pass/fail), mandatory reflection between failed attempts, and circuit breaker give-up protocol (3/session, 7 total)
- **Two woodshed modes**: Faithful (exact reproduction proving tool control) must be mastered before Creative (enhanced reproduction proving creative judgment) is unlocked per exemplar
- **Progressive mastery**: L1-L4 single artifacts → L5 multi-artifact sets. L-levels with dot extensions. Levels provisional — agents may propose changes. Regression runs ensure mastered exemplars stay mastered
- **Export and download**: All reproductions must download production-quality artifacts (PNG for production, PDF for review, PPTX for editing, MP3 for audio) — screenshots supplementary only
- **Evaluator design requirements** (from Story 3.1): Guide the tool's intelligence (never suppress), extract and compare actual output (not just process compliance), score on content coverage (not exact match), use cheap quality signals per medium, separate woodshed training from production QA, capture know-how from user checkpoint reviews. See `skills/woodshed/SKILL.md` for full reference
- **Gamma API Mastery module**: Double-dispatch variant generation, literal-visual composite fallback, PNG export normalization with `_materialize_exported_slide_paths`
- **HIL gates**: human checkpoints at every stage with rubrics and signoff tracking
- **Pre-flight checks**: Hook-driven MCP/API connectivity verification + tool documentation scanning
- **Production run reporting**: Comprehensive effectiveness analysis with learning capture in agent memory

### State Management (Confirmed)
- **YAML files**: Course context, style guides, policies (human-readable, git-versioned) in `state/config/`
- **SQLite database**: Runtime coordination state, production run tracking in `state/runtime/`
- **BMad memory sidecars**: Agent learning, expertise patterns, session history in `_bmad/memory/`

### Repository Contract (Confirmed)
```
.cursor-plugin/   # Cursor plugin manifest
skills/           # SKILL.md directories with references/ + scripts/ (auto-discovered)
  woodshed/       # Shared exemplar mastery skill (study, reproduce, compare, regress)
rules/            # .mdc rules files for agent guidance
hooks/            # Event-driven automation triggers
commands/         # Agent-executable command files
state/            # YAML configs + SQLite runtime
_bmad/memory/     # Agent memory sidecars for persistent learning
scripts/          # Shared Python infrastructure (API clients, utilities)
tests/            # Unit + integration tests
docs/             # Architecture + agent guides + troubleshooting
resources/
  exemplars/      # Per-tool exemplar libraries with _catalog.yaml, briefs, source, reproductions
    _shared/      # Comparison rubric template, woodshed workflow protocol
    gamma/        # Gamma exemplars (slides/presentations)
    elevenlabs/   # ElevenLabs exemplars (audio/voiceover)
    canvas/       # Canvas exemplars (LMS deployment)
    qualtrics/    # Qualtrics exemplars (surveys/assessments)
    canva/        # Canva exemplars (visual design)
  style-bible/    # Authoritative brand reference
  tool-inventory/ # Tool access matrix
```

## Tool Universe (Researched March 26, 2026)

17 tools classified by programmatic access. Full details in `resources/tool-inventory/tool-access-matrix.md`.

| Tier | Tools | Access |
|------|-------|--------|
| **Tier 1: API + MCP** | Gamma, ElevenLabs, Canvas LMS, Qualtrics, Canva, Notion | Platform capability: REST API and published MCP server |
| **Tier 2: API Only** | Botpress, Wondercraft, Kling, Panopto | REST API, no MCP server |
| **Tier 3: Limited API** | Descript, Midjourney, CapCut | Early access / third-party only |
| **Tier 4: Manual Only** | Vyond, CourseArc, Articulate (Storyline/Rise) | No usable programmatic access for this repo setup |
| **Local FS** | Box Drive | Local filesystem via desktop sync client, no API needed |

- **Live Cursor-verified MCP servers** in `.mcp.json` / `.cursor/mcp.json`: Gamma, Canvas LMS
- **API-verified but MCP-deferred platforms**: ElevenLabs, Qualtrics
- **Documented but currently deferred MCPs**: ElevenLabs (Cursor tool-name filtering), Canva (OAuth redirect rejection), Qualtrics (GitHub-only build step), Fetch (no usable surfaced tools in this setup), Brave Search (not enabled by default)
- **User-level MCPs** already available: Playwright (browser automation), Ref (doc search/reading)
- **API keys** documented in `docs/admin-guide.md`: Tier 1-3 tools with documentation links; values live in local `.env` only
- **Manual tools** require agent-guided workflows where agents provide specs and users execute in tool UI

## Current State

- [x] Repository scaffolded with directory structure, local `.env` pattern, content standards
- [x] BMad Method installed (BMM, Core, CIS modules)
- [x] **BRAINSTORMING COMPLETED**: 10 comprehensive epics defined
- [x] **PRD COMPLETED**: 70 FRs across 11 capability domains (recast for agent .md approach)
- [x] **ARCHITECTURE COMPLETED**: BMad Agent + Cursor Plugin architecture validated (recast)
- [x] **EPICS RECAST**: All 10 epics updated to reflect bmad-agent-builder creation approach
- [x] **Strategic Decisions**: Party Mode team validated and recast for agent .md patterns
- [x] **STORY CREATION COMPLETED**: 31 stories across 10 epics, 100% FR coverage validated
- [x] **API-FIRST SEQUENCING**: API/MCP clients (Gamma, ElevenLabs, Canvas) built in Epic 1 before agent creation
- [x] **TOOL UNIVERSE AUDIT**: 15 tools researched and classified (Tier 1-4), MCP servers configured, credentials documented for local `.env`
- [x] **EPIC 1 COMPLETE**: All 11 stories implemented, tested with live APIs, validated by Party Mode review team
- [x] **STORY 1.1**: Cursor plugin foundation — plugin.json, .mcp.json, hooks, directory structure
- [x] **STORY 1.2**: Python infrastructure — BaseAPIClient with retry/pagination/binary, utilities, venv
- [x] **STORY 1.3**: State management — SQLite (3 tables), YAML configs (3 files), BMad memory sidecars (5 agents)
- [x] **STORY 1.4**: Pre-flight check skill — SKILL.md + Python runner + doc scanner + 3 reference docs
- [x] **STORIES 1.5-1.11**: Testing framework + 5 full-featured API clients (Gamma, ElevenLabs, Canvas, Qualtrics, Panopto) + Canva MCP config
- [x] **LIVE API VALIDATION**: 117 tests pass against real services (Gamma, ElevenLabs, Canvas, Qualtrics), 3 skipped (Panopto — no creds)
- [x] **FR EXPANSION (Party Mode)**: 10 new FRs (FR71-FR80) added for Source Wrangling + Run Mode Management
- [x] **TOOLS EXPANSION**: Notion (API + MCP, source wrangling) and Box Drive (local FS) added to tool universe (17 tools total)
- [x] **SOURCE WRANGLER**: New architectural component for pulling reference materials from Notion/Box into production context; agent vs. skill design decision deferred to story creation
- [x] **AD-HOC MODE**: Binary ad-hoc/default mode switch for Master Orchestrator; ad-hoc routes assets to scratch/staging, suppresses state tracking; QA always runs; future per-level modality matrix deferred
- [x] **STORY 2.1 (Marcus Orchestrator)**: Agent built via bmad-agent-builder (6-phase discovery with Party Mode coaching), quality scan passed (0 critical), 12 interaction test scenarios passed, Party Mode team validation complete. 13 files: SKILL.md + 8 references + 2 scripts + 2 test files. Memory sidecar active with 4 files. First production plan staged (C1-M1-P2S1-VID-001).
- [x] **EPIC 2 COMPLETE**: Stories 2.2–2.6 all done. Production-coordination skill (4 scripts, 4 refs, 40 tests). Marcus references updated for workflow management, delegation, parameter intelligence, pre-flight, and mode management.
- [x] **EXEMPLAR-DRIVEN DEVELOPMENT**: Woodshed skill created (`skills/woodshed/`), exemplar library scaffolded (`resources/exemplars/` per tool), comparison rubric, run logging, reflection protocol, circuit breaker, two-mode woodshed (faithful + creative), doc refresh protocol, and L-level difficulty system all in place. 5 Gamma exemplars provided (L1-L4.2). Smoke test validated: Gamma API produces single-card output, PDF export/download works (205KB), 5 credits/card. GammaClient needs parameter name updates (inputText, textMode, exportAs). Epic 3 stories updated with exemplar reproduction as acceptance criteria.
- [x] **STORY 3.3.1 (Composition Architecture Harmonization + Gary Deck)**: DONE — Party Mode composition decisions implemented: segment manifest as Irene artifact, two-pass Irene model, Irene/Quinn-R/Kira/Marcus/Gary all updated, architecture.md updated with pipeline graph, tool inventory updated (Descript as sole composition platform), Gary deck mode + theme/template preview (TP capability), gary_slide_output return field, GammaClient.list_themes() live-tested (10 themes). Epic 3 re-sequenced to 11 stories: Compositor added as 3.5, Canvas→3.6, Qualtrics→3.7, Canva→3.8, Source Wrangler→3.9, Tech Spec Wrangler→3.10.
- [x] **PROMPT 7 DISPATCH HARDENING (2026-03-30)**: Canonical `gamma_operations.py` generate path now enforces fail-fast for metadata-only slide payloads, supports explicit merge of `gary-fidelity-slides.json` + `gary-slide-content.json`, and keeps placeholder content behind debug-only override (`--allow-placeholder-content`). Regression tests added to prevent recurrence of intent-placeholder slide outputs while preserving parameter, theme-handshake, and export reliability gains.
- [x] **Epic 2A: Fidelity Assurance & APP Intelligence Infrastructure** (9/9 stories DONE — Vera agent covering G0-G5, sensory bridges, perception protocol, source_ref resolver, cumulative drift tracking, fidelity-control vocabulary, maturity audit skill. GOLD document: `_bmad-output/brainstorming/party-mode-fidelity-assurance-architecture.md`)
- [x] Epic 3: Core Tool Specialist Agents & Mastery Skills (11/11 stories DONE — Stories 3.1-3.11 complete, including Story 3.8 Canva specialist.)

## APP Design Principles & Fidelity Architecture (Added 2026-03-28)

See `_bmad-output/brainstorming/party-mode-fidelity-assurance-architecture.md` (GOLD document) for full analysis.

- **Agentic Production Platform (APP):** Formal naming — the IDE is the runtime environment for a network of specialized agents. The platform gets smarter over time as LLMs improve and agent memory accumulates.
- **Three-Layer Intelligence Model:** L1 deterministic contracts (invariant standards), L2 agentic evaluation (evolves with LLM capability), L3 learning memory (compound improvement via sidecars). Applies to every APP capability.
- **Hourglass Model:** Wide cognitive top (synthesis) → narrow deterministic neck (schema/parameter binding) → wide cognitive bottom (creative execution). Intelligence must not enforce constraints that can be deterministic.
- **Leaky Neck Diagnostic:** Any point where agentic judgment enforces a deterministic constraint is a design defect.
- **Sensory Horizon:** Agents cannot verify what they cannot perceive. Sensory bridges (image, audio, PDF, video) with mandatory confirmation protocol.
- **Fidelity Assessor:** New forensic agent distinct from Quinn-R. Produces Fidelity Trace Reports (Omissions/Inventions/Alterations). Circuit breaker on failure. Runs before quality review.
- **Provenance Protocol:** Mandatory `source_ref` fields in all artifact schemas for traceable provenance chains.
- **Current maturity:** Upgraded from Level 0 — Vera covers G0-G5 with 30 L1 criteria, perception bridges for all modalities, cumulative drift tracking, source_ref resolver, fidelity-control vocabulary enforcement in merge_parameters(). G6 (composition) remains future.
- **Epic 2A** COMPLETE (9/9 stories). Story 3.11 mixed-fidelity system COMPLETE with `execute_generation()` production entry point.

## Roadmap Rebaseline (2026-03-28)

Party Mode consensus + parallel GPT-5.4 architectural review identified significant overlap between completed Epics 1-3/2A work and downstream epic scope. Rebaseline applied:

- **Epic 4A** (Agent Governance, Quality Optimization & APP Observability): **6 stories** — run baton, lane matrix, envelope governance, agent QA gate, perception caching + observability (**`run_mode` tagging; ad-hoc excluded from course-progress metrics**), **ad-hoc ledger & learning enforcement (4A-6, FR91)**. Must complete before Epic 4. FRs FR81–FR91 on PRD.
- **Epic 4 updated**: Dependency on 4A. Stories 4.2 (Quality Gates) and 4.4 (Reporting) updated to assume governance layer + Vera fidelity checks.
- **Epic 5 trimmed**: Story 5.2 (Assembly Coordination) dropped — compositor skill delivers this. Story 5.3 (Style Orchestration) merged into governance. Story 5.4 edited — Panopto and Kling already done. Story 5.1 and 5.4 are now complete.
- **Epic 6 trimmed**: Story 6.2 (Enhanced Canvas) merged into Story 3.6 (Canvas Specialist). Story 6.1 completed.
- **Epics 7, 8, 9 collapsed** into Epic G (Governance Synthesis & Intelligence Optimization): **3 stories** — platform allocation (G.1), tool/doc synthesis (G.2), **APP session readiness & health monitoring** (G.3, 2026-03-30: SQLite/`state`/imports + report; composes with pre-flight-check). Epic G is now complete.
- **2026-04-03:** `scripts.utilities.run_constants` loads frozen **`run-constants.yaml`** per bundle; wired into `app_session_readiness --bundle-dir` and `validate-source-bundle-confidence` when the file exists (contract v1.2).
- **2026-04-03:** Prompt 3 hardening pass added `scripts.utilities.validate_source_bundle_confidence` as stable CLI wrapper, normalized validator parsing for heading/ingestion format variants, and centralized hyphenated skill loading in `scripts.utilities.skill_module_loader`.
- **Epic 10**: Predictive optimization requires Epic 4 + Epic G telemetry. Story 10.1 is now complete.

**Net: 11 epics → 9 epics, 46 stories → 40 stories** (historical rebaseline); **+Story 4A-6 → 41 stories** (2026-03-29); **+Story G.3 → 42 stories** (2026-03-30). Architecture updated with governance section.

## Composition Architecture (Added 2026-03-27)

See `_bmad-output/brainstorming/party-mode-composition-architecture.md` for full decision record.

- **Silent Video + Smart Audio:** Kling always `sound-off`. ElevenLabs owns all audio (narration, SFX, music).
- **Segment manifest:** YAML file produced by Irene Pass 2. Single source of truth. All downstream agents read/write.
- **G4 anti-drift rule:** The G4 fidelity contract references both the narration script template and the segment manifest template so Pass 2 changes cannot drift out of validation coverage.
- **Narration-paced video:** ElevenLabs generates first; narration_duration becomes clip duration target for Kira.
- **Descript:** Sole composition platform (manual-tool pattern). Compositor skill (Story 3.5) generates Descript Assembly Guide and can **`sync-visuals`** to copy Gate-approved stills into the assembly bundle (`visuals/`) next to audio, captions, and summaries.
- **Four HIL gates:** Lesson plan → slides → script+manifest → final video.
- **Quinn-R two-pass:** Pre-composition (asset quality) + post-composition (final export).
- **Irene two-pass:** Pass 1 (lesson plan + slide brief before Gary); Pass 2 (narration script + segment manifest after Gary + HIL Gate 2).
- **2026-04-03 anti-drift hardening:** Prompt 6B now requires literal-visual operator packet + readiness confirmation before Gary dispatch side effects; Storyboard A (post-Gary) and Storyboard B (post-Irene Pass 2) are explicit approval checkpoints before advancing to subsequent pipeline spend.
- **2026-04-05 literal-visual rendering policy:** literal-visual slides are enforced as full-slide image-only at dispatch input. Supporting prose is moved to Irene Pass 2 narration/script, and Gate 2 preflight validation fails on non-URL literal-visual payload content.
- **2026-04-05 literal-visual reliability fix:** Anti-fade prompt ("full opacity, not as background, not faded") + initial attempt plus one retry (`_MAX_TEMPLATE_RETRIES = 2`) + composite fallback (preintegration PNG or URL download). Gamma classifies images as accent/background by content — not API-controllable. `visual_fill_validator` enhanced with variance-based content detection (`content_stddev`). Provenance tracked via `literal_visual_source` field.
- **2026-04-08 motion review contract:** Storyboard B shows both the approved still and the approved motion clip for motion segments. Downstream contracts preserve the still in `visual_file` and the playback asset in `motion_asset_path`; motion-first narration is the expected design when `visual_mode: video`.
- **2026-04-10 Storyboard B HTML redesign:** Summary banner uses 3-column grid (was 2-col with overflow) and is wrapped in a collapsible `<details>` element (open by default). Motion-enabled cards use stacked layout (slide + video in column 1, script spanning column 2) to eliminate dead space. Static cards unchanged. Regression test in `test_generate_storyboard.py`.
- **Gary deck enhancement:** Deck mode (numCards by content type), theme/template preview (TP capability), gary_slide_output return field.
- **Seven instructional use cases:** Narrated deck, dialogue, walkthrough, case study, assessment prompt, concept explainer, module bumper — all one pipeline.

## Key Files

- `_bmad-output/planning-artifacts/prd.md` - Complete PRD (70 FRs, recast)
- `_bmad-output/planning-artifacts/architecture.md` - Complete architecture (recast)
- `_bmad-output/planning-artifacts/epics.md` - Epic breakdown with requirements (recast)
- `_bmad-output/strategic-decisions-collaborative-intelligence.md` - Strategic decisions
- `_bmad-output/brainstorming/brainstorming-session-20260325-150802.md` - Brainstorming session
- `_bmad-output/brainstorming/party-mode-coaching-marcus-orchestrator.md` - Marcus coaching doc
- `_bmad-output/brainstorming/party-mode-composition-architecture.md` - **Composition architecture decisions (2026-03-27)**
- `_bmad-output/implementation-artifacts/3-3-1-composition-architecture-harmonization.md` - **Story 3.3.1 (DONE)**
- `skills/bmad-agent-marcus/SKILL.md` - **Marcus orchestrator agent (Story 2.1 DONE)**
- `skills/reports/bmad-agent-marcus/quality-scan/2026-03-26_152243/quality-report.md` - Marcus quality scan
- `tests/agents/bmad-agent-marcus/interaction-test-guide.md` - Marcus interaction tests
- `resources/tool-inventory/tool-access-matrix.md` - **Tool universe access matrix (17 tools)**
- `scripts/heartbeat_check.mjs` - Baseline read-only API heartbeat across configured tools
- `scripts/smoke_elevenlabs.mjs` - Focused ElevenLabs API smoke check
- `scripts/smoke_qualtrics.mjs` - Focused Qualtrics API smoke check
- `skills/woodshed/SKILL.md` - **Shared exemplar mastery skill (study, reproduce, compare, regress)**
- `resources/exemplars/_shared/woodshed-workflow.md` - **Complete woodshed workflow protocol (logging, reflection, circuit breaker)**
- `resources/exemplars/_shared/comparison-rubric-template.md` - **Rubric for scoring exemplar reproductions**
- `resources/exemplars/gamma/_catalog.yaml` - **Gamma exemplar registry**
- `docs/agent-environment.md` - Agent/MCP guidance  
- `docs/operations-context.md` - Compact operations-only context for production sessions
- `docs/workflow/human-in-the-loop.md` - HIL procedure
- `.cursor/rules/course-content-agents.mdc` - Cursor agent rules
