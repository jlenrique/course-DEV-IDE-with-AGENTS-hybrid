# 7c.18b Ready For Review

Story: `_bmad-output/implementation-artifacts/migration-7c-18b-section-07c-storyboard-build-html-reviewer-surface.md`
Status: `review`

## Implemented

- Added standalone §07C storyboard build HIL surface with `surface_id="section_07c_storyboard_build"`, no `alias_of`, mandatory `cli`, optional `http` + `mcp-stdio`.
- Added `Section07COperatorVerdict` and LF/no-BOM schema with `submit / edit / discard` verb-payload validation and `.html`/sha256 checks.
- Added deterministic stdlib-only HTML reviewer emitter in `app/gates/section_07c/storyboard_html_emitter.py`.
- Added DSL-registration, 3-transport parity, HTML byte-determinism, and schema shape-pin tests.
- Appended `app.gates.section_07c` to C6 and updated structural known-target allowlists.

## Evidence

- Focused Wave-5 new tests: `35 passed`.
- Structural target-list regression: `10 passed`.
- Broad regression: `45 failed, 4412 passed, 27 skipped, 2 xfailed`; remaining failures are inherited baseline classes, and the Wave-5 C6 structural failures are gone.
- `lint-imports`: `12 kept, 0 broken`.
- Ruff: clean.
- Sandbox-AC: PASS.
- Class conformance: PASS at 19 parity contract files.

## Review Notes

The HTML emitter uses `html.escape`, writes UTF-8 LF-only bytes with no BOM, and is sha256-stable across identical invocations. `display_storyboard_targets` accepts a dict or JSON path and is shaped around Gary slide-content plus plan-unit metadata.
