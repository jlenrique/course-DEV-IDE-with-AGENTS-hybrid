# 7c.17b Ready for Review

Story: `migration-7c-17b-marcus-writers-theme-resolution-outbound-envelope`
Status: `review`
Implemented by: Codex GPT-5
Date: 2026-05-06

## Scope Completed

- Authored `theme_resolution.py` and `outbound_envelope.py`.
- Each writer declares FR-7c-54 sanctum alignment at module import with `sanctum_path="_bmad/memory/bmad-agent-marcus/"` and default `alignment_kind="bmb-pattern"`.
- Implemented deterministic YAML envelope emission with PyYAML and LF-only output.
- Implemented `pre_dispatch_package_gary_md` deterministic markdown aggregation ordered by `writer_id`.
- Generated LF-only, no-BOM schemas:
  - `_bmad/memory/bmad-agent-marcus/schemas/gary-theme-resolution.schema.json`
  - `_bmad/memory/bmad-agent-marcus/schemas/pre-dispatch-package.schema.json`
- Added focused shape-pin tests, envelope aggregation tests, complement sanctum-registry assertions, and 5-entry manifest-emission smoke coverage.

## Schema Hash Pins

- `GaryThemeResolution`: `5b3b24b1c84197de2f819adb636b968153cad568a803691f33645cd41e0c9c2e`
- `GaryOutboundEnvelope`: `382985141ffe8e6572f17415d41d8eda33c6c49e6942d0e5a6d623ea9c1b4056`

## Verification

- `pytest tests/marcus/orchestrator/writers/ -p no:randomly -q --tb=short`: `27 passed`
- `pytest tests/parity/test_sanctum_alignment_dsl.py -p no:randomly -q --tb=short`: `6 passed`
- `pytest tests/gates/section_02a/ -p no:randomly -q --tb=short`: `12 passed`
- Wave-3 gate sweep: `79 passed`
- `pytest tests/marcus/ -p no:randomly -q --tb=short`: `27 passed`
- `pytest --smoke -p no:randomly -q --tb=short`: `181 passed, 18 skipped`
- `validate_parity_test_class_conformance.py tests/parity/`: `PASS: 19`
- `lint-imports`: `12 kept, 0 broken`
- `validate_migration_story_sandbox_acs.py` for 7c.17b: PASS
- `ruff check app/marcus/orchestrator/writers/ tests/marcus/orchestrator/writers/`: clean

## Broad Regression

Full broad regression completed: `45 failed, 4377 passed, 27 skipped, 2 xfailed`.

No failures are in the new 7c.17b writer modules, schemas, or tests. The failure set is inherited repo drift/baseline noise already represented by prior Wave-3 notes, including run HUD drift, pipeline manifest/version drift, scaffold roster drift, replay pack hash drift, sanctum fixture state, Windows async psycopg event-loop behavior, and the pre-existing `test_no_unauthorized_callers` scanner-staleness failure.

## Coordination Notes

- Shared `app/marcus/orchestrator/writers/__init__.py` and `tests/marcus/orchestrator/writers/__init__.py` created once for the 7c.17a/7c.17b batch.
- Shared `_bmad/memory/bmad-agent-marcus/schemas/` directory created once; 7c.17a and 7c.17b schema filenames are distinct.
- Manifest smoke imports all five writer modules and verifies deterministic writer-id ordering from `emit_sanctum_alignment_manifest`.
- No `pyproject.toml`, parity DSL implementation, existing Marcus orchestrator modules, 7c.17a writer files after creation, closed Wave-3 gate packages, or Marcus BMB files were modified.
