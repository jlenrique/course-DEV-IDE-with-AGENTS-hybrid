# Slab 1 Story 1.3 reference shape for per-specialist model_config.yaml.
# Each Slab-2 specialist subpackage carries its own model_config.yaml
# conforming to app.models.specialist_model_config.SpecialistModelConfig.
specialist_id: "_scaffold"
default_model: "gpt-5.4"
per_node_overrides:
  # Optional per-node-name model_id overrides; node names are specialist-internal.
  # Empty by default; real specialists populate per their internal node graph.
  {}
temperature_default: 0.0
