"""Specialist package — Slab 2+ specialist migrations land here.

Slab 1 ships the `_scaffold/model_config.yaml` reference shape only; each
Slab 2 specialist creates its own subpackage with a `model_config.yaml`
conforming to `app.models.specialist_model_config.SpecialistModelConfig`.
"""
