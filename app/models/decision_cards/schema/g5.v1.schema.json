{
  "$defs": {
    "CacheState": {
      "description": "Closed cache-state vocabulary for gate-family decision cards.",
      "enum": [
        "healthy",
        "mixed",
        "cold"
      ],
      "title": "CacheState",
      "type": "string"
    },
    "DecisionCardMeta": {
      "additionalProperties": false,
      "description": "Shared metadata attached to every Slab 7c gate-family DecisionCard.",
      "properties": {
        "affected_nodes": {
          "description": "Manifest node ids affected by this decision card.",
          "items": {
            "type": "string"
          },
          "title": "Affected Nodes",
          "type": "array"
        },
        "cache_state": {
          "$ref": "#/$defs/CacheState",
          "description": "Closed cache-state tier."
        },
        "override_trail": {
          "description": "Operator override events already applied to this run.",
          "items": {
            "$ref": "#/$defs/OverrideEvent"
          },
          "title": "Override Trail",
          "type": "array"
        }
      },
      "required": [
        "cache_state"
      ],
      "title": "DecisionCardMeta",
      "type": "object"
    },
    "OverrideEvent": {
      "additionalProperties": false,
      "description": "One operator-approved override that affects cache warmth and routing context.",
      "properties": {
        "applied_at": {
          "description": "Timezone-aware timestamp for when the override applied.",
          "format": "date-time",
          "title": "Applied At",
          "type": "string"
        },
        "confirm_token": {
          "description": "Confirmation token bound to the override submission surface.",
          "minLength": 1,
          "title": "Confirm Token",
          "type": "string"
        },
        "event_id": {
          "description": "UUID4 identity for this override event.",
          "format": "uuid",
          "title": "Event Id",
          "type": "string"
        },
        "new_value": {
          "additionalProperties": true,
          "description": "Canonical JSON-like view of the new setting.",
          "title": "New Value",
          "type": "object"
        },
        "node_id": {
          "description": "Manifest node id that the override touches.",
          "minLength": 1,
          "title": "Node Id",
          "type": "string"
        },
        "operator_id": {
          "description": "Operator identity that confirmed the override.",
          "minLength": 1,
          "title": "Operator Id",
          "type": "string"
        },
        "previous_value": {
          "additionalProperties": true,
          "description": "Canonical JSON-like view of the previous setting.",
          "title": "Previous Value",
          "type": "object"
        }
      },
      "required": [
        "event_id",
        "node_id",
        "operator_id",
        "confirm_token"
      ],
      "title": "OverrideEvent",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Final-operator-handoff DecisionCard for the G5 gate family.",
  "properties": {
    "bundle_run_id": {
      "description": "UUID4 identity for the run that produced the handoff bundle.",
      "format": "uuid4",
      "title": "Bundle Run Id",
      "type": "string"
    },
    "card_id": {
      "description": "UUID4 identity for this DecisionCard instance.",
      "format": "uuid4",
      "title": "Card Id",
      "type": "string"
    },
    "created_at": {
      "description": "Timezone-aware creation timestamp for the emitted DecisionCard.",
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "decision_card_digest": {
      "description": "Lowercase sha256 digest of the canonical decision card.",
      "title": "Decision Card Digest",
      "type": "string"
    },
    "gate_focus": {
      "const": "final_operator_handoff",
      "default": "final_operator_handoff",
      "description": "Closed marker for the G5 gate family.",
      "title": "Gate Focus",
      "type": "string"
    },
    "gate_id": {
      "const": "G5",
      "default": "G5",
      "description": "Discriminator for the G5 final-operator-handoff gate.",
      "title": "Gate Id",
      "type": "string"
    },
    "handoff_artifact_paths": {
      "description": "Artifact paths included in the final operator handoff.",
      "items": {
        "format": "path",
        "type": "string"
      },
      "title": "Handoff Artifact Paths",
      "type": "array"
    },
    "handoff_summary": {
      "description": "Operator-facing summary of the final handoff.",
      "minLength": 1,
      "title": "Handoff Summary",
      "type": "string"
    },
    "meta": {
      "$ref": "#/$defs/DecisionCardMeta"
    },
    "schema_version": {
      "const": "v1",
      "default": "v1",
      "description": "DecisionCard schema version.",
      "title": "Schema Version",
      "type": "string"
    },
    "trial_id": {
      "description": "UUID4 identity for the enclosing trial run.",
      "format": "uuid4",
      "title": "Trial Id",
      "type": "string"
    },
    "verb": {
      "description": "Closed decision verb set: approve | edit | reject.",
      "enum": [
        "approve",
        "edit",
        "reject"
      ],
      "title": "Verb",
      "type": "string"
    }
  },
  "required": [
    "decision_card_digest",
    "meta",
    "card_id",
    "trial_id",
    "bundle_run_id",
    "handoff_artifact_paths",
    "handoff_summary",
    "verb"
  ],
  "title": "G5Card",
  "type": "object"
}
