GOAL (clustering + follow-along trial, 2026-06-24) — Reproduce the successful full production runs to Descript (like the 2026-06-24 per-slide A/B mirror runs 7d530d0a/6cb8eafd), now WITH: (1) clustering ACTIVE so dense slides become coherent sub-slide clusters → tight VO-to-screen coupling; (2) per-slide A/B variant selection completing FULLY to Descript each run; (3) NO ghost numbers in VO (figure-citation gate holds live). Autonomous via BMAD workflows + party-mode gates + NEW CYCLE Codex dev (T1–T10) + Claude T11. Live fix-on-the-fly: trial → review → repair → re-trial until all three hold TOGETHER.

CONTEXT: Branch fidelity-perception-arc-2026-06-19. Clustering substrate (Epics 19-21/20c/23: cluster head+interstitials, narrative_arc, Epic-23 bridges, density controls, slide_count_runtime_estimator) is BUILT but DORMANT — the LLM-first Pass-1 rebuild dropped cluster emission (emits flat one-slide-per-unit plan_units); downstream cluster fields survive in code, unexercised since April. Reading-path LLM-first + per-slide A/B + presenter voice + figure-citation gate (5cf0684) all shipped. Authority: _bmad-output/planning-artifacts/forward-development-sequence-2026-06-24.md + memory chunking-via-clustering-followalong.

ARC (stop where reliability runs out; honest partial > forced green):
1. PHASE-1 CLUSTERING RE-ACTIVATION — party green-light scope FIRST. NEW CYCLE: re-wire cluster emission into LLM-first Pass-1 (cluster_id/role/position/narrative_arc in plan_units + cluster-decision/density logic). RE-VERIFY downstream end-to-end (Gary→segment-manifest→Epic-23 bridges→timing) on a real smoke — bit-rot is the main unknown; verify EARLY, re-scope if the chain needs repair not just reconnection. Adds: per-slide chunk directive (special_treatment_directives); keep-dense marker so Irene keeps synthesis/big-picture slides DENSE (never chunk a gestalt); inter-sub-slide transition timing.
2. INTEGRATION (clustering × per-slide A/B): chunking expands the slide set, so the chooser + variant dispatch + figure-gate must address post-chunk sub-slides coherently (each addressable; variant pick per sub-slide; bounded render volume). Resolve in the trial/repair loop; surface to operator only if it needs a product decision.
3. PHASE-2 LIVE A/B PROOF RUNS to Descript — real trials, clustering ON + per-slide A/B via real Playwright chooser clicks → completing to Descript segment-manifest + ElevenLabs audio, 0 errors. VERIFY ON REAL ARTIFACTS (not code-intent): clusters in manifest + bridges in narration + VO-screen tightness + NO figure-contradiction. Fix each failure forward → re-trial.
4. PROVE TWICE — two successful A/B runs fully to Descript (an A/B mix + a mirror), each error-free, each with clustering + tight VO + no ghost numbers. The prior mirror-run bar, raised.

DISCIPLINE: no mocks — live gpt-5.5/Gamma/ElevenLabs/Descript; every reading-path number carries (subject, substrate=fresh|stale@date) + resubstitution stamp (consumed-14; NO generalization claim); first-run-stands; no retry-to-green; verify clusters/figures on REAL run artifacts.

GOVERNANCE: party green-light precedes substrate dev; bmad-code-review before any done; Codex T1-T10 / Claude T11; cleanup-arc Claude-direct; Quinn→John chain on impasse before escalating to me. Each substrate story: green-light → Codex → T11 (battery + code-review + party CLOSE + commit + push).

HARD FENCES: additive non-regression (reading-path / per-slide-variant / figure-gate / pipeline stay green); G5 figure detector UNTOUCHED; never leave the 4-file lockstep / manifest half-modified; push at safety checkpoints + completion; no force-push, no --no-verify.

WRAP-UP (mandatory at stop): update SESSION-HANDOFF + next-session-start-here + deferred-inventory + STATE-OF-THE-APP (honestly, numbers tagged); resume banner; commit + push.

TERMINAL STATES best→floor: (a) TWO successful A/B-to-Descript runs with clustering + tight VO + no ghost numbers; (b) ONE such run; (c) clustering re-wired + downstream re-verified green on a smoke; (d) clustering Pass-1 emission re-wired + T11-closed.

STOP & ASK ONLY ON: true impasse after Quinn→John; a governance change (gate-mode / pack-version Tier-2+); the clustering×variant integration needing a product decision only you can make; or a blocker genuinely needing me.
