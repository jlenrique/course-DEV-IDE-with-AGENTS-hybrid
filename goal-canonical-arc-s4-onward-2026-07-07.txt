GOAL — Canonical Production Conversation arc, S4 onward (~6h autonomous)

Branch dev/workbook-2026-07-06 @ 13792617 (pushed, 0 ahead). Class S. S0·S1·S2·S3
CLOSED + live-proven; S-flip parity clock at tick 1. Strays to EXCLUDE from every
commit: _bmad-output/artifacts/workbooks-test/, the 3 claude-shadow-monitor-* ledgers,
runs/a18c2a86.../ .

TASK 1 — START HERE. Spawn the BMAD custom agent Marcus (read skills/bmad-agent-marcus/
SKILL.md; follow its activation sequence, sanctum batch under _bmad/memory/bmad-agent-
marcus/). Marcus orchestrates; specialists are delegated through him. Then author the
S4 spec (canonical-arc-s4-fail-loud-flip.md). S4 = FAIL-LOUD flip: Gary's DEFAULT_VARIANT_
PAIR WARN-seed (_act.py:518-528 at 7630d091) -> hard failure; parity WARN -> ERROR. The
authority S-flip stays DEFERRED (clock at tick 1, not this story). The spec MUST satisfy
the four SOP-011 obligations at the monitor-ledger tail: (a) explicitly weigh the E4
defer — does the S-flip clock require full THREE-way attestation or does two-way suffice;
(b) scope == the party-record §7 S4 row, citing the committed v1 receipt schema (13-key
app/styleguide/parity.py) as input contract; (c) F-705 WARN-seed ownership formally
transfers to S4 WITH a byte-diff witness planned; (d) the §10 deferred filings (retry-
taxonomy, runs-root split) stay FILED, not silently absorbed.

TASK 2 — Dev + close S4 through the full gate rhythm (held 3x, do not shortcut):
SOP-011 pre-dispatch poll -> fresh dev agent RED-first -> SOP-012 + 3-lane bmad-code-
review -> RED-first remediation -> orchestrator re-verify -> LIVE witness (PASS required)
-> commit (strays excluded) -> SOP close poll.

TASK 3 — Author + dev + close the pre-S5 g0-battery story (party record §8 BINDING
precondition for S5): green the 13-red g0_enrichment battery — never-committed corpus
fixture studio-smoke-min, stale ACTIVE_TERMINAL_GATES pin, the b59679ce "T11 owed" tail.
Same gate rhythm. This unblocks S5.

TASK 4 — If time remains in the ~6h window, open the S5 (G0 canonical) spec and take it
as far as the gate rhythm allows; do not rush a live close. Otherwise hand off cleanly at
a committed, pushed, green checkpoint with next-session-start-here + SESSION-HANDOFF
updated.

WORKING RULES (binding):
- BMAD-first. Use BMAD workflows for all story/spec work (bmad-quick-dev / bmad-create-
  story as routed) and fully-spawned bmad-party-mode for green-lighting, re-scope, and
  close/concurrence rounds — never an improvised single persona for those gates. Substrate
  dev goes to a FRESH dev agent, RED-first; Claude reviews + live-proves + commits (never
  a fork, never Claude-direct for substrate).
- LIVE TESTING IS INDISPENSABLE. Every component is live-tested AS IT IS AUTHORED OR
  EDITED — real APIs, no mocks, real dispatches. A story NEVER closes on offline-green;
  the LIVE witness must PASS (skip never counts, first-run-stands, no retry-to-green).
  E2E is confirmation, not first contact.
- REVIEW THE CODEX SHADOW-MONITOR at every major interval. Read the latest ledger entry
  (canonical-arc-claude-shadow-monitor-2026-07-06.md) BEFORE crossing any gate; relay
  every poll verbatim; apply its material catches. The operator's external monitor ledger
  is untracked — read, do not stage it.
- Autonomous party consensus: escalate only if truly blocked (Quinn synth -> John tie-
  break -> operator only if both fail). Do NOT ask the operator to pick options.
- Baseline-verify pre-existing reds via a throwaway worktree, never git stash.
- KG/ONBOARDING regen is OWED (~2 sessions behind) — flag/handle at WRAPUP.

DONE-SIGNAL. This goal is complete ONLY when a bmad-party-mode team, FULLY SPAWNED for
the purpose, CONCURS that the final task in scope is accomplished and validated (live-
proven, committed, pushed) — corroborated by the shadow-monitor's close poll. Absent that
concurrence, the goal is not done.
